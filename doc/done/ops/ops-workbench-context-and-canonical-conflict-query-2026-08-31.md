# 运营工作台上下文与 Canonical 冲突查询

## 已完成范围

- 平台用户、平台连接、模型、功能开关、存储、账务、审计域进入 `platform` 工作台。
- 成员、任务、规则、客服、事故域进入 `workspace` 工作台。
- 深链和域切换会自动选择正确的请求上下文；上下文替换不额外污染用户导航历史。
- 平台专属背景 hydration 在商家工作台不发送，避免无意义的 403；模型状态仅在商家工作台读取。
- PostgreSQL Canonical 冲突队列与 `products` 联查时对冲突表列使用 `c.` 限定，消除 `id` 歧义导致的 500。
- 品牌树真实空态文案补全为“当前工作区还没有可访问的品牌”。

## 验证证据

- 运营前端 TypeScript/build 通过。
- 运营全页浏览器验收 1/1 通过。
- 运营深链历史验收 1/1 通过。
- 成员生命周期验收 1/1 通过。
- 用户目录桌面、390px、375px 验收 3/3 通过。
- Canonical 冲突队列真实本地 PostgreSQL API 返回 200，空结果以真实空态返回。
- CodeGraph：826 文件、11,701 节点、43,641 边，`pendingRefs=0`，`worktreeMismatch=null`。

## 上线边界

本文件只归档已完成的代码和本地 Compose/桌面验收子能力，不代表整个运营后台已具备生产上线条件。真实 OIDC、生产 PostgreSQL/RLS、多副本、外部平台、托管密钥和正式 ChatGPT Host 证据仍属于 `doc/todo` 的发布门禁。
