services:
  pilot-gateway:
    build:
      context: ../..
      dockerfile: infra/docker/pilot-gateway-https.Dockerfile
    ports:
      - "0.0.0.0:80:8080"
      - "0.0.0.0:443:8443"
    volumes:
      - /opt/merchant-deploy/deploy/certs:/etc/nginx/certs:ro
