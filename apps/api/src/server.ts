import { createServer, type IncomingMessage, type ServerResponse } from 'node:http'
import { readFileSync } from 'node:fs'
import { createHash, createHmac, randomBytes, randomUUID, timingSafeEqual } from 'node:crypto'
import { alertNotificationReadiness, notifyOperationalAlert } from './alert-notifier.js'
import { Pool } from 'pg'
import { createClient } from 'redis'
import { DeleteObjectCommand, GetObjectCommand, HeadObjectCommand, ListObjectsV2Command, PutObjectCommand, S3Client } from '@aws-sdk/client-s3'
import { MerchantService, assetReadiness, imageArchiveReceiptDigest, imageGenerationCandidateUsability, isTrustedCleanAsset, DomainError, type AssetRegistrationResult, type BrandVisualRules, type KnowledgeGenerationContext, type Platform, type PlatformAccount, type PlatformRejection, type Product, type Task } from '../../../packages/application/src/service.js'
import { canonicalBackfillConflictQueueFailure } from '../../../packages/application/src/canonical-backfill-queue.js'
import { defaultRuleCenterSeeds, type RuleHit, type RulePack } from '../../../packages/review/src/rule-center.js'
import { reviewProductImages } from '../../../packages/review/src/review.js'
import { ConnectorMappingPreflightError, ConnectorRuntime, SyncPaginationError, type ConnectorRuntimeMappingPreflightAdapter } from '../../../packages/application/src/connector-runtime.js'
import { allowedModelUsageSettlementDecisions, AssetScanRedriveError, BusinessSnapshotVersionConflictError, COMMERCIAL_PLATFORMS, loadMigrations, MemoryActionLedgerRepository, MemoryAuditCenterRepository, MemoryAuthorizationRepository, MemoryBrandUnitRepository, MemoryCommercialExtensionsRepository, MemoryCommercialRepository, MemoryContextSnapshotRepository, MemoryDataLifecycleRepository, MemoryEntitlementRepository, MemoryGrowthRepository, MemoryMembersRepository, MemoryModelUsageRepository, MemoryObjectOrphanRepository, MemoryOperationsRepository, MemoryOperationalAlertsRepository, MemoryPaymentCallbackNonceRepository, MemoryStorageQuotaRepository, MemorySubscriptionRepository, MemoryUsageRepository, PLATFORM_ASSIGNED_ROLES, PostgresActionLedgerRepository, PostgresAssetScanRedriveRepository, PostgresAuditCenterRepository, PostgresAuthorizationRepository, PostgresBillingRepository, PostgresBrandUnitRepository, PostgresBusinessRepository, PostgresCommercialExtensionsRepository, PostgresCommercialRepository, PostgresContextSnapshotRepository, PostgresDataLifecycleRepository, PostgresEntitlementRepository, PostgresGrowthRepository, PostgresMembersRepository, PostgresModelUsageRepository, PostgresObjectOrphanRepository, PostgresOperationsRepository, PostgresOperationalAlertsRepository, PostgresOpsDataRepository, PostgresOutboxRepository, PostgresPaymentCallbackNonceRepository, PostgresRuleRepository, PostgresStorageQuotaRepository, PostgresSubscriptionRepository, PostgresUsageRepository, MemoryKnowledgeHydrationRepository, PostgresKnowledgeHydrationRepository, MemoryAssetPromotionCleanupRepository, PostgresAssetPromotionCleanupRepository, runMigrations, withWorkspaceTransaction, type ActionKind, type ActionLedgerRepository, type ActionSettlement, type AssetPromotionCleanupBinding, type AssetPromotionCleanupRepository, type AssetPromotionCleanupTask, type AssetScanRedriveRepository, type AuditCenterRepository, type AuthorizationGrant, type AuthorizationRepository, type BillingCycle, type BrandAccessRole, type BusinessEntityType, type CommercialPlatform, type CommercialExtensionsRepository, type ContextSnapshotRepository, type DataDeletionScope, type DataLifecycleRepository, type EntitlementKind, type EntitlementRepository, type GrowthRepository, type MemberRole, type MemberStatus, type MembersRepository, type ModelUsageRepository, type ModelUsageSettlementDecision, type ObjectOrphanRepository, type OperationsRepository, type OperationalAlert, type OperationalAlertsRepository, type PaymentCallbackNonceRepository, type PersistedRuleAudit, type PersistedRuleVersion, type PlatformAssignedRole, type PlatformRoleAssignment, type SqlPool, type StorageQuotaRepository, type SubscriptionRepository, type UsageRepository, type KnowledgeHydrationRepository } from '../../../packages/persistence/src/index.js'
import type { OutboxEvent, OutboxRepository } from '../../../packages/persistence/src/repository.js'
import type { SqlClient } from '../../../packages/persistence/src/repository.js'
import { IdentityLifecycleError, MemoryIdentityLifecycleRepository, PostgresIdentityLifecycleRepository, type IdentityAuthorizationSnapshot, type IdentityLifecycleRepository, type IdentityOperationsDetail } from '../../../packages/persistence/src/identity-lifecycle-repository.js'
import { MemoryReconciliationStatusRepository, PostgresReconciliationStatusRepository, type ReconciliationStatusRepository } from '../../../packages/persistence/src/reconciliation-status-repository.js'
import { MemoryCanonicalBackfillRunRepository, PostgresCanonicalBackfillRunRepository, type CanonicalBackfillRunRepository } from '../../../packages/persistence/src/canonical-backfill-run-repository.js'
import { MemorySupportSlaReportingRepository, PostgresSupportSlaReportingRepository, type SupportSlaReportingRepository } from '../../../packages/persistence/src/support-sla-report-repository.js'
import { CanonicalBackfillConflictRevisionConflictError, MemoryCanonicalBackfillConflictRepository, PostgresCanonicalBackfillConflictRepository, type CanonicalBackfillConflictRepository } from '../../../packages/persistence/src/canonical-backfill-conflict-repository.js'
import { CanonicalBackfillRemediationError, PostgresCanonicalBackfillRemediationRepository } from '../../../packages/persistence/src/canonical-backfill-remediation-repository.js'
import { runCanonicalProductBackfill as executeCanonicalProductBackfill, type CanonicalBackfillResult } from '../../../packages/persistence/src/canonical-product-backfill.js'
import { CampaignLifecycleError, type CampaignBatchRow, type CampaignBatchState, type CampaignItemRow, type CampaignItemState } from '../../../packages/persistence/src/brand-unit-repository.js'
import { contextEnvelopeHash } from '../../../packages/persistence/src/context-snapshot-repository.js'
import { redactAuditEvidence, redactAuditReason } from '../../../packages/persistence/src/audit-center-repository.js'
import type { DataDeletionRequest } from '../../../packages/persistence/src/data-lifecycle-repository.js'
import { hashPkceVerifier, OAuthStateError, OAuthStateStore } from '../../../packages/security/src/oauth.js'
import { RedisOAuthStateStore, type OAuthRedisPort } from '../../../packages/security/src/redis-oauth.js'
import { assetScanReceiptDigest, parseAssetScanReceipt, verifyAssetScanReceiptSignature, type SignedAssetScanReceipt } from '../../../packages/security/src/asset-scan-receipt.js'
import { verifyScannerRequestProof } from '../../../packages/security/src/scanner-request-proof.js'
import { verifyWorkerRequestProof, WORKER_ROLES, type WorkerRequestRole } from '../../../packages/security/src/worker-request-proof.js'
import { ConnectorFailure, createVaultCredentialProviderFromEnv, isProductionCanaryReady, validatePlatformCapabilityEvidence } from '../../../packages/connectors/src/index.js'
import { platformWriteAllowed } from '../../../packages/connectors/src/write-boundary.js'
import { runFencedSinglePublish, PublishCommitStatusUnknownError } from './publish-fenced-orchestrator.js'
import { createContentGeneratorFromEnv, validateContentSchema, type ContentModule, type StaticBrief } from '../../../packages/ai/src/generator.js'
import { createImageGeneratorFromEnv } from '../../../packages/ai/src/image-generator.js'
import { createImageEditGeneratorFromEnv } from '../../../packages/ai/src/image-editor.js'
import { createImageFactsExtractorFromEnv } from '../../../packages/ai/src/image-facts.js'
import { createVideoGeneratorFromEnv, videoDurationSeconds } from '../../../packages/ai/src/video-generator.js'
import { relayUsageReceiptKey, type RelayUsageRecord } from '../../../packages/ai/src/relay-usage.js'
import { createNewApiSelfLogClientFromEnv } from '../../../packages/ai/src/provider-usage-log.js'
import { createRelayPricingClientFromEnv } from '../../../packages/ai/src/relay-pricing.js'
import { evaluatePlatformModelBudgetEstimate, evaluatePlatformModelCostGate, evaluatePlatformModelGate, evaluatePlatformModelRelayGate, evaluatePlatformModelTaskCostLimit, evaluatePlatformModelTaskRequestCost, type PlatformModelKind } from '../../../packages/ai/src/platform-model-gate.js'
import { DocumentParseError, parseDocumentFacts, type ParseErrorContext } from '../../../packages/application/src/document-parser.js'
import { validateProtectedProductIntent, type ProtectedProductIntentValidation } from '../../../packages/application/src/protected-product-intent.js'
import { projectCommercialEntitlement } from '../../../packages/application/src/commercial-entitlement-projection.js'
import { buildCanonicalChainConsistencyReport, canonicalProductReadModeFromFlag, CANONICAL_PRODUCT_READ_MODE_FLAG, resolveCanonicalProductReadScope, type CanonicalChainConsistencyInput, type CanonicalProductReadMode } from '../../../packages/application/src/canonical-product-consistency.js'
import { CampaignDeliveryOrchestratorAdapter, type CampaignDeliveryLifecycleOperation } from '../../../packages/application/src/campaign-delivery-orchestrator.js'
import { CampaignManifestError, type CampaignDeliveryManifestInput } from '../../../packages/application/src/campaign-delivery-manifest.js'
import { LocalObjectStorage, ObjectStorageError, ObjectStoragePartialWriteError, S3CompatibleObjectStorage, withObjectStorageReadRetry, runReconciliationCycle, type CloudObjectTransport, type ObjectStoragePort, type PutQuarantineObjectInput, MemoryReconciliationStatusStore, type ReconciliationReport, type ReconciliationStatusStore, type DurableObjectReference, type ObjectInventoryEntry } from '../../../packages/storage/src/index.js'
import { checkDurableArchiveReference } from '../../../packages/storage/src/archive-lifecycle-contract.js'
import { AUTHZ_POLICY_VERSION, CANONICAL_ROLES, CAPABILITIES, MCP_METHODS, MCP_METHOD_POLICIES, capabilitiesForRoles, canonicalizeRole, evaluateAuthorizationDecision, evaluatePermissionAtoms, getHttpOperationPolicy, getMcpMethodPolicy, resolveCanonicalRoles, ERROR_CODES, isMcpMethod, validateMcpRequest, validateImageGenerationCallbackResult, type ApiEnvelope, type AuthorizationDecision, type AuthorizationDecisionMode, type AuthorizationObligation, type CanonicalRole, type CapabilityId, type HttpOperationPolicy, type McpRequest, type OpsWorkbench, type PermissionAtom } from '../../../packages/contracts/src/index.js'
import { KnowledgeError, KnowledgeModule, type LearningSuggestion, type RuleEntry } from '../../../packages/knowledge/src/index.js'
import { cleanObjectStorageOrphans } from '../../../packages/workers/src/object-orphan-cleaner.js'
import { planSupportSlaScan } from '../../../packages/workers/src/support-sla-scan.js'
import { aggregateScannerHeartbeats, SCANNER_HEARTBEAT_INDEX_KEY, SCANNER_HEARTBEAT_SCHEMA, type ScannerHeartbeat } from '../../../packages/workers/src/scanner-heartbeat.js'
import { parseWorkerAuthorizationSnapshot, type CriticalWorkerOperation, type WorkerAuthorizationSnapshot } from '../../../packages/workers/src/execution-authorization.js'
import { createImageEditCandidate, createOneSentenceGenerationRequest, createVideoGenerationRequest, createVideoRenderingRequest, type GenerationContext } from '../../../packages/multimodal/src/index.js'
import { generateSeoGeoSuggestions } from '../../../packages/seo/src/index.js'
import { createPaymentProviderFromEnv, FixturePaymentProvider, type PaymentProvider } from '../../../packages/billing/src/payment-provider.js'
import { paymentFixturePolicy } from '../../../packages/application/src/payment-fixture-guard.js'
import { platformRuleSyncStatus } from '../../../packages/review/src/platform-rule-sync.js'
import { verifyAndParsePlatformRuleManifest } from '../../../packages/review/src/platform-rule-manifest.js'
import { assertOutboundUrl } from '../../../packages/connectors/src/outbound-security.js'
import { readBoundedResponseText } from '../../../packages/connectors/src/bounded-response.js'
import { mapWithConcurrency } from './bounded-concurrency.js'
import { redisClientOptions, withRedisOperationTimeout } from './redis-resilience.js'
import { SupportAuthorizationError, SupportService, SupportValidationError, type SupportAuthorizationContext } from './ops/support-service.js'
import { buildSupportSlaMonthlyReport, createSupportSlaCorrectionRun, type SupportSlaCorrectionApproval, type SupportSlaReportTicket } from '../../../packages/contracts/src/ops/support-sla-report.js'
import { IncidentsService, IncidentServiceError } from './ops/incidents-service.js'
import { FeatureFlagAuthorizationError, FeatureFlagsService } from './ops/feature-flags-service.js'
import { FinanceSearchService, FinanceSearchServiceError } from './ops/finance-search-service.js'
import { AuditCenterService, AuditCenterServiceError } from './ops/audit-center-service.js'
import { supportRolePermissions, type SupportPermission, type SupportRole, type SupportTicketContract, type SupportTicketPageCursor, type SupportTicketPriority, type SupportTicketStatus } from '../../../packages/contracts/src/ops/support.js'
import { FeatureFlagValidationError, type FeatureFlagEmergencyRequest, type FeatureFlagEvaluationContext, type FeatureFlagListRequest, type FeatureFlagMutationRequest } from '../../../packages/contracts/src/ops/feature-flags.js'
import { FinanceSearchValidationError, type FinanceRecordKind, type FinanceSearchQuery } from '../../../packages/contracts/src/ops/finance-search.js'
import { IncidentContractError } from '../../../packages/contracts/src/ops/incidents.js'
import { AuditCenterContractError, type AuditAccessRole, type AuditSource } from '../../../packages/contracts/src/ops/audit-center.js'
import { MemorySupportRepository, PostgresSupportRepository, SupportTicketIdempotencyConflictError, SupportTicketNotFoundError, SupportTicketRevisionConflictError, type SupportRepository } from '../../../packages/persistence/src/support-repository.js'
import { IncidentRepositoryError, MemoryIncidentRepository, PostgresIncidentRepository, type IncidentRepository, type IncidentSeverity, type IncidentStatus } from '../../../packages/persistence/src/incidents-repository.js'
import { FeatureFlagRepositoryError, MemoryFeatureFlagsRepository, PostgresFeatureFlagsRepository, type FeatureFlagsRepository } from '../../../packages/persistence/src/feature-flags-repository.js'
import { FinanceRecordVersionConflictError, FinanceSearchAccessError, FinanceSearchCursorError, PostgresFinanceSearchRepository, type FinanceSearchRepository } from '../../../packages/persistence/src/finance-search-repository.js'
import { AuditCenterCursorError } from '../../../packages/persistence/src/audit-center-repository.js'
import { MemoryWorkspaceBootstrapRepository, PostgresWorkspaceBootstrapRepository, WorkspaceBootstrapError, type WorkspaceBootstrapRepository } from '../../../packages/persistence/src/workspace-bootstrap-repository.js'
import { buildRequestLogEvent, getRequestCorrelation, serializeRequestLogEvent, type RequestLogInput } from './request-observability.js'
import { classifyAssetUpload, classifyAssetUploadBatch, type AssetUploadSecurityResult } from './asset-upload-security.js'
import { assetScanWaitingState, localAssetScanFixture } from './asset-scan-automation.js'
import { AssetParseRepositoryError, MemoryAssetParseRepository, PostgresAssetParseRepository, type AssetParseRecord, type AssetParseRepository } from '../../../packages/persistence/src/asset-parse-repository.js'
import { ImageContinuationLeaseError, MemoryImageContinuationLeaseRepository, PostgresImageContinuationLeaseRepository, type ImageContinuationLeaseRepository } from '../../../packages/persistence/src/image-generation-continuation-repository.js'
import { ImageGenerationExecutionError, MemoryImageGenerationExecutionRepository, PostgresImageGenerationExecutionRepository, type ImageGenerationExecutionRepository } from '../../../packages/persistence/src/image-generation-execution-repository.js'
import { MemoryReconciliationEvidenceRepository, PostgresReconciliationEvidenceRepository, ReconciliationEvidenceIdempotencyConflictError, type ReconciliationEvidenceRepository } from '../../../packages/persistence/src/reconciliation-evidence-repository.js'
import { MemoryUnifiedLinkAuditRepository, PostgresUnifiedLinkAuditRepository, type UnifiedLinkAuditRepository } from '../../../packages/persistence/src/unified-link-audit-repository.js'
import { AssetScanReceiptConflictError, MemoryAssetScanReceiptRepository, PostgresAssetScanReceiptRepository, type AppendAssetScanReceiptInput, type AssetScanReceiptRepository } from '../../../packages/persistence/src/asset-scan-repository.js'
import { MemoryPlatformMediaSpecRepository, PlatformMediaSpecRepositoryError, PostgresPlatformMediaSpecRepository, PLATFORM_MEDIA_SPEC_PLATFORMS, type PlatformMediaSpecDevice, type PlatformMediaSpecPlatform, type PlatformMediaSpecRepository, type PlatformMediaSpecStatus, type StoredPlatformMediaSpec } from '../../../packages/persistence/src/platform-media-spec-repository.js'
import { MappingPreflightApprovalRepositoryError, MemoryMappingPreflightApprovalRepository, PostgresMappingPreflightApprovalRepository, type MappingPreflightApprovalRepository, type StoredMappingPreflightApproval } from '../../../packages/persistence/src/mapping-preflight-approval-repository.js'
import { MemoryInteractiveConfirmationTicketRepository, PostgresInteractiveConfirmationTicketRepository, type InteractiveConfirmationTicketRepository, type TransactionalInteractiveConfirmationTicketRepository } from '../../../packages/persistence/src/interactive-confirmation-ticket-repository.js'
import { AssetParseExecutionError, executeAssetParse } from './asset-parse-runtime.js'
import { checkpointFromKnowledgeSnapshot, isKnowledgeEventAfterCheckpoint, isKnowledgeHydrationCheckpointCurrent, mergeKnowledgeHydrationEvents, type KnowledgeHydrationCheckpoint } from './knowledge-hydration-checkpoint.js'
import { evaluatePlatformFieldMapping, type PlatformFieldMappingGateInput, type PlatformFieldMappingGateResult } from '../../../packages/application/src/platform-field-mapping-gate.js'
import { buildDeliveryBundleManifest, evaluateVideoStoryboardQuality, evaluateVisualAuthenticity, verifyDeliveryBundle, type DeliveryBundleFile, type DeliveryBundleManifest, type DeliveryBundleManifestInput, type VideoStoryboardQualityInput, type VisualAuthenticityGateInput } from '../../../packages/multimodal/src/index.js'
import { projectPlatformCapabilityEvidence } from './platform-capability-response.js'

const port = Number(process.env.PORT ?? 8787)
const fixtureMode = process.env.CONNECTOR_FIXTURE_MODE === 'true'
const maxActiveJobsPerWorkspace = Number(process.env.MAX_ACTIVE_JOBS_PER_WORKSPACE ?? 3)
const MAX_MCP_EXPORT_BYTES = 25 * 1024 * 1024
// Social-commerce platforms are API/fixture targets, but remain fail-closed
// for production until official OAuth, mapping and canary evidence exists.
const SUPPORTED_PLATFORMS: readonly Platform[] = ['jd', 'taobao', 'tmall', 'pinduoduo', 'xiaohongshu', 'douyin']
const PLATFORM_LABELS: Record<Platform, string> = { jd: '京东', taobao: '淘宝', tmall: '天猫', pinduoduo: '拼多多', xiaohongshu: '小红书', douyin: '抖音' }

function productImportIdentity(input: { platform: Platform; accountId?: string; remoteId?: string; localProductKey?: string; title: string; storeName?: string }) {
  const remoteId = input.remoteId?.trim()
  if (remoteId) return `${input.platform}:account=${input.accountId ?? ''}:remote=${remoteId}`
  const localKey = input.localProductKey?.trim() || `${input.accountId ?? (input.storeName?.trim() || '导入店铺')}:${input.title.trim()}`
  return `${input.platform}:account=${input.accountId ?? ''}:local=${localKey}`
}

function assertUniqueBatchProductImportIdentities(items: Array<{ platform: Platform; accountId?: string; remoteId?: string; localProductKey?: string; title: string; storeName?: string }>) {
  const seen = new Map<string, number>()
  const duplicates: Array<{ identity: string; first_index: number; duplicate_index: number }> = []
  for (const [index, item] of items.entries()) {
    const identity = productImportIdentity(item)
    const firstIndex = seen.get(identity)
    if (firstIndex !== undefined) duplicates.push({ identity, first_index: firstIndex + 1, duplicate_index: index + 1 })
    else seen.set(identity, index)
  }
  if (duplicates.length) throw new DomainError('PRODUCT_IMPORT_IDENTITY_CONFLICT', '批量导入包含重复商品身份，已在写入前阻断', 409, { duplicates })
}

const knowledgeByWorkspace = new Map<string, KnowledgeModule>()
function knowledgeForWorkspace(workspaceId: string): KnowledgeModule {
  const existing = knowledgeByWorkspace.get(workspaceId)
  if (existing) return existing
  const created = new KnowledgeModule()
  knowledgeByWorkspace.set(workspaceId, created)
  return created
}
const relayPricing = createRelayPricingClientFromEnv(process.env)

const MERCHANT_CAPABILITY_CARDS = [
  { id: 'stores-products', title: '店铺与商品', summary: '绑定平台店铺、同步商品、查看多店铺商品事实。', entryMethod: 'platform.connect', nextMethods: ['platform.connect', 'catalog.sync.start', 'catalog.search'], readOnly: false },
  { id: 'first-value', title: '示例体验', summary: '可选查看安全预览，了解商品内容和视觉交付方式，不会发布或覆盖商品。', entryMethod: 'merchant.first_value', nextMethods: ['merchant.first_value'], readOnly: true },
  { id: 'knowledge-assets', title: '知识库与素材', summary: '管理商品原图、品牌资料、扫描状态和商用权益。', entryMethod: 'asset.list', nextMethods: ['asset.list', 'asset.upload', 'asset.parse', 'brand.get'], readOnly: true },
  { id: 'content', title: '商品内容', summary: '按事实确认、方案确认、生成、审核和版本交付商品文案。', entryMethod: 'task.understand', nextMethods: ['task.understand', 'task.plan.confirm', 'content.generate', 'content.review'], readOnly: false },
  { id: 'visuals', title: '主图与视觉', summary: '使用已授权商品素材生成候选主图，审阅后选择版本。', entryMethod: 'catalog.image.generate', nextMethods: ['catalog.image.generate', 'catalog.image.get', 'catalog.image.review', 'content.visual.select'], readOnly: false },
  { id: 'review-publish', title: '审核与发布', summary: '查看字段差异、规则结果和发布前确认状态。', entryMethod: 'publish.prepare', nextMethods: ['content.approve', 'publish.prepare', 'publish.confirm', 'publish.get'], readOnly: false },
  { id: 'bulk-publish', title: '批量发布', summary: '按店铺逐项预览、确认、暂停和重试最多 50 个商品。', entryMethod: 'publish.batch.prepare', nextMethods: ['publish.batch.prepare', 'publish.batch.confirm', 'publish.batch.get', 'publish.batch.pause', 'publish.batch.resume', 'publish.batch.retry_failed'], readOnly: false },
  { id: 'rules', title: '平台规则', summary: '按当前平台和店铺查看适用规则、版本新鲜度与生成前违规预检。', entryMethod: 'rule.sync.status', nextMethods: ['rule.sync.status', 'rule.list', 'content.review'], readOnly: true },
  { id: 'billing', title: '套餐与钱包', summary: '查看套餐、权益、模型成本和充值到账状态。', entryMethod: 'billing.status', nextMethods: ['billing.status', 'subscription.get', 'billing.reconciliation', 'billing.recharge.create'], readOnly: true },
] as const

const legacyImageActionPrefix = ['image', 'addon'].join('-') + ':'

async function getActionLedgerWithHistoricalImageCompat(workspaceId: string, actionKey: string) {
  const exact = await persistence.actionLedger?.get(workspaceId, actionKey)
  if (exact || !actionKey.startsWith(legacyImageActionPrefix)) return { action: exact, actionKey }
  const historicalActionKey = `image:${actionKey.slice(legacyImageActionPrefix.length)}`
  const historical = await persistence.actionLedger?.get(workspaceId, historicalActionKey)
  return historical ? { action: historical, actionKey: historicalActionKey } : { action: exact, actionKey }
}

async function recordRelayUsage(usage: RelayUsageRecord) {
  if (isProduction() && !usage.workspaceId?.trim()) throw new Error('MODEL_USAGE_WORKSPACE_REQUIRED')
  if (isProduction() && !usage.actionId?.trim()) throw new Error('MODEL_USAGE_ACTION_REQUIRED')
  if (isProduction() && !usage.runKey?.trim()) throw new Error('MODEL_USAGE_RUN_KEY_REQUIRED')
  if (!usage.workspaceId) return
  const workspaceId = usage.workspaceId
  await persistenceReady
  if (!persistence.modelUsage) throw new Error('MODEL_USAGE_LEDGER_NOT_CONFIGURED')
  const receiptKey = relayUsageReceiptKey(usage)
  const policy = await (persistence.commercialExtensions ?? memoryCommercialExtensions).getModelMarkupPolicy()
  const durableAuthorizationLookup = usage.actionId ? await getActionLedgerWithHistoricalImageCompat(workspaceId, usage.actionId) : undefined
  const durableAuthorization = durableAuthorizationLookup?.action
  const durableAuthorizationActionKey = durableAuthorizationLookup?.actionKey ?? usage.actionId
  const effectiveMultiplier = durableAuthorization?.multiplier ?? policy.multiplier
  let pricingFailure: { code: string; message: string } | undefined
  if (usage.costCny === undefined && relayPricing) {
    try {
      const quote = await relayPricing.quote(usage)
      usage.costCny = quote.costCny
      usage.metadata = { ...(usage.metadata ?? {}), ...quote.metadata }
    } catch (error) {
      pricingFailure = { code: (error as { code?: string })?.code ?? 'MODEL_PRICING_DERIVATION_FAILED', message: error instanceof Error ? error.message : String(error) }
    }
  }
  if (usage.costCny === undefined) {
    if (durableAuthorization && durableAuthorizationActionKey) await persistence.actionLedger?.transitionSettlementStatus({ workspaceId, actionKey: durableAuthorizationActionKey, from: ['authorized', 'pending_receipt'], to: 'pending_receipt' })
    const pending = await persistence.modelUsage.record({ receiptKey, workspaceId, ...(usage.actionId ? { actionId: usage.actionId, ...(isProduction() ? { budgetReservationKey: usage.actionId, budgetRunKey: usage.runKey! } : {}) } : {}), ...(usage.contextLinkId && usage.contextHash ? { contextLinkId: usage.contextLinkId, contextHash: usage.contextHash } : {}), modality: usage.modality, model: usage.model, ...(usage.providerRequestId ? { providerRequestId: usage.providerRequestId } : {}), ...(usage.inputTokens !== undefined ? { inputTokens: usage.inputTokens } : {}), ...(usage.outputTokens !== undefined ? { outputTokens: usage.outputTokens } : {}), ...(usage.totalTokens !== undefined ? { totalTokens: usage.totalTokens } : {}), settlementStatus: 'pending_cost', lastError: { code: pricingFailure?.code ?? 'MODEL_USAGE_COST_MISSING', message: pricingFailure?.message ?? 'provider receipt omitted actual cost' }, nextAttemptAt: new Date().toISOString(), metadata: { ...(usage.metadata ?? {}), settlement_reason: pricingFailure?.code ?? 'provider_cost_missing' } })
    const alert = await (persistence.alerts ?? memoryAlerts).upsert({ workspaceId, alertKey: `model-cost-missing:${receiptKey}`, code: pricingFailure?.code ?? 'MODEL_USAGE_COST_MISSING', severity: 'high', entityType: 'model_usage', entityId: pending.id, title: '模型中转成本证据不足，结果已阻断交付', observedAt: usage.observedAt, evidence: { receipt_key: receiptKey, modality: usage.modality, model: usage.model, provider_request_id: usage.providerRequestId ?? null, action_id: usage.actionId ?? null, pricing_error: pricingFailure ?? null }, nextAction: '核对中转站回执成本或价格快照、计费分组和汇率；完成待结算记录后再恢复模型交付。' })
    void persistOperationalAlertNotification(alert)
    throw Object.assign(new Error('model relay usage is missing actual cost'), { code: 'MODEL_USAGE_COST_MISSING' })
  }
  const zeroCustomerChargeAuthorization = durableAuthorization?.settlement === 'entitlement' || durableAuthorization?.settlement === 'included_quota'
  const customerChargeCny = usage.costCny === undefined ? undefined : zeroCustomerChargeAuthorization ? 0 : Number((usage.costCny * effectiveMultiplier).toFixed(6))
  const usageInput = { receiptKey, workspaceId, ...(usage.actionId ? { actionId: usage.actionId } : {}), ...(usage.contextLinkId && usage.contextHash ? { contextLinkId: usage.contextLinkId, contextHash: usage.contextHash } : {}), modality: usage.modality, model: usage.model, ...(usage.providerRequestId ? { providerRequestId: usage.providerRequestId } : {}), ...(usage.inputTokens !== undefined ? { inputTokens: usage.inputTokens } : {}), ...(usage.outputTokens !== undefined ? { outputTokens: usage.outputTokens } : {}), ...(usage.totalTokens !== undefined ? { totalTokens: usage.totalTokens } : {}), ...(usage.metadata || usage.runKey ? { metadata: { ...(usage.metadata ?? {}), ...(usage.runKey ? { run_key: usage.runKey } : {}) } } : {}) }
  let recordedUsage
  if (isProduction() && usage.actionId && usage.costCny !== undefined) {
    try {
      recordedUsage = (await persistence.modelUsage.recordUsageAndSettleBudget({ ...usageInput, budgetReservationKey: usage.actionId, budgetRunKey: usage.runKey!, costCny: usage.costCny, markupMultiplier: effectiveMultiplier, customerChargeCny, pricingPolicyRevision: policy.revision, ...(usage.observedAt ? { observedAt: usage.observedAt } : {}) })).usage
    } catch (error) {
      if (!['MODEL_DAILY_COST_ACTUAL_EXCEEDED', 'MODEL_TASK_COST_ACTUAL_EXCEEDED'].includes((error as { code?: string })?.code ?? '')) throw error
      const details = (error as { details: { usedCny: number; reservedCny: number; requestCny: number; limitCny: number } }).details
      const committedUsageId = (error as { committed?: { usage?: { id?: string } } }).committed?.usage?.id ?? receiptKey
      await (persistence.alerts ?? memoryAlerts).upsert({ workspaceId, alertKey: `model-budget-overrun:${usage.actionId}`, code: (error as { code?: string }).code ?? 'MODEL_DAILY_COST_ACTUAL_EXCEEDED', severity: 'high', entityType: 'model_usage', entityId: committedUsageId, title: '模型实际成本超过预算预留，结果已阻断', observedAt: usage.observedAt, evidence: { action_id: usage.actionId, provider_request_id: usage.providerRequestId ?? null, used_cny: details.usedCny, reserved_cny: details.reservedCny, request_cny: details.requestCny, limit_cny: details.limitCny }, nextAction: '核对价格版本和实际成本；在运营后台人工处理，不得静默放行或重复调用模型。' })
      throw Object.assign(error as Error, { providerSucceeded: true, details: { used_cny: details.usedCny, reserved_cny: details.reservedCny, request_cny: details.requestCny, limit_cny: details.limitCny } })
    }
  } else {
    recordedUsage = await persistence.modelUsage.record({ ...usageInput, ...(usage.costCny !== undefined ? { costCny: usage.costCny, markupMultiplier: effectiveMultiplier, customerChargeCny, pricingPolicyRevision: policy.revision, settlementStatus: 'pending_wallet' as const } : { settlementStatus: 'pending_cost' as const }) })
  }
  if (recordedUsage.settlementStatus === 'settled' || recordedUsage.settlementStatus === 'waived') return
  const reservation = usage.actionId ? modelBillingReservations.get(`${workspaceId}:${usage.actionId}`) : undefined
  const durableWalletAuthorization = durableAuthorization && (durableAuthorization.settlement === 'wallet' || durableAuthorization.settlement === 'wallet_overage') ? durableAuthorization : undefined
  const durableZeroChargeAuthorization = zeroCustomerChargeAuthorization ? durableAuthorization : undefined
  try {
    if (durableZeroChargeAuthorization && durableAuthorizationActionKey) {
      let settlementActionKey = durableAuthorizationActionKey
      if (durableZeroChargeAuthorization.settlementStatus === 'settled') {
        settlementActionKey = `model-usage:${receiptKey}`
        const repairAction = await recordActionSettlement({
          workspaceId,
          actionKey: settlementActionKey,
          actionKind: durableZeroChargeAuthorization.actionKind,
          settlement: durableZeroChargeAuthorization.settlement,
          amountFen: 0,
          reservedAmountFen: 0,
          multiplier: effectiveMultiplier,
          settlementStatus: 'authorized',
          actorId: durableZeroChargeAuthorization.actorId,
          description: '模型结构修复请求真实用量结算',
          ...(durableZeroChargeAuthorization.taskId ? { taskId: durableZeroChargeAuthorization.taskId } : {}),
          ...(durableZeroChargeAuthorization.campaignItemId ? { campaignItemId: durableZeroChargeAuthorization.campaignItemId } : {}),
          ...(durableZeroChargeAuthorization.contextLinkId && durableZeroChargeAuthorization.contextHash ? { contextLinkId: durableZeroChargeAuthorization.contextLinkId, contextHash: durableZeroChargeAuthorization.contextHash } : {}),
        })
        if (repairAction.settlementStatus === 'authorized') await persistence.actionLedger?.transitionSettlementStatus({ workspaceId, actionKey: settlementActionKey, from: ['authorized'], to: 'pending_receipt' })
      } else if (durableZeroChargeAuthorization.settlementStatus === 'authorized') {
        await persistence.actionLedger?.transitionSettlementStatus({ workspaceId, actionKey: settlementActionKey, from: ['authorized'], to: 'pending_receipt' })
      } else if (durableZeroChargeAuthorization.settlementStatus !== 'pending_receipt') {
        throw Object.assign(new Error(`model action settlement is ${durableZeroChargeAuthorization.settlementStatus}`), { code: 'MODEL_USAGE_ZERO_CHARGE_SETTLEMENT_BLOCKED' })
      }
      await persistence.actionLedger?.settleProviderUsage({ workspaceId, actionKey: settlementActionKey, actualAmountFen: 0, ...(usage.providerRequestId ? { providerRequestId: usage.providerRequestId } : {}) })
    } else if ((durableWalletAuthorization || reservation) && recordedUsage.customerChargeCny !== undefined) {
      const actualAmountFen = Math.max(1, Math.ceil(recordedUsage.customerChargeCny * 100))
      if (durableWalletAuthorization && usage.actionId) {
        const actionKey = durableAuthorizationActionKey ?? usage.actionId
        if (durableWalletAuthorization.settlementStatus === 'authorized' || durableWalletAuthorization.settlementStatus === 'pending_receipt') {
          await persistence.actionLedger?.transitionSettlementStatus({ workspaceId, actionKey, from: ['authorized', 'pending_receipt'], to: 'pending_receipt' })
          await settlePluginWalletDebit({ workspaceId, debitIdempotencyKey: usage.actionId, finalAmountFen: actualAmountFen, actorId: durableWalletAuthorization.actorId, providerRequestId: usage.providerRequestId })
        } else if (durableWalletAuthorization.settlementStatus === 'settled') {
          const repairDebitKey = `model-usage:${receiptKey}`
          await debitPluginWallet({ workspaceId, amountFen: actualAmountFen, idempotencyKey: repairDebitKey, actorId: durableWalletAuthorization.actorId, description: '模型结构修复请求真实用量结算' })
          await persistence.actionLedger?.settleProviderUsage({ workspaceId, actionKey: repairDebitKey, actualAmountFen, ...(usage.providerRequestId ? { providerRequestId: usage.providerRequestId } : {}) })
        } else {
          throw Object.assign(new Error(`model action settlement is ${durableWalletAuthorization.settlementStatus}`), { code: 'MODEL_USAGE_WALLET_SETTLEMENT_BLOCKED' })
        }
        modelBillingReservations.delete(`${workspaceId}:${usage.actionId}`)
      } else if (reservation) {
        const requestKey = usage.providerRequestId ?? receiptKey
        if (!reservation.providerRequests.has(requestKey)) {
          if (reservation.providerRequests.size === 0) await settlePluginWalletDebit({ workspaceId, debitIdempotencyKey: reservation.debitIdempotencyKey, finalAmountFen: actualAmountFen, actorId: reservation.actorId, providerRequestId: usage.providerRequestId })
          else await debitPluginWallet({ workspaceId, amountFen: actualAmountFen, idempotencyKey: `model-usage:${receiptKey}`, actorId: reservation.actorId, description: '模型修复请求真实用量结算' })
          reservation.providerRequests.add(requestKey)
        }
      }
    }
    await persistence.modelUsage.resolve({ workspaceId, id: recordedUsage.id, expectedRevision: recordedUsage.revision, status: 'settled', actorId: 'model-usage-settlement', reason: '中转回执成本与钱包结算完成', evidenceRef: usage.providerRequestId ?? receiptKey })
  } catch (error) {
    const message = error instanceof Error ? error.message : String(error)
    try {
      if (durableAuthorization && durableAuthorizationActionKey) await persistence.actionLedger?.transitionSettlementStatus({ workspaceId, actionKey: durableAuthorizationActionKey, from: ['authorized', 'pending_receipt'], to: 'pending_receipt' })
      await persistence.modelUsage.resolve({ workspaceId, id: recordedUsage.id, expectedRevision: recordedUsage.revision, status: 'pending_wallet', actorId: 'model-usage-settlement', reason: '等待钱包结算重试', lastError: { code: (error as { code?: string })?.code ?? 'MODEL_USAGE_WALLET_SETTLEMENT_FAILED', message }, nextAttemptAt: new Date(Date.now() + 60_000).toISOString() })
    } catch { /* a concurrent reconciler may already have advanced the row */ }
    await (persistence.alerts ?? memoryAlerts).upsert({ workspaceId, alertKey: `model-wallet-settlement:${receiptKey}`, code: 'MODEL_USAGE_WALLET_SETTLEMENT_FAILED', severity: 'high', entityType: 'model_usage', entityId: recordedUsage.id, title: '模型已返回结果，但钱包结算尚未完成', observedAt: usage.observedAt, evidence: { receipt_key: receiptKey, action_id: usage.actionId ?? null, error: message }, nextAction: '在运营后台重试该笔模型用量结算；不要向用户重复退款或重复调用模型。' })
    throw error
  }
}

const rawContentGenerator = createContentGeneratorFromEnv(process.env, recordRelayUsage)
const rawImageGenerator = createImageGeneratorFromEnv(process.env, recordRelayUsage)
const protectedProductPromptConstraints = validateProtectedProductIntent('').promptConstraints

export function appendProtectedProductConstraints(prompt: string) {
  return `${prompt.trim()}\n\n不可变商品约束（必须逐条遵守）：\n${protectedProductPromptConstraints.zh}\n\nImmutable product constraints (must all be followed):\n${protectedProductPromptConstraints.en}`
}

function protectedProductConclusion(validation: ProtectedProductIntentValidation) {
  return {
    policy: 'protected-product-intent-v1',
    allowed: validation.allowed,
    immutableAttributes: validation.immutableConstraints.map(item => item.attribute),
    safeModifications: validation.safeModifications.map(item => item.category),
    blockedAttributes: [...new Set(validation.findings.map(item => item.attribute))],
    findingCodes: validation.findings.map(item => item.code),
  }
}

function requireProtectedProductIntent(prompt: string) {
  const validation = validateProtectedProductIntent(prompt)
  if (!validation.allowed) {
    throw new DomainError('PROTECTED_PRODUCT_MUTATION_BLOCKED', '图片请求会改变受保护的商品事实，仅允许调整背景、场景、光影或构图', 409, {
      product_protection: protectedProductConclusion(validation),
    })
  }
  return validation
}

const contentGenerator = rawContentGenerator ? { generate: (input: Parameters<typeof rawContentGenerator.generate>[0]) => withDailyModelBudget('text', input.usageContext, () => rawContentGenerator.generate(input)) } : undefined
const imageGenerator = rawImageGenerator ? {
  generate: (input: Parameters<typeof rawImageGenerator.generate>[0]) => withDailyModelBudget('image', input.usageContext, () => rawImageGenerator.generate({ ...input, direction: appendProtectedProductConstraints(input.direction) })),
} : undefined

export const KNOWLEDGE_CONTEXT_LIMITS = {
  softRules: 24,
  confirmedLearningSuggestions: 8,
} as const

type KnowledgeCompetitorReference = NonNullable<KnowledgeGenerationContext['competitorReferences']>[number]

function isHardKnowledgeRule(rule: Pick<RuleEntry, 'severity' | 'action'>) {
  return rule.severity === 'error' || rule.action === 'block'
}

function newestKnowledgeFirst<T extends { id: string; updatedAt: string }>(items: readonly T[]) {
  return items.map((item, index) => ({ item, index })).sort((left, right) =>
    right.item.updatedAt.localeCompare(left.item.updatedAt)
    || left.item.id.localeCompare(right.item.id)
    || left.index - right.index,
  )
}

/**
 * Bound model context without dropping governed hard rules. The returned rule
 * order is deterministic; ranking only determines which non-hard rules and
 * confirmed suggestions survive the budget.
 */
export function buildBoundedKnowledgeGenerationContext(input: {
  rules: readonly RuleEntry[]
  learningSuggestions: readonly LearningSuggestion[]
  competitorReference?: KnowledgeCompetitorReference
}): KnowledgeGenerationContext {
  const softRuleIds = new Set(newestKnowledgeFirst(input.rules.filter(rule => !isHardKnowledgeRule(rule))).slice(0, KNOWLEDGE_CONTEXT_LIMITS.softRules).map(({ item }) => item.id))
  const selectedRules = input.rules.filter(rule => isHardKnowledgeRule(rule) || softRuleIds.has(rule.id)).sort((left, right) => {
    const leftHard = isHardKnowledgeRule(left)
    const rightHard = isHardKnowledgeRule(right)
    if (leftHard !== rightHard) return Number(rightHard) - Number(leftHard)
    // Hard rules are retained in stable ID order so context ordering does not
    // change when the input source or update timestamps are reordered.
    if (leftHard && rightHard) return left.id.localeCompare(right.id)
    return right.updatedAt.localeCompare(left.updatedAt) || left.id.localeCompare(right.id)
  })
  const selectedLearningIds = new Set(newestKnowledgeFirst(input.learningSuggestions).slice(0, KNOWLEDGE_CONTEXT_LIMITS.confirmedLearningSuggestions).map(({ item }) => item.id))
  const selectedLearningSuggestions = newestKnowledgeFirst(input.learningSuggestions.filter(item => selectedLearningIds.has(item.id))).map(({ item }) => item)
  return {
    rules: selectedRules.map(rule => ({ id: rule.id, content: rule.content, version: rule.version, sourceReference: rule.source.reference, ...(rule.effectiveFrom ? { effectiveFrom: rule.effectiveFrom } : {}), ...(rule.effectiveTo ? { effectiveTo: rule.effectiveTo } : {}) })),
    assets: [],
    confirmedLearningSuggestions: selectedLearningSuggestions.map(item => ({ id: item.id, summary: item.summary, proposedRule: { content: item.proposedRule.content, scope: item.proposedRule.scope, version: item.proposedRule.version } })),
    ...(input.competitorReference ? { competitorReferences: [input.competitorReference] } : {}),
  }
}

const service = new MerchantService({
  fixtureMode,
  seedFixture: fixtureMode || process.env.NODE_ENV === 'test',
  strictAccountScope: true,
  contentGenerator,
  imageGenerator,
  contextSnapshotSink: async ({ task, envelope, inputTokensEstimate, maxInputTokens, versions }) => {
    await persistenceReady
    const persistedEnvelope = envelope as unknown as Record<string, unknown>
    const saved = await (persistence.contextSnapshots ?? memoryContextSnapshots).save({ workspaceId: task.workspaceId, ...(task.brandId ? { brandId: task.brandId } : {}), envelope: persistedEnvelope, inputTokensEstimate, maxInputTokens, versions, taskId: task.id, ...(task.campaignId ? { campaignId: task.campaignId, campaignItemId: task.campaignItemId! } : {}), ...(task.canonicalProductId ? { canonicalProductId: task.canonicalProductId } : {}), ...(task.listingId ? { listingId: task.listingId } : {}), linkId: taskContextLinkId(task.id, persistedEnvelope) })
    return { id: saved.id, contextHash: saved.contextHash }
  },
  maxActiveJobsPerWorkspace: Number.isFinite(maxActiveJobsPerWorkspace) && maxActiveJobsPerWorkspace > 0 ? maxActiveJobsPerWorkspace : 3,
  knowledgeContextProvider: ({ workspaceId, platform, category, brand, store, competitorReference, asOf }) => buildBoundedKnowledgeGenerationContext({
    rules: knowledgeForWorkspace(workspaceId).findApplicableRules({ platform, ...(category ? { category } : {}), ...(brand ? { brand } : {}), ...(store ? { store } : {}) }, asOf, workspaceId),
    learningSuggestions: knowledgeForWorkspace(workspaceId).listLearningSuggestions(workspaceId, 'confirmed'),
    ...(competitorReference ? { competitorReference } : {}),
  }),
})
type BrandUnitRecord = {
  id: string
  workspaceId: string
  name: string
  revision: number
  storeBindings: Array<{ platform: Platform; accountId: string }>
  createdAt: string
  updatedAt: string
}
type CampaignBatchRecord = {
  id: string
  workspaceId: string
  brandId: string
  platform: Platform
  accountId: string
  productIds: string[]
  targets?: Array<{ productId: string; platform: Platform; accountId: string; listingId?: string; canonicalProductId?: string }>
  state: 'draft'
  storage: 'memory'
  durable: false
  createdAt: string
  updatedAt: string
}
const brandUnits = new Map<string, BrandUnitRecord>()
const campaignBatches = new Map<string, CampaignBatchRecord>()
const invalidDurableSnapshots = new Map<string, Array<{ entityType: string; entityId: string; missing: string[] }>>()
// Hydration is a compatibility bridge for the in-memory domain service. Keep
// it request-safe without reloading every snapshot on every request; all
// server-side persistence helpers invalidate this entry after a successful
// write. Cross-instance updates may be observed within this short TTL.
const workspaceHydrationInFlight = new Map<string, Promise<void>>()
const workspaceHydratedAt = new Map<string, number>()
const workspaceHydrationGeneration = new Map<string, number>()
const WORKSPACE_HYDRATION_TTL_MS = 1_000
function invalidateWorkspaceHydration(workspaceId: string) {
  for (const key of workspaceHydratedAt.keys()) if (key === workspaceId || key.startsWith(`${workspaceId}:`)) workspaceHydratedAt.delete(key)
  workspaceHydrationGeneration.set(workspaceId, (workspaceHydrationGeneration.get(workspaceId) ?? 0) + 1)
}
const rawImageFactsExtractor = createImageFactsExtractorFromEnv(process.env, recordRelayUsage)
const rawImageEditGenerator = createImageEditGeneratorFromEnv(process.env, recordRelayUsage)
const rawVideoGenerator = createVideoGeneratorFromEnv(process.env, recordRelayUsage)
const imageFactsExtractor = rawImageFactsExtractor ? { extract: (input: Parameters<typeof rawImageFactsExtractor.extract>[0]) => withDailyModelBudget('ocr', input.usageContext, () => rawImageFactsExtractor.extract(input)) } : undefined
const imageEditGenerator = rawImageEditGenerator ? { generate: (input: Parameters<typeof rawImageEditGenerator.generate>[0]) => withDailyModelBudget('image_edit', input.usageContext, () => rawImageEditGenerator.generate(input)) } : undefined
const videoGenerator = rawVideoGenerator ? { generate: (input: Parameters<typeof rawVideoGenerator.generate>[0]) => withDailyModelBudget('video', input.usageContext, () => rawVideoGenerator.generate(input)), getStatus: rawVideoGenerator.getStatus.bind(rawVideoGenerator) } : undefined
let paymentProvider = createPaymentProviderFromEnv()
const fixturePaymentProvider = new FixturePaymentProvider()

type PaymentChannel = 'alipay' | 'wechat'
export function fixturePaymentAllowed(source: NodeJS.ProcessEnv = process.env) {
  return paymentFixturePolicy(source).fixtureAllowed
}
function paymentChannel(params: Record<string, unknown>): PaymentChannel {
  if (params.channel !== 'alipay' && params.channel !== 'wechat') throw new DomainError('BILLING_CHANNEL_INVALID', '订阅支付渠道必须是支付宝或微信', 400)
  return params.channel
}

async function createSubscriptionCheckout(input: { channel: PaymentChannel; orderId: string; idempotencyKey: string; workspaceId: string; amountFen: number; kind: 'subscriptions' }) {
  const providerMode = process.env.PAYMENT_MODE === 'provider'
  if (!providerMode) return fixturePaymentProvider.createCheckout({ ...input, callbackUrl: `fixture://${input.workspaceId}/callback`, description: `merchant-marketing 订阅订单 ${input.orderId}` })
  if (!paymentProvider) throw new DomainError('PAYMENT_NOT_CONFIGURED', '支付 provider adapter 未装配', 503)
  const callbackBase = process.env.PAYMENT_CALLBACK_BASE_URL?.trim().replace(/\/$/u, '')
  if (!callbackBase) throw new DomainError('PAYMENT_NOT_CONFIGURED', '支付回调地址未配置', 503)
  try {
    const checkout = await paymentProvider.createCheckout({ channel: input.channel, orderId: input.orderId, idempotencyKey: input.idempotencyKey, workspaceId: input.workspaceId, amountFen: input.amountFen, callbackUrl: `${callbackBase}/${input.kind}/callback/${input.channel}`, description: `merchant-marketing 订阅订单 ${input.orderId}` })
    return { paymentUrl: checkout.paymentUrl, ...(checkout.providerOrderId ? { providerOrderId: checkout.providerOrderId } : {}), ...(checkout.expiresAt ? { expiresAt: checkout.expiresAt } : {}) }
  } catch (error) {
    throw new DomainError('PAYMENT_PROVIDER_CHECKOUT_FAILED', error instanceof Error ? error.message : '支付服务商下单失败', 503)
  }
}

async function grantSubscriptionEntitlements(input: { workspaceId: string; orderNo: string; addonCodes: readonly string[]; extensions: CommercialExtensionsRepository; entitlements: EntitlementRepository }) {
  const addons = await input.extensions.listAddons()
  return Promise.all(input.addonCodes.map(code => {
    const addon = addons.find(item => item.code === code)
    if (!addon) throw new DomainError('SUBSCRIPTION_ADDON_NOT_FOUND', `订阅订单中的加购项不存在：${code}`, 409)
    return input.entitlements.grant({ workspaceId: input.workspaceId, orderNo: input.orderNo, addonCode: addon.code, kind: addon.kind, units: addon.units })
  }))
}
const hydratedKnowledgeWorkspaces = new Map<string, number>()
// Knowledge hydration reads a bounded durable window for recovery, but the
// projection itself should only replay events it has not already applied.
// This keeps repeated MCP calls cheap without changing the restart fallback.
const hydratedKnowledgeEventIds = new Map<string, Set<string>>()
const hydratedKnowledgeCheckpoints = new Map<string, KnowledgeHydrationCheckpoint>()
const knowledgeHydrationFlights = new Map<string, Promise<void>>()

type ConnectorMappingProduct = Pick<Product, 'id' | 'workspaceId' | 'platform' | 'accountId' | 'remoteId' | 'category' | 'version'>
type ConnectorMappingProductLookup = (input: { workspaceId: string; platform: Platform; accountId: string; remoteId?: string }) => ConnectorMappingProduct | undefined | Promise<ConnectorMappingProduct | undefined>

function connectorMappingFieldType(value: unknown): 'string' | 'number' | 'integer' | 'boolean' | 'money' | undefined {
  if (typeof value === 'string') return 'string'
  if (typeof value === 'boolean') return 'boolean'
  if (typeof value === 'number' && Number.isFinite(value)) return Number.isInteger(value) ? 'integer' : 'number'
  if (value && typeof value === 'object' && !Array.isArray(value)) {
    const money = value as Record<string, unknown>
    if (Object.keys(money).length === 2 && typeof money.amount === 'string' && /^-?\d+(?:\.\d+)?$/u.test(money.amount) && typeof money.currency === 'string' && /^[A-Z]{3}$/u.test(money.currency)) return 'money'
  }
  return undefined
}

export function buildConnectorMappingGate(input: { approval: StoredMappingPreflightApproval; product: ConnectorMappingProduct; sourceProductId: string; fields: Readonly<Record<string, unknown>>; category: string }): PlatformFieldMappingGateInput | undefined {
  const entries = Object.entries(input.fields)
  const seen = new Set<string>()
  const definitions: PlatformFieldMappingGateInput['schema']['fields'][number][] = []
  const rules: PlatformFieldMappingGateInput['mapping']['rules'][number][] = []
  for (const [field, value] of entries) {
    const canonical = field.normalize('NFKC').trim()
    const type = connectorMappingFieldType(value)
    if (!canonical || canonical !== field || seen.has(canonical) || ['__proto__', 'constructor', 'prototype'].includes(canonical) || !type) return undefined
    seen.add(canonical)
    definitions.push({ name: canonical, scope: 'product', required: true, type, ...(type === 'money' ? { money: { scale: ((value as { amount: string }).amount.split('.')[1] ?? '').length, currency: (value as { currency: string }).currency } } : {}) })
    rules.push({ scope: 'product', sourceField: canonical, targetField: canonical })
  }
  const category = input.category.normalize('NFKC').trim()
  const sourceProductId = input.sourceProductId.normalize('NFKC').trim()
  if (!category || !sourceProductId) return undefined
  const evidence = (sha256: string, kind: 'schema' | 'mapping') => ({ state: 'production_canary' as const, reference: `urn:merchant:mapping-preflight:${kind}:${sha256}`, sha256, capturedAt: input.approval.evaluatedAt })
  const gate: PlatformFieldMappingGateInput = {
    platform: input.product.platform,
    category,
    schema: { source: 'official', version: input.approval.schemaVersion, immutableEvidence: evidence(input.approval.schemaEvidenceHash, 'schema'), fields: definitions },
    mapping: { version: input.approval.mappingVersion, schemaVersion: input.approval.schemaVersion, immutableEvidence: evidence(input.approval.mappingEvidenceHash, 'mapping'), rules },
    source: { productId: sourceProductId, productFields: structuredClone(input.fields), skuPages: [{ items: [{ skuId: `product:${input.product.id}`, fields: {} }] }] },
    remoteSnapshot: { hash: input.approval.remoteSnapshotHash, schemaVersion: input.approval.schemaVersion },
  }
  let preview: PlatformFieldMappingGateResult
  try { preview = evaluatePlatformFieldMapping(gate) } catch { return undefined }
  gate.remoteSnapshot.confirmation = {
    id: `durable-mapping-preflight-r${input.approval.revision}`,
    schemaVersion: input.approval.schemaVersion,
    schemaEvidenceHash: input.approval.schemaEvidenceHash,
    mappingVersion: input.approval.mappingVersion,
    mappingEvidenceHash: input.approval.mappingEvidenceHash,
    payloadHash: preview.mappedPayloadHash,
    remoteSnapshotHash: input.approval.remoteSnapshotHash,
    confirmedBy: 'durable-mapping-preflight',
    confirmedAt: input.approval.evaluatedAt,
  }
  return gate
}

export function createApiConnectorMappingPreflightAdapter(input: { repository: () => MappingPreflightApprovalRepository | Promise<MappingPreflightApprovalRepository>; findProduct: ConnectorMappingProductLookup }): ConnectorRuntimeMappingPreflightAdapter {
  const activeGate = async (scope: { workspaceId: string; platform: Platform; accountId: string; remoteId?: string }, fields: Readonly<Record<string, unknown>>, sourceProductId: 'local' | 'remote', category: string) => {
    if (!scope.workspaceId.trim() || !scope.accountId.trim()) return undefined
    const product = await input.findProduct(scope)
    if (!product || product.workspaceId !== scope.workspaceId || product.platform !== scope.platform || product.accountId !== scope.accountId || (scope.remoteId !== undefined && product.remoteId !== scope.remoteId)) return undefined
    const repository = await input.repository()
    const approval = await repository.get({ workspaceId: scope.workspaceId, platform: scope.platform, productId: product.id })
    if (!approval) return undefined
    const localGate = buildConnectorMappingGate({ approval, product, sourceProductId: product.id, fields, category })
    if (!localGate) return undefined
    const localReport = evaluatePlatformFieldMapping(localGate)
    if (!localReport.publishable || localReport.externallyUnverified) return undefined
    const active = await repository.resolveActive({
      workspaceId: scope.workspaceId,
      platform: scope.platform,
      productId: product.id,
      productVersion: product.version ?? 1,
      mappedPayloadHash: localReport.mappedPayloadHash,
      remoteSnapshotHash: approval.remoteSnapshotHash,
      schemaVersion: approval.schemaVersion,
      schemaEvidenceHash: approval.schemaEvidenceHash,
      mappingVersion: approval.mappingVersion,
      mappingEvidenceHash: approval.mappingEvidenceHash,
    })
    if (!active) return undefined
    return sourceProductId === 'local' ? localGate : buildConnectorMappingGate({ approval: active, product, sourceProductId: scope.remoteId!, fields, category })
  }
  return {
    sync: async ({ platform, context, rawProduct }) => {
      if (!platformGovernanceGatesRequired()) return undefined
      const fields: Record<string, unknown> = {
        title: rawProduct.title,
        description: rawProduct.description,
        price: rawProduct.price,
        stock: rawProduct.stock,
        category: rawProduct.category,
        merchantSourcePayloadSha256: createHash('sha256').update(canonicalJson({ remoteId: rawProduct.remoteId, title: rawProduct.title, description: rawProduct.description, price: rawProduct.price, stock: rawProduct.stock, sku: rawProduct.sku, images: rawProduct.images, category: rawProduct.category, attributes: rawProduct.attributes, platformFields: rawProduct.platformFields, listingStatus: rawProduct.listingStatus ?? null })).digest('hex'),
      }
      if (rawProduct.listingStatus !== undefined) fields.listingStatus = rawProduct.listingStatus
      for (const [field, value] of Object.entries(rawProduct.platformFields)) {
        if (Object.hasOwn(fields, field)) return undefined
        fields[field] = value
      }
      const gate = await activeGate({ workspaceId: context.workspaceId, platform, accountId: context.accountId, remoteId: rawProduct.remoteId }, fields, 'remote', rawProduct.category)
      if (!gate) throw new ConnectorMappingPreflightError('sync', `mapping preflight approval is missing, stale, expired, or outside ${platform}/${context.accountId}/${rawProduct.remoteId}`)
      return gate
    },
    write: async ({ platform, context, fields, remoteId, operation }) => {
      if (operation !== 'update' || !remoteId) return undefined
      const gateInput = await activeGate({ workspaceId: context.workspaceId, platform, accountId: context.accountId, remoteId }, fields, 'local', typeof fields.category === 'string' ? fields.category : '')
      return gateInput ? { gateInput } : undefined
    },
  }
}

const connectorMappingPreflight = createApiConnectorMappingPreflightAdapter({
  repository: platformMappingApprovalRepository,
  findProduct: ({ workspaceId, platform, accountId, remoteId }) => {
    if (!remoteId) return undefined
    const matches = service.listProducts(workspaceId, { platform, accountId, remoteProductId: remoteId })
    return matches.length === 1 ? matches[0] : undefined
  },
})
export const connectorRuntime = new ConnectorRuntime({ fixtureMode, allowFixtureWrites: process.env.PLUGIN_WRITE_ENABLED === 'true', credentialProvider: createVaultCredentialProviderFromEnv(), mappingPreflight: connectorMappingPreflight, environment: process.env.NODE_ENV === 'production' ? 'production' : process.env.NODE_ENV === 'test' ? 'test' : 'development' })
export const oauthStates = new OAuthStateStore()
const redisOAuthPort = createRedisOAuthPort(process.env.REDIS_URL)
const oauthStateStore = redisOAuthPort ? new RedisOAuthStateStore(redisOAuthPort) : oauthStates
const redisRateLimit = createRedisRateLimit(process.env.REDIS_URL)
const redisOidcNonce = createRedisOidcNonce(process.env.REDIS_URL)
const redisAssetScannerNonce = createRedisAssetScannerNonce(process.env.REDIS_URL)
const redisWorkerNonce = createRedisWorkerNonce(process.env.REDIS_URL)
const redisHealth = createRedisHealth(process.env.REDIS_URL)
const redisJobAdmission = createRedisJobAdmission(process.env.REDIS_URL)
const redisAutomationLease = createRedisAutomationLease(process.env.REDIS_URL)
let redisRateLimitHealth: { state: 'not_configured' | 'configured' | 'ready' | 'degraded'; lastFailureAt?: string } = { state: redisRateLimit ? 'configured' : 'not_configured' }
const localAutomationLeases = new Map<string, { token: string; expiresAt: number }>()
type JsonObject = Record<string, unknown>

type BatchProduct = { id: string; workspaceId: string; version?: number }
type BatchProductWrite = { product: BatchProduct; version: number }

export function rollbackBatchProducts(products: Map<string, BatchProduct>, workspaceId: string, writes: readonly BatchProductWrite[], before: ReadonlyMap<string, BatchProduct>): void {
  for (const write of writes) {
    const current = products.get(write.product.id)
    // A concurrent request may have replaced or mutated this product after the
    // batch write. Preserve the newer state instead of rolling it back as if it
    // belonged to the failed batch.
    if (current !== write.product || current.workspaceId !== workspaceId || current.version !== write.version) continue
    const previous = before.get(write.product.id)
    if (previous) products.set(write.product.id, previous)
    else products.delete(write.product.id)
  }
}

function brandUnitsFor(workspaceId: string) {
  return [...brandUnits.values()].filter(unit => unit.workspaceId === workspaceId)
}

function requireBrandUnit(workspaceId: string, brandId: string) {
  const unit = brandUnits.get(brandId)
  if (!unit || unit.workspaceId !== workspaceId) throw new DomainError('BRAND_UNIT_NOT_FOUND', '品不存在或不属于当前工作区', 404, { brand_id: brandId })
  return unit
}

function parseCampaignProductIds(value: unknown) {
  if (typeof value !== 'string') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'product_ids_json 必须是 1 至 50 个商品 ID 的 JSON 数组', 400)
  let parsed: unknown
  try { parsed = JSON.parse(value) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'product_ids_json 必须是 1 至 50 个商品 ID 的 JSON 数组', 400) }
  if (!Array.isArray(parsed) || parsed.length < 1 || parsed.length > 50 || parsed.some(item => typeof item !== 'string' || !item.trim()) || new Set(parsed).size !== parsed.length) {
    throw new DomainError('CAMPAIGN_PRODUCT_LIMIT', 'product_ids_json 必须是 1 至 50 个不重复商品 ID 的 JSON 数组', 400)
  }
  return parsed.map(item => String(item).trim())
}

function parseCampaignTargets(value: unknown) {
  if (typeof value !== 'string') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'targets_json 必须是 1 至 50 个目标的 JSON 数组', 400)
  let parsed: unknown
  try { parsed = JSON.parse(value) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'targets_json 必须是有效 JSON 数组', 400) }
  if (!Array.isArray(parsed) || parsed.length < 1 || parsed.length > 50) throw new DomainError('CAMPAIGN_TARGET_LIMIT', 'targets_json 必须包含 1 至 50 个目标', 400)
  return parsed.map((item, index) => {
    if (!item || typeof item !== 'object' || (typeof (item as Record<string, unknown>).product_id !== 'string' && typeof (item as Record<string, unknown>).canonical_product_id !== 'string') || typeof (item as Record<string, unknown>).platform !== 'string' || !SUPPORTED_PLATFORMS.includes((item as Record<string, unknown>).platform as Platform) || typeof (item as Record<string, unknown>).account_id !== 'string' || !(item as Record<string, unknown>).account_id) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `第 ${index + 1} 个批量目标必须包含 product_id 或 canonical_product_id，以及 platform、account_id`, 400)
    const target = item as Record<string, unknown>
    return { productId: typeof target.product_id === 'string' ? target.product_id.trim() : '', platform: target.platform as Platform, accountId: String(target.account_id).trim(), ...(typeof target.canonical_product_id === 'string' && target.canonical_product_id.trim() ? { canonicalProductId: target.canonical_product_id.trim() } : {}), ...(typeof target.listing_id === 'string' && target.listing_id.trim() ? { listingId: target.listing_id.trim() } : {}) }
  })
}

type ExecutionModality = 'content' | 'image' | 'image_edit' | 'ocr' | 'video'

/**
 * Keep merchant-facing execution truth at the API boundary. Application
 * services intentionally have deterministic local fallbacks for tests and
 * fixture workflows, so a completed result is not sufficient evidence that a
 * provider was called.
 */
export function executionContract(modality: ExecutionModality, providerExecuted: boolean, providerKind?: string) {
  const labels: Record<ExecutionModality, { provider: string; simulated: string }> = {
    content: { provider: '已由配置的内容模型生成', simulated: '本地演示文案，未调用内容模型' },
    image: { provider: '已由配置的图片模型生成', simulated: '本地演示图片，未调用图片模型' },
    image_edit: { provider: '已由配置的图片编辑模型生成', simulated: '本地演示图片编辑结果，未调用图片编辑模型' },
    ocr: { provider: '已由配置的 OCR 模型解析', simulated: '本地解析器结果，未调用 OCR 模型' },
    video: { provider: '已由配置的视频模型执行', simulated: '视频脚本/分镜演示结果，未调用视频模型' },
  }
  const label = providerExecuted ? labels[modality].provider : labels[modality].simulated
  return {
    mode: providerExecuted ? 'provider' as const : 'simulated' as const,
    simulated: !providerExecuted,
    providerExecuted,
    ...(providerKind ? { providerKind } : {}),
    label,
    message: label,
  }
}

async function contentExecutionEvidence(workspaceId: string, actionId: string) {
  await persistenceReady
  const rows = await (persistence.modelUsage ?? memoryModelUsage).listByAction(workspaceId, actionId)
  const receipt = rows
    .filter(item => item.modality === 'text')
    .slice()
    .sort((left, right) => right.revision - left.revision || Date.parse(right.observedAt) - Date.parse(left.observedAt))[0]
  const usage = receipt && (receipt.inputTokens !== undefined || receipt.outputTokens !== undefined || receipt.totalTokens !== undefined)
    ? {
        ...(receipt.inputTokens !== undefined ? { inputTokens: receipt.inputTokens } : {}),
        ...(receipt.outputTokens !== undefined ? { outputTokens: receipt.outputTokens } : {}),
        ...(receipt.totalTokens !== undefined ? { totalTokens: receipt.totalTokens } : {}),
      }
    : undefined
  const settled = Boolean(
    receipt
    && receipt.settlementStatus === 'settled'
    && receipt.providerRequestId?.trim()
    && usage
    && receipt.costCny !== undefined,
  )
  return {
    ...executionContract('content', settled, receipt?.model),
    ...(receipt?.providerRequestId ? { providerRequestId: receipt.providerRequestId } : {}),
    ...(usage ? { usage } : {}),
    ...(receipt?.costCny !== undefined ? { costCny: receipt.costCny } : {}),
    ...(receipt ? { settlementStatus: receipt.settlementStatus, actionId } : {}),
  }
}

function controlledRelayEnvironment() {
  return ['staging', 'preview', 'production'].includes(process.env.NODE_ENV ?? '')
}

function durableContentGenerationEnvironment() {
  return isProduction() || process.env.LOCAL_COMPOSE === 'true'
}

async function requireSettledContentExecutionEvidence(workspaceId: string, actionId: string) {
  const execution = await contentExecutionEvidence(workspaceId, actionId)
  if (controlledRelayEnvironment() && execution.providerExecuted !== true) {
    throw new DomainError('MODEL_RELAY_EVIDENCE_REQUIRED', '中转模型回执尚未完成，内容暂不能交付', 409, {
      operation_status: 'pending_receipt',
      action_id: actionId,
      missing: ['provider_request_id', 'usage', 'cost_cny', 'settlement'],
    })
  }
  return execution
}

export function taskContextLinkId(taskId: string, envelope: Record<string, unknown>) {
  return `context_link_${taskId}_${contextEnvelopeHash(envelope).slice(0, 16)}`
}
type SnapshotInput = { entityType: BusinessEntityType; entityId: string; entityVersion: number; payload: Record<string, unknown> }
type TrustedScanPromotionPersistenceInput = {
  workspaceId: string
  receipt: AppendAssetScanReceiptInput
  snapshots: SnapshotInput[]
  aggregateId: string
  eventType: string
  sequence: number
  eventPayload: Record<string, unknown>
  cleanup: Omit<AssetPromotionCleanupBinding, 'readyOutboxEventId'>
}

function createRedisOAuthPort(url: string | undefined): OAuthRedisPort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async set(key, value, ttlSeconds) { await withRedisOperationTimeout(ready); await withRedisOperationTimeout(client.set(key, value, { EX: ttlSeconds })) },
    async get(key) { await withRedisOperationTimeout(ready); return await withRedisOperationTimeout(client.get(key)) },
    async eval(script, keys, args) { await withRedisOperationTimeout(ready); return await withRedisOperationTimeout(client.eval(script, { keys, arguments: args })) },
  }
}

interface RedisRateLimitPort { increment(key: string, ttlSeconds: number): Promise<number> }
interface RedisOidcNoncePort { consume(nonce: string, ttlSeconds: number): Promise<boolean> }
interface RedisHealthPort {
  ping(): Promise<void>
  scannerHeartbeats(nowEpochMs: number): Promise<unknown[]>
}

function createRedisHealth(url: string | undefined): RedisHealthPort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async ping() {
      await withRedisOperationTimeout(ready)
      await withRedisOperationTimeout(client.ping())
    },
    async scannerHeartbeats(nowEpochMs) {
      await withRedisOperationTimeout(ready)
      const keys = await withRedisOperationTimeout(client.zRangeByScore(SCANNER_HEARTBEAT_INDEX_KEY, `(${Math.floor(nowEpochMs)}`, '+inf'))
      if (keys.length === 0) return []
      return (await withRedisOperationTimeout(client.mGet(keys))).flatMap(value => {
        if (!value) return []
        try { return [JSON.parse(value) as unknown] } catch { return [] }
      })
    },
  }
}

function createRedisOidcNonce(url: string | undefined): RedisOidcNoncePort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async consume(nonce, ttlSeconds) {
      await withRedisOperationTimeout(ready)
      const result = await withRedisOperationTimeout(client.set(`merchant:oidc-nonce:${createHash('sha256').update(nonce).digest('hex')}`, '1', { NX: true, EX: ttlSeconds }))
      return result === 'OK'
    },
  }
}

function createRedisAssetScannerNonce(url: string | undefined): RedisOidcNoncePort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async consume(nonce, ttlSeconds) {
      await withRedisOperationTimeout(ready)
      const digest = createHash('sha256').update(nonce).digest('hex')
      const result = await withRedisOperationTimeout(client.set(`merchant:asset-scanner-nonce:${digest}`, '1', { NX: true, EX: ttlSeconds }))
      return result === 'OK'
    },
  }
}

function createRedisWorkerNonce(url: string | undefined): RedisOidcNoncePort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async consume(nonce, ttlSeconds) {
      await withRedisOperationTimeout(ready)
      const digest = createHash('sha256').update(nonce).digest('hex')
      const result = await withRedisOperationTimeout(client.set(`merchant:worker-nonce:${digest}`, '1', { NX: true, EX: ttlSeconds }))
      return result === 'OK'
    },
  }
}

function createRedisRateLimit(url: string | undefined): RedisRateLimitPort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async increment(key, ttlSeconds) {
      await withRedisOperationTimeout(ready)
      const result = await withRedisOperationTimeout(client.eval(`
        local count = redis.call('INCR', KEYS[1])
        if count == 1 then redis.call('EXPIRE', KEYS[1], ARGV[1]) end
        return count
      `, { keys: [key], arguments: [String(ttlSeconds)] }))
      return Number(result)
    },
  }
}

interface RedisJobAdmissionPort {
  acquire(workspaceId: string, reservationId: string, limit: number, ttlSeconds: number): Promise<'owned' | 'existing' | 'quota'>
  release(workspaceId: string, reservationId: string): Promise<void>
}

interface RedisAutomationLeasePort {
  acquire(key: string, token: string, ttlMs: number): Promise<boolean>
  renew(key: string, token: string, ttlMs: number): Promise<boolean>
  release(key: string, token: string): Promise<void>
}

function createRedisAutomationLease(url: string | undefined): RedisAutomationLeasePort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async acquire(key, token, ttlMs) {
      await withRedisOperationTimeout(ready)
      const result = await withRedisOperationTimeout(client.eval(`
        if redis.call('SET', KEYS[1], ARGV[1], 'NX', 'PX', ARGV[2]) then return 1 end
        return 0
      `, { keys: [key], arguments: [token, String(ttlMs)] }))
      return Number(result) === 1
    },
    async renew(key, token, ttlMs) {
      await withRedisOperationTimeout(ready)
      const result = await withRedisOperationTimeout(client.eval(`
        if redis.call('GET', KEYS[1]) == ARGV[1] then return redis.call('PEXPIRE', KEYS[1], ARGV[2]) end
        return 0
      `, { keys: [key], arguments: [token, String(ttlMs)] }))
      return Number(result) === 1
    },
    async release(key, token) {
      await withRedisOperationTimeout(ready)
      await withRedisOperationTimeout(client.eval(`
        if redis.call('GET', KEYS[1]) == ARGV[1] then redis.call('DEL', KEYS[1]) end
        return 1
      `, { keys: [key], arguments: [token] }))
    },
  }
}

function admissionKey(workspaceId: string, reservationId: string) {
  return createHash('sha256').update(`${workspaceId}\n${reservationId}`).digest('hex')
}

function createRedisJobAdmission(url: string | undefined): RedisJobAdmissionPort | undefined {
  if (!url?.trim()) return undefined
  const client = createClient(redisClientOptions(url.trim()))
  client.on('error', () => undefined)
  const ready = client.connect()
  return {
    async acquire(workspaceId, reservationId, limit, ttlSeconds) {
      await withRedisOperationTimeout(ready)
      const digest = admissionKey(workspaceId, reservationId)
      const result = await withRedisOperationTimeout(client.eval(`
        local reservation = KEYS[1]
        local counter = KEYS[2]
        if redis.call('EXISTS', reservation) == 1 then return 2 end
        local current = tonumber(redis.call('GET', counter) or '0')
        if current >= tonumber(ARGV[1]) then return 0 end
        redis.call('SET', reservation, '1', 'EX', ARGV[2])
        redis.call('INCR', counter)
        redis.call('EXPIRE', counter, ARGV[2])
        return 1
      `, { keys: [`merchant:job-admission:${digest}`, `merchant:job-admission-count:${createHash('sha256').update(workspaceId).digest('hex')}`], arguments: [String(limit), String(ttlSeconds)] }))
      return Number(result) === 1 ? 'owned' : Number(result) === 2 ? 'existing' : 'quota'
    },
    async release(workspaceId, reservationId) {
      await withRedisOperationTimeout(ready)
      const digest = admissionKey(workspaceId, reservationId)
      await withRedisOperationTimeout(client.eval(`
        if redis.call('DEL', KEYS[1]) == 1 then
          local current = tonumber(redis.call('DECR', KEYS[2]) or '0')
          if current <= 0 then redis.call('DEL', KEYS[2]) end
        end
        return 1
      `, { keys: [`merchant:job-admission:${digest}`, `merchant:job-admission-count:${createHash('sha256').update(workspaceId).digest('hex')}`], arguments: [] }))
    },
  }
}

export interface ApiPersistence {
  mode: 'memory' | 'postgres'
  outbox?: OutboxRepository
  business?: PostgresBusinessRepository
  billing?: PostgresBillingRepository
  commercial?: import('../../../packages/persistence/src/index.js').CommercialRepository
  usage?: UsageRepository
  modelUsage?: ModelUsageRepository
  actionLedger?: ActionLedgerRepository
  entitlements?: EntitlementRepository
  operations?: OperationsRepository
  subscriptions?: SubscriptionRepository
  commercialExtensions?: CommercialExtensionsRepository
  growth?: GrowthRepository
  alerts?: OperationalAlertsRepository
  dataLifecycle?: DataLifecycleRepository
  members?: MembersRepository
  rules?: RuleRepositoryPort
  brandUnits?: import('../../../packages/persistence/src/index.js').BrandUnitRepository
  objectOrphans?: ObjectOrphanRepository
  contextSnapshots?: ContextSnapshotRepository
  identities?: IdentityLifecycleRepository
  authorization?: AuthorizationRepository
  paymentCallbackNonces?: PaymentCallbackNonceRepository
  support?: SupportRepository
  supportSlaReporting?: SupportSlaReportingRepository
  incidents?: IncidentRepository
  featureFlags?: FeatureFlagsRepository
  financeSearch?: FinanceSearchRepository
  auditCenter?: AuditCenterRepository
  opsData?: import('../../../packages/persistence/src/index.js').OpsDataRepository
  workspaceBootstrap?: WorkspaceBootstrapRepository
  assetParse?: AssetParseRepository
  assetScanReceipts?: AssetScanReceiptRepository
  assetScanRedrive?: AssetScanRedriveRepository
  assetPromotionCleanup?: AssetPromotionCleanupRepository
  imageContinuationLeases?: ImageContinuationLeaseRepository
  imageGenerationExecutions?: ImageGenerationExecutionRepository
  reconciliationEvidence?: ReconciliationEvidenceRepository
  unifiedLinkAudit?: UnifiedLinkAuditRepository
  platformMediaSpecs?: PlatformMediaSpecRepository
  mappingPreflightApprovals?: MappingPreflightApprovalRepository
  knowledgeHydration?: KnowledgeHydrationRepository
  storageQuota?: StorageQuotaRepository
  /** Read-only, redacted reconciliation snapshots; object inventory remains outside the API response. */
  storageReconciliation?: ReconciliationStatusStore
  reconciliationStatuses?: ReconciliationStatusRepository
  canonicalBackfillRuns?: CanonicalBackfillRunRepository
  executeCanonicalBackfill?: (input: { workspaceId: string; dryRun?: boolean; afterProductId?: string; limit?: number }) => Promise<CanonicalBackfillResult>
  canonicalBackfillConflicts?: CanonicalBackfillConflictRepository
  canonicalBackfillRemediation?: PostgresCanonicalBackfillRemediationRepository
  interactiveConfirmationTickets?: InteractiveConfirmationTicketRepository
  persistSnapshotAndEvent?: (input: { workspaceId: string; entityType: BusinessEntityType; entityId: string; entityVersion: number; payload: Record<string, unknown>; eventType: string; eventPayload: Record<string, unknown> }) => Promise<void>
  persistSnapshotsAndEvent?: (input: { workspaceId: string; snapshots: SnapshotInput[]; aggregateId: string; eventType: string; sequence: number; eventPayload: Record<string, unknown> }) => Promise<void>
  persistPublishTransaction?: (input: { workspaceId: string; snapshots: SnapshotInput[]; aggregateId: string; eventType: string; sequence: number; eventPayload: Record<string, unknown>; finalizeTicketInTransaction: (client: SqlClient) => Promise<void> }) => Promise<void>
  persistTrustedScanPromotion?: (input: TrustedScanPromotionPersistenceInput) => Promise<AssetPromotionCleanupTask>
  ensureWorkspace?: (workspaceId: string) => Promise<void>
  listWorkspaceIds?: () => Promise<string[]>
  listWorkspaceSummaries?: () => Promise<Array<{ workspaceId: string; status: 'active' | 'disabled'; planName: string; monthlyPriceCny: number; usedTasks: number; includedTasks: number; subscriptionStatus: string; memberCount: number }>>
  listWorkspaceDirectory?: (query: { query?: string; status?: 'active' | 'disabled'; subscriptionStatus?: string; offset: number; limit: number }) => Promise<{ items: Array<{ workspaceId: string; status: 'active' | 'disabled'; planName: string; monthlyPriceCny: number; usedTasks: number; includedTasks: number; subscriptionStatus: string; memberCount: number }>; total: number; offset: number; limit: number; hasMore: boolean }>
  getWorkspaceStatus?: (workspaceId: string) => Promise<'active' | 'disabled'>
  setWorkspaceStatus?: (workspaceId: string, status: 'active' | 'disabled') => Promise<void>
  checkHealth?: () => Promise<void>
  close?: () => Promise<void>
}

/** Keep Ops storage status useful without turning reconciliation into an object browser. */
export function redactStorageReconciliation(report?: ReconciliationReport) {
  if (!report) return { status: 'unavailable' as const, message: '尚未收到对象清单对账结果' }
  const findingCounts: Record<string, number> = {}
  for (const finding of report.findings) findingCounts[finding.code.toLowerCase()] = (findingCounts[finding.code.toLowerCase()] ?? 0) + 1
  const parsedRunAt = report.lastRunAt ? Date.parse(report.lastRunAt) : NaN
  const ageMs = Number.isFinite(parsedRunAt) ? Date.now() - parsedRunAt : NaN
  const freshness = !Number.isFinite(ageMs) ? 'unknown' : ageMs > 72 * 60 * 60 * 1000 ? 'expired' : ageMs > 24 * 60 * 60 * 1000 ? 'stale' : 'fresh'
  const failed = report.runStatus === 'failed'
  return { status: failed ? 'failed' as const : report.status, runStatus: report.runStatus ?? 'succeeded' as const, errorMessage: failed ? '对账提供方失败，请检查对象清单、数据库和定时任务后重试。' : undefined, lastRunAt: report.lastRunAt ?? null, freshness, freshnessAfterMinutes: 24 * 60, quota: { ...report.quota }, counts: { ...report.counts }, findingCounts }
}

export interface RuleRepositoryPort {
  list(workspaceId: string, packId?: string): Promise<PersistedRuleVersion[]>
  insertVersion(input: Omit<PersistedRuleVersion, 'createdAt' | 'updatedAt'> & { createdAt?: string; updatedAt?: string }): Promise<PersistedRuleVersion>
  appendAudit(input: PersistedRuleAudit): Promise<PersistedRuleAudit>
  listAudit(workspaceId: string, packId?: string): Promise<PersistedRuleAudit[]>
  updateStatus(input: { workspaceId: string; id: string; status: string; revision: number; updatedAt?: string; activatedAt?: string | null; deactivatedAt?: string | null }): Promise<PersistedRuleVersion>
  insertVersionWithAudit?(input: { version: Omit<PersistedRuleVersion, 'createdAt' | 'updatedAt'> & { createdAt?: string; updatedAt?: string }; audit: PersistedRuleAudit }): Promise<{ version: PersistedRuleVersion; audit: PersistedRuleAudit }>
  transitionStatusWithAudit?(input: { workspaceId: string; packId: string; targetId: string; status: string; actorId: string; reason: string; occurredAt: string; targetAuditId: string; currentAuditId?: string; auditData?: Record<string, unknown> }): Promise<{ version: PersistedRuleVersion; audits: PersistedRuleAudit[] }>
}

const memoryWorkspaceStatuses = new Map<string, 'active' | 'disabled'>()
const knownWorkspaces = new Set<string>()
const memoryCommercial = new MemoryCommercialRepository()
const memoryUsage = new MemoryUsageRepository(workspaceId => memoryCommercial.getSettings(workspaceId))
const memoryModelUsage = new MemoryModelUsageRepository()
const memoryActionLedger = new MemoryActionLedgerRepository()
const memoryEntitlements = new MemoryEntitlementRepository()
const memoryOperations = new MemoryOperationsRepository()
const memorySubscriptions = new MemorySubscriptionRepository()
const memoryPaymentCallbackNonces = new MemoryPaymentCallbackNonceRepository()
const memoryMembers = new MemoryMembersRepository()
const memoryCommercialExtensions = new MemoryCommercialExtensionsRepository()
const memoryGrowth = new MemoryGrowthRepository()
const memoryAlerts = new MemoryOperationalAlertsRepository()
const memoryDataLifecycle = new MemoryDataLifecycleRepository()
const memoryBrandUnits = new MemoryBrandUnitRepository()
memoryBrandUnits.setConsistencyProjections({
  legacyProducts: () => [...service.products.values()].map(product => ({ id: product.id, workspaceId: product.workspaceId, ...(typeof (product as Product & { brandId?: unknown }).brandId === 'string' ? { brandId: (product as Product & { brandId: string }).brandId } : {}), ...(product.platform ? { platform: product.platform } : {}), ...(product.accountId ? { accountId: product.accountId } : {}), ...(product.sourceAssetIds?.length ? { sourceAssetIds: [...product.sourceAssetIds] } : {}) })),
  tasks: () => [...service.tasks.values()].map(task => ({ id: task.id, workspaceId: task.workspaceId, productId: task.productId, ...(task.brandId ? { brandId: task.brandId } : {}), ...(task.canonicalProductId ? { canonicalProductId: task.canonicalProductId } : {}), ...(task.listingId ? { listingId: task.listingId } : {}), ...(task.campaignItemId ? { campaignItemId: task.campaignItemId } : {}), platform: task.platform, ...(task.accountId ? { accountId: task.accountId } : {}) })),
  publishJobs: () => [...service.publishJobs.values()].map(job => ({ id: job.id, workspaceId: job.workspaceId, taskId: job.taskId, platform: job.platform, ...(job.accountId ? { accountId: job.accountId } : {}), ...(job.canonicalBinding?.canonicalProductId ? { canonicalProductId: job.canonicalBinding.canonicalProductId } : {}), ...(job.canonicalBinding?.listingId ? { listingId: job.canonicalBinding.listingId } : {}) })),
  assetBindings: () => [...service.products.values()].flatMap(product => (product.sourceAssetIds ?? []).map(assetId => { const asset = service.assets.get(assetId); return { workspaceId: product.workspaceId, productId: product.id, assetId, assetRole: 'source' as const, status: 'active' as const, assetExists: Boolean(asset), ...(asset ? { scanStatus: asset.scanStatus, rightsStatus: asset.rightsStatus } : {}) } })),
})
const memoryObjectOrphans = new MemoryObjectOrphanRepository()
const memoryContextSnapshots = new MemoryContextSnapshotRepository()
const memoryIdentities = new MemoryIdentityLifecycleRepository()
const memoryAuthorization = new MemoryAuthorizationRepository()
let authorizationRepositoryOverride: AuthorizationRepository | undefined

export function setAuthorizationRepositoryForTests(repository?: AuthorizationRepository) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('AUTHORIZATION_REPOSITORY_OVERRIDE_TEST_ONLY')
  authorizationRepositoryOverride = repository
}

function authorizationRepository() { return authorizationRepositoryOverride ?? persistence.authorization }
const memorySupport = new MemorySupportRepository()
const memorySupportSlaReporting = new MemorySupportSlaReportingRepository()
const memoryIncidents = new MemoryIncidentRepository()
const memoryFeatureFlags = new MemoryFeatureFlagsRepository()
const memoryAssetParse = new MemoryAssetParseRepository()
const memoryAssetScanReceipts = new MemoryAssetScanReceiptRepository()
const memoryAssetPromotionCleanup = new MemoryAssetPromotionCleanupRepository()
const memoryImageContinuationLeases = new MemoryImageContinuationLeaseRepository()
const memoryPlatformMediaSpecs = new MemoryPlatformMediaSpecRepository()
const memoryMappingPreflightApprovals = new MemoryMappingPreflightApprovalRepository()
const memoryKnowledgeHydration = new MemoryKnowledgeHydrationRepository()
const memoryStorageQuota = new MemoryStorageQuotaRepository()
const memoryStorageReconciliation = new MemoryReconciliationStatusStore()
const memoryReconciliationStatuses = new MemoryReconciliationStatusRepository()

/** Durable projection adapter: Ops may read status, never object keys or payloads. */
class DurableReconciliationStatusStore implements ReconciliationStatusStore {
  constructor(private readonly repository: ReconciliationStatusRepository) {}
  async put(report: ReconciliationReport) {
    await this.repository.upsert({
      workspaceId: report.workspaceId,
      resourceType: 'storage_inventory',
      resourceId: 'workspace',
      status: report.runStatus === 'failed' ? 'failed' : report.status === 'clean' ? 'succeeded' : 'manual_attention',
      idempotencyKey: `storage-reconciliation:${report.lastRunAt ?? 'unknown'}`,
      details: report as unknown as Record<string, unknown>,
      observedAt: report.lastRunAt,
    })
  }
  async get(workspaceId: string) {
    const record = await this.repository.getLatest({ workspaceId, resourceType: 'storage_inventory', resourceId: 'workspace' })
    return record?.details as unknown as ReconciliationReport | undefined
  }
  async list(workspaceId: string) {
    const records = await this.repository.list({ workspaceId, resourceType: 'storage_inventory' })
    return records.map(record => record.details as unknown as ReconciliationReport)
  }
}

/** The worker has no database or object-store credentials. It requests one
 * workspace-scoped run and the API assembles both sides inside its trust
 * boundary. */
async function runWorkspaceStorageReconciliation(workspaceId: string) {
  const inventory = async (): Promise<readonly ObjectInventoryEntry[]> => (await getAssetStorage().list(workspaceId)).map(item => ({ workspaceId: item.workspaceId, storageKey: item.key, sha256: item.sha256, sizeBytes: item.sizeBytes }))
  const references = async (): Promise<readonly DurableObjectReference[]> => {
    const snapshots = persistence.business
      ? await persistence.business.loadWorkspace(workspaceId, { excludeEntityTypes: ['product', 'task', 'content_version', 'publish_job', 'publish_batch', 'platform_account', 'generation_job', 'image_generation_job', 'brand_profile', 'feedback', 'sync_job', 'automation_policy'] })
      : []
    // In Postgres, an empty asset result is authoritative. Falling back to the
    // in-memory service here could silently hide durable deletions or report a
    // stale subset when hydration was intentionally bounded.
    const assets = persistence.business ? snapshots.filter(snapshot => snapshot.entityType === 'asset') : service.listAssets(workspaceId)
    return assets.flatMap(asset => {
      const payload = 'payload' in asset ? asset.payload : asset
      const storageKey = typeof payload.storageKey === 'string' ? payload.storageKey.trim() : ''
      const sha256 = typeof payload.sha256 === 'string' ? payload.sha256.trim() : ''
      const sizeBytes = payload.sizeBytes
      if (!storageKey) return []
      if (!/^[a-f0-9]{64}$/iu.test(sha256) || !Number.isSafeInteger(sizeBytes) || Number(sizeBytes) < 0) throw new DomainError('STORAGE_REFERENCE_INVALID', '素材持久化引用缺少合法 sha256 或 sizeBytes，已阻止对账写入', 503)
      const assetId = 'entityId' in asset ? asset.entityId : typeof payload.id === 'string' ? payload.id : ''
      if (!assetId) throw new DomainError('STORAGE_REFERENCE_INVALID', '素材持久化引用缺少 asset_id，已阻止对账写入', 503)
      return [{ workspaceId, assetId, storageKey, sha256, sizeBytes: Number(sizeBytes) }]
    })
  }
  const quota = persistence.storageQuota ? await persistence.storageQuota.getSnapshot(workspaceId) : undefined
  const status = persistence.storageReconciliation ?? memoryStorageReconciliation
  const result = await runReconciliationCycle({
    workspaces: [workspaceId],
    inventory: { list: inventory },
    references: { list: references },
    ...(quota ? { quota: { get: async () => ({ limitBytes: quota.limitBytes, reservedBytes: quota.reservedBytes }) } } : {}),
    status,
  })
  return result.reports[0] ?? { workspaceId, status: 'attention_required' as const, runStatus: 'failed' as const, error: { code: 'STORAGE_RECONCILIATION_EMPTY', message: '对象对账未生成报告' }, quota: { reservedBytes: 0, usedBytes: 0, projectedBytes: 0 }, counts: { references: 0, inventoryObjects: 0, matched: 0, missing: 0, metadataMismatches: 0, orphans: 0, crossWorkspace: 0, duplicates: 0 }, findings: [] }
}

async function runSupportSlaScan(workspaceId: string, limit = 100) {
  const repository = persistence.support
  if (!repository) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置，SLA 扫描已阻断', 503)
  const tickets: Awaited<ReturnType<SupportRepository['list']>>['items'] = []
  let cursor: SupportTicketPageCursor | undefined
  do {
    const page = await repository.list({ workspaceId, limit: Math.min(100, limit), ...(cursor ? { cursor } : {}) })
    tickets.push(...page.items)
    cursor = page.nextCursor
  } while (cursor && tickets.length < limit)
  const actions = planSupportSlaScan(tickets.slice(0, limit).map(ticket => ({ workspaceId, ticketId: ticket.id, status: ticket.status, sla: ticket.sla })))
  const recorded: Array<{ ticketId: string; state: 'at_risk' | 'breached'; replayed: boolean }> = []
  for (const action of actions) {
    const ticket = tickets.find(candidate => candidate.id === action.ticketId)
    if (!ticket) continue
    const result = await repository.recordSlaAction({ workspaceId, ticketId: action.ticketId, state: action.state, dueAt: action.dueAt, expectedRevision: ticket.revision, actorId: 'worker:support-sla', idempotencyKey: action.idempotencyKey })
    recorded.push({ ticketId: action.ticketId, state: action.state, replayed: result.replayed })
    const alert = await (persistence.alerts ?? memoryAlerts).upsert({
      workspaceId,
      alertKey: `support-sla:${action.ticketId}:${action.state}:${action.dueAt}`,
      code: action.state === 'breached' ? 'SUPPORT_SLA_BREACHED' : 'SUPPORT_SLA_AT_RISK',
      severity: 'high',
      entityType: 'support_ticket',
      entityId: action.ticketId,
      title: action.state === 'breached' ? '客服工单已违反 SLA' : '客服工单即将违反 SLA',
      observedAt: new Date().toISOString(),
      evidence: { ticket_id: action.ticketId, state: action.state, due_at: action.dueAt, event_replayed: result.replayed },
      nextAction: action.state === 'breached' ? '立即检查负责人和暂停原因，补充人工处理记录并在解决后复盘。' : '立即分派客服负责人并完成有效人工响应，避免工单进入违约。',
    })
    // Alert persistence is authoritative; notification delivery is separately
    // recorded and must not turn a durable SLA event into a failed scan.
    void persistOperationalAlertNotification(alert)
  }
  return { workspaceId, checked: tickets.length, planned: actions.length, recorded }
}

async function buildWorkspaceSupportSlaMonthlyReport(input: { workspaceId: string; reportId: string; periodStart: string; periodEnd: string; cutoffAt: string }) {
  const support = persistence.support
  const reporting = persistence.supportSlaReporting ?? memorySupportSlaReporting
  if (!support) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置，SLA 月报已阻断', 503)
  const tickets: Awaited<ReturnType<SupportRepository['list']>>['items'] = []
  let cursor: SupportTicketPageCursor | undefined
  do {
    const page = await support.list({ workspaceId: input.workspaceId, limit: 100, ...(cursor ? { cursor } : {}) })
    tickets.push(...page.items)
    cursor = page.nextCursor
  } while (cursor)
  const reportTickets: SupportSlaReportTicket[] = []
  for (const ticket of tickets) {
    const events = await support.listEvents(input.workspaceId, ticket.id)
    reportTickets.push({ workspaceId: input.workspaceId, ticketId: ticket.id, sla: ticket.sla, events })
  }
  try {
    const report = buildSupportSlaMonthlyReport({ ...input, tickets: reportTickets })
    return report
  } catch (error) {
    if (error instanceof RangeError) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'SLA 月报的周期和截止时间必须是规范 UTC 时间且周期有效', 400)
    throw error
  }
}

async function generateSupportSlaMonthlyReport(input: { workspaceId: string; reportId: string; periodStart: string; periodEnd: string; cutoffAt: string }) {
  const report = await buildWorkspaceSupportSlaMonthlyReport(input)
  return (persistence.supportSlaReporting ?? memorySupportSlaReporting).createReport({ report })
}
const memoryWorkspaceBootstrap = new MemoryWorkspaceBootstrapRepository(memoryMembers, memoryOperations, workspaceId => {
  knownWorkspaces.add(workspaceId)
  memoryWorkspaceStatuses.set(workspaceId, 'active')
}, workspaceId => memoryWorkspaceStatuses.get(workspaceId) ?? 'active')
const memoryAuditCenter: AuditCenterRepository = {
  async list(query) {
    const operations = await memoryOperations.list(query.workspaceId, 5_000)
    return new MemoryAuditCenterRepository(operations.map(item => ({ id: item.id, source: 'operation' as const, workspace_id: item.workspaceId, actor_id: item.actorId, action: item.action, resource_type: item.resourceType, resource_id: item.resourceId, reason: item.reason, occurred_at: item.createdAt, evidence: { before: item.before, after: item.after } }))).list(query)
  },
  async detail(workspaceId, source, id) {
    if (source !== 'operation') return undefined
    const operations = await memoryOperations.list(workspaceId, 5_000)
    return new MemoryAuditCenterRepository(operations.map(item => ({ id: item.id, source: 'operation' as const, workspace_id: item.workspaceId, actor_id: item.actorId, action: item.action, resource_type: item.resourceType, resource_id: item.resourceId, reason: item.reason, occurred_at: item.createdAt, evidence: { before: item.before, after: item.after } }))).detail(workspaceId, source, id)
  },
  async exportRows(query, limit) {
    const operations = await memoryOperations.list(query.workspaceId, Math.min(5_001, limit + 1))
    return new MemoryAuditCenterRepository(operations.map(item => ({ id: item.id, source: 'operation' as const, workspace_id: item.workspaceId, actor_id: item.actorId, action: item.action, resource_type: item.resourceType, resource_id: item.resourceId, reason: item.reason, occurred_at: item.createdAt, evidence: { before: item.before, after: item.after } }))).exportRows(query, limit)
  },
}
const memoryCanonicalBackfillRuns = new MemoryCanonicalBackfillRunRepository()
const memoryCanonicalBackfillConflicts = new MemoryCanonicalBackfillConflictRepository()
const memoryInteractiveConfirmationTickets = new MemoryInteractiveConfirmationTicketRepository()
let imageSelectionTicketRepositoryOverride: InteractiveConfirmationTicketRepository | undefined

export function setImageSelectionTicketRepositoryForTests(repository?: InteractiveConfirmationTicketRepository) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('IMAGE_SELECTION_TICKET_REPOSITORY_OVERRIDE_TEST_ONLY')
  imageSelectionTicketRepositoryOverride = repository
}

const memoryPersistence: ApiPersistence = { mode: 'memory', commercial: memoryCommercial, usage: memoryUsage, modelUsage: memoryModelUsage, actionLedger: memoryActionLedger, entitlements: memoryEntitlements, operations: memoryOperations, subscriptions: memorySubscriptions, members: memoryMembers, commercialExtensions: memoryCommercialExtensions, growth: memoryGrowth, alerts: memoryAlerts, dataLifecycle: memoryDataLifecycle, brandUnits: memoryBrandUnits, objectOrphans: memoryObjectOrphans, contextSnapshots: memoryContextSnapshots, identities: memoryIdentities, authorization: memoryAuthorization, paymentCallbackNonces: memoryPaymentCallbackNonces, support: memorySupport, supportSlaReporting: memorySupportSlaReporting, incidents: memoryIncidents, featureFlags: memoryFeatureFlags, auditCenter: memoryAuditCenter, workspaceBootstrap: memoryWorkspaceBootstrap, assetParse: memoryAssetParse, assetScanReceipts: memoryAssetScanReceipts, assetPromotionCleanup: memoryAssetPromotionCleanup, imageContinuationLeases: memoryImageContinuationLeases, imageGenerationExecutions: new MemoryImageGenerationExecutionRepository(), reconciliationEvidence: new MemoryReconciliationEvidenceRepository(), unifiedLinkAudit: new MemoryUnifiedLinkAuditRepository(), platformMediaSpecs: memoryPlatformMediaSpecs, mappingPreflightApprovals: memoryMappingPreflightApprovals, knowledgeHydration: memoryKnowledgeHydration, storageQuota: memoryStorageQuota, storageReconciliation: memoryStorageReconciliation, reconciliationStatuses: memoryReconciliationStatuses, canonicalBackfillRuns: memoryCanonicalBackfillRuns, canonicalBackfillConflicts: memoryCanonicalBackfillConflicts, interactiveConfirmationTickets: memoryInteractiveConfirmationTickets }
let persistence: ApiPersistence = memoryPersistence
let persistenceError: unknown
const workspaceEventSequences = new Map<string, number>()
let assetStorage: ObjectStoragePort | undefined
let ruleRepositoryOverride: RuleRepositoryPort | undefined
const inMemoryTimelineEvents = new Map<string, OutboxEvent[]>()
type RechargeChannel = 'alipay' | 'wechat'
type RechargeState = 'pending' | 'paid' | 'closed' | 'failed'
interface RechargeOrder {
  id: string
  workspaceId: string
  channel: RechargeChannel
  amountFen: number
  state: RechargeState
  paymentMode: 'fixture' | 'provider'
  paymentUrl?: string
  providerTradeId?: string
  createdByActorId?: string
  createdAt: string
  updatedAt: string
}
interface WalletTransaction {
  id: string
  workspaceId: string
  type: 'recharge' | 'debit' | 'refund'
  amountFen: number
  orderId?: string
  actorId?: string
  description: string
  createdAt: string
}
const rechargeOrders = new Map<string, RechargeOrder>()
const rechargeIdempotency = new Map<string, string>()
const rechargeCreationInFlight = new Map<string, { channel: RechargeChannel; amountFen: number; actorId: string; promise: Promise<Record<string, unknown>> }>()
const subscriptionCreationInFlight = new Map<string, { intent: string; promise: Promise<Record<string, unknown>> }>()
const subscriptionChangeInFlight = new Map<string, { intent: string; promise: Promise<Record<string, unknown>> }>()
const merchantIntents = new Map<string, Record<string, unknown>>()
const walletTransactions: WalletTransaction[] = []
const modelBillingReservations = new Map<string, { debitIdempotencyKey: string; actorId: string; providerRequests: Set<string> }>()
type AutomationPolicy = { workspaceId: string; id: string; platform?: Platform; accountId?: string; enabled: boolean; mode: 'scan_alert_manual_retry' | 'scan_sync_alert_manual_retry'; syncEnabled: boolean; frequencyMinutes: number; retryLimit: number; windowStart?: string; windowEnd?: string; pauseReason?: string; lastRunAt?: string; nextRunAt?: string; /** Set while the scheduler has claimed work but has not recorded the resulting sync handle. */ claimedAt?: string; lastSyncJobId?: string; revision: number; updatedAt: string }
type AutomationRisk = { kind: string; product_id?: string; publish_job_id?: string; platform: Platform; account_id: string | null; message: string }
type AutomationRecommendation = { id: string; kind: string; priority: 'high' | 'medium'; title: string; action: string; method: string; parameters: Record<string, string>; execution: 'read_only' | 'interactive_confirmation'; requiresInteractiveConfirmation: boolean }
type PublishBatchItem = { taskId: string; platform: Platform; accountId?: string; contentVersionId?: string; confirmationHash?: string; remoteSnapshotHash?: string; state: 'prepared' | 'queued' | 'failed' | 'paused' | 'submitted' | 'published' | 'rejected' | 'unknown'; jobId?: string; error?: { code: string; message: string } }
type PublishBatch = { id: string; workspaceId: string; state: 'prepared' | 'queued' | 'paused' | 'partial' | 'completed' | 'failed'; items: PublishBatchItem[]; pauseReason?: string; createdAt: string; updatedAt: string; revision: number }
const automationPolicies = new Map<string, AutomationPolicy>()
const publishBatches = new Map<string, PublishBatch>()
const persistedPublishBatches = new Map<string, PublishBatch>()

function publicMoneyRecord<T extends { amountFen: number }>(record: T) {
  const { amountFen: _amountFen, ...withoutMinorUnit } = record
  return { ...withoutMinorUnit, amount_cny: (record.amountFen / 100).toFixed(2) }
}

function publicRechargeOrder(order: RechargeOrder) {
  return { id: order.id, workspace_id: order.workspaceId, channel: order.channel, amount_cny: (order.amountFen / 100).toFixed(2), state: order.state, payment_mode: order.paymentMode, payment_url: order.paymentUrl ?? null, provider_trade_id: order.providerTradeId ?? null, created_by_actor_id: order.createdByActorId ?? null, attribution_status: order.createdByActorId ? 'attributed' : 'legacy_unattributed', expires_at: null, paid_at: order.state === 'paid' ? order.updatedAt : null, created_at: order.createdAt, updated_at: order.updatedAt }
}

/** Prefix spreadsheet formula-like cells before emitting a user-downloadable CSV. */
export function csvCell(value: string) {
  const safe = /^[=+\-@]/u.test(value) ? `'${value}` : value
  return `"${safe.replace(/"/gu, '""')}"`
}

export async function readWorkspaceStatusInTransaction(pool: SqlPool, workspaceId: string): Promise<'active' | 'disabled'> {
  return withWorkspaceTransaction(pool, workspaceId, async client => {
    const row = await client.query<{ status: 'active' | 'disabled' }>('SELECT status FROM workspaces WHERE id = $1', [workspaceId])
    return row.rows[0]?.status ?? 'active'
  })
}

function parseCnyToFen(value: unknown) {
  if (typeof value !== 'string' || !/^\d{1,8}(?:\.\d{1,2})?$/u.test(value.trim())) throw new DomainError('BILLING_AMOUNT_INVALID', '充值金额必须是合法的人民币金额', 400)
  const [yuan, fraction = ''] = value.trim().split('.')
  const fen = Number(yuan) * 100 + Number((fraction + '00').slice(0, 2))
  if (!Number.isSafeInteger(fen) || fen < 100 || fen > 1_000_000_00) throw new DomainError('BILLING_AMOUNT_INVALID', '充值金额需在1元到100万元之间', 400)
  return fen
}

function walletBalanceFen(workspaceId: string) {
  return walletTransactions.filter(item => item.workspaceId === workspaceId).reduce((sum, item) => sum + (item.type === 'debit' ? -item.amountFen : item.amountFen), 0)
}

function actionKindForDescription(description: string): ActionKind {
  if (description.includes('同步')) return 'catalog_sync'
  if (description.includes('连接')) return 'platform_connect'
  if (description.includes('图片编辑') || description.includes('image_edit')) return 'image_edit'
  if (description.includes('创意预览')) return 'creative_preview'
  if (description.includes('SEO')) return 'seo'
  if (description.includes('Brief')) return 'brief'
  if (description.includes('发布')) return 'publish'
  if (description.includes('图片') || description.includes('图像') || description.includes('image')) return 'model_image'
  if (description.includes('OCR') || description.includes('解析') || description.includes('ocr')) return 'model_ocr'
  if (description.includes('视频') || description.includes('video')) return 'model_video'
  return 'model_text'
}

async function recordActionSettlement(input: { workspaceId: string; actionKey: string; actionKind: ActionKind; settlement: ActionSettlement; amountFen: number; actorId: string; description: string; taskId?: string; campaignItemId?: string; contextLinkId?: string; contextHash?: string; reservedAmountFen?: number; multiplier?: number; settlementStatus?: 'authorized' | 'pending_receipt' | 'settled' | 'released' | 'refunded' | 'manual_attention' }) {
  await persistenceReady
  if (!persistence.actionLedger) throw new Error('ACTION_LEDGER_NOT_CONFIGURED')
  return persistence.actionLedger.record({ ...input, units: 1 })
}

async function consumeEntitlement(input: { workspaceId: string; kind: EntitlementKind; actionKey: string; actionKind: ActionKind; actorId: string; description: string; modelRunKey?: string }) {
  await persistenceReady
  const modelRunKey = input.modelRunKey?.trim()
  const modality = modelRunKey ? modalityForActionKind(input.actionKind) : undefined
  if (modelRunKey && !modality) throw new DomainError('ENTITLEMENT_MODEL_ACTION_INVALID', '权益消费未绑定合法模型动作，已阻断上游请求', 503)
  let consumption
  try {
    consumption = await (persistence.entitlements ?? memoryEntitlements).consume({ workspaceId: input.workspaceId, kind: input.kind, units: 1, idempotencyKey: input.actionKey })
  } catch (error) {
    if ((error as { code?: string })?.code === 'ENTITLEMENT_CONSUMPTION_IDEMPOTENCY_CONFLICT' || String(error).includes('ENTITLEMENT_CONSUMPTION_IDEMPOTENCY_CONFLICT')) throw new DomainError('ENTITLEMENT_CONSUMPTION_IDEMPOTENCY_CONFLICT', '权益消费幂等键已绑定到其他消费意图，请换用新的幂等键', 409)
    throw error
  }
  if (consumption) {
    if (!modelRunKey) {
      await recordActionSettlement({ workspaceId: input.workspaceId, actionKey: input.actionKey, actionKind: input.actionKind, settlement: 'entitlement', amountFen: 0, actorId: input.actorId, description: input.description })
      return consumption
    }
    try {
      const policy = await (persistence.commercialExtensions ?? memoryCommercialExtensions).getModelMarkupPolicy()
      const authorization = await recordActionSettlement({ workspaceId: input.workspaceId, actionKey: input.actionKey, actionKind: input.actionKind, settlement: 'entitlement', amountFen: 0, reservedAmountFen: 0, multiplier: policy.multiplier, settlementStatus: 'authorized', actorId: input.actorId, description: input.description })
      if (authorization.settlementStatus !== 'authorized' && authorization.settlementStatus !== 'pending_receipt') throw new DomainError('MODEL_ACTION_ALREADY_STARTED', '权益模型操作已经受理或完成，禁止重复调用模型', 409, { settlement_status: authorization.settlementStatus ?? null })
      await reserveDailyModelBudget(input.workspaceId, input.actionKey, modelRunKey, modality!)
    } catch (error) {
      const compensationErrors: string[] = []
      try { await releaseDailyModelBudget(input.workspaceId, input.actionKey) } catch (compensationError) { compensationErrors.push(compensationError instanceof Error ? compensationError.message : String(compensationError)) }
      try {
        const refunded = await (persistence.entitlements ?? memoryEntitlements).refund({ workspaceId: input.workspaceId, idempotencyKey: input.actionKey })
        if (refunded.refunded) await refundActionSettlement({ workspaceId: input.workspaceId, actionKey: input.actionKey, reason: '模型权益授权或预算预留失败' })
      } catch (compensationError) { compensationErrors.push(compensationError instanceof Error ? compensationError.message : String(compensationError)) }
      if (compensationErrors.length) throw new DomainError('ENTITLEMENT_MODEL_AUTHORIZATION_COMPENSATION_FAILED', '模型权益授权失败且补偿未完整完成，已阻断上游请求并等待人工核对', 503, { action_id: input.actionKey, compensation_errors: compensationErrors })
      throw error
    }
  }
  return consumption
}

async function refundEntitlement(input: { workspaceId: string; actionKey: string; reason: string }) {
  await persistenceReady
  const refunded = await (persistence.entitlements ?? memoryEntitlements).refund({ workspaceId: input.workspaceId, idempotencyKey: input.actionKey })
  if (refunded.refunded) await refundActionSettlement({ workspaceId: input.workspaceId, actionKey: input.actionKey, reason: input.reason })
  return refunded
}

async function refundModelEntitlement(input: { workspaceId: string; actionKey: string; reason: string }) {
  await releaseDailyModelBudget(input.workspaceId, input.actionKey)
  return refundEntitlement(input)
}


async function refundActionSettlement(input: { workspaceId: string; actionKey: string; reason: string }) {
  await persistenceReady
  return persistence.actionLedger?.refund(input) ?? { refunded: false }
}

async function currentWalletBalanceFen(workspaceId: string) {
  await persistenceReady
  return persistence.billing ? persistence.billing.balanceFen(workspaceId) : walletBalanceFen(workspaceId)
}

type PlatformUserCommercialSummary = {
  planCode: string
  planName: string
  subscriptionStatus: string
  usedTasks: number
  includedTasks: number
  remainingTasks: number
  walletBalanceCny: string
}

async function loadPlatformUserCommercialSummaries(workspaceIds: readonly string[]) {
  const usageRepository = persistence.usage ?? memoryUsage
  const subscriptionRepository = persistence.subscriptions ?? memorySubscriptions
  const summaries = await mapWithConcurrency(workspaceIds, 8, async workspaceId => {
    const [usage, subscription, balanceFen] = await Promise.all([
      usageRepository.get(workspaceId),
      subscriptionRepository.get(workspaceId),
      currentWalletBalanceFen(workspaceId),
    ])
    return [workspaceId, {
      planCode: subscription.planCode,
      planName: subscription.planName,
      subscriptionStatus: subscription.status,
      usedTasks: usage.usedTasks,
      includedTasks: usage.includedTasks,
      remainingTasks: usage.remainingTasks,
      walletBalanceCny: (balanceFen / 100).toFixed(2),
    }] as const
  })
  return new Map(summaries)
}

async function synchronizeCommercialQuotaFromSubscription(subscription: { workspaceId: string; planCode: string; planName: string; billingCycle: 'monthly' | 'annual'; priceCny: number; includedStores: number; includedTasks: number }) {
  const commercialRepository = persistence.commercial ?? memoryCommercial
  const current = await commercialRepository.getSettings(subscription.workspaceId)
  const next = {
    planCode: subscription.planCode,
    planName: subscription.planName,
    monthlyPriceCny: subscription.billingCycle === 'monthly' ? subscription.priceCny : current.monthlyPriceCny,
    annualPriceCny: subscription.billingCycle === 'annual' ? subscription.priceCny : current.annualPriceCny,
    includedStores: subscription.includedStores,
    includedTasks: subscription.includedTasks,
    updatedBy: 'payment_provider',
  }
  if (current.planCode === next.planCode && current.planName === next.planName && current.monthlyPriceCny === next.monthlyPriceCny && current.annualPriceCny === next.annualPriceCny && current.includedStores === next.includedStores && current.includedTasks === next.includedTasks) return current
  return commercialRepository.updateSettings({ workspaceId: subscription.workspaceId, ...next, expectedRevision: current.revision })
}

export async function assertProviderActionCanStart(workspaceId: string, actionKey: string) {
  await persistenceReady
  const existing = await persistence.actionLedger?.get(workspaceId, actionKey)
  const settlementStatus = existing?.settlementStatus ?? existing?.state
  if (!existing) return
  throw new DomainError(
    'MODEL_ACTION_ALREADY_STARTED',
    '这次模型操作已经受理或正在结算，禁止重复调用模型；请刷新任务状态，待结算异常由运营后台处理',
    409,
    { settlement_status: settlementStatus },
  )
}

function modelIdForBudget(kind: PlatformModelKind) {
  return kind === 'text' ? process.env.AI_MODEL?.trim() || process.env.MODEL_ID?.trim()
    : kind === 'image' ? process.env.IMAGE_MODEL?.trim() || process.env.AI_IMAGE_MODEL?.trim()
      : kind === 'image_edit' ? process.env.IMAGE_EDIT_MODEL?.trim() || process.env.IMAGE_MODEL?.trim() || process.env.AI_IMAGE_MODEL?.trim()
        : kind === 'ocr' ? process.env.OCR_MODEL?.trim() || process.env.AI_VISION_MODEL?.trim()
          : process.env.VIDEO_MODEL?.trim() || process.env.AI_VIDEO_MODEL?.trim()
}

function modalityForActionKind(kind: ActionKind): PlatformModelKind | undefined {
  if (kind === 'model_text') return 'text'
  if (kind === 'model_image') return 'image'
  if (kind === 'image_edit') return 'image_edit'
  if (kind === 'model_ocr') return 'ocr'
  if (kind === 'model_video') return 'video'
  return undefined
}

async function reserveDailyModelBudget(workspaceId: string, reservationKey: string, runKey: string, kind: PlatformModelKind) {
  if (!isProduction()) return undefined
  requirePlatformModelCostGate(kind)
  await persistenceReady
  const repository = persistence.modelUsage
  const model = modelIdForBudget(kind)
  const estimate = evaluatePlatformModelBudgetEstimate(process.env, kind)
  const costGate = evaluatePlatformModelCostGate(process.env)
  const taskCostGate = evaluatePlatformModelTaskRequestCost(estimate.amountCny, process.env)
  if (!repository || !model || !estimate.ready || !estimate.version || !costGate.ready || !taskCostGate.ready) throw new DomainError(taskCostGate.reasons.includes('request_cost_exceeds_task_limit') ? 'MODEL_TASK_COST_LIMIT_EXCEEDED' : 'MODEL_COST_BUDGET_PREFLIGHT_UNAVAILABLE', taskCostGate.reasons.includes('request_cost_exceeds_task_limit') ? '本次生成请求的保守成本预估超过单任务安全上限，未调用上游' : `生产${kind}模型缺少版本化保守成本预估或持久化预算仓储，已阻断上游请求`, taskCostGate.reasons.includes('request_cost_exceeds_task_limit') ? 422 : 503, { reasons: [...estimate.reasons, ...costGate.reasons, ...taskCostGate.reasons, ...(!repository ? ['budget_repository_missing'] : []), ...(!model ? ['model_missing'] : [])] })
  try {
    return await repository.reserveDailyBudget({ workspaceId, reservationKey, runKey, modality: kind, model, estimateCny: estimate.amountCny, estimateVersion: estimate.version, dailyLimitCny: costGate.dailyCnyLimit, runLimitCny: taskCostGate.limitCny })
  } catch (error) {
    if ((error as { code?: string })?.code === 'MODEL_TASK_COST_LIMIT_EXCEEDED') {
      const details = (error as { details: { usedCny: number; reservedCny: number; requestCny: number; limitCny: number } }).details
      throw new DomainError('MODEL_TASK_COST_LIMIT_EXCEEDED', '本次生成方案预计成本超过单任务安全上限，未扣款且未调用上游', 422, { used_cny: details.usedCny, reserved_cny: details.reservedCny, request_cny: details.requestCny, maximum_task_cost_cny: details.limitCny })
    }
    if ((error as { code?: string })?.code === 'MODEL_DAILY_COST_BUDGET_EXCEEDED') {
      const details = (error as { details: { usedCny: number; reservedCny: number; requestCny: number; limitCny: number } }).details
      throw new DomainError('MODEL_DAILY_COST_BUDGET_EXCEEDED', '当日模型成本预算不足，未扣钱包且未调用上游', 429, { used_cny: details.usedCny, reserved_cny: details.reservedCny, request_cny: details.requestCny, limit_cny: details.limitCny })
    }
    throw error
  }
}

async function releaseDailyModelBudget(workspaceId: string, reservationKey: string) {
  if (!isProduction()) return
  await persistenceReady
  await persistence.modelUsage?.releaseDailyBudget({ workspaceId, reservationKey })
}

async function withDailyModelBudget<T>(kind: PlatformModelKind, usageContext: { workspaceId?: string; actionId?: string; runKey?: string } | undefined, invoke: () => Promise<T>): Promise<T> {
  if (!isProduction()) return invoke()
  const workspaceId = usageContext?.workspaceId?.trim(); const actionId = usageContext?.actionId?.trim(); const runKey = usageContext?.runKey?.trim()
  if (!workspaceId || !actionId || !runKey) throw new DomainError('MODEL_COST_BUDGET_CONTEXT_REQUIRED', '生产模型调用缺少工作区、幂等动作标识或任务预算标识，已阻断上游请求', 503)
  await reserveDailyModelBudget(workspaceId, actionId, runKey, kind)
  try { return await invoke() }
  catch (error) {
    if (!providerSucceededButSettlementPending(error)) await releaseDailyModelBudget(workspaceId, actionId)
    throw error
  }
}

type PluginWalletDebitInput = { workspaceId: string; amountFen?: number; idempotencyKey: string; actorId: string; description: string; taskId?: string; campaignItemId?: string; modelRunKey?: string; contextLinkId?: string; contextHash?: string }

async function debitPluginWallet(input: PluginWalletDebitInput) {
  const modality = modalityForActionKind(actionKindForDescription(input.description))
  if (!modality) return debitPluginWalletAfterBudget(input)
  const runKey = input.modelRunKey?.trim() || input.taskId?.trim() || input.campaignItemId?.trim() || input.idempotencyKey
  await reserveDailyModelBudget(input.workspaceId, input.idempotencyKey, runKey, modality)
  try { return await debitPluginWalletAfterBudget(input) }
  catch (error) { await releaseDailyModelBudget(input.workspaceId, input.idempotencyKey); throw error }
}

async function debitPluginWalletAfterBudget(input: PluginWalletDebitInput) {
  const amountFen = input.amountFen ?? 1
  const actionKind = actionKindForDescription(input.description)
  const providerMetered = ['model_text', 'model_image', 'model_ocr', 'model_video', 'image_edit'].includes(actionKind)
  await persistenceReady
  if (providerMetered) await assertProviderActionCanStart(input.workspaceId, input.idempotencyKey)
  const pricingPolicy = providerMetered ? await (persistence.commercialExtensions ?? memoryCommercialExtensions).getModelMarkupPolicy() : undefined
  const settlement = input.description.includes('套餐额度外') ? 'wallet_overage' as const : 'wallet' as const
  const authorization = pricingPolicy ? { reservedAmountFen: amountFen, multiplier: pricingPolicy.multiplier, settlementStatus: 'authorized' as const } : {}
  if (process.env.NODE_ENV === 'test') {
    const transaction = { id: `fixture_debit_${input.idempotencyKey}`, workspaceId: input.workspaceId, type: 'debit' as const, amountFen, orderId: input.idempotencyKey, actorId: input.actorId, description: input.description, createdAt: new Date().toISOString() }
    await recordActionSettlement({ workspaceId: input.workspaceId, actionKey: input.idempotencyKey, actionKind, settlement, amountFen: transaction.amountFen, actorId: input.actorId, description: input.description, ...(input.taskId ? { taskId: input.taskId } : {}), ...(input.campaignItemId ? { campaignItemId: input.campaignItemId } : {}), ...(input.contextLinkId && input.contextHash ? { contextLinkId: input.contextLinkId, contextHash: input.contextHash } : {}), ...authorization })
    return transaction
  }
  if (!Number.isSafeInteger(amountFen) || amountFen <= 0) throw new DomainError('BILLING_AMOUNT_INVALID', '扣款金额必须是正整数分', 400)
  if (persistence.billing) {
    try {
      const transaction = await persistence.billing.debit({ ...input, amountFen })
      try {
        await recordActionSettlement({ workspaceId: input.workspaceId, actionKey: input.idempotencyKey, actionKind, settlement, amountFen, actorId: input.actorId, description: input.description, ...(input.taskId ? { taskId: input.taskId } : {}), ...(input.campaignItemId ? { campaignItemId: input.campaignItemId } : {}), ...(input.contextLinkId && input.contextHash ? { contextLinkId: input.contextLinkId, contextHash: input.contextHash } : {}), ...authorization })
      } catch (error) {
        // The provider debit is durable even when the action ledger is down.
        // Compensate here because callers do not receive the transaction and
        // therefore cannot know that a refund is required.
        try {
          if (transaction.created) await persistence.billing.refundDebit({ workspaceId: input.workspaceId, debitIdempotencyKey: input.idempotencyKey, actorId: input.actorId, reason: '扣款台账写入失败，自动退款' })
        } catch { /* preserve the original ledger error for operational alerting */ }
        throw error
      }
      return transaction
    }
    catch (error) { if (error instanceof Error && error.message === 'BILLING_INSUFFICIENT_BALANCE') throw new DomainError('RECHARGE_REQUIRED', '插件钱包余额不足，请充值后继续', 402); if ((error as { code?: string })?.code === 'WALLET_DEBIT_IDEMPOTENCY_CONFLICT' || String(error).includes('WALLET_DEBIT_IDEMPOTENCY_CONFLICT')) throw new DomainError('WALLET_DEBIT_IDEMPOTENCY_CONFLICT', '钱包扣款幂等键已绑定到不同金额或动作，请换用新的幂等键', 409); throw error }
  }
  const existing = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === 'debit' && item.orderId === input.idempotencyKey)
  if (existing) { if (existing.amountFen !== amountFen || existing.description !== `${input.description}（${input.actorId}）`) throw new DomainError('WALLET_DEBIT_IDEMPOTENCY_CONFLICT', '钱包扣款幂等键已绑定到不同金额或动作，请换用新的幂等键', 409); return existing }
  if (walletBalanceFen(input.workspaceId) < amountFen) throw new DomainError('RECHARGE_REQUIRED', '插件钱包余额不足，请充值后继续', 402)
  const transaction = { id: `billing_tx_${randomUUID()}`, workspaceId: input.workspaceId, type: 'debit' as const, amountFen, orderId: input.idempotencyKey, actorId: input.actorId, description: `${input.description}（${input.actorId}）`, createdAt: new Date().toISOString() }
  walletTransactions.push(transaction)
  try {
    await recordActionSettlement({ workspaceId: input.workspaceId, actionKey: input.idempotencyKey, actionKind, settlement, amountFen, actorId: input.actorId, description: input.description, ...(input.taskId ? { taskId: input.taskId } : {}), ...(input.campaignItemId ? { campaignItemId: input.campaignItemId } : {}), ...(input.contextLinkId && input.contextHash ? { contextLinkId: input.contextLinkId, contextHash: input.contextHash } : {}), ...authorization })
  } catch (error) {
    const index = walletTransactions.findIndex(item => item.id === transaction.id)
    if (index >= 0) walletTransactions.splice(index, 1)
    throw error
  }
  return transaction
}

async function settlePluginWalletDebit(input: { workspaceId: string; debitIdempotencyKey: string; finalAmountFen: number; actorId: string; providerRequestId?: string }) {
  await persistenceReady
  if (process.env.NODE_ENV !== 'test' && persistence.billing) {
    try {
      await persistence.billing.settleDebit({ workspaceId: input.workspaceId, debitIdempotencyKey: input.debitIdempotencyKey, finalAmountFen: input.finalAmountFen, actorId: input.actorId, description: '模型真实用量结算' })
    } catch (error) {
      if (error instanceof Error && error.message === 'BILLING_INSUFFICIENT_BALANCE') throw new DomainError('RECHARGE_REQUIRED', '模型已返回真实用量，但钱包不足以完成结算，请充值', 402)
      throw error
    }
  } else if (process.env.NODE_ENV !== 'test') {
    const original = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === 'debit' && item.orderId === input.debitIdempotencyKey)
    if (!original) throw new Error('billing debit not found')
    const delta = input.finalAmountFen - original.amountFen
    const orderId = `${delta > 0 ? 'settlement' : 'settlement-refund'}:${input.debitIdempotencyKey}`
    const type: WalletTransaction['type'] = delta > 0 ? 'debit' : 'refund'
    const existing = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === type && item.orderId === orderId)
    if (!existing && delta !== 0) {
      if (delta > 0 && walletBalanceFen(input.workspaceId) < delta) throw new DomainError('RECHARGE_REQUIRED', '模型已返回真实用量，但钱包不足以完成结算，请充值', 402)
      walletTransactions.push({ id: `billing_tx_${randomUUID()}`, workspaceId: input.workspaceId, type, amountFen: Math.abs(delta), orderId, actorId: input.actorId, description: `模型真实用量结算（${input.actorId}）`, createdAt: new Date().toISOString() })
    }
  }
  await persistence.actionLedger?.settleProviderUsage({ workspaceId: input.workspaceId, actionKey: input.debitIdempotencyKey, ...(input.providerRequestId ? { providerRequestId: input.providerRequestId } : {}), actualAmountFen: input.finalAmountFen })
}

async function settlePendingModelUsage(input: { workspaceId: string; usageId: string; actorId: string; expectedRevision: number; reason?: string; evidenceRef?: string }) {
  if (!persistence.modelUsage) throw new DomainError('MODEL_USAGE_LEDGER_NOT_CONFIGURED', '模型用量结算台账未配置', 503)
  let usage = (await persistence.modelUsage.list(input.workspaceId, 1000)).find(item => item.id === input.usageId)
  if (!usage) throw new DomainError('MODEL_USAGE_NOT_FOUND', '模型用量记录不存在', 404)
  if (usage.revision !== input.expectedRevision) throw new DomainError('MODEL_USAGE_REVISION_CONFLICT', '模型用量记录已被其他操作更新，请刷新后重试', 409)
  if (usage.costCny === undefined || usage.customerChargeCny === undefined) throw new DomainError('MODEL_USAGE_COST_MISSING', '该回执仍缺少实际成本，无法自动结算', 409)
  const usageCostCny = usage.costCny
  const usageCustomerChargeCny = usage.customerChargeCny
  if (usage.budgetReservationKey && usage.budgetRunKey) {
    usage = (await persistence.modelUsage.recordUsageAndSettleBudget({ workspaceId: usage.workspaceId, receiptKey: usage.receiptKey, receiptHash: usage.receiptHash, ...(usage.actionId ? { actionId: usage.actionId } : {}), budgetReservationKey: usage.budgetReservationKey, budgetRunKey: usage.budgetRunKey, ...(usage.contextLinkId && usage.contextHash ? { contextLinkId: usage.contextLinkId, contextHash: usage.contextHash } : {}), modality: usage.modality, model: usage.model, ...(usage.providerRequestId ? { providerRequestId: usage.providerRequestId } : {}), ...(usage.inputTokens !== undefined ? { inputTokens: usage.inputTokens } : {}), ...(usage.outputTokens !== undefined ? { outputTokens: usage.outputTokens } : {}), ...(usage.totalTokens !== undefined ? { totalTokens: usage.totalTokens } : {}), costCny: usageCostCny, markupMultiplier: usage.markupMultiplier, customerChargeCny: usageCustomerChargeCny, pricingPolicyRevision: usage.pricingPolicyRevision, observedAt: usage.observedAt, ...(usage.metadata ? { metadata: usage.metadata } : {}) })).usage
  }
  if (usage.actionId) {
    const actionLookup = await getActionLedgerWithHistoricalImageCompat(input.workspaceId, usage.actionId)
    const action = actionLookup?.action
    if (!action) throw new DomainError('MODEL_USAGE_ACTION_NOT_FOUND', '原始扣费授权不存在，需人工核对', 409)
    const actionKey = actionLookup?.actionKey ?? usage.actionId
    if (action.settlement === 'wallet' || action.settlement === 'wallet_overage') {
      await settlePluginWalletDebit({ workspaceId: input.workspaceId, debitIdempotencyKey: actionKey, finalAmountFen: Math.max(1, Math.ceil(usageCustomerChargeCny * 100)), actorId: input.actorId, ...(usage.providerRequestId ? { providerRequestId: usage.providerRequestId } : {}) })
    } else if (action.settlement === 'entitlement' || action.settlement === 'included_quota') {
      if (action.settlementStatus === 'authorized') await persistence.actionLedger?.transitionSettlementStatus({ workspaceId: input.workspaceId, actionKey, from: ['authorized'], to: 'pending_receipt' })
      await persistence.actionLedger?.settleProviderUsage({ workspaceId: input.workspaceId, actionKey, actualAmountFen: 0, ...(usage.providerRequestId ? { providerRequestId: usage.providerRequestId } : {}) })
    }
  }
  return persistence.modelUsage.resolve({ workspaceId: input.workspaceId, id: usage.id, expectedRevision: usage.revision, status: 'settled', actorId: input.actorId, reason: input.reason ?? '运营对账重试完成', evidenceRef: input.evidenceRef ?? usage.providerRequestId ?? usage.receiptKey })
}

async function runModelUsageReconciliation(input: { workspaceId: string; actorId: string; limit: number }) {
  if (!persistence.modelUsage) throw new DomainError('MODEL_USAGE_LEDGER_NOT_CONFIGURED', '模型用量结算台账未配置', 503)
  const owner = `model-reconciliation:${input.actorId}:${randomUUID()}`
  const claimed = await persistence.modelUsage.claimPending({ workspaceId: input.workspaceId, owner, limit: input.limit, leaseSeconds: 120, now: new Date().toISOString() })
  const settled: string[] = []
  const pending: Array<{ usage_id: string; status: string; code: string }> = []
  for (const usage of claimed) {
    try {
      const completed = await settlePendingModelUsage({ workspaceId: input.workspaceId, usageId: usage.id, actorId: input.actorId, expectedRevision: usage.revision })
      settled.push(completed.id)
    } catch (error) {
      const code = (error as { code?: string })?.code ?? (error instanceof Error ? error.message : 'MODEL_USAGE_RECONCILIATION_FAILED')
      const terminal = usage.attemptCount >= 5
      const status = terminal ? 'manual_attention' as const : usage.costCny === undefined ? 'pending_cost' as const : 'pending_wallet' as const
    try {
      if (usage.actionId) {
          const actionLookup = await getActionLedgerWithHistoricalImageCompat(input.workspaceId, usage.actionId)
        const actionKey = actionLookup?.actionKey ?? usage.actionId
        await persistence.actionLedger?.transitionSettlementStatus({ workspaceId: input.workspaceId, actionKey, from: ['authorized', 'pending_receipt'], to: terminal ? 'manual_attention' : 'pending_receipt' })
      }
        await persistence.modelUsage.resolve({ workspaceId: input.workspaceId, id: usage.id, expectedRevision: usage.revision, status, actorId: input.actorId, reason: terminal ? '自动重试达到上限，转人工核对' : '自动对账尚未完成', lastError: { code, message: error instanceof Error ? error.message : String(error) }, ...(terminal ? {} : { nextAttemptAt: new Date(Date.now() + Math.min(3600, 60 * 2 ** Math.min(usage.attemptCount, 5)) * 1000).toISOString() }) })
      } catch { /* another reconciler won the optimistic lock */ }
      pending.push({ usage_id: usage.id, status, code })
    }
  }
  return { state: pending.length ? 'attention_required' as const : 'completed' as const, checked: claimed.length, settled, pending, actor_id: input.actorId }
}

async function refundPluginWalletDebit(input: { workspaceId: string; debitIdempotencyKey: string; actorId: string; reason: string }) {
  await releaseDailyModelBudget(input.workspaceId, input.debitIdempotencyKey)
  if (process.env.NODE_ENV === 'test') {
    await refundActionSettlement({ workspaceId: input.workspaceId, actionKey: input.debitIdempotencyKey, reason: input.reason })
    return { refunded: false }
  }
  await persistenceReady
  if (persistence.billing?.refundDebit) {
    try { const result = { refunded: true, transaction: await persistence.billing.refundDebit(input) }; await refundActionSettlement({ workspaceId: input.workspaceId, actionKey: input.debitIdempotencyKey, reason: input.reason }); return result }
    catch (error) { if (error instanceof Error && error.message === 'billing debit not found') return { refunded: false }; throw error }
  }
  const debit = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === 'debit' && item.orderId === input.debitIdempotencyKey)
  if (!debit) { await refundActionSettlement({ workspaceId: input.workspaceId, actionKey: input.debitIdempotencyKey, reason: input.reason }); return { refunded: false } }
  const refundOrderId = `refund:${input.debitIdempotencyKey}`
  const existing = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === 'refund' && item.orderId === refundOrderId)
  if (existing) return { refunded: false, transaction: existing }
  const transaction = { id: `billing_tx_${randomUUID()}`, workspaceId: input.workspaceId, type: 'refund' as const, amountFen: debit.amountFen, orderId: refundOrderId, actorId: input.actorId, description: `模型失败退款（${input.actorId}）：${input.reason}`, createdAt: new Date().toISOString() }
  walletTransactions.push(transaction)
  await refundActionSettlement({ workspaceId: input.workspaceId, actionKey: input.debitIdempotencyKey, reason: input.reason })
  return { refunded: true, transaction }
}

/** A settled wallet balance is the product-wide paid-capability gate. Included
 * task quota changes how a paid action is settled, but never unlocks the
 * plugin while the merchant wallet is empty. */
async function requirePluginWalletAccess(workspaceId: string) {
  if (process.env.NODE_ENV === 'test') return 1
  const balanceFen = await currentWalletBalanceFen(workspaceId)
  if (balanceFen <= 0) throw new DomainError('RECHARGE_REQUIRED', '请先为插件钱包充值，充值到账后才能使用生成、检查和发布能力', 402, { balance_cny: '0.00', recharge_required: true })
  return balanceFen
}

function resolveTaskPublishAccount(task: { accountId?: string | null }, requestedAccountId?: string) {
  const requested = requestedAccountId?.trim() || undefined
  if (task.accountId && requested && task.accountId !== requested) {
    throw new DomainError('STORE_ACCOUNT_CONFLICT', '发布店铺必须与任务已绑定店铺一致，不能改投同平台其他店铺', 409, { task_account_id: task.accountId, requested_account_id: requested })
  }
  return requested ?? task.accountId ?? undefined
}

async function markRechargePaid(input: { workspaceId: string; orderId: string; providerTradeId: string; amountFen: number; eventSource: string }) {
  await persistenceReady
  if (persistence.billing) return persistence.billing.markPaid(input)
  const order = rechargeOrders.get(input.orderId)
  if (!order || order.workspaceId !== input.workspaceId) return undefined
  if (order.amountFen !== input.amountFen) throw new DomainError('BILLING_CALLBACK_AMOUNT_MISMATCH', '支付回调金额与订单金额不一致', 400)
  if (order.state === 'paid') {
    if (order.providerTradeId && order.providerTradeId !== input.providerTradeId) throw new DomainError('PAYMENT_CALLBACK_REPLAY_CONFLICT', '已到账订单不能使用不同的支付交易号重复入账', 409)
    return order
  }
  if (order.state !== 'pending') throw new DomainError('BILLING_ORDER_NOT_PAYABLE', '充值订单当前不可入账', 409)
  order.state = 'paid'; order.paymentUrl = undefined; order.providerTradeId = input.providerTradeId; order.updatedAt = new Date().toISOString()
  walletTransactions.push({ id: `billing_tx_${randomUUID()}`, workspaceId: input.workspaceId, type: 'recharge', amountFen: input.amountFen, orderId: input.orderId, actorId: order.createdByActorId, description: `充值到账（${order.channel}）`, createdAt: new Date().toISOString() })
  await persistEvent(input.workspaceId, input.orderId, 'billing.recharge.paid', 1, { order_id: input.orderId, provider_trade_id: input.providerTradeId, amount_fen: input.amountFen, channel: order.channel, source: input.eventSource })
  return order
}

async function markRechargeProviderState(input: { workspaceId: string; orderId: string; state: 'closed' | 'failed' }) {
  await persistenceReady
  if (persistence.billing?.markProviderState) return persistence.billing.markProviderState(input)
  const order = rechargeOrders.get(input.orderId)
  if (!order || order.workspaceId !== input.workspaceId || order.state !== 'pending') return undefined
  order.state = input.state
  order.paymentUrl = undefined
  order.updatedAt = new Date().toISOString()
  return order
}

type RechargeRefundReservation = WalletTransaction & { created: boolean; completed: boolean }

async function reserveRechargeRefund(input: { workspaceId: string; orderId: string; actorId: string; reason: string }): Promise<RechargeRefundReservation> {
  await persistenceReady
  if (persistence.billing) {
    try { return await persistence.billing.reserveRechargeRefund(input) }
    catch (error) {
      if ((error as { code?: string })?.code === 'BILLING_REFUND_BALANCE_UNAVAILABLE') throw new DomainError('BILLING_REFUND_BALANCE_UNAVAILABLE', '充值金额已被消费或冻结，钱包可用余额不足，不能发起原路退款', 409)
      if (error instanceof Error && error.message === 'billing order is not refundable') throw new DomainError('BILLING_ORDER_NOT_REFUNDABLE', '充值订单当前不可退款', 409)
      throw error
    }
  }
  const order = rechargeOrders.get(input.orderId)
  if (!order || order.workspaceId !== input.workspaceId) throw new DomainError('BILLING_ORDER_NOT_FOUND', '充值订单不存在', 404)
  const prefix = `recharge-refund:${input.orderId}:`
  const reservations = walletTransactions.filter(item => item.workspaceId === input.workspaceId && item.type === 'debit' && item.orderId?.startsWith(prefix))
  if (order.state === 'closed') {
    const completed = reservations.at(-1)
    if (!completed) throw new DomainError('BILLING_ORDER_NOT_REFUNDABLE', '充值订单当前不可退款', 409)
    return { ...completed, created: false, completed: true }
  }
  if (order.state !== 'paid') throw new DomainError('BILLING_ORDER_NOT_REFUNDABLE', '充值订单当前不可退款', 409)
  const released = new Set(walletTransactions.filter(item => item.workspaceId === input.workspaceId && item.type === 'refund' && item.orderId?.startsWith(`release:${prefix}`)).map(item => item.orderId?.replace(/^release:/u, '')))
  const active = reservations.find(item => item.orderId && !released.has(item.orderId))
  if (active) return { ...active, created: false, completed: false }
  if (walletBalanceFen(input.workspaceId) < order.amountFen) throw new DomainError('BILLING_REFUND_BALANCE_UNAVAILABLE', '充值金额已被消费或冻结，钱包可用余额不足，不能发起原路退款', 409)
  const transaction = { id: `billing_tx_${randomUUID()}`, workspaceId: input.workspaceId, type: 'debit' as const, amountFen: order.amountFen, orderId: `${prefix}${reservations.length + 1}`, actorId: input.actorId, description: `充值原路退款预留（${input.actorId}）：${input.reason}`, createdAt: new Date().toISOString() }
  walletTransactions.push(transaction)
  return { ...transaction, created: true, completed: false }
}

async function completeRechargeRefund(input: { workspaceId: string; orderId: string; reservationKey: string; actorId: string; reason: string; providerRefundId: string }) {
  await persistenceReady
  if (persistence.billing) return persistence.billing.completeRechargeRefund(input)
  const order = rechargeOrders.get(input.orderId)
  if (!order || order.workspaceId !== input.workspaceId) throw new DomainError('BILLING_ORDER_NOT_FOUND', '充值订单不存在', 404)
  const reservation = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === 'debit' && item.orderId === input.reservationKey)
  if (!reservation || reservation.amountFen !== order.amountFen) throw new DomainError('BILLING_REFUND_RESERVATION_NOT_FOUND', '退款订单缺少钱包扣减记录，需人工核对', 409)
  if (walletTransactions.some(item => item.workspaceId === input.workspaceId && item.type === 'refund' && item.orderId === `release:${input.reservationKey}`)) throw new DomainError('BILLING_REFUND_RESERVATION_RELEASED', '退款钱包预留已释放，需重新发起退款', 409)
  if (order.state === 'closed') return reservation
  if (order.state !== 'paid') throw new DomainError('BILLING_ORDER_NOT_REFUNDABLE', '充值订单当前不可退款', 409)
  order.state = 'closed'; order.paymentUrl = undefined; order.updatedAt = new Date().toISOString()
  await persistEvent(input.workspaceId, input.orderId, 'billing.recharge.refunded', 1, { order_id: input.orderId, provider_refund_id: input.providerRefundId, amount_fen: order.amountFen, actor_id: input.actorId, reason: input.reason })
  return reservation
}

async function releaseRechargeRefund(input: { workspaceId: string; orderId: string; reservationKey: string; actorId: string; reason: string }) {
  await persistenceReady
  if (persistence.billing) return persistence.billing.releaseRechargeRefund(input)
  const order = rechargeOrders.get(input.orderId)
  if (!order || order.workspaceId !== input.workspaceId) throw new DomainError('BILLING_ORDER_NOT_FOUND', '充值订单不存在', 404)
  if (order.state === 'closed') return undefined
  const reservation = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === 'debit' && item.orderId === input.reservationKey)
  if (!reservation) throw new DomainError('BILLING_REFUND_RESERVATION_NOT_FOUND', '退款订单缺少钱包扣减记录，需人工核对', 409)
  const releaseKey = `release:${input.reservationKey}`
  const existing = walletTransactions.find(item => item.workspaceId === input.workspaceId && item.type === 'refund' && item.orderId === releaseKey)
  if (existing) return existing
  const release = { id: `billing_tx_${randomUUID()}`, workspaceId: input.workspaceId, type: 'refund' as const, amountFen: reservation.amountFen, orderId: releaseKey, actorId: input.actorId, description: `充值退款失败释放预留（${input.actorId}）：${input.reason}`, createdAt: new Date().toISOString() }
  walletTransactions.push(release)
  return release
}

function verifyPaymentCallback(req: IncomingMessage, input: { channel: RechargeChannel; workspaceId: string; payload: { order_id: string; provider_trade_id: string; amount_fen: number; state: string } }) {
  requireProviderPaymentConfigured()
  const secret = process.env.PAYMENT_CALLBACK_SECRET?.trim()
  const testCallbackCompatibility = process.env.VITEST === 'true' || process.env.NODE_ENV === 'test'
  const localUnsignedCallback = testCallbackCompatibility || (fixturePaymentAllowed() && process.env.NODE_ENV === 'development')
  if (!secret) {
    if (!localUnsignedCallback) throw new DomainError('PAYMENT_CALLBACK_PROOF_REQUIRED', '非本地测试环境必须配置支付回调签名密钥', 503)
    return undefined
  }
  const provided = header(req, 'x-payment-signature')?.trim() ?? ''
  const timestampHeader = header(req, 'x-payment-timestamp')?.trim()
  const nonce = header(req, 'x-payment-nonce')?.trim()
  const legacyAllowed = localUnsignedCallback && !timestampHeader && !nonce
  if (!legacyAllowed && (!timestampHeader || !/^\d{10,13}$/u.test(timestampHeader) || !nonce || !/^[A-Za-z0-9_-]{16,128}$/u.test(nonce))) throw new DomainError('PAYMENT_CALLBACK_PROOF_REQUIRED', '支付回调必须携带有效时间戳和一次性 nonce', 401)
  const timestampMs = timestampHeader ? (timestampHeader.length === 10 ? Number(timestampHeader) * 1000 : Number(timestampHeader)) : Date.now()
  if (!legacyAllowed && (!Number.isSafeInteger(timestampMs) || Math.abs(Date.now() - timestampMs) > 5 * 60_000)) throw new DomainError('PAYMENT_CALLBACK_EXPIRED', '支付回调已超过五分钟有效窗口', 401)
  const canonical = legacyAllowed
    ? `${input.payload.order_id}|${input.payload.provider_trade_id}|${input.payload.amount_fen}|${input.payload.state}`
    : `${input.channel}|${input.workspaceId}|${input.payload.order_id}|${input.payload.provider_trade_id}|${input.payload.amount_fen}|${input.payload.state}|${timestampHeader}|${nonce}`
  const expected = createHmac('sha256', secret).update(canonical).digest('hex')
  if (!provided || provided.length !== expected.length || !timingSafeEqual(Buffer.from(provided), Buffer.from(expected))) throw new DomainError('PAYMENT_CALLBACK_SIGNATURE_INVALID', '支付回调验签失败', 401)
  if (legacyAllowed) return undefined
  return { nonce: nonce!, signedAt: new Date(timestampMs).toISOString(), payloadHash: createHash('sha256').update(canonical).digest('hex') }
}

async function consumePaymentCallbackProof(input: { workspaceId: string; channel: RechargeChannel; nonce: string; signedAt: string; payloadHash: string }) {
  await persistenceReady
  return (persistence.paymentCallbackNonces ?? memoryPaymentCallbackNonces).consume(input)
}

function paymentProviderReadiness(source: NodeJS.ProcessEnv = process.env) {
  const reasons: string[] = []
  const adapters = (source.PAYMENT_PROVIDER_ADAPTERS ?? '').split(',').map(value => value.trim()).filter(Boolean)
  if (!adapters.includes('alipay') || !adapters.includes('wechat')) reasons.push('provider_adapters_incomplete')
  if (!/^https:\/\//iu.test(source.PAYMENT_CHECKOUT_BASE_URL?.trim() ?? '')) reasons.push('checkout_endpoint_must_use_https')
  if (!/^https:\/\//iu.test(source.PAYMENT_PROVIDER_CHECKOUT_API_URL?.trim() ?? '')) reasons.push('provider_checkout_api_must_use_https')
  if (!/^https:\/\//iu.test(source.PAYMENT_PROVIDER_QUERY_API_URL?.trim() ?? '')) reasons.push('provider_query_api_must_use_https')
  if (!source.PAYMENT_PROVIDER_API_KEY?.trim()) reasons.push('provider_api_key_missing')
  if (!source.PAYMENT_PROVIDER_MERCHANT_ID?.trim()) reasons.push('provider_merchant_id_missing')
  if (!/^https:\/\//iu.test(source.PAYMENT_PROVIDER_REFUND_API_URL?.trim() ?? '')) reasons.push('provider_refund_api_must_use_https')
  if (!/^https:\/\//iu.test(source.PAYMENT_CALLBACK_BASE_URL?.trim() ?? '')) reasons.push('callback_endpoint_must_use_https')
  if (!source.PAYMENT_CALLBACK_SECRET?.trim()) reasons.push('callback_secret_missing')
  if (source.PAYMENT_RECONCILIATION_ENABLED !== 'true') reasons.push('reconciliation_disabled')
  if (source.PAYMENT_REFUND_ENABLED !== 'true') reasons.push('refund_disabled')
  return { ready: reasons.length === 0, reasons }
}

function requireProviderPaymentConfigured() {
  if (!isProduction()) return
  const readiness = paymentProviderReadiness()
  if (process.env.PAYMENT_MODE !== 'provider' || !readiness.ready) throw new DomainError('PAYMENT_NOT_CONFIGURED', `生产环境支付 provider 未就绪：${readiness.reasons.join(', ')}`, 503, { reasons: readiness.reasons })
}

/** Test-only seam for exercising the HTTP authorization/orchestration boundary
 * without replacing the production Postgres repository implementation. */
export function setRuleRepositoryForTests(repository?: RuleRepositoryPort) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('RULE_REPOSITORY_OVERRIDE_TEST_ONLY')
  ruleRepositoryOverride = repository
}

export function setPaymentProviderForTests(provider?: PaymentProvider) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('PAYMENT_PROVIDER_OVERRIDE_TEST_ONLY')
  paymentProvider = provider
}

function ruleRepository() { return ruleRepositoryOverride ?? persistence.rules }

function iso(value: string | Date) { return typeof value === 'string' ? value : new Date(String(value)).toISOString() }
function publicRule(version: PersistedRuleVersion) {
  const lifecycleStatus = version.status === 'active' ? 'published' : version.status === 'inactive' ? 'disabled' : version.status
  return { id: version.id, workspaceId: version.workspaceId, packId: version.packId, name: version.name, version: version.version, scope: version.scope, status: version.status, lifecycleStatus, createdBy: version.createdBy, updatedAt: iso(version.updatedAt), source: { kind: version.sourceKind, reference: version.sourceReference, checkedAt: iso(version.sourceCheckedAt) }, checksum: version.checksum, revision: version.revision, ...(version.effectiveFrom ? { effectiveFrom: iso(version.effectiveFrom) } : {}), ...(version.effectiveTo ? { effectiveTo: iso(version.effectiveTo) } : {}), ...(version.severity ? { severity: version.severity } : {}), ...(version.action ? { action: version.action } : {}), ...(version.targetId ? { targetId: version.targetId } : {}), ...(version.scopeValue ? { scopeValue: version.scopeValue } : {}), ...(version.activatedAt ? { activatedAt: iso(version.activatedAt) } : {}), ...(version.deactivatedAt ? { deactivatedAt: iso(version.deactivatedAt) } : {}) }
}

function rulePackProjection(version: PersistedRuleVersion): RulePack {
  return { id: version.id, name: version.name, version: version.version, scope: version.scope as RulePack['scope'], status: version.status as RulePack['status'], updatedAt: iso(version.updatedAt), source: { kind: version.sourceKind as RulePack['source']['kind'], reference: version.sourceReference, checkedAt: iso(version.sourceCheckedAt) }, checksum: version.checksum, revision: version.revision, ...(version.effectiveFrom ? { effectiveFrom: iso(version.effectiveFrom) } : {}), ...(version.effectiveTo ? { effectiveTo: iso(version.effectiveTo) } : {}), ...(version.severity ? { severity: version.severity as RulePack['severity'] } : {}), ...(version.action ? { action: version.action as RulePack['action'] } : {}), ...(version.targetId ? { targetId: version.targetId } : {}), ...(version.scopeValue ? { scopeValue: version.scopeValue } : {}), ...(version.activatedAt ? { activatedAt: iso(version.activatedAt) } : {}), ...(version.deactivatedAt ? { deactivatedAt: iso(version.deactivatedAt) } : {}) }
}

async function rulePacksForWorkspace(workspaceId: string) {
  const repository = ruleRepository()
  if (repository) {
    const rows = await repository.list(workspaceId)
    return rows.map(rulePackProjection)
  }
  return service.ruleCenter.list({ includeInactive: false })
}

const lastPlatformRuleSync = new Map<string, number>()

export async function syncSignedPlatformRules(workspaceId: string) {
  const manifestUrl = process.env.PLATFORM_RULE_SYNC_MANIFEST_URL?.trim()
  const signingSecret = process.env.PLATFORM_RULE_SYNC_SIGNING_SECRET?.trim()
  const intervalHours = Math.max(1, Number(process.env.PLATFORM_RULE_SYNC_INTERVAL_HOURS ?? 24))
  if (!manifestUrl || !signingSecret) return { state: 'not_configured' as const, imported: 0, activated: 0, reason: !manifestUrl ? 'manifest_url_missing' : 'signing_secret_missing' }
  const repository = ruleRepository()
  if (!repository) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '规则定时同步需要持久化规则仓储', 503)
  if (!repository.insertVersionWithAudit || !repository.transitionStatusWithAudit) throw new DomainError('RULE_REPOSITORY_ATOMIC_SYNC_UNAVAILABLE', '规则仓储不支持原子导入和激活', 503)
  const last = lastPlatformRuleSync.get(workspaceId)
  if (last && Date.now() - last < intervalHours * 3_600_000) return { state: 'not_due' as const, imported: 0, activated: 0, next_sync_at: new Date(last + intervalHours * 3_600_000).toISOString() }
  await assertOutboundUrl(manifestUrl, { environment: process.env.NODE_ENV })
  const response = await fetch(manifestUrl, { headers: { accept: 'application/json' }, redirect: 'error', signal: AbortSignal.timeout(15_000) })
  if (!response.ok) throw new DomainError('RULE_MANIFEST_FETCH_FAILED', `签名规则清单返回 HTTP ${response.status}`, 503)
  const raw = await readBoundedResponseText(response, 2 * 1024 * 1024, 'platform rule manifest')
  let manifest
  try { manifest = verifyAndParsePlatformRuleManifest(raw, response.headers.get('x-rule-manifest-signature') ?? '', signingSecret) }
  catch (error) { throw new DomainError('RULE_MANIFEST_INVALID', error instanceof Error ? error.message : '签名规则清单无效', 409) }
  const existing = await repository.list(workspaceId)
  let imported = 0
  let activated = 0
  const versions: Array<{ platform: string; pack_id: string; version: string; state: string }> = []
  for (const entry of manifest.entries) {
    const checksum = createHash('sha256').update(canonicalJson({ platform: entry.platform, packId: entry.packId, version: entry.version, sourceReference: entry.sourceReference, sourceCheckedAt: entry.sourceCheckedAt, checks: entry.checks, severity: entry.severity, action: entry.action, effectiveFrom: entry.effectiveFrom ?? null, effectiveTo: entry.effectiveTo ?? null })).digest('hex')
    let target = existing.find(row => row.packId === entry.packId && row.version === entry.version)
    if (target && target.checksum !== checksum) throw new DomainError('RULE_MANIFEST_VERSION_CONFLICT', `规则 ${entry.packId}@${entry.version} 已存在但摘要不同`, 409)
    const at = new Date().toISOString()
    if (!target) {
      const id = `rule_sync_${createHash('sha256').update(`${workspaceId}:${entry.platform}:${entry.packId}:${entry.version}`).digest('hex').slice(0, 32)}`
      target = (await repository.insertVersionWithAudit({
        version: { id, workspaceId, packId: entry.packId, name: entry.name, version: entry.version, scope: 'platform', status: 'draft', sourceKind: 'official', sourceReference: entry.sourceReference, sourceCheckedAt: entry.sourceCheckedAt, checksum, checks: entry.checks, createdBy: 'signed-rule-sync', revision: 1, targetId: entry.platform, severity: entry.severity, action: entry.action, ...(entry.effectiveFrom ? { effectiveFrom: entry.effectiveFrom } : {}), ...(entry.effectiveTo ? { effectiveTo: entry.effectiveTo } : {}) },
        audit: { id: `rule_audit_${randomUUID()}`, workspaceId, rulePackId: entry.packId, ruleVersionId: id, version: entry.version, action: 'created', actorId: 'signed-rule-sync', reason: '签名平台规则清单定时导入', occurredAt: at, data: { manifest_generated_at: manifest.generatedAt, checksum } },
      })).version
      existing.push(target)
      imported += 1
    }
    if (target.status !== 'active') {
      target = (await repository.transitionStatusWithAudit({ workspaceId, packId: entry.packId, targetId: target.id, status: 'active', actorId: 'signed-rule-sync', reason: '签名平台规则清单定时激活', occurredAt: at, targetAuditId: `rule_audit_${randomUUID()}`, currentAuditId: `rule_audit_${randomUUID()}`, auditData: { manifest_generated_at: manifest.generatedAt, checksum } })).version
      activated += 1
    }
    versions.push({ platform: entry.platform, pack_id: entry.packId, version: entry.version, state: target.status })
  }
  lastPlatformRuleSync.set(workspaceId, Date.now())
  return { state: 'succeeded' as const, imported, activated, manifest_generated_at: manifest.generatedAt, versions }
}

const catalogCategories = [
  { code: '1312', name: '服装 / 防晒外套', fields: ['材质', '成分', '重量', '尺码', '颜色', '功能依据'], platforms: ['jd', 'taobao', 'tmall', 'pinduoduo', 'xiaohongshu', 'douyin'], status: 'active', updatedAt: '今天' },
  { code: '1408', name: '鞋靴 / 户外鞋', fields: ['鞋面材质', '闭合方式', '适用场景', '尺码'], platforms: ['jd', 'taobao', 'pinduoduo'], status: 'active', updatedAt: '昨天' },
  { code: '1503', name: '运动 / 速干裤装', fields: ['面料', '版型', '弹性', '洗护', '尺码'], platforms: ['taobao', 'tmall'], status: 'active', updatedAt: '3 天前' },
] as const

async function persistedRules(workspaceId: string, fillMissingDefaults = false) {
  const repository = ruleRepository()
  if (!repository) return undefined
  const rows = await repository.list(workspaceId)
  if (rows.length && !fillMissingDefaults) return rows.map(publicRule)
  // Bootstrap only the first request for a new workspace. This keeps the
  // durable rule center usable after migration without silently changing the
  // in-memory fixture registry used by local tests.
  for (const seed of defaultRuleCenterSeeds) {
    if (rows.some(row => row.id === `${seed.packId}@${seed.version}`)) continue
    const at = seed.createdAt ?? new Date().toISOString()
    const checksum = createHash('sha256').update(canonicalJson({ packId: seed.packId, version: seed.version, scope: seed.scope, source: seed.source, checks: seed.checks ?? {} })).digest('hex')
    try {
      await repository.insertVersion({ id: `${seed.packId}@${seed.version}`, workspaceId, packId: seed.packId, name: seed.name, version: seed.version, scope: seed.scope, status: seed.status ?? 'draft', sourceKind: seed.source.kind, sourceReference: seed.source.reference, sourceCheckedAt: seed.source.checkedAt, checksum, checks: (seed.checks ?? {}) as Record<string, unknown>, createdBy: seed.createdBy ?? 'system', revision: 1, ...(seed.effectiveFrom ? { effectiveFrom: seed.effectiveFrom } : {}), ...(seed.effectiveTo ? { effectiveTo: seed.effectiveTo } : {}), ...(seed.severity ? { severity: seed.severity } : {}), ...(seed.action ? { action: seed.action } : {}), ...(seed.targetId ? { targetId: seed.targetId } : {}), ...(seed.scopeValue ? { scopeValue: seed.scopeValue } : {}), activatedAt: seed.status === 'active' ? at : null })
      await repository.appendAudit({ id: `rule_audit_${randomUUID()}`, workspaceId, rulePackId: seed.packId, ruleVersionId: `${seed.packId}@${seed.version}`, version: seed.version, action: 'created', actorId: seed.createdBy ?? 'system', reason: 'workspace rule bootstrap', occurredAt: at, data: {} })
      if (seed.status === 'active') await repository.appendAudit({ id: `rule_audit_${randomUUID()}`, workspaceId, rulePackId: seed.packId, ruleVersionId: `${seed.packId}@${seed.version}`, version: seed.version, action: 'activated', actorId: seed.createdBy ?? 'system', reason: 'workspace rule bootstrap', occurredAt: at, data: {} })
    } catch { /* another request may have bootstrapped this workspace */ }
  }
  return (await repository.list(workspaceId)).map(publicRule)
}

type RuleEvaluationScope = { platform: Platform; category?: string; brand?: string; store?: string; campaign?: string }

async function evaluationRules(workspaceId: string, context?: RuleEvaluationScope): Promise<{ availableRuleVersionIds: string[]; forbiddenTerms: string[]; ruleHits: import('../../../packages/review/src/rule-center.js').RuleHit[] } | undefined> {
  const repository = ruleRepository()
  if (!repository) return undefined
  let rows = await repository.list(workspaceId)
  // Older and concurrently bootstrapped workspaces can contain only a subset
  // of the immutable defaults. Fill missing packs before review; otherwise a
  // task can freeze an in-memory default that the durable reviewer cannot see.
  if (rows.length === 0 || defaultRuleCenterSeeds.some(seed => !rows.some(row => row.id === `${seed.packId}@${seed.version}`))) {
    await persistedRules(workspaceId, true)
    rows = await repository.list(workspaceId)
  }
  const scopeOrder = ['global', 'platform', 'category', 'brand', 'store', 'campaign']
  const active = rows.filter(row => {
    if (row.status !== 'active') return false
    if (!context) return true
    const scope = row.scope
    if (scope === 'global') return true
    const expected = context[scope as keyof RuleEvaluationScope]
    const target = row.targetId ?? row.scopeValue
    return typeof expected === 'string' && expected.length > 0 && typeof target === 'string' && target === expected
  }).sort((left, right) => scopeOrder.indexOf(left.scope) - scopeOrder.indexOf(right.scope))
  const terms = new Set<string>()
  for (const row of active) {
    const checks = row.checks as Record<string, unknown>
    const values = Array.isArray(checks.forbiddenTerms) ? checks.forbiddenTerms : Array.isArray(checks.forbidden_terms) ? checks.forbidden_terms : []
    for (const value of values) if (typeof value === 'string' && value.trim()) terms.add(value)
  }
  const inMemoryHits = context ? service.ruleCenter.evaluate(context).hits : []
  const durableHits = context
    ? (await persistedRuleEvaluation(workspaceId, { platform: context.platform, ...(context.category ? { category: context.category } : {}), ...(context.store ? { storeName: context.store } : {}) })).hits
    : []
  const seenHits = new Set<string>()
  const ruleHits = [...inMemoryHits, ...durableHits].filter(hit => {
    if (seenHits.has(hit.ruleVersionId)) return false
    seenHits.add(hit.ruleVersionId)
    return true
  })
  // Availability answers whether a frozen immutable version still exists and
  // remains active; applicability is separately represented by the scoped
  // terms and hits above. Comparing frozen category fallbacks only against the
  // currently matched rows creates a false MISSING_RULE_VERSION finding.
  return { availableRuleVersionIds: rows.filter(row => row.status === 'active').map(row => row.version), forbiddenTerms: [...terms], ruleHits }
}

function ruleContextForTask(task: import('../../../packages/application/src/service.js').Task): RuleEvaluationScope {
  const product = service.products.get(task.productId)
  return {
    platform: task.platform,
    ...(product?.category ? { category: product.category } : {}),
    ...(product?.storeName ? { store: product.storeName } : {}),
  }
}

async function generationRulePreflight(workspaceId: string, productId: string) {
  const product = service.products.get(productId)
  if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '生成请求引用的商品不存在或不属于当前工作区', 404)
  let category = product.category
  if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') {
    const repository = persistence.brandUnits ?? memoryBrandUnits
    const candidates = (await repository.listCanonicalProducts({ workspaceId })).filter(row => row.sourceProductId === product.id)
    if (candidates.length > 1) throw new DomainError('CANONICAL_PRODUCT_AMBIGUOUS', '生成前发现多个规范商品映射，已阻断规则预检', 409, { product_id: product.id, canonical_product_ids: candidates.map(row => row.id), next_action: 'canonical.product.consistency' })
    const canonical = candidates[0]
    if (!canonical) throw new DomainError('CANONICAL_PRODUCT_MAPPING_REQUIRED', '生成前缺少规范商品映射，已阻断规则预检', 409, { product_id: product.id, next_action: 'canonical.product.consistency' })
    if (!canonical.facts || Object.keys(canonical.facts).length === 0) throw new DomainError('CANONICAL_PRODUCT_FACTS_REQUIRED', '生成前缺少规范商品事实，已阻断规则预检', 409, { product_id: product.id, canonical_product_id: canonical.id, next_action: 'catalog.facts.confirm' })
    category = typeof canonical.facts.category === 'string' && canonical.facts.category.trim() ? canonical.facts.category.trim() : undefined
  }
  const context: RuleEvaluationScope = { platform: product.platform, ...(category ? { category } : {}), ...(product.storeName ? { store: product.storeName } : {}) }
  const evaluation = service.ruleCenter.evaluate(context)
  const durable = await hydrateDurableRuleSnapshot(workspaceId, product)
  const findings = [...evaluation.findings, ...durable.findings]
  const seenHits = new Set<string>()
  const ruleHits = [...evaluation.hits, ...durable.hits].filter(hit => {
    if (seenHits.has(hit.ruleVersionId)) return false
    seenHits.add(hit.ruleVersionId)
    return true
  })
  const forbiddenTerms = new Set(evaluation.checks.forbiddenTerms)
  const currentRules = await evaluationRules(workspaceId, context)
  for (const term of currentRules?.forbiddenTerms ?? []) forbiddenTerms.add(term)
  return { blocking: findings.some(finding => finding.severity === 'error'), finding_count: findings.length, rule_hits: ruleHits, forbidden_terms: [...forbiddenTerms], findings }
}

async function requireGenerationRulePreflight(workspaceId: string, productId: string, message = '当前店铺平台规则存在阻断项，不能继续生成') {
  const rulePreflight = await generationRulePreflight(workspaceId, productId)
  if (rulePreflight.blocking) throw new DomainError('PLATFORM_RULE_PREFLIGHT_BLOCKED', message, 409, { rule_preflight: rulePreflight })
  return rulePreflight
}

function requireRuleSafeGenerationText(rulePreflight: Awaited<ReturnType<typeof generationRulePreflight>>, values: unknown[], message = '生成请求包含当前平台规则禁用表达') {
  const text = values.filter(value => value !== undefined && value !== null).map(value => typeof value === 'string' ? value : JSON.stringify(value)).join('\n')
  const matchedTerms = rulePreflight.forbidden_terms.filter(term => term && text.includes(term))
  if (matchedTerms.length) throw new DomainError('PLATFORM_RULE_CONTENT_BLOCKED', message, 409, { matched_terms: matchedTerms, rule_preflight: rulePreflight })
}

async function requireCurrentPublishReview(workspaceId: string, task: ReturnType<typeof service.getTask>) {
  if (!task.contentVersionId) throw new DomainError('CONTENT_NOT_APPROVED', '内容未批准，不能准备发布', 409)
  const report = service.reviewContentReport(workspaceId, task.contentVersionId, await evaluationRules(workspaceId, ruleContextForTask(task)))
  const blocking = report.findings.filter(finding => finding.severity === 'error' && finding.status === 'open')
  if (blocking.length) throw new DomainError('PUBLISH_RULE_REVIEW_BLOCKED', '内容未通过当前店铺平台的最新规则复检，不能准备发布', 409, { review: report, blocking_findings: blocking })
  return report
}

/**
 * Evaluate rules restored from PostgreSQL as part of generation preflight.
 * The in-memory RuleCenter is useful for fixture defaults, but it is not the
 * source of truth after a restart. Keep this projection structurally aligned
 * with RuleCenter.evaluate so callers receive the same hits and findings.
 */
async function persistedRuleEvaluation(workspaceId: string, product: { platform: Platform; category?: string; storeName?: string }): Promise<{ findings: Array<{ code: 'RULE_EXPIRED' | 'RULE_NOT_YET_EFFECTIVE' | 'RULE_PRIORITY_CONFLICT'; severity: 'error' | 'warning'; action: 'block' | 'warn' | 'review' | 'allow'; field: 'rules'; ruleVersionId: string; message: string }>; hits: RuleHit[] }> {
  const repository = ruleRepository()
  if (!repository) return { findings: [] as Array<{ code: 'RULE_EXPIRED' | 'RULE_NOT_YET_EFFECTIVE' | 'RULE_PRIORITY_CONFLICT'; severity: 'error' | 'warning'; action: 'block' | 'warn' | 'review' | 'allow'; field: 'rules'; ruleVersionId: string; message: string }>, hits: [] }
  let rows = await repository.list(workspaceId)
  if (!rows.length) {
    await persistedRules(workspaceId, true)
    rows = await repository.list(workspaceId)
  }
  const scopeOrder = ['global', 'platform', 'category', 'brand', 'store', 'campaign']
  const context: Record<string, string | undefined> = { platform: product.platform, category: product.category, store: product.storeName }
  const applicable: PersistedRuleVersion[] = []
  const findings: Array<{ code: 'RULE_EXPIRED' | 'RULE_NOT_YET_EFFECTIVE' | 'RULE_PRIORITY_CONFLICT'; severity: 'error' | 'warning'; action: 'block' | 'warn' | 'review' | 'allow'; field: 'rules'; ruleVersionId: string; message: string }> = []
  const now = Date.now()
  for (const row of rows) {
    if (row.status !== 'active' && row.status !== 'expired') continue
    const expected = row.scope === 'global' ? undefined : context[row.scope]
    const target = row.targetId ?? row.scopeValue
    if (row.scope !== 'global' && (!expected || !target || expected !== target)) continue
    const action = persistedRuleAction(row)
    const severity = persistedRuleSeverity(row)
    if (row.status === 'expired') {
      findings.push({ code: 'RULE_EXPIRED', ...persistedRuleExpirationPolicy(row), field: 'rules', ruleVersionId: row.id, message: `规则 ${row.version} 已标记为过期，不能用于本次生成` })
      continue
    }
    const from = row.effectiveFrom ? Date.parse(String(row.effectiveFrom)) : Number.NEGATIVE_INFINITY
    const to = row.effectiveTo ? Date.parse(String(row.effectiveTo)) : Number.POSITIVE_INFINITY
    if ((row.effectiveFrom && Number.isNaN(from)) || (row.effectiveTo && Number.isNaN(to))) continue
    if (now < from) {
      findings.push({ code: 'RULE_NOT_YET_EFFECTIVE', severity, action, field: 'rules', ruleVersionId: row.id, message: `规则 ${row.version} 尚未到生效时间 ${row.effectiveFrom}` })
      continue
    }
    if (now >= to) {
      findings.push({ code: 'RULE_EXPIRED', ...persistedRuleExpirationPolicy(row), field: 'rules', ruleVersionId: row.id, message: `规则 ${row.version} 已于 ${row.effectiveTo} 过期，不能用于本次生成` })
      continue
    }
    applicable.push(row)
  }
  applicable.sort((left, right) => scopeOrder.indexOf(left.scope) - scopeOrder.indexOf(right.scope))
  for (let higherIndex = 0; higherIndex < applicable.length; higherIndex += 1) {
    const higher = applicable[higherIndex]!
    for (let lowerIndex = higherIndex + 1; lowerIndex < applicable.length; lowerIndex += 1) {
      const lower = applicable[lowerIndex]!
      if (higher.scope === lower.scope || persistedRuleAction(higher) !== 'block' || persistedRuleAction(lower) !== 'allow') continue
      const sharedKeys = persistedRuleConflictIntersection(higher, lower)
      if (sharedKeys.length) findings.push({ code: 'RULE_PRIORITY_CONFLICT', severity: 'error', action: 'block', field: 'rules', ruleVersionId: lower.id, message: `低优先级规则 ${lower.version} 不能覆盖 ${higher.scope} 范围的硬阻断规则 ${higher.version}（冲突键：${sharedKeys.join('、')}）` })
    }
  }
  const hits: RuleHit[] = applicable.map(row => {
    const checks = persistedRuleChecks(row)
    return { ruleVersionId: row.id, version: row.version, scope: row.scope as RuleHit['scope'], action: persistedRuleAction(row), severity: persistedRuleSeverity(row), matchedChecks: [...checks.forbiddenTerms.map(() => 'forbiddenTerms'), ...checks.requiredFields.map(() => 'requiredFields'), ...checks.conflictKeys.map(() => 'conflictKeys')] }
  })
  return { findings, hits }
}

function persistedRuleAction(row: Pick<PersistedRuleVersion, 'action'>): 'block' | 'warn' | 'review' | 'allow' {
  return row.action === 'warn' || row.action === 'review' || row.action === 'allow' ? row.action : 'block'
}

function persistedRuleSeverity(row: Pick<PersistedRuleVersion, 'severity'>): 'error' | 'warning' {
  return row.severity === 'warning' ? 'warning' : 'error'
}

function persistedRuleExpirationPolicy(row: Pick<PersistedRuleVersion, 'scope' | 'severity' | 'action'>) {
  if (row.scope === 'platform') return { severity: 'error' as const, action: 'block' as const }
  const action = persistedRuleAction(row)
  const blocking = persistedRuleSeverity(row) === 'error' || action === 'block'
  return { severity: blocking ? 'error' as const : 'warning' as const, action: blocking ? 'block' as const : action }
}

function persistedRuleStringList(checks: Record<string, unknown>, camel: string, snake: string) {
  const values = Array.isArray(checks[camel]) ? checks[camel] : Array.isArray(checks[snake]) ? checks[snake] : []
  return values.filter((value): value is string => typeof value === 'string' && Boolean(value.trim()))
}

function persistedRuleChecks(row: Pick<PersistedRuleVersion, 'checks'>) {
  const checks = row.checks as Record<string, unknown>
  return {
    forbiddenTerms: persistedRuleStringList(checks, 'forbiddenTerms', 'forbidden_terms'),
    requiredFields: persistedRuleStringList(checks, 'requiredFields', 'required_fields'),
    conflictKeys: persistedRuleStringList(checks, 'conflictKeys', 'conflict_keys'),
  }
}

function persistedRuleConflictKeys(row: Pick<PersistedRuleVersion, 'checks'>) {
  const checks = persistedRuleChecks(row)
  const normalize = (value: string) => value.normalize('NFKC').trim().toLocaleLowerCase('zh-CN').replace(/\s+/gu, ' ')
  return new Set([
    ...checks.forbiddenTerms.map(value => `term:${normalize(value)}`),
    ...checks.requiredFields.map(value => `field:${normalize(value)}`),
    ...checks.conflictKeys.map(value => `conflict:${normalize(value)}`),
  ])
}

function persistedRuleConflictIntersection(higher: Pick<PersistedRuleVersion, 'checks'>, lower: Pick<PersistedRuleVersion, 'checks'>) {
  const higherKeys = persistedRuleConflictKeys(higher)
  return [...persistedRuleConflictKeys(lower)].filter(key => higherKeys.has(key)).sort()
}

async function hydrateDurableRuleSnapshot(workspaceId: string, product: { id: string; platform: Platform; category?: string; storeName?: string }) {
  const durable = await persistedRuleEvaluation(workspaceId, product)
  const repository = ruleRepository()
  if (!repository) return durable
  const hitIds = new Set(durable.hits.map(hit => hit.ruleVersionId))
  const rows = await repository.list(workspaceId)
  const forbiddenTerms = new Set<string>()
  const requiredFields = new Set<string>()
  for (const row of rows) {
    if (!hitIds.has(row.id)) continue
    const checks = row.checks as Record<string, unknown>
    const terms = Array.isArray(checks.forbiddenTerms) ? checks.forbiddenTerms : Array.isArray(checks.forbidden_terms) ? checks.forbidden_terms : []
    const fields = Array.isArray(checks.requiredFields) ? checks.requiredFields : Array.isArray(checks.required_fields) ? checks.required_fields : []
    for (const value of terms) if (typeof value === 'string' && value.trim()) forbiddenTerms.add(value)
    for (const value of fields) if (typeof value === 'string' && value.trim()) requiredFields.add(value)
  }
  service.setDurableRuleSnapshot(workspaceId, product.id, { ruleVersionIds: durable.hits.map(hit => hit.version), ruleChecks: { forbiddenTerms: [...forbiddenTerms], requiredFields: [...requiredFields] } })
  return durable
}

/** Evaluate durable workspace rules for scheduler safety. The application
 * RuleCenter is intentionally fixture-friendly, but production automation
 * must also see rules created through the PostgreSQL rule repository after a
 * process restart. Keep the blocking subset aligned with RuleCenter.evaluate.
 */
async function persistedRuleHasBlockingRisk(workspaceId: string, product: { platform: Platform; category?: string; storeName?: string }) {
  return (await persistedRuleEvaluation(workspaceId, product)).findings.some(finding => finding.severity === 'error')
}

const MAX_ASSET_BYTES = 50 * 1024 * 1024

function configuredAssetLimit(): number {
  const configured = Number(process.env.ASSET_UPLOAD_MAX_BYTES ?? MAX_ASSET_BYTES)
  if (!Number.isSafeInteger(configured) || configured < 1 || configured > MAX_ASSET_BYTES) {
    throw new DomainError('ASSET_STORAGE_CONFIG_INVALID', `ASSET_UPLOAD_MAX_BYTES 必须是 1-${MAX_ASSET_BYTES} 的整数`, 503)
  }
  return configured
}

function configuredStorageQuotaLimit(): number {
  const configured = Number(process.env.ASSET_STORAGE_QUOTA_BYTES ?? (isProduction() ? NaN : 5 * 1024 * 1024 * 1024))
  if (!Number.isSafeInteger(configured) || configured < 1) throw new DomainError('ASSET_STORAGE_QUOTA_CONFIG_INVALID', '生产环境必须配置有效的 ASSET_STORAGE_QUOTA_BYTES', 503)
  return configured
}

function getAssetStorage(): ObjectStoragePort {
  if (assetStorage) return assetStorage
  if (isProduction()) {
    if (process.env.DEPLOYMENT_PROFILE === 'local_acceptance' && process.env.ALLOW_LOCAL_DURABLE_OBJECT_STORAGE === 'true') {
      const root = process.env.ASSET_STORAGE_ROOT?.trim()
      if (!root || !root.startsWith('/var/lib/merchant-assets/')) throw new DomainError('ASSET_STORAGE_CONFIG_INVALID', '本地生产验收对象存储必须位于专用持久卷 /var/lib/merchant-assets/', 503)
      assetStorage = new LocalObjectStorage(root, { maxObjectBytes: configuredAssetLimit() })
      return assetStorage
    }
    const bucket = process.env.ASSET_STORAGE_BUCKET?.trim()
    const region = process.env.ASSET_STORAGE_REGION?.trim()
    const endpoint = process.env.ASSET_STORAGE_ENDPOINT?.trim()
    const kmsKeyId = process.env.ASSET_STORAGE_KMS_KEY_ID?.trim()
    if (!bucket || !region || !endpoint || !kmsKeyId || !/^https:\/\//u.test(endpoint)) throw new DomainError('ASSET_STORAGE_NOT_CONFIGURED', '生产环境对象存储必须配置 bucket、region、HTTPS endpoint 和 KMS key', 503)
    const client = new S3Client({ region, endpoint, forcePathStyle: process.env.ASSET_STORAGE_FORCE_PATH_STYLE === 'true' })
    const request = (key: string) => ({ Bucket: bucket, Key: key })
    const transport: CloudObjectTransport = {
      async head(key) {
        try {
          const result = await client.send(new HeadObjectCommand(request(key)))
          return { contentType: result.ContentType, sizeBytes: result.ContentLength, metadata: result.Metadata }
        } catch (error) {
          if (isS3NotFound(error)) return null
          throw error
        }
      },
      async list(prefix) {
        const keys: string[] = []
        let continuationToken: string | undefined
        do {
          const page = await client.send(new ListObjectsV2Command({ Bucket: bucket, Prefix: prefix, ContinuationToken: continuationToken }))
          for (const item of page.Contents ?? []) if (item.Key) keys.push(item.Key)
          continuationToken = page.IsTruncated ? page.NextContinuationToken : undefined
        } while (continuationToken)
        return keys
      },
      async get(key) {
        const result = await client.send(new GetObjectCommand(request(key)))
        if (!result.Body || typeof result.Body.transformToByteArray !== 'function') throw new Error('S3_OBJECT_BODY_UNAVAILABLE')
        return { body: new Uint8Array(await result.Body.transformToByteArray()), contentType: result.ContentType, metadata: result.Metadata }
      },
      async put(key, input) {
        await client.send(new PutObjectCommand({ ...request(key), Body: input.body, ContentType: input.contentType, Metadata: input.metadata, ...(input.ifAbsent ? { IfNoneMatch: '*' } : {}), ServerSideEncryption: 'aws:kms', SSEKMSKeyId: kmsKeyId }))
      },
      async delete(key) { await client.send(new DeleteObjectCommand(request(key))) },
    }
    assetStorage = new S3CompatibleObjectStorage(transport, { keyPrefix: process.env.ASSET_STORAGE_PREFIX?.trim(), maxObjectBytes: configuredAssetLimit() })
    return assetStorage
  }
  const root = process.env.ASSET_STORAGE_ROOT?.trim() || '/tmp/merchant-marketing-codex-assets'
  assetStorage = new LocalObjectStorage(root, { maxObjectBytes: configuredAssetLimit() })
  return assetStorage
}

async function getStoredObjectWithRetry(workspaceId: string, key: string, options?: { includeQuarantine?: boolean }) {
  return withObjectStorageReadRetry(() => getAssetStorage().get(workspaceId, key, options))
}

async function compensateStoredObject(workspaceId: string, objectKey: string, reason: string): Promise<boolean> {
  try {
    await getAssetStorage().delete(workspaceId, objectKey, { includeQuarantine: true })
    return true
  } catch (deleteError) {
    try {
      await (persistence.objectOrphans ?? memoryObjectOrphans).enqueue({ workspaceId, objectKey, reason, lastError: deleteError instanceof Error ? deleteError.message : 'object compensation delete failed' })
    } catch (enqueueError) {
      throw new DomainError('OBJECT_ORPHAN_RECORD_FAILED', '对象补偿删除与孤儿登记同时失败，必须立即触发 P0 存储告警', 503, { object_key: objectKey, delete_error: deleteError instanceof Error ? deleteError.message : 'delete failed', enqueue_error: enqueueError instanceof Error ? enqueueError.message : 'enqueue failed' })
    }
    return false
  }
}

/** Reclaim settled quota only after physical deletion is confirmed. */
export async function releaseStorageQuotaAfterConfirmedDeletion(input: { quota: StorageQuotaRepository; workspaceId: string; reservationKey: string; objectKey: string; deleteObject: () => Promise<boolean | void> }) {
  const deleted = await input.deleteObject()
  if (deleted === false) return
  try {
    await input.quota.releaseAfterPhysicalDeletion({ workspaceId: input.workspaceId, reservationKey: input.reservationKey, receipt: { objectKey: input.objectKey, deletedAt: new Date().toISOString(), verification: 'delete_ack' } })
  } catch { /* preserve the original failure; reconciliation will surface the reservation */ }
}

async function compensateStoredAsset(workspaceId: string, assetId: string, objectKey: string, reason: string) {
  const quota = persistence.storageQuota
  if (!quota) {
    await compensateStoredObject(workspaceId, objectKey, reason)
    return
  }
  await releaseStorageQuotaAfterConfirmedDeletion({ quota, workspaceId, reservationKey: `asset:${assetId}`, objectKey, deleteObject: () => compensateStoredObject(workspaceId, objectKey, reason) })
}

function assetReservationKeyForDeletedObject(workspaceId: string, objectKey: string): string | undefined {
  const parts = objectKey.split('/')
  if (parts.length < 4 || !['quarantine', 'clean'].includes(parts[0] ?? '') || parts[1] !== workspaceId || !parts[2]?.trim() || parts[3]?.endsWith('.merchant-meta.json')) return undefined
  return `asset:${parts[2]}`
}

async function putQuarantineObject(input: PutQuarantineObjectInput) {
  await persistenceReady
  const quota = persistence.storageQuota
  const reservationKey = `asset:${input.assetId}`
  if (quota) {
    await persistence.ensureWorkspace?.(input.workspaceId)
    await quota.reserve({ workspaceId: input.workspaceId, reservationKey, assetId: input.assetId, bytes: input.body.byteLength, limitBytes: configuredStorageQuotaLimit() })
  }
  let storedKey: string | undefined
  try {
    const stored = await getAssetStorage().putQuarantine(input)
    storedKey = stored.key
    if (quota) await quota.settle({ workspaceId: input.workspaceId, reservationKey, actualBytes: stored.sizeBytes })
    return stored
  } catch (error) {
    let deletionConfirmed = !storedKey
    try {
      if (storedKey) {
        if (!quota) await compensateStoredObject(input.workspaceId, storedKey, 'storage quota settlement or metadata persistence failed')
        else await releaseStorageQuotaAfterConfirmedDeletion({ quota, workspaceId: input.workspaceId, reservationKey, objectKey: storedKey, deleteObject: () => compensateStoredObject(input.workspaceId, storedKey!, 'storage quota settlement or metadata persistence failed') })
        deletionConfirmed = true
      }
    } finally {
      // A settled reservation can be released only after the object deletion
      // is confirmed.  If deletion is deferred to orphan cleanup, retain the
      // used bytes so the quota cannot be silently under-counted.
      if (quota && !storedKey && deletionConfirmed) {
        try { await quota.release({ workspaceId: input.workspaceId, reservationKey }) } catch { /* preserve the original failure; reconciliation will surface the reservation */ }
      }
    }
    if (!(error instanceof ObjectStoragePartialWriteError)) throw error
    try {
      await (persistence.objectOrphans ?? memoryObjectOrphans).enqueue({ workspaceId: input.workspaceId, objectKey: error.orphanKey, reason: 'object metadata write and compensation cleanup failed', lastError: error.cleanupErrors.map(item => item instanceof Error ? item.message : String(item)).join('; ') })
    } catch (enqueueError) {
      throw new DomainError('OBJECT_ORPHAN_RECORD_FAILED', '对象写入补偿与孤儿登记同时失败，必须立即触发 P0 存储告警', 503, { object_key: error.orphanKey, enqueue_error: enqueueError instanceof Error ? enqueueError.message : 'enqueue failed' })
    }
    throw error.cause
  }
}

type AssetFactExtraction = { facts: Record<string, unknown>; source: 'parser' | 'model_ocr' }
let assetFactParserForTests: ((input: { name: string; mimeType: string; body: Uint8Array; usageContext?: { workspaceId?: string; actionId?: string; runKey?: string }; signal?: AbortSignal }) => Promise<AssetFactExtraction>) | undefined
let assetParseRepositoryForTests: AssetParseRepository | undefined

export function setAssetParseRuntimeForTests(input?: { repository?: AssetParseRepository; parse?: typeof assetFactParserForTests }) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('ASSET_PARSE_RUNTIME_TEST_ONLY')
  assetParseRepositoryForTests = input?.repository
  assetFactParserForTests = input?.parse
}

async function parseAssetFacts(input: { name: string; mimeType: string; body: Uint8Array; usageContext?: { workspaceId?: string; actionId?: string; runKey?: string }; signal?: AbortSignal }): Promise<AssetFactExtraction> {
  if (assetFactParserForTests) return assetFactParserForTests(input)
  try {
    return { facts: await parseDocumentFacts(input), source: 'parser' as const }
  } catch (error) {
    const image = input.mimeType.toLowerCase().startsWith('image/')
    if (!(error instanceof DocumentParseError) || !image || !imageFactsExtractor) throw error
    return { facts: await imageFactsExtractor.extract(input), source: 'model_ocr' as const }
  }
}

const MAX_ASSET_PARSE_TIMEOUT_MS = 5 * 60 * 1_000
const MAX_ASSET_PARSE_ATTEMPTS = 20

function assetParseRuntimeConfig() {
  const timeoutMs = Number(process.env.ASSET_PARSE_TIMEOUT_MS ?? 30_000)
  const maxAttempts = Number(process.env.ASSET_PARSE_MAX_ATTEMPTS ?? 3)
  if (!Number.isSafeInteger(timeoutMs) || timeoutMs < 1 || timeoutMs > MAX_ASSET_PARSE_TIMEOUT_MS) throw new DomainError('ASSET_PARSE_CONFIG_INVALID', `ASSET_PARSE_TIMEOUT_MS 必须是 1 到 ${MAX_ASSET_PARSE_TIMEOUT_MS} 的整数`, 503)
  if (!Number.isSafeInteger(maxAttempts) || maxAttempts < 1 || maxAttempts > MAX_ASSET_PARSE_ATTEMPTS) throw new DomainError('ASSET_PARSE_CONFIG_INVALID', `ASSET_PARSE_MAX_ATTEMPTS 必须是 1 到 ${MAX_ASSET_PARSE_ATTEMPTS} 的整数`, 503)
  return { timeoutMs, maxAttempts }
}

async function assetParseRepository() {
  await persistenceReady
  return assetParseRepositoryForTests ?? persistence.assetParse ?? memoryAssetParse
}

function sameDurableParseRecord(left: AssetParseRecord | undefined, right: AssetParseRecord) {
  if (!left || left.state !== right.state || left.updatedAt !== right.updatedAt || left.attempts !== right.attempts) return false
  return left.state !== 'succeeded' || right.state !== 'succeeded' || canonicalJson(left.facts) === canonicalJson(right.facts)
}

async function assertDurableParseRecordCurrent(repository: AssetParseRepository, record: AssetParseRecord) {
  if (!sameDurableParseRecord(await repository.get({ workspaceId: record.workspaceId, assetId: record.assetId }), record)) throw new DomainError('ASSET_PARSE_STALE_RESULT', '素材解析结果已被更新的租约或人工事实取代，旧结果未写入业务状态', 409)
}

function durableParseErrorContext(error: unknown): ParseErrorContext {
  if (error instanceof DocumentParseError) return error.context
  return { code: 'parser_failure', message: error instanceof Error ? error.message.slice(0, 1_000) : '素材解析失败', manualAction: 'asset.facts.confirm' }
}

async function executeDurableAssetParse(workspaceId: string, assetId: string, req: IncomingMessage) {
  await requirePluginWalletAccess(workspaceId)
  const asset = assetForWorkspace(workspaceId, assetId)
  if (asset.scanStatus !== 'clean') throw new DomainError('ASSET_PARSE_SCAN_REQUIRED', '素材必须完成安全扫描后才能解析', 409)
  if (asset.parseStatus === 'succeeded' && asset.extractedFactsSource === 'manual') throw new DomainError('ASSET_FACTS_MANUAL_LOCKED', '素材事实已由商家人工确认；自动解析不能覆盖或降级人工确认结果', 409)
  const repository = await assetParseRepository()
  const { timeoutMs, maxAttempts } = assetParseRuntimeConfig()
  const ocrCandidate = asset.mimeType.toLowerCase().startsWith('image/')
  const ocrDebitKeyPrefix = `asset-parse:${asset.id}:attempt:`
  const actorId = requestActor(req)
  if (ocrCandidate && imageFactsExtractor) requirePlatformModelCostGate('ocr')
  let extraction: AssetFactExtraction | undefined
  let parserFailure: unknown
  let parseAttempt = 0
  let activeDebitKey: string | undefined

  try {
    const executed = await executeAssetParse({
      repository,
      workspaceId,
      assetId: asset.id,
      timeoutMs,
      maxAttempts,
      onClaim: async lease => {
        parseAttempt = lease.attempts
        const processing = service.updateAssetParse({ workspaceId, assetId: asset.id, state: 'processing', source: 'parser' })
        await persistSnapshot(workspaceId, 'asset', processing, processing as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, asset.id, 'asset.parse_processing', processing.revision, { asset_id: asset.id, parse_status: 'processing', attempt: lease.attempts, lease_until: lease.leaseUntil })
      },
      parse: async signal => {
        let debited = false
        const ocrDebitKey = `${ocrDebitKeyPrefix}${parseAttempt}`
        try {
          if (ocrCandidate) {
            await debitPluginWallet({ workspaceId, idempotencyKey: ocrDebitKey, modelRunKey: `asset-parse:${asset.id}`, actorId, description: '图片 OCR/素材解析调用' })
            debited = true
            activeDebitKey = ocrDebitKey
          }
          const stored = await getStoredObjectWithRetry(workspaceId, asset.storageKey)
          extraction = await parseAssetFacts({ name: asset.name, mimeType: asset.mimeType, body: stored.body, usageContext: { workspaceId, actionId: ocrDebitKey, runKey: `asset-parse:${asset.id}` }, signal })
          if (ocrCandidate && extraction.source !== 'model_ocr') {
            await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: ocrDebitKey, actorId, reason: '本次图片由本地解析器完成，未产生 OCR 中转调用' })
            debited = false
            activeDebitKey = undefined
          }
          return extraction.facts
        } catch (error) {
          parserFailure = error
          if (debited && !providerSucceededButSettlementPending(error)) {
            await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: ocrDebitKey, actorId, reason: '素材解析失败' })
            activeDebitKey = undefined
          }
          throw error
        }
      },
      classifyFailure: error => ({ code: error instanceof AssetParseRepositoryError && error.code === 'ASSET_PARSE_EMPTY' ? error.code : 'ASSET_PARSE_FAILED', message: error instanceof AssetParseRepositoryError && error.code === 'ASSET_PARSE_EMPTY' ? 'asset parser returned no facts' : error instanceof Error ? error.message.slice(0, 1_000) : '素材解析失败', retryable: true }),
    })
    await assertDurableParseRecordCurrent(repository, executed.record)
    const source = extraction?.source ?? (asset.extractedFactsSource === 'model_ocr' ? 'model_ocr' : 'parser')
    const alreadySynchronized = executed.replayed && asset.parseStatus === 'succeeded' && asset.extractedFactsSource !== 'manual' && canonicalJson(asset.extractedFacts ?? {}) === canonicalJson(executed.record.facts)
    const parsed = alreadySynchronized ? asset : service.updateAssetParse({ workspaceId, assetId: asset.id, state: 'succeeded', facts: executed.record.facts, source })
    const execution = { ...executionContract('ocr', source === 'model_ocr'), replayed: executed.replayed, attempts: executed.record.attempts }
    if (!alreadySynchronized) {
      await persistSnapshot(workspaceId, 'asset', parsed, parsed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, asset.id, executed.replayed ? 'asset.parse_replayed' : 'asset.parsed', parsed.revision, { asset_id: asset.id, parse_status: parsed.parseStatus, fact_keys: Object.keys(executed.record.facts), source, execution })
    }
    return { ...parsed, execution }
  } catch (error) {
    if (error instanceof AssetParseRepositoryError) {
      if (error.code === 'ASSET_PARSE_BUSY') {
        const current = await repository.get({ workspaceId, assetId: asset.id })
        const retryAfterSeconds = current?.state === 'processing' ? Math.max(1, Math.ceil((Date.parse(current.leaseUntil) - Date.now()) / 1_000)) : 1
        throw new DomainError(error.code, '素材正在由另一个请求解析，请稍后重试', 409, { retry_after_seconds: retryAfterSeconds })
      }
      if (error.code === 'ASSET_PARSE_LEASE_LOST') {
        if (activeDebitKey && !extraction && !providerSucceededButSettlementPending(parserFailure)) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: activeDebitKey, actorId, reason: '素材解析租约已由人工确认或更新请求取代' })
        throw new DomainError('ASSET_PARSE_STALE_RESULT', '素材解析租约已失效，旧结果未写入业务状态', 409)
      }
      if (error.code === 'ASSET_PARSE_ATTEMPTS_EXHAUSTED') throw new DomainError(error.code, '素材自动解析重试次数已用尽，请人工补录事实', 409, { asset_id: asset.id, asset_persisted: true, next_actions: ['asset.facts.confirm'] })
    }
    if (error instanceof DomainError && error.code === 'ASSET_PARSE_STALE_RESULT') throw error
    if (error instanceof AssetParseExecutionError && error.record) {
      if (activeDebitKey && !providerSucceededButSettlementPending(error.cause)) {
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: activeDebitKey, actorId, reason: error.code === 'ASSET_PARSE_TIMEOUT' ? '素材解析超时' : '素材解析失败' })
        activeDebitKey = undefined
      }
      await assertDurableParseRecordCurrent(repository, error.record)
      if (!(asset.parseStatus === 'succeeded' && asset.extractedFactsSource === 'manual')) {
        const context = durableParseErrorContext(parserFailure ?? error)
        const failed = service.updateAssetParse({ workspaceId, assetId: asset.id, state: 'failed', error: error.message, errorContext: context })
        await persistSnapshot(workspaceId, 'asset', failed, failed as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, asset.id, 'asset.parse_failed', failed.revision, { asset_id: asset.id, error_code: error.code, retryable: error.record.retryable, attempts: error.record.attempts })
      }
      const status = error.code === 'ASSET_PARSE_TIMEOUT' ? 504 : 422
      throw new DomainError(error.code, error.message, status, { asset_id: asset.id, asset_persisted: true, retryable: error.record.retryable, attempts: error.record.attempts, next_actions: error.record.retryable ? ['asset.parse', 'asset.facts.confirm'] : ['asset.facts.confirm'] })
    }
    throw error
  }
}

async function confirmDurableAssetFacts(input: { workspaceId: string; assetId: string; facts: Record<string, unknown>; reason: string; req: IncomingMessage }) {
  const asset = assetForWorkspace(input.workspaceId, input.assetId)
  if (asset.scanStatus !== 'clean') throw new DomainError('ASSET_FACTS_SCAN_REQUIRED', '素材完成安全扫描后才能人工确认事实', 409)
  await (await assetParseRepository()).confirm({ workspaceId: input.workspaceId, assetId: asset.id, facts: input.facts })
  const confirmed = service.updateAssetParse({ workspaceId: input.workspaceId, assetId: asset.id, state: 'succeeded', facts: input.facts, source: 'manual', confirmedBy: requestActor(input.req) })
  await persistSnapshot(input.workspaceId, 'asset', confirmed, confirmed as unknown as Record<string, unknown>)
  await persistEvent(input.workspaceId, asset.id, 'asset.facts_manually_confirmed', confirmed.revision, { asset_id: asset.id, fact_keys: Object.keys(input.facts), source: 'manual', confirmed_by: confirmed.factsConfirmedBy, reason: input.reason })
  return confirmed
}

function isS3NotFound(error: unknown) {
  const candidate = error as { $metadata?: { httpStatusCode?: number }; name?: string }
  return candidate?.$metadata?.httpStatusCode === 404 || candidate?.name === 'NotFound' || candidate?.name === 'NoSuchKey'
}

async function initializePersistence(): Promise<ApiPersistence> {
  // Tests and fixture-only local development intentionally keep the memory
  // service. DATABASE_URL is the explicit switch to the durable adapter.
  if (process.env.NODE_ENV === 'test' || (process.env.VITEST === 'true' && !process.env.DATABASE_URL) || (process.env.NODE_ENV !== 'production' && process.env.CONNECTOR_FIXTURE_MODE === 'true' && process.env.PERSISTENCE_MODE !== 'postgres')) return memoryPersistence
  if (!process.env.DATABASE_URL) throw new Error('DATABASE_URL is required outside test runtime; refusing to start with memory persistence')
  const pool = new Pool({ connectionString: process.env.DATABASE_URL, max: Number(process.env.DB_POOL_MAX ?? 20), connectionTimeoutMillis: Number(process.env.DB_CONNECTION_TIMEOUT_MS ?? 3000) })
  if (process.env.NODE_ENV === 'production' && !process.env.OPS_DATABASE_URL) throw new Error('OPS_DATABASE_URL is required in production for the isolated control-plane database role')
  const opsPool = process.env.OPS_DATABASE_URL
    ? new Pool({ connectionString: process.env.OPS_DATABASE_URL, max: Number(process.env.OPS_DB_POOL_MAX ?? 5), connectionTimeoutMillis: Number(process.env.DB_CONNECTION_TIMEOUT_MS ?? 3000) })
    : undefined
  try {
    const sqlPool = pool as unknown as SqlPool
    const opsSqlPool = (opsPool ?? pool) as unknown as SqlPool
    const migrations = await loadMigrations()
    const expectedMigrationVersion = migrations.at(-1)?.version ?? 0
    if (process.env.RUN_MIGRATIONS_ON_STARTUP !== 'false') await runMigrations(sqlPool, migrations)
    const outbox = new PostgresOutboxRepository(sqlPool)
    const business = new PostgresBusinessRepository(sqlPool, { normalizedProjection: true })
    const billing = new PostgresBillingRepository(sqlPool, (client, event) => outbox.appendInTransaction(client, event))
    const commercial = new PostgresCommercialRepository(sqlPool)
    const usage = new PostgresUsageRepository(sqlPool)
    const modelUsage = new PostgresModelUsageRepository(sqlPool)
    const actionLedger = new PostgresActionLedgerRepository(sqlPool)
    const entitlements = new PostgresEntitlementRepository(sqlPool)
    const operations = new PostgresOperationsRepository(sqlPool)
    const subscriptions = new PostgresSubscriptionRepository(sqlPool, (client, event) => outbox.appendInTransaction(client, event))
    const members = new PostgresMembersRepository(sqlPool)
    const commercialExtensions = new PostgresCommercialExtensionsRepository(sqlPool)
    const growth = new PostgresGrowthRepository(sqlPool)
    const alerts = new PostgresOperationalAlertsRepository(sqlPool)
    const dataLifecycle = new PostgresDataLifecycleRepository(sqlPool)
    const rules = new PostgresRuleRepository(sqlPool)
    const brandUnits = new PostgresBrandUnitRepository(sqlPool)
    const objectOrphans = new PostgresObjectOrphanRepository(sqlPool)
    const contextSnapshots = new PostgresContextSnapshotRepository(sqlPool)
    const identities = new PostgresIdentityLifecycleRepository(opsSqlPool)
    const authorization = new PostgresAuthorizationRepository(opsSqlPool)
    const workspaceBootstrap = new PostgresWorkspaceBootstrapRepository(sqlPool)
    const paymentCallbackNonces = new PostgresPaymentCallbackNonceRepository(sqlPool)
    const support = new PostgresSupportRepository(sqlPool)
    const supportSlaReporting = new PostgresSupportSlaReportingRepository(sqlPool)
    const incidents = new PostgresIncidentRepository(sqlPool)
    const featureFlags = new PostgresFeatureFlagsRepository(opsSqlPool)
    const financeSearch = new PostgresFinanceSearchRepository(opsSqlPool)
    const auditCenter = new PostgresAuditCenterRepository(sqlPool)
    // Workspace directory is a platform projection. Keep it on the isolated
    // control-plane role; tenant business repositories remain on merchant_app.
    const opsData = new PostgresOpsDataRepository(opsSqlPool)
    const assetParse = new PostgresAssetParseRepository(sqlPool)
    const assetScanReceipts = new PostgresAssetScanReceiptRepository(sqlPool)
    const assetScanRedrive = new PostgresAssetScanRedriveRepository(sqlPool)
    const assetPromotionCleanup = new PostgresAssetPromotionCleanupRepository(sqlPool)
    const imageContinuationLeases = new PostgresImageContinuationLeaseRepository(sqlPool)
    const imageGenerationExecutions = new PostgresImageGenerationExecutionRepository(sqlPool)
    const reconciliationEvidence = new PostgresReconciliationEvidenceRepository(sqlPool)
    const unifiedLinkAudit = new PostgresUnifiedLinkAuditRepository(sqlPool)
    // The registry tables are deliberately unavailable to the merchant DB
    // role. All mutations use the isolated ops pool; route-level reads expose
    // only currently active approved rows to merchant principals.
    const platformMediaSpecs = new PostgresPlatformMediaSpecRepository(opsSqlPool)
    const mappingPreflightApprovals = new PostgresMappingPreflightApprovalRepository(sqlPool)
    const knowledgeHydration = new PostgresKnowledgeHydrationRepository(sqlPool)
    const storageQuota = new PostgresStorageQuotaRepository(sqlPool)
    const storageReconciliation = new DurableReconciliationStatusStore(new PostgresReconciliationStatusRepository(sqlPool))
    const reconciliationStatuses = new PostgresReconciliationStatusRepository(sqlPool)
    const canonicalBackfillRuns = new PostgresCanonicalBackfillRunRepository(sqlPool)
    const canonicalBackfillConflicts = new PostgresCanonicalBackfillConflictRepository(sqlPool)
    const canonicalBackfillRemediation = new PostgresCanonicalBackfillRemediationRepository(sqlPool)
    const interactiveConfirmationTickets = new PostgresInteractiveConfirmationTicketRepository(sqlPool)
    const executeCanonicalBackfill = (input: { workspaceId: string; dryRun?: boolean; afterProductId?: string; limit?: number }) => executeCanonicalProductBackfill(sqlPool, input)
    const ensureWorkspace = async (workspaceId: string) => {
      const client = await pool.connect()
      try {
        await client.query('BEGIN')
        await client.query(`SELECT set_config('app.workspace_id', $1, true)`, [workspaceId])
        await client.query(`INSERT INTO workspaces (id, status) VALUES ($1, 'active') ON CONFLICT (id) DO NOTHING`, [workspaceId])
        await client.query('COMMIT')
      } catch (error) {
        try { await client.query('ROLLBACK') } catch { /* preserve original error */ }
        throw error
      } finally {
        client.release()
      }
    }
    const persistSnapshotAndEvent = async (input: { workspaceId: string; entityType: BusinessEntityType; entityId: string; entityVersion: number; payload: Record<string, unknown>; eventType: string; eventPayload: Record<string, unknown> }) => {
      await ensureWorkspace(input.workspaceId)
      await withWorkspaceTransaction(sqlPool, input.workspaceId, async client => {
        let saved
        try {
          saved = await business.saveInTransaction(client, {
            workspaceId: input.workspaceId,
            entityType: input.entityType,
            entityId: input.entityId,
            entityVersion: input.entityVersion,
            payload: input.payload,
          })
        } catch (error) {
          if (error instanceof BusinessSnapshotVersionConflictError) throw new DomainError(error.code, '业务状态已被其他实例以相同版本更新，请刷新后重试', 409, { entity_type: error.entityType, entity_id: error.entityId, expected_version: error.entityVersion })
          throw error
        }
        if (saved.entityVersion !== input.entityVersion) throw new DomainError('BUSINESS_SNAPSHOT_VERSION_CONFLICT', '业务状态已被其他实例更新，请刷新后重试', 409, { entity_type: input.entityType, entity_id: input.entityId, expected_version: input.entityVersion, current_version: saved.entityVersion })
        await outbox.appendInTransaction(client, {
          workspaceId: input.workspaceId,
          aggregateId: input.entityId,
          eventType: input.eventType,
          sequence: input.entityVersion,
          payload: input.eventPayload,
        })
      })
    }
    const persistSnapshotsAndEvent = async (input: { workspaceId: string; snapshots: SnapshotInput[]; aggregateId: string; eventType: string; sequence: number; eventPayload: Record<string, unknown> }) => {
      await ensureWorkspace(input.workspaceId)
      await withWorkspaceTransaction(sqlPool, input.workspaceId, async client => {
        for (const snapshot of input.snapshots) {
          let saved
          try {
            saved = await business.saveInTransaction(client, { workspaceId: input.workspaceId, ...snapshot })
          } catch (error) {
            if (error instanceof BusinessSnapshotVersionConflictError) throw new DomainError(error.code, '业务状态已被其他实例以相同版本更新，请刷新后重试', 409, { entity_type: error.entityType, entity_id: error.entityId, expected_version: error.entityVersion })
            throw error
          }
          if (saved.entityVersion !== snapshot.entityVersion) throw new DomainError('BUSINESS_SNAPSHOT_VERSION_CONFLICT', '业务状态已被其他实例更新，请刷新后重试', 409, { entity_type: snapshot.entityType, entity_id: snapshot.entityId, expected_version: snapshot.entityVersion, current_version: saved.entityVersion })
        }
        await outbox.appendInTransaction(client, { workspaceId: input.workspaceId, aggregateId: input.aggregateId, eventType: input.eventType, sequence: input.sequence, payload: input.eventPayload })
      })
    }
    const persistPublishTransaction = async (input: { workspaceId: string; snapshots: SnapshotInput[]; aggregateId: string; eventType: string; sequence: number; eventPayload: Record<string, unknown>; finalizeTicketInTransaction: (client: SqlClient) => Promise<void> }) => {
      await ensureWorkspace(input.workspaceId)
      await withWorkspaceTransaction(sqlPool, input.workspaceId, async client => {
        for (const snapshot of input.snapshots) {
          let saved
          try {
            saved = await business.saveInTransaction(client, { workspaceId: input.workspaceId, ...snapshot })
          } catch (error) {
            if (error instanceof BusinessSnapshotVersionConflictError) throw new DomainError(error.code, '业务状态已被其他实例以相同版本更新，请刷新后重试', 409, { entity_type: error.entityType, entity_id: error.entityId, expected_version: error.entityVersion })
            throw error
          }
          if (saved.entityVersion !== snapshot.entityVersion) throw new DomainError('BUSINESS_SNAPSHOT_VERSION_CONFLICT', '业务状态已被其他实例更新，请刷新后重试', 409, { entity_type: snapshot.entityType, entity_id: snapshot.entityId, expected_version: snapshot.entityVersion })
        }
        await outbox.appendInTransaction(client, { workspaceId: input.workspaceId, aggregateId: input.aggregateId, eventType: input.eventType, sequence: input.sequence, payload: input.eventPayload })
        await input.finalizeTicketInTransaction(client)
      })
    }
    const persistTrustedScanPromotion = async (input: TrustedScanPromotionPersistenceInput) => {
      await ensureWorkspace(input.workspaceId)
      return withWorkspaceTransaction(sqlPool, input.workspaceId, async client => {
        try {
          // Receipt first: migration 086's clean-snapshot trigger resolves this
          // row within the same transaction.
          await assetScanReceipts.appendInTransaction(client, input.receipt)
          for (const snapshot of input.snapshots) {
            const saved = await business.saveInTransaction(client, { workspaceId: input.workspaceId, ...snapshot })
            if (saved.entityVersion !== snapshot.entityVersion) throw new BusinessSnapshotVersionConflictError(input.workspaceId, snapshot.entityType, snapshot.entityId, snapshot.entityVersion)
          }
          const readyEvent = await outbox.appendInTransaction(client, { workspaceId: input.workspaceId, aggregateId: input.aggregateId, eventType: input.eventType, sequence: input.sequence, payload: input.eventPayload })
          return (await assetPromotionCleanup.createInTransaction(client, { ...input.cleanup, readyOutboxEventId: readyEvent.id })).task
        } catch (error) {
          if (error instanceof AssetScanReceiptConflictError) throw new DomainError(error.code, 'asset source revision already has a different immutable scan receipt', 409)
          if (error instanceof BusinessSnapshotVersionConflictError) throw new DomainError(error.code, '业务状态已被其他实例以相同版本更新，请刷新后重试', 409, { entity_type: error.entityType, entity_id: error.entityId, expected_version: error.entityVersion })
          throw error
        }
      })
    }
    const getWorkspaceStatus = async (workspaceId: string): Promise<'active' | 'disabled'> => {
      await ensureWorkspace(workspaceId)
      return readWorkspaceStatusInTransaction(sqlPool, workspaceId)
    }
    const setWorkspaceStatus = async (workspaceId: string, status: 'active' | 'disabled'): Promise<void> => {
      await ensureWorkspace(workspaceId)
      await withWorkspaceTransaction(sqlPool, workspaceId, async client => {
        await client.query('UPDATE workspaces SET status = $2 WHERE id = $1', [workspaceId, status])
      })
    }
    const checkHealth = async () => {
      const client = await pool.connect()
      try {
        const migrationState = await client.query<{ version: number | null }>('SELECT max(version)::int AS version FROM schema_migrations')
        if ((migrationState.rows[0]?.version ?? 0) < expectedMigrationVersion) throw new Error('database schema is behind the application migrations')
      } finally { client.release() }
      if (opsPool) {
        const opsClient = await opsPool.connect()
        try { await opsClient.query('SELECT 1 FROM platform_feature_flags LIMIT 1') } finally { opsClient.release() }
      }
    }
    const listWorkspaceIds = async () => {
      const client = await (opsPool ?? pool).connect()
      try {
        await client.query('BEGIN')
        await client.query(`SELECT set_config('app.platform_scope', 'platform_ops', true)`)
        const result = await client.query<{ id: string }>('SELECT id FROM workspaces ORDER BY created_at DESC, id ASC')
        await client.query('COMMIT')
        return result.rows.map(row => row.id)
      } catch (error) {
        try { await client.query('ROLLBACK') } catch { /* preserve original error */ }
        throw error
      } finally { client.release() }
    }
    return { mode: 'postgres', outbox, business, billing, commercial, usage, modelUsage, actionLedger, entitlements, operations, subscriptions, members, commercialExtensions, growth, alerts, dataLifecycle, rules, brandUnits, objectOrphans, contextSnapshots, identities, authorization, workspaceBootstrap, paymentCallbackNonces, support, supportSlaReporting, incidents, featureFlags, financeSearch, auditCenter, opsData, assetParse, assetScanReceipts, assetScanRedrive, assetPromotionCleanup, imageContinuationLeases, imageGenerationExecutions, reconciliationEvidence, unifiedLinkAudit, platformMediaSpecs, mappingPreflightApprovals, knowledgeHydration, storageQuota, storageReconciliation, reconciliationStatuses, canonicalBackfillRuns, canonicalBackfillConflicts, canonicalBackfillRemediation, interactiveConfirmationTickets, executeCanonicalBackfill, persistSnapshotAndEvent, persistSnapshotsAndEvent, persistPublishTransaction, persistTrustedScanPromotion, ensureWorkspace, listWorkspaceIds, listWorkspaceSummaries: () => opsData.listWorkspaceSummaries(), listWorkspaceDirectory: query => opsData.listWorkspaceDirectory(query), getWorkspaceStatus, setWorkspaceStatus, checkHealth, close: async () => { await Promise.all([pool.end(), opsPool?.end()]) } }
  } catch (error) {
    await pool.end().catch(() => undefined)
    await opsPool?.end().catch(() => undefined)
    throw error
  }
}

const persistenceReady = initializePersistence().then(value => { persistence = value; return value }).catch(error => { persistenceError = error; throw error })

/** Keep notification delivery evidence durable without coupling alert creation
 * to a remote receiver's latency or availability. */
async function persistOperationalAlertNotification(alert: OperationalAlert): Promise<void> {
  try {
    await persistenceReady
    const result = await notifyOperationalAlert(alert)
    await persistence.alerts?.recordNotification({ workspaceId: alert.workspaceId, alertId: alert.id, delivery: result.delivery, attempts: result.attempts, ...(result.requestId ? { requestId: result.requestId } : {}), ...(result.reason ? { reason: result.reason } : {}) })
  } catch {
    // Alert persistence remains authoritative. A database or notifier outage
    // must not turn an already-recorded operational alert into a lost API error.
  }
}

async function persistEvent(workspaceId: string, aggregateId: string, eventType: string, sequence: number, payload: Record<string, unknown>) {
  await persistenceReady
  const event = persistence.outbox
    ? (await persistence.ensureWorkspace?.(workspaceId), await persistence.outbox.append({ workspaceId, aggregateId, eventType, sequence, payload }))
    : { id: `evt_${randomUUID()}`, workspaceId, aggregateId, eventType, sequence, payload, createdAt: new Date().toISOString() }
  const local = inMemoryTimelineEvents.get(workspaceId) ?? []
  if (!local.some(item => item.id === event.id)) local.push(event)
  inMemoryTimelineEvents.set(workspaceId, local)
}

function taskFeedbackEventPayload(feedback: import('../../../packages/application/src/service.js').TaskFeedback): Record<string, unknown> {
  const payload = feedback as unknown as Record<string, unknown>
  if (feedback.rating !== 'needs_improvement') return payload
  return {
    ...payload,
    knowledge_observation: {
      workspaceId: feedback.workspaceId,
      kind: 'feedback',
      sourceKey: `task_feedback:${feedback.id}`,
      ...(feedback.contentVersionId ? { contentId: feedback.contentVersionId } : {}),
      reason: feedback.reason ?? feedback.comment ?? '商家要求改进内容',
      ...(feedback.comment && feedback.comment !== feedback.reason ? { details: feedback.comment } : {}),
      metadata: { task_id: feedback.taskId, rating: feedback.rating, actor_id: feedback.actorId },
      createdAt: feedback.createdAt,
    },
  }
}

function publishRejectionKnowledgeObservation(job: import('../../../packages/application/src/service.js').PublishJob): Record<string, unknown> | undefined {
  if (!job.rejection || job.remoteState !== 'rejected') return undefined
  const rejectionPayload = {
    raw_code: job.rejection.rawCode,
    ...(job.rejection.message ? { message: job.rejection.message } : {}),
    fields: job.rejection.fields.map(field => ({ path: field.path, ...(field.rawCode ? { raw_code: field.rawCode } : {}), message: field.message })),
  }
  const rejectionHash = createHash('sha256').update(canonicalJson(rejectionPayload)).digest('hex').slice(0, 24)
  return {
    workspaceId: job.workspaceId,
    kind: 'platform_rejection',
    sourceKey: `publish_rejection:${job.id}:${rejectionHash}`,
    platform: job.platform,
    contentId: job.contentVersionId,
    reason: job.rejection.message ?? job.rejection.rawCode ?? '平台驳回发布内容',
    ...(job.rejection.fields.length ? { details: job.rejection.fields.map(field => `${field.path}: ${field.message}`).join('\n') } : {}),
    metadata: { task_id: job.taskId, publish_job_id: job.id, ...(job.rejection.rawCode ? { raw_code: job.rejection.rawCode } : {}) },
    createdAt: job.remoteObservedAt ?? job.createdAt,
  }
}

function recordValue(value: unknown): Record<string, unknown> | undefined {
  return Boolean(value) && typeof value === 'object' && !Array.isArray(value) ? value as Record<string, unknown> : undefined
}

async function assertVideoProviderJobScope(workspaceId: string, providerJobId: string) {
  await persistenceReady
  const events = persistence.outbox?.listWorkspaceEvents
    ? await persistence.outbox.listWorkspaceEvents(workspaceId, 5000)
    : (inMemoryTimelineEvents.get(workspaceId) ?? [])
  const owned = events.some(event => {
    if (event.eventType !== 'multimodal.video_completed' && event.eventType !== 'multimodal.generation.completed') return false
    const rendering = recordValue(event.payload.rendering)
    return rendering?.providerJobId === providerJobId
  })
  if (!owned) throw new DomainError('VIDEO_PROVIDER_SCOPE_DENIED', '视频任务不存在或不属于当前工作区', 403)
}

async function getWorkspaceStatus(workspaceId: string): Promise<'active' | 'disabled'> {
  await persistenceReady
  if (persistence.getWorkspaceStatus) return persistence.getWorkspaceStatus(workspaceId)
  if (!memoryWorkspaceStatuses.has(workspaceId)) memoryWorkspaceStatuses.set(workspaceId, 'active')
  return memoryWorkspaceStatuses.get(workspaceId) ?? 'active'
}

async function setWorkspaceStatus(workspaceId: string, status: 'active' | 'disabled') {
  await persistenceReady
  if (persistence.setWorkspaceStatus) {
    await persistence.setWorkspaceStatus(workspaceId, status)
    return
  }
  memoryWorkspaceStatuses.set(workspaceId, status)
}

async function requireActiveWorkspace(workspaceId: string, method: string) {
  if (method === 'workspace.health' || method === 'workspace.activate' || method === 'workspace.deactivate') return
  if (await getWorkspaceStatus(workspaceId) !== 'active') throw new DomainError('WORKSPACE_DISABLED', '工作区已停用；请先重新启用后再执行商家操作', 423)
}

const ONBOARDING_METHODS = new Set([
  'merchant.start', 'workspace.health', 'platform.connect', 'billing.status', 'billing.recharge.create', 'billing.recharge.get', 'billing.recharge.list', 'billing.transactions', 'billing.reconciliation', 'billing.model-usage.statement',
  'subscription.get', 'subscription.orders.list', 'subscription.order.create', 'subscription.change', 'platform.model.status',
  'platform.media.spec.list', 'platform.media.spec.get', 'platform.media.spec.create', 'platform.media.spec.update', 'platform.media.spec.approve', 'platform.media.spec.expire', 'platform.mapping.preflight', 'delivery.bundle.verify',
  'asset.scan',
  'workspace.commercial.get', 'workspace.commercial.update', 'workspace.usage.get', 'workspace.activate', 'workspace.deactivate', 'workspace.interactive.confirm',
])

function requireStoreOnboarding(workspaceId: string, method: string) {
  // Local fixture workflows intentionally support unbound planning data. The
  // production App flow must bind at least one live store before any catalog,
  // asset, task, sync, generation, or publishing operation is reachable.
  if (!isProduction() || ONBOARDING_METHODS.has(method) || method.startsWith('ops.') || method.startsWith('knowledge.') || method.startsWith('rule.')) return
  const hasBoundStore = service.listPlatformAccounts(workspaceId).some(account => account.tokenState === 'connected')
  if (!hasBoundStore) throw new DomainError('STORE_ONBOARDING_REQUIRED', '请先完成至少一个平台店铺授权绑定，再继续使用商品、素材、任务或生成能力', 428, { onboarding_required: true, next_actions: ['调用 workspace.health 查看六平台授权入口', '选择平台后调用 platform.connect', '授权回调完成后重新调用 workspace.health'] })
}

function isHttpOnboardingExempt(path: string) {
  return path === '/mcp'
    || path === '/v1/internal/automation/tick'
    || path === '/v1/internal/storage/orphans/cleanup'
    || path === '/v1/platform-accounts'
    || path === '/v1/platform-capabilities'
    || path === '/v1/delivery-readiness'
    || path.startsWith('/v1/rules')
    || path.startsWith('/v1/billing')
    || path.startsWith('/v1/subscriptions')
    || path.startsWith('/v1/ops')
    || /^\/v1\/platform-accounts\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)\/authorize$/u.test(path)
    || /^\/v1\/generation-jobs\/[^/]+$/u.test(path)
    || /^\/v1\/generation-jobs\/[^/]+\/(defer|result)$/u.test(path)
    || /^\/v1\/image-generation-jobs\/[^/]+$/u.test(path)
    || /^\/v1\/sync-jobs\/[^/]+\/progress$/u.test(path)
    || /^\/v1\/publish-jobs\/[^/]+\/observation$/u.test(path)
}

async function hydrateKnowledge(workspaceId: string): Promise<void> {
  if (Date.now() - (hydratedKnowledgeWorkspaces.get(workspaceId) ?? 0) < 1_000) return
  const active = knowledgeHydrationFlights.get(workspaceId)
  if (active) return active
  const flight = (async () => {
    await persistenceReady
    const hydrationWindow = 5000
    const snapshot = await persistence.knowledgeHydration?.load(workspaceId)
    const snapshotEvents = snapshot?.events ?? []
    const hydratedCheckpoint = hydratedKnowledgeCheckpoints.get(workspaceId)
    if (snapshot && !isKnowledgeHydrationCheckpointCurrent(hydratedCheckpoint, snapshot)) {
      const eventsToHydrate = snapshotEvents.filter(event => isKnowledgeEventAfterCheckpoint(event, hydratedCheckpoint))
      knowledgeForWorkspace(workspaceId).hydrate(eventsToHydrate)
      hydratedKnowledgeCheckpoints.set(workspaceId, checkpointFromKnowledgeSnapshot(snapshot))
    }
    let events: OutboxEvent[] = []
    let cursor = snapshot ? { createdAt: snapshot.cursorCreatedAt, eventId: snapshot.cursorEventId } : undefined
    if (persistence.outbox?.listWorkspaceEventsAfter) {
      // Page by the durable `(created_at, id)` cursor. A full page is not
      // treated as complete; this prevents large workspaces from silently
      // losing old rules after the first 5,000 events.
      for (let page = 0; page < 20; page += 1) {
        const next = await persistence.outbox.listWorkspaceEventsAfter(workspaceId, cursor, hydrationWindow)
        events.push(...next)
        if (next.length < hydrationWindow) break
        const last = next[next.length - 1]!
        cursor = { createdAt: last.createdAt, eventId: last.id }
        if (page === 19) throw new DomainError('KNOWLEDGE_HYDRATION_WINDOW_FULL', '知识恢复事件超过增量恢复上限，已停止使用可能不完整的知识上下文；请先执行知识快照或分批恢复', 503, { workspace_id: workspaceId, window: hydrationWindow, max_pages: 20, next_step: '继续执行持久化知识快照或后台增量恢复后重试' })
      }
    } else {
      events = persistence.outbox?.listWorkspaceEvents
        ? await persistence.outbox.listWorkspaceEvents(workspaceId, hydrationWindow + 1)
        : (inMemoryTimelineEvents.get(workspaceId) ?? [])
      if (events.length > hydrationWindow) throw new DomainError('KNOWLEDGE_HYDRATION_WINDOW_FULL', '知识恢复事件超过单次安全窗口，已停止使用可能不完整的知识上下文；请先执行知识快照或增量恢复', 503, { workspace_id: workspaceId, returned_events: events.length, window: hydrationWindow, next_step: '执行持久化知识快照或游标增量恢复后重试' })
    }
    for (const event of events) {
      if (event.workspaceId !== workspaceId) {
        throw new DomainError('KNOWLEDGE_EVENT_SCOPE_MISMATCH', '知识恢复事件与当前工作区不匹配，已停止恢复', 503, { workspace_id: workspaceId, event_id: event.id })
      }
      const payloadWorkspace = event.payload.workspaceId ?? event.payload.workspace_id
      if (payloadWorkspace !== undefined && payloadWorkspace !== workspaceId) {
        throw new DomainError('KNOWLEDGE_EVENT_PAYLOAD_SCOPE_MISMATCH', '知识恢复事件内容与当前工作区不匹配，已停止恢复', 503, { workspace_id: workspaceId, event_id: event.id })
      }
    }
    const seen = hydratedKnowledgeEventIds.get(workspaceId) ?? new Set<string>()
    const relevant = events.filter(event => event.eventType.startsWith('knowledge.') || event.eventType === 'task_feedback_submitted' || event.eventType === 'publish.observation')
    const pending = relevant.filter(event => !seen.has(event.id))
    if (pending.length) knowledgeForWorkspace(workspaceId).hydrate(pending.map(event => ({ id: event.id, workspaceId: event.workspaceId, aggregateId: event.aggregateId, sequence: event.sequence, createdAt: event.createdAt, eventType: event.eventType, payload: event.payload })))
    for (const event of relevant) seen.add(event.id)
    while (seen.size > 10_000) seen.delete(seen.values().next().value as string)
    hydratedKnowledgeEventIds.set(workspaceId, seen)
    if (persistence.knowledgeHydration && events.length) {
      const last = events[events.length - 1]!
      const durableEvents = mergeKnowledgeHydrationEvents(snapshotEvents, relevant)
      try {
        const saved = await persistence.knowledgeHydration.save({ workspaceId, cursorCreatedAt: last.createdAt, cursorEventId: last.id, events: durableEvents.map(event => ({ id: event.id, workspaceId: event.workspaceId, aggregateId: event.aggregateId, sequence: event.sequence, eventType: event.eventType, payload: event.payload, createdAt: event.createdAt })), snapshotId: snapshot?.snapshotId, expectedRevision: snapshot?.revision ?? null, expectedCursor: snapshot ? { createdAt: snapshot.cursorCreatedAt, eventId: snapshot.cursorEventId } : null })
        hydratedKnowledgeCheckpoints.set(workspaceId, checkpointFromKnowledgeSnapshot(saved))
      } catch (error) {
        // Another API request may have advanced the same workspace snapshot
        // between load and save. The repository CAS must still reject stale
        // writes, but a normal race must not turn an otherwise successful read
        // into HTTP 500. Adopt the winning snapshot and continue; all payload
        // and workspace checks happened before this write.
        if (!(error instanceof Error) || error.message !== 'KNOWLEDGE_SNAPSHOT_CONFLICT') throw error
        const latest = await persistence.knowledgeHydration.load(workspaceId)
        if (!latest) throw error
        hydratedKnowledgeCheckpoints.set(workspaceId, checkpointFromKnowledgeSnapshot(latest))
      }
    }
    hydratedKnowledgeWorkspaces.set(workspaceId, Date.now())
  })()
  knowledgeHydrationFlights.set(workspaceId, flight)
  try { await flight } finally { if (knowledgeHydrationFlights.get(workspaceId) === flight) knowledgeHydrationFlights.delete(workspaceId) }
}

function nextWorkspaceEventSequence(workspaceId: string) {
  const candidate = Math.floor(Date.now() / 1000)
  const next = Math.max(candidate, (workspaceEventSequences.get(workspaceId) ?? 0) + 1)
  workspaceEventSequences.set(workspaceId, next)
  return next
}

export function timelineEvent(event: OutboxEvent) {
  const terminalFailure = event.lastError?.retryable === false
  const delivery = event.unknownAt ? 'unknown' : terminalFailure ? 'dead_letter' : event.publishedAt ? 'delivered' : 'pending'
  return {
    id: event.id,
    aggregate_id: event.aggregateId,
    event_type: event.eventType,
    sequence: event.sequence,
    occurred_at: event.createdAt,
    delivery,
    ...(event.attempts !== undefined ? { attempts: event.attempts } : {}),
    ...(event.lastError ? { error: event.lastError } : {}),
    payload: event.payload,
  }
}

async function taskTimeline(workspaceId: string, taskId: string, limit = 100) {
  const task = service.getTask(taskId)
  if (task.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该任务', 403)
  if (!Number.isInteger(limit) || limit < 1 || limit > 200) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 到 200 的整数', 400)
  const aggregateIds = new Set<string>([task.id])
  for (const version of service.contentVersions.values()) if (version.taskId === task.id) aggregateIds.add(version.id)
  for (const job of service.generationJobs.values()) if (job.taskId === task.id) aggregateIds.add(job.id)
  for (const job of service.imageGenerationJobs.values()) if (job.taskId === task.id) aggregateIds.add(job.id)
  for (const job of service.publishJobs.values()) if (job.taskId === task.id) aggregateIds.add(job.id)
  for (const feedback of service.feedback.values()) if (feedback.taskId === task.id) aggregateIds.add(feedback.id)
  const events: OutboxEvent[] = []
  if (persistence.outbox) {
    await persistenceReady
    for (const aggregateId of aggregateIds) events.push(...await persistence.outbox.listAggregateEvents(workspaceId, aggregateId, limit))
  } else {
    events.push(...(inMemoryTimelineEvents.get(workspaceId) ?? []).filter(event => aggregateIds.has(event.aggregateId)))
  }
  const unique = [...new Map(events.map(event => [event.id, event])).values()]
  return unique.sort((left, right) => left.createdAt.localeCompare(right.createdAt) || left.id.localeCompare(right.id)).slice(-limit).map(timelineEvent)
}

async function syncOperationalAlerts(workspaceId: string) {
  const repository = persistence.alerts ?? memoryAlerts
  const alerts: Array<Omit<OperationalAlert, 'id' | 'status' | 'acknowledgedBy' | 'acknowledgedAt' | 'acknowledgementReason' | 'updatedAt'>> = []
  const accounts = service.listPlatformAccounts(workspaceId)
  for (const account of accounts) {
    if (account.tokenState === 'revoked' || account.tokenState === 'refresh_required') alerts.push({ alertKey: `oauth:${account.platform}:${account.id}:${account.tokenState}`, code: 'OAUTH_REAUTH_REQUIRED', severity: 'high', platform: account.platform, accountId: account.id, entityType: 'platform_account', entityId: account.id, title: `${account.platform} 店铺需要重新授权`, observedAt: account.tokenStateUpdatedAt ?? account.lastAuthorizedAt ?? account.createdAt, evidence: { tokenState: account.tokenState }, nextAction: '打开授权流程完成官方 OAuth 重新授权，并重新执行只读同步验证。', workspaceId })
  }
  for (const job of service.listSyncJobs(workspaceId)) {
    if (job.state === 'failed' || job.state === 'partial') alerts.push({ alertKey: `sync:${job.id}:${job.state}`, code: 'SYNC_FAILED', severity: 'medium', platform: job.platform, accountId: job.accountId, entityType: 'sync_job', entityId: job.id, title: `${job.platform} 商品同步${job.state === 'partial' ? '部分失败' : '失败'}`, observedAt: job.updatedAt, evidence: { state: job.state, failureCount: job.failedItems.length }, nextAction: '查看失败项和平台原始错误，确认商品事实后再重试。', workspaceId })
  }
  for (const job of service.listPublishJobs(workspaceId)) {
    if (job.remoteState === 'rejected' || job.state === 'rejected' || job.remoteState === 'unknown' || job.state === 'unknown' || job.state === 'manual_attention') alerts.push({ alertKey: `publish:${job.id}:${job.remoteState ?? job.state}`, code: job.remoteState === 'rejected' || job.state === 'rejected' ? 'PUBLISH_REJECTED' : 'PUBLISH_UNKNOWN', severity: 'high', platform: job.platform, accountId: job.accountId, entityType: 'publish_job', entityId: job.id, title: job.remoteState === 'rejected' || job.state === 'rejected' ? `${job.platform} 发布被平台拒绝` : `${job.platform} 发布结果未知，需要人工核对`, observedAt: job.remoteObservedAt ?? job.createdAt, evidence: { state: job.state, remoteState: job.remoteState, rejectionCode: job.rejection?.rawCode ?? null }, nextAction: '先读取平台回执和任务时间线；拒绝需修正版重新审核，未知状态禁止自动重发。', workspaceId })
  }
  for (const task of service.listTasks(workspaceId)) {
    if (task.state === 'failed_recoverable') alerts.push({ alertKey: `task:${task.id}:${task.state}`, code: 'TASK_FAILED', severity: 'medium', platform: task.platform, accountId: task.accountId, entityType: 'task', entityId: task.id, title: '营销任务失败，需要恢复或人工处理', observedAt: task.createdAt, evidence: { state: task.state }, nextAction: '打开任务时间线确认失败阶段；可恢复任务使用恢复入口，不要重复创建订单或发布。', workspaceId })
  }
  for (const version of service.contentVersions.values()) {
    const versionTask = service.getTask(version.taskId)
    if (versionTask.workspaceId !== workspaceId) continue
    const findings = service.reviewContent(workspaceId, version.id)
    const blocking = findings.filter(finding => finding.severity === 'error')
    if (blocking.length) alerts.push({ alertKey: `content:${version.id}:${version.revision ?? version.version ?? 1}`, code: 'CONTENT_BLOCKING', severity: 'high', platform: versionTask.platform, accountId: versionTask.accountId, entityType: 'content_version', entityId: version.id, title: '内容版本存在 P0 审核阻断', observedAt: versionTask.createdAt, evidence: { findingCount: blocking.length, findingCodes: blocking.map(finding => finding.code) }, nextAction: '修正事实、价格或品牌规则冲突后创建修正版，重新审核并批准。', workspaceId })
  }
  for (const alert of alerts) {
    const persisted = await repository.upsert(alert)
    // Alert persistence is authoritative; a slow or unavailable notification
    // receiver must not delay the API response or hide the alert from Ops.
    void persistOperationalAlertNotification(persisted)
  }
  return repository
}
async function persistSnapshot(workspaceId: string, entityType: 'product' | 'task' | 'content_version' | 'publish_job' | 'publish_batch' | 'platform_account' | 'generation_job' | 'image_generation_job' | 'brand_profile' | 'asset' | 'feedback' | 'sync_job' | 'automation_policy', entity: { id: string; version?: number; revision?: number }, value: Record<string, unknown>) {
  await persistenceReady
  const entityVersion = entity.version ?? entity.revision ?? 1
  if (persistence.persistSnapshotAndEvent) {
    await persistence.persistSnapshotAndEvent({ workspaceId, entityType: entityType as BusinessEntityType, entityId: entity.id, entityVersion, payload: value, eventType: 'state.snapshot', eventPayload: { entityType, entity: value } })
    invalidateWorkspaceHydration(workspaceId)
    return
  }
  await persistence.ensureWorkspace?.(workspaceId)
  await persistence.business?.save({ workspaceId, entityType: entityType as BusinessEntityType, entityId: entity.id, entityVersion, payload: value })
  await persistEvent(workspaceId, entity.id, 'state.snapshot', entityVersion, { entityType, entity: value })
  invalidateWorkspaceHydration(workspaceId)
}

/**
 * An uploaded/quarantined asset must never be committed without the event
 * that makes its durable lifecycle observable.  In Postgres this deliberately
 * uses the existing single-snapshot transaction instead of the older
 * persistSnapshot() + persistEvent() pair: the latter can commit the asset
 * snapshot and then fail while writing the domain event, after which object
 * compensation would leave a database record pointing at a deleted object.
 */
export async function persistAssetSnapshotAndEvent(workspaceId: string, asset: { id: string; revision?: number }, eventType: string, eventPayload: Record<string, unknown>, value: Record<string, unknown>, persistenceOverride?: ApiPersistence) {
  await persistenceReady
  const target = persistenceOverride ?? persistence
  const entityVersion = asset.revision ?? 1
  if (target.persistSnapshotAndEvent) {
    await target.persistSnapshotAndEvent({ workspaceId, entityType: 'asset', entityId: asset.id, entityVersion, payload: value, eventType, eventPayload })
    invalidateWorkspaceHydration(workspaceId)
    return
  }
  // Memory mode has no durable business repository.  If a durable repository
  // is ever wired without the atomic capability, fail closed rather than
  // silently reintroducing the split-commit path.
  if (target.mode === 'postgres') throw new DomainError('ASSET_PERSISTENCE_ATOMIC_REQUIRED', '素材归档必须使用原子 snapshot/event 持久化能力', 503)
  await persistSnapshot(workspaceId, 'asset', { id: asset.id, revision: entityVersion }, value)
  await persistEvent(workspaceId, asset.id, eventType, entityVersion, eventPayload)
}

async function persistExpiredDeliveryIfNeeded(workspaceId: string, contentVersionId: string) {
  const expired = service.markExpiredDeliveryIfNeeded(workspaceId, contentVersionId)
  if (!expired) return
  await persistSnapshot(workspaceId, 'content_version', expired, expired as unknown as Record<string, unknown>)
  await persistEvent(workspaceId, expired.id, 'content.delivery_expired', expired.revision, { content_version_id: expired.id, reason: expired.deliveryStatusReason ?? '价格或促销有效期已过期', delivery_status: expired.deliveryStatus, updated_at: expired.deliveryStatusUpdatedAt })
}

async function persistAssetReference(workspaceId: string, asset: AssetRegistrationResult) {
  if (asset.deduplication.mode !== 'deduplicated' || !asset.deduplication.referenceAdded) return
  await persistSnapshot(workspaceId, 'asset', asset, asset as unknown as Record<string, unknown>)
  const reference = asset.references.at(-1)
  await persistEvent(workspaceId, asset.id, 'asset.reference_added', asset.revision, { asset_id: asset.id, reference_name: reference?.name, mime_type: reference?.mimeType, reference_count: asset.references.length })
}

async function ensureFixtureAccount(workspaceId: string, platform: Platform, accountId: string) {
  if (!fixtureMode) return
  try {
    service.getPlatformAccount(workspaceId, accountId, platform)
    return
  } catch (error) {
    if (!(error instanceof DomainError) || error.code !== 'PLATFORM_ACCOUNT_NOT_FOUND') throw error
  }
  const account = service.registerPlatformAccount({
    workspaceId,
    platform,
    remoteAccountId: accountId,
    credentialRef: `fixture-secret/${platform}/${accountId}`,
    grantedScopes: ['fixture.product.read', 'fixture.product.write'],
    accessTokenExpiresAt: new Date(Date.now() + 30 * 24 * 3600_000).toISOString(),
    credentialRefreshable: true,
  })
  await persistSnapshot(workspaceId, 'platform_account', account, account as unknown as Record<string, unknown>)
}

function grantedScopes(scope?: string) {
  return scope?.split(/[\s,]+/u).map(item => item.trim()).filter(Boolean)
}

function paginatedResult<T>(url: URL, items: T[]) {
  const requestedLimit = url.searchParams.get('limit')
  const requestedOffset = url.searchParams.get('offset')
  const limit = requestedLimit === null ? 20 : Number(requestedLimit)
  const offset = requestedOffset === null ? 0 : Number(requestedOffset)
  if (!Number.isSafeInteger(limit) || limit < 1 || limit > 100 || !Number.isSafeInteger(offset) || offset < 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须为 1–100 的整数，offset 必须为非负整数', 400)
  return { items: items.slice(offset, offset + limit), total: items.length, limit, offset }
}

function paginationRequest(url: URL): { limit: number; offset: number } {
  const requestedLimit = url.searchParams.get('limit')
  const requestedOffset = url.searchParams.get('offset')
  const limit = requestedLimit === null ? 20 : Number(requestedLimit)
  const offset = requestedOffset === null ? 0 : Number(requestedOffset)
  if (!Number.isSafeInteger(limit) || limit < 1 || limit > 100 || !Number.isSafeInteger(offset) || offset < 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须为 1–100 的整数，offset 必须为非负整数', 400)
  return { limit, offset }
}

function mcpPagination(params: JsonObject): { limit: number; offset: number } {
  const limit = params.limit === undefined ? 50 : Number(params.limit)
  const offset = params.offset === undefined ? 0 : Number(params.offset)
  if (!Number.isSafeInteger(limit) || limit < 1 || limit > 100 || !Number.isSafeInteger(offset) || offset < 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 到 100 的整数，offset 必须是非负整数', 400)
  for (const field of ['date_from', 'date_to'] as const) if (typeof params[field] === 'string' && !Number.isFinite(Date.parse(params[field]))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${field} 必须是合法时间`, 400)
  return { limit, offset }
}

async function persistSnapshotsAndEvent(input: { workspaceId: string; snapshots: SnapshotInput[]; aggregateId: string; eventType: string; sequence: number; eventPayload: Record<string, unknown> }) {
  await persistenceReady
  if (persistence.persistSnapshotsAndEvent) {
    await persistence.persistSnapshotsAndEvent(input)
    invalidateWorkspaceHydration(input.workspaceId)
    return
  }
  for (const snapshot of input.snapshots) {
    if (snapshot.entityType === 'merchant_intent') merchantIntents.set(`${input.workspaceId}:${snapshot.entityId}`, snapshot.payload)
    else await persistSnapshot(input.workspaceId, snapshot.entityType, { id: snapshot.entityId, revision: snapshot.entityVersion }, snapshot.payload)
  }
  await persistEvent(input.workspaceId, input.aggregateId, input.eventType, input.sequence, input.eventPayload)
  invalidateWorkspaceHydration(input.workspaceId)
}

function rollbackGenerationCompletion(previous: { job: import('../../../packages/application/src/service.js').GenerationJob; task: import('../../../packages/application/src/service.js').Task }, contentVersionId: string) {
  service.contentVersions.delete(contentVersionId)
  service.generationJobs.set(previous.job.id, previous.job)
  service.tasks.set(previous.task.id, previous.task)
}

function jobWithQueueMetadata<T extends { id: string }>(job: T, workspaceId: string, type: 'generation' | 'publish') {
  return { ...job, ...service.getJobQueueMetadata(workspaceId, { type, jobId: job.id }) }
}

function workflowStoreScope(workspaceId: string, platform: Platform, accountId?: string, productId?: string) {
  const account = accountId ? service.listPlatformAccounts(workspaceId).find(item => item.id === accountId) : undefined
  const product = productId ? service.products.get(productId) : undefined
  return {
    workspace_id: workspaceId,
    platform,
    ...(accountId ? { account_id: accountId } : {}),
    ...(account?.storeAlias || product?.storeName ? { store_name: account?.storeAlias ?? product?.storeName } : {}),
    ...(productId ? { product_id: productId } : {}),
  }
}

function workflowStateLabel(state: string, kind: 'sync' | 'generation' | 'publish') {
  if (kind === 'publish' && ['unknown', 'reconciling'].includes(state)) return '发布结果待确认'
  if (['queued', 'pending'].includes(state)) return '排队中'
  if (['running', 'processing', 'submitting', 'submitted', 'executing'].includes(state)) return '处理中'
  if (['succeeded', 'published', 'completed'].includes(state)) return '已完成'
  if (['rejected', 'failed', 'dead_letter'].includes(state)) return '处理失败'
  return '需要查看状态'
}

function projectSyncWorkflow(workspaceId: string, job: import('../../../packages/application/src/service.js').SyncJob) {
  const retryable = job.failedItems.some(item => item.retryable)
  const active = job.state === 'queued' || job.state === 'running'
  return {
    kind: 'sync' as const,
    resource_id: job.id,
    scope: workflowStoreScope(workspaceId, job.platform, job.accountId),
    status: { internal_state: job.state, user_state: workflowStateLabel(job.state, 'sync'), terminal: !active, updated_at: job.updatedAt },
    progress: { known: false, completed: job.itemsUpserted + job.itemsFailed, total: null, label: '总量未知' },
    next_action: active
      ? { method: 'catalog.sync.get', label: '刷新同步状态', allowed: true }
      : retryable
        ? { method: 'sync.retry_failed', label: '重试失败项', allowed: true, reason: `有 ${job.failedItems.filter(item => item.retryable).length} 项失败项可以重试` }
        : { method: 'catalog.sync.get', label: '查看同步结果', allowed: true },
    recovery: { retryable, ...(retryable ? { retry_scope: '仅可重试的失败项' } : {}), resume_method: 'catalog.sync.get', reconciliation_required: false },
    evidence: { source: fixtureMode ? 'fixture' : 'official_api', simulated: fixtureMode },
  }
}

function projectGenerationWorkflow(workspaceId: string, job: import('../../../packages/application/src/service.js').GenerationJob) {
  const task = service.tasks.get(job.taskId)
  const active = job.state === 'queued' || job.state === 'running'
  const retryable = job.state === 'failed'
  return {
    kind: 'generation' as const,
    resource_id: job.id,
    scope: workflowStoreScope(workspaceId, task?.platform ?? 'taobao', task?.accountId, task?.productId),
    status: { internal_state: job.state, user_state: workflowStateLabel(job.state, 'generation'), terminal: !active, updated_at: job.updatedAt },
    progress: { known: false, completed: active ? 0 : job.state === 'succeeded' ? 1 : 0, total: 1, label: active ? '处理中' : undefined },
    next_action: active
      ? { method: 'generation.get', label: '刷新生成状态', allowed: true }
      : retryable
        ? { method: 'generation.get', label: '查看失败原因', allowed: true }
        : { method: 'generation.get', label: '查看生成结果', allowed: true },
    recovery: { retryable, ...(retryable ? { retry_scope: '当前生成任务' } : {}), resume_method: 'generation.get', reconciliation_required: false },
    evidence: { source: fixtureMode ? 'fixture' : 'official_api', simulated: fixtureMode },
  }
}

function projectPublishWorkflow(workspaceId: string, job: import('../../../packages/application/src/service.js').PublishJob) {
  const unknown = job.state === 'unknown' || job.state === 'reconciling' || job.remoteState === 'unknown'
  const terminal = !unknown && ['published', 'rejected', 'failed'].includes(job.remoteState ?? job.state)
  const state = unknown ? job.remoteState === 'unknown' ? 'unknown' : 'reconciling' : job.remoteState ?? job.state
  const task = service.tasks.get(job.taskId)
  return {
    kind: 'publish' as const,
    resource_id: job.id,
    scope: workflowStoreScope(workspaceId, job.platform, job.accountId ?? task?.accountId, task?.productId),
    status: { internal_state: state, user_state: workflowStateLabel(state, 'publish'), terminal, updated_at: job.remoteObservedAt ?? job.createdAt },
    progress: { known: false, completed: terminal ? 1 : 0, total: 1, label: unknown ? '结果未知' : undefined },
    next_action: unknown
      ? { method: 'publish.get', label: '查询发布状态', allowed: true, reason: '平台最终回执尚未确认，不能重复提交' }
      : terminal
        ? { method: 'publish.get', label: '查看发布结果', allowed: true }
        : { method: 'publish.get', label: '刷新发布状态', allowed: true },
    recovery: { retryable: false, resume_method: 'publish.get', reconciliation_required: unknown, ...(unknown ? { retry_scope: '人工对账' } : {}) },
    evidence: { source: fixtureMode ? 'fixture' : 'official_api', simulated: job.remoteSimulated === true || fixtureMode, ...(job.requestId ? { request_id: job.requestId } : {}) },
  }
}

function taskWorkflowProjections(workspaceId: string, taskId: string) {
  const workflows = [
    ...[...service.generationJobs.values()].filter(job => job.workspaceId === workspaceId && job.taskId === taskId).sort((a, b) => b.updatedAt.localeCompare(a.updatedAt)).slice(0, 1).map(job => projectGenerationWorkflow(workspaceId, job)),
    ...[...service.publishJobs.values()].filter(job => job.workspaceId === workspaceId && job.taskId === taskId).sort((a, b) => b.createdAt.localeCompare(a.createdAt)).slice(0, 1).map(job => projectPublishWorkflow(workspaceId, job)),
  ]
  return workflows
}

export function batchStateFromItems(items: PublishBatchItem[]): PublishBatch['state'] {
  // `submitted` only means the platform accepted the request. The final
  // publish receipt is required before a batch can be reported completed.
  if (items.length && items.every(item => item.state === 'published')) return 'completed'
  if (items.some(item => item.state === 'failed' || item.state === 'rejected' || item.state === 'unknown')) return items.some(item => ['queued', 'submitted', 'published'].includes(item.state)) ? 'partial' : 'failed'
  if (items.every(item => item.state === 'paused')) return 'paused'
  if (items.some(item => ['queued', 'submitted', 'published'].includes(item.state))) return 'queued'
  return 'prepared'
}

async function savePublishBatch(batch: PublishBatch, eventType = 'publish.batch.updated') {
  const previous = persistedPublishBatches.get(batch.id)
  const next = structuredClone(batch)
  next.updatedAt = new Date().toISOString()
  next.revision += 1
  const eventPayload = { batch_id: next.id, state: next.state, items: next.items.map(item => ({ task_id: item.taskId, state: item.state, job_id: item.jobId ?? null })) }
  try {
    await persistSnapshotsAndEvent({ workspaceId: next.workspaceId, snapshots: [{ entityType: 'publish_batch', entityId: next.id, entityVersion: next.revision, payload: next as unknown as Record<string, unknown> }], aggregateId: next.id, eventType, sequence: next.revision, eventPayload })
  } catch (error) {
    if (previous) {
      for (const key of Object.keys(batch as unknown as Record<string, unknown>)) delete (batch as unknown as Record<string, unknown>)[key]
      Object.assign(batch, structuredClone(previous))
      publishBatches.set(batch.id, batch)
    } else {
      publishBatches.delete(batch.id)
    }
    throw error
  }
  for (const key of Object.keys(batch as unknown as Record<string, unknown>)) delete (batch as unknown as Record<string, unknown>)[key]
  Object.assign(batch, next)
  publishBatches.set(batch.id, batch)
  persistedPublishBatches.set(batch.id, structuredClone(batch))
  return batch
}

/**
 * Bulk publish admission must make the parent batch and child publish job
 * recoverable together. The normalized projections and outbox event are
 * committed in one workspace transaction; a restart can therefore never
 * observe a durable child without its parent batch item.
 */
async function persistPublishJobWithBatch(input: { batch: PublishBatch; task: ReturnType<typeof service.getTask>; job: import('../../../packages/application/src/service.js').PublishJob; itemState: PublishBatchItem['state']; error?: PublishBatchItem['error'] }) {
  const next = structuredClone(input.batch)
  const item = next.items.find(candidate => candidate.taskId === input.task.id)
  if (!item) throw new DomainError('PUBLISH_BATCH_ITEM_NOT_FOUND', `任务 ${input.task.id} 不属于该批次`, 400)
  Object.assign(item, { state: input.itemState, jobId: input.job.id, contentVersionId: input.job.contentVersionId, confirmationHash: input.job.confirmationHash, remoteSnapshotHash: input.job.remoteSnapshotHash, ...(input.job.accountId ? { accountId: input.job.accountId } : {}), ...(input.error ? { error: input.error } : { error: undefined }) })
  next.state = batchStateFromItems(next.items)
  next.updatedAt = new Date().toISOString()
  next.revision += 1
  // confirmPublish is called with deferCommit for this path. Persist the
  // exact post-commit task state atomically with the job and parent batch;
  // only then mutate the process-local service indexes.
  const persistedTask = structuredClone(input.task)
  persistedTask.state = 'publishing'
  persistedTask.version += 1
  await persistSnapshotsAndEvent({
    workspaceId: next.workspaceId,
    snapshots: [
      { entityType: 'task', entityId: persistedTask.id, entityVersion: persistedTask.version, payload: persistedTask as unknown as Record<string, unknown> },
      { entityType: 'publish_job', entityId: input.job.id, entityVersion: input.job.revision, payload: input.job as unknown as Record<string, unknown> },
      { entityType: 'publish_batch', entityId: next.id, entityVersion: next.revision, payload: next as unknown as Record<string, unknown> },
    ],
    aggregateId: input.job.id,
    eventType: 'publish.requested',
    sequence: input.job.revision,
    eventPayload: { ...publishEventPayload(input.job), batch_id: next.id, batch_revision: next.revision },
  })
  for (const key of Object.keys(input.batch as unknown as Record<string, unknown>)) delete (input.batch as unknown as Record<string, unknown>)[key]
  Object.assign(input.batch, next)
  publishBatches.set(next.id, input.batch)
  persistedPublishBatches.set(next.id, structuredClone(next))
  service.commitPublishConfirmation(input.job)
  return input.batch
}

function automationPolicyKey(workspaceId: string, platform?: Platform, accountId?: string) { return `${workspaceId}:${platform ?? '*'}:${accountId ?? '*'}` }

function normalizeAutomationTime(value: string | undefined, field: string): string | undefined {
  if (value === undefined || value.trim() === '') return undefined
  const normalized = value.trim()
  if (!/^([01]\d|2[0-3]):[0-5]\d$/u.test(normalized)) throw new DomainError('AUTOMATION_WINDOW_INVALID', `${field} 必须是 HH:mm 格式`, 400)
  return normalized
}

function automationWindowContains(now: Date, start?: string, end?: string) {
  if (!start && !end) return true
  if (!start || !end) return false
  const current = now.getHours() * 60 + now.getMinutes()
  const toMinutes = (value: string) => Number(value.slice(0, 2)) * 60 + Number(value.slice(3, 5))
  const from = toMinutes(start); const until = toMinutes(end)
  if (from === until) return true
  return from < until ? current >= from && current < until : current >= from || current < until
}

function nextAutomationWindowStart(now: Date, start?: string, end?: string) {
  if (!start || !end || automationWindowContains(now, start, end)) return now
  const [hours, minutes] = start.split(':').map(Number)
  const next = new Date(now)
  next.setHours(hours!, minutes!, 0, 0)
  if (next.getTime() <= now.getTime()) next.setDate(next.getDate() + 1)
  return next
}

function validateAutomationScope(workspaceId: string, platform?: Platform, accountId?: string) {
  if (platform && !SUPPORTED_PLATFORMS.includes(platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 无效', 400)
  if (accountId && !platform) throw new DomainError('STORE_PLATFORM_REQUIRED', '使用 account_id 配置店铺自动化时必须同时指定 platform', 400)
  if (platform && accountId) service.getPlatformAccount(workspaceId, accountId, platform)
  return { platform, accountId }
}

async function saveAutomationPolicy(policy: AutomationPolicy, eventType = 'automation.policy.updated') {
  policy.updatedAt = new Date().toISOString()
  automationPolicies.set(automationPolicyKey(policy.workspaceId, policy.platform, policy.accountId), policy)
  await persistSnapshot(policy.workspaceId, 'automation_policy', policy, policy as unknown as Record<string, unknown>)
  await persistEvent(policy.workspaceId, policy.id, eventType, policy.revision, { policy_id: policy.id, platform: policy.platform ?? null, account_id: policy.accountId ?? null, enabled: policy.enabled, mode: policy.mode, frequency_minutes: policy.frequencyMinutes, retry_limit: policy.retryLimit, pause_reason: policy.pauseReason ?? null })
  return policy
}

async function executeAutomationScan(workspaceId: string, platform?: Platform, accountId?: string) {
  const products = service.listProducts(workspaceId, { ...(platform ? { platform } : {}), ...(accountId ? { accountId } : {}) })
  const jobs = [...service.publishJobs.values()].filter(job => job.workspaceId === workspaceId && (!platform || job.platform === platform) && (!accountId || job.accountId === accountId))
  const selectedAccounts = service.listPlatformAccounts(workspaceId).filter(account => (!platform || account.platform === platform) && (!accountId || account.id === accountId))
  // Rule evaluation performs a durable read for every product. Keep this
  // bounded so one operator refresh cannot exhaust the shared tenant pool
  // when a workspace has a large catalog.
  const ruleRiskCandidates = await mapWithConcurrency(products, 4, async product => {
    const evaluation = service.ruleCenter.evaluate({ platform: product.platform, ...(product.category ? { category: product.category } : {}), ...(product.storeName ? { store: product.storeName } : {}) })
    const inMemoryBlocking = evaluation.findings.some(finding => finding.severity === 'error' && ['RULE_EXPIRED', 'RULE_NOT_YET_EFFECTIVE', 'RULE_PRIORITY_CONFLICT'].includes(finding.code))
    if (!inMemoryBlocking && !(await persistedRuleHasBlockingRisk(workspaceId, product))) return undefined
    return { kind: 'rule_conflict' as const, product_id: product.id, platform: product.platform, account_id: product.accountId ?? null, message: '适用平台规则存在过期、未生效或优先级冲突' }
  })
  const ruleRisks = ruleRiskCandidates.filter((risk): risk is NonNullable<typeof risk> => Boolean(risk))
  const risks: AutomationRisk[] = [
    ...selectedAccounts.filter(account => account.tokenState === 'revoked' || account.tokenState === 'refresh_required').map(account => ({ kind: 'authorization', platform: account.platform, account_id: account.id, message: `店铺授权状态为 ${account.tokenState}，需要重新授权` })),
    ...ruleRisks,
    ...products.filter(product => !product.factsConfirmed).map(product => ({ kind: 'unconfirmed_facts', product_id: product.id, platform: product.platform, account_id: product.accountId ?? null, message: '商品事实尚未确认' })),
    ...products.filter(product => product.stock <= 0).map(product => ({ kind: 'out_of_stock', product_id: product.id, platform: product.platform, account_id: product.accountId ?? null, message: '库存为零' })),
    ...products.filter(product => product.stock > 0 && product.stock <= 10).map(product => ({ kind: 'low_stock', product_id: product.id, platform: product.platform, account_id: product.accountId ?? null, message: `库存偏低：${product.stock}` })),
    ...jobs.filter(job => ['rejected', 'unknown', 'manual_attention'].includes(job.state)).map(job => ({ kind: 'publish_attention', publish_job_id: job.id, platform: job.platform, account_id: job.accountId ?? null, message: `发布状态需要人工处理：${job.state}` })),
  ]
  const alertRepository = persistence.alerts ?? memoryAlerts
  for (const risk of risks) {
    const entityId = risk.product_id ?? risk.publish_job_id ?? `${risk.kind}:${workspaceId}`
    const persisted = await alertRepository.upsert({ alertKey: `automation:${risk.kind}:${entityId}`, code: `AUTOMATION_${risk.kind.toUpperCase()}`, severity: risk.kind === 'publish_attention' ? 'high' : 'medium', ...(risk.platform ? { platform: risk.platform } : {}), ...(risk.account_id ? { accountId: risk.account_id } : {}), entityType: risk.product_id ? 'product' : 'publish_job', entityId, title: risk.message, observedAt: new Date().toISOString(), evidence: { kind: risk.kind, ...(risk.product_id ? { productId: risk.product_id } : {}), ...(risk.publish_job_id ? { publishJobId: risk.publish_job_id } : {}) }, nextAction: '在交互会话中查看详情，修正后再执行人工确认或重试。', workspaceId })
    void persistOperationalAlertNotification(persisted)
  }
  const recommendations: AutomationRecommendation[] = risks.map((risk, index): AutomationRecommendation => {
    const id = `automation-recommendation:${risk.kind}:${risk.product_id ?? risk.publish_job_id ?? index}`
    if (risk.kind === 'authorization') return { id, kind: risk.kind, priority: 'high', title: '恢复店铺授权', action: '打开官方授权入口并完成重新授权', method: 'platform.connect', parameters: { platform: risk.platform }, execution: 'interactive_confirmation', requiresInteractiveConfirmation: true }
    if (risk.kind === 'unconfirmed_facts') return { id, kind: risk.kind, priority: 'high', title: '确认商品事实', action: '核对价格、库存、SKU、图片和属性后确认', method: 'catalog.facts.confirm', parameters: { product_id: risk.product_id ?? '' }, execution: 'interactive_confirmation', requiresInteractiveConfirmation: true }
    if (risk.kind === 'out_of_stock' || risk.kind === 'low_stock') {
      return risk.account_id
        ? { id, kind: risk.kind, priority: risk.kind === 'out_of_stock' ? 'high' : 'medium', title: risk.kind === 'out_of_stock' ? '处理缺货商品' : '复核低库存商品', action: '同步店铺库存并由运营确认补货、下架或调整推广', method: 'catalog.sync.start', parameters: { platform: risk.platform, account_id: risk.account_id }, execution: 'interactive_confirmation', requiresInteractiveConfirmation: true }
        : { id, kind: risk.kind, priority: 'high', title: '绑定店铺后处理库存', action: '先完成店铺授权，再同步库存', method: 'platform.connect', parameters: { platform: risk.platform }, execution: 'interactive_confirmation', requiresInteractiveConfirmation: true }
    }
    if (risk.kind === 'publish_attention') return { id, kind: risk.kind, priority: 'high', title: '复核发布异常', action: '查看远端状态、差异和失败原因', method: 'publish.get', parameters: { job_id: risk.publish_job_id ?? '' }, execution: 'read_only', requiresInteractiveConfirmation: false }
    return { id, kind: risk.kind, priority: 'medium', title: '检查平台规则', action: '查看当前平台适用规则和冲突项', method: 'rule.list', parameters: { platform: risk.platform }, execution: 'read_only', requiresInteractiveConfirmation: false }
  })
  return { scannedAt: new Date().toISOString(), scope: { platform: platform ?? null, accountId: accountId ?? null }, counts: { products: products.length, publishJobs: jobs.length, risks: risks.length, alertsUpserted: risks.length }, risks, recommendations, actions: ['查看结构化优化建议', '在交互会话中确认后执行建议动作'], humanConfirmationRequired: true as const, unattendedAutoResubmit: false as const }
}

async function scanAutomationAfterOperationalCompletion(workspaceId: string, platform: Platform, accountId: string, trigger: string) {
  const policy = automationPolicies.get(automationPolicyKey(workspaceId, platform, accountId))
  if (!policy?.enabled) return { triggered: false as const, reason: 'automation_policy_disabled' as const }
  try {
    const scan = await executeAutomationScan(workspaceId, platform, accountId)
    await recordOperationAudit({
      workspaceId,
      actorId: 'automation-sync-hook',
      action: 'automation.post_sync_scan',
      resourceType: 'automation_policy',
      resourceId: policy.id,
      before: {},
      after: { platform, accountId, trigger, risks: scan.counts.risks, products: scan.counts.products },
      reason: '商品同步完成后按店铺策略即时执行风险扫描；不自动发布或重试',
    })
    return { triggered: true as const, ...scan }
  } catch (error) {
    return { triggered: false as const, reason: 'automation_scan_failed' as const, error: error instanceof Error ? error.message : '自动化同步后扫描失败' }
  }
}

async function requestCatalogSync(workspaceId: string, req: IncomingMessage, params: JsonObject) {
  const platform = required(params, 'platform') as Platform
  if (!platformConnectorConfigured(platform)) throw new DomainError('NOT_CONFIGURED', `${platform} 官方 API 尚未配置，无法同步商品`, 503)
  const accountId = (typeof params.account_id === 'string' && params.account_id.trim()) || header(req, 'x-account-id')?.trim() || (isProduction() ? '' : defaultFixtureAccountId(workspaceId, platform))
  if (!accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产同步必须指定已授权平台账号', 400)
  const platformAccount = isProduction() || fixtureMode ? requireActivePlatformAccount(workspaceId, accountId, platform) : undefined
  await ensureFixtureAccount(workspaceId, platform, accountId)
  const job = service.createSyncJob({ workspaceId, platform, accountId, mode: params.mode === 'full' ? 'full' : 'incremental', ...(typeof params.cursor === 'string' && params.cursor.trim() ? { cursor: params.cursor } : {}) })
  const authorizationSnapshot = workerAuthorizationSnapshot(req, workspaceId, job.id, 'catalog.sync.execute', { method: 'catalog.sync.start', platform, account_id: accountId, mode: job.mode, resource_revision: job.revision, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}) })
  if (requiresStrictAuth() && !authorizationSnapshot) {
    service.removeSyncJob(workspaceId, job.id)
    throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '商品同步缺少持久身份授权快照，已拒绝入队', 503)
  }
  const syncEntitlementKey = `bulk-sync-job:${job.id}`
  let syncEntitlement: Awaited<ReturnType<typeof consumeEntitlement>>
  try {
    syncEntitlement = await consumeEntitlement({ workspaceId, kind: 'bulk_sync', actionKey: syncEntitlementKey, actionKind: 'catalog_sync', actorId: requestActor(req), description: '商品批量同步权益（可选加购）' })
  } catch (error) {
    service.removeSyncJob(workspaceId, job.id)
    throw error
  }
  try {
    await persistSnapshot(workspaceId, 'sync_job', job, job as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, job.id, 'sync.requested', job.revision, { job_id: job.id, platform, account_id: accountId, mode: job.mode, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}), ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}) })
  } catch (error) {
    const failed = service.updateSyncJob(workspaceId, job.id, { state: 'failed', errorMessage: error instanceof Error ? error.message : '同步任务持久化失败' })
    let failureProjected = false
    try {
      await persistSnapshot(workspaceId, 'sync_job', failed, failed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, job.id, 'sync.failed', failed.revision, { job_id: job.id, platform, account_id: accountId, error_message: failed.errorMessage ?? '同步任务持久化失败' })
      failureProjected = true
    } catch { /* preserve the original failure; remove the in-memory ghost below */ }
    if (!failureProjected) service.removeSyncJob(workspaceId, job.id)
    if (syncEntitlement) await refundEntitlement({ workspaceId, actionKey: syncEntitlementKey, reason: '同步任务持久化失败' }).catch(() => undefined)
    throw error
  }
  if (fixtureMode) {
    try {
      const synced = await connectorRuntime.sync(platform, { workspaceId, accountId, ...(platformAccount ? { credentialRef: platformAccount.credentialRef } : {}), traceId: requestId(req) }, job.resumeCursor)
      const products = service.upsertSyncedProducts({ workspaceId, platform, accountId, items: synced.items })
      await invalidateCanonicalFactsAfterSync(workspaceId, products)
      const completed = service.updateSyncJob(workspaceId, job.id, { state: 'succeeded', pages: synced.pages, itemsUpserted: products.length, itemsFailed: 0, failedItems: [], nextCursor: undefined, resumeCursor: undefined })
      for (const product of products) await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      await persistSnapshot(workspaceId, 'sync_job', completed, completed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, job.id, 'sync.completed', completed.revision, { job_id: job.id, platform, account_id: accountId, pages: synced.pages, items_upserted: products.length, simulated: true })
      const automation = await scanAutomationAfterOperationalCompletion(workspaceId, platform, accountId, 'catalog.sync.start.completed')
      return { ...completed, products, simulated: true, automation }
    } catch (error) {
      const failed = service.updateSyncJob(workspaceId, job.id, { state: 'failed', errorMessage: error instanceof Error ? error.message : '同步 provider 调用失败' })
      await persistSnapshot(workspaceId, 'sync_job', failed, failed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, job.id, 'sync.failed', failed.revision, { job_id: job.id, platform, account_id: accountId, error_message: failed.errorMessage ?? '同步 provider 调用失败', simulated: true })
      if (syncEntitlement) await refundEntitlement({ workspaceId, actionKey: syncEntitlementKey, reason: '商品批量同步失败' })
      throw error
    }
  }
  return job
}

async function runAutomationTickUnlocked(workspaceId: string, req: IncomingMessage, actorId: string) {
  const nowMs = Date.now(); const executed: Array<Record<string, unknown>> = []
  for (const policy of [...automationPolicies.values()].filter(item => item.workspaceId === workspaceId && item.enabled)) {
    if (policy.nextRunAt && Date.parse(policy.nextRunAt) > nowMs) continue
    const current = new Date(nowMs)
    if (!automationWindowContains(current, policy.windowStart, policy.windowEnd)) {
      const nextWindow = nextAutomationWindowStart(current, policy.windowStart, policy.windowEnd)
      policy.nextRunAt = nextWindow.toISOString()
      policy.revision += 1
      await saveAutomationPolicy(policy, 'automation.policy.deferred_window')
      await recordOperationAudit({ workspaceId, actorId, action: 'automation.policy.deferred_window', resourceType: 'automation_policy', resourceId: policy.id, before: {}, after: { nextRunAt: policy.nextRunAt, windowStart: policy.windowStart, windowEnd: policy.windowEnd }, reason: '当前时间不在店铺自动化执行窗口内' })
      executed.push({ policyId: policy.id, platform: policy.platform ?? null, accountId: policy.accountId ?? null, deferred: true, reason: 'outside_execution_window', nextRunAt: policy.nextRunAt })
      continue
    }
    const scan = await executeAutomationScan(workspaceId, policy.platform, policy.accountId)
    const blockingRisk = scan.risks.find(risk => risk.kind === 'authorization' || risk.kind === 'rule_conflict')
    if (blockingRisk) {
      const pauseReason = `自动暂停：${blockingRisk.message}`
      const before = { enabled: policy.enabled, pauseReason: policy.pauseReason ?? null, syncEnabled: policy.syncEnabled }
      policy.enabled = false
      policy.pauseReason = pauseReason
      policy.lastRunAt = scan.scannedAt
      policy.nextRunAt = undefined
      policy.revision += 1
      await saveAutomationPolicy(policy, 'automation.policy.auto_paused')
      await recordOperationAudit({ workspaceId, actorId, action: 'automation.policy.auto_paused', resourceType: 'automation_policy', resourceId: policy.id, before, after: { enabled: false, pauseReason, syncEnabled: policy.syncEnabled, risk: blockingRisk }, reason: pauseReason })
      executed.push({ policyId: policy.id, platform: policy.platform ?? null, accountId: policy.accountId ?? null, ...scan, paused: true, pauseReason, syncSkipped: true })
      continue
    }
    let sync: Record<string, unknown> | undefined
    let syncError: Record<string, unknown> | undefined
    // Claim the next execution before creating any external sync work. If the
    // process dies after the job is accepted but before the final audit write,
    // the persisted schedule still prevents an immediate duplicate tick.
    policy.lastRunAt = scan.scannedAt
    policy.nextRunAt = new Date(Date.parse(scan.scannedAt) + policy.frequencyMinutes * 60_000).toISOString()
    policy.claimedAt = scan.scannedAt
    policy.revision += 1
    await saveAutomationPolicy(policy, 'automation.policy.claimed')
    if (policy.syncEnabled && policy.platform && policy.accountId) {
      try { sync = await requestCatalogSync(workspaceId, req, { platform: policy.platform, account_id: policy.accountId }) as unknown as Record<string, unknown> }
      catch (error) { syncError = { code: error instanceof DomainError ? error.code : 'SYNC_REQUEST_FAILED', message: error instanceof Error ? error.message : '同步任务创建失败' } }
    }
    if (typeof sync?.id === 'string') policy.lastSyncJobId = sync.id
    policy.claimedAt = undefined
    await saveAutomationPolicy(policy, 'automation.policy.executed')
    await recordOperationAudit({ workspaceId, actorId, action: 'automation.tick', resourceType: 'automation_policy', resourceId: policy.id, before: {}, after: { lastRunAt: policy.lastRunAt, nextRunAt: policy.nextRunAt, risks: scan.counts.risks, syncJobId: sync?.id ?? null, syncError: syncError?.code ?? null }, reason: policy.syncEnabled ? '自动化调度器执行店铺风险扫描并请求商品同步' : '自动化调度器执行到期店铺风险扫描' })
    executed.push({ policyId: policy.id, platform: policy.platform ?? null, accountId: policy.accountId ?? null, ...scan, ...(sync ? { sync } : {}), ...(syncError ? { syncError } : {}) })
  }
  return { executedAt: new Date().toISOString(), executed, skipped: false as const, skipReason: undefined, unattendedAutoResubmit: false as const, humanConfirmationRequired: true as const }
}

export function internalAutomationTickAllowed(env: NodeJS.ProcessEnv = process.env) {
  // Merchant-facing scheduled Automations are owned by the Codex App host.
  // Keep the legacy API/Worker scheduler available for tests and explicit
  // operations migrations, but fail closed in production unless the
  // deployment has deliberately enabled that separate internal scheduler.
  const controlledEnvironment = ['staging', 'preview', 'production'].includes(env.NODE_ENV ?? '')
  return !controlledEnvironment || env.MERCHANT_INTERNAL_AUTOMATION_TICK_ENABLED === 'true'
}

async function runAutomationTick(workspaceId: string, req: IncomingMessage, actorId: string) {
  if (!internalAutomationTickAllowed()) {
    return {
      executedAt: new Date().toISOString(),
      executed: [],
      skipped: true as const,
      skipReason: 'codex_native_automations_only' as const,
      unattendedAutoResubmit: false as const,
      humanConfirmationRequired: true as const,
    }
  }
  const configuredTtl = Number(process.env.AUTOMATION_TICK_LEASE_MS ?? 120_000)
  const ttlMs = Number.isSafeInteger(configuredTtl) ? Math.min(10 * 60_000, Math.max(5_000, configuredTtl)) : 120_000
  const key = `merchant:automation-tick:${createHash('sha256').update(workspaceId).digest('hex')}`
  const token = randomUUID()
  let acquired = false
  if (redisAutomationLease) {
    acquired = await redisAutomationLease.acquire(key, token, ttlMs)
  } else {
    const current = localAutomationLeases.get(key)
    if (!current || current.expiresAt <= Date.now()) {
      localAutomationLeases.set(key, { token, expiresAt: Date.now() + ttlMs })
      acquired = true
    }
  }
  if (!acquired) return { executedAt: new Date().toISOString(), executed: [], skipped: true as const, skipReason: 'automation_tick_lease_held', unattendedAutoResubmit: false as const, humanConfirmationRequired: true as const }
  const renewLease = async () => {
    if (redisAutomationLease) {
      await redisAutomationLease.renew(key, token, ttlMs)
      return
    }
    const current = localAutomationLeases.get(key)
    if (current?.token === token) current.expiresAt = Date.now() + ttlMs
  }
  const heartbeat = setInterval(() => { void renewLease().catch(() => undefined) }, Math.max(1_000, Math.floor(ttlMs / 3)))
  try {
    const testDelayMs = process.env.NODE_ENV === 'test' ? Number(process.env.AUTOMATION_TICK_LEASE_TEST_DELAY_MS ?? 0) : 0
    if (Number.isSafeInteger(testDelayMs) && testDelayMs > 0) await new Promise(resolve => setTimeout(resolve, Math.min(testDelayMs, 1_000)))
    return await runAutomationTickUnlocked(workspaceId, req, actorId)
  } finally {
    clearInterval(heartbeat)
    if (redisAutomationLease) await redisAutomationLease.release(key, token)
    else if (localAutomationLeases.get(key)?.token === token) localAutomationLeases.delete(key)
  }
}

async function refreshPublishBatch(batch: PublishBatch) {
  let changed = false
  for (const item of batch.items) {
    if (!item.jobId) continue
    const job = service.publishJobs.get(item.jobId)
    if (!job) continue
    const nextState = (job.state === 'published' ? 'published' : job.state === 'submitted' ? 'submitted' : job.state === 'rejected' ? 'rejected' : job.state === 'unknown' ? 'unknown' : job.state === 'queued' || job.state === 'submitting' ? 'queued' : item.state) as PublishBatchItem['state']
    if (item.state !== nextState || item.error?.message !== job.rejection?.message) {
      item.state = nextState
      if (job.rejection) item.error = { code: job.rejection.rawCode, message: job.rejection.message ?? '平台拒绝发布' }
      changed = true
    }
  }
  const state = batch.state === 'paused' ? 'paused' : batchStateFromItems(batch.items)
  if (batch.state !== state) { batch.state = state; changed = true }
  if (changed) await savePublishBatch(batch, 'publish.batch.reconciled')
  return batch
}

/** Recover a scheduler claim that was persisted immediately before a process
 * crash. A durable sync job proves the claim completed; otherwise requeue the
 * policy immediately instead of waiting for the old future nextRunAt. */
async function reconcileAutomationClaims(workspaceId: string) {
  const policies = [...automationPolicies.values()].filter(policy => policy.workspaceId === workspaceId && policy.enabled && policy.claimedAt)
  for (const policy of policies) {
    const claimedAtMs = Date.parse(policy.claimedAt!)
    const matching = policy.syncEnabled && Number.isFinite(claimedAtMs)
      ? service.listSyncJobs(workspaceId).filter(job => (!policy.platform || job.platform === policy.platform) && (!policy.accountId || job.accountId === policy.accountId)).filter(job => {
        const createdAtMs = Date.parse(job.createdAt)
        return Number.isFinite(createdAtMs) && createdAtMs >= claimedAtMs - 5_000
      }).sort((left, right) => right.createdAt.localeCompare(left.createdAt))[0]
      : undefined
    const recovered = structuredClone(policy)
    recovered.claimedAt = undefined
    if (matching) recovered.lastSyncJobId = matching.id
    else if (policy.syncEnabled) recovered.nextRunAt = new Date().toISOString()
    recovered.revision += 1
    await saveAutomationPolicy(recovered, matching ? 'automation.policy.claim.recovered' : 'automation.policy.claim.requeued')
  }
}

type DurableStateSnapshot = { aggregateId: string; sequence: number; payload: Record<string, unknown> }

function recordInvalidDurableSnapshot(workspaceId: string, entityType: string, entityId: string, error: unknown) {
  const candidate = error as { code?: string; details?: { missing?: unknown } }
  const missing = Array.isArray(candidate.details?.missing) ? candidate.details.missing.filter((item): item is string => typeof item === 'string') : [candidate.code ?? 'unknown']
  const warnings = invalidDurableSnapshots.get(workspaceId) ?? []
  if (!warnings.some(item => item.entityType === entityType && item.entityId === entityId)) warnings.push({ entityType, entityId, missing })
  invalidDurableSnapshots.set(workspaceId, warnings)
}

export function hydrateOutboxSnapshot(workspaceId: string, snapshot: DurableStateSnapshot) {
  const payload = snapshot.payload
  try {
    if (payload.entityType === 'publish_batch') {
      const entity = payload.entity as (PublishBatch & { id?: string }) | undefined
      if (!entity) return
      const batchId = String(entity.id ?? snapshot.aggregateId)
      publishBatches.set(batchId, entity)
      persistedPublishBatches.set(batchId, structuredClone(entity))
    }
    else if (payload.entityType === 'automation_policy') {
      const policy = payload.entity as AutomationPolicy | undefined
      if (policy) automationPolicies.set(automationPolicyKey(workspaceId, policy.platform, policy.accountId), policy)
    }
    else if (payload.entityType === 'merchant_intent' && payload.entity && typeof payload.entity === 'object') {
      merchantIntents.set(`${workspaceId}:${snapshot.aggregateId}`, payload.entity as Record<string, unknown>)
    }
    else if (payload.entityType === 'product' || payload.entityType === 'task' || payload.entityType === 'content_version' || payload.entityType === 'publish_job' || payload.entityType === 'platform_account' || payload.entityType === 'generation_job' || payload.entityType === 'image_generation_job' || payload.entityType === 'brand_profile' || payload.entityType === 'asset' || payload.entityType === 'feedback' || payload.entityType === 'sync_job') {
      service.hydrateSnapshot({ entityType: payload.entityType, entity: payload.entity })
    }
  } catch (error) {
    const entity = payload.entity as { id?: unknown } | undefined
    const entityId = typeof entity?.id === 'string' ? entity.id : snapshot.aggregateId
    recordInvalidDurableSnapshot(workspaceId, typeof payload.entityType === 'string' ? payload.entityType : 'unknown', entityId, error)
  }
}

async function hydrateWorkspaceFromPersistence(workspaceId: string, options: { excludeEntityTypes?: readonly BusinessEntityType[] } = {}) {
  await persistenceReady
  if (!persistence.business) return
  const durable = await persistence.business.loadWorkspace(workspaceId, options)
  const durableKeys = new Set(durable.map(snapshot => `${snapshot.entityType}:${snapshot.entityId}`))
  for (const snapshot of durable) {
    if (snapshot.entityType === 'publish_batch') {
      const batch = snapshot.payload as unknown as PublishBatch
      publishBatches.set(snapshot.entityId, batch)
      persistedPublishBatches.set(snapshot.entityId, structuredClone(batch))
    }
    else if (snapshot.entityType === 'automation_policy') {
      const policy = snapshot.payload as unknown as AutomationPolicy
      automationPolicies.set(automationPolicyKey(workspaceId, policy.platform, policy.accountId), policy)
    }
    else if (snapshot.entityType === 'merchant_intent') {
      merchantIntents.set(`${workspaceId}:${snapshot.entityId}`, snapshot.payload)
    }
    else {
      try { service.hydrateSnapshot({ entityType: snapshot.entityType, entity: snapshot.payload }) }
      catch (error) {
        // A malformed durable entity must remain unavailable, but it must not
        // prevent unrelated entities in the same workspace from hydrating.
        // Keep the invalid-snapshot evidence for Ops/metrics and continue.
        recordInvalidDurableSnapshot(workspaceId, snapshot.entityType, snapshot.entityId, error)
      }
    }
  }
  const durableOutbox = persistence.outbox as (OutboxRepository & { loadStateSnapshots?: (workspaceId: string, options?: { excludeEntityTypes?: readonly string[] }) => Promise<DurableStateSnapshot[]> }) | undefined
  if (durableOutbox?.loadStateSnapshots) {
    for (const snapshot of await durableOutbox.loadStateSnapshots(workspaceId, options)) {
      const entity = snapshot.payload.entity as { id?: unknown } | undefined
      const entityId = typeof entity?.id === 'string' ? entity.id : snapshot.aggregateId
      if (durable.length && typeof snapshot.payload.entityType === 'string' && durableKeys.has(`${snapshot.payload.entityType}:${entityId}`)) continue
      hydrateOutboxSnapshot(workspaceId, snapshot)
    }
  }
  await reconcileAutomationClaims(workspaceId)
}

async function hydrateWorkspace(workspaceId: string, options: { excludeEntityTypes?: readonly BusinessEntityType[] } = {}) {
  const cacheKey = `${workspaceId}:${[...(options.excludeEntityTypes ?? [])].sort().join(',')}`
  const active = workspaceHydrationInFlight.get(cacheKey)
  if (active) return active
  const hydratedAt = workspaceHydratedAt.get(cacheKey)
  if (hydratedAt !== undefined && Date.now() - hydratedAt < WORKSPACE_HYDRATION_TTL_MS) return
  const generation = workspaceHydrationGeneration.get(workspaceId) ?? 0
  const load = hydrateWorkspaceFromPersistence(workspaceId, options)
    .then(() => { if ((workspaceHydrationGeneration.get(workspaceId) ?? 0) === generation) workspaceHydratedAt.set(cacheKey, Date.now()) })
    .finally(() => { workspaceHydrationInFlight.delete(cacheKey) })
  workspaceHydrationInFlight.set(cacheKey, load)
  return load
}

type WorkspaceMetricsDataReadiness = {
  source: 'durable_repository' | 'memory_service'
  dataCompleteness: 'complete' | 'process_local' | 'partial'
  hydration: {
    status: 'hydrated' | 'not_available'
    attempted: boolean
    invalidSnapshotCount: number
  }
}

/**
 * Metrics must not silently aggregate an empty process-local service after an
 * API restart. The service remains the compatibility read model, but in
 * durable mode it is populated from the workspace-scoped repository before
 * any metric is calculated. A failed durable read is intentionally propagated
 * so callers never receive a plausible-looking partial report.
 */
async function prepareWorkspaceMetricsData(workspaceId: string): Promise<WorkspaceMetricsDataReadiness> {
  await persistenceReady
  if (!persistence.business) {
    return {
      source: 'memory_service',
      dataCompleteness: 'process_local',
      hydration: { status: 'not_available', attempted: false, invalidSnapshotCount: 0 },
    }
  }
  await hydrateWorkspace(workspaceId)
  const invalidSnapshotCount = invalidDurableSnapshots.get(workspaceId)?.length ?? 0
  return {
    source: 'durable_repository',
    dataCompleteness: invalidSnapshotCount ? 'partial' : 'complete',
    hydration: { status: 'hydrated', attempted: true, invalidSnapshotCount },
  }
}

function publishEventPayload(job: import('../../../packages/application/src/service.js').PublishJob) {
  const payloadSnapshot = job.payloadSnapshot ?? {}
  const fields = structuredClone(payloadSnapshot.fields ?? {})
  return {
    taskId: job.taskId,
    contentVersionId: job.contentVersionId,
    platform: job.platform,
    workspaceId: job.workspaceId,
    idempotencyKey: job.idempotencyKey,
    ...(job.accountId ? { account_id: job.accountId } : {}),
    ...(job.payloadSnapshot?.remoteId || job.remoteId ? { remote_id: job.payloadSnapshot?.remoteId ?? job.remoteId } : {}),
    fields,
    payload_hash: job.payloadHash,
    media_required: job.selectedVisuals.length > 0,
    ...(job.selectionHash ? { selection_hash: job.selectionHash } : {}),
    ...(job.canonicalBinding ? { canonical_binding: structuredClone(job.canonicalBinding), canonical_binding_hash: job.canonicalBinding.snapshotHash } : {}),
    ...(job.authorizationSnapshot ? { authorization_snapshot: { ...serializedWorkerAuthorizationSnapshot(job.authorizationSnapshot), resource_id: job.id } } : {}),
    image_mode: job.payloadSnapshot?.imageMode ?? 'unchanged',
  }
}

const workerOperationCapabilities: Record<CriticalWorkerOperation, CapabilityId> = {
  'publish.execute': 'customer.publish.execute',
  'publish.reconcile': 'customer.content.read',
  'generation.execute': 'customer.content.update',
  'image_generation.execute': 'customer.content.update',
  'catalog.sync.execute': 'customer.content.update',
  'asset.scan.execute': 'marketing.queue.update',
  'asset.continuation.execute': 'customer.content.update',
}

export function workerAuthorizationDecisionMatches(decision: AuthorizationDecision | undefined, workspaceId: string, requiredCapability: CapabilityId) {
  const resolvedScopes = decision?.scope.resolved ?? []
  const workspaceResolutionMatches = resolvedScopes.some(scope => scope.type === 'workspace' && scope.ids.includes(workspaceId))
  const scopeMatches = decision?.scope.required === 'workspace'
    ? decision.scope.resource_id === workspaceId && workspaceResolutionMatches
    : requiredCapability === 'customer.publish.execute'
      && decision?.scope.required === 'brand'
      && typeof decision.scope.resource_id === 'string'
      && decision.scope.resource_id.trim().length > 0
      && workspaceResolutionMatches
  return Boolean(decision
    && decision.authorized
    && decision.capability === requiredCapability
    && decision.workbench === 'workspace'
    && scopeMatches)
}

function workerAuthorizationSnapshot<T extends CriticalWorkerOperation>(req: IncomingMessage, workspaceId: string, resourceId: string, capability: T, binding: Record<string, unknown>): (WorkerAuthorizationSnapshot & { capability: T }) | undefined {
  const decision = requestAuthorizationDecisions.get(req)
  const principal = requestPrincipals.get(req)
  if (!decision || !principal?.actorId || !principal.identityId || principal.authorizationRevision === undefined) return undefined
  const requiredCapability = workerOperationCapabilities[capability]
  // Worker authorization is an execution credential, not an observation of a
  // shadow deny. A shadow-mode decision may mint a snapshot only when the
  // underlying permission evaluation itself authorized the exact capability;
  // rollout mode never turns an unauthorized observation into authority.
  if (!workerAuthorizationDecisionMatches(decision, workspaceId, requiredCapability)) return undefined
  const actionScopeHash = createHash('sha256').update(JSON.stringify({ workspaceId, resourceId, capability, binding, scope: decision.scope.required, workbench: decision.workbench })).digest('hex')
  // Bind the worker continuation to the exact grant atomically consumed by
  // this request. Selecting another active grant with the same capability can
  // create a snapshot that was never authorized by the current decision.
  const grant = exactConsumedGrantForRequest(req, workspaceId, requiredCapability)
  const scopeHash = grant?.scopeHash ?? actionScopeHash
  // This opaque token is a real, repository-backed subject authorization
  // revision. It deliberately records the authority source so execution can
  // independently re-check either the membership or the temporary grant.
  const grantRevision = grant
    ? `grant:${grant.id}:${grant.revision}:${principal.identityId}:${principal.authorizationRevision}`
    : `membership:${principal.identityId}:${principal.authorizationRevision}`
  const resourceRevision = binding.resource_revision ?? binding.job_revision ?? binding.expected_asset_revision ?? binding.source_product_version ?? binding.task_version
  if (typeof resourceRevision !== 'number' && typeof resourceRevision !== 'string' || String(resourceRevision).trim() === '') return undefined
  return {
    schemaVersion: 1 as const,
    decisionId: decision.decision_id,
    actorId: principal.actorId,
    identityId: principal.identityId,
    workspaceId,
    workbench: 'workspace' as const,
    contextId: decision.scope.required === 'brand' ? `brand:${decision.scope.resource_id}` : `workspace:${workspaceId}`,
    contextVersion: decision.policy_version,
    policyVersion: decision.policy_version,
    grantRevision,
    grantIds: grant ? [grant.id] : [],
    capability,
    resourceId,
    resourceRevision: String(resourceRevision),
    requestId: requestId(req),
    traceId: getRequestCorrelation(req).traceId,
    authorized: true as const,
    decidedAt: new Date().toISOString(),
    scopeHash,
  }
}

function publishAuthorizationSnapshot(req: IncomingMessage, workspaceId: string, task: { version: number; id: string }) {
  return workerAuthorizationSnapshot(req, workspaceId, `pub-intent:${task.id}`, 'publish.execute', { method: 'publish.confirm', task_id: task.id, resource_revision: task.version })
}

export function requirePublishAuthorizationSnapshot(snapshot: ReturnType<typeof publishAuthorizationSnapshot>, required = requiresStrictAuth()) {
  if (!snapshot && required) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '发布确认缺少持久身份授权快照，已拒绝扣款、占槽和入队', 503)
  return snapshot
}

function serializedWorkerAuthorizationSnapshot<T extends CriticalWorkerOperation>(snapshot: WorkerAuthorizationSnapshot & { capability: T }) {
  return { schema_version: snapshot.schemaVersion, decision_id: snapshot.decisionId, actor_id: snapshot.actorId, identity_id: snapshot.identityId, workspace_id: snapshot.workspaceId, workbench: snapshot.workbench, context_id: snapshot.contextId, context_version: snapshot.contextVersion, policy_version: snapshot.policyVersion, grant_revision: snapshot.grantRevision, grant_ids: snapshot.grantIds, scope_hash: snapshot.scopeHash, capability: snapshot.capability, resource_id: snapshot.resourceId, resource_revision: snapshot.resourceRevision, request_id: snapshot.requestId, trace_id: snapshot.traceId, authorized: snapshot.authorized, decided_at: snapshot.decidedAt }
}

const workerEventOperations: Record<string, CriticalWorkerOperation> = {
  'publish.requested': 'publish.execute',
  'publish.reconcile_requested': 'publish.reconcile',
  'generation.requested': 'generation.execute',
  'image.generation.requested': 'image_generation.execute',
  'sync.requested': 'catalog.sync.execute',
  'asset.scan_redrive_requested': 'asset.scan.execute',
  'asset.generation_continuations.ready': 'asset.continuation.execute',
}

export async function recheckWorkerAuthorizationSnapshot(snapshot: WorkerAuthorizationSnapshot, workspaceId: string, resourceId: string, execution?: { eventId: string }) {
  const membershipMatch = /^membership:([^:]+):(\d+)$/u.exec(snapshot.grantRevision)
  const grantMatch = /^grant:([^:]+):(\d+):([^:]+):(\d+)$/u.exec(snapshot.grantRevision)
  if (!membershipMatch && !grantMatch) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_INVALID', '执行授权来源格式无效', 403)
  const subjectIdentityId = (membershipMatch?.[1] ?? grantMatch?.[3])!
  const expectedAuthorizationRevision = Number(membershipMatch?.[2] ?? grantMatch?.[4])
  if (snapshot.identityId !== subjectIdentityId || snapshot.workspaceId !== workspaceId || snapshot.resourceId !== resourceId) {
    throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_INVALID', '执行授权快照未绑定当前身份、工作区或资源，已拒绝执行', 403)
  }
  const authzRepository = authorizationRepository()
  if (!authzRepository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '执行前授权仓储不可用，已拒绝执行', 503)
  const currentAuthorizationRevision = await authzRepository.getAuthorizationRevision(subjectIdentityId)
  if (currentAuthorizationRevision !== expectedAuthorizationRevision) throw new DomainError('AUTHZ_EXECUTION_REVOKED', '入队后授权修订已变化，已拒绝执行', 403)
  if (membershipMatch) {
    const member = (await (persistence.members ?? memoryMembers).list(workspaceId)).find(candidate => candidate.externalSubject === snapshot.actorId)
    if (!member || member.status !== 'active' || member.identityId !== subjectIdentityId) throw new DomainError('AUTHZ_EXECUTION_REVOKED', '入队成员资格已失效，已拒绝执行', 403)
    const canonicalMemberRole = canonicalizeRole(member.role, 'membership')
    const requiredCapability = workerOperationCapabilities[snapshot.capability]
    if (!canonicalMemberRole || !capabilitiesForRoles([canonicalMemberRole]).includes(requiredCapability)) throw new DomainError('AUTHZ_EXECUTION_REVOKED', '当前成员角色不再具备该 worker 操作能力，已拒绝执行', 403)
    const brandContext = snapshot.capability === 'publish.execute' ? /^brand:(.+)$/u.exec(snapshot.contextId) : undefined
    if (brandContext && member.role !== 'workspace_owner') {
      const stillPublisher = await (persistence.brandUnits ?? memoryBrandUnits).hasBrandAccess({ workspaceId, brandId: brandContext[1]!, externalSubject: snapshot.actorId, minimumRole: 'publisher' })
      if (!stillPublisher) throw new DomainError('AUTHZ_EXECUTION_REVOKED', '入队后品牌发布权限已撤销，已拒绝执行', 403)
    }
  } else {
    const grant = await authzRepository.getGrant(grantMatch![1]!, subjectIdentityId)
    const expectedGrantRevision = Number(grantMatch![2])
    const requiredCapability = workerOperationCapabilities[snapshot.capability]
    if (!grant
      || !Array.isArray(snapshot.grantIds)
      || snapshot.grantIds.length !== 1
      || snapshot.grantIds[0] !== grant.id
      || grant.revision !== expectedGrantRevision
      || grant.subjectIdentityId !== subjectIdentityId
      || grant.workspaceId !== workspaceId
      || !grant.capabilities.includes(requiredCapability)
      || grant.scopeHash !== snapshot.scopeHash
      || grant.revokedAt
      || Date.parse(grant.issuedAt) > Date.now()
      || Date.parse(grant.expiresAt) <= Date.now()) {
      throw new DomainError('AUTHZ_EXECUTION_REVOKED', '入队临时授权已撤销、过期或范围变化，已拒绝执行', 403)
    }
  }
  const recheck = { recheck_id: `authz_recheck_${randomUUID()}`, actor_id: snapshot.actorId, identity_id: snapshot.identityId, workspace_id: workspaceId, workbench: snapshot.workbench, context_id: snapshot.contextId, context_version: `${AUTHZ_POLICY_VERSION}:${currentAuthorizationRevision}`, policy_version: AUTHZ_POLICY_VERSION, grant_revision: snapshot.grantRevision, grant_ids: snapshot.grantIds, scope_hash: snapshot.scopeHash, capability: snapshot.capability, resource_id: resourceId, resource_revision: snapshot.resourceRevision, request_id: snapshot.requestId, trace_id: snapshot.traceId, authorized: true, checked_at: new Date().toISOString() }
  if (!execution) return recheck
  if (!execution.eventId.trim()) throw new DomainError('AUTHZ_EXECUTION_EVENT_REQUIRED', '执行授权预留缺少持久事件标识，已拒绝执行', 503)
  let reservation: Awaited<ReturnType<AuthorizationRepository['reserveExecution']>>
  try {
    reservation = await authzRepository.reserveExecution({
      reservationId: `worker-execution:${execution.eventId}:${snapshot.capability}`,
      eventId: execution.eventId,
      subjectIdentityId,
      workspaceId,
      capability: workerOperationCapabilities[snapshot.capability],
      resourceId,
      scopeHash: snapshot.scopeHash,
      expectedAuthorizationRevision,
      ...(grantMatch ? { grantId: grantMatch[1]!, expectedGrantRevision: Number(grantMatch[2]) } : {}),
    })
  } catch (error) {
    if (error instanceof DomainError) throw error
    throw new DomainError('AUTHZ_EXECUTION_RESERVATION_UNAVAILABLE', '执行授权预留仓储不可用，Provider 未调用', 503)
  }
  if (!reservation) throw new DomainError('AUTHZ_EXECUTION_REVOKED', '执行授权在预留提交前已变化，Provider 未调用', 403, { event_id: execution.eventId, reservation_id: `worker-execution:${execution.eventId}:${snapshot.capability}` })
  return { ...recheck, reservation_id: reservation.reservationId, event_id: reservation.eventId }
}

export async function deriveWorkerContinuationAuthorizationSnapshot(
  source: WorkerAuthorizationSnapshot,
  workspaceId: string,
  resourceId: string,
  capability: CriticalWorkerOperation,
  binding: Record<string, unknown>,
): Promise<WorkerAuthorizationSnapshot> {
  if (source.capability !== 'publish.execute' || capability !== 'publish.reconcile') throw new DomainError('AUTHZ_EXECUTION_DELEGATION_INVALID', 'worker continuation 授权来源或目标操作无效', 403)
  const grantBound = source.grantRevision.startsWith('grant:')
  const candidate: WorkerAuthorizationSnapshot = {
    ...source,
    decisionId: `authz_continuation_${randomUUID()}`,
    capability,
    resourceId,
    decidedAt: new Date().toISOString(),
    scopeHash: grantBound
      ? source.scopeHash
      : createHash('sha256').update(JSON.stringify({ workspaceId, resourceId, capability, binding, sourceDecisionId: source.decisionId })).digest('hex'),
  }
  const current = await recheckWorkerAuthorizationSnapshot(candidate, workspaceId, resourceId)
  return {
    ...candidate,
    contextVersion: current.context_version,
    policyVersion: current.policy_version,
    grantRevision: current.grant_revision,
    scopeHash: current.scope_hash,
    decidedAt: current.checked_at,
  }
}

async function publishMediaPayload(workspaceId: string, job: import('../../../packages/application/src/service.js').PublishJob) {
  const media = [] as Array<{ visual_ref: string; role: 'main' | 'secondary'; sku_ids?: string[]; mime_type: string; sha256: string; content_base64: string }>
  for (const selected of job.selectedVisuals) {
    const imageJob = service.resolveImageGenerationByVisualRef(workspaceId, selected.visualRef)
    const output = imageJob.outputs?.find(candidate => candidate.visualRef === selected.visualRef)
    if (!output) throw new DomainError('VISUAL_NOT_FOUND', `发布媒体候选 ${selected.visualRef} 不存在`, 404)
    const asset = output.assetId ? assetForWorkspace(workspaceId, output.assetId) : undefined
    if (!asset || asset.scanStatus !== 'clean' || output.storageKey.startsWith('quarantine/')) throw new DomainError('GENERATED_IMAGE_SCAN_REQUIRED', '生成图片仍在隔离区，平台自动安全扫描通过后才能交付', 409, { visual_ref: selected.visualRef, asset_id: output.assetId ?? null, user_action_required: false, next_step: '平台正在自动执行安全扫描；商家和运营人员无需提交扫描证据' })
    const stored = await getStoredObjectWithRetry(workspaceId, output.storageKey)
    media.push({ visual_ref: selected.visualRef, role: selected.role, ...(selected.skuIds?.length ? { sku_ids: [...selected.skuIds] } : {}), mime_type: output.mimeType, sha256: output.sha256, content_base64: Buffer.from(stored.body).toString('base64') })
  }
  return media
}

function publishReconcileEventPayload(job: import('../../../packages/application/src/service.js').PublishJob, authorizationSnapshot?: WorkerAuthorizationSnapshot) {
  return {
    id: job.id,
    workspaceId: job.workspaceId,
    taskId: job.taskId,
    contentVersionId: job.contentVersionId,
    platform: job.platform,
    ...(job.accountId ? { account_id: job.accountId } : {}),
    ...(job.payloadSnapshot?.remoteId || job.remoteId ? { remote_id: job.remoteId ?? job.payloadSnapshot.remoteId } : {}),
    idempotencyKey: job.idempotencyKey,
    payload_hash: job.payloadHash,
    ...(job.selectionHash ? { selection_hash: job.selectionHash } : {}),
    ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}),
  }
}

function creativeBrief(workspaceId: string, product: import('../../../packages/application/src/service.js').Product, params: JsonObject) {
  const assetType = typeof params.asset_type === 'string' ? params.asset_type : ''
  if (!['banner', 'ad', 'video_storyboard'].includes(assetType)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'asset_type 必须是 banner、ad 或 video_storyboard', 400)
  const platform = typeof params.platform === 'string' && SUPPORTED_PLATFORMS.includes(params.platform as Platform) ? params.platform as Platform : product.platform
  const parseJson = (key: string) => {
    const raw = params[key]
    if (typeof raw !== 'string' || !raw.trim()) return undefined
    try { return JSON.parse(raw) as unknown } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是有效 JSON`, 400) }
  }
  const dimensions = parseJson('dimensions_json')
  if (dimensions !== undefined && (!dimensions || typeof dimensions !== 'object' || Array.isArray(dimensions))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'dimensions_json 必须是对象', 400)
  const requestedSkus = parseJson('sku_ids_json')
  if (requestedSkus !== undefined && (!Array.isArray(requestedSkus) || requestedSkus.some(item => typeof item !== 'string'))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'sku_ids_json 必须是 SKU ID 数组', 400)
  const skuIds = (requestedSkus as string[] | undefined) ?? (product.skus?.map(sku => sku.id) ?? [])
  const knownSkuIds = new Set(product.skus?.map(sku => sku.id) ?? [])
  const unknownSku = skuIds.find(sku => !knownSkuIds.has(sku))
  if (unknownSku) throw new DomainError('SKU_NOT_FOUND', `SKU ${unknownSku} 不属于当前商品，不能进入创意 Brief`, 409, { sku_id: unknownSku })
  const promotion = parseJson('promotion_json')
  if (promotion !== undefined && (!promotion || typeof promotion !== 'object' || Array.isArray(promotion))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'promotion_json 必须是对象', 400)
  if (promotion && typeof promotion === 'object' && !Array.isArray(promotion) && Object.keys(promotion).length > 0 && typeof (promotion as Record<string, unknown>).valid_to !== 'string') {
    throw new DomainError('PROMOTION_VALIDITY_REQUIRED', '促销信息必须包含 valid_to 有效期，避免过期价格进入正式素材', 409)
  }
  const placement = typeof params.placement === 'string' && params.placement.trim() ? params.placement.trim() : assetType === 'banner' ? '店铺首页移动端首屏' : assetType === 'ad' ? '信息流商品广告位' : '商品详情页主图视频'
  const goal = typeof params.goal === 'string' && params.goal.trim() ? params.goal.trim() : '清晰传达已确认商品价值并引导查看商品'
  const audience = typeof params.audience === 'string' && params.audience.trim() ? params.audience.trim() : '以已确认品牌人群为准，未提供时不擅自推断'
  const textDensity = typeof params.text_density === 'string' ? params.text_density : 'title_and_subtitle'
  const dimensionsValue = dimensions ?? (assetType === 'video_storyboard' ? { ratio: '9:16', resolution: '1080x1920' } : assetType === 'banner' ? { ratio: '3:1', resolution: '1200x400' } : { ratio: '1:1', resolution: '1080x1080' })
  const base = { id: `brief_${createHash('sha256').update(canonicalJson({ workspaceId, productId: product.id, assetType, platform, placement, skuIds, promotion })).digest('hex').slice(0, 16)}`, version: 1, workspaceId, productId: product.id, platform, assetType, placement, goal, audience, dimensions: dimensionsValue, textDensity, skuIds, ...(promotion ? { promotion } : {}), factSnapshot: { productVersion: product.version ?? 1, title: product.title, category: product.category, skuIds, source: product.source }, protectedAreas: ['商品本体结构、颜色、材质、Logo/印花、认证标识和 SKU 对应关系'], externallyUnverified: ['真实渲染效果', '尺寸/清晰度', '平台最终审核', ...(assetType === 'ad' ? ['投放效果与预算'] : [])], renderable: false }
  if (assetType === 'banner') return { ...base, layout: { hierarchy: ['商品真实图', '主标题', '核心卖点', 'CTA'], modules: ['品牌区', '主推商品区', '卖点/优惠区', '行动入口'], safeArea: '四边至少保留 5% 安全区', multiSize: true, productBinding: skuIds.map(skuId => ({ skuId, role: '主推商品/SKU' })) } }
  if (assetType === 'ad') return { ...base, matrix: ['主标题+单卖点', '场景利益点', '事实参数+CTA'].map((angle, index) => ({ id: `variant_${index + 1}`, angle, copyScope: '仅使用已确认事实和已提供促销', visual: '真实商品图为主体，背景和装饰可后续制作', dimensions: dimensionsValue })), restrictions: ['不自动上传、不自动投放', '不得使用无证据功效、绝对化或虚假稀缺表达'] }
  const duration = typeof params.duration_seconds === 'string' && /^\d+$/.test(params.duration_seconds) ? Math.min(60, Math.max(3, Number(params.duration_seconds))) : 15
  return { ...base, durationSeconds: duration, scenes: [{ seconds: '0-2', purpose: '建立商品与品牌识别', shot: '真实商品全景', narration: '只读商品已确认名称', subtitle: '商品名来自事实快照' }, { seconds: '2-7', purpose: '展示核心卖点', shot: '真实商品细节/材质特写', narration: '逐条引用已确认卖点', subtitle: textDensity === 'none' ? '无字幕版本' : '卖点字幕需逐条对应事实' }, { seconds: '7-12', purpose: '展示使用场景', shot: '已确认使用场景，禁止臆造功效', narration: '场景说明不得扩展商品事实', subtitle: '场景说明' }, { seconds: `12-${duration}`, purpose: '行动号召', shot: '商品与品牌收束', narration: '查看商品详情', subtitle: 'CTA 与平台版位一致' }], audio: { music: '待确认授权音乐', voiceover: '可选，需单独确认文本与授权' }, storyboardConfirmationRequired: true }
}

function escapeXml(value: string) {
  return value.replace(/[<>&'\"]/g, character => ({ '<': '&lt;', '>': '&gt;', '&': '&amp;', "'": '&apos;', '"': '&quot;' }[character] ?? character))
}

function creativePreview(workspaceId: string, product: import('../../../packages/application/src/service.js').Product, params: JsonObject) {
  const assetType = typeof params.asset_type === 'string' ? params.asset_type : ''
  if (!['banner', 'ad'].includes(assetType)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'creative.preview 只支持 banner 或 ad；视频请先使用 creative.brief 生成分镜', 400)
  const platform = typeof params.platform === 'string' && SUPPORTED_PLATFORMS.includes(params.platform as Platform) ? params.platform : product.platform
  const count = typeof params.count === 'string' && /^\d+$/.test(params.count) ? Math.min(3, Math.max(1, Number(params.count))) : 1
  const density = typeof params.text_density === 'string' ? params.text_density : 'title_and_subtitle'
  const width = assetType === 'banner' ? 1200 : 1080
  const height = assetType === 'banner' ? 400 : 1080
  const title = escapeXml(product.title)
  const subtitle = density === 'none' ? '' : density === 'single_selling_point' ? '已确认商品卖点' : `${escapeXml(product.category ?? '商品')} · 事实可追溯`
  const images = Array.from({ length: count }, (_, index) => {
    const hue = 214 + index * 24
    const svg = `<svg xmlns="http://www.w3.org/2000/svg" width="${width}" height="${height}" viewBox="0 0 ${width} ${height}"><defs><linearGradient id="bg" x1="0" x2="1"><stop stop-color="#eff6ff"/><stop offset="1" stop-color="#dbeafe"/></linearGradient></defs><rect width="${width}" height="${height}" fill="url(#bg)"/><rect x="40" y="40" width="${width - 80}" height="${height - 80}" rx="28" fill="#fff" stroke="#bfdbfe" stroke-width="4"/><circle cx="${Math.round(width * 0.7)}" cy="${Math.round(height * 0.48)}" r="${Math.round(Math.min(width, height) * 0.22)}" fill="hsl(${hue} 65% 78%)"/><text x="80" y="110" font-family="Arial, sans-serif" font-size="26" fill="#2563eb">${escapeXml(platform)} · REVIEW PREVIEW</text><text x="80" y="${Math.round(height * 0.62)}" font-family="Arial, sans-serif" font-size="44" font-weight="700" fill="#111827">${title}</text>${subtitle ? `<text x="80" y="${Math.round(height * 0.7)}" font-family="Arial, sans-serif" font-size="26" fill="#475569">${subtitle}</text>` : ''}<text x="80" y="${height - 75}" font-family="Arial, sans-serif" font-size="18" fill="#64748b">AI 预览 · 不代表已渲染、投放或平台审核通过 · ${escapeXml(product.source)}</text></svg>`
    return `data:image/svg+xml;base64,${Buffer.from(svg).toString('base64')}`
  })
  return { id: `preview_${createHash('sha256').update(`${workspaceId}:${product.id}:${assetType}:${platform}:${density}`).digest('hex').slice(0, 16)}`, productId: product.id, platform, assetType, width, height, images, renderMode: 'deterministic_review_preview', externallyUnverified: ['真实视觉成片质量', '尺寸/清晰度', 'OCR 与平台最终审核', ...(assetType === 'ad' ? ['投放效果与预算'] : [])] }
}

/**
 * Local Codex acceptance only: there is no external platform worker in the
 * fixture LaunchAgent, so leave a durable, explicit simulated submission
 * observation instead of allowing a demo publish job to remain queued forever.
 * Production is deliberately excluded and still requires a real worker and a
 * verifiable platform receipt before it can become published.
 */
function scheduleFixturePublishObservation(job: import('../../../packages/application/src/service.js').PublishJob) {
  if (!fixtureMode || process.env.CONNECTOR_FIXTURE_PUBLISH_AUTO_OBSERVE === 'false') return
  setTimeout(() => {
    void (async () => {
      try {
        const current = service.getPublishJob(job.id)
        if (current.state !== 'queued') return
        const observed = service.recordPublishObservation({
          workspaceId: job.workspaceId,
          publishJobId: job.id,
          status: { found: true, state: 'submitted', requestId: `fixture-request-${job.id}`, simulated: true },
        })
        await persistSnapshot(job.workspaceId, 'task', service.getTask(job.taskId), service.getTask(job.taskId) as unknown as Record<string, unknown>)
        await persistSnapshot(job.workspaceId, 'publish_job', observed, observed as unknown as Record<string, unknown>)
        await persistEvent(job.workspaceId, job.id, 'publish.observation', observed.revision, {
          job_id: job.id,
          task_id: job.taskId,
          source: 'fixture_worker',
          status: 'submitted',
          request_id: observed.requestId,
          simulated: true,
        })
        // A fixture submission has crossed the local API/connector admission
        // boundary. It intentionally remains `submitted` (never claim a real
        // platform success), but it must not hold a distributed execution
        // slot forever while a local demo waits for an external receipt.
        await releaseDistributedJobSlot(job.workspaceId, `publish:${job.idempotencyKey}`)
      } catch {
        // The normal worker/reconciliation path owns production failures. A
        // local demo callback must never crash or take down the API process.
      }
    })()
  }, 50)
}
const DEFAULT_BODY_LIMIT = 1_048_576
// MCP asset uploads follow the PRD's 50 MB per-file limit. Base64 expands the
// payload, so leave headroom for JSON framing while keeping the normal REST
// request limit unchanged.
const MCP_BODY_LIMIT = 70 * 1024 * 1024
const DEFAULT_RATE_LIMIT = 120

const rateBuckets = new Map<string, { windowStartedAt: number; count: number }>()
const metricsStartedAt = process.hrtime.bigint()
const metricRequests = new Map<string, number>()
let metricRequestCount = 0
let metricRequestDurationSeconds = 0
let metricInFlight = 0

function observeHttpMetric(req: IncomingMessage, res: ServerResponse, startedAt: bigint) {
  const key = `${req.method ?? 'UNKNOWN'}:${res.statusCode}`
  metricRequests.set(key, (metricRequests.get(key) ?? 0) + 1)
  metricRequestCount += 1
  metricRequestDurationSeconds += Number(process.hrtime.bigint() - startedAt) / 1_000_000_000
  metricInFlight = Math.max(0, metricInFlight - 1)
}

async function prometheusMetrics() {
  const nowMs = Date.now()
  const ageSeconds = (value: string) => Math.max(0, (nowMs - Date.parse(value)) / 1000)
  const stateCounts = new Map<string, number>()
  for (const job of service.syncJobs.values()) stateCounts.set(`sync:${job.state}`, (stateCounts.get(`sync:${job.state}`) ?? 0) + 1)
  for (const job of service.publishJobs.values()) stateCounts.set(`publish:${job.state}`, (stateCounts.get(`publish:${job.state}`) ?? 0) + 1)
  for (const job of service.generationJobs.values()) stateCounts.set(`generation:${job.state}`, (stateCounts.get(`generation:${job.state}`) ?? 0) + 1)
  const oldest = (prefix: string, states: readonly string[]) => Math.max(0, ...[...service.syncJobs.values()].filter(job => prefix === 'sync' && states.includes(job.state)).map(job => ageSeconds(job.updatedAt)), ...[...service.publishJobs.values()].filter(job => prefix === 'publish' && states.includes(job.state)).map(job => ageSeconds(job.createdAt)), ...[...service.generationJobs.values()].filter(job => prefix === 'generation' && states.includes(job.state)).map(job => ageSeconds(job.updatedAt)))
  let outboxPending = 0
  const connectorErrors: Record<string, number> = { RATE_LIMITED: 0, TIMEOUT: 0 }
  if (persistence.outbox?.metrics && persistence.listWorkspaceIds) {
    const workspaceMetrics = await mapWithConcurrency(await persistence.listWorkspaceIds(), 8, workspaceId => persistence.outbox!.metrics!(workspaceId))
    for (const metrics of workspaceMetrics) {
      outboxPending += metrics.pending
      for (const [code, count] of Object.entries(metrics.connectorErrors)) connectorErrors[code] = (connectorErrors[code] ?? 0) + count
    }
  }
  const lines = [
    '# HELP merchant_http_requests_total Total HTTP requests handled by status code.',
    '# TYPE merchant_http_requests_total counter',
    ...[...metricRequests.entries()].map(([key, value]) => {
      const [method, status] = key.split(':')
      return `merchant_http_requests_total{method="${method}",status="${status}"} ${value}`
    }),
    '# HELP merchant_http_request_duration_seconds_sum Total HTTP request duration in seconds.',
    '# TYPE merchant_http_request_duration_seconds_sum counter',
    `merchant_http_request_duration_seconds_sum ${metricRequestDurationSeconds}`,
    '# HELP merchant_http_request_duration_seconds_count Total HTTP request count used for the duration summary.',
    '# TYPE merchant_http_request_duration_seconds_count counter',
    `merchant_http_request_duration_seconds_count ${metricRequestCount}`,
    '# HELP merchant_http_inflight_requests Current in-flight HTTP requests.',
    '# TYPE merchant_http_inflight_requests gauge',
    `merchant_http_inflight_requests ${metricInFlight}`,
    '# HELP merchant_process_uptime_seconds Process uptime in seconds.',
    '# TYPE merchant_process_uptime_seconds gauge',
    `merchant_process_uptime_seconds ${Number(process.hrtime.bigint() - metricsStartedAt) / 1_000_000_000}`,
    '# HELP merchant_job_state_count Current durable business jobs by queue and state.',
    '# TYPE merchant_job_state_count gauge',
    ...[...stateCounts.entries()].map(([key, value]) => { const [queue, state] = key.split(':'); return `merchant_job_state_count{queue="${queue}",state="${state}"} ${value}` }),
    '# HELP merchant_queue_oldest_job_age_seconds Age of the oldest active job by queue.',
    '# TYPE merchant_queue_oldest_job_age_seconds gauge',
    `merchant_queue_oldest_job_age_seconds{queue="sync"} ${oldest('sync', ['queued', 'running', 'partial'])}`,
    `merchant_queue_oldest_job_age_seconds{queue="publish"} ${oldest('publish', ['prepared', 'confirmed', 'queued', 'submitting', 'reconciling', 'unknown'])}`,
    `merchant_queue_oldest_job_age_seconds{queue="generation"} ${oldest('generation', ['queued', 'running'])}`,
    `merchant_queue_oldest_job_age_seconds{queue="reconcile"} ${oldest('publish', ['reconciling', 'unknown'])}`,
    '# HELP merchant_connector_errors_total Durable connector failures recorded by code.',
    '# TYPE merchant_connector_errors_total counter',
    ...Object.entries(connectorErrors).map(([code, value]) => `merchant_connector_errors_total{code="${code}"} ${value}`),
    '# HELP merchant_outbox_pending_events Durable outbox events awaiting successful publication.',
    '# TYPE merchant_outbox_pending_events gauge',
    `merchant_outbox_pending_events ${outboxPending}`,
    '# HELP merchant_publish_unknown_age_seconds Age of the oldest unknown publish job.',
    '# TYPE merchant_publish_unknown_age_seconds gauge',
    `merchant_publish_unknown_age_seconds ${oldest('publish', ['unknown'])}`,
  ]
  return `${lines.join('\n')}\n`
}

function header(req: IncomingMessage, name: string) {
  const value = req?.headers?.[name.toLowerCase()]
  return Array.isArray(value) ? value[0] : value
}

type RequestObservationState = RequestLogInput & { startedAt: bigint; failed: boolean }
const requestObservationStates = new WeakMap<IncomingMessage, RequestObservationState>()

function requestObservabilityEnabled() {
  // Vitest simulates production in several suites. Do not let that switch on
  // per-request JSON logging for the whole process; observability tests opt in
  // explicitly, while real production remains always-on.
  if (process.env.VITEST === 'true') return process.env.REQUEST_OBSERVABILITY_LOGS === 'true'
  return isProduction() || process.env.REQUEST_OBSERVABILITY_LOGS === 'true'
}

function requestDurationMs(startedAt: bigint) {
  return Number(process.hrtime.bigint() - startedAt) / 1_000_000
}

function writeRequestObservation(req: IncomingMessage, event: 'request.received' | 'request.completed' | 'request.failed', input: RequestLogInput) {
  if (!requestObservabilityEnabled()) return
  console.info(serializeRequestLogEvent(buildRequestLogEvent(req, event, input)))
}

function beginRequestObservation(req: IncomingMessage) {
  const state: RequestObservationState = {
    startedAt: process.hrtime.bigint(),
    failed: false,
  }
  requestObservationStates.set(req, state)
  getRequestCorrelation(req)
  writeRequestObservation(req, 'request.received', state)
}

function enrichRequestObservation(req: IncomingMessage, input: RequestLogInput) {
  const state = requestObservationStates.get(req)
  if (state) Object.assign(state, Object.fromEntries(Object.entries(input).filter(([, value]) => value !== undefined)))
}

function trustedRequestObservationActor(req: IncomingMessage) {
  return requiresStrictAuth() ? requestPrincipals.get(req)?.actorId : undefined
}

function completeRequestObservation(req: IncomingMessage, res: ServerResponse) {
  const state = requestObservationStates.get(req)
  if (!state || state.failed) return
  if (res.statusCode >= 400) {
    failRequestObservation(req, res.statusCode, typeof state.errorCode === 'string' ? state.errorCode : 'HTTP_ERROR')
    return
  }
  writeRequestObservation(req, 'request.completed', { ...state, status: res.statusCode, durationMs: requestDurationMs(state.startedAt) })
}

function failRequestObservation(req: IncomingMessage, status: number, errorCode: string) {
  const state = requestObservationStates.get(req)
  if (!state || state.failed) return
  state.failed = true
  writeRequestObservation(req, 'request.failed', { ...state, status, errorCode, durationMs: requestDurationMs(state.startedAt) })
}

function requestId(req?: IncomingMessage) {
  return req ? getRequestCorrelation(req).requestId : `req_${randomUUID()}`
}

function send<T>(res: ServerResponse, status: number, workspaceId: string, data: T | null, error: ApiEnvelope<T>['error'] = null, req?: IncomingMessage) {
  const id = requestId(req)
  const traceId = req ? getRequestCorrelation(req).traceId : id
  if (req) enrichRequestObservation(req, { workspaceId, status, actorId: trustedRequestObservationActor(req), ...(error ? { errorCode: error.code } : {}) })
  const envelope: ApiEnvelope<T> = {
    request_id: id,
    trace_id: traceId as never,
    workspace_id: workspaceId as never,
    data: error ? null : data,
    warnings: [],
    next_actions: error && isObject(error.details) && Array.isArray(error.details.next_actions) ? error.details.next_actions : [],
    error,
  }
  res.statusCode = status
  res.setHeader('content-type', 'application/json; charset=utf-8')
  const origin = header(req ?? ({} as IncomingMessage), 'origin')
  const configuredOrigins = (process.env.ALLOWED_ORIGINS ?? process.env.ALLOWED_ORIGIN ?? (requiresStrictAuth() ? '' : '*'))
    .split(',').map(value => value.trim()).filter(Boolean)
  const exactOriginAllowed = Boolean(origin && configuredOrigins.includes(origin))
  if (configuredOrigins.includes('*')) res.setHeader('access-control-allow-origin', '*')
  else if (exactOriginAllowed) {
    res.setHeader('access-control-allow-origin', origin!)
    res.setHeader('access-control-allow-credentials', 'true')
    res.setHeader('vary', 'Origin')
  }
  res.setHeader('access-control-allow-headers', 'authorization, content-type, idempotency-key, x-workspace-id, x-ops-workbench, x-account-id, x-actor-id, x-request-id, x-trace-id, x-role, x-rule-approval-token')
  res.setHeader('access-control-allow-methods', 'DELETE,GET,POST,PUT,OPTIONS')
  res.setHeader('access-control-expose-headers', 'x-request-id, x-trace-id')
  res.setHeader('x-request-id', id)
  res.setHeader('x-trace-id', traceId)
  res.end(JSON.stringify(envelope))
}

function wantsOAuthHtml(req: IncomingMessage) {
  return /^\/v1\/oauth\/callback\//u.test(req.url ?? '') && (header(req, 'accept') ?? '').split(',').some(value => value.trim().toLowerCase() === 'text/html')
}

function sendOAuthCallbackPage(res: ServerResponse, status: number, input: { state: 'success' | 'error'; platform: string; storeLabel?: string; syncState?: string; code?: string; message?: string }, req?: IncomingMessage) {
  const escapeHtml = (value: string) => value.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#39;')
  const platform = escapeHtml(PLATFORM_LABELS[input.platform as Platform] ?? input.platform)
  const title = input.state === 'success' ? '店铺授权成功' : '店铺授权未完成'
  const heading = input.state === 'success' ? `已连接${platform}` : '授权失败'
  const body = input.state === 'success'
    ? `<p>店铺已安全绑定到大麦工作区。</p><p>状态：${escapeHtml(input.syncState === 'queued' ? '首轮商品同步已排队' : '等待同步配置')}</p><p>请返回 Codex App，刷新店铺状态后选择具体店铺继续。</p>`
    : `<p>${escapeHtml(input.message ?? '授权回调未完成，请返回 Codex App 重试。')}</p><p>请重新发起授权，不要重复使用当前页面参数。</p>`
  const request = req ? requestId(req) : 'oauth-callback'
  res.statusCode = status
  res.setHeader('content-type', 'text/html; charset=utf-8')
  res.setHeader('content-security-policy', "default-src 'none'; style-src 'unsafe-inline'")
  res.setHeader('cache-control', 'no-store, max-age=0')
  res.setHeader('pragma', 'no-cache')
  res.setHeader('x-request-id', request)
  res.end(`<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)}</title><style>body{font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;background:#f5f7fb;color:#172033;margin:0;padding:48px 20px}.card{max-width:560px;margin:auto;background:#fff;border:1px solid #dfe5ef;border-radius:16px;padding:32px;box-shadow:0 10px 30px #17203314}h1{margin:0 0 16px;font-size:24px}p{line-height:1.7;color:#526078}.mark{font-size:32px;margin-bottom:12px}</style></head><body><main class="card"><div class="mark">${input.state === 'success' ? '✓' : '!'}</div><h1>${escapeHtml(heading)}</h1>${body}</main></body></html>`)
}

function sendDownload(res: ServerResponse, content: { fileName: string; contentType: string; body: string; binaryBody?: Uint8Array }, req?: IncomingMessage) {
  const id = requestId(req)
  res.statusCode = 200
  res.setHeader('content-type', content.contentType)
  res.setHeader('content-disposition', `attachment; filename="${content.fileName.replace(/[^A-Za-z0-9._-]/g, '_')}"`)
  res.setHeader('cache-control', 'no-store, max-age=0')
  res.setHeader('pragma', 'no-cache')
  res.setHeader('vary', 'Authorization')
  res.setHeader('x-request-id', id)
  res.end(content.binaryBody ?? content.body)
}

function sendAssetDownload(res: ServerResponse, asset: { name: string; mimeType: string; sizeBytes: number }, stored: { body: Uint8Array }, req?: IncomingMessage) {
  const id = requestId(req)
  res.statusCode = 200
  res.setHeader('content-type', asset.mimeType)
  res.setHeader('content-length', String(stored.body.byteLength))
  res.setHeader('content-disposition', `inline; filename="${asset.name.replace(/[^A-Za-z0-9._-]/g, '_')}"`)
  res.setHeader('cache-control', 'private, no-store, max-age=0')
  res.setHeader('pragma', 'no-cache')
  res.setHeader('x-content-type-options', 'nosniff')
  res.setHeader('content-security-policy', "default-src 'none'; sandbox")
  res.setHeader('referrer-policy', 'no-referrer')
  res.setHeader('x-request-id', id)
  res.end(stored.body)
}

const SIGNED_ASSET_URL_TTL_SECONDS = 300

function assetDisplayUrlConfig() {
  const secret = process.env.ASSET_DISPLAY_URL_SIGNING_SECRET?.trim() ?? ''
  const activeKid = process.env.ASSET_DISPLAY_URL_SIGNING_KEY_ID?.trim() || 'primary'
  const rawBaseUrl = process.env.PUBLIC_ASSET_BASE_URL?.trim() ?? ''
  if (!secret || !rawBaseUrl) {
    if (isProduction()) throw new DomainError('ASSET_DISPLAY_URL_NOT_CONFIGURED', '生产图片展示域名或签名密钥未配置，已阻止生成可公开读取的图片地址', 503)
    return undefined
  }
  if (secret.length < 32) throw new DomainError('ASSET_DISPLAY_URL_SIGNING_SECRET_WEAK', '图片展示签名密钥长度不足，已阻止签发', 503)
  if (!/^[A-Za-z0-9._-]{1,64}$/u.test(activeKid)) throw new DomainError('ASSET_DISPLAY_URL_SIGNING_KEY_ID_INVALID', '图片展示签名密钥标识无效，已阻止签发', 503)
  const verificationKeys = new Map<string, string>([[activeKid, secret]])
  const rawPreviousKeys = process.env.ASSET_DISPLAY_URL_PREVIOUS_KEYS_JSON?.trim()
  if (rawPreviousKeys) {
    let parsed: unknown
    try { parsed = JSON.parse(rawPreviousKeys) } catch { throw new DomainError('ASSET_DISPLAY_URL_PREVIOUS_KEYS_INVALID', '图片展示历史验签密钥配置无效，已阻止签发', 503) }
    if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new DomainError('ASSET_DISPLAY_URL_PREVIOUS_KEYS_INVALID', '图片展示历史验签密钥配置无效，已阻止签发', 503)
    for (const [kid, value] of Object.entries(parsed as Record<string, unknown>)) {
      if (!/^[A-Za-z0-9._-]{1,64}$/u.test(kid) || typeof value !== 'string' || value.trim().length < 32 || kid === activeKid) throw new DomainError('ASSET_DISPLAY_URL_PREVIOUS_KEYS_INVALID', '图片展示历史验签密钥配置无效，已阻止签发', 503)
      verificationKeys.set(kid, value.trim())
    }
  }
  let baseUrl: URL
  try { baseUrl = new URL(rawBaseUrl) } catch { throw new DomainError('PUBLIC_ASSET_BASE_URL_INVALID', '图片展示基础地址无效，已阻止签发', 503) }
  const localHttp = baseUrl.protocol === 'http:' && ['127.0.0.1', 'localhost'].includes(baseUrl.hostname) && !isProduction()
  if (baseUrl.protocol !== 'https:' && !localHttp) throw new DomainError('PUBLIC_ASSET_BASE_URL_INSECURE', '图片展示基础地址必须使用 HTTPS', 503)
  if (baseUrl.username || baseUrl.password || baseUrl.search || baseUrl.hash || !['', '/'].includes(baseUrl.pathname)) throw new DomainError('PUBLIC_ASSET_BASE_URL_INVALID', '图片展示基础地址不能包含凭据、路径或查询参数', 503)
  if (isProduction()) {
    const allowedHosts = (process.env.ASSET_DISPLAY_ALLOWED_HOSTS ?? process.env.MERCHANT_BEARER_HOSTNAME ?? '').split(',').map(value => value.trim().toLowerCase()).filter(Boolean)
    if (!allowedHosts.length || !allowedHosts.includes(baseUrl.hostname.toLowerCase())) throw new DomainError('PUBLIC_ASSET_BASE_URL_HOST_NOT_ALLOWED', '图片展示域名不在生产允许列表，已阻止签发', 503)
  }
  return { secret, activeKid, verificationKeys, baseUrl }
}

function signedAssetCanonical(path: string, workspaceId: string, assetId: string, expires: number, sha256: string, version?: string, kid?: string) {
  return version && kid
    ? `${version}\n${kid}\nGET\n${path}\n${workspaceId}\n${assetId}\n${expires}\n${sha256}`
    : `GET\n${path}\n${workspaceId}\n${assetId}\n${expires}\n${sha256}`
}

function signedAssetDisplayUrl(workspaceId: string, assetId: string) {
  const config = assetDisplayUrlConfig()
  if (!config) return undefined
  const asset = assetForWorkspace(workspaceId, assetId)
  if (!isTrustedCleanAsset(asset)) throw new DomainError('QUARANTINE_ACCESS_DENIED', '素材尚未通过可信安全扫描，已阻止签发图片地址', 403)
  const path = `/v1/public/assets/${encodeURIComponent(asset.id)}/display`
  const expires = Math.floor(Date.now() / 1000) + SIGNED_ASSET_URL_TTL_SECONDS
  const version = '1'
  const signature = createHmac('sha256', config.secret).update(signedAssetCanonical(path, workspaceId, asset.id, expires, asset.sha256, version, config.activeKid)).digest('base64url')
  const url = new URL(path, config.baseUrl)
  url.searchParams.set('v', version)
  url.searchParams.set('kid', config.activeKid)
  url.searchParams.set('workspace_id', workspaceId)
  url.searchParams.set('expires', String(expires))
  url.searchParams.set('sha256', asset.sha256)
  url.searchParams.set('signature', signature)
  return url.toString()
}

async function serveSignedAssetDisplay(req: IncomingMessage, res: ServerResponse, url: URL, assetId: string) {
  const config = assetDisplayUrlConfig()
  if (!config) throw new DomainError('ASSET_DISPLAY_URL_NOT_CONFIGURED', '图片展示签名服务未配置', 503)
  const workspaceId = url.searchParams.get('workspace_id')?.trim() ?? ''
  const sha256 = url.searchParams.get('sha256')?.trim().toLowerCase() ?? ''
  const signature = url.searchParams.get('signature')?.trim() ?? ''
  const version = url.searchParams.get('v')?.trim() ?? ''
  const kid = url.searchParams.get('kid')?.trim() ?? ''
  const expires = Number(url.searchParams.get('expires'))
  const now = Math.floor(Date.now() / 1000)
  if (!workspaceId || !/^[a-f0-9]{64}$/u.test(sha256) || !Number.isSafeInteger(expires) || expires < now || expires > now + SIGNED_ASSET_URL_TTL_SECONDS + 30) throw new DomainError('ASSET_DISPLAY_URL_EXPIRED_OR_INVALID', '图片地址无效或已过期，请在对话中重新读取', 403)
  const path = `/v1/public/assets/${encodeURIComponent(assetId)}/display`
  if ((version || kid) && (version !== '1' || !kid)) throw new DomainError('ASSET_DISPLAY_URL_SIGNATURE_VERSION_INVALID', '图片地址签名版本无效', 403)
  const verificationSecret = version === '1' ? config.verificationKeys.get(kid) : config.secret
  if (!verificationSecret) throw new DomainError('ASSET_DISPLAY_URL_SIGNING_KEY_UNKNOWN', '图片地址签名密钥已失效，请在对话中重新读取', 403)
  const expected = createHmac('sha256', verificationSecret).update(signedAssetCanonical(path, workspaceId, assetId, expires, sha256, version || undefined, kid || undefined)).digest('base64url')
  if (!signature || !safeEqual(signature, expected)) throw new DomainError('ASSET_DISPLAY_URL_SIGNATURE_INVALID', '图片地址签名无效', 403)
  await enforceRateLimit(req, workspaceId)
  await hydrateWorkspace(workspaceId)
  const asset = assetForWorkspace(workspaceId, assetId)
  if (!isTrustedCleanAsset(asset) || asset.sha256 !== sha256) throw new DomainError('ASSET_DISPLAY_SNAPSHOT_MISMATCH', '图片安全快照已变化，请在对话中重新读取', 409)
  if (!['image/png', 'image/jpeg', 'image/webp'].includes(asset.mimeType.toLowerCase())) throw new DomainError('ASSET_DISPLAY_MIME_NOT_ALLOWED', '该素材类型不能通过图片展示地址读取', 415)
  let stored: Awaited<ReturnType<typeof getStoredObjectWithRetry>>
  try { stored = await getStoredObjectWithRetry(workspaceId, asset.storageKey) } catch (error) {
    if (error instanceof ObjectStorageError && error.code === 'OBJECT_NOT_FOUND') throw new DomainError('ASSET_BINARY_UNAVAILABLE', '图片文件不可用，请重新生成', 410)
    throw error
  }
  const storedDigest = createHash('sha256').update(stored.body).digest('hex')
  if (stored.metadata.sha256 !== asset.sha256 || stored.metadata.sizeBytes !== asset.sizeBytes || stored.metadata.contentType.toLowerCase() !== asset.mimeType.toLowerCase() || storedDigest !== asset.sha256) throw new DomainError('ASSET_BINARY_INTEGRITY_FAILED', '图片对象与已扫描快照不一致，已阻止展示', 409)
  return sendAssetDownload(res, asset, stored, req)
}

function fail(res: ServerResponse, status: number, workspaceId: string, code: string, message: string, req?: IncomingMessage, details?: Readonly<Record<string, unknown>>) {
  return send(res, status, workspaceId, null, { code, message, ...(details ? { details } : {}) }, req)
}

const requestBodyCache = new WeakMap<IncomingMessage, Buffer>()

async function requestBodyBytes(req: IncomingMessage, limit: number): Promise<Buffer> {
  const cached = requestBodyCache.get(req)
  if (cached) {
    if (cached.byteLength > limit) throw new DomainError(ERROR_CODES.REQUEST_BODY_TOO_LARGE, '请求体超过允许大小', 413)
    return cached
  }
  const declaredLength = Number(req.headers['content-length'] ?? 0)
  if (Number.isFinite(declaredLength) && declaredLength > limit) throw new DomainError(ERROR_CODES.REQUEST_BODY_TOO_LARGE, '请求体超过允许大小', 413)
  const chunks: Buffer[] = []
  let received = 0
  for await (const chunk of req) {
    const buffer = Buffer.from(chunk)
    received += buffer.byteLength
    if (received > limit) throw new DomainError(ERROR_CODES.REQUEST_BODY_TOO_LARGE, '请求体超过允许大小', 413)
    chunks.push(buffer)
  }
  const bytes = Buffer.concat(chunks)
  requestBodyCache.set(req, bytes)
  return bytes
}

async function body(req: IncomingMessage, requestedLimit = DEFAULT_BODY_LIMIT): Promise<JsonObject> {
  const configuredLimit = Number(process.env.REQUEST_BODY_LIMIT_BYTES ?? requestedLimit)
  const limit = Number.isFinite(configuredLimit) && configuredLimit > 0 ? configuredLimit : DEFAULT_BODY_LIMIT
  const bytes = await requestBodyBytes(req, limit)
  if (!bytes.length) return {}
  try {
    const value: unknown = JSON.parse(bytes.toString('utf8'))
    if (!value || typeof value !== 'object' || Array.isArray(value)) throw new Error('not an object')
    return value as JsonObject
  } catch {
    throw new DomainError(ERROR_CODES.INVALID_JSON_BODY, '请求体必须是合法 JSON 对象', 400)
  }
}

async function binaryBody(req: IncomingMessage, limit: number): Promise<Uint8Array> {
  const bytes = await requestBodyBytes(req, limit)
  if (!bytes.length) throw new DomainError('ASSET_BODY_REQUIRED', '素材请求体不能为空', 400)
  return new Uint8Array(bytes)
}

function isProduction() { return process.env.NODE_ENV === 'production' }
function providerSucceededButSettlementPending(error: unknown) {
  if (!error || typeof error !== 'object') return false
  const candidate = error as { code?: unknown; providerSucceeded?: unknown; details?: Record<string, unknown> }
  return candidate.providerSucceeded === true || candidate.details?.provider_succeeded === true || candidate.code === 'MODEL_USAGE_SETTLEMENT_PENDING' || candidate.code === 'MODEL_USAGE_COST_MISSING'
}
function requiresStrictAuth() {
  if (process.env.AUTH_ENFORCEMENT === 'strict') return true
  if (['staging', 'preview', 'production'].includes(process.env.NODE_ENV ?? '')) return true
  if (process.env.AUTH_ENFORCEMENT === 'local') return !['development', 'test'].includes(process.env.NODE_ENV ?? '') && process.env.VITEST !== 'true'
  return process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true'
}

type RequestPrincipal = {
  actorId: string
  workspaces: string[]
  roles: string[]
  workbench: OpsWorkbench
  availableWorkbenches?: OpsWorkbench[]
  issuer?: string
  sessionSubject?: string
  sessionKind?: 'oidc' | 'api_token'
  sessionIssuedAt?: string
  sessionExpiresAt?: string
  mfaVerified?: boolean
  identityId?: string
  sessionId?: string
  identityStatus?: 'active' | 'suspended'
  riskDecision?: 'allow' | 'step_up' | 'block'
  memberRole?: MemberRole
  memberStatus?: MemberStatus
  explicitDeniedCapabilities?: CapabilityId[]
  authorizationRevision?: number
  activeAuthorizationGrants?: AuthorizationGrant[]
  platformRoleAssignments?: PlatformRoleAssignment[]
}
const requestPrincipals = new WeakMap<IncomingMessage, RequestPrincipal>()
const requestAuthorizationDecisions = new WeakMap<IncomingMessage, AuthorizationDecision>()
const requestMemberChecks = new WeakSet<IncomingMessage>()
// Set only after the shared capability evaluator has atomically consumed the
// exact durable grant used by this request. Merely having some active grant is
// never enough to bypass workspace membership.
type ConsumedAuthorizationGrantEvidence = {
  grant: AuthorizationGrant
  decisionId: string
  method: string
  capability: CapabilityId
  workspaceId: string
}
const requestConsumedAuthorizationGrants = new WeakMap<IncomingMessage, ConsumedAuthorizationGrantEvidence>()

function exactConsumedGrantForRequest(req: IncomingMessage, workspaceId: string, capability?: CapabilityId) {
  const evidence = requestConsumedAuthorizationGrants.get(req)
  const grant = evidence?.grant
  const decision = requestAuthorizationDecisions.get(req)
  const principal = requestPrincipals.get(req)
  if (!evidence || !grant || !decision || !principal?.identityId || !decision.authorized
    || evidence.decisionId !== decision.decision_id || evidence.method !== decision.method
    || evidence.capability !== decision.capability || evidence.workspaceId !== workspaceId
    || decision.workbench !== 'workspace' || decision.scope.required !== 'workspace'
    || decision.scope.resource_id !== workspaceId || grant.subjectIdentityId !== principal.identityId
    || grant.workspaceId !== workspaceId || !grant.capabilities.includes(decision.capability)
    || capability && decision.capability !== capability
    || grant.resourceScope.type !== 'workspace' || !Array.isArray(grant.resourceScope.ids)
    || grant.resourceScope.ids.length !== 1 || grant.resourceScope.ids[0] !== workspaceId) return undefined
  return grant
}
const workerAuthorizedRequests = new WeakSet<IncomingMessage>()
// Authentication is read-heavy, but the durable identity boundary also
// refreshes the identity/session row. Coalesce cold-start observations for
// the same session so a burst of UI requests cannot queue on the same unique
// key and exhaust the API pool.
const authenticatedObservationFlights = new Map<string, Promise<IdentityAuthorizationSnapshot>>()
const testOidcNonces = new Map<string, number>()
const testAssetScannerNonces = new Map<string, number>()
const testWorkerNonces = new Map<string, number>()
const workspaceMemberRoles = new Set<MemberRole>(['workspace_owner', 'merchant_admin', 'operator', 'support', 'finance', 'platform_ops'])
// Rules governance is tenant-scoped on the rule-center HTTP boundary and on
// knowledge.rule.create. Keep this explicit gateway role in a workspace
// projection without making any platform-only capability workspace-scoped.
const workspaceGatewayRoles = new Set<CanonicalRole>(['rules_admin'])

function authorizedRoles(principal?: RequestPrincipal) {
  if (!principal) return []
  if (principal.workbench === 'platform') return [...new Set(principal.roles.filter(role => {
    const canonical = canonicalizeRole(role, 'gateway')
    return canonical !== undefined && platformCanonicalRoles.has(canonical)
  }))]
  const gatewayWorkspaceRoles = principal.roles.filter(role => {
    const canonical = canonicalizeRole(role, 'gateway')
    return canonical !== undefined && (!platformCanonicalRoles.has(canonical) || workspaceGatewayRoles.has(canonical)) && !workspaceMemberRoles.has(role as MemberRole)
  })
  return [...new Set([...(principal.memberRole ? [principal.memberRole] : []), ...gatewayWorkspaceRoles])]
}

const platformCanonicalRoles = new Set<CanonicalRole>(['platform_admin', 'ops_admin', 'support_agent', 'finance_ops', 'security_admin', 'auditor', 'rules_admin', 'model_admin', 'release_admin'])
const alwaysEnforcedMcpMethods = new Set([
  'ops.user.suspend', 'ops.user.activate', 'ops.user.risk.transition', 'ops.user.session.revoke',
  'ops.member.upsert', 'ops.member.suspend', 'ops.feature-flag.upsert', 'ops.feature-flag.emergency.set',
  'ops.data.delete.cancel', 'ops.data.delete.approve', 'workspace.data.delete.request', 'billing.usage.refund', 'billing.refund',
  'rule.publish', 'publish.confirm', 'publish.batch.confirm',
  'catalog.image.select',
  'ops.marketing.asset_scan.retry',
])
const knownAuthorizationDomains = new Set(CAPABILITIES.map(capability => capability.split('.')[0]!))

export function productionAuthorizationReadiness(source: NodeJS.ProcessEnv = process.env, production = source.NODE_ENV === 'production') {
  if (!production) return { ready: true, reasons: [] as string[] }
  const reasons: string[] = []
  if (source.MCP_AUTHZ_MODE !== 'enforce') reasons.push('mcp_authz_mode_not_enforce')
  if (source.AUTHZ_DURABLE_ASSIGNMENTS_REQUIRED !== 'true') reasons.push('durable_assignments_not_required')
  if (source.MCP_AUTHZ_ENFORCE_DOMAINS?.trim()) reasons.push('enforce_domains_must_be_empty')
  let coverage: ReturnType<typeof mcpAuthorizationCoverageReport> | { mode: string; available: false }
  try {
    coverage = mcpAuthorizationCoverageReport(source, false, false)
  } catch {
    // Readiness is a diagnostic boundary: malformed configuration is reported
    // as not ready while runtime admission still throws and refuses startup.
    coverage = { mode: source.MCP_AUTHZ_MODE?.trim() || 'unset', available: false }
  }
  return { ready: reasons.length === 0, reasons, coverage }
}

function assertProductionAuthorizationRuntime(source: NodeJS.ProcessEnv = process.env, production = isProduction(), testRuntime = source.VITEST === 'true') {
  if (!production || testRuntime) return
  const readiness = productionAuthorizationReadiness(source, true)
  if (!readiness.ready) throw new DomainError('AUTHORIZATION_RUNTIME_NOT_READY', '生产授权运行时未满足全量强制执行和持久角色权威要求', 503)
}

export function mcpAuthorizationRuntimeConfig(source: NodeJS.ProcessEnv = process.env, production = isProduction(), testRuntime = source.VITEST === 'true') {
  assertProductionAuthorizationRuntime(source, production, testRuntime)
  const rawMode = source.MCP_AUTHZ_MODE?.trim()
  if (!rawMode) {
    return { mode: 'shadow' as const, enforceDomains: new Set<string>() }
  }
  if (rawMode !== 'shadow' && rawMode !== 'staged' && rawMode !== 'enforce') throw new DomainError('AUTHZ_MODE_INVALID', 'MCP_AUTHZ_MODE 仅支持 shadow、staged 或 enforce，未知模式已拒绝启动授权', 503)
  const rawDomains = (source.MCP_AUTHZ_ENFORCE_DOMAINS ?? '').split(',').map(value => value.trim()).filter(Boolean)
  if (rawMode === 'shadow' && rawDomains.length) throw new DomainError('AUTHZ_ENFORCE_DOMAINS_INVALID', 'shadow 模式不能声明强制域，避免产生已强制的错误预期', 503)
  if (rawMode === 'enforce' && rawDomains.length) throw new DomainError('AUTHZ_ENFORCE_DOMAINS_INVALID', 'enforce 模式已经覆盖全部 capability 域，不应再声明分阶段域', 503)
  if (rawMode === 'staged' && rawDomains.length === 0) throw new DomainError('AUTHZ_ENFORCE_DOMAINS_REQUIRED', 'staged 模式必须显式声明至少一个 capability 域', 503)
  const invalid = rawDomains.filter(domain => !knownAuthorizationDomains.has(domain))
  if (invalid.length) throw new DomainError('AUTHZ_ENFORCE_DOMAIN_UNKNOWN', `未知 capability 域: ${invalid.join(',')}`, 503)
  return { mode: rawMode, enforceDomains: new Set(rawDomains) }
}

export function mcpAuthorizationEnforcedMethods(source: NodeJS.ProcessEnv = process.env, production = isProduction(), testRuntime = source.VITEST === 'true') {
  const runtime = mcpAuthorizationRuntimeConfig(source, production, testRuntime)
  return MCP_METHODS.filter(method => {
    const policy = getMcpMethodPolicy(method)!
    return runtime.mode === 'enforce' || alwaysEnforcedMcpMethods.has(method) || runtime.enforceDomains.has(policy.capability.split('.')[0]!)
  })
}

export function mcpAuthorizationCoverageReport(source: NodeJS.ProcessEnv = process.env, production = isProduction(), testRuntime = source.VITEST === 'true') {
  const runtime = mcpAuthorizationRuntimeConfig(source, production, testRuntime)
  const enforcedMethods = new Set(mcpAuthorizationEnforcedMethods(source, production, testRuntime))
  const domains = [...knownAuthorizationDomains].sort()
  const enforcedDomains = domains.filter(domain => MCP_METHODS
    .filter(method => getMcpMethodPolicy(method)!.capability.split('.')[0] === domain)
    .every(method => enforcedMethods.has(method)))
  return Object.freeze({
    mode: runtime.mode,
    policy_version: AUTHZ_POLICY_VERSION,
    method_total: MCP_METHODS.length,
    enforced_method_count: enforcedMethods.size,
    shadow_method_count: MCP_METHODS.length - enforcedMethods.size,
    enforcement_ratio: enforcedMethods.size / MCP_METHODS.length,
    domain_total: domains.length,
    enforced_domains: enforcedDomains,
    shadow_domains: domains.filter(domain => !enforcedDomains.includes(domain)),
  })
}

function effectiveAuthorizationProjection(principal: RequestPrincipal | undefined, workspaceId: string) {
  const roles = authorizedRoles(principal)
  const canonicalRoles = principal?.workbench === 'workspace'
    ? resolveCanonicalRoles({ gatewayRoles: roles.filter(role => role !== principal.memberRole), memberRole: principal.memberRole })
    : resolveCanonicalRoles({ gatewayRoles: roles })
  const atoms: PermissionAtom[] = []
  if (principal?.actorId) atoms.push({ capability: 'authorization.session.read', effect: 'allow', scope: { type: 'self', ids: [principal.actorId] }, source: 'gateway_assertion', sourceId: `authenticated:${principal.identityId ?? principal.actorId}`, obligations: [], ...(principal.authorizationRevision !== undefined ? { revision: String(principal.authorizationRevision) } : {}) })
  for (const role of canonicalRoles) {
    const assignment = principal?.platformRoleAssignments?.find(item => item.role === role)
    const fromMembership = principal?.workbench === 'workspace' && principal.memberRole && canonicalizeRole(principal.memberRole, 'membership') === role
    const source = assignment ? 'platform_assignment' as const : fromMembership ? 'workspace_membership' as const : 'gateway_assertion' as const
    const sourceId = assignment?.id ?? (fromMembership ? `membership:${workspaceId}:${principal?.actorId ?? 'unknown'}` : `gateway:${role}`)
    for (const capability of capabilitiesForRoles([role])) {
      const capabilityPolicies = MCP_METHODS
        .map(method => getMcpMethodPolicy(method))
        .filter((policy): policy is NonNullable<ReturnType<typeof getMcpMethodPolicy>> => policy?.capability === capability)
      const scopes = [...new Set(capabilityPolicies.filter(policy => (
        policy.scope === 'self'
        || principal?.workbench === 'platform' && policy.scope === 'platform'
        || principal?.workbench === 'workspace' && policy.scope !== 'platform'
      )).map(policy => policy.scope))]
      // A platform role may need a capability for global navigation/projection even
      // when every executable MCP method for that capability is workspace-scoped.
      // Binding that atom to the platform scope keeps the UI truthful without
      // authorizing a workspace method: exact-scope evaluation still rejects it.
      if (scopes.length === 0 && principal?.workbench === 'platform') scopes.push(capability === 'authorization.session.read' ? 'self' : 'platform')
      else if (scopes.length === 0 && capabilityPolicies.length === 0) scopes.push(capability === 'authorization.session.read' ? 'self' : 'workspace')
      for (const scope of scopes) {
        const ids = scope === 'self' ? (principal?.actorId ? [principal.actorId] : [])
          : scope === 'platform' ? ['*']
            : scope === 'workspace' && workspaceId ? [workspaceId]
              : scope === 'account' ? workspaceStoreDirectory(workspaceId).map(store => store.accountId)
              : []
        if (ids.length) atoms.push({ capability, effect: 'allow', scope: { type: scope, ids }, source, sourceId, obligations: [], ...(assignment?.expiresAt ? { expiresAt: assignment.expiresAt } : {}), ...(assignment ? { revision: String(assignment.authorizationRevision) } : {}) })
      }
    }
  }
  for (const grant of principal?.activeAuthorizationGrants ?? []) {
    const rawType = grant.resourceScope.type
    const rawIds = grant.resourceScope.ids
    if ((rawType !== 'workspace' && rawType !== 'brand' && rawType !== 'account') || !Array.isArray(rawIds) || rawIds.length === 0 || rawIds.some(id => typeof id !== 'string' || !id.trim()) || (rawType === 'workspace' && (rawIds.length !== 1 || rawIds[0] !== workspaceId))) {
      throw new DomainError('AUTHORIZATION_GRANT_INVALID', '持久授权 grant 的 resource_scope 无法安全解析，已拒绝使用', 503, { grant_id: grant.id })
    }
    for (const value of grant.capabilities) {
      if (!CAPABILITIES.includes(value as CapabilityId)) throw new DomainError('AUTHORIZATION_GRANT_INVALID', '持久授权 grant 包含未知 capability，已拒绝使用', 503, { grant_id: grant.id })
      atoms.push({ capability: value as CapabilityId, effect: 'allow', scope: { type: rawType, ids: rawIds as string[] }, source: 'temporary_grant', sourceId: grant.id, obligations: [], effectLimit: grant.accessMode, expiresAt: grant.expiresAt, revision: String(grant.authorizationRevision) })
    }
  }
  for (const capability of principal?.explicitDeniedCapabilities ?? []) atoms.push({ capability, effect: 'deny', scope: { type: 'self', ids: principal?.actorId ? [principal.actorId] : [] }, source: 'explicit_deny', sourceId: `identity:${principal?.identityId ?? principal?.actorId ?? 'unknown'}`, obligations: [] })
  const deniedCapabilities = new Set(atoms.filter(atom => atom.effect === 'deny').map(atom => atom.capability))
  const allowedAtoms = atoms.filter(atom => atom.effect === 'allow' && !deniedCapabilities.has(atom.capability))
  const capabilities = [...new Set(allowedAtoms.map(atom => atom.capability))].sort()
  const scopes = [...new Map(allowedAtoms.map(atom => [`${atom.scope.type}:${atom.scope.ids.join(',')}`, atom.scope])).values()]
  return { roles, canonicalRoles, capabilities, scopes, atoms }
}

const authorizationGovernanceCapabilities = new Set<CapabilityId>([
  'authorization.role.manage', 'authorization.grant.manage', 'workspace.status.update', 'workspace.delete.execute',
  'identity.update', 'identity.session.revoke', 'incident.administer', 'feature_flag.administer', 'audit.export',
  'billing.reconcile.execute', 'billing.refund.execute', 'commercial.update', 'platform.settings.update',
  'platform.media_spec.approve', 'model.policy.update', 'rule.publish.approve', 'canonical.backfill.update',
])

function canonicalRoleMethodAccess(role: CanonicalRole, policy: (typeof MCP_METHOD_POLICIES)[keyof typeof MCP_METHOD_POLICIES]) {
  if (!capabilitiesForRoles([role]).includes(policy.capability)) return 'hidden' as const
  if (policy.effect === 'read') return 'read' as const
  return authorizationGovernanceCapabilities.has(policy.capability) ? 'govern' as const : 'operate' as const
}

function satisfiedAuthorizationObligations(params: Record<string, unknown>, req: IncomingMessage) {
  const principal = requestPrincipals.get(req)
  const satisfied: AuthorizationObligation[] = []
  if (typeof params.reason === 'string' && params.reason.trim()) satisfied.push('reason')
  if ((typeof params.expected_revision === 'string' && params.expected_revision.trim()) || (typeof params.expected_authorization_revision === 'string' && params.expected_authorization_revision.trim()) || (typeof params.expected_asset_revision === 'string' && params.expected_asset_revision.trim())) satisfied.push('revision')
  if (typeof params.approved_by === 'string' && params.approved_by.trim() && typeof params.approved_at === 'string' && params.approved_at.trim()) satisfied.push('approval')
  if ((typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim()) satisfied.push('idempotency')
  if ((typeof params.confirmation_hash === 'string' && params.confirmation_hash.trim()) || (typeof params.confirmations_json === 'string' && params.confirmations_json.trim())) satisfied.push('confirmation')
  if (principal?.mfaVerified) satisfied.push('mfa')
  return satisfied
}

export function resolveAuthorizationResourceScope(policy: ReturnType<typeof getMcpMethodPolicy>, workspaceId: string, params: Record<string, unknown>, principal?: Pick<RequestPrincipal, 'actorId'>) {
  if (!policy) return undefined
  if (policy.scope === 'platform') return { type: 'platform' as const, id: '*' }
  const exactId = (value: unknown) => typeof value === 'string' && value.trim() ? value.trim() : undefined
  if (policy.scope === 'workspace') return { type: 'workspace' as const, id: exactId(workspaceId) }
  if (policy.scope === 'self') return { type: 'self' as const, id: exactId(principal?.actorId) }
  if (policy.scope === 'brand') return { type: 'brand' as const, id: exactId(params.brand_id) }
  return { type: 'account' as const, id: exactId(params.account_id) }
}

async function resolveLoadedAuthorizationResourceScope(policy: NonNullable<ReturnType<typeof getMcpMethodPolicy>>, workspaceId: string, params: Record<string, unknown>, principal?: Pick<RequestPrincipal, 'actorId'>) {
  const direct = resolveAuthorizationResourceScope(policy, workspaceId, params, principal)
  if (direct?.id || (direct?.type !== 'brand' && direct?.type !== 'account')) return direct
  const taskId = typeof params.task_id === 'string' && params.task_id.trim() ? params.task_id.trim() : undefined
  const contentVersionId = typeof params.content_version_id === 'string' && params.content_version_id.trim() ? params.content_version_id.trim() : undefined
  const contentVersion = contentVersionId ? service.contentVersions.get(contentVersionId) : undefined
  const requestedTask = taskId ? service.tasks.get(taskId) : undefined
  const contentTask = contentVersion ? service.tasks.get(contentVersion.taskId) : undefined
  const requestedProductId = typeof params.product_id === 'string' && params.product_id.trim() ? params.product_id.trim() : undefined
  const requestedProduct = requestedProductId ? service.products.get(requestedProductId) : undefined
  if (taskId && (!requestedTask || requestedTask.workspaceId !== workspaceId)) return direct
  if (contentVersionId && (!contentVersion || !contentTask || contentTask.workspaceId !== workspaceId)) return direct
  if (requestedProductId && (!requestedProduct || requestedProduct.workspaceId !== workspaceId)) return direct
  if (requestedTask && contentTask && requestedTask.id !== contentTask.id) return direct
  const resourceTask = requestedTask ?? contentTask
  if (resourceTask && requestedProduct && resourceTask.productId !== requestedProduct.id) return direct
  const generationJobId = policy.method === 'generation.get' && typeof params.job_id === 'string' && params.job_id.trim() ? params.job_id.trim() : undefined
  const generationJob = generationJobId ? service.generationJobs.get(generationJobId) : undefined
  const publishJobId = policy.method === 'publish.get' && typeof params.publish_job_id === 'string' && params.publish_job_id.trim() ? params.publish_job_id.trim() : undefined
  const publishJob = publishJobId ? service.publishJobs.get(publishJobId) : undefined
  const task = requestedTask
    ? requestedTask
    : contentTask
      ? contentTask
      : generationJob?.workspaceId === workspaceId
        ? service.tasks.get(generationJob.taskId)
        : publishJob?.workspaceId === workspaceId
          ? service.tasks.get(publishJob.taskId)
        : undefined
  if (task?.workspaceId === workspaceId) {
    if (direct.type === 'brand') {
      const canonical = await (persistence.brandUnits ?? memoryBrandUnits).listCanonicalProducts({ workspaceId })
      const canonicalBrandIds = [...new Set(canonical.filter(item => item.sourceProductId === task.productId).map(item => item.brandId))]
      return { type: 'brand' as const, id: canonicalBrandIds.length === 1 && canonicalBrandIds[0] === task.brandId ? canonicalBrandIds[0] : undefined }
    }
    return { type: 'account' as const, id: task.accountId }
  }
  const productId = typeof params.product_id === 'string' && params.product_id.trim() ? params.product_id.trim() : undefined
  const imageJobId = typeof params.job_id === 'string' && params.job_id.trim() ? params.job_id.trim() : undefined
  const visualRef = typeof params.visual_ref === 'string' && params.visual_ref.trim() ? params.visual_ref.trim() : undefined
  const imageJob = imageJobId
    ? service.imageGenerationJobs.get(imageJobId)
    : visualRef
      ? [...service.imageGenerationJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.outputs?.some(output => output.visualRef === visualRef))
      : undefined
  const product = productId
    ? service.products.get(productId)
    : imageJob?.workspaceId === workspaceId
      ? service.products.get(imageJob.productId)
      : undefined
  if (!product || product.workspaceId !== workspaceId) return direct
  if (direct.type === 'account') return { type: 'account' as const, id: product.accountId }
  const canonical = await (persistence.brandUnits ?? memoryBrandUnits).listCanonicalProducts({ workspaceId })
  const brandIds = [...new Set(canonical.filter(item => item.sourceProductId === product.id).map(item => item.brandId))]
  return { type: 'brand' as const, id: brandIds.length === 1 ? brandIds[0] : undefined }
}

export function workspaceCapabilitySourceForBrandScope(policy: NonNullable<ReturnType<typeof getMcpMethodPolicy>>, workspaceId: string, atoms: readonly PermissionAtom[]) {
  return atoms.find(atom => atom.capability === policy.capability
    && atom.effect === 'allow'
    && atom.scope.type === 'workspace'
    && atom.scope.ids.includes(workspaceId)
    && atom.source !== 'temporary_grant')
}

export function minimumBrandRoleForPolicy(policy: NonNullable<ReturnType<typeof getMcpMethodPolicy>>): BrandAccessRole {
  if (policy.capability === 'customer.publish.execute') return 'publisher'
  return policy.effect === 'write' ? 'editor' : 'viewer'
}

async function permissionAtomsForResolvedResource(req: IncomingMessage, workspaceId: string, policy: NonNullable<ReturnType<typeof getMcpMethodPolicy>>, resourceScope: ReturnType<typeof resolveAuthorizationResourceScope>, atoms: readonly PermissionAtom[]) {
  if (resourceScope?.type !== 'brand' || !resourceScope.id) return atoms
  const principal = requestPrincipals.get(req)
  const capabilitySource = workspaceCapabilitySourceForBrandScope(policy, workspaceId, atoms)
  if (!principal?.actorId || !capabilitySource) return atoms
  const minimumRole = minimumBrandRoleForPolicy(policy)
  const granted = hasWorkspaceWideBrandAccess(req) || await (persistence.brandUnits ?? memoryBrandUnits).hasBrandAccess({ workspaceId, brandId: resourceScope.id, externalSubject: principal.actorId, minimumRole })
  if (!granted) return atoms
  return [...atoms, { capability: policy.capability, effect: 'allow' as const, scope: { type: 'brand' as const, ids: [resourceScope.id] }, source: 'resource_grant' as const, sourceId: `brand-access:${principal.actorId}:${resourceScope.id}`, obligations: capabilitySource.obligations, ...(capabilitySource.effectLimit ? { effectLimit: capabilitySource.effectLimit } : {}), ...(capabilitySource.expiresAt ? { expiresAt: capabilitySource.expiresAt } : {}), ...(capabilitySource.revision ? { revision: capabilitySource.revision } : {}) }]
}

export function authorizationDecisionAuditEvidence(decision: AuthorizationDecision, correlation: { requestId: string; traceId: string }) {
  return {
    decision_id: decision.decision_id,
    request_id: correlation.requestId,
    trace_id: correlation.traceId,
    policy_version: decision.policy_version,
    workbench: decision.workbench,
    capability: decision.capability,
    scope: decision.scope.required,
    ...(decision.scope.resource_id ? { resource_id: decision.scope.resource_id } : {}),
    resolved_scopes: decision.scope.resolved.map(scope => ({ type: scope.type, ids: [...scope.ids] })),
    result: decision.result,
    reason_code: decision.reason_code,
    explicit_deny: decision.explicit_deny,
    obligations: {
      required: [...decision.obligations.required],
      satisfied: [...decision.obligations.satisfied],
      missing: [...decision.obligations.missing],
    },
  }
}

export function authorizationDecisionRequiresAudit(decision: AuthorizationDecision) {
  return decision.enforced && (!decision.authorized || getMcpMethodPolicy(decision.method)?.audit === 'allow_and_deny')
}

export function authorizationDecisionAuditContextIsValid(decision: AuthorizationDecision, workspaceId: string, actorId: string | undefined) {
  return !authorizationDecisionRequiresAudit(decision) || Boolean(workspaceId && actorId)
}

async function recordAuthorizationDecision(req: IncomingMessage, workspaceId: string, decision: AuthorizationDecision) {
  const correlation = getRequestCorrelation(req)
  enrichRequestObservation(req, {
    authorizationDecisionId: decision.decision_id,
    authorizationPolicyVersion: decision.policy_version,
    authorizationMode: decision.mode,
    authorizationResult: decision.result,
    authorizationReason: decision.reason_code,
    authorizationCapability: decision.capability,
  })
  const actorId = requestPrincipals.get(req)?.actorId
  if (!authorizationDecisionRequiresAudit(decision)) return
  if (!authorizationDecisionAuditContextIsValid(decision, workspaceId, actorId)) {
    throw new DomainError('AUTHZ_AUDIT_CONTEXT_INVALID', '授权审计缺少工作区或操作者身份，操作已拒绝', 503, {
      decision_id: decision.decision_id,
      policy_version: decision.policy_version,
    })
  }
  try {
    await recordOperationAudit({
      workspaceId,
      actorId: actorId!,
      action: 'authz.decision',
      resourceType: 'mcp_method',
      resourceId: decision.method,
      before: {},
      after: authorizationDecisionAuditEvidence(decision, correlation),
      reason: decision.reason_code,
    })
  } catch (error) {
    // An enforced authorization decision without durable audit evidence is not
    // safe to continue: callers must see a stable retry/operational signal,
    // while the sink's internal error and customer data stay server-side.
    console.error(`AUTHZ_AUDIT_UNAVAILABLE decision=${decision.decision_id} method=${decision.method}`, error)
    throw new DomainError('AUTHZ_AUDIT_UNAVAILABLE', '授权审计暂不可用，操作已拒绝且未执行', 503, {
      decision_id: decision.decision_id,
      policy_version: decision.policy_version,
    })
  }
}

export function authorizationDenialDetails(decision: AuthorizationDecision) {
  return {
    decision_id: decision.decision_id,
    capability: decision.capability,
    reason_code: decision.reason_code,
    required_scope: decision.scope.required,
    workbench: decision.workbench,
    explicit_deny: decision.explicit_deny,
    obligations_missing: [...decision.obligations.missing],
    policy_version: decision.policy_version,
  }
}

export function authorizationGrantFailureDetails(decision: AuthorizationDecision, grantId?: string) {
  return {
    decision_id: decision.decision_id,
    capability: decision.capability,
    required_scope: decision.scope.required,
    workbench: decision.workbench,
    policy_version: decision.policy_version,
    ...(grantId ? { grant_id: grantId } : {}),
  }
}

export function authorizationPolicyUnavailableDetails(input: {
  transport: 'mcp' | 'http'
  method?: string
  operation?: string
}) {
  return {
    policy_version: AUTHZ_POLICY_VERSION,
    transport: input.transport,
    ...(input.method ? { method: input.method } : {}),
    ...(input.operation ? { operation: input.operation } : {}),
  }
}

async function enforceRegisteredMcpCapability(req: IncomingMessage, workspaceId: string, method: string, params: Record<string, unknown>) {
  const policy = getMcpMethodPolicy(method)
  if (!policy) throw new DomainError('AUTHZ_POLICY_UNAVAILABLE', '当前方法缺少服务端授权策略，已拒绝执行', 503, authorizationPolicyUnavailableDetails({ transport: 'mcp', method }))
  if (!requiresStrictAuth()) return policy
  const runtime = mcpAuthorizationRuntimeConfig()
  // A signed OIDC principal is intentionally allowed to create its first
  // workspace before membership exists. All subsequent methods require the
  // projected capability and active membership.
  const projection = effectiveAuthorizationProjection(requestPrincipals.get(req), workspaceId)
  const capabilityDomain = policy.capability.split('.')[0]!
  const enforce = runtime.mode === 'enforce' || alwaysEnforcedMcpMethods.has(method) || runtime.enforceDomains.has(capabilityDomain)
  const principal = requestPrincipals.get(req)
  const resourceScope = params.__http_authorization_scope_conflict === true
    ? { type: policy.scope, id: '__http_path_scope_conflict__' }
    : await resolveLoadedAuthorizationResourceScope(policy, workspaceId, params, principal)
  const decisionAtoms = await permissionAtomsForResolvedResource(req, workspaceId, policy, resourceScope, projection.atoms)
  const decision = registeredMcpAuthorizationDecision({
    decisionId: `authz_${randomUUID()}`,
    method,
    atoms: decisionAtoms,
    satisfiedObligations: satisfiedAuthorizationObligations(params, req),
    resourceScope,
    workbench: principal?.workbench ?? 'workspace',
    mode: enforce ? 'enforce' : 'shadow',
  })
  if (method === 'workspace.bootstrap' && !requestPrincipals.get(req)?.memberRole) {
    const bootstrapDecision = { ...decision, authorized: true, allowed: true, result: 'shadow_allow' as const, reason_code: 'AUTHZ_ALLOWED' as const }
    await recordAuthorizationDecision(req, workspaceId, bootstrapDecision)
    return policy
  }
  await recordAuthorizationDecision(req, workspaceId, decision)
  requestAuthorizationDecisions.set(req, decision)
  if (!decision.allowed) throw new DomainError(ERROR_CODES.FORBIDDEN, `当前身份授权决策拒绝 ${policy.capability}`, 403, authorizationDenialDetails(decision))
  if (decision.authorized && principal?.identityId) {
    const matches = (atom: PermissionAtom) => atom.capability === policy.capability
      && atom.effect === 'allow'
      && (policy.effect === 'read' || atom.effectLimit !== 'read')
      && atom.scope.type === resourceScope?.type
      && (atom.scope.type === 'platform' && atom.scope.ids.includes('*') || resourceScope?.id !== undefined && (atom.scope.ids.includes(resourceScope.id) || atom.scope.ids.includes('*')))
    const matchingAtoms = projection.atoms.filter(matches)
    const roleOrMembershipAuthorized = matchingAtoms.some(atom => atom.source !== 'temporary_grant')
    const grantAtom = roleOrMembershipAuthorized ? undefined : matchingAtoms.find(atom => atom.source === 'temporary_grant')
    if (grantAtom) {
      const grant = principal.activeAuthorizationGrants?.find(item => item.id === grantAtom.sourceId)
      const repository = authorizationRepository()
      if (!grant || !repository) throw new DomainError('AUTHORIZATION_GRANT_RECHECK_UNAVAILABLE', '临时授权无法在执行前复核，已拒绝操作', 503, authorizationGrantFailureDetails(decision, grant?.id))
      const consumed = await repository.consumeGrant({ id: grant.id, subjectIdentityId: principal.identityId, workspaceId, capability: policy.capability, scopeHash: grant.scopeHash, expectedRevision: grant.revision, actorId: principal.actorId, reason: `mcp:${method}` })
      if (!consumed) throw new DomainError('AUTHORIZATION_GRANT_REVOKED', '临时授权已撤销、过期、耗尽或版本变化，已拒绝操作', 403, authorizationGrantFailureDetails(decision, grant.id))
      principal.activeAuthorizationGrants = principal.activeAuthorizationGrants?.map(item => item.id === consumed.id ? consumed : item)
      principal.authorizationRevision = consumed.authorizationRevision
      requestConsumedAuthorizationGrants.set(req, { grant: consumed, decisionId: decision.decision_id, method, capability: policy.capability, workspaceId })
    }
  }
  return policy
}

export function registeredMcpAuthorizationDecision(input: {
  decisionId: string
  method: string
  atoms: readonly PermissionAtom[]
  satisfiedObligations?: readonly AuthorizationObligation[]
  resourceScope?: Parameters<typeof evaluatePermissionAtoms>[0]['resourceScope']
  workbench: OpsWorkbench
  mode: AuthorizationDecisionMode
  now?: string
}) {
  const policy = getMcpMethodPolicy(input.method)
  if (!policy) throw new DomainError('AUTHZ_POLICY_UNAVAILABLE', '当前方法缺少服务端授权策略，已拒绝执行', 503, authorizationPolicyUnavailableDetails({ transport: 'mcp', method: input.method }))
  return evaluatePermissionAtoms({
    decisionId: input.decisionId,
    policy,
    atoms: input.atoms,
    satisfiedObligations: input.satisfiedObligations,
    resourceScope: input.resourceScope,
    workbench: input.workbench,
    mode: input.mode,
    now: input.now,
  })
}

export function httpAuthorizationPathParams(pathTemplate: string, pathname: string, mcpMethod: string): Record<string, string> {
  const templateSegments = pathTemplate.split('/').filter(Boolean)
  const pathSegments = pathname.split('/').filter(Boolean)
  if (templateSegments.length !== pathSegments.length) return {}
  const params: Record<string, string> = {}
  for (let index = 0; index < templateSegments.length; index += 1) {
    const placeholder = /^\{([A-Za-z][A-Za-z0-9]*)\}$/u.exec(templateSegments[index]!)
    if (!placeholder) continue
    const name = placeholder[1]!
    const snakeName = name === 'jobId'
      ? (mcpMethod.startsWith('publish.') ? 'publish_job_id' : 'job_id')
      : name.replace(/([a-z0-9])([A-Z])/gu, '$1_$2').toLowerCase()
    params[snakeName] = decodeURIComponent(pathSegments[index]!)
  }
  return params
}

async function enforceRegisteredHttpCapability(req: IncomingMessage, url: URL, workspaceId: string): Promise<HttpOperationPolicy | undefined> {
  const httpPolicy = getHttpOperationPolicy(req.method, url.pathname)
  if (!httpPolicy || httpPolicy.authentication !== 'identity' || !httpPolicy.mcpMethod) return httpPolicy
  const mcpPolicy = getMcpMethodPolicy(httpPolicy.mcpMethod)
  if (!mcpPolicy) throw new DomainError('AUTHZ_POLICY_UNAVAILABLE', '当前 HTTP 操作引用的 MCP 授权策略不存在，已拒绝执行', 503, authorizationPolicyUnavailableDetails({ transport: 'http', method: httpPolicy.mcpMethod, operation: httpPolicy.operation }))
  if (mcpPolicy.scope !== 'platform' && workspaceId === 'unknown') throw new DomainError(ERROR_CODES.WORKSPACE_SCOPE_REQUIRED, '当前 HTTP 操作需要明确工作区', 400, { operation: httpPolicy.operation })
  const params: Record<string, unknown> = Object.fromEntries(url.searchParams.entries())
  let pathScopeConflict = false
  const hasJsonAuthorizationParams = httpPolicy.pathTemplate !== '/v1/assets/upload'
    && req.method !== 'GET'
    && (header(req, 'content-type') ?? '').toLowerCase().includes('application/json')
  if (hasJsonAuthorizationParams) Object.assign(params, await body(req))
  const pathParams = httpAuthorizationPathParams(httpPolicy.pathTemplate, url.pathname, httpPolicy.mcpMethod)
  for (const [name, value] of Object.entries(pathParams)) {
    if (typeof params[name] === 'string' && params[name]!.trim() && params[name]!.trim() !== value) {
      // Keep the path authoritative, but let the shared authorization engine
      // produce the denial so HTTP and MCP have the same decision/audit
      // evidence. The marker is internal and never reaches a route handler.
      pathScopeConflict = true
    }
    params[name] = value
  }
  // HTTP routes do not carry the MCP envelope's workspace_id. Bind the
  // already-resolved request scope before evaluating the shared policy so a
  // missing body/query field cannot silently widen or bypass tenant scope.
  params.workspace_id = workspaceId
  if (pathScopeConflict) params.__http_authorization_scope_conflict = true
  await enforceRegisteredMcpCapability(req, workspaceId, httpPolicy.mcpMethod, params)
  return httpPolicy
}

function mcpPolicyMatchesWorkbench(policy: NonNullable<ReturnType<typeof getMcpMethodPolicy>>, workbench: OpsWorkbench) {
  return policy.scope === 'self'
    || (workbench === 'platform' && policy.scope === 'platform')
    || (workbench === 'workspace' && policy.scope !== 'platform')
}

async function enforceMcpWorkbenchBoundary(req: IncomingMessage, workspaceId: string, method: string, params: Record<string, unknown>) {
  if (!requiresStrictAuth()) return
  const policy = getMcpMethodPolicy(method)
  if (!policy || mcpPolicyMatchesWorkbench(policy, requestPrincipals.get(req)?.workbench ?? 'workspace')) return
  // Resolve the shared authorization decision before any workspace membership,
  // lifecycle, hydration, or handler work. Workbench isolation is an enforced
  // trust boundary even while the capability policy is running in shadow mode.
  await enforceRegisteredMcpCapability(req, workspaceId, method, params)
}

function requestActor(req: IncomingMessage, fallback = 'merchant') {
  const principal = requestPrincipals.get(req)
  return (requiresStrictAuth() ? principal?.actorId : undefined) || header(req, 'x-actor-id')?.trim() || fallback
}

function safeEqual(leftValue: string, rightValue: string) {
  const left = Buffer.from(leftValue)
  const right = Buffer.from(rightValue)
  return left.length === right.length && timingSafeEqual(left, right)
}

function authenticatedSessionHash(rawSessionId: string) {
  const secret = process.env.SESSION_ID_HASH_SECRET?.trim()
  if (!secret) throw new DomainError('SESSION_HASH_SECRET_MISSING', '严格认证环境必须配置独立的会话指纹密钥', 503)
  return createHmac('sha256', secret).update(rawSessionId).digest('hex')
}

async function observeAuthenticatedPrincipal(req: IncomingMessage, principal: RequestPrincipal) {
  if (!requiresStrictAuth() || !principal.issuer || !principal.sessionSubject || !principal.sessionKind || !principal.sessionIssuedAt) return
  assertProductionAuthorizationRuntime()
  await persistenceReady
  const identityRepository = persistence.identities ?? memoryIdentities
  const sessionHash = authenticatedSessionHash(principal.sessionSubject)
  const flightKey = `${principal.issuer}\u0000${principal.actorId}\u0000${principal.sessionKind}\u0000${sessionHash}`
  try {
    const inFlightObservation = authenticatedObservationFlights.get(flightKey)
    const observation = inFlightObservation ?? identityRepository.observeAuthenticatedSession({
        issuer: principal.issuer,
        externalSubject: principal.actorId,
        sessionHash,
        kind: principal.sessionKind,
        issuedAt: principal.sessionIssuedAt,
        ...(principal.sessionExpiresAt ? { expiresAt: principal.sessionExpiresAt } : {}),
        mfaVerified: principal.mfaVerified === true,
        userAgentHash: header(req, 'user-agent') ? authenticatedSessionHash(`ua:${header(req, 'user-agent')}`) : undefined,
      })
    if (!inFlightObservation) {
      authenticatedObservationFlights.set(flightKey, observation)
      void observation.then(() => {
        if (authenticatedObservationFlights.get(flightKey) === observation) authenticatedObservationFlights.delete(flightKey)
      }, () => {
        if (authenticatedObservationFlights.get(flightKey) === observation) authenticatedObservationFlights.delete(flightKey)
      })
    }
    const snapshot = await observation
    principal.identityId = snapshot.identity.id
    principal.sessionId = snapshot.session.id
    principal.identityStatus = snapshot.identity.accessStatus
    principal.riskDecision = snapshot.identity.riskDecision
    if (!snapshot.allowed) throw new DomainError(snapshot.denialReason ?? 'IDENTITY_ACCESS_DENIED', '平台身份、风险或会话状态已拒绝本次访问', 403)
    const authzRepository = authorizationRepository()
    if (!authzRepository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '持久授权仓储未配置，已拒绝建立受控会话', 503)
    principal.authorizationRevision = await authzRepository.getAuthorizationRevision(snapshot.identity.id)
    const requestedWorkspace = header(req, 'x-workspace-id')?.trim()
    if (principal.workbench === 'workspace' && requestedWorkspace) {
      principal.activeAuthorizationGrants = await authzRepository.listActiveGrants(snapshot.identity.id, requestedWorkspace)
      if (principal.activeAuthorizationGrants.length) principal.availableWorkbenches = [...new Set<OpsWorkbench>([...(principal.availableWorkbenches ?? []), 'workspace'])]
    }
    if (principal.workbench === 'platform' && process.env.AUTHZ_DURABLE_ASSIGNMENTS_REQUIRED === 'true') {
      const assignments = await authzRepository.listActivePlatformRoles(snapshot.identity.id)
      if (assignments.some(assignment => assignment.role === 'platform_owner')) throw new DomainError('AUTHZ_PLATFORM_ROLE_UNSUPPORTED', 'platform_owner 尚未进入 canonical role contract，已拒绝授权', 503)
      principal.platformRoleAssignments = assignments
      principal.roles = assignments.map(assignment => assignment.role)
      principal.availableWorkbenches = principal.roles.length ? ['platform' as OpsWorkbench] : []
    }
  } catch (error) {
    if (error instanceof DomainError) throw error
    if (error instanceof IdentityLifecycleError) throw new DomainError(error.code, '平台身份或会话状态校验失败', 403)
    throw error
  }
}

function mapIdentityLifecycleError(error: unknown): never {
  if (error instanceof DomainError) throw error
  if (error instanceof IdentityLifecycleError) {
    const status = error.code.endsWith('_NOT_FOUND') ? 404 : error.code.includes('REVISION') || error.code.includes('ALREADY') || error.code.includes('IDEMPOTENCY') ? 409 : 400
    throw new DomainError(error.code, '平台身份生命周期操作失败', status)
  }
  throw error
}

async function consumeOidcNonce(nonce: string): Promise<boolean> {
  if (redisOidcNonce) return redisOidcNonce.consume(nonce, 120)
  if (!process.env.VITEST) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, 'OIDC 网关一次性 nonce 存储不可用', 503)
  const now = Date.now()
  for (const [key, expiresAt] of testOidcNonces) if (expiresAt <= now) testOidcNonces.delete(key)
  if (testOidcNonces.has(nonce)) return false
  testOidcNonces.set(nonce, now + 120_000)
  return true
}

async function consumeAssetScannerNonce(nonce: string): Promise<boolean> {
  if (redisAssetScannerNonce) return redisAssetScannerNonce.consume(nonce, 120)
  if (!process.env.VITEST) throw new DomainError('ASSET_SCANNER_NONCE_STORE_UNAVAILABLE', 'asset scanner nonce store is unavailable', 503)
  const now = Date.now()
  for (const [key, expiresAt] of testAssetScannerNonces) if (expiresAt <= now) testAssetScannerNonces.delete(key)
  if (testAssetScannerNonces.has(nonce)) return false
  testAssetScannerNonces.set(nonce, now + 120_000)
  return true
}

async function consumeWorkerNonce(nonce: string): Promise<boolean> {
  if (redisWorkerNonce) return redisWorkerNonce.consume(nonce, 120)
  if (!process.env.VITEST) throw new DomainError('WORKER_NONCE_STORE_UNAVAILABLE', 'worker request nonce store is unavailable', 503)
  const now = Date.now()
  for (const [key, expiresAt] of testWorkerNonces) if (expiresAt <= now) testWorkerNonces.delete(key)
  if (testWorkerNonces.has(nonce)) return false
  testWorkerNonces.set(nonce, now + 120_000)
  return true
}

async function authenticateOidcGateway(req: IncomingMessage): Promise<RequestPrincipal> {
  const secret = process.env.OIDC_PROXY_SIGNING_SECRET?.trim()
  if (!secret) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, 'OIDC 网关签名密钥未配置', 503)
  const subject = header(req, 'x-oidc-sub')?.trim() ?? ''
  const issuer = header(req, 'x-oidc-issuer')?.trim() ?? ''
  const sessionId = header(req, 'x-oidc-sid')?.trim() ?? ''
  const workspaceId = header(req, 'x-oidc-workspace')?.trim() ?? ''
  const workbench = header(req, 'x-oidc-workbench')?.trim() ?? ''
  const bootstrapRequested = header(req, 'x-workspace-bootstrap') === 'true'
  const roles = (header(req, 'x-oidc-roles') ?? '').split(',').map(value => value.trim()).filter(Boolean).sort()
  const amr = (header(req, 'x-oidc-amr') ?? '').split(',').map(value => value.trim()).filter(Boolean).sort()
  const authTime = header(req, 'x-oidc-auth-time')?.trim() ?? ''
  const sessionExpiresAt = header(req, 'x-oidc-session-expires-at')?.trim() ?? ''
  const timestamp = header(req, 'x-oidc-timestamp')?.trim() ?? ''
  const nonce = header(req, 'x-oidc-nonce')?.trim() ?? ''
  const bodyDigest = header(req, 'x-oidc-body-sha256')?.trim().toLowerCase() ?? ''
  const signature = header(req, 'x-oidc-signature')?.trim() ?? ''
  const timestampSeconds = Number(timestamp)
  const nowSeconds = Math.floor(Date.now() / 1000)
  const authTimeSeconds = Number(authTime)
  const expiresAtSeconds = Number(sessionExpiresAt)
  if (!issuer || !subject || !sessionId || (workbench !== 'platform' && workbench !== 'workspace') || (workbench === 'workspace' && !workspaceId && !bootstrapRequested) || !/^\d{10}$/.test(timestamp) || !/^\d{10}$/.test(authTime) || !/^\d{10}$/.test(sessionExpiresAt) || !Number.isSafeInteger(timestampSeconds) || !Number.isSafeInteger(authTimeSeconds) || !Number.isSafeInteger(expiresAtSeconds) || authTimeSeconds > nowSeconds + 60 || expiresAtSeconds <= nowSeconds || Math.abs(nowSeconds - timestampSeconds) > 60 || !/^[A-Za-z0-9_-]{16,128}$/u.test(nonce) || !/^[a-f0-9]{64}$/u.test(bodyDigest) || !/^[a-f0-9]{64}$/i.test(signature)) throw new DomainError('AUTHZ_WORKBENCH_ASSERTION_INVALID', 'OIDC 网关身份断言缺少有效且受签名保护的工作台', 401)
  // Sign the exact request-target, including the raw query string. The gateway
  // and API must agree on bytes before any URL decoding or parameter sorting.
  const requestTarget = req.url ?? '/'
  const canonical = [req.method ?? 'GET', requestTarget, workspaceId, workbench, issuer, subject, sessionId, roles.join(','), amr.join(','), authTime, sessionExpiresAt, timestamp, bodyDigest, nonce].join('\n')
  const expected = createHmac('sha256', secret).update(canonical).digest('hex')
  if (!safeEqual(expected, signature)) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, 'OIDC 网关身份签名无效', 401)
  const hasPlatformRole = roles.some(role => {
    const canonicalRole = canonicalizeRole(role, 'gateway')
    return canonicalRole !== undefined && platformCanonicalRoles.has(canonicalRole)
  })
  if (workbench === 'platform' && !hasPlatformRole) throw new DomainError('AUTHZ_WORKBENCH_FORBIDDEN', '当前 OIDC 平台身份没有平台工作台角色', 403)
  const requested = header(req, 'x-workspace-id')?.trim()
  if (workbench === 'workspace' && !requested && !bootstrapRequested) throw new DomainError(ERROR_CODES.WORKSPACE_SCOPE_REQUIRED, 'workspace 工作台必须携带 X-Workspace-Id', 401)
  if (requested && requested !== workspaceId) throw new DomainError(ERROR_CODES.FORBIDDEN, '请求工作区与 OIDC 会话工作区不一致', 403)
  const configuredMax = Number(process.env.OIDC_PROXY_MAX_BODY_BYTES ?? 50 * 1024 * 1024)
  const maxBodyBytes = Number.isSafeInteger(configuredMax) && configuredMax > 0 ? configuredMax : 50 * 1024 * 1024
  const actualBodyDigest = createHash('sha256').update(await requestBodyBytes(req, maxBodyBytes)).digest('hex')
  if (!safeEqual(actualBodyDigest, bodyDigest)) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, 'OIDC 网关请求体摘要不一致', 401)
  if (!await consumeOidcNonce(nonce)) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, 'OIDC 网关身份断言已使用', 401)
  return { actorId: subject, workspaces: workspaceId ? [workspaceId] : [], roles, workbench: workbench as OpsWorkbench, availableWorkbenches: hasPlatformRole ? ['platform'] : [], issuer, sessionSubject: sessionId, sessionKind: 'oidc', sessionIssuedAt: new Date(authTimeSeconds * 1000).toISOString(), sessionExpiresAt: new Date(expiresAtSeconds * 1000).toISOString(), mfaVerified: amr.includes('mfa') }
}

/** Production identity boundary: opaque bearer token -> permitted workspaces. */
async function authenticate(req: IncomingMessage) {
  if (!requiresStrictAuth()) {
    const requestedWorkbench = header(req, 'x-ops-workbench')?.trim()
    if (requestedWorkbench && requestedWorkbench !== 'platform' && requestedWorkbench !== 'workspace') throw new DomainError('AUTHZ_WORKBENCH_ASSERTION_INVALID', '工作台只能是 platform 或 workspace', 400)
    const roles = (header(req, 'x-role') ?? '').split(',').map(value => value.trim()).filter(Boolean)
    const workbench: OpsWorkbench = requestedWorkbench === 'platform' ? 'platform' : 'workspace'
    const localMemberRole = workbench === 'workspace' ? roles.find(role => workspaceMemberRoles.has(role as MemberRole)) as MemberRole | undefined : undefined
    requestPrincipals.set(req, {
      actorId: requestActor(req, 'actor_demo'),
      workspaces: [header(req, 'x-workspace-id')?.trim() || 'ws_demo'],
      roles,
      workbench,
      availableWorkbenches: requestedWorkbench === 'platform' ? ['platform', 'workspace'] : ['workspace'],
      ...(localMemberRole ? { memberRole: localMemberRole, memberStatus: 'active' as const } : {}),
    })
    return
  }
  // The merchant UI/plugin and the operations console share this API service
  // in the Kubernetes baseline, but intentionally have different identity
  // boundaries. Only the explicitly configured merchant host may use the
  // bearer-token branch; every other production host remains OIDC-only.
  const merchantBearerHostname = process.env.MERCHANT_BEARER_HOSTNAME?.trim().toLowerCase()
  const requestHostname = (header(req, 'host')?.trim().toLowerCase().split(':')[0] ?? '')
  const merchantBearerRequest = Boolean(merchantBearerHostname && requestHostname === merchantBearerHostname)
  if (process.env.OPS_AUTH_MODE === 'oidc' && !merchantBearerRequest) {
    const principal = await authenticateOidcGateway(req)
    requestPrincipals.set(req, principal)
    await observeAuthenticatedPrincipal(req, principal)
    const claimedActor = header(req, 'x-actor-id')?.trim()
    if (claimedActor && claimedActor !== principal.actorId) throw new DomainError(ERROR_CODES.FORBIDDEN, 'X-Actor-Id 与认证身份不一致', 403)
    return
  }
  const authorization = header(req, 'authorization')?.trim()
  const token = authorization?.match(/^Bearer\s+([^\s]+)$/i)?.[1]
  if (!token) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '生产请求必须携带有效 Bearer token', 401)
  let grants: Record<string, unknown>
  try {
    const parsed: unknown = JSON.parse(process.env.API_AUTH_TOKENS ?? '{}')
    if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('invalid grants')
    grants = parsed as Record<string, unknown>
  } catch {
    throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '服务端认证映射未正确配置', 401)
  }
  const matched = Object.keys(grants).find(known => safeEqual(known, token))
  const permitted = matched ? grants[matched] : undefined
  const grant = permitted && typeof permitted === 'object' && !Array.isArray(permitted) ? permitted as Record<string, unknown> : undefined
  const workspaceValues = Array.isArray(permitted) ? permitted : grant?.workspaces
  const workspaces = Array.isArray(workspaceValues) ? workspaceValues.filter((value): value is string => typeof value === 'string') : []
  const roles = Array.isArray(grant?.roles) ? grant.roles.filter((value): value is string => typeof value === 'string') : []
  const rawDeniedCapabilities = Array.isArray(grant?.denied_capabilities) ? grant.denied_capabilities.filter((value): value is string => typeof value === 'string') : []
  const unknownDeniedCapabilities = rawDeniedCapabilities.filter(value => !CAPABILITIES.includes(value as CapabilityId))
  if (unknownDeniedCapabilities.length) throw new DomainError('AUTHZ_EXPLICIT_DENY_INVALID', '服务端 token grant 包含未知 capability deny，已拒绝认证', 503)
  const explicitDeniedCapabilities = [...new Set(rawDeniedCapabilities)] as CapabilityId[]
  const configuredWorkbenches = Array.isArray(grant?.workbenches) ? grant.workbenches.filter((value): value is string => typeof value === 'string') : []
  const invalidWorkbenches = configuredWorkbenches.filter(value => value !== 'platform' && value !== 'workspace')
  if (invalidWorkbenches.length) throw new DomainError('AUTHZ_WORKBENCH_GRANT_INVALID', '服务端 token grant 包含未知 workbench，已拒绝认证', 503)
  const availableWorkbenches = [...new Set((configuredWorkbenches.length ? configuredWorkbenches : ['workspace']) as OpsWorkbench[])]
  const requestedWorkbench = header(req, 'x-ops-workbench')?.trim()
  if (requestedWorkbench && requestedWorkbench !== 'platform' && requestedWorkbench !== 'workspace') throw new DomainError('AUTHZ_WORKBENCH_ASSERTION_INVALID', '工作台只能是 platform 或 workspace', 400)
  const workbench: OpsWorkbench = merchantBearerRequest ? 'workspace'
    : requestedWorkbench === 'platform' ? 'platform'
      : requestedWorkbench === 'workspace' ? 'workspace'
        : availableWorkbenches.length === 1 ? availableWorkbenches[0]!
          : 'workspace'
  if (merchantBearerRequest && requestedWorkbench === 'platform') throw new DomainError('AUTHZ_WORKBENCH_FORBIDDEN', '商家 Bearer 边界固定为 workspace 工作台', 403)
  if (!availableWorkbenches.includes(workbench)) throw new DomainError('AUTHZ_WORKBENCH_FORBIDDEN', 'Bearer token grant 未授权所选工作台', 403)
  const actorId = typeof grant?.actor_id === 'string' && grant.actor_id.trim() ? grant.actor_id.trim() : ''
  const requested = header(req, 'x-workspace-id')?.trim()
  const bootstrapRequested = header(req, 'x-workspace-bootstrap') === 'true'
  const wildcardAllowed = process.env.NODE_ENV !== 'production' && process.env.ALLOW_WILDCARD_WORKSPACE_GRANT === 'true'
  const hasPlatformRole = roles.some(role => {
    const canonicalRole = canonicalizeRole(role, 'gateway')
    return canonicalRole !== undefined && platformCanonicalRoles.has(canonicalRole)
  })
  if (workbench === 'platform' && !hasPlatformRole) throw new DomainError('AUTHZ_WORKBENCH_FORBIDDEN', 'Bearer token 没有平台工作台角色', 403)
  if (!matched || (workbench === 'workspace' && !requested && !bootstrapRequested) || (!requested && bootstrapRequested && grant?.bootstrap !== true) || (requested && (workspaces.includes('*') ? !wildcardAllowed : !workspaces.includes(requested)))) throw new DomainError(ERROR_CODES.FORBIDDEN, 'Bearer token 无权访问该工作区或创建新工作区', 403)
  const principal: RequestPrincipal = { actorId, workspaces, roles, workbench, availableWorkbenches: merchantBearerRequest ? ['workspace'] : availableWorkbenches, explicitDeniedCapabilities, issuer: typeof grant?.issuer === 'string' && grant.issuer.trim() ? grant.issuer.trim() : 'urn:merchant:api-token', sessionSubject: token, sessionKind: 'api_token', sessionIssuedAt: new Date().toISOString(), mfaVerified: false }
  requestPrincipals.set(req, principal)
  await observeAuthenticatedPrincipal(req, principal)
  const claimedActor = header(req, 'x-actor-id')?.trim()
  if (claimedActor && claimedActor !== principal.actorId) throw new DomainError(ERROR_CODES.FORBIDDEN, 'X-Actor-Id 与认证身份不一致', 403)
}

function requireRuleAdmin(req: IncomingMessage): RequestPrincipal {
  const principal = requestPrincipals.get(req)
  const roles = authorizedRoles(principal)
  if (!principal || !roles.includes('rules_admin') || !principal.actorId) throw new DomainError(ERROR_CODES.FORBIDDEN, '规则中心写操作需要绑定 actor_id 的 rules_admin 权限', 403)
  const claimedActor = header(req, 'x-actor-id')?.trim()
  if (requiresStrictAuth() && claimedActor && claimedActor !== principal.actorId) throw new DomainError(ERROR_CODES.FORBIDDEN, 'X-Actor-Id 与认证身份不一致', 403)
  return principal
}

function requireOperationsRole(req: IncomingMessage, allowed: readonly string[]) {
  const principal = requestPrincipals.get(req)
  const roles = authorizedRoles(principal)
  const canonicalRoles = principal?.workbench === 'workspace'
    ? resolveCanonicalRoles({ gatewayRoles: roles.filter(role => role !== principal.memberRole), memberRole: principal.memberRole })
    : resolveCanonicalRoles({ gatewayRoles: roles })
  const allowedCanonical = new Set(allowed.flatMap(role => [canonicalizeRole(role, 'gateway'), canonicalizeRole(role, 'membership')]).filter((role): role is CanonicalRole => role !== undefined))
  if (requiresStrictAuth() && (!principal?.actorId || (!roles.some(role => allowed.includes(role)) && !canonicalRoles.some(role => allowedCanonical.has(role))))) throw new DomainError(ERROR_CODES.FORBIDDEN, '该运营操作需要对应的工作区或平台运营权限', 403)
  return principal?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'actor_demo'
}

type BillingReadScope = 'mine' | 'workspace'

function billingReadScope(req: IncomingMessage, params: Record<string, unknown>, defaultPrivilegedToWorkspace = false): { scope: BillingReadScope; actorId: string } {
  const principal = requestPrincipals.get(req)
  const privileged = authorizedRoles(principal).some(role => ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'].includes(role))
  const legacyPersonalBillingMode = process.env.AUTH_ENFORCEMENT === 'strict'
  const requested = params.scope === 'workspace' || (params.scope === undefined && defaultPrivilegedToWorkspace && !legacyPersonalBillingMode && privileged) ? 'workspace' : 'mine'
  const actorId = requestActor(req, 'actor_demo')
  if (requested === 'workspace') requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
  return { scope: requested, actorId }
}

/** Customer product/content data is never a platform-wide operations surface. */
function requireWorkspaceDataRole(req: IncomingMessage) {
  const principal = requestPrincipals.get(req)
  const workspaceId = header(req, 'x-workspace-id')?.trim() ?? ''
  if (requiresStrictAuth() && principal?.actorId && exactConsumedGrantForRequest(req, workspaceId)) {
    return principal.actorId
  }
  if (localComposeOpsCustomerDataAccess()) {
    return requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops'])
  }
  return requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support'])
}

const CUSTOMER_DATA_METHOD_PREFIXES = [
  'brand.', 'brand-unit.', 'catalog.', 'asset.', 'content.', 'deliverable.', 'feedback.', 'knowledge.',
  'sync.', 'task.', 'publish.', 'multimodal.',
] as const
const CUSTOMER_DATA_EXACT_METHODS = new Set([
  'platform.store.list',
  'platform.connect',
  'platform.sync',
  'platform.revoke',
  'canonical.product.consistency',
  'ops.audit.detail',
  'ops.support.tickets.list',
  'ops.support.ticket.get',
  'ops.support.ticket.create',
  'ops.support.ticket.assign',
  'ops.support.ticket.transition',
  'ops.support.ticket.comment',
  'ops.support.sla.report',
  'ops.support.sla.correction.create',
  'ops.support.sla.correction.decide',
  'ops.support.crm.export',
])
const CUSTOMER_DATA_READ_METHODS = new Set(['brand.get', 'brand.list', 'brand-unit.list', 'brand-unit.listing.list', 'catalog.search', 'catalog.categories', 'catalog.image.get', 'catalog.sync.get', 'deliverable.list', 'asset.list', 'asset.get', 'content.get', 'content.list', 'feedback.list', 'knowledge.rule.list', 'knowledge.asset.list', 'knowledge.learning.list', 'knowledge.competitor.list', 'task.history', 'task.timeline', 'publish.get', 'ops.audit.detail'])

type CustomerDataAccessGrant = {
  grantId: string
  actorId: string
  workspaceId: string
  scopes: readonly string[]
  issuedAt: number
  expiresAt: number
}

function isCustomerDataMethod(method: string) {
  return CUSTOMER_DATA_EXACT_METHODS.has(method) || CUSTOMER_DATA_METHOD_PREFIXES.some(prefix => method === prefix || method.startsWith(prefix))
}

function isCustomerDataWriteMethod(method: string) {
  return !CUSTOMER_DATA_READ_METHODS.has(method)
}

/** Map HTTP customer surfaces to the same temporary-grant boundary as MCP.
 * Platform operations must not get a second, weaker authorization path merely
 * by switching transports.  The method name is intentionally coarse here;
 * resource-level workspace/brand checks remain enforced by each route. */
export function customerDataMethodForHttp(method: string | undefined, path: string): string | undefined {
  if (!['GET', 'POST', 'PUT', 'PATCH', 'DELETE'].includes(method ?? '')) return undefined
  if (method === 'GET' && path === '/v1/platform-accounts') return 'platform.store.list'
  if (method === 'POST' && /^\/v1\/platform-accounts\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)\/authorize$/u.test(path)) return 'platform.connect'
  if (method === 'POST' && /^\/v1\/platform-accounts\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)\/sync$/u.test(path)) return 'platform.sync'
  if (method === 'DELETE' && /^\/v1\/platform-accounts\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)$/u.test(path)) return 'platform.revoke'
  const customerPath = /^\/v1\/(?:products|assets|tasks|content-versions|content|brand-profile|knowledge|sync-jobs|generation-jobs|image-generation-jobs|publish-jobs|delivery-readiness|canonical)(?:\/|$)/u.test(path)
  if (!customerPath) return undefined
  return method === 'GET' ? 'catalog.search' : 'catalog.product.update'
}

// Local Compose is an explicitly isolated pilot surface. It uses a fixed
// local Bearer token and wildcard workspace grant, so the Ops console can
// inspect the seeded workspace without pretending that a production gateway
// issued a customer-data grant. Production never enables this escape hatch.
function localComposeOpsCustomerDataAccess() {
  return process.env.NODE_ENV !== 'production'
    && process.env.LOCAL_COMPOSE === 'true'
    && process.env.ALLOW_WILDCARD_WORKSPACE_GRANT === 'true'
}

function base64UrlDecode(value: string) {
  return Buffer.from(value.replaceAll('-', '+').replaceAll('_', '/') + '='.repeat((4 - value.length % 4) % 4), 'base64').toString('utf8')
}

/** Validate a gateway-issued, short-lived customer-data access grant. */
export function validateCustomerDataAccessGrant(raw: string | undefined, expected: { actorId: string; workspaceId: string; method: string; nowSeconds?: number }, secret = process.env.OPS_CUSTOMER_ACCESS_SIGNING_SECRET?.trim()): CustomerDataAccessGrant {
  const parts = raw?.trim().split('.') ?? []
  if (parts.length !== 3 || parts[0] !== 'v1') throw new DomainError('OPS_CUSTOMER_ACCESS_REQUIRED', 'platform_ops 访问客户营销数据必须携带有效的限时授权票据', 403)
  if (!secret) throw new DomainError('OPS_CUSTOMER_ACCESS_GRANT_UNAVAILABLE', '客户数据临时授权未配置签名密钥', 503)
  const payloadPart = parts[1]!
  const signature = createHmac('sha256', secret).update(`v1.${payloadPart}`).digest('base64url')
  if (!safeEqual(signature, parts[2]!)) throw new DomainError('OPS_CUSTOMER_ACCESS_INVALID', '客户数据临时授权票据签名无效', 403)
  let payload: Record<string, unknown>
  try {
    const parsed: unknown = JSON.parse(base64UrlDecode(payloadPart))
    if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('invalid payload')
    payload = parsed as Record<string, unknown>
  } catch {
    throw new DomainError('OPS_CUSTOMER_ACCESS_INVALID', '客户数据临时授权票据内容无效', 403)
  }
  const grantId = typeof payload.grant_id === 'string' ? payload.grant_id.trim() : ''
  const actorId = typeof payload.actor_id === 'string' ? payload.actor_id.trim() : ''
  const workspaceId = typeof payload.workspace_id === 'string' ? payload.workspace_id.trim() : ''
  const scopes = Array.isArray(payload.scopes) ? payload.scopes.filter((value): value is string => typeof value === 'string').map(value => value.trim()).filter(Boolean) : []
  const issuedAt = typeof payload.issued_at === 'number' ? payload.issued_at : Number(payload.issued_at)
  const expiresAt = typeof payload.expires_at === 'number' ? payload.expires_at : Number(payload.expires_at)
  const nowSeconds = expected.nowSeconds ?? Math.floor(Date.now() / 1000)
  const requiredScope = isCustomerDataWriteMethod(expected.method) ? 'customer_data.write' : 'customer_data.read'
  if (!/^[A-Za-z0-9_-]{8,128}$/u.test(grantId) || actorId !== expected.actorId || workspaceId !== expected.workspaceId || !Number.isSafeInteger(issuedAt) || !Number.isSafeInteger(expiresAt) || issuedAt > nowSeconds + 60 || expiresAt <= nowSeconds || expiresAt - issuedAt > 15 * 60 || (!scopes.includes(requiredScope) && !scopes.includes('customer_data.admin'))) {
    throw new DomainError('OPS_CUSTOMER_ACCESS_INVALID', '客户数据临时授权票据已过期、越权或范围不匹配', 403)
  }
  return { grantId, actorId, workspaceId, scopes, issuedAt, expiresAt }
}

async function enforceCustomerDataAccess(req: IncomingMessage, workspaceId: string, method: string) {
  if (!requiresStrictAuth() || !isPlatformOperations(req) || !isCustomerDataMethod(method) || localComposeOpsCustomerDataAccess()) return
  const principal = requestPrincipals.get(req)
  const grant = validateCustomerDataAccessGrant(header(req, 'x-ops-customer-access-grant'), { actorId: principal?.actorId ?? '', workspaceId, method })
  await recordOperationAudit({ workspaceId, actorId: grant.actorId, action: 'ops.customer_data.access', resourceType: 'workspace', resourceId: workspaceId, before: {}, after: { grant_id: grant.grantId, scope: isCustomerDataWriteMethod(method) ? 'customer_data.write' : 'customer_data.read', method, expires_at: new Date(grant.expiresAt * 1000).toISOString() }, reason: '平台运营临时客户数据授权访问' })
}

function isPlatformOperations(req: IncomingMessage) {
  const principal = requestPrincipals.get(req)
  const roles = authorizedRoles(principal)
  return principal?.workbench === 'platform' && roles.includes('platform_ops')
}

function canViewRuleLifecycle(req: IncomingMessage) {
  const roles = authorizedRoles(requestPrincipals.get(req))
  return roles.includes('rules_admin') || roles.includes('platform_ops')
}

function scopeCommercialRolloutTarget(req: IncomingMessage, currentWorkspaceId: string, targetWorkspaceId?: string) {
  const target = targetWorkspaceId?.trim() || undefined
  if (isPlatformOperations(req)) return target
  if (!target || target !== currentWorkspaceId) throw new DomainError(ERROR_CODES.FORBIDDEN, '普通工作区运营角色只能管理当前工作区灰度配置', 403)
  return target
}

async function resolveActiveWorkspaceMember(req: IncomingMessage, workspaceId: string, required: boolean) {
  if (!requiresStrictAuth() || requestMemberChecks.has(req)) return true
  const principal = requestPrincipals.get(req)
  if (!principal?.actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '生产工作区访问必须绑定可识别的成员身份', 401)
  if (!workspaceId) throw new DomainError(ERROR_CODES.FORBIDDEN, '生产工作区访问缺少工作区范围', 403)
  const member = (await (persistence.members ?? memoryMembers).list(workspaceId)).find(item => item.externalSubject === principal.actorId)
  if (!member) {
    if (required) throw new DomainError('WORKSPACE_MEMBERSHIP_REQUIRED', '当前身份不是该工作区的有效成员，请由工作区所有者邀请后重试', 403, { workspace_id: workspaceId })
    return false
  }
  principal.memberRole = member.role
  principal.memberStatus = member.status
  if (member.status === 'suspended') throw new DomainError('MEMBER_SUSPENDED', '该运营成员已被暂停，当前工作区访问已撤销', 403)
  if (member.status !== 'active') throw new DomainError('MEMBER_NOT_ACTIVE', '该运营成员尚未激活，当前工作区访问未开放', 403)
  if (principal.identityId && member.identityId !== principal.identityId) {
    try { await (persistence.members ?? memoryMembers).bindIdentity({ workspaceId, externalSubject: principal.actorId, identityId: principal.identityId }) }
    catch (error) { if (String(error).includes('MEMBER_IDENTITY_CONFLICT')) throw new DomainError('MEMBER_IDENTITY_CONFLICT', '成员关系已绑定到其他平台身份，访问已拒绝', 403); throw error }
  }
  const gatewayMemberRoles = principal.roles.filter(role => workspaceMemberRoles.has(role as MemberRole))
  if (gatewayMemberRoles.length > 0 && !gatewayMemberRoles.includes(member.role)) throw new DomainError('MEMBER_ROLE_MISMATCH', '身份网关角色与工作区成员角色不一致，访问已拒绝', 403, { member_role: member.role })
  requestMemberChecks.add(req)
  return true
}

async function enforceActiveWorkspaceMember(req: IncomingMessage, workspaceId: string) {
  await resolveActiveWorkspaceMember(req, workspaceId, true)
}

/** Brand profile is a workspace-level customer surface on both HTTP and MCP.
 * Keep the HTTP transport from bypassing the active-member and write-role gates
 * that the MCP route already applies. Platform operators still need the
 * temporary customer-data grant enforced by the shared HTTP preflight. */
async function enforceBrandProfileHttpAccess(req: IncomingMessage, workspaceId: string, write = false) {
  await enforceActiveWorkspaceMember(req, workspaceId)
  if (!requiresStrictAuth() || isPlatformOperations(req)) return
  if (write && !['workspace_owner', 'merchant_admin', 'operator'].includes(requestPrincipals.get(req)?.memberRole ?? '')) {
    throw new DomainError(ERROR_CODES.FORBIDDEN, '品牌资料写入需要工作区所有者、商家管理员或运营角色', 403)
  }
}

function hasWorkspaceWideBrandAccess(req: IncomingMessage) {
  const role = requestPrincipals.get(req)?.memberRole
  return role === 'workspace_owner' || role === 'merchant_admin' || role === 'platform_ops'
}

async function enforceBrandAccess(req: IncomingMessage, workspaceId: string, brandId: string, minimumRole: BrandAccessRole = 'viewer') {
  if (!requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)) return
  const principal = requestPrincipals.get(req)
  if (!principal?.actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '品权限校验缺少成员身份', 401)
  const allowed = await (persistence.brandUnits ?? memoryBrandUnits).hasBrandAccess({ workspaceId, brandId, externalSubject: principal.actorId, minimumRole })
  if (!allowed) throw new DomainError('BRAND_ACCESS_REQUIRED', '当前成员没有该品所需权限', 403, { brand_id: brandId, required_role: minimumRole })
}

function campaignTaskProjection(workspaceId: string, item: CampaignItemRow): { state: CampaignItemState; error?: CampaignItemRow['error'] } {
  if (!item.taskId) return { state: 'pending', error: { code: 'TASK_NOT_CREATED', message: '尚未创建商品内容任务', nextAction: 'campaign.batch.generate' } }
  const task = service.tasks.get(item.taskId)
  if (!task || task.workspaceId !== workspaceId) return { state: 'unknown', error: { code: 'TASK_SNAPSHOT_UNAVAILABLE', message: '任务快照暂不可用，需要恢复后继续', nextAction: 'task.resume' } }
  if (task.state === 'draft') return { state: 'blocked', error: { code: 'PRODUCT_FACTS_CONFIRMATION_REQUIRED', message: '请先确认该商品事实', nextAction: 'catalog.facts.confirm' } }
  if (task.state === 'ready_for_direction') return { state: 'manual_attention', error: { code: 'DIRECTION_CONFIRMATION_REQUIRED', message: '请为该商品选择内容方向', nextAction: 'task.select_direction' } }
  if (task.state === 'direction_selected') return { state: 'manual_attention', error: { code: 'PLAN_CONFIRMATION_REQUIRED', message: '请确认该商品的生产方案', nextAction: 'task.plan.confirm' } }
  if (task.state === 'plan_confirmed') return { state: 'generating', error: { code: 'CONTENT_GENERATION_READY', message: '生产方案已确认，可以生成内容', nextAction: 'content.generate' } }
  if (task.state === 'review_required') return { state: 'review_required', error: { code: 'HUMAN_REVIEW_REQUIRED', message: '内容已生成，等待规则审核和人工批准', nextAction: 'content.review' } }
  if (task.state === 'approved') return { state: 'approved', error: { code: 'PUBLISH_PREPARATION_READY', message: '内容已批准，可以加入批量发布预览', nextAction: 'publish.batch.prepare' } }
  if (task.state === 'publish_prepared') return { state: 'approved', error: { code: 'PUBLISH_CONFIRMATION_REQUIRED', message: '发布预览已冻结，等待逐项确认', nextAction: 'publish.batch.confirm' } }
  if (task.state === 'publishing') {
    const publish = service.listPublishJobs(workspaceId).filter(job => job.taskId === task.id).sort((left, right) => right.createdAt.localeCompare(left.createdAt))[0]
    if (publish?.state === 'published' || publish?.remoteState === 'published') return { state: 'published' }
    if (publish?.state === 'rejected' || publish?.remoteState === 'rejected') return { state: 'failed', error: { code: publish.rejection?.rawCode ?? 'PLATFORM_REJECTED', message: publish.rejection?.message ?? '平台驳回了该商品', nextAction: 'ops.marketing.revision.create' } }
    if (publish?.state === 'manual_attention') return { state: 'manual_attention', error: { code: 'PUBLISH_MANUAL_ATTENTION', message: '发布任务需要运营处理', nextAction: 'publish.get' } }
    if (publish?.state === 'unknown') return { state: 'unknown', error: { code: 'PUBLISH_STATE_UNKNOWN', message: '平台发布状态未知，等待查单', nextAction: 'publish.get' } }
    return { state: 'publishing', error: { code: 'PUBLISH_IN_PROGRESS', message: '平台正在处理发布任务', nextAction: 'publish.get' } }
  }
  if (task.state === 'delivered') return { state: 'published' }
  return { state: 'failed', error: { code: 'TASK_FAILED_RECOVERABLE', message: '商品任务失败，可修复后重试', nextAction: 'task.resume' } }
}

function campaignAggregateState(states: CampaignItemState[]): CampaignBatchState {
  if (states.length && states.every(state => state === 'published')) return 'completed'
  if (states.some(state => state === 'publishing')) return 'publishing'
  if (states.some(state => state === 'unknown')) return 'unknown'
  if (states.some(state => state === 'manual_attention' || state === 'blocked')) return 'manual_attention'
  if (states.some(state => state === 'review_required' || state === 'approved')) return 'review_required'
  if (states.some(state => state === 'generating')) return 'generating'
  if (states.some(state => state === 'failed') && states.some(state => state === 'published')) return 'partial'
  if (states.some(state => state === 'failed')) return 'failed'
  return 'draft'
}

async function refreshCampaignProgress(campaign: CampaignBatchRow) {
  if (campaign.state === 'paused') return campaign
  if (!campaign.items?.length) return campaign
  const items = campaign.items.map(item => ({ ...item, ...campaignTaskProjection(campaign.workspaceId, item) }))
  const state = campaignAggregateState(items.map(item => item.state))
  const changed = state !== campaign.state || items.some((item, index) => item.state !== campaign.items![index]!.state || JSON.stringify(item.error ?? null) !== JSON.stringify(campaign.items![index]!.error ?? null))
  if (!changed) return campaign
  return await (persistence.brandUnits ?? memoryBrandUnits).updateCampaignProgress({ workspaceId: campaign.workspaceId, id: campaign.id, state, items: items.map(item => ({ id: item.id, ...(item.taskId ? { taskId: item.taskId } : {}), state: item.state, ...(item.error ? { error: item.error } : {}) })) })
}

function campaignWorkflow(campaign: CampaignBatchRow) {
  const items = (campaign.items ?? []).map(item => ({ item_id: item.id, product_id: item.productId, platform: item.platform, account_id: item.accountId, task_id: item.taskId ?? null, state: item.state, blocker: item.error ?? null, next_action: item.error?.nextAction ?? (item.state === 'published' ? null : 'campaign.batch.get') }))
  return { items, summary: { total: items.length, planned: items.filter(item => item.state === 'pending').length, published: items.filter(item => item.state === 'published').length, blocked: items.filter(item => ['blocked', 'failed', 'unknown', 'manual_attention'].includes(item.state)).length, review_required: items.filter(item => ['review_required', 'approved'].includes(item.state)).length, in_progress: items.filter(item => ['generating', 'publishing'].includes(item.state)).length } }
}

function campaignDeliveryInput(campaign: CampaignBatchRow): CampaignDeliveryManifestInput {
  const manifestHash = campaign.manifestHash ?? createHash('sha256').update(JSON.stringify({ workspaceId: campaign.workspaceId, campaignId: campaign.id, brandId: campaign.brandId, productIds: campaign.productIds })).digest('hex')
  const items = (campaign.items ?? []).map(item => {
    const task = item.taskId ? service.tasks.get(item.taskId) : undefined
    // A campaign intent is not proof of a canonical listing. Never invent a
    // production identity for an unbound item; validation below must surface
    // the missing scope as an actionable blocked manifest.
    const listingId = item.listingId ?? ''
    const contentVersionId = task?.contentVersionId ?? `planned-content:${item.id}`
    const contentHash = createHash('sha256').update(JSON.stringify({ campaignId: campaign.id, itemId: item.id, taskId: task?.id ?? null, taskVersion: task?.version ?? 0, contentVersionId })).digest('hex')
    const visualVersion = { id: `planned-visual:${item.id}`, hash: createHash('sha256').update(`${manifestHash}:${item.id}:visual`).digest('hex') }
    // A campaign row proves durable intent, not an external platform canary or
    // rule capture. Keep these inputs explicitly unverified until repositories
    // provide applicable immutable evidence for this exact listing scope.
    const specification = { id: `unverified-campaign-spec:${campaign.id}:${item.id}`, hash: createHash('sha256').update(`${manifestHash}:${item.platform}:${item.accountId}:unverified`).digest('hex'), evidenceState: 'unverified' as const }
    const ruleSnapshot = { id: `unverified-campaign-rules:${campaign.id}:${item.id}`, hash: createHash('sha256').update(`${manifestHash}:${campaign.workspaceId}:${item.id}:rules:unverified`).digest('hex'), checkedAt: campaign.updatedAt, evidenceRef: '' }
    const visualVersions = [visualVersion]
    const versionVector = { campaignId: campaign.id, brandId: campaign.brandId, productId: item.productId, listingId, skuIds: [`planned-sku:${item.productId}`], platform: item.platform, accountId: item.accountId, contentVersionId, visualVersionIds: visualVersions.map(version => version.id), specificationId: specification.id, ruleSnapshotId: ruleSnapshot.id }
    const blocked = ['blocked', 'failed', 'unknown', 'manual_attention'].includes(item.state)
    return {
      id: item.id,
      productId: item.productId,
      listingId,
      skuIds: versionVector.skuIds,
      platform: item.platform,
      accountId: item.accountId,
      contentVersion: { id: contentVersionId, hash: contentHash },
      visualVersions,
      specification,
      ruleSnapshot,
      versionVector,
      review: { status: 'blocked' as const, reason: blocked ? item.error?.message ?? 'campaign item blocked' : 'platform specification and rule evidence are externally unverified' },
      publish: item.state === 'failed' ? { status: 'failed' as const, attempts: 1, error: { code: item.error?.code ?? 'CAMPAIGN_ITEM_FAILED', message: item.error?.message ?? 'campaign item failed' } } : { status: 'not_ready' as const, attempts: 0 },
    }
  })
  return { id: `delivery-manifest:${campaign.id}`, workspaceId: campaign.workspaceId, campaignId: campaign.id, brandId: campaign.brandId, items, paused: campaign.state === 'paused', ...(campaign.state === 'paused' ? { pauseReason: 'durable campaign state is paused' } : {}), revision: campaign.revision ?? Math.max(1, Math.floor(Date.parse(campaign.updatedAt) / 1_000)) }
}

async function validateCampaignDelivery(operation: CampaignDeliveryLifecycleOperation, workspaceId: string, campaign: CampaignBatchRow) {
  // This adapter is intentionally request-scoped. Its in-memory replay guard is
  // only a consistency check; the durable campaign row remains the source of truth.
  const input = campaignDeliveryInput(campaign)
  const durableProjection = { ...campaign, items: campaign.items?.map((item, index) => ({ ...item, listingId: item.listingId ?? input.items[index]!.listingId })) }
  const adapter = new CampaignDeliveryOrchestratorAdapter({ execute: async () => ({ row: durableProjection, deliveryItems: input.items, manifestId: input.id, pauseReason: input.pauseReason }) })
  try {
    const request = { workspaceId, campaignId: campaign.id }
    if (operation === 'create') return (await adapter.create(request)).manifest
    if (operation === 'generate') return (await adapter.generate(request)).manifest
    if (operation === 'pause') return (await adapter.pause(request)).manifest
    if (operation === 'resume') return (await adapter.resume(request)).manifest
    if (operation === 'retry_failed') return (await adapter.retryFailed(request)).manifest
    return (await adapter.get(request)).manifest
  } catch (error) {
    if (error instanceof CampaignManifestError && ['CAMPAIGN_ITEM_EVIDENCE_REQUIRED', 'CAMPAIGN_MANIFEST_INVALID'].includes(error.code)) {
      const missingListing = error.code === 'CAMPAIGN_MANIFEST_INVALID'
      const items = (campaign.items ?? []).map(item => ({ id: item.id, productId: item.productId, platform: item.platform, accountId: item.accountId, state: 'blocked' as const, nextAction: 'resolve_review' as const, blockers: [{ code: error.code, message: missingListing ? '缺少唯一规范 listing，不能伪造 planned-listing 身份' : '缺少与当前平台、店铺和商品范围绑定的真实规格或规则证据', path: error.path ?? null }] }))
      return { id: `delivery-manifest:${campaign.id}`, workspaceId, campaignId: campaign.id, brandId: campaign.brandId, state: campaign.state === 'paused' ? 'paused' as const : 'blocked' as const, paused: campaign.state === 'paused', revision: campaign.revision ?? 1, externallyUnverified: true, validation: { valid: false, code: error.code, path: error.path ?? null }, progress: { total: items.length, reviewed: 0, confirmed: 0, publishing: 0, published: 0, failed: 0, blocked: items.length, percent: 0 }, items }
    }
    if (error instanceof CampaignManifestError) throw new DomainError(error.code, error.message, 409, { path: error.path ?? null })
    throw error
  }
}

const CAMPAIGN_LIFECYCLE_METHODS = new Set(['campaign.batch.pause', 'campaign.batch.resume', 'campaign.batch.retry_failed'])

function assertCampaignLifecycleParams(method: string, params: JsonObject) {
  for (const key of ['campaign_id', 'expected_revision', 'reason', 'idempotency_key']) if (typeof params[key] !== 'string' || !params[key].trim()) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 为必填字符串`, 400)
  if (!/^[1-9]\d*$/u.test(String(params.expected_revision))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_revision 必须是正整数', 400)
  if (String(params.reason).trim().length < 3 || String(params.reason).length > 1_000) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'reason 长度必须为 3 到 1000', 400)
  if (!/^[A-Za-z0-9._:-]{8,200}$/u.test(String(params.idempotency_key))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'idempotency_key 格式无效', 400)
  if (method === 'campaign.batch.retry_failed' && params.item_ids_json !== undefined && typeof params.item_ids_json !== 'string') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'item_ids_json 必须是 JSON 字符串数组', 400)
}

function campaignLifecycleError(error: unknown): never {
  if (!(error instanceof CampaignLifecycleError)) throw error
  const mapped = error.code === 'CAMPAIGN_BATCH_NOT_FOUND' ? { status: 404, message: '批量运营计划不存在或不属于当前工作区' }
    : error.code === 'CAMPAIGN_REVISION_CONFLICT' ? { status: 409, message: 'campaign revision 已变化，请刷新后重试' }
      : error.code === 'CAMPAIGN_LIFECYCLE_IDEMPOTENCY_CONFLICT' ? { status: 409, message: '幂等键已绑定其他 campaign 生命周期操作' }
        : error.code === 'CAMPAIGN_RETRY_ITEM_INVALID' ? { status: 409, message: 'retry_failed 只能选择当前 failed 的 campaign item' }
          : { status: 409, message: 'campaign 当前状态不允许该操作' }
  throw new DomainError(error.code, mapped.message, mapped.status)
}

async function accessibleBrandNavigation(req: IncomingMessage, workspaceId: string) {
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const brands = await repository.listBrands({ workspaceId })
  const visible = !requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)
    ? brands
    : (await Promise.all(brands.map(async brand => await repository.hasBrandAccess({ workspaceId, brandId: brand.id, externalSubject: requestPrincipals.get(req)!.actorId }) ? brand : undefined))).filter((brand): brand is typeof brands[number] => Boolean(brand))
  return visible.map(brand => ({
    id: brand.id,
    title: brand.name,
    revision: brand.revision,
    action: { method: 'brand-unit.listing.list', arguments: { brand_id: brand.id } },
    platforms: Object.entries(brand.storeBindings.reduce<Record<string, typeof brand.storeBindings>>((groups, store) => { (groups[store.platform] ??= []).push(store); return groups }, {})).map(([platform, stores]) => ({
      id: `${brand.id}:${platform}`,
      platform,
      title: PLATFORM_LABELS[platform as Platform] ?? platform,
      stores: stores.map(store => ({ id: `${brand.id}:${store.platform}:${store.accountId}`, accountId: store.accountId, action: { method: 'brand-unit.listing.list', arguments: { brand_id: brand.id, platform: store.platform, account_id: store.accountId } } })),
    })),
  }))
}

async function requireEnabledPlatform(workspaceId: string, platform: Platform) {
  if (!COMMERCIAL_PLATFORMS.includes(platform as CommercialPlatform)) return
  const setting = (await (persistence.commercial ?? memoryCommercial).listPlatformSettings(workspaceId)).find(item => item.platform === platform)
  if (setting?.enabled === false) throw new DomainError('PLATFORM_DISABLED', `${platform} 平台已被运营后台关闭，不能创建新任务或发布`, 409)
}

async function storeCapacity(workspaceId: string) {
  const subscription = await (persistence.subscriptions ?? memorySubscriptions).get(workspaceId)
  const used = service.listPlatformAccounts(workspaceId).filter(account => account.tokenState !== 'revoked').length
  const included = Math.max(0, subscription.includedStores)
  return { used, included, remaining: Math.max(0, included - used), planCode: subscription.planCode, planName: subscription.planName }
}

function commercialActionCards() {
  return [
    {
      method: 'subscription.change',
      label: '升级套餐增加店铺数',
      required_inputs: ['to_plan_code', 'billing_cycle', 'channel', 'reason', 'idempotency_key'],
      input_schema: { type: 'object', properties: { to_plan_code: { type: 'string' }, billing_cycle: { type: 'string', enum: ['monthly', 'annual'] }, channel: { type: 'string', enum: ['alipay', 'wechat'] }, reason: { type: 'string' }, idempotency_key: { type: 'string' } }, required: ['to_plan_code', 'billing_cycle', 'channel', 'reason', 'idempotency_key'] },
      confirmation: 'interactive_confirmation',
    },
    {
      method: 'ops.commercial.addons.list',
      label: '查看店铺加购包',
      arguments: {},
      confirmation: 'none',
    },
  ]
}

function billingActionCards() {
  return [
    {
      method: 'billing.recharge.create',
      label: '创建充值订单',
      required_inputs: ['channel', 'amount_cny', 'idempotency_key'],
      input_schema: { type: 'object', properties: { channel: { type: 'string', enum: ['alipay', 'wechat'] }, amount_cny: { type: 'string', pattern: '^[0-9]+(\\.[0-9]{1,2})?$' }, idempotency_key: { type: 'string' } }, required: ['channel', 'amount_cny', 'idempotency_key'] },
      confirmation: 'interactive_confirmation',
    },
    {
      method: 'subscription.change',
      label: '升级套餐',
      required_inputs: ['to_plan_code', 'billing_cycle', 'channel', 'reason', 'idempotency_key'],
      input_schema: { type: 'object', properties: { to_plan_code: { type: 'string' }, billing_cycle: { type: 'string', enum: ['monthly', 'annual'] }, channel: { type: 'string', enum: ['alipay', 'wechat'] }, reason: { type: 'string' }, idempotency_key: { type: 'string' } }, required: ['to_plan_code', 'billing_cycle', 'channel', 'reason', 'idempotency_key'] },
      confirmation: 'interactive_confirmation',
    },
  ]
}

async function requireStoreCapacity(workspaceId: string) {
  // Fixture UX can deliberately exercise multiple stores without a paid
  // subscription. Production, and explicit capacity tests, enforce the plan.
  if (!isProduction() && process.env.ENFORCE_STORE_CAPACITY !== 'true') return
  const capacity = await storeCapacity(workspaceId)
  if (capacity.used >= capacity.included) throw new DomainError('STORE_QUOTA_EXCEEDED', `当前套餐已使用 ${capacity.used}/${capacity.included} 家店铺`, 402, { ...capacity, next_actions: ['升级套餐增加店铺数', '购买店铺加购包'], action_cards: commercialActionCards() })
}

function parseApprovalGrant(req: IncomingMessage, workspaceId: string, actorId: string, input: JsonObject) {
  const approvalValue = input.approval
  if (!approvalValue || typeof approvalValue !== 'object' || Array.isArray(approvalValue)) {
    throw new DomainError('RULE_APPROVAL_REQUIRED', '激活规则必须携带审批证据', 409)
  }
  const approval = approvalValue as JsonObject
  const approvalRef = required(approval, 'approval_ref')
  const approvedAt = required(approval, 'approved_at')
  if (!Number.isFinite(Date.parse(approvedAt))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'approved_at 必须是合法时间', 400)
  let approvedBy = required(approval, 'approved_by')
  if (requiresStrictAuth()) {
    const token = header(req, 'x-rule-approval-token')?.trim()
    if (!token) throw new DomainError('RULE_APPROVAL_REQUIRED', '严格认证环境的激活请求必须携带 X-Rule-Approval-Token', 409)
    let grants: Record<string, unknown>
    try {
      const parsed: unknown = JSON.parse(process.env.RULE_APPROVAL_TOKENS ?? '{}')
      if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('invalid')
      grants = parsed as Record<string, unknown>
    } catch { throw new DomainError('RULE_APPROVAL_CONFIG_INVALID', '规则审批令牌配置无效', 503) }
    const matched = Object.keys(grants).find(known => safeEqual(known, token))
    const grant = matched && grants[matched] && typeof grants[matched] === 'object' && !Array.isArray(grants[matched]) ? grants[matched] as Record<string, unknown> : undefined
    const grantWorkspaces = Array.isArray(grant?.workspaces) ? grant.workspaces.filter((value): value is string => typeof value === 'string') : []
    const grantActor = typeof grant?.actor_id === 'string' ? grant.actor_id.trim() : ''
    if (!grantActor || !grantWorkspaces.includes(workspaceId) || grantActor !== approvedBy) throw new DomainError('RULE_APPROVAL_INVALID', '规则审批令牌无效或不属于当前工作区/审批人', 403)
    approvedBy = grantActor
  }
  if (approvedBy === actorId) throw new DomainError('RULE_SEPARATION_OF_DUTIES_REQUIRED', '创建人与审批人必须分离', 409)
  return { approvalRef, approvedAt: new Date(approvedAt).toISOString(), approvedBy }
}

function objectField(input: JsonObject, key: string): Record<string, unknown> {
  const value = input[key]
  if (!value || typeof value !== 'object' || Array.isArray(value)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是 JSON 对象`, 400)
  return value as Record<string, unknown>
}

function canonicalJson(value: unknown): string {
  if (Array.isArray(value)) return `[${value.map(canonicalJson).join(',')}]`
  if (value && typeof value === 'object') return `{${Object.entries(value as Record<string, unknown>).sort(([a], [b]) => a.localeCompare(b)).map(([key, item]) => `${JSON.stringify(key)}:${canonicalJson(item)}`).join(',')}}`
  return JSON.stringify(value)
}

function fixturePlatformEnabled(_platform: Platform) {
  // The disposable fixture environment intentionally exposes an independent
  // fake connector for every supported platform so the six-platform UI flow
  // can be exercised without mistaking it for production readiness.
  return fixtureMode
}

function platformConnectorConfigured(platform: Platform) {
  return fixturePlatformEnabled(platform) || connectorRuntime.canRead(platform)
}

function platformAuthorizationConfigured(platform: Platform) {
  return fixturePlatformEnabled(platform) || connectorRuntime.isOAuthConfigured(platform)
}

function fixtureAccountId(workspaceId: string, platform: Platform) {
  return `fixture_${workspaceId}_${platform}`
}

function defaultFixtureAccountId(workspaceId: string, platform: Platform) {
  const accounts = service.listPlatformAccounts(workspaceId).filter(account => account.platform === platform)
  const existing = accounts.find(account => account.tokenState === 'connected') ?? accounts.find(account => account.tokenState !== 'revoked')
  return existing?.id ?? fixtureAccountId(workspaceId, platform)
}

function connectedPlatformAccountId(workspaceId: string, platform: Platform) {
  return service.listPlatformAccounts(workspaceId).find(account => account.platform === platform && account.tokenState === 'connected')?.id
}

function requireActivePlatformAccount(workspaceId: string, accountId: string, platform: Platform) {
  try {
    return service.getActivePlatformAccount(workspaceId, accountId, platform)
  } catch (error) {
    if (error instanceof DomainError && error.code === 'PLATFORM_ACCOUNT_NOT_FOUND') {
      throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', `请先在 Codex 中完成${platform}店铺授权，再同步商品`, 400)
    }
    throw error
  }
}

function resolveTaskAccountId(_workspaceId: string, _platform: Platform, requested?: string) {
  // Never select the first connected account. Product-bound context is
  // enforced by the domain service; unbound products remain unbound until the
  // merchant explicitly selects a platform + accountId pair.
  return requested?.trim()
}

function resolveProductTaskAccount(workspaceId: string, platform: Platform, productId: string, requested?: string) {
  const explicit = resolveTaskAccountId(workspaceId, platform, requested)
  const product = service.products.get(productId)
  if (product?.workspaceId === workspaceId && product.platform === platform && product.accountId) return product.accountId
  return explicit
}

function requireProductionTaskStore(platform: Platform, accountId: string | undefined) {
  if (isProduction() && !accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', `生产任务必须绑定已授权的${platform}店铺`, 400, { platform, next_action: '选择 platform + account_id 后重新创建任务' })
}

function requireProductionRequestStores(workspaceId: string, understanding: ReturnType<MerchantService['understandTaskRequest']>) {
  if (!isProduction()) return
  for (const child of understanding.executionPlan.childTasks) {
    const productId = child.candidateProductIds[0]
    const product = productId ? service.products.get(productId) : undefined
    requireProductionTaskStore(child.platform, product?.workspaceId === workspaceId && product.platform === child.platform ? product.accountId : undefined)
  }
}

function platformAccessFlags(platform: Platform, account: { tokenState: string } | undefined) {
  const active = account?.tokenState === 'connected'
  return {
    readEnabled: active && (fixturePlatformEnabled(platform) || platformConnectorConfigured(platform)),
    writeEnabled: active && platformWriteReady(platform),
  }
}

function platformWriteReady(platform: Platform) {
  return platformWriteAllowed({ production: isProduction(), fixtureMode, connectorConfigured: platformConnectorConfigured(platform), pluginWriteEnabled: process.env.PLUGIN_WRITE_ENABLED === 'true', canaryReady: isProductionCanaryReady(connectorRuntime.capabilityEvidence[platform], platform) })
}

function workspaceConnectorReadiness(platform: Platform) {
  const media = connectorRuntime.mediaUploadReadiness(platform)
  const mediaUpload = {
    ...media,
    ...(media.ready ? {} : { reason: !media.configured ? '主图/副图上传路径或回执映射未配置' : !media.evidence ? '主图/副图上传证据未通过门禁' : '主图/副图媒体连接器未就绪' }),
  }
  if (fixturePlatformEnabled(platform)) {
    return { platform, ready: true, reasons: ['FIXTURE_MODE'], verifiedCapabilities: [], mediaUpload: { configured: false, evidence: false, ready: false, reason: 'fixture 模式不计入真实媒体上传证据' } }
  }
  return { ...connectorRuntime.readiness[platform], mediaUpload }
}

function configuredEnv(...keys: string[]) {
  return keys.some(key => Boolean(process.env[key]?.trim()))
}

const MODEL_COST_EVIDENCE_KEYS = {
  text: 'MODEL_RELAY_TEXT_COST_EVIDENCE', image: 'MODEL_RELAY_IMAGE_COST_EVIDENCE', image_edit: 'MODEL_RELAY_IMAGE_EDIT_COST_EVIDENCE', ocr: 'MODEL_RELAY_OCR_COST_EVIDENCE', video: 'MODEL_RELAY_VIDEO_COST_EVIDENCE',
} as const

function modelCostEvidenceReady(kind: keyof typeof MODEL_COST_EVIDENCE_KEYS, source: NodeJS.ProcessEnv = process.env) {
  if (source.NODE_ENV !== 'production') return true
  const specific = source[MODEL_COST_EVIDENCE_KEYS[kind]]
  return specific === undefined ? source.MODEL_RELAY_COST_EVIDENCE === 'true' : specific === 'true'
}

function modelCostEvidenceByModality(source: NodeJS.ProcessEnv = process.env) {
  return Object.fromEntries(Object.keys(MODEL_COST_EVIDENCE_KEYS).map(kind => [kind, modelCostEvidenceReady(kind as keyof typeof MODEL_COST_EVIDENCE_KEYS, source)])) as Record<keyof typeof MODEL_COST_EVIDENCE_KEYS, boolean>
}

function requirePlatformModelCostGate(kind: 'text' | 'image' | 'image_edit' | 'ocr' | 'video') {
  if (!isProduction()) return
  const relayGate = evaluatePlatformModelRelayGate(process.env)
  if (!relayGate.ready) throw new DomainError('MODEL_RELAY_NOT_CONFIGURED', `生产环境必须配置平台模型中转站：${relayGate.reasons.join(', ')}`, 503, { reasons: relayGate.reasons })
  const modelGate = evaluatePlatformModelGate(process.env, kind)
  const costGate = evaluatePlatformModelCostGate(process.env)
  const costEvidenceReady = modelCostEvidenceReady(kind)
  if (!modelGate.ready || !costGate.ready || !costEvidenceReady) {
    const labels = { text: '文案', image: '图片', image_edit: '图片编辑', ocr: '图片解析', video: '视频' } as const
    throw new DomainError('MODEL_COST_GATE_BLOCKED', `平台${labels[kind]}模型未通过成本与配额门禁；需配置 HTTPS 中转站、模型、RPM、TPM、每日人民币成本上限和真实成本回执证据`, 503)
  }
}

async function requireVideoModelCostPreflight() {
  requirePlatformModelCostGate('video')
  if (!isProduction()) return
  const model = process.env.VIDEO_MODEL?.trim() || process.env.AI_VIDEO_MODEL?.trim()
  if (!relayPricing || !model) throw new DomainError('MODEL_VIDEO_COST_PREFLIGHT_UNAVAILABLE', '生产视频生成缺少可验证的预调用定价快照，已阻断上游请求', 503)
  let quote: Awaited<ReturnType<typeof relayPricing.quote>>
  try {
    quote = await relayPricing.quote({ modality: 'video', model, observedAt: new Date().toISOString(), metadata: { duration_seconds: videoDurationSeconds(process.env.VIDEO_DURATION_SECONDS) } })
  } catch (error) {
    throw new DomainError('MODEL_VIDEO_COST_PREFLIGHT_FAILED', '生产视频生成无法完成预调用成本核算，已阻断上游请求', 503, { pricing_error: (error as { code?: string })?.code ?? 'unknown' })
  }
  const gate = evaluatePlatformModelTaskRequestCost(quote.costCny, process.env)
  if (!gate.ready) throw new DomainError(gate.reasons.includes('request_cost_exceeds_task_limit') ? 'MODEL_TASK_COST_LIMIT_EXCEEDED' : 'MODEL_TASK_COST_PREFLIGHT_UNAVAILABLE', gate.reasons.includes('request_cost_exceeds_task_limit') ? '本次视频生成预计成本超过单任务安全上限，未扣款，也未调用生成服务；请减少视频时长后重试' : '单任务成本保护未就绪，未调用上游', gate.reasons.includes('request_cost_exceeds_task_limit') ? 422 : 503, { estimated_cost_cny: quote.costCny, maximum_task_cost_cny: gate.limitCny })
}

function lifecycleDiagnostics(source: NodeJS.ProcessEnv = process.env) {
  if (source.NODE_ENV !== 'production') return { state: 'not_required' as const, configured: true, objectVersioning: true, retentionDays: 90, quarantineRetentionDays: 7, cleanRetentionDays: 30, deletionGraceDays: 7, backupRetentionDays: 30, reasons: [] as string[] }
  const numberValue = (key: string, fallback: number) => { const value = Number(source[key] ?? fallback); return Number.isInteger(value) ? value : -1 }
  const retentionDays = numberValue('DATA_RETENTION_DAYS', -1)
  const quarantineRetentionDays = numberValue('ASSET_QUARANTINE_RETENTION_DAYS', -1)
  const cleanRetentionDays = numberValue('ASSET_CLEAN_RETENTION_DAYS', -1)
  const deletionGraceDays = numberValue('DELETION_REQUEST_GRACE_DAYS', -1)
  const backupRetentionDays = numberValue('BACKUP_RETENTION_DAYS', -1)
  const objectVersioning = source.OBJECT_STORAGE_VERSIONING === 'true'
  const reasons: string[] = []
  if (retentionDays < 90) reasons.push('DATA_RETENTION_DAYS must be at least 90')
  if (quarantineRetentionDays < 7) reasons.push('ASSET_QUARANTINE_RETENTION_DAYS must be at least 7')
  if (cleanRetentionDays < 30) reasons.push('ASSET_CLEAN_RETENTION_DAYS must be at least 30')
  if (deletionGraceDays < 7 || deletionGraceDays > 30) reasons.push('DELETION_REQUEST_GRACE_DAYS must be between 7 and 30')
  if (backupRetentionDays < 30) reasons.push('BACKUP_RETENTION_DAYS must be at least 30')
  if (!objectVersioning) reasons.push('OBJECT_STORAGE_VERSIONING must be true')
  if (!source.LIFECYCLE_POLICY_REF?.trim()) reasons.push('LIFECYCLE_POLICY_REF is not configured')
  if (!source.ALERT_CHANNEL_SECRET_REF?.trim()) reasons.push('ALERT_CHANNEL_SECRET_REF is not configured')
  return { state: reasons.length ? 'blocked' as const : 'ready' as const, configured: reasons.length === 0, objectVersioning, retentionDays, quarantineRetentionDays, cleanRetentionDays, deletionGraceDays, backupRetentionDays, reasons }
}

type EvidenceReadiness = {
  state: 'not_required' | 'blocked' | 'ready'
  configured: boolean
  sourceRef?: string
  schemaVersion?: string
  releaseId?: string
  environment?: string
  verifiedBy?: string
  verifiedAt?: string
  profile?: string
  reasons: string[]
}

function evidenceReadiness(kind: 'capability' | 'capacity'): EvidenceReadiness {
  const pathKey = kind === 'capability' ? 'CAPABILITY_EVIDENCE_PATH' : 'CAPACITY_REPORT_PATH'
  const sourceRef = process.env[pathKey]?.trim()
  const base: EvidenceReadiness = { state: isProduction() ? 'blocked' : 'not_required', configured: false, ...(sourceRef ? { sourceRef: 'configured' } : {}), reasons: [] }
  if (!sourceRef) {
    base.reasons.push(`${pathKey} is not configured`)
    return base
  }
  let document: unknown
  try { document = JSON.parse(readFileSync(sourceRef, 'utf8')) } catch { base.reasons.push(`${pathKey} cannot be read`) ; return base }
  if (!document || typeof document !== 'object' || Array.isArray(document)) { base.reasons.push('evidence document must be a JSON object'); return base }
  const value = document as Record<string, any>
  base.schemaVersion = typeof value.schema_version === 'string' ? value.schema_version : undefined
  base.releaseId = typeof value.release_id === 'string' ? value.release_id : undefined
  base.environment = typeof value.environment === 'string' ? value.environment : undefined
  base.profile = typeof value.profile === 'string' ? value.profile : undefined
  base.verifiedBy = typeof value.sign_off?.verified_by === 'string' ? value.sign_off.verified_by : undefined
  base.verifiedAt = typeof value.sign_off?.verified_at === 'string' ? value.sign_off.verified_at : (typeof value.generated_at === 'string' ? value.generated_at : undefined)
  if (kind === 'capability') {
    const errors = validatePlatformCapabilityEvidence(document, { requireCanary: true, expectedReleaseId: process.env.RELEASE_ID?.trim() || undefined })
    base.reasons.push(...errors)
    if (value.environment !== 'preproduction' && value.environment !== 'production') base.reasons.push('environment must be preproduction or production')
  } else {
    if (value.schema_version !== '1') base.reasons.push('schema_version must be 1')
    if (value.status !== 'pass') base.reasons.push('status must be pass')
    if (value.cloud_gate !== true) base.reasons.push('cloud_gate must be true')
    if (!['preproduction', 'production'].includes(value.environment)) base.reasons.push('environment must be preproduction or production')
    if (value.platform_mock_ratio !== 0 || value.model_mock_ratio !== 0) base.reasons.push('platform/model mock ratio must be 0')
    if (!value.sign_off?.verified_by || !value.sign_off?.verified_at) base.reasons.push('sign_off is incomplete')
  }
  base.configured = base.reasons.length === 0
  base.state = base.configured ? 'ready' : 'blocked'
  return base
}

type ProductionReadinessGate = { ready: boolean; reasons: string[] }

function productionIdentityReadiness(source: NodeJS.ProcessEnv): ProductionReadinessGate {
  const reasons: string[] = []
  if (source.OPS_AUTH_MODE !== 'oidc') reasons.push('ops_auth_mode_must_be_oidc')
  if (!source.OIDC_PROXY_SIGNING_SECRET?.trim()) reasons.push('oidc_proxy_signing_secret_missing')
  if (!source.SESSION_ID_HASH_SECRET?.trim()) reasons.push('session_id_hash_secret_missing')
  const merchantHostname = source.MERCHANT_BEARER_HOSTNAME?.trim().toLowerCase()
  if (!merchantHostname) reasons.push('merchant_bearer_hostname_missing')
  else if (merchantHostname.includes('*') || merchantHostname.includes('://') || merchantHostname.includes('/')) reasons.push('merchant_bearer_hostname_invalid')
  let grantsReady = false
  try {
    const parsed: unknown = JSON.parse(source.API_AUTH_TOKENS ?? '{}')
    if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
      grantsReady = Object.values(parsed as Record<string, unknown>).some(value => {
        if (!value || typeof value !== 'object' || Array.isArray(value)) return false
        const grant = value as Record<string, unknown>
        return typeof grant.actor_id === 'string'
          && grant.actor_id.trim().length > 0
          && Array.isArray(grant.workspaces)
          && grant.workspaces.some(workspace => typeof workspace === 'string' && workspace.trim().length > 0 && workspace !== '*')
      })
    }
  } catch { /* reported below without exposing the configured token map */ }
  if (!grantsReady) reasons.push('merchant_api_token_grants_missing_or_invalid')
  return { ready: reasons.length === 0, reasons }
}

function productionObjectStorageReadiness(source: NodeJS.ProcessEnv): ProductionReadinessGate {
  const reasons: string[] = []
  for (const key of ['ASSET_STORAGE_BUCKET', 'ASSET_STORAGE_REGION', 'ASSET_STORAGE_KMS_KEY_ID'] as const) {
    if (!source[key]?.trim()) reasons.push(`${key.toLowerCase()}_missing`)
  }
  const endpoint = source.ASSET_STORAGE_ENDPOINT?.trim()
  if (!endpoint) reasons.push('asset_storage_endpoint_missing')
  else {
    try { if (new URL(endpoint).protocol !== 'https:') reasons.push('asset_storage_endpoint_must_use_https') }
    catch { reasons.push('asset_storage_endpoint_invalid') }
  }
  const displayBaseUrl = source.PUBLIC_ASSET_BASE_URL?.trim()
  if (!displayBaseUrl) reasons.push('public_asset_base_url_missing')
  else {
    try { if (new URL(displayBaseUrl).protocol !== 'https:') reasons.push('public_asset_base_url_must_use_https') }
    catch { reasons.push('public_asset_base_url_invalid') }
  }
  if ((source.ASSET_DISPLAY_URL_SIGNING_SECRET?.trim().length ?? 0) < 32) reasons.push('asset_display_url_signing_secret_missing_or_weak')
  if (!/^[A-Za-z0-9._-]{1,64}$/u.test(source.ASSET_DISPLAY_URL_SIGNING_KEY_ID?.trim() ?? '')) reasons.push('asset_display_url_signing_key_id_missing_or_invalid')
  if (source.ASSET_DISPLAY_URL_PREVIOUS_KEYS_JSON?.trim()) {
    try {
      const parsed: unknown = JSON.parse(source.ASSET_DISPLAY_URL_PREVIOUS_KEYS_JSON)
      if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed) || Object.entries(parsed as Record<string, unknown>).some(([kid, value]) => !/^[A-Za-z0-9._-]{1,64}$/u.test(kid) || typeof value !== 'string' || value.trim().length < 32 || kid === source.ASSET_DISPLAY_URL_SIGNING_KEY_ID?.trim())) reasons.push('asset_display_url_previous_keys_invalid')
    } catch { reasons.push('asset_display_url_previous_keys_invalid') }
  }
  reasons.push(...lifecycleDiagnostics(source).reasons.map(reason => `lifecycle:${reason}`))
  return { ready: reasons.length === 0, reasons }
}

function productionReleaseMetadataReadiness(source: NodeJS.ProcessEnv): ProductionReadinessGate {
  const reasons: string[] = []
  if (!source.RELEASE_ID?.trim()) reasons.push('release_id_missing')
  if (!/^[0-9a-f]{40}$/u.test(source.RELEASE_GIT_SHA?.trim() ?? '')) reasons.push('release_git_sha_invalid')
  if (!/^[0-9a-f]{64}$/u.test(source.RELEASE_MANIFEST_SHA256?.trim() ?? '')) reasons.push('release_manifest_sha256_invalid')
  if (!/^sha256:[0-9a-f]{64}$/u.test(source.RELEASE_IMAGE_SET_DIGEST?.trim() ?? '')) reasons.push('release_image_set_digest_invalid')
  for (const key of ['PLUGIN_VERSION', 'SKILL_BUNDLE_VERSION', 'MCP_VERSION', 'CONNECTOR_BUILD', 'PROMPT_BUNDLE_VERSION'] as const) {
    const value = source[key]?.trim().toLowerCase()
    if (!value || value === 'local' || value.includes('fixture')) reasons.push(`${key.toLowerCase()}_missing_or_non_production`)
  }
  return { ready: reasons.length === 0, reasons }
}

function productionAssetScannerReadiness(source: NodeJS.ProcessEnv): ProductionReadinessGate {
  const reasons: string[] = []
  if (source.ASSET_SCANNER_MODE !== 'clamav_worker') reasons.push('asset_scanner_mode_must_be_clamav_worker')
  if (!source.ASSET_SCANNER_API_TOKEN?.trim()) reasons.push('asset_scanner_api_token_missing')
  if (!source.ASSET_SCANNER_WORKSPACE_SIGNING_SECRET?.trim()) reasons.push('asset_scanner_workspace_signing_secret_missing')
  if (source.ALLOW_LOCAL_ASSET_SCAN_FIXTURE === 'true') reasons.push('local_asset_scan_fixture_forbidden')
  if (!source.ASSET_SCAN_POLICY_VERSION?.trim()) reasons.push('asset_scan_policy_version_missing')
  if (!(source.ASSET_SCAN_APPROVED_SCANNER_SERVICE_IDS ?? '').split(',').some(value => value.trim().length > 0)) reasons.push('asset_scan_approved_scanner_service_ids_missing')
  const minimumDefinitionsVersion = Number(source.ASSET_SCAN_MIN_DEFINITIONS_VERSION)
  if (!Number.isSafeInteger(minimumDefinitionsVersion) || minimumDefinitionsVersion <= 0) reasons.push('asset_scan_min_definitions_version_missing_or_invalid')
  try {
    const keys = JSON.parse(source.ASSET_SCAN_TRUSTED_PUBLIC_KEYS ?? '') as unknown
    if (!keys || typeof keys !== 'object' || Array.isArray(keys) || !Object.values(keys as Record<string, unknown>).some(value => typeof value === 'string' && value.includes('BEGIN PUBLIC KEY'))) throw new Error('invalid')
  } catch { reasons.push('asset_scan_trusted_public_keys_missing_or_invalid') }
  return { ready: reasons.length === 0, reasons }
}

function productionPaymentReadiness(source: NodeJS.ProcessEnv): ProductionReadinessGate {
  const readiness = paymentProviderReadiness(source)
  const reasons = source.PAYMENT_MODE === 'provider' ? [...readiness.reasons] : ['payment_mode_must_be_provider', ...readiness.reasons]
  return { ready: reasons.length === 0, reasons }
}

function productionRuleSyncReadiness(source: NodeJS.ProcessEnv): ProductionReadinessGate {
  const reasons: string[] = []
  const manifestUrl = source.PLATFORM_RULE_SYNC_MANIFEST_URL?.trim()
  if (!manifestUrl) reasons.push('platform_rule_sync_manifest_url_missing')
  else {
    try {
      const parsed = new URL(manifestUrl)
      const hostname = parsed.hostname.toLowerCase().replace(/\.$/u, '')
      const privateLiteral = hostname === 'localhost' || hostname === '::1' || /^127\./u.test(hostname) || /^10\./u.test(hostname) || /^192\.168\./u.test(hostname) || /^172\.(?:1[6-9]|2\d|3[01])\./u.test(hostname)
      if (parsed.protocol !== 'https:' || parsed.username || parsed.password || parsed.search || parsed.hash || privateLiteral) reasons.push('platform_rule_sync_manifest_url_invalid')
    } catch { reasons.push('platform_rule_sync_manifest_url_invalid') }
  }
  if (!source.PLATFORM_RULE_SYNC_SIGNING_SECRET?.trim()) reasons.push('platform_rule_sync_signing_secret_missing')
  const interval = Number(source.PLATFORM_RULE_SYNC_INTERVAL_HOURS)
  if (!Number.isSafeInteger(interval) || interval <= 0) reasons.push('platform_rule_sync_interval_invalid')
  return { ready: reasons.length === 0, reasons }
}

function controlledScannerEnvironment(source: NodeJS.ProcessEnv): boolean {
  return ['staging', 'preview', 'production'].includes(source.NODE_ENV ?? '') && source.ASSET_SCANNER_MODE === 'clamav_worker'
}

function configuredAssetScanMaxAttempts(source: NodeJS.ProcessEnv = process.env): number {
  const raw = source.WORKER_SCAN_MAX_ATTEMPTS?.trim()
  const value = raw ? Number(raw) : 12
  if (!Number.isSafeInteger(value) || value < 1 || value > 1000) throw new DomainError('ASSET_SCAN_RETRY_POLICY_INVALID', '素材扫描重试预算配置无效，已拒绝恢复操作', 503)
  return value
}

export function scannerHeartbeatRequiredForProbe(path: string, source: NodeJS.ProcessEnv = process.env): boolean {
  return path === '/readyz' && controlledScannerEnvironment(source)
}

function scannerMinimumReadyInstances(source: NodeJS.ProcessEnv): number {
  const configured = Number(source.SCANNER_MINIMUM_READY_INSTANCES)
  const minimum = Number.isSafeInteger(configured) && configured > 0 ? configured : 1
  return source.NODE_ENV === 'production' ? Math.max(2, minimum) : minimum
}

function isScannerHeartbeat(value: unknown): value is ScannerHeartbeat {
  if (!value || typeof value !== 'object' || Array.isArray(value)) return false
  const item = value as Partial<ScannerHeartbeat>
  return item.schemaVersion === SCANNER_HEARTBEAT_SCHEMA
    && typeof item.instanceId === 'string'
    && typeof item.observedAt === 'string'
    && typeof item.expiresAt === 'string'
    && typeof item.ready === 'boolean'
    && typeof item.recoveryCapable === 'boolean'
    && Boolean(item.checks && typeof item.checks.databaseReady === 'boolean' && typeof item.checks.redisReady === 'boolean' && typeof item.checks.apiReady === 'boolean')
    && Boolean(item.clamav && typeof item.clamav.reachable === 'boolean')
    && Boolean(item.eicar && typeof item.eicar.passed === 'boolean')
    && Boolean(item.callback && typeof item.callback.configured === 'boolean' && typeof item.callback.capable === 'boolean')
    && Boolean(item.queue && Number.isFinite(item.queue.backlog) && Number.isFinite(item.queue.deadLetter))
}

export type ScannerReadinessCode = 'SCANNER_HEARTBEAT_MISSING' | 'SCANNER_READY_REPLICA_QUORUM_UNMET' | 'SCANNER_NOT_READY'
export type ScannerReadinessSummary = {
  ready: boolean
  degraded: boolean
  observed_at: string
  live_instances: number
  ready_instances: number
  minimum_ready_instances: number
  backlog: number
  dead_letter: number
  newest_definitions_version: string | null
  latest_eicar_at: string | null
  latest_callback_accepted_at: string | null
}

type ScannerHeartbeatReadinessInput = {
  redis: Pick<RedisHealthPort, 'scannerHeartbeats'>
  env?: NodeJS.ProcessEnv
  now?: Date
}

async function evaluateScannerHeartbeats(input: ScannerHeartbeatReadinessInput, purpose: 'service' | 'recovery'): Promise<{ ready: true; summary: ScannerReadinessSummary } | { ready: false; code: ScannerReadinessCode; summary: ScannerReadinessSummary; reasons: string[] }> {
  const env = input.env ?? process.env
  const now = input.now ?? new Date()
  const raw = await input.redis.scannerHeartbeats(now.getTime())
  const minimumDefinitionsVersion = Number(env.ASSET_SCAN_MIN_DEFINITIONS_VERSION)
  const heartbeats = raw.filter(isScannerHeartbeat).map(heartbeat => {
    const purposeReady = purpose === 'recovery' ? heartbeat.recoveryCapable : heartbeat.ready
    const scopedHeartbeat = purposeReady === heartbeat.ready ? heartbeat : { ...heartbeat, ready: purposeReady }
    if (!Number.isSafeInteger(minimumDefinitionsVersion) || minimumDefinitionsVersion <= 0) return scopedHeartbeat
    const actual = Number(heartbeat.clamav.definitionsVersion)
    return Number.isSafeInteger(actual) && actual >= minimumDefinitionsVersion
      ? scopedHeartbeat
      : { ...scopedHeartbeat, ready: false, failure: { code: 'SCANNER_DEFINITIONS_BELOW_MINIMUM', message: 'definitions below configured minimum' } }
  })
  const aggregate = aggregateScannerHeartbeats(heartbeats, {
    now,
    minimumReadyInstances: scannerMinimumReadyInstances(env),
  })
  const summary: ScannerReadinessSummary = {
    ready: aggregate.ready,
    degraded: aggregate.degraded,
    observed_at: aggregate.observedAt,
    live_instances: aggregate.liveInstances,
    ready_instances: aggregate.readyInstances,
    minimum_ready_instances: aggregate.minimumReadyInstances,
    backlog: aggregate.backlog,
    dead_letter: aggregate.deadLetter,
    newest_definitions_version: aggregate.newestDefinitionsVersion ?? null,
    latest_eicar_at: aggregate.latestEicarAt ?? null,
    latest_callback_accepted_at: aggregate.latestCallbackAcceptedAt ?? null,
  }
  if (aggregate.ready) return { ready: true, summary }
  const code: ScannerReadinessCode = aggregate.liveInstances === 0
    ? 'SCANNER_HEARTBEAT_MISSING'
    : aggregate.liveInstances < aggregate.minimumReadyInstances
      ? 'SCANNER_READY_REPLICA_QUORUM_UNMET'
      : 'SCANNER_NOT_READY'
  const reasons = code === 'SCANNER_NOT_READY'
    ? ['SCANNER_INSTANCE_NOT_READY']
    : [code]
  return { ready: false, code, summary, reasons }
}

export async function evaluateScannerHeartbeatReadiness(input: ScannerHeartbeatReadinessInput) {
  return evaluateScannerHeartbeats(input, 'service')
}

/** Recovery admission deliberately ignores only the dead-letter count. The
 * worker computes recoveryCapable from every scanner dependency and freshness
 * gate before applying the queue-health requirement to normal readiness. */
export async function evaluateScannerRecoveryAdmission(input: ScannerHeartbeatReadinessInput) {
  return evaluateScannerHeartbeats(input, 'recovery')
}

/** Deployment readiness is stricter than process liveness. Test profiles keep
 * their dependency-only readiness behavior; every production process,
 * including one accidentally configured for fixtures, must fail closed before
 * it receives traffic when a critical external-control-plane gate is missing. */
export function productionReadinessDiagnostics(source: NodeJS.ProcessEnv = process.env) {
  // CONNECTOR_FIXTURE_MODE is acceptable for local/test acceptance only. It
  // must never make a production process ready or eligible for traffic.
  const required = source.NODE_ENV === 'production'
  if (!required) return { required: false, ready: true, gates: {} as Record<string, ProductionReadinessGate> }

  const relayResults = {
    relay: evaluatePlatformModelRelayGate(source),
    text: evaluatePlatformModelGate(source, 'text'),
    image: evaluatePlatformModelGate(source, 'image'),
    image_edit: evaluatePlatformModelGate(source, 'image_edit'),
    ocr: evaluatePlatformModelGate(source, 'ocr'),
    video: evaluatePlatformModelGate(source, 'video'),
  }
  const relay: ProductionReadinessGate = {
    ready: Object.values(relayResults).every(result => result.ready),
    reasons: Object.entries(relayResults).flatMap(([kind, result]) => result.reasons.map(reason => `${kind}:${reason}`)),
  }
  const costGate = evaluatePlatformModelCostGate(source)
  const taskCostGate = evaluatePlatformModelTaskCostLimit(source)
  const costEvidence = modelCostEvidenceByModality(source)
  const cost: ProductionReadinessGate = {
    ready: costGate.ready && taskCostGate.ready && Object.values(costEvidence).every(Boolean),
    reasons: [
      ...costGate.reasons.map(reason => `limits:${reason}`),
      ...taskCostGate.reasons.map(reason => `limits:${reason}`),
      ...Object.entries(costEvidence).filter(([, ready]) => !ready).map(([kind]) => `${kind}:cost_evidence_missing`),
    ],
  }
  const gates: Record<string, ProductionReadinessGate> = {
    relay,
    authorization: productionAuthorizationReadiness(source, true),
    identity: productionIdentityReadiness(source),
    object_storage: productionObjectStorageReadiness(source),
    asset_scanner: productionAssetScannerReadiness(source),
    payment: productionPaymentReadiness(source),
    rule_sync: productionRuleSyncReadiness(source),
    cost,
    release_metadata: productionReleaseMetadataReadiness(source),
  }
  return { required: true, ready: Object.values(gates).every(gate => gate.ready), gates }
}

/** A safe, secret-free setup report for the Codex App. It tells the operator
 * what can run locally and what still needs real credentials/infrastructure,
 * without echoing tokens, keys, endpoints, or bucket names. */
function setupDiagnostics() {
  const production = isProduction()
  const relayGate = evaluatePlatformModelRelayGate(process.env)
  const paymentReadiness = paymentProviderReadiness()
  const contentProviderConfigured = evaluatePlatformModelGate(process.env, 'text').ready
  const imageProviderConfigured = evaluatePlatformModelGate(process.env, 'image').ready
  const imageEditModelGate = evaluatePlatformModelGate(process.env, 'image_edit')
  const ocrModelGate = evaluatePlatformModelGate(process.env, 'ocr')
  const videoModelGate = evaluatePlatformModelGate(process.env, 'video')
  const imageFactsConfigured = Boolean(imageFactsExtractor) && ocrModelGate.ready
  const imageEditProviderConfigured = Boolean(imageEditGenerator) && imageEditModelGate.ready
  const videoProviderConfigured = Boolean(videoGenerator) && videoModelGate.ready
  const modelCostGateConfigured = evaluatePlatformModelCostGate(process.env).ready && Object.values(modelCostEvidenceByModality()).every(Boolean)
  const vaultConfigured = connectorRuntime.credentialProviderConfigured && !fixtureMode
  const localAcceptanceObjectStorage = production && process.env.DEPLOYMENT_PROFILE === 'local_acceptance' && process.env.ALLOW_LOCAL_DURABLE_OBJECT_STORAGE === 'true' && (process.env.ASSET_STORAGE_ROOT?.startsWith('/var/lib/merchant-assets/') ?? false)
  const objectStorageConfigured = production
    ? localAcceptanceObjectStorage || (configuredEnv('ASSET_STORAGE_BUCKET') && configuredEnv('ASSET_STORAGE_REGION') && configuredEnv('ASSET_STORAGE_ENDPOINT') && configuredEnv('ASSET_STORAGE_KMS_KEY_ID'))
    : true
  const dataLifecycle = lifecycleDiagnostics()
  const capabilityEvidence = evidenceReadiness('capability')
  const capacityEvidence = evidenceReadiness('capacity')
  const platformDiagnostics = Object.fromEntries(SUPPORTED_PLATFORMS.map(platform => {
    const readiness = connectorRuntime.readiness[platform]
    return [platform, {
      mode: fixturePlatformEnabled(platform) ? 'fixture' : connectorRuntime.isHttpConfigured(platform) ? 'official_api' : connectorRuntime.isOAuthConfigured(platform) ? 'oauth_only' : 'not_configured',
      oauthConfigured: connectorRuntime.isOAuthConfigured(platform),
      httpConfigured: connectorRuntime.isHttpConfigured(platform),
      credentialProviderConfigured: vaultConfigured,
      ready: fixturePlatformEnabled(platform) || connectorRuntime.canRead(platform),
      reasons: fixturePlatformEnabled(platform) ? [] : readiness.reasons,
    }]
  }))
  const nextActions: string[] = []
  if (fixtureMode) nextActions.push('当前是 Codex 本地 fixture 模式；如要操作真实店铺，请关闭 CONNECTOR_FIXTURE_MODE 并配置六个平台的官方 OAuth、API endpoint、scope 和回调地址；小红书/抖音在 readiness 完成前保持 fixture/API 或只读')
  if (production && !relayGate.ready) nextActions.push('配置 HTTPS MODEL_RELAY_BASE_URL 和平台托管的 MODEL_RELAY_API_KEY；生产模型 token 只允许经自有中转站转发')
  if (!contentProviderConfigured) nextActions.push('配置平台模型中转站、AI_MODEL 和平台密钥后，启用真实商品文案生成；当前文案生成使用本地规则/fixture 回退')
  if (!imageProviderConfigured) nextActions.push('配置平台模型中转站、IMAGE_MODEL 和平台密钥后，启用真实商品主图生成；当前主图生成使用本地规则/fixture 回退')
  if (!imageEditProviderConfigured) nextActions.push('配置 IMAGE_EDIT_MODEL（或复用 IMAGE_MODEL）和图片编辑中转 provider 后启用局部图片编辑；未配置时保留原图并阻断编辑请求')
  if (!imageFactsConfigured) nextActions.push('配置平台模型中转站、MODEL_RELAY_API_KEY 和 OCR_MODEL 后启用图片 OCR 候选；未配置时继续要求商家人工确认图片事实')
  if (!videoProviderConfigured) nextActions.push('配置平台模型中转站、MODEL_RELAY_API_KEY、VIDEO_MODEL 和视频 provider 后启用视频渲染；未配置时只能生成无渲染分镜')
  if (!modelCostGateConfigured) nextActions.push('配置平台模型 RPM、TPM 和每日人民币成本上限；成本门禁未通过时生产模型请求保持阻断')
  if (production && !paymentReadiness.ready) nextActions.push('配置支付宝/微信服务端 checkout provider、商户号、回调验签、对账和退款能力：' + paymentReadiness.reasons.join('、'))
  if (!vaultConfigured) nextActions.push('配置 VAULT_ADDR 和 VAULT_TOKEN（或接入外部凭据服务），让 Codex 安全读取商家授权凭据；不要把平台 token 放进插件参数')
  if (!objectStorageConfigured) nextActions.push('配置生产对象存储 bucket、region、HTTPS endpoint 和 KMS key，素材上传才可切换到云端持久化')
  if (!dataLifecycle.configured) nextActions.push('补齐生产数据生命周期、对象版本化和告警通道配置；缺少删除与告警门禁时禁止接收真实商家数据')
  if (!capabilityEvidence.configured) nextActions.push('运营后台未检测到通过发布门禁的平台 capability 证据（六平台范围）；example、fixture 或 test_e2e 证据不能标记生产可写，社交平台未就绪时必须保持 fixture/API 或只读')
  if (!capacityEvidence.configured) nextActions.push('运营后台未检测到通过真实云门禁的容量报告；必须绑定 release、profile、云环境、零 mock 和签署人')
  if (!production) nextActions.push('当前不是生产模式；上线前还需完成真实平台 canary、TLS/DNS/WAF、备份恢复、监控告警和容量压测')
  return {
    mode: production ? 'production' : fixtureMode ? 'fixture' : 'local',
    ai: { ownership: 'platform', userKeyRequired: false, relay: { configured: relayGate.ready, host: relayGate.endpointHost ?? null }, contentGeneration: contentProviderConfigured ? 'configured' : fixtureMode ? 'fixture_fallback' : 'not_configured', imageGeneration: imageProviderConfigured ? 'configured' : fixtureMode ? 'fixture_fallback' : 'not_configured', imageEditing: imageEditProviderConfigured ? 'configured' : 'blocked', imageFacts: imageFactsConfigured ? 'configured' : 'manual_fallback', videoRendering: videoProviderConfigured ? 'configured' : 'storyboard_only', costGate: modelCostGateConfigured ? 'ready' : 'blocked' },
    modelReadiness: {
      text: { ...evaluatePlatformModelGate(process.env, 'text'), providerConfigured: contentProviderConfigured },
      image: { ...evaluatePlatformModelGate(process.env, 'image'), providerConfigured: imageProviderConfigured },
      image_edit: { ...imageEditModelGate, providerConfigured: imageEditProviderConfigured },
      ocr: { ...ocrModelGate, providerConfigured: imageFactsConfigured },
      video: { ...videoModelGate, providerConfigured: videoProviderConfigured },
    },
    objectStorage: { configured: objectStorageConfigured, mode: localAcceptanceObjectStorage ? 'local_acceptance_durable' : production ? 's3_compatible' : 'local' },
    alertNotifications: alertNotificationReadiness(),
    dataLifecycle,
    productionEvidence: { capability: capabilityEvidence, capacity: capacityEvidence },
    credentialProvider: { configured: vaultConfigured, mode: fixtureMode ? 'fixture' : vaultConfigured ? 'vault_or_external' : 'none' },
    platforms: platformDiagnostics,
    payment: { mode: process.env.PAYMENT_MODE === 'provider' ? 'provider' : 'fixture', configured: process.env.PAYMENT_MODE === 'provider' && paymentReadiness.ready, reasons: paymentReadiness.reasons },
    productionGate: production && !fixtureMode && relayGate.ready && paymentReadiness.ready && Object.values(platformDiagnostics).every(item => item.ready) && contentProviderConfigured && imageProviderConfigured && imageEditProviderConfigured && imageFactsConfigured && videoProviderConfigured && modelCostGateConfigured && objectStorageConfigured && vaultConfigured && dataLifecycle.configured && capabilityEvidence.configured && capacityEvidence.configured,
    nextActions,
  }
}

function billingCapabilityEntitlements(input: {
  balanceFen: number
  usage: { remainingTasks: number } | undefined
  setup: ReturnType<typeof setupDiagnostics>
  stores: ReturnType<typeof workspaceStoreDirectory>
}) {
  const walletReady = input.balanceFen > 0
  const quotaReady = Boolean(input.usage && input.usage.remainingTasks > 0)
  const generationReady = walletReady && input.setup.ai.contentGeneration !== 'not_configured'
  const readableStore = input.stores.find(store => store.readable)
  const writableStore = input.stores.find(store => store.readable && store.writeEnabled)
  const publishReady = walletReady && Boolean(writableStore)
  const blocked = (reason: string, code: string) => ({ state: 'blocked' as const, label: reason, reason, code })
  return {
    balance: { state: walletReady ? 'available' as const : 'recharge_required' as const, label: walletReady ? '余额可用' : '需要充值', value_cny: (input.balanceFen / 100).toFixed(2), reason: walletReady ? '充值已到账，可用于钱包结算' : '充值到账后才会开放付费能力', code: walletReady ? null : 'wallet_balance' },
    package_quota: quotaReady
      ? { state: 'available' as const, label: '套餐额度可用', remaining: input.usage!.remainingTasks, reason: '优先消耗套餐内模型行动额度', code: null }
      : { state: 'exhausted' as const, label: '套餐额度已用尽', remaining: input.usage?.remainingTasks ?? null, reason: walletReady ? '后续模型行动将按钱包余额结算' : '可查看套餐信息；生成能力仍需钱包到账', code: 'package_quota' },
    generation: generationReady
      ? { state: 'available' as const, label: '生成能力可用', reason: '仍需素材事实、权益和人工确认门禁', code: null }
      : blocked(!walletReady ? '钱包余额未到账' : '模型中转或内容生成配置未就绪', !walletReady ? 'wallet_balance' : 'model_configuration'),
    platform_publish: publishReady
      ? { state: 'available' as const, label: '平台发布可用', platform: writableStore!.platform, store: writableStore!.label, reason: '发布前仍需商品范围、内容审核和交互确认', code: null }
      : blocked(!walletReady ? '钱包余额未到账' : !readableStore ? '没有可读取的已授权店铺' : '已授权店铺尚未通过平台写入门禁', !walletReady ? 'wallet_balance' : !readableStore ? 'store_authorization' : 'platform_write_readiness'),
  }
}

function commercialEntitlementProjection(subscription: Awaited<ReturnType<SubscriptionRepository['get']>>) {
  const source = {
    workspaceId: subscription.workspaceId,
    status: subscription.status,
    planCode: subscription.planCode,
    planName: subscription.planName,
    billingCycle: subscription.billingCycle,
    priceCny: subscription.priceCny,
    includedStores: subscription.includedStores,
    includedTasks: subscription.includedTasks,
    currentPeriodStart: subscription.currentPeriodStart,
    currentPeriodEnd: subscription.currentPeriodEnd,
    revision: subscription.revision,
    updatedAt: subscription.updatedAt,
  }
  return projectCommercialEntitlement({
    plan: subscription.planCode,
    period: { start: subscription.currentPeriodStart, end: subscription.currentPeriodEnd },
    status: subscription.status,
    sourceVersion: `${subscription.planCode}:revision-${subscription.revision}`,
    checksum: createHash('sha256').update(canonicalJson(source)).digest('hex'),
  })
}

function runtimeHealth() {
  const base = service.health()
  const configuredRateLimit = Number(process.env.API_RATE_LIMIT_PER_MINUTE ?? DEFAULT_RATE_LIMIT)
  const configuredOpsRateLimit = Number(process.env.OPS_API_RATE_LIMIT_PER_MINUTE ?? 600)
  return {
    ...base,
    capacity: {
      maxActiveJobsPerWorkspace,
      apiRateLimitPerMinute: Number.isFinite(configuredRateLimit) && configuredRateLimit > 0 ? configuredRateLimit : DEFAULT_RATE_LIMIT,
      opsApiRateLimitPerMinute: Number.isFinite(configuredOpsRateLimit) && configuredOpsRateLimit > 0 ? configuredOpsRateLimit : 600,
      rateLimitScope: 'workspace_actor',
      rateLimit: { mode: redisRateLimitHealth.state === 'ready' ? 'redis_atomic' : 'process_local', ...redisRateLimitHealth },
      jobAdmission: redisJobAdmission ? 'redis_atomic' : 'process_local',
    },
    writesEnabled: SUPPORTED_PLATFORMS.some(platform => platformWriteReady(platform)),
      setup: setupDiagnostics(),
    connectors: {
      ...base.connectors,
      jd: connectorRuntime.isOAuthConfigured('jd') ? 'configured_provider_required' : base.connectors.jd,
      taobao: connectorRuntime.isOAuthConfigured('taobao') ? 'configured_provider_required' : base.connectors.taobao,
      tmall: connectorRuntime.isOAuthConfigured('tmall') ? 'configured_provider_required' : base.connectors.tmall,
      pinduoduo: connectorRuntime.isOAuthConfigured('pinduoduo') ? 'configured_provider_required' : base.connectors.pinduoduo,
    },
  }
}

function workspacePlatformStatus(workspaceId: string) {
  const registered = service.listPlatformAccounts(workspaceId)
  return SUPPORTED_PLATFORMS.flatMap(platform => {
    const accounts = registered.filter(item => item.platform === platform)
    const rows = accounts.length ? accounts : [undefined]
    return rows.map(account => {
      const state = account ? account.tokenState : fixturePlatformEnabled(platform) ? 'fixture_ready' : connectorRuntime.isOAuthConfigured(platform) && !connectorRuntime.credentialProviderConfigured ? 'configured_provider_required' : connectorRuntime.isHttpConfigured(platform) ? 'configured' : connectorRuntime.isOAuthConfigured(platform) ? 'oauth_configured' : 'not_configured'
      return {
        platform,
        state,
        ...(account ? { accountId: account.id, ...(account.storeAlias ? { storeAlias: account.storeAlias } : {}) } : {}),
        ...platformAccessFlags(platform, account),
        readiness: workspaceConnectorReadiness(platform),
        capabilityEvidenceKind: 'application_profile',
        capabilities: connectorRuntime.capabilityMatrix(platform).map(item => ({ capability: item.capability, state: item.state, ...(item.verifiedAt ? { verifiedAt: item.verifiedAt } : {}) })),
      }
    })
  })
}

type PublicPlatformAccount = Omit<PlatformAccount, 'credentialRef'> & { credentialRef?: undefined }

function publicAuthorization(account: PublicPlatformAccount, simulated: boolean) {
  const expiresAt = account.accessTokenExpiresAt
  const expiry = expiresAt ? Date.parse(expiresAt) : Number.NaN
  const lastKnownExpiryState = !Number.isFinite(expiry) ? 'unknown' : expiry <= Date.now() ? 'expired' : 'valid'
  return {
    state: simulated ? 'fixture' : account.tokenState,
    reauthorizationRequired: account.tokenState === 'refresh_required' || account.tokenState === 'revoked',
    grantedScopes: account.grantedScopes ?? [],
    scopeState: account.grantedScopes?.length ? 'reported_by_provider' : 'unknown',
    lastKnownAccessTokenExpiresAt: expiresAt ?? null,
    lastKnownExpiryState,
    renewalMode: account.credentialRefreshable === true ? 'automatic' : account.credentialRefreshable === false ? 'reauthorize' : 'unknown',
    refreshSupported: account.credentialRefreshable ?? null,
    lastAuthorizedAt: account.lastAuthorizedAt ?? null,
    metadataObservedAt: account.credentialMetadataObservedAt ?? null,
    metadataFreshness: account.credentialMetadataObservedAt ? 'last_known' : 'unknown',
    stateChangedAt: account.tokenStateUpdatedAt ?? null,
    revokedAt: account.revokedAt ?? null,
  }
}

export function workspaceStoreDirectory(workspaceId: string, platformFilter?: Platform) {
  const products = service.listProducts(workspaceId)
  const syncJobs = service.listSyncJobs(workspaceId)
  const productsByAccount = new Map<string, { storeNames: Set<string>; hasFixtureProduct: boolean }>()
  for (const product of products) {
    if (!product.accountId) continue
    const key = `${product.platform}:${product.accountId}`
    const grouped = productsByAccount.get(key) ?? { storeNames: new Set<string>(), hasFixtureProduct: false }
    if (product.storeName) grouped.storeNames.add(product.storeName)
    if (product.source === 'fixture') grouped.hasFixtureProduct = true
    productsByAccount.set(key, grouped)
  }
  const syncByAccount = new Map<string, { latestAttempt?: typeof syncJobs[number]; lastSuccessful?: typeof syncJobs[number]; lastUsable?: typeof syncJobs[number] }>()
  for (const job of syncJobs) {
    const key = `${job.platform}:${job.accountId}`
    const current = syncByAccount.get(key) ?? {}
    if (!current.latestAttempt || job.updatedAt > current.latestAttempt.updatedAt) current.latestAttempt = job
    if (job.state === 'succeeded' && (!current.lastSuccessful || job.updatedAt > current.lastSuccessful.updatedAt)) current.lastSuccessful = job
    if ((job.state === 'succeeded' || job.state === 'partial') && (!current.lastUsable || job.updatedAt > current.lastUsable.updatedAt)) current.lastUsable = job
    syncByAccount.set(key, current)
  }
  return service.listPlatformAccounts(workspaceId)
    .filter(account => !platformFilter || account.platform === platformFilter)
    .map(account => {
      const productGroup = productsByAccount.get(`${account.platform}:${account.id}`)
      const names = [...(productGroup?.storeNames ?? [])].sort()
      const storeName = names.length === 1 ? names[0]! : names.length > 1 ? `${names[0]} 等 ${names.length} 个店铺名` : undefined
      const state = account.tokenState
      const access = platformAccessFlags(account.platform, account)
      const simulated = fixtureMode || Boolean(productGroup?.hasFixtureProduct)
      const dataMode = simulated ? 'fixture' : access.readEnabled ? 'official_api' : 'account_record_only'
      const syncGroup = syncByAccount.get(`${account.platform}:${account.id}`)
      const latestAttempt = syncGroup?.latestAttempt
      const lastSuccessful = syncGroup?.lastSuccessful
      const lastUsable = syncGroup?.lastUsable
      return {
        platform: account.platform,
        accountId: account.id,
        ...(account.storeAlias ? { alias: account.storeAlias } : {}),
        ...(storeName ? { storeName } : {}),
        label: account.storeAlias ?? storeName ?? `${account.platform} 店铺`,
        state,
        dataMode,
        readable: account.tokenState === 'connected' && (access.readEnabled || simulated),
        writeEnabled: access.writeEnabled,
        authorization: publicAuthorization(account, simulated),
        sync: {
          latestState: latestAttempt?.state ?? null,
          lastAttemptAt: latestAttempt?.updatedAt ?? null,
          lastSuccessfulAt: lastSuccessful?.updatedAt ?? null,
          lastUsableAt: lastUsable?.updatedAt ?? null,
          failedItems: latestAttempt?.itemsFailed ?? 0,
        },
        revision: account.revision,
      }
    })
    .sort((left, right) => `${left.platform}:${left.label}:${left.accountId}`.localeCompare(`${right.platform}:${right.label}:${right.accountId}`))
}

function platformAccountAccessItems(workspaceId: string, platformFilter?: Platform) {
  const registered = service.listPlatformAccounts(workspaceId)
  const directory = new Map(workspaceStoreDirectory(workspaceId).map(store => [`${store.platform}:${store.accountId}`, store]))
  return SUPPORTED_PLATFORMS
    .filter(platform => !platformFilter || platform === platformFilter)
    .flatMap(platform => {
      const accounts = registered.filter(item => item.platform === platform)
      const rows = accounts.length ? accounts : [undefined]
      return rows.map(account => {
        if (account) return { ...directory.get(`${platform}:${account.id}`), ...platformAccessFlags(platform, account), readiness: workspaceConnectorReadiness(platform) }
        return { platform, state: fixturePlatformEnabled(platform) ? 'fixture_ready' : connectorRuntime.isOAuthConfigured(platform) && !connectorRuntime.credentialProviderConfigured ? 'configured_provider_required' : connectorRuntime.isHttpConfigured(platform) ? 'configured' : connectorRuntime.isOAuthConfigured(platform) ? 'oauth_configured' : 'not_configured', ...platformAccessFlags(platform, account), readiness: workspaceConnectorReadiness(platform) }
      })
    })
}

function merchantPlatformOptions(workspaceId: string, directory = workspaceStoreDirectory(workspaceId)) {
  return SUPPORTED_PLATFORMS.map(platform => {
    const stores = directory.filter(store => store.platform === platform)
    const readiness = workspaceConnectorReadiness(platform)
    const realStore = stores.find(store => store.state === 'connected' && store.dataMode === 'official_api' && store.readable)
    const demoStore = stores.find(store => store.dataMode === 'fixture')
    const state = realStore ? 'connected' : demoStore ? 'demo' : readiness.ready ? 'available' : 'not_configured'
    const mode = realStore ? '真实授权' : demoStore ? '本地演示' : readiness.ready ? '可授权' : '待配置'
    return {
      platform,
      label: PLATFORM_LABELS[platform],
      state,
      mode,
      storeCount: stores.length,
      action: 'platform.connect',
      cta: state === 'connected' ? `管理${PLATFORM_LABELS[platform]}店铺` : `连接${PLATFORM_LABELS[platform]}`,
      nextAction: state === 'not_configured' ? '等待平台官方接口配置' : `选择${PLATFORM_LABELS[platform]}店铺并授权`,
      readiness: { ready: readiness.ready, reasons: readiness.reasons, mediaUpload: readiness.mediaUpload },
    }
  })
}

function workspaceOnboarding(workspaceId: string, directory = workspaceStoreDirectory(workspaceId)) {
  const products = service.listProducts(workspaceId)
  const selectedStoreKeys = new Set(directory.map(store => `${store.platform}:${store.accountId}`))
  const boundProducts = products.filter(product => product.accountId ? selectedStoreKeys.has(`${product.platform}:${product.accountId}`) : false)
  const assets = service.listAssets(workspaceId)
  const tasks = service.listTasks(workspaceId).filter(task => task.accountId ? selectedStoreKeys.has(`${task.platform}:${task.accountId}`) : false)
  const confirmedProducts = boundProducts.filter(product => product.factsConfirmed)
  const readyAssets = assets.filter(asset => assetReadiness(asset).status === 'ready')
  const deliverableTasks = tasks.filter(task => service.listContentVersions(workspaceId, task.id).some(version => version.state === 'approved' || version.state === 'delivered'))
  const steps = [
    { id: 'workspace', title: '工作区', summary: '当前工作区已建立，后续状态按工作区隔离', state: 'complete', entryMethod: 'workspace.health', nextMethod: 'workspace.health' },
    { id: 'bind-store', title: '连接店铺', summary: directory.length ? `已绑定 ${directory.length} 家店铺` : '先选择平台并完成官方授权', state: directory.length ? 'complete' : 'required', entryMethod: 'platform.connect', nextMethod: 'platform.connect' },
    { id: 'choose-product', title: '选择商品', summary: boundProducts.length ? `已找到 ${boundProducts.length} 个商品，可按店铺选择` : '绑定店铺后同步或导入商品', state: !directory.length ? 'blocked' : boundProducts.length ? 'next' : 'required', entryMethod: 'catalog.search', nextMethod: 'catalog.search' },
    { id: 'add-assets', title: '上传素材与资料', summary: readyAssets.length ? `已确认 ${readyAssets.length} 份可用素材` : assets.length ? '素材已上传，仍需完成扫描、权益和事实确认' : '添加商品图片、品牌资料和知识库文件', state: !boundProducts.length ? 'blocked' : readyAssets.length ? 'complete' : assets.length ? 'next' : 'required', entryMethod: 'asset.upload', nextMethod: 'asset.upload' },
    { id: 'start-content', title: '生成并审核', summary: deliverableTasks.length ? `已有 ${deliverableTasks.length} 个内容交付` : confirmedProducts.length ? '商品事实已确认，可以开始文案、主图或视频分镜' : '先确认商品、价格、库存和图片事实', state: !boundProducts.length || !confirmedProducts.length ? 'blocked' : deliverableTasks.length ? 'complete' : 'next', entryMethod: 'task.understand', nextMethod: 'task.understand' },
    { id: 'publish', title: '发布', summary: deliverableTasks.length ? '已有已批准交付物，可查看发布前预检' : '完成内容审核后才能进入发布预检', state: deliverableTasks.length ? 'next' : 'blocked', entryMethod: 'publish.prepare', nextMethod: 'publish.prepare' },
  ] as const
  const current = steps.find(step => step.state === 'required' || step.state === 'next' || step.state === 'blocked') ?? steps.at(-1)!
  return { steps, currentStep: { id: current.id, title: current.title, state: current.state, entryMethod: current.entryMethod }, summary: { stores: directory.length, products: boundProducts.length, unboundProducts: products.length - boundProducts.length, confirmedProducts: confirmedProducts.length, assets: assets.length, readyAssets: readyAssets.length, tasks: tasks.length, deliverableTasks: deliverableTasks.length } }
}

function onboardingPrimaryAction(step: ReturnType<typeof workspaceOnboarding>['steps'][number]) {
  const actions: Record<string, { label: string; requiredInputs: string[] }> = {
    'workspace.health': { label: '查看工作区状态', requiredInputs: [] },
    'platform.connect': { label: '连接平台店铺', requiredInputs: ['platform'] },
    'catalog.search': { label: '选择或导入商品', requiredInputs: ['platform', 'account_id'] },
    'asset.upload': { label: '上传商品资料', requiredInputs: ['name', 'mime_type'] },
    'task.understand': { label: '开始生成并审核', requiredInputs: ['instruction', 'platform', 'account_id', 'product_id'] },
    'publish.prepare': { label: '查看发布预览', requiredInputs: ['task_id'] },
  }
  const action = actions[step.nextMethod] ?? { label: step.title, requiredInputs: [] }
  return { method: step.nextMethod, label: action.label, required_inputs: action.requiredInputs }
}

function merchantOnboardingProjection(onboarding: ReturnType<typeof workspaceOnboarding>, sideEffects: 'none' | 'merchant_intent_recorded' | 'workspace_bootstrapped' = 'none') {
  const stepId: Record<string, string> = { workspace: 'workspace', 'bind-store': 'connect_store', 'choose-product': 'select_product', 'add-assets': 'add_assets', 'start-content': 'generate_review', publish: 'publish' }
  const toState = (state: string) => state === 'next' ? 'required' : state
  const steps = onboarding.steps.map(step => ({ id: stepId[step.id] ?? step.id, title: step.title, state: toState(step.state), summary: step.summary, entry_method: step.entryMethod, primary_action: onboardingPrimaryAction(step) }))
  const current = onboarding.steps.find(step => step.id === onboarding.currentStep.id) ?? onboarding.steps[0]!
  return {
    schema_version: '2',
    source: 'server',
    side_effects: sideEffects,
    current_step: { id: stepId[current.id] ?? current.id, title: current.title, state: toState(current.state), summary: current.summary, primary_action: onboardingPrimaryAction(current) },
    steps,
  }
}

function merchantCapabilityCardAction(card: typeof MERCHANT_CAPABILITY_CARDS[number], onboarding: ReturnType<typeof workspaceOnboarding>, directory: ReturnType<typeof workspaceStoreDirectory>) {
  const { summary } = onboarding
  if (card.id === 'first-value') return { method: 'merchant.first_value', arguments: { example: 'true' }, required_inputs: [], reason: '先查看安全示例预览；如需真实商品，请先选择 platform + account_id + product_id', blocked_by: [] as string[] }
  if (card.id === 'stores-products') {
    if (!directory.length) return { method: 'platform.connect', arguments: {}, required_inputs: ['platform'], reason: '先绑定一个平台店铺', blocked_by: [] as string[] }
    return { method: 'catalog.search', arguments: { scope: 'store' }, required_inputs: ['platform', 'account_id'], reason: '先选择具体平台和店铺，再查看商品', blocked_by: summary.products ? [] : ['product_sync_or_import'] }
  }
  if (card.id === 'knowledge-assets') {
    return { method: 'asset.list', arguments: {}, required_inputs: [], reason: summary.products ? '查看素材状态；没有素材时再上传商品图片或品牌资料' : '先选择店铺商品', blocked_by: summary.products ? [] : ['store_product_selection'] }
  }
  if (card.id === 'content') {
    if (!summary.products) return { method: 'catalog.search', arguments: { scope: 'store' }, required_inputs: ['platform', 'account_id'], reason: '先选择商品', blocked_by: ['store_product_selection'] }
    return { method: 'task.understand', arguments: {}, required_inputs: ['instruction', 'platform', 'account_id'], reason: summary.confirmedProducts ? '输入一句营销目标，开始创建内容任务' : '先确认商品、价格、库存和图片事实', blocked_by: summary.confirmedProducts ? [] : ['product_facts_confirmation'] }
  }
  if (card.id === 'visuals') return { method: 'catalog.image.generate', arguments: {}, required_inputs: ['product_id', 'platform', 'account_id'], reason: '选择商品后生成图片候选；结果仍需审核', blocked_by: summary.products ? [] : ['store_product_selection'] }
  if (card.id === 'review-publish') return { method: summary.tasks ? 'publish.prepare' : 'task.history', arguments: {}, required_inputs: summary.tasks ? ['task_id'] : [], reason: summary.tasks ? '先查看发布前检查和店铺范围' : '先创建内容任务', blocked_by: summary.tasks ? [] : ['content_task'] }
  if (card.id === 'bulk-publish') return { method: 'publish.batch.prepare', arguments: {}, required_inputs: ['task_ids_json'], reason: '批量发布前逐项预检和确认；每个任务仍需独立确认哈希', blocked_by: summary.tasks ? [] : ['content_task'] }
  if (card.id === 'rules') return { method: 'rule.sync.status', arguments: {}, required_inputs: [], reason: '先查看六个平台规则是否新鲜，再按店铺平台查看适用规则；生成和审核会自动执行同一平台预检', blocked_by: [] as string[] }
  return { method: card.entryMethod, arguments: {}, required_inputs: [], reason: '查看套餐、模型成本和充值到账状态', blocked_by: [] as string[] }
}

const JOB_ADMISSION_TTL_SECONDS = 24 * 60 * 60

async function reserveDistributedJobSlot(workspaceId: string, reservationId: string) {
  if (!redisJobAdmission) return false
  try {
    const acquired = await redisJobAdmission.acquire(workspaceId, reservationId, Math.max(1, Math.floor(maxActiveJobsPerWorkspace)), JOB_ADMISSION_TTL_SECONDS)
    if (acquired === 'quota') throw new DomainError('WORKSPACE_JOB_QUOTA_EXCEEDED', '当前工作区已有较多任务排队，请稍后重试', 429, { limit: maxActiveJobsPerWorkspace, retry_after_seconds: 5 })
    if (acquired === 'existing') throw new DomainError('IDEMPOTENCY_IN_PROGRESS', '相同幂等键的任务正在其他服务实例创建，请稍后重试', 409, { retry_after_seconds: 1 })
    return true
  } catch (error) {
    if (error instanceof DomainError) throw error
    // Redis is an acceleration/coordination dependency. The in-process domain
    // quota remains active and health exposes this degraded mode.
    return false
  }
}

async function hydrateDurableIdempotentJob(workspaceId: string, entityType: 'publish_job' | 'generation_job', idempotencyKey: string) {
  await persistenceReady
  const snapshot = await persistence.business?.findByIdempotencyKey(workspaceId, entityType, idempotencyKey)
  if (snapshot) service.hydrateSnapshot({ entityType, entity: snapshot.payload })
  return snapshot?.payload
}

async function waitForDurableIdempotentJob(workspaceId: string, entityType: 'publish_job' | 'generation_job', idempotencyKey: string) {
  for (let attempt = 0; attempt < 20; attempt += 1) {
    const payload = await hydrateDurableIdempotentJob(workspaceId, entityType, idempotencyKey)
    if (payload) return payload
    await new Promise(resolve => setTimeout(resolve, 50))
  }
  throw new DomainError('IDEMPOTENCY_IN_PROGRESS', '相同幂等键的任务正在其他服务实例创建，请稍后重试', 409, { retry_after_seconds: 1 })
}

async function releaseDistributedJobSlot(workspaceId: string, reservationId: string) {
  if (!redisJobAdmission) return
  try { await redisJobAdmission.release(workspaceId, reservationId) } catch { /* health/alerts surface Redis degradation */ }
}

/** Shared OAuth initiation path for REST and MCP so both surfaces enforce the
 * same redirect, PKCE, state and connector readiness rules. */
const oauthRedirectEnv: Record<Platform, string> = {
  jd: 'JD_OAUTH_REDIRECT_URI',
  taobao: 'TAOBAO_OAUTH_REDIRECT_URI',
  tmall: 'TMALL_OAUTH_REDIRECT_URI',
  pinduoduo: 'PDD_OAUTH_REDIRECT_URI',
  xiaohongshu: 'XHS_OAUTH_REDIRECT_URI',
  douyin: 'DOUYIN_OAUTH_REDIRECT_URI',
}

export function configuredOAuthRedirectUri(platform: Platform, source: NodeJS.ProcessEnv = process.env): string | undefined {
  const platformUri = source[oauthRedirectEnv[platform]]?.trim()
  if (platformUri) return platformUri
  const shared = source.PUBLIC_OAUTH_REDIRECT_URI?.trim()
  if (!shared) return undefined
  if (shared.includes('{platform}')) return shared.replaceAll('{platform}', platform)
  try {
    const parsed = new URL(shared)
    const path = parsed.pathname.replace(/\/+$/u, '')
    if (path.endsWith('/v1/oauth/callback')) {
      parsed.pathname = `${path}/${platform}`
      return parsed.toString()
    }
  } catch { /* validated by the authorization boundary below */ }
  return shared
}

function validOAuthRedirectUri(platform: Platform, redirectUri: string): boolean {
  try {
    const parsed = new URL(redirectUri)
    return parsed.protocol === 'https:' && parsed.pathname.replace(/\/+$/u, '') === `/v1/oauth/callback/${platform}`
  } catch { return false }
}

async function beginPlatformAuthorization(req: IncomingMessage, platform: Platform, input: JsonObject, workspaceId: string) {
  if (isProduction() && !redisOAuthPort) throw new DomainError('OAUTH_STATE_STORE_UNAVAILABLE', '生产 OAuth 必须配置 Redis 状态存储，禁止使用单副本内存状态', 503)
  const actorId = requestActor(req, typeof input.actor_id === 'string' && input.actor_id.trim() ? input.actor_id.trim() : 'actor_demo')
  const codeVerifier = randomBytes(32).toString('base64url')
  const state = await oauthStateStore.issue({ workspaceId, actorId, platform, codeVerifier, codeChallenge: hashPkceVerifier(codeVerifier) })
  const configuredRedirectUri = configuredOAuthRedirectUri(platform)
  const requestedRedirectUri = typeof input.redirect_uri === 'string' ? input.redirect_uri.trim() : undefined
  const redirectUri = isProduction() ? configuredRedirectUri ?? '' : requestedRedirectUri ?? configuredRedirectUri ?? 'http://127.0.0.1:8787/oauth/callback'
  if (!redirectUri || (isProduction() && (!validOAuthRedirectUri(platform, redirectUri) || (requestedRedirectUri && requestedRedirectUri !== configuredRedirectUri)))) throw new DomainError('OAUTH_REDIRECT_URI_REQUIRED', `生产 OAuth 必须配置匹配 ${platform} 回调路由的 HTTPS ${oauthRedirectEnv[platform]}（或 PUBLIC_OAUTH_REDIRECT_URI 模板）`, 503)
  if (!platformAuthorizationConfigured(platform)) throw new DomainError('NOT_CONFIGURED', `${platform} 官方 OAuth 尚未配置`, 503)
  const result = await connectorRuntime.connector(platform).authorize({ workspaceId, actorId, redirectUri, state, codeVerifier })
  if (!result.ok) throw new DomainError(result.code ?? 'NOT_CONFIGURED', result.message ?? '平台官方 API 尚未配置', 503)
  return result
}

async function enforceRateLimit(req: IncomingMessage, workspaceId: string) {
  if (req.method === 'OPTIONS' || req.url?.startsWith('/healthz')) return
  const principal = requestPrincipals.get(req)
  const operationsPrincipal = Boolean(principal && authorizedRoles(principal).some(role => ['platform_ops', 'finance'].includes(role)))
  const configuredLimit = Number(operationsPrincipal ? process.env.OPS_API_RATE_LIMIT_PER_MINUTE ?? 600 : process.env.API_RATE_LIMIT_PER_MINUTE ?? DEFAULT_RATE_LIMIT)
  const limit = Number.isFinite(configuredLimit) && configuredLimit > 0 ? configuredLimit : operationsPrincipal ? 600 : DEFAULT_RATE_LIMIT
  const rateScope = `${workspaceId}:${principal?.actorId || 'anonymous'}:${operationsPrincipal ? 'ops' : 'merchant'}`
  const now = Date.now()
  if (redisRateLimit) {
    try {
      const window = Math.floor(now / 60_000)
      const count = await redisRateLimit.increment(`merchant:rate:${rateScope}:${window}`, 120)
      redisRateLimitHealth = { state: 'ready' }
      if (count > limit) {
        const retryAfterSeconds = Math.max(1, 60 - Math.floor((now % 60_000) / 1_000))
        throw new DomainError(ERROR_CODES.RATE_LIMITED, '请求频率超过当前工作区限制', 429, { retry_after_seconds: retryAfterSeconds })
      }
      return
    } catch (error) {
      if (error instanceof DomainError) throw error
      redisRateLimitHealth = { state: 'degraded', lastFailureAt: new Date().toISOString() }
      // Redis outage falls back to the process bucket so a transient cache
      // failure does not take down the API; production health/alerts must
      // surface this degraded protection state.
    }
  }
  const current = rateBuckets.get(rateScope)
  if (!current || now - current.windowStartedAt >= 60_000) {
    rateBuckets.set(rateScope, { windowStartedAt: now, count: 1 })
    return
  }
  current.count += 1
  if (current.count > limit) {
    const retryAfterSeconds = Math.max(1, Math.ceil((60_000 - (now - current.windowStartedAt)) / 1_000))
    throw new DomainError(ERROR_CODES.RATE_LIMITED, '请求频率超过当前工作区限制', 429, { retry_after_seconds: retryAfterSeconds })
  }
}

/** Resolve tenant scope from server identity; body/query values are only consistency checks. */
function resolveWorkspace(req: IncomingMessage, candidate?: unknown): string {
  const fromHeader = header(req, 'x-workspace-id')?.trim()
  if (!fromHeader && requiresStrictAuth()) throw new DomainError(ERROR_CODES.WORKSPACE_SCOPE_REQUIRED, '受控环境请求必须携带 X-Workspace-Id', 401)
  const workspaceId = fromHeader || 'ws_demo'
  knownWorkspaces.add(workspaceId)
  if (candidate !== undefined && candidate !== null && String(candidate) !== '' && String(candidate) !== workspaceId) {
    throw new DomainError(ERROR_CODES.WORKSPACE_SCOPE_MISMATCH, '请求工作区与身份工作区不一致', 403)
  }
  return workspaceId
}

function required(input: JsonObject, key: string): string {
  const value = input[key]
  if (typeof value !== 'string' || !value.trim()) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `缺少必填字段: ${key}`, 400)
  return value
}

function requiredOperationalReason(input: JsonObject): string {
  const reason = required(input, 'reason').trim()
  if (reason.length < 4 || reason.length > 500) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'reason 必须是 4 到 500 个字符', 400)
  return reason
}

function parseJsonObjectParameter(input: JsonObject, key: string): Record<string, unknown> {
  const encoded = required(input, key)
  try {
    const parsed: unknown = JSON.parse(encoded)
    if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('object required')
    return parsed as Record<string, unknown>
  } catch {
    throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是合法 JSON 对象`, 400)
  }
}

function parseJsonArrayParameter(input: JsonObject, key: string): unknown[] {
  const encoded = required(input, key)
  try {
    const parsed: unknown = JSON.parse(encoded)
    if (!Array.isArray(parsed)) throw new Error('array required')
    return parsed
  } catch {
    throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是合法 JSON 数组`, 400)
  }
}

function requiredPositiveInteger(input: JsonObject, key: string, allowZero = false) {
  const value = required(input, key)
  if (!/^\d+$/u.test(value)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是整数`, 400)
  const parsed = Number(value)
  if (!Number.isSafeInteger(parsed) || parsed < (allowZero ? 0 : 1)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 超出允许范围`, 400)
  return parsed
}

function platformGovernanceGatesRequired() {
  if (process.env.REQUIRE_PLATFORM_GOVERNANCE_GATES === 'true') return true
  if (process.env.REQUIRE_PLATFORM_GOVERNANCE_GATES === 'false') return false
  return isProduction() && persistence.mode === 'postgres'
}

function isObject(value: unknown): value is Record<string, unknown> {
  return Boolean(value) && typeof value === 'object' && !Array.isArray(value)
}

function boundedText(value: unknown, label: string, maxLength: number, required = false): string | undefined {
  if (value === undefined && !required) return undefined
  if (typeof value !== 'string' || !value.trim() || value.trim().length > maxLength) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${label}无效`, 400)
  return value.trim()
}

function readPlatformRejection(value: unknown): PlatformRejection | undefined {
  if (value === undefined) return undefined
  if (!isObject(value)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台拒绝详情无效', 400)
  const fieldsInput = value.fields
  if (!Array.isArray(fieldsInput) || fieldsInput.length > 50) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台字段错误必须为不超过 50 项的数组', 400)
  const fields = fieldsInput.map((field, index) => {
    if (!isObject(field)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `平台字段错误 ${index + 1} 无效`, 400)
    return {
      path: boundedText(field.path, `平台字段错误 ${index + 1} 路径`, 256, true)!,
      ...(field.raw_code === undefined ? {} : { rawCode: boundedText(field.raw_code, `平台字段错误 ${index + 1} 代码`, 128, true)! }),
      message: boundedText(field.message, `平台字段错误 ${index + 1} 原因`, 1000, true)!,
    }
  })
  return {
    rawCode: boundedText(value.raw_code, '平台拒绝代码', 128, true)!,
    ...(value.message === undefined ? {} : { message: boundedText(value.message, '平台拒绝原因', 2000, true)! }),
    fields,
  }
}

function readStaticBrief(value: unknown): StaticBrief | undefined {
  if (!value || typeof value !== 'object' || Array.isArray(value)) return undefined
  const brief = value as Record<string, unknown>
  const strings = ['platform', 'placement', 'targetDimensions', 'productImageGuidance', 'logoSafety', 'headline', 'subheadline', 'coreSellingPoint', 'cta', 'textDensity', 'safeArea']
  if (strings.some(key => typeof brief[key] !== 'string' || !(brief[key] as string).trim()) || !Array.isArray(brief.visualHierarchy) || !Array.isArray(brief.protectedAreas)) return undefined
  const visualHierarchy = brief.visualHierarchy.filter((item): item is string => typeof item === 'string' && item.trim().length > 0)
  const protectedAreas = brief.protectedAreas.filter((item): item is string => typeof item === 'string' && item.trim().length > 0)
  if (!visualHierarchy.length || !protectedAreas.length) return undefined
  return {
    platform: brief.platform as string, placement: brief.placement as string, targetDimensions: brief.targetDimensions as string,
    visualHierarchy, productImageGuidance: brief.productImageGuidance as string, logoSafety: brief.logoSafety as string,
    headline: brief.headline as string, subheadline: brief.subheadline as string, coreSellingPoint: brief.coreSellingPoint as string,
    ...(typeof brief.priceExpression === 'string' && brief.priceExpression.trim() ? { priceExpression: brief.priceExpression } : {}),
    cta: brief.cta as string, textDensity: brief.textDensity as string, safeArea: brief.safeArea as string, protectedAreas,
  }
}

export function readContentModules(value: unknown): ContentModule[] | undefined {
  if (value === undefined) throw new DomainError('CONTENT_SCHEMA_INVALID', '异步 worker 的 modules 必须包含 1 至 16 个合法模块', 400)
  if (!Array.isArray(value) || value.length === 0 || value.length > 16) throw new DomainError('CONTENT_SCHEMA_INVALID', '异步 worker 的 modules 必须包含 1 至 16 个合法模块', 400)
  try {
    return validateContentSchema({ title: 'worker-result', detail: 'worker-result', sellingPoints: ['worker-result'], modules: value }, '异步 worker', { requireDecisionContracts: true }).modules
  } catch (error) {
    throw new DomainError('CONTENT_SCHEMA_INVALID', error instanceof Error ? error.message : '异步 worker 的 modules 结构无效', 400)
  }
}

function reviewProductImagesForMcp(images: readonly string[] | undefined) {
  return reviewProductImages(images)
}

function parseImageListForMcp(value: unknown): string[] | undefined {
  if (typeof value !== 'string' || !value.trim()) return undefined
  const raw = value.trim()
  try {
    const parsed = JSON.parse(raw)
    if (Array.isArray(parsed) && parsed.every(item => typeof item === 'string')) {
      return parsed.map(item => item.trim()).filter(Boolean)
    }
  } catch {
    // Keep accepting the original comma-separated form for ordinary URLs.
  }
  // A data URI contains a comma between its media type and payload. Split only
  // at the boundary between multiple data URIs so Codex cannot turn one image
  // into IMAGE_URL_INVALID fragments during review.
  if (/^data:image\//iu.test(raw)) return raw.split(/,(?=data:image\/)/iu).map(item => item.trim()).filter(Boolean)
  return raw.split(',').map(item => item.trim()).filter(Boolean)
}

const GENERATED_IMAGE_MIME = new Map([
  ['image/png', 'png'], ['image/jpeg', 'jpg'], ['image/webp', 'webp'],
])
const MAX_ARCHIVED_IMAGE_BYTES = 15 * 1024 * 1024

function imageArtifactAllowedHosts(): readonly string[] | undefined {
  const configured = (process.env.IMAGE_ARTIFACT_ALLOWED_HOSTS ?? '').split(',').map(value => value.trim().toLowerCase()).filter(Boolean)
  if (configured.length) return configured
  if (process.env.NODE_ENV === 'production' || process.env.NODE_ENV === 'staging') throw new DomainError('IMAGE_ARTIFACT_ALLOWLIST_MISSING', '生产环境必须配置图片 artifact 域名白名单', 503)
  return undefined
}

// DashScope image artifacts are served from short-lived OSS URLs whose bucket
// names can rotate. Alibaba documents the host shape rather than a stable list
// of bucket names, so validate that narrow shape and then allow only the exact
// host observed on this request. This keeps the SSRF allowlist fail-closed
// without trusting all of aliyuncs.com.
export function trustedDashScopeImageArtifactHost(raw: string): string | undefined {
  let host: string
  try { host = new URL(raw).hostname.toLowerCase().replace(/\.$/u, '') } catch { return undefined }
  return /^dashscope-[a-z0-9-]{1,96}\.oss-(?:accelerate|cn-[a-z0-9-]{1,48})\.aliyuncs\.com$/u.test(host) ? host : undefined
}

async function imageArtifactBody(raw: string): Promise<{ mimeType: string; body: Uint8Array }> {
  const configuredHosts = imageArtifactAllowedHosts()
  const trustedDynamicHost = trustedDashScopeImageArtifactHost(raw)
  const allowedHosts = trustedDynamicHost ? [...(configuredHosts ?? []), trustedDynamicHost] : configuredHosts
  await assertOutboundUrl(raw, { environment: process.env.NODE_ENV, allowedHosts, resolveDns: true })
  const response = await fetch(raw, { method: 'GET', headers: { accept: 'image/png, image/jpeg, image/webp' }, redirect: 'error' })
  if (!response.ok || !response.body) throw new DomainError('IMAGE_ARTIFACT_DOWNLOAD_FAILED', `图片归档下载失败（HTTP ${response.status}）`, 502)
  const declaredSize = Number(response.headers.get('content-length') ?? '')
  if (Number.isFinite(declaredSize) && declaredSize > MAX_ARCHIVED_IMAGE_BYTES) throw new DomainError('GENERATED_IMAGE_TOO_LARGE', '生成图片超过归档大小限制', 413)
  const reader = response.body.getReader()
  const chunks: Uint8Array[] = []
  let total = 0
  try {
    while (true) {
      const next = await reader.read()
      if (next.done) break
      total += next.value.byteLength
      if (total > MAX_ARCHIVED_IMAGE_BYTES) throw new DomainError('GENERATED_IMAGE_TOO_LARGE', '生成图片超过归档大小限制', 413)
      chunks.push(next.value)
    }
  } finally { reader.releaseLock() }
  const body = new Uint8Array(total)
  let offset = 0
  for (const chunk of chunks) { body.set(chunk, offset); offset += chunk.byteLength }
  const mimeType = (response.headers.get('content-type')?.split(';', 1)[0] ?? '').trim().toLowerCase()
  if (!GENERATED_IMAGE_MIME.has(mimeType) || !generatedImageSignatureMatches(mimeType, body)) throw new DomainError('GENERATED_IMAGE_SIGNATURE_INVALID', '图片生成服务返回的 MIME 类型与文件内容不匹配', 502)
  return { mimeType, body }
}

const MAX_ARCHIVED_VIDEO_BYTES = 50 * 1024 * 1024
let videoArtifactFetcherForTests: typeof fetch | undefined

function videoArtifactAllowedHosts(): readonly string[] | undefined {
  const configured = (process.env.VIDEO_ARTIFACT_ALLOWED_HOSTS ?? '').split(',').map(value => value.trim().toLowerCase()).filter(Boolean)
  if (configured.length) return configured
  if (process.env.NODE_ENV === 'production' || process.env.NODE_ENV === 'staging') throw new DomainError('VIDEO_ARTIFACT_ALLOWLIST_MISSING', '生产环境必须配置视频 artifact 域名白名单', 503)
  return undefined
}

export async function assertVideoArtifactUrl(raw: string): Promise<void> {
  await assertOutboundUrl(raw, { environment: process.env.NODE_ENV, allowedHosts: videoArtifactAllowedHosts(), resolveDns: true })
}

export function setVideoArtifactFetcherForTests(fetcher?: typeof fetch) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('VIDEO_ARCHIVE_RUNTIME_TEST_ONLY')
  videoArtifactFetcherForTests = fetcher
}

export function videoSignatureMatches(mimeType: string, body: Uint8Array) {
  if (mimeType === 'video/mp4' || mimeType === 'video/quicktime') return body.length >= 12 && Buffer.from(body.slice(4, 8)).toString('ascii') === 'ftyp'
  if (mimeType === 'video/webm') return body.length >= 4 && body[0] === 0x1a && body[1] === 0x45 && body[2] === 0xdf && body[3] === 0xa3
  return false
}

async function readBoundedVideoBody(response: Response, limit: number) {
  if (!response.body) throw new DomainError('VIDEO_ARTIFACT_BODY_MISSING', '视频 provider 响应没有可读取的二进制内容', 502)
  const reader = response.body.getReader()
  const chunks: Uint8Array[] = []
  let total = 0
  try {
    while (true) {
      const next = await reader.read()
      if (next.done) break
      total += next.value.byteLength
      if (total > limit) throw new DomainError('VIDEO_ARTIFACT_TOO_LARGE', '视频归档超过 50MB 限制', 413)
      chunks.push(next.value)
    }
  } finally {
    reader.releaseLock()
  }
  const body = new Uint8Array(total)
  let offset = 0
  for (const chunk of chunks) { body.set(chunk, offset); offset += chunk.byteLength }
  return body
}

async function archiveCompletedVideo(workspaceId: string, rendering: { status: 'completed' | 'queued'; videoUrl?: string; providerJobId?: string }): Promise<{ status: 'completed' | 'queued'; videoUrl?: string; providerJobId?: string; assetId?: string; archiveState?: 'quarantined' | 'archived' }> {
  if (rendering.status === 'queued') return rendering
  if (!rendering.videoUrl || !rendering.providerJobId) throw new DomainError('VIDEO_ARTIFACT_REFERENCE_INCOMPLETE', '视频 provider 已标记完成，但缺少可归档的 HTTPS artifact URL 或 provider job id', 502)
  const existing = service.findAssetBySourceProviderJobId(workspaceId, rendering.providerJobId)
  if (existing) return { ...rendering, assetId: existing.id, archiveState: existing.scanStatus === 'clean' ? 'archived' as const : 'quarantined' as const }
  await assertVideoArtifactUrl(rendering.videoUrl)
  const response = await (videoArtifactFetcherForTests ?? fetch)(rendering.videoUrl, { method: 'GET', headers: { accept: 'video/mp4, video/webm, video/quicktime' }, redirect: 'error' })
  if (!response.ok) throw new DomainError('VIDEO_ARTIFACT_DOWNLOAD_FAILED', `视频归档下载失败（HTTP ${response.status}）`, 502)
  const declaredSize = Number(response.headers.get('content-length') ?? '')
  if (Number.isFinite(declaredSize) && declaredSize > MAX_ARCHIVED_VIDEO_BYTES) throw new DomainError('VIDEO_ARTIFACT_TOO_LARGE', '视频归档超过 50MB 限制', 413)
  const body = await readBoundedVideoBody(response, MAX_ARCHIVED_VIDEO_BYTES)
  if (!body.byteLength) throw new DomainError('VIDEO_ARTIFACT_BODY_MISSING', '视频 provider 返回了空二进制内容', 502)
  const mimeType = (response.headers.get('content-type')?.split(';', 1)[0] ?? '').trim().toLowerCase()
  if (!['video/mp4', 'video/webm', 'video/quicktime'].includes(mimeType) || !videoSignatureMatches(mimeType, body)) throw new DomainError('VIDEO_ARTIFACT_SIGNATURE_INVALID', '视频 provider 返回的 MIME 类型或文件签名无法验证，已阻断归档', 502)
  const sha256 = createHash('sha256').update(body).digest('hex')
  const name = `provider-${rendering.providerJobId}.mp4`
  const provisional = service.registerAsset({ workspaceId, name, mimeType, sizeBytes: body.byteLength, sha256, storageKey: `quarantine/${workspaceId}/video_pending_${randomBytes(12).toString('hex')}/${name}`, sourceProviderJobId: rendering.providerJobId })
  if (provisional.deduplication.mode === 'deduplicated') {
    await persistAssetReference(workspaceId, provisional)
    return { ...rendering, assetId: provisional.id, archiveState: provisional.scanStatus === 'clean' ? 'archived' as const : 'quarantined' as const }
  }
  let storedKey: string | undefined
  try {
    const stored = await putQuarantineObject({ workspaceId, assetId: provisional.id, fileName: name, contentType: mimeType, body, expectedSha256: sha256, expectedSizeBytes: body.byteLength })
    storedKey = stored.key
    provisional.storageKey = stored.key
    provisional.sha256 = stored.sha256
    provisional.sizeBytes = stored.sizeBytes
    const archiveReference = checkDurableArchiveReference({ kind: 'generated_video', workspaceId, entityId: provisional.id, storageKey: stored.key, sha256: stored.sha256, sizeBytes: stored.sizeBytes, revision: provisional.revision ?? 1 })
    if (!archiveReference.restorable) {
      throw new DomainError('VIDEO_ARCHIVE_CONTRACT_INVALID', '视频已写入隔离对象，但归档引用不满足恢复契约，已阻断结果收敛', 502, { reasons: archiveReference.reasons, asset_id: provisional.id, provider_job_id: rendering.providerJobId })
    }
    await persistAssetSnapshotAndEvent(workspaceId, provisional, 'asset.video_quarantined', { asset_id: provisional.id, provider_job_id: rendering.providerJobId, storage_key: stored.key, sha256: stored.sha256, size_bytes: stored.sizeBytes, revision: provisional.revision, next_action: 'asset.scan' }, provisional as unknown as Record<string, unknown>)
    return { ...rendering, assetId: provisional.id, archiveState: 'quarantined' as const }
  } catch (error) {
    service.assets.delete(provisional.id)
    if (storedKey) await compensateStoredAsset(workspaceId, provisional.id, storedKey, 'video archive metadata or event persistence failed')
    throw error
  }
}

function generatedImageSignatureMatches(mimeType: string, body: Uint8Array) {
  if (mimeType === 'image/png') return body.length >= 8 && [0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a].every((value, index) => body[index] === value)
  if (mimeType === 'image/jpeg') return body.length >= 3 && body[0] === 0xff && body[1] === 0xd8 && body[2] === 0xff
  if (mimeType === 'image/webp') return body.length >= 12 && Buffer.from(body.slice(0, 4)).toString('ascii') === 'RIFF' && Buffer.from(body.slice(8, 12)).toString('ascii') === 'WEBP'
  return false
}

async function archiveGeneratedImages(workspaceId: string, jobId: string, images: readonly string[]) {
  const outputs: import('../../../packages/application/src/service.js').VisualGenerationOutput[] = []
  const createdAssetIds: string[] = []
  const storedAssets: Array<{ assetId: string; objectKey: string }> = []
  let totalBytes = 0
  try {
    for (const [index, image] of images.entries()) {
    const match = image.match(/^data:(image\/[a-z0-9.+-]+);base64,([A-Za-z0-9+/=]+)$/iu)
    const downloaded = !match && /^https:\/\//iu.test(image) ? await imageArtifactBody(image) : undefined
    if (!match && !downloaded) throw new DomainError('GENERATED_IMAGE_FORMAT_INVALID', '图片生成服务返回了不支持的图片引用', 502)
    const mimeType = downloaded?.mimeType ?? match![1]!.toLowerCase()
    const extension = GENERATED_IMAGE_MIME.get(mimeType)
    if (!extension || (match && !/^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/u.test(match[2]!))) throw new DomainError('GENERATED_IMAGE_FORMAT_INVALID', '图片生成服务返回了不支持的图片格式', 502)
    const body = downloaded?.body ?? new Uint8Array(Buffer.from(match![2]!, 'base64'))
    if (!generatedImageSignatureMatches(mimeType, body)) throw new DomainError('GENERATED_IMAGE_SIGNATURE_INVALID', '图片生成服务返回的 MIME 类型与文件内容不匹配', 502)
    totalBytes += body.byteLength
    if (!body.byteLength || body.byteLength > 15 * 1024 * 1024 || totalBytes > 50 * 1024 * 1024) throw new DomainError('GENERATED_IMAGE_TOO_LARGE', '生成图片超过归档大小限制', 413)
    const asset = service.registerAsset({ workspaceId, name: `candidate-${index + 1}.${extension}`, mimeType, sizeBytes: body.byteLength, sha256: createHash('sha256').update(body).digest('hex'), storageKey: `quarantine/${workspaceId}/generated_pending_${randomBytes(12).toString('hex')}/candidate-${index + 1}.${extension}` })
    createdAssetIds.push(asset.id)
    const stored = await putQuarantineObject({ workspaceId, assetId: asset.id, fileName: `candidate-${index + 1}.${extension}`, contentType: mimeType, body, expectedSizeBytes: body.byteLength })
      storedAssets.push({ assetId: asset.id, objectKey: stored.key })
      asset.storageKey = stored.key
      asset.sha256 = stored.sha256
      asset.sizeBytes = stored.sizeBytes
      const archiveReceiptId = `image_archive_${randomUUID()}`
      const archiveReceiptDigest = imageArchiveReceiptDigest({ archiveReceiptId, workspaceId, jobId, assetId: asset.id, objectSha256: stored.sha256, sizeBytes: stored.sizeBytes, mimeType, createdAt: stored.createdAt })
      await persistAssetSnapshotAndEvent(workspaceId, asset, 'asset.generated_quarantined', { asset_id: asset.id, job_id: jobId, archive_receipt_id: archiveReceiptId, archive_receipt_digest: archiveReceiptDigest, storage_key: stored.key, sha256: stored.sha256, size_bytes: stored.sizeBytes, next_action: 'asset.scan' }, asset as unknown as Record<string, unknown>)
      outputs.push({ visualRef: `dvis_${randomBytes(18).toString('base64url')}`, assetId: asset.id, archiveReceiptId, archiveReceiptDigest, ordinal: index + 1, storageKey: stored.key, mimeType, sizeBytes: stored.sizeBytes, sha256: stored.sha256, createdAt: stored.createdAt, reviewStatus: 'unreviewed' })
    }
    const archiveState = outputs.length === images.length ? (process.env.NODE_ENV === 'test' ? 'archived' : 'pending') : outputs.length ? 'partial' : 'external_unarchived'
    if (!outputs.length) throw new DomainError('GENERATED_IMAGE_ARCHIVE_EMPTY', '图片模型已返回但没有可安全归档的候选，已停止自动重试并等待对账', 502)
    const archived = service.archiveImageGenerationOutputs(workspaceId, jobId, outputs, archiveState)
    for (const output of outputs) {
      if (!output.assetId) continue
      const asset = assetForWorkspace(workspaceId, output.assetId)
      await automaticallyScanLocalFixture(workspaceId, asset)
    }
    const current = service.imageGenerationJobs.get(jobId)
    if (current && (current.outputs ?? []).length === outputs.length && (current.outputs ?? []).every(output => output.assetId && output.archiveReceiptId && output.archiveReceiptDigest && service.assets.get(output.assetId)?.scanStatus === 'clean')) {
      return service.archiveImageGenerationOutputs(workspaceId, jobId, current.outputs ?? outputs, 'archived')
    }
    return service.imageGenerationJobs.get(jobId) ?? archived
  } catch (error) {
    await Promise.all(storedAssets.map(item => compensateStoredAsset(workspaceId, item.assetId, item.objectKey, 'generated image archive failed after object upload')))
    for (const assetId of createdAssetIds) service.assets.delete(assetId)
    throw error
  }
}

async function readArchivedGeneratedImages(workspaceId: string, job: import('../../../packages/application/src/service.js').ImageGenerationJob, visualRef?: string) {
  const images: string[] = []
  if (job.archiveState !== 'archived') throw new DomainError('GENERATED_IMAGE_ARCHIVE_REQUIRED', '生成候选尚未完成可信归档，暂不可读取', 409, { job_id: job.id, user_action_required: false })
  const outputs = [...(job.outputs ?? [])].filter(output => !visualRef || output.visualRef === visualRef).sort((left, right) => left.ordinal - right.ordinal)
  for (const output of outputs) {
    const asset = output.assetId ? assetForWorkspace(workspaceId, output.assetId) : undefined
    const usability = imageGenerationCandidateUsability({ workspaceId, job, output, asset })
    if (!usability.currentlyUsable) {
      if (usability.reason === 'asset_scan_required' || usability.reason === 'asset_missing_or_scope_mismatch') throw new DomainError('GENERATED_IMAGE_SCAN_REQUIRED', '生成候选仍在平台自动安全扫描中，完成前不会向 ChatGPT 返回图片内容', 409, { job_id: job.id, visual_ref: output.visualRef, user_action_required: false })
      throw new DomainError('GENERATED_IMAGE_INTEGRITY_FAILED', '历史生成图片归档完整性校验失败', 500, { job_id: job.id, visual_ref: output.visualRef, reason: usability.reason })
    }
    if (!asset) throw new DomainError('GENERATED_IMAGE_INTEGRITY_FAILED', '历史生成图片缺少可验证的素材归档引用', 500, { job_id: job.id, visual_ref: output.visualRef })
    const stored = await getStoredObjectWithRetry(workspaceId, asset.storageKey)
    const digest = createHash('sha256').update(stored.body).digest('hex')
    if (stored.metadata.sha256 !== output.sha256 || stored.metadata.sizeBytes !== output.sizeBytes || stored.metadata.contentType !== output.mimeType || digest !== output.sha256 || asset.sha256 !== output.sha256) throw new DomainError('GENERATED_IMAGE_INTEGRITY_FAILED', '历史生成图片完整性校验失败', 500)
    images.push(`data:${output.mimeType};base64,${Buffer.from(stored.body).toString('base64')}`)
  }
  return images
}

function imageJobOutputsAreClean(job: import('../../../packages/application/src/service.js').ImageGenerationJob, visualRef?: string) {
  const outputs = job.outputs?.filter(output => !visualRef || output.visualRef === visualRef) ?? []
  return job.archiveState === 'archived' && Boolean(outputs.length) && outputs.every(output => {
    const asset = output.assetId ? service.assets.get(output.assetId) : undefined
    return imageGenerationCandidateUsability({ workspaceId: job.workspaceId, job, output, asset }).currentlyUsable
  })
}

/** Replays the durable completion projection safely after an API response was
 * committed but the Worker lost the follow-up acknowledgement. The outbox
 * unique key (workspace, aggregate, event type, sequence) makes this repair
 * idempotent, while the snapshot and event still commit in one transaction. */
async function persistImageGenerationCompletion(workspaceId: string, job: import('../../../packages/application/src/service.js').ImageGenerationJob) {
  await persistence.persistSnapshotAndEvent?.({
    workspaceId,
    entityType: 'image_generation_job',
    entityId: job.id,
    entityVersion: job.revision,
    payload: job as unknown as Record<string, unknown>,
    eventType: 'product.image_candidates_generated',
    eventPayload: {
      job_id: job.id,
      product_id: job.productId,
      task_id: job.taskId ?? null,
      content_version_id: job.contentVersionId ?? null,
      candidate_count: job.outputs?.length ?? 0,
      archive_receipts: (job.outputs ?? []).map(output => ({ visual_ref: output.visualRef, asset_id: output.assetId ?? null, receipt_id: output.archiveReceiptId ?? null, receipt_digest: output.archiveReceiptDigest ?? null })),
      archive_state: job.archiveState,
      direction: job.direction,
      artifact_role: 'candidate',
      source: 'image_generation_worker',
    },
  })
}

function publicImageJob(job: import('../../../packages/application/src/service.js').ImageGenerationJob) {
  return {
    jobId: job.id, productId: job.productId, taskId: job.taskId ?? null, contentVersionId: job.contentVersionId ?? null, revision: job.revision,
    state: job.state, artifactRole: 'candidate', imageMode: job.imageMode, direction: job.direction, requestedCount: job.count, skuIds: job.skuIds ?? [], sourceAssetIds: job.sourceAssetIds ?? [],
    ...(job.continuation ? { continuationState: job.continuation.state, continuationUserActionRequired: job.continuation.state === 'awaiting_rights' } : {}),
    createdAt: job.createdAt, updatedAt: job.updatedAt, archiveState: job.archiveState,
    ...(job.errorCode ? { errorCode: job.errorCode } : {}), ...(job.errorMessage ? { errorMessage: job.errorMessage } : {}),
    binding: job.contentVersionId ? 'exact' : 'unbound', candidateCount: job.outputs?.length ?? job.images?.length ?? 0,
    candidates: (job.outputs ?? []).map(output => ({ visualRef: output.visualRef, ...(output.assetId ? { assetId: output.assetId } : {}), ordinal: output.ordinal, mimeType: output.mimeType, sizeBytes: output.sizeBytes, reviewStatus: output.reviewStatus, scanStatus: output.assetId ? service.assets.get(output.assetId)?.scanStatus ?? 'missing' : 'unknown' })),
    ...(job.preferredSelection ? { preferredCandidate: { visualRef: job.preferredSelection.visualRef, selectedAt: job.preferredSelection.selectedAt, status: 'preferred', reviewRequired: true, approvalRequired: true, platformPublished: false } } : {}),
    platformUsage: { status: 'not_submitted', observed: false }, selectionRequired: true,
  }
}

async function publicImageJobExecutionProjection(workspaceId: string, jobId: string) {
  const execution = await persistence.imageGenerationExecutions?.get({ workspaceId, jobId })
  return {
    executionState: execution?.state ?? null,
    providerRequestId: execution?.providerRequestId ?? null,
    executionAttempt: execution?.attempt ?? null,
    reconciliationRequired: execution?.state === 'provider_reserved'
      || execution?.state === 'provider_dispatching'
      || execution?.state === 'provider_started'
      || execution?.state === 'outcome_unknown',
  }
}

function assetForWorkspace(workspaceId: string, assetId: string) {
  const asset = service.assets.get(assetId)
  if (!asset || asset.workspaceId !== workspaceId) throw new DomainError('ASSET_NOT_FOUND', '素材不存在或不属于当前工作区', 404)
  return asset
}

function assetDisplayProjection(asset: ReturnType<MerchantService['listAssets']>[number]) {
  const base = { sourceState: asset.readiness.status, reasons: asset.readiness.reasons }
  if (asset.scanStatus === 'blocked') return { ...base, primaryStatus: 'scan_blocked', label: '安全检查未通过', nextAction: { method: 'asset.upload', label: '重新上传素材', allowed: true } }
  if (!isTrustedCleanAsset(asset)) return { ...base, primaryStatus: 'awaiting_scan', label: '正在安全检查', nextAction: { method: 'asset.list', label: '刷新状态', allowed: true } }
  if (asset.parseStatus === 'failed') return { ...base, primaryStatus: 'parse_failed', label: '内容读取失败', nextAction: { method: 'asset.facts.confirm', label: '人工确认素材事实', allowed: true } }
  if (asset.parseStatus !== 'succeeded') return { ...base, primaryStatus: 'awaiting_parse', label: '正在读取内容', nextAction: { method: 'asset.parse', label: '读取素材内容', allowed: true } }
  if (asset.rightsStatus === 'rejected' || asset.rightsScope === 'unusable') return { ...base, primaryStatus: 'rights_blocked', label: '使用权益受限', nextAction: { method: 'asset.rights.update', label: '重新确认使用权', allowed: true } }
  if (asset.rightsStatus !== 'approved') return { ...base, primaryStatus: 'awaiting_rights', label: '等待确认使用权', nextAction: { method: 'asset.rights.update', label: '确认商用权益', allowed: true } }
  if (!asset.factsConfirmedBy || !asset.factsConfirmedAt) return { ...base, primaryStatus: 'awaiting_facts_confirmation', label: '等待核对素材事实', nextAction: { method: 'asset.facts.confirm', label: '核对素材事实', allowed: true } }
  return { ...base, primaryStatus: 'ready', label: '可以用于当前任务', nextAction: null }
}

function promotionCleanupFailure(error: unknown) {
  return { code: error instanceof ObjectStorageError ? error.code : 'OBJECT_PROMOTION_CLEANUP_FAILED', message: error instanceof Error ? error.message : String(error), observed_at: new Date().toISOString() }
}

async function runPromotionCleanup(task: AssetPromotionCleanupTask) {
  if (task.status === 'completed') return task
  const repository = persistence.assetPromotionCleanup
  if (!repository) throw new DomainError('ASSET_PROMOTION_CLEANUP_UNAVAILABLE', '素材隔离区清理服务未配置', 503)
  try {
    await getAssetStorage().deleteQuarantineAfterCommit({ workspaceId: task.workspaceId, quarantineKey: task.quarantineKey, scanEvidenceRef: task.scanEvidenceRef, expectedSha256: task.objectSha256, expectedSizeBytes: task.sizeBytes })
    return await repository.markCompleted({ workspaceId: task.workspaceId, cleanupId: task.cleanupId, ...(task.leaseToken ? { leaseToken: task.leaseToken } : {}) })
  } catch (error) {
    const delayMs = Math.min(15 * 60_000, 5_000 * 2 ** Math.min(task.attempts, 7))
    try {
      await repository.recordFailure({ workspaceId: task.workspaceId, cleanupId: task.cleanupId, ...(task.leaseToken ? { leaseToken: task.leaseToken } : {}), error: promotionCleanupFailure(error), nextAttemptAt: new Date(Date.now() + delayMs).toISOString() })
    } catch (recordError) {
      console.error(JSON.stringify({ event: 'asset.promotion_cleanup_state_failed', workspace_id: task.workspaceId, asset_id: task.assetId, cleanup_id: task.cleanupId, quarantine_key: task.quarantineKey, clean_key: task.cleanKey, error: promotionCleanupFailure(recordError) }))
    }
    console.error(JSON.stringify({ event: 'asset.promotion_cleanup_deferred', workspace_id: task.workspaceId, asset_id: task.assetId, cleanup_id: task.cleanupId, quarantine_key: task.quarantineKey, clean_key: task.cleanKey, sha256: task.objectSha256, size_bytes: task.sizeBytes, scan_evidence_ref: task.scanEvidenceRef, error: promotionCleanupFailure(error) }))
    return task
  }
}

async function resumePromotionCleanup(workspaceId: string, receiptId: string) {
  const task = await persistence.assetPromotionCleanup?.getByReceipt(workspaceId, receiptId)
  if (task?.status === 'pending') await runPromotionCleanup(task)
}

let promotionCleanupDrainRunning = false
async function drainPromotionCleanupTasks() {
  if (promotionCleanupDrainRunning || persistence.mode !== 'postgres' || !persistence.assetPromotionCleanup || !persistence.listWorkspaceIds) return
  promotionCleanupDrainRunning = true
  try {
    for (const workspaceId of await persistence.listWorkspaceIds()) {
      const tasks = await persistence.assetPromotionCleanup.claimPending(workspaceId, { limit: 10, leaseMs: 30_000 })
      for (const task of tasks) await runPromotionCleanup(task)
    }
  } catch (error) {
    console.error(JSON.stringify({ event: 'asset.promotion_cleanup_drain_failed', error: promotionCleanupFailure(error) }))
  } finally {
    promotionCleanupDrainRunning = false
  }
}

async function persistTrustedScanPromotion(input: TrustedScanPromotionPersistenceInput) {
  await persistenceReady
  if (persistence.persistTrustedScanPromotion) return persistence.persistTrustedScanPromotion(input)
  if (persistence.mode === 'postgres') throw new DomainError('ASSET_PROMOTION_ATOMIC_PERSISTENCE_REQUIRED', '可信素材晋级必须使用单事务持久化能力', 503)
  // Test/local memory mode mirrors ordering; production is fail-closed above.
  await memoryAssetScanReceipts.append(input.receipt)
  await persistSnapshotsAndEvent(input)
  const readyEvent = (inMemoryTimelineEvents.get(input.workspaceId) ?? []).find(event => event.aggregateId === input.aggregateId && event.eventType === input.eventType && event.sequence === input.sequence)
  if (!readyEvent) throw new DomainError('ASSET_PROMOTION_READY_EVENT_MISSING', '素材晋级事件未写入', 500)
  return memoryAssetPromotionCleanup.create({ ...input.cleanup, readyOutboxEventId: readyEvent.id }).task
}

/** Two-phase promotion: verified copy, one database commit, then best-effort
 * source deletion backed by a durable cleanup task. No process-local state is
 * changed before the transaction commits. */
async function promoteAssetAndPersist(workspaceId: string, asset: import('../../../packages/application/src/service.js').AssetMetadata, scanEvidenceRef: string, signed?: { receipt: import('../../../packages/security/src/asset-scan-receipt.js').AssetScanReceipt; digest: string; signature: string }) {
  if (asset.workspaceId !== workspaceId || !asset.storageKey.startsWith(`quarantine/${workspaceId}/`)) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '素材不属于当前工作区或不在隔离区', 403)
  const promotion = { workspaceId, quarantineKey: asset.storageKey, scanEvidenceRef, expectedSha256: asset.sha256, expectedSizeBytes: asset.sizeBytes }
  const promoted = await getAssetStorage().copyQuarantineToClean(promotion)
  if (promoted.workspaceId !== workspaceId || promoted.zone !== 'clean' || !promoted.key.startsWith(`clean/${workspaceId}/`) || promoted.sha256 !== asset.sha256 || promoted.sizeBytes !== asset.sizeBytes || promoted.scanEvidenceRef !== scanEvidenceRef) throw new DomainError('ASSET_PROMOTION_SCOPE_INVALID', '对象存储返回的 clean 素材未通过范围或扫描证据校验', 500)
  const scan = signed?.receipt.scan
  // Unsigned promotion exists only on the non-production fixture endpoint.
  // Give that path an explicit, content-bound fixture credential so the
  // application trust predicate can distinguish it from historical
  // `scanStatus=clean` rows. Production never reaches this branch: it accepts
  // only a verified, persisted scanner receipt above.
  const fixtureReceipt = signed ? undefined : {
    scanReceiptId: `local-fixture:${asset.id}:${asset.sourceRevision ?? 1}`,
    scanReceiptDigest: createHash('sha256').update(JSON.stringify({ workspaceId, assetId: asset.id, sourceRevision: asset.sourceRevision ?? 1, objectSha256: asset.sha256, sizeBytes: asset.sizeBytes, scanEvidenceRef })).digest('hex'),
    scanVerdict: 'clean' as const,
    scanCompletedAt: new Date().toISOString(),
    scanFindings: [] as string[],
  }
  const nextAsset = { ...asset, storageKey: promoted.key, scanStatus: 'clean' as const, ...(signed ? { scanReceiptId: signed.receipt.receipt_id, scanReceiptDigest: signed.digest, scanVerdict: 'clean' as const, scanCompletedAt: scan!.completed_at, scanFindings: [...scan!.findings] } : fixtureReceipt), revision: asset.revision + 1 }
  const touchedById = new Map<string, import('../../../packages/application/src/service.js').ImageGenerationJob>()
  const continuationAwaitingConfirmationIds: string[] = []
  for (const current of service.imageGenerationJobs.values()) {
    if (current.workspaceId !== workspaceId || (!current.outputs?.some(output => output.assetId === asset.id) && current.continuation?.sourceAssetId !== asset.id)) continue
    const job = structuredClone(current)
    let touched = false
    if (job.outputs?.some(output => output.assetId === asset.id)) {
      for (const output of job.outputs) if (output.assetId === asset.id) output.storageKey = promoted.key
      job.revision += 1; job.updatedAt = new Date().toISOString(); touched = true
    }
    if (job.continuation?.sourceAssetId === asset.id && !['completed','executing','ready','failed'].includes(job.continuation.state)) {
      const nextState = awaitingConfirmationState(imageContinuationGate(nextAsset, job))
      if (job.continuation.state !== nextState) {
        job.continuation.state = nextState; job.continuation.updatedAt = new Date().toISOString(); job.updatedAt = job.continuation.updatedAt; job.revision += 1; touched = true
      }
      if (nextState === 'awaiting_confirmation') continuationAwaitingConfirmationIds.push(job.id)
    }
    if (touched) touchedById.set(job.id, job)
  }
  const eventType = continuationAwaitingConfirmationIds.length ? 'asset.generation_continuations.awaiting_confirmation' : 'asset.scan_promoted'
  const snapshots: SnapshotInput[] = [
    { entityType: 'asset', entityId: nextAsset.id, entityVersion: nextAsset.revision, payload: nextAsset as unknown as Record<string, unknown> },
    ...[...touchedById.values()].map(job => ({ entityType: 'image_generation_job' as const, entityId: job.id, entityVersion: job.revision, payload: job as unknown as Record<string, unknown> })),
  ]
  let cleanupTask: AssetPromotionCleanupTask | undefined
  if (signed) {
    cleanupTask = await persistTrustedScanPromotion({ workspaceId, receipt: { receipt: signed.receipt, receiptDigest: signed.digest, signature: signed.signature }, snapshots, aggregateId: nextAsset.id, eventType, sequence: nextAsset.revision, eventPayload: { asset_id: nextAsset.id, scan_evidence_ref: promoted.scanEvidenceRef, storage_key: promoted.key, continuation_job_ids: continuationAwaitingConfirmationIds }, cleanup: { workspaceId, receiptId: signed.receipt.receipt_id, assetId: nextAsset.id, assetSourceRevision: signed.receipt.subject.asset_source_revision, quarantineKey: asset.storageKey, cleanKey: promoted.key, scanEvidenceRef, objectSha256: asset.sha256, sizeBytes: asset.sizeBytes } })
  } else {
    await persistSnapshotsAndEvent({ workspaceId, snapshots, aggregateId: nextAsset.id, eventType, sequence: nextAsset.revision, eventPayload: { asset_id: nextAsset.id, scan_evidence_ref: promoted.scanEvidenceRef, storage_key: promoted.key, continuation_job_ids: continuationAwaitingConfirmationIds } })
  }
  // The commit is now durable; only now may process-local projections advance.
  service.assets.set(nextAsset.id, nextAsset)
  for (const job of touchedById.values()) service.imageGenerationJobs.set(job.id, job)
  if (cleanupTask) await runPromotionCleanup(cleanupTask)
  else {
    try { await getAssetStorage().deleteQuarantineAfterCommit(promotion) }
    catch (error) { console.error(JSON.stringify({ event: 'asset.fixture_promotion_cleanup_deferred', workspace_id: workspaceId, asset_id: asset.id, quarantine_key: asset.storageKey, clean_key: promoted.key, error: promotionCleanupFailure(error) })) }
  }
  return { ...nextAsset, scanEvidenceRef: promoted.scanEvidenceRef }
}

async function applySignedAssetScanResult(workspaceId: string, asset: import('../../../packages/application/src/service.js').AssetMetadata, input: SignedAssetScanReceipt) {
  let receipt
  const replayingPersistedReceipt = Boolean(asset.scanReceiptId || asset.scanReceiptDigest)
  try {
    // A byte-identical, already-persisted signed receipt remains a valid
    // idempotency key after its admission window expires. Parse it at issuance
    // time so chronology/lifetime/signature/binding are still verified; new
    // receipts continue to use the real current time and fail when expired.
    const replayIssuedAt = replayingPersistedReceipt && typeof input.receipt?.issued_at === 'string'
      ? new Date(input.receipt.issued_at)
      : undefined
    receipt = parseAssetScanReceipt(input.receipt, replayIssuedAt && Number.isFinite(replayIssuedAt.valueOf()) ? { now: replayIssuedAt } : undefined)
  } catch (error) {
    const message = error instanceof Error ? error.message : 'asset scan receipt is invalid'
    throw new DomainError(message === 'receipt expired' ? 'ASSET_SCAN_RECEIPT_EXPIRED' : 'ASSET_SCAN_RECEIPT_INVALID', message, 400)
  }
  const digest = assetScanReceiptDigest(receipt)
  if (!verifyAssetScanReceiptSignature(receipt, input.signature, trustedAssetScanPublicKey(receipt.issuer.key_id))) throw new DomainError('ASSET_SCAN_RECEIPT_SIGNATURE_INVALID', 'asset scan receipt signature is invalid', 403)
  const approvedServices = (process.env.ASSET_SCAN_APPROVED_SCANNER_SERVICE_IDS ?? '').split(',').map(value => value.trim()).filter(Boolean)
  if (!approvedServices.length) throw new DomainError('ASSET_SCANNER_TRUST_MISCONFIGURED', 'approved asset scanner services are not configured', 503)
  if (!approvedServices.includes(receipt.issuer.scanner_service_id)) throw new DomainError('ASSET_SCAN_RECEIPT_ISSUER_UNAPPROVED', 'asset scan receipt issuer is not approved', 403)
  if (receipt.scan.engine !== 'clamav') throw new DomainError('ASSET_SCAN_RECEIPT_ENGINE_INVALID', 'asset scan receipt engine must be clamav', 409)
  const expectedPolicy = process.env.ASSET_SCAN_POLICY_VERSION?.trim()
  if (!expectedPolicy) throw new DomainError('ASSET_SCANNER_TRUST_MISCONFIGURED', 'asset scan policy version is not configured', 503)
  if (receipt.scan.policy_version !== expectedPolicy) throw new DomainError('ASSET_SCAN_RECEIPT_POLICY_MISMATCH', 'asset scan receipt policy does not match the active policy', 409)
  const minimumDefinitionsVersion = Number(process.env.ASSET_SCAN_MIN_DEFINITIONS_VERSION)
  const receiptDefinitionsVersion = Number(receipt.scan.definitions_version)
  if (!Number.isSafeInteger(minimumDefinitionsVersion) || minimumDefinitionsVersion < 1) throw new DomainError('ASSET_SCANNER_TRUST_MISCONFIGURED', 'minimum ClamAV definitions version is not configured', 503)
  if (!Number.isSafeInteger(receiptDefinitionsVersion) || receiptDefinitionsVersion < minimumDefinitionsVersion) throw new DomainError('ASSET_SCAN_RECEIPT_DEFINITIONS_STALE', 'asset scan receipt uses stale or invalid ClamAV definitions', 409)
  const subject = receipt.subject
  const hasPersistedReceipt = Boolean(asset.scanReceiptId || asset.scanReceiptDigest)
  if (hasPersistedReceipt && (asset.scanReceiptId !== receipt.receipt_id || asset.scanReceiptDigest !== digest)) throw new DomainError('ASSET_SCAN_RECEIPT_CONFLICT', 'asset source revision already has a different scan receipt', 409)
  const expectedStorageKey = hasPersistedReceipt && asset.scanStatus === 'clean'
    ? subject.object_key.replace(/^quarantine\//u, 'clean/')
    : subject.object_key
  if (subject.workspace_id !== workspaceId || subject.asset_id !== asset.id || subject.asset_source_revision !== (asset.sourceRevision ?? 1)
    || expectedStorageKey !== asset.storageKey || subject.sha256 !== asset.sha256 || subject.size_bytes !== asset.sizeBytes || subject.mime_type !== asset.mimeType.toLowerCase()) {
    throw new DomainError('ASSET_SCAN_RECEIPT_BINDING_INVALID', 'asset scan receipt does not match the quarantined object', 409)
  }
  if (!hasPersistedReceipt && (asset.scanStatus !== 'quarantined' || !asset.storageKey.startsWith(`quarantine/${workspaceId}/`))) throw new DomainError('ASSET_SCAN_STATE_INVALID', 'asset is not awaiting an automatic platform scan', 409)
  const scanEvents = persistence.outbox
    ? await persistence.outbox.listAggregateEvents(workspaceId, asset.id, 100)
    : (inMemoryTimelineEvents.get(workspaceId) ?? []).filter(event => event.aggregateId === asset.id)
  const scanJob = scanEvents.find(event => event.id === receipt.scan_job_id)
  const approvedEventTypes = new Set(['asset.uploaded', 'asset.generated_quarantined', 'asset.video_quarantined', 'asset.scan_redrive_requested'])
  const scanJobSourceRevision = Number(scanJob?.payload.source_revision ?? 1)
  const scanJobMimeType = typeof scanJob?.payload.mime_type === 'string' ? scanJob.payload.mime_type.toLowerCase() : undefined
  if (!scanJob || !approvedEventTypes.has(scanJob.eventType) || scanJob.aggregateId !== asset.id
    || scanJob.payload.asset_id !== asset.id || scanJob.payload.storage_key !== subject.object_key
    || scanJob.payload.sha256 !== asset.sha256 || Number(scanJob.payload.size_bytes) !== asset.sizeBytes
    || scanJobSourceRevision !== subject.asset_source_revision
    || (scanJobMimeType !== undefined && scanJobMimeType !== subject.mime_type)) {
    throw new DomainError('ASSET_SCAN_RECEIPT_JOB_BINDING_INVALID', 'asset scan receipt is not bound to the real quarantine outbox event', 409)
  }
  if (hasPersistedReceipt) {
    if (receipt.scan.verdict === 'clean') await resumePromotionCleanup(workspaceId, receipt.receipt_id)
    return asset
  }
  const evidenceRef = `scan-receipt://${receipt.receipt_id}/${digest}`
  if (receipt.scan.verdict === 'clean') {
    return promoteAssetAndPersist(workspaceId, asset, evidenceRef, { receipt, digest, signature: input.signature })
  }
  try {
    await (persistence.assetScanReceipts ?? memoryAssetScanReceipts).append({ receipt, receiptDigest: digest, signature: input.signature })
  } catch (error) {
    if (error instanceof AssetScanReceiptConflictError) throw new DomainError(error.code, 'asset source revision already has a different immutable scan receipt', 409)
    throw error
  }
  const nextAsset = { ...asset, scanStatus: 'blocked' as const, scanReceiptId: receipt.receipt_id, scanReceiptDigest: digest, scanVerdict: receipt.scan.verdict, scanCompletedAt: receipt.scan.completed_at, scanFindings: [...receipt.scan.findings], revision: asset.revision + 1 }
  const failedContinuations = [...service.imageGenerationJobs.values()].filter(job => job.workspaceId === workspaceId && job.continuation?.sourceAssetId === asset.id && !['completed', 'failed'].includes(job.continuation.state))
  const previousContinuations = failedContinuations.map(job => ({ id: job.id, value: structuredClone(job) }))
  for (const job of failedContinuations) {
    job.continuation!.state = 'failed'
    job.continuation!.updatedAt = new Date().toISOString()
    job.errorCode = 'SOURCE_ASSET_SCAN_BLOCKED'
    job.errorMessage = '源素材未通过平台自动安全检查'
    job.updatedAt = job.continuation!.updatedAt
    job.revision += 1
  }
  try {
    await persistSnapshotsAndEvent({
      workspaceId,
      snapshots: [
        { entityType: 'asset', entityId: nextAsset.id, entityVersion: nextAsset.revision, payload: nextAsset as unknown as Record<string, unknown> },
        ...failedContinuations.map(job => ({ entityType: 'image_generation_job' as const, entityId: job.id, entityVersion: job.revision, payload: job as unknown as Record<string, unknown> })),
      ],
      aggregateId: nextAsset.id,
      eventType: 'asset.scan_blocked',
      sequence: nextAsset.revision,
      eventPayload: { asset_id: nextAsset.id, receipt_id: receipt.receipt_id, receipt_digest: digest, verdict: receipt.scan.verdict, finding_codes: receipt.scan.findings, continuation_job_ids: failedContinuations.map(job => job.id) },
    })
  } catch (error) {
    for (const previous of previousContinuations) service.imageGenerationJobs.set(previous.id, previous.value)
    throw error
  }
  service.assets.set(nextAsset.id, nextAsset)
  return nextAsset
}

async function automaticallyScanLocalFixture(workspaceId: string, asset: import('../../../packages/application/src/service.js').AssetMetadata) {
  const fixture = localAssetScanFixture({ assetId: asset.id, sha256: asset.sha256 })
  if (!fixture) return { asset, scanAutomation: conversationalAssetScanWaitingState() }
  const promoted = await promoteAssetAndPersist(workspaceId, asset, fixture.evidenceRef)
  return {
    asset: promoted,
    scanAutomation: {
      state: 'completed' as const,
      mode: fixture.mode,
      userActionRequired: false,
      productionEvidence: fixture.productionEvidence,
      message: fixture.label,
    },
  }
}

function conversationalAssetScanWaitingState() {
  const state = assetScanWaitingState()
  return {
    ...state,
    message: state.state === 'pending'
      ? '系统正在自动检查，通过后自动继续；无需操作'
      : '当前暂时无法检查图片，素材和任务已保留；请稍后继续，无需提交扫描结果',
  }
}

function requireApprovedAssetForImageGeneration(workspaceId: string, product: { platform: Platform }, requestedAssetIds?: string[]) {
  const platform = product.platform
  if (requestedAssetIds?.length) {
    const invalid: Array<Record<string, unknown>> = []
    for (const assetId of requestedAssetIds) {
      const asset = assetForWorkspace(workspaceId, assetId)
      const valid = asset.mimeType.toLowerCase().startsWith('image/')
        && asset.scanStatus === 'clean'
        && asset.rightsStatus === 'approved'
        && asset.rightsScope !== 'unusable'
        && asset.aiModificationAllowed === true
        && (!asset.applicablePlatforms?.length || asset.applicablePlatforms.includes(platform))
        && (!asset.usageScopes?.length || asset.usageScopes.includes('commercial') || asset.usageScopes.includes('ai_generation'))
        && (!asset.validFrom || Date.parse(asset.validFrom) <= Date.now())
        && (!asset.validTo || Date.parse(asset.validTo) >= Date.now())
      if (!valid) invalid.push({ asset_id: assetId, scan_status: asset.scanStatus, rights_status: asset.rightsStatus, applicable_platforms: asset.applicablePlatforms ?? [], usage_scopes: asset.usageScopes ?? [] })
    }
    if (invalid.length) throw new DomainError('IMAGE_SOURCE_ASSET_INVALID', '指定的商品素材未通过图片生成所需的安全、权益、平台或 AI 修改检查', 409, { platform, invalid_assets: invalid, scan_user_action_required: false, next_step: '安全扫描由平台自动完成；扫描通过后，仅在需要时确认商用权益和 AI 修改许可' })
    return
  }
  if (process.env.REQUIRE_APPROVED_ASSET_FOR_GENERATION !== 'true') return
  const candidates = [...service.assets.values()].filter(asset => asset.workspaceId === workspaceId && asset.mimeType.toLowerCase().startsWith('image/'))
  const eligible = candidates.filter(asset => asset.scanStatus === 'clean'
    && asset.rightsStatus === 'approved'
    && asset.aiModificationAllowed === true
    && asset.applicablePlatforms?.includes(platform))
  if (eligible.length) return
  throw new DomainError(
    'APPROVED_ASSET_REQUIRED_FOR_GENERATION',
    `正式商品主图生成前必须提供当前工作区已通过安全扫描、已确认商用权益、允许 AI 修改且适用于 ${platform} 的商品素材`,
    409,
    {
      workspace_id: workspaceId,
      platform,
      required: {
        mime_type: 'image/*',
        scan_status: 'clean',
        rights_status: 'approved',
        ai_modification_allowed: true,
        applicable_platforms: [platform],
      },
      candidate_count: candidates.length,
      next_step: '请先上传商品图片；平台会自动完成安全扫描，扫描通过后仅需确认权益和 AI 修改许可',
    },
  )
}

type ImageContinuationExecutionResult = {
  job: import('../../../packages/application/src/service.js').ImageGenerationJob
  alreadyCompleted: boolean
  product?: import('../../../packages/application/src/service.js').Product
  rulePreflight?: unknown
  productProtection?: unknown
}
const imageContinuationFlights = new Map<string, Promise<ImageContinuationExecutionResult>>()
const IMAGE_CONTINUATION_LEASE_MS = 60_000

function imageContinuationProviderOutcomeUnknown(error: unknown) {
  if (!error || typeof error !== 'object') return false
  const candidate = error as { code?: unknown; providerOutcome?: unknown; reconciliationRequired?: unknown; details?: Record<string, unknown> }
  return candidate.code === 'MODEL_PROVIDER_OUTCOME_UNKNOWN'
    || candidate.providerOutcome === 'unknown'
    || candidate.reconciliationRequired === true
    || candidate.details?.provider_outcome === 'unknown'
    || candidate.details?.reconciliation_required === true
    || providerSucceededButSettlementPending(error)
}

async function continuationLeaseRepository() {
  await persistenceReady
  return persistence.imageContinuationLeases ?? memoryImageContinuationLeases
}

async function refreshDurableImageContinuation(workspaceId: string, jobId: string) {
  if (!persistence.business) return service.getImageGenerationJob(workspaceId, jobId)
  try {
    const snapshot = await persistence.business.get(workspaceId, 'image_generation_job', jobId)
    const job = snapshot.payload as unknown as import('../../../packages/application/src/service.js').ImageGenerationJob
    if (job.workspaceId === workspaceId && job.id === jobId) service.imageGenerationJobs.set(job.id, job)
    return job
  } catch {
    return service.getImageGenerationJob(workspaceId, jobId)
  }
}

function imageContinuationFullyCompleted(job: import('../../../packages/application/src/service.js').ImageGenerationJob) {
  return job.state === 'succeeded'
    && job.continuation?.state === 'completed'
    && job.archiveState === 'archived'
    && Boolean(job.outputs?.length)
}

function continuationLeaseDomainError(error: ImageContinuationLeaseError) {
  if (error.code === 'IMAGE_CONTINUATION_EXECUTION_BUSY') return new DomainError('IMAGE_CONTINUATION_IN_PROGRESS', '图片续跑任务正由另一个执行器处理', 409, { retryable: true, lease_attempt: error.lease?.attempt })
  if (error.code === 'IMAGE_CONTINUATION_PROVIDER_OUTCOME_UNKNOWN') return new DomainError('IMAGE_CONTINUATION_PROVIDER_OUTCOME_UNKNOWN', '图片模型请求结果尚不确定，任务已停止自动重试并等待对账', 409, { retryable: false, reconciliation_required: true, lease_attempt: error.lease?.attempt })
  if (error.code === 'IMAGE_CONTINUATION_EXECUTION_FAILED') return new DomainError('IMAGE_CONTINUATION_FAILED', '图片续跑任务已失败，不会自动重复调用模型', 409, { retryable: false, lease_attempt: error.lease?.attempt })
  return new DomainError('IMAGE_CONTINUATION_EXECUTION_LEASE_LOST', '图片续跑任务执行权已变化，当前结果未被提交', 409, { retryable: false, lease_attempt: error.lease?.attempt })
}

async function executeReadyImageContinuation(workspaceId: string, jobId: string) {
  const key = `${workspaceId}\0${jobId}`
  const existing = imageContinuationFlights.get(key)
  if (existing) return existing
  const flight = executeReadyImageContinuationOnce(workspaceId, jobId).finally(() => imageContinuationFlights.delete(key))
  imageContinuationFlights.set(key, flight)
  return flight
}

async function executeReadyImageContinuationOnce(workspaceId: string, jobId: string): Promise<ImageContinuationExecutionResult> {
  let job = service.getImageGenerationJob(workspaceId, jobId)
  let continuation = job.continuation
  if (!continuation) throw new DomainError('IMAGE_CONTINUATION_NOT_FOUND', '图片任务不是自动续跑任务', 409)
  if (imageContinuationFullyCompleted(job)) return { job, alreadyCompleted: true }
  const leases = await continuationLeaseRepository()
  const existingLease = await leases.get({ workspaceId, jobId })
  if (continuation.state === 'executing' && !existingLease) {
    throw new DomainError('IMAGE_CONTINUATION_PROVIDER_OUTCOME_UNKNOWN', '旧版图片续跑任务缺少可证明的 provider 前租约，已禁止自动重试', 409, { retryable: false, reconciliation_required: true, continuation_state: continuation.state })
  }
  if (continuation.state !== 'ready' && continuation.state !== 'executing') throw new DomainError('IMAGE_CONTINUATION_NOT_READY', '图片续跑任务尚未通过扫描和权益门禁', 409, { continuation_state: continuation.state })
  let lease
  try {
    lease = await leases.claim({ workspaceId, jobId, leaseMs: IMAGE_CONTINUATION_LEASE_MS })
  } catch (error) {
    if (!(error instanceof ImageContinuationLeaseError)) throw error
    if (error.code === 'IMAGE_CONTINUATION_EXECUTION_COMPLETED') {
      job = await refreshDurableImageContinuation(workspaceId, jobId)
      if (imageContinuationFullyCompleted(job)) return { job, alreadyCompleted: true }
      throw new DomainError('IMAGE_ARTIFACT_RECONCILIATION_REQUIRED', '图片任务租约已完成，但缺少已归档且可交付的候选；已阻断完成状态并等待对账', 409, { retryable: false, reconciliation_required: true })
    }
    throw continuationLeaseDomainError(error)
  }
  // A takeover may run on a replica whose in-process projection predates the
  // previous owner's durable `executing` snapshot. Refresh before incrementing
  // the entity version, otherwise the same-version CAS correctly rejects the
  // recovery forever.
  job = await refreshDurableImageContinuation(workspaceId, jobId)
  continuation = job.continuation
  if (!continuation) {
    await leases.releaseBeforeProvider({ workspaceId, jobId, ownerToken: lease.ownerToken })
    throw new DomainError('IMAGE_CONTINUATION_NOT_FOUND', '图片任务不是自动续跑任务', 409)
  }
  if (imageContinuationFullyCompleted(job)) {
    await leases.releaseBeforeProvider({ workspaceId, jobId, ownerToken: lease.ownerToken })
    return { job, alreadyCompleted: true }
  }
  let providerStarted = false
  const releaseBeforeProvider = async () => {
    if (providerStarted) return
    try { await leases.releaseBeforeProvider({ workspaceId, jobId, ownerToken: lease.ownerToken }) }
    catch { /* the expired lease is already safely reclaimable */ }
  }
  try {
  const product = service.products.get(job.productId)
  if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
  if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
  if (!product.factsConfirmed) throw new DomainError('PRODUCT_FACTS_CONFIRMATION_REQUIRED', '商品事实尚未确认，不能自动开始图片生成', 409)
  const protectedProductValidation = requireProtectedProductIntent(job.direction)
  const productProtection = protectedProductConclusion(protectedProductValidation)
  requirePlatformModelCostGate('image')
  const rulePreflight = await requireGenerationRulePreflight(workspaceId, product.id, '主图生成前平台规则校验未通过')
  requireRuleSafeGenerationText(rulePreflight, [product.title, job.direction], '主图生成方向命中当前平台规则禁用表达')
  const imageTask = job.taskId ? service.getTask(job.taskId) : undefined
  service.assertBrandVisualGenerationReady(workspaceId, product.platform, imageTask?.region)
  requireApprovedAssetForImageGeneration(workspaceId, product, job.sourceAssetIds)

  const walletDebitKey = `image:${job.idempotencyKey}`
  let entitlementConsumed = false
  if (continuation.billingState === 'pending') {
    entitlementConsumed = Boolean(await consumeEntitlement({ workspaceId, kind: 'image_generation', actionKey: walletDebitKey, actionKind: 'model_image', modelRunKey: walletDebitKey, actorId: continuation.requestedBy, description: '商品主图生成权益' }))
    if (!entitlementConsumed) await requirePluginWalletAccess(workspaceId)
    if (!entitlementConsumed) await debitPluginWallet({ workspaceId, idempotencyKey: walletDebitKey, modelRunKey: walletDebitKey, actorId: continuation.requestedBy, description: '商品主图生成调用' })
    continuation.billingState = 'settled'
  }
  continuation.state = 'executing'
  continuation.updatedAt = new Date().toISOString()
  job.updatedAt = continuation.updatedAt
  job.revision += 1
  await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)

  await leases.markProviderStarted({ workspaceId, jobId, ownerToken: lease.ownerToken })
  providerStarted = true

  let completed: Awaited<ReturnType<typeof service.completeImageGeneration>>
  try {
    completed = await service.completeImageGeneration({ workspaceId, jobId: job.id })
  } catch (error) {
    if (imageContinuationProviderOutcomeUnknown(error)) {
      const code = (error as { code?: string })?.code ?? 'MODEL_PROVIDER_OUTCOME_UNKNOWN'
      const message = error instanceof Error ? error.message.slice(0, 500) : 'provider outcome unknown'
      await leases.markOutcomeUnknown({ workspaceId, jobId, ownerToken: lease.ownerToken, errorCode: code, errorMessage: message })
      continuation.state = 'executing'
      continuation.updatedAt = new Date().toISOString()
      job.state = 'running'
      job.errorCode = 'IMAGE_CONTINUATION_PROVIDER_OUTCOME_UNKNOWN'
      job.errorMessage = '图片模型请求结果尚不确定，等待对账，不会自动重试'
      job.updatedAt = continuation.updatedAt
      job.revision += 1
      try { await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>) } catch { /* the lease remains the fail-closed source of truth */ }
      throw new DomainError('IMAGE_CONTINUATION_PROVIDER_OUTCOME_UNKNOWN', '图片模型请求结果尚不确定，任务已停止自动重试并等待对账', 409, { retryable: false, reconciliation_required: true, lease_attempt: lease.attempt })
    }
    {
      if (entitlementConsumed) await refundModelEntitlement({ workspaceId, actionKey: walletDebitKey, reason: '图片生成失败' })
      else await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: continuation.requestedBy, reason: '图片生成失败' })
      continuation.state = 'failed'
      continuation.updatedAt = new Date().toISOString()
      job.updatedAt = continuation.updatedAt
      job.revision += 1
    }
    await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)
    await leases.markFailed({ workspaceId, jobId, ownerToken: lease.ownerToken, errorCode: (error as { code?: string })?.code ?? 'IMAGE_GENERATION_FAILED', errorMessage: error instanceof Error ? error.message.slice(0, 500) : 'image generation failed' })
    throw error
  }
  let archived: Awaited<ReturnType<typeof archiveGeneratedImages>>
  try {
    archived = await archiveGeneratedImages(workspaceId, job.id, completed.images)
  } catch (error) {
    const errorCode = (error as { code?: string })?.code ?? 'IMAGE_ARTIFACT_ARCHIVE_FAILED'
    const errorMessage = error instanceof Error ? error.message.slice(0, 500) : 'generated image archive failed'
    await leases.markOutcomeUnknown({ workspaceId, jobId, ownerToken: lease.ownerToken, errorCode: 'IMAGE_ARTIFACT_RECONCILIATION_REQUIRED', errorMessage })
    continuation.state = 'executing'
    continuation.updatedAt = new Date().toISOString()
    job.state = 'running'
    job.archiveState = 'pending'
    job.errorCode = 'IMAGE_ARTIFACT_RECONCILIATION_REQUIRED'
    job.errorMessage = '图片模型已返回结果，但候选归档尚未确认；系统不会自动重复生成'
    job.updatedAt = continuation.updatedAt
    job.revision += 1
    delete job.images
    try { await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>) } catch { /* lease remains the fail-closed source of truth */ }
    throw new DomainError('IMAGE_ARTIFACT_RECONCILIATION_REQUIRED', '图片模型已返回结果，但候选归档尚未确认；任务已停止自动重试并等待对账', 409, { retryable: false, reconciliation_required: true, archive_error_code: errorCode, lease_attempt: lease.attempt })
  }
  archived.continuation!.state = 'completed'
  archived.continuation!.updatedAt = new Date().toISOString()
  archived.updatedAt = archived.continuation!.updatedAt
  archived.revision += 1
  await persistSnapshot(workspaceId, 'image_generation_job', archived, archived as unknown as Record<string, unknown>)
  await persistEvent(workspaceId, archived.id, 'product.image_candidates_generated', archived.revision, { job_id: archived.id, product_id: product.id, task_id: archived.taskId ?? null, content_version_id: archived.contentVersionId ?? null, candidate_count: archived.outputs?.length ?? 0, archive_receipts: (archived.outputs ?? []).map(output => ({ visual_ref: output.visualRef, asset_id: output.assetId ?? null, receipt_id: output.archiveReceiptId ?? null, receipt_digest: output.archiveReceiptDigest ?? null })), archive_state: archived.archiveState, direction: archived.direction, artifact_role: 'candidate', product_protection: productProtection, resumed_from_asset_id: continuation.sourceAssetId })
  await leases.markCompleted({ workspaceId, jobId, ownerToken: lease.ownerToken })
  return { job: archived, product, rulePreflight, productProtection, alreadyCompleted: false }
  } catch (error) {
    await releaseBeforeProvider()
    throw error
  }
}

function assetUploadRejectionError(result: AssetUploadSecurityResult, auditEventId: string) {
  const status = result.reasonCode === 'ASSET_SIZE_INVALID' ? 413
    : result.reasonCode && ['ASSET_INPUT_INVALID', 'ASSET_FILENAME_CONTROL_CHARACTER', 'ASSET_FILENAME_UNICODE_CONFUSABLE', 'ASSET_FILENAME_PATH_UNSAFE', 'ASSET_DOUBLE_EXTENSION_REJECTED', 'ASSET_DECLARED_MIME_INVALID'].includes(result.reasonCode) ? 400
      : 415
  return new DomainError(result.reasonCode ?? 'ASSET_UPLOAD_REJECTED', '素材未通过上传安全检查，已拒绝进入隔离区', status, { reason_codes: result.reasonCodes, security_audit_event_id: auditEventId })
}

async function persistRejectedAssetUpload(workspaceId: string, result: AssetUploadSecurityResult, batchIndex?: number) {
  const auditEventId = `asset_security_${randomUUID()}`
  await persistEvent(workspaceId, auditEventId, 'security.asset_upload_rejected', 1, { ...result.audit, reason_codes: result.reasonCodes, ...(batchIndex !== undefined ? { batch_index: batchIndex } : {}) })
  return auditEventId
}

async function requireAssetUploadSecurity(workspaceId: string, name: string, mimeType: string, bytes: Uint8Array, req?: IncomingMessage, batchIndex?: number) {
  const result = classifyAssetUpload({ fileName: name, declaredMime: mimeType, bytes }, { workspaceId, actorId: req ? requestActor(req) : undefined, requestId: req ? requestId(req) : undefined })
  if (result.decision === 'reject') throw assetUploadRejectionError(result, await persistRejectedAssetUpload(workspaceId, result, batchIndex))
  return result
}

export function securityAuditEventsForTests(workspaceId: string) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('SECURITY_AUDIT_EVENTS_TEST_ONLY')
  return (inMemoryTimelineEvents.get(workspaceId) ?? []).filter(event => event.eventType === 'security.asset_upload_rejected').map(timelineEvent)
}

export function imageSelectionEventsForTests(workspaceId: string) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('IMAGE_SELECTION_EVENTS_TEST_ONLY')
  return (inMemoryTimelineEvents.get(workspaceId) ?? []).filter(event => event.eventType === 'image.candidate.preferred').map(timelineEvent)
}

export function assetScanJobIdForTests(workspaceId: string, assetId: string) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('ASSET_SCAN_JOB_TEST_ONLY')
  const event = (inMemoryTimelineEvents.get(workspaceId) ?? []).find(candidate => candidate.aggregateId === assetId && candidate.eventType === 'asset.uploaded')
  if (!event) throw new Error('ASSET_SCAN_JOB_NOT_FOUND')
  return event.id
}

export function assetContinuationReadyEventsForTests(workspaceId: string, assetId: string) {
  if (process.env.NODE_ENV !== 'test' && process.env.VITEST !== 'true') throw new Error('ASSET_CONTINUATION_EVENTS_TEST_ONLY')
  return (inMemoryTimelineEvents.get(workspaceId) ?? [])
    .filter(event => event.aggregateId === assetId && event.eventType === 'asset.generation_continuations.ready')
    .map(timelineEvent)
}

const ASSET_CONTINUATION_FIELDS = [
  'continuation_product_id', 'continuation_task_id', 'continuation_content_version_id',
  'continuation_sku_ids_json', 'continuation_direction', 'continuation_count',
  'continuation_idempotency_key',
] as const

function assetContinuationGate(asset: import('../../../packages/application/src/service.js').AssetMetadata): 'waiting_scan' | 'awaiting_rights' | 'ready' | 'failed' {
  if (asset.scanStatus !== 'clean') return 'waiting_scan'
  if (asset.rightsStatus === 'rejected' || asset.rightsScope === 'unusable') return 'failed'
  const now = Date.now()
  const rightsReady = asset.rightsStatus === 'approved'
    && asset.aiModificationAllowed === true
    && (!asset.validFrom || Date.parse(asset.validFrom) <= now)
    && (!asset.validTo || Date.parse(asset.validTo) >= now)
  return rightsReady ? 'ready' : 'awaiting_rights'
}

function imageContinuationGate(asset: import('../../../packages/application/src/service.js').AssetMetadata, job: import('../../../packages/application/src/service.js').ImageGenerationJob) {
  const base = assetContinuationGate(asset)
  if (base !== 'ready') return base
  const product = service.products.get(job.productId)
  if (!product || product.workspaceId !== asset.workspaceId) return 'awaiting_rights' as const
  if (asset.applicablePlatforms?.length && !asset.applicablePlatforms.includes(product.platform)) return 'awaiting_rights' as const
  const imageGenerationScopes = new Set(['commercial', 'ai_generation', 'ai_edit', 'product_main_image', 'product_detail', 'marketing_visual'])
  if (asset.usageScopes?.length && !asset.usageScopes.some(scope => imageGenerationScopes.has(scope))) return 'awaiting_rights' as const
  return 'ready' as const
}

function awaitingConfirmationState(state: import('../../../packages/application/src/service.js').ImageGenerationContinuationState) {
  return state === 'ready' ? 'awaiting_confirmation' as const : state
}

function createAssetGenerationContinuation(workspaceId: string, asset: import('../../../packages/application/src/service.js').AssetMetadata, params: JsonObject, actorId: string) {
  const kind = typeof params.continuation_kind === 'string' ? params.continuation_kind.trim() : ''
  const supplied = ASSET_CONTINUATION_FIELDS.some(field => params[field] !== undefined)
  if (!kind && !supplied) return undefined
  if (kind !== 'image_generation') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'continuation_kind 必须是 image_generation', 400)
  const productId = typeof params.continuation_product_id === 'string' ? params.continuation_product_id.trim() : ''
  const idempotencyKey = typeof params.continuation_idempotency_key === 'string' ? params.continuation_idempotency_key.trim() : ''
  if (!productId || !idempotencyKey) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '图片续跑必须提供 continuation_product_id 和 continuation_idempotency_key', 400)
  if (idempotencyKey.length > 200) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'continuation_idempotency_key 不能超过 200 个字符', 400)
  let skuIds: string[] | undefined
  if (typeof params.continuation_sku_ids_json === 'string') {
    try {
      const parsed = JSON.parse(params.continuation_sku_ids_json)
      if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string' || !value.trim())) throw new Error('invalid')
      skuIds = [...new Set(parsed.map(value => String(value).trim()))]
    } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'continuation_sku_ids_json 必须是 SKU ID 字符串数组 JSON', 400) }
  }
  const rawCount = params.continuation_count
  const count = rawCount === undefined ? undefined : typeof rawCount === 'string' && /^[1-6]$/u.test(rawCount) ? Number(rawCount) : NaN
  if (rawCount !== undefined && !Number.isInteger(count)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'continuation_count 必须是 1 到 6 的整数字符串', 400)
  const continuationState = awaitingConfirmationState(assetContinuationGate(asset))
  const job = service.enqueueImageGeneration({
    workspaceId,
    productId,
    sourceAssetIds: [asset.id],
    imageMode: 'optimize',
    idempotencyKey,
    ...(skuIds ? { skuIds } : {}),
    ...(typeof params.continuation_task_id === 'string' && params.continuation_task_id.trim() ? { taskId: params.continuation_task_id.trim() } : {}),
    ...(typeof params.continuation_content_version_id === 'string' && params.continuation_content_version_id.trim() ? { contentVersionId: params.continuation_content_version_id.trim() } : {}),
    ...(typeof params.continuation_direction === 'string' && params.continuation_direction.trim() ? { direction: params.continuation_direction.trim() } : {}),
    ...(count ? { count } : {}),
    continuation: { sourceAssetId: asset.id, state: continuationState, requestedBy: actorId, billingState: 'pending' },
  })
  if (!job.continuation || job.continuation.sourceAssetId !== asset.id) throw new DomainError('IDEMPOTENCY_KEY_REUSED', '相同幂等键已用于非续跑图片任务或其他素材', 409)
  const scopedState = awaitingConfirmationState(imageContinuationGate(asset, job))
  if (job.continuation.state !== scopedState && job.continuation.state !== 'executing' && job.continuation.state !== 'completed') {
    job.continuation.state = scopedState
    job.continuation.updatedAt = new Date().toISOString()
    job.updatedAt = job.continuation.updatedAt
    job.revision += 1
  }
  return job
}

async function persistUploadedAssetAndContinuation(
  workspaceId: string,
  asset: import('../../../packages/application/src/service.js').AssetMetadata,
  eventType: string,
  eventPayload: Record<string, unknown>,
  continuation?: import('../../../packages/application/src/service.js').ImageGenerationJob,
) {
  if (!continuation) return persistAssetSnapshotAndEvent(workspaceId, asset, eventType, eventPayload, asset as unknown as Record<string, unknown>)
  await persistSnapshotsAndEvent({
    workspaceId,
    snapshots: [
      { entityType: 'asset', entityId: asset.id, entityVersion: asset.revision, payload: asset as unknown as Record<string, unknown> },
      { entityType: 'image_generation_job', entityId: continuation.id, entityVersion: continuation.revision, payload: continuation as unknown as Record<string, unknown> },
    ],
    aggregateId: asset.id,
    eventType,
    sequence: asset.revision,
    eventPayload: { ...eventPayload, continuation_job_id: continuation.id },
  })
}

async function uploadAssetForMcp(workspaceId: string, params: JsonObject, req?: IncomingMessage, batchIndex?: number, securityPreflighted = false) {
  const encoded = required(params, 'content_base64')
  if (!/^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/u.test(encoded)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'content_base64 无效', 400)
  let bytes: Uint8Array
  try { bytes = new Uint8Array(Buffer.from(encoded, 'base64')) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'content_base64 无效', 400) }
  if (!bytes.byteLength) throw new DomainError('ASSET_UPLOAD_LIMIT', 'Codex MCP 单个素材上传限制为 1 字节到 50MB', 413)
  const assetName = required(params, 'name')
  const assetMime = required(params, 'mime_type')
  if (!securityPreflighted) await requireAssetUploadSecurity(workspaceId, assetName, assetMime, bytes, req, batchIndex)
  let applicablePlatforms: Platform[] | undefined
  if (typeof params.applicable_platforms_json === 'string') {
    try { const parsed = JSON.parse(params.applicable_platforms_json); if (!Array.isArray(parsed) || parsed.some(value => !SUPPORTED_PLATFORMS.includes(String(value) as Platform))) throw new Error('applicable_platforms_json'); applicablePlatforms = parsed as Platform[] } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'applicable_platforms_json 必须是支持平台字符串数组 JSON', 400) }
  }
  const parseAssetList = (key: string, label: string) => {
    if (typeof params[key] !== 'string') return undefined
    try { const parsed = JSON.parse(params[key] as string); if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string')) throw new Error(key); return parsed as string[] } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是字符串数组 JSON`, 400) }
  }
  const applicableRegions = parseAssetList('applicable_regions_json', '适用地区')
  const usageScopes = parseAssetList('usage_scopes_json', '使用范围')
  const rightsScope = typeof params.rights_scope === 'string' ? params.rights_scope as import('../../../packages/application/src/service.js').AssetMetadata['rightsScope'] : undefined
  if (rightsScope && !['owned', 'commercial_authorized', 'limited_use', 'internal_only', 'unknown', 'unusable'].includes(rightsScope)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'rights_scope 无效', 400)
  const provisional = service.registerAsset({ workspaceId, name: assetName, mimeType: assetMime, sizeBytes: bytes.byteLength, sha256: typeof params.sha256 === 'string' && /^[a-f0-9]{64}$/iu.test(params.sha256) ? params.sha256 : createHash('sha256').update(bytes).digest('hex'), storageKey: `quarantine/${workspaceId}/pending/${randomUUID()}/${assetName}`, ...(req ? { uploadedByActorId: requestActor(req) } : {}), ...(rightsScope ? { rightsScope } : {}), ...(applicablePlatforms ? { applicablePlatforms } : {}), ...(applicableRegions ? { applicableRegions } : {}), ...(usageScopes ? { usageScopes } : {}), ...(typeof params.valid_from === 'string' ? { validFrom: params.valid_from } : {}), ...(typeof params.valid_to === 'string' ? { validTo: params.valid_to } : {}), ...(params.ai_modification_allowed === 'true' || params.ai_modification_allowed === 'false' ? { aiModificationAllowed: params.ai_modification_allowed === 'true' } : {}) })
  const continuation = createAssetGenerationContinuation(workspaceId, provisional, params, req ? requestActor(req) : 'merchant')
  if (provisional.deduplication.mode === 'deduplicated') {
    if (continuation) {
      const eventType = continuation.continuation?.state === 'awaiting_confirmation' ? 'asset.generation_continuations.awaiting_confirmation' : continuation.continuation?.state === 'awaiting_rights' ? 'asset.generation_continuation.awaiting_rights' : 'asset.generation_continuation.waiting_scan'
      await persistUploadedAssetAndContinuation(workspaceId, provisional, eventType, { asset_id: provisional.id, continuation_job_ids: [] }, continuation)
    } else await persistAssetReference(workspaceId, provisional)
    if (isTrustedCleanAsset(provisional)) return { ...provisional, ...(continuation ? { generationContinuation: { jobId: continuation.id, state: continuation.continuation!.state } } : {}) }
    // A duplicate may still be quarantined even though its original durable
    // scan event has already reached a terminal failure. Treat every
    // non-trusted duplicate as a fresh source revision so re-uploading the
    // same bytes always creates recoverable scan work instead of merely adding
    // a reference to an asset that can no longer progress.
    {
      const previousAsset = structuredClone(provisional)
      const stored = await putQuarantineObject({ workspaceId, assetId: provisional.id, fileName: assetName, contentType: assetMime, body: bytes, expectedSizeBytes: bytes.byteLength, expectedSha256: provisional.sha256 })
      try {
        const rescanning = service.prepareAssetRescan({ workspaceId, assetId: provisional.id, storageKey: stored.key, sizeBytes: stored.sizeBytes, sha256: stored.sha256, mimeType: assetMime })
        await persistUploadedAssetAndContinuation(workspaceId, rescanning, 'asset.uploaded', { asset_id: rescanning.id, storage_key: stored.key, size_bytes: stored.sizeBytes, sha256: stored.sha256, rescan: true, source_revision: rescanning.sourceRevision }, continuation)
        const automated = await automaticallyScanLocalFixture(workspaceId, rescanning)
        return { ...automated.asset, scanAutomation: automated.scanAutomation, ...(continuation ? { generationContinuation: { jobId: continuation.id, state: continuation.continuation!.state } } : {}) }
      } catch (error) {
        service.assets.set(previousAsset.id, previousAsset)
        await compensateStoredAsset(workspaceId, provisional.id, stored.key, 'duplicate asset rescan persistence failed')
        throw error
      }
    }
  }
  let storedKey: string | undefined
  try {
    const stored = await putQuarantineObject({ workspaceId, assetId: provisional.id, fileName: provisional.name, contentType: provisional.mimeType, body: bytes, expectedSizeBytes: bytes.byteLength, expectedSha256: provisional.sha256 })
    storedKey = stored.key
    provisional.storageKey = stored.key
    await persistUploadedAssetAndContinuation(workspaceId, provisional, 'asset.uploaded', { asset_id: provisional.id, storage_key: stored.key, size_bytes: stored.sizeBytes, sha256: stored.sha256 }, continuation)
    const automated = await automaticallyScanLocalFixture(workspaceId, provisional)
    return { ...automated.asset, scanAutomation: automated.scanAutomation, ...(continuation ? { generationContinuation: { jobId: continuation.id, state: continuation.continuation!.state } } : {}) }
  } catch (error) {
    // Do not leave an in-memory asset that can deduplicate a later retry when
    // the object was never durably stored.
    service.assets.delete(provisional.id)
    if (continuation) service.discardUnpersistedImageGeneration(workspaceId, continuation.id)
    if (storedKey) await compensateStoredAsset(workspaceId, provisional.id, storedKey, 'asset snapshot or event persistence failed')
    throw error
  }
}

function headerRequired(req: IncomingMessage, name: string): string {
  const value = header(req, name)?.trim()
  if (!value) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `缺少必填请求头: ${name}`, 400)
  return value
}

function isWorkerRoute(method: string | undefined, path: string): boolean {
  if (method === 'POST') {
    return path === '/v1/internal/automation/tick'
      || path === '/v1/internal/model-usage'
      || path === '/v1/internal/model-usage/reconciliation'
      || path === '/v1/ops/data-deletion/complete'
      || path === '/v1/internal/storage/orphans/cleanup'
      || path === '/v1/internal/storage/reconciliation'
      || path === '/v1/internal/support/sla-scan'
      || path === '/v1/internal/support/sla-report'
      || path === '/v1/internal/image-generation-jobs/reconciliation'
      || /^\/v1\/sync-jobs\/[^/]+\/(?:progress|result)$/.test(path)
      || /^\/v1\/generation-jobs\/[^/]+\/(?:defer|result)$/.test(path)
      || /^\/v1\/internal\/image-generation-jobs\/[^/]+\/result$/.test(path)
      || /^\/v1\/internal\/image-generation-jobs\/[^/]+\/execution$/.test(path)
      || /^\/v1\/internal\/image-generation-jobs\/[^/]+\/reconciliation-evidence$/.test(path)
      || /^\/v1\/internal\/image-generation-continuations\/[^/]+\/execute$/.test(path)
      || /^\/v1\/publish-jobs\/[^/]+\/observation$/.test(path)
      || /^\/v1\/assets\/[^/]+\/scan$/.test(path)
  }
  if (method === 'GET') {
    return /^\/v1\/generation-jobs\/[^/]+$/.test(path)
      || /^\/v1\/sync-jobs\/[^/]+\/execution-context$/.test(path)
      || /^\/v1\/worker-events\/[^/]+\/execution-check$/.test(path)
      || /^\/v1\/publish-jobs\/[^/]+\/(?:execution-check|media)$/.test(path)
  }
  return false
}

function isAssetScannerRoute(method: string | undefined, path: string): boolean {
  return (method === 'GET' && /^\/v1\/internal\/assets\/[^/]+\/scan-content$/.test(path))
    || (method === 'POST' && /^\/v1\/internal\/assets\/[^/]+\/scan-result$/.test(path))
}

async function requireAssetScannerAuthorization(req: IncomingMessage) {
  const expected = process.env.ASSET_SCANNER_API_TOKEN?.trim()
  const authorization = header(req, 'authorization')
  if (!expected || authorization !== `Bearer ${expected}`) throw new DomainError(ERROR_CODES.FORBIDDEN, 'asset scanner authorization required', 403)
  const signingSecret = process.env.ASSET_SCANNER_WORKSPACE_SIGNING_SECRET?.trim()
  if (!signingSecret) throw new DomainError('ASSET_SCANNER_AUTH_MISCONFIGURED', 'asset scanner workspace signing secret is required', 503)
  const workspaceId = header(req, 'x-workspace-id')?.trim()
  const timestamp = header(req, 'x-scanner-timestamp')?.trim() ?? ''
  const nonce = header(req, 'x-scanner-nonce')?.trim() ?? ''
  const bodyDigest = header(req, 'x-scanner-body-sha256')?.trim().toLowerCase() ?? ''
  const signature = header(req, 'x-scanner-workspace-signature')?.trim() ?? ''
  const rawBody = await requestBodyBytes(req, DEFAULT_BODY_LIMIT)
  if (!workspaceId || !verifyScannerRequestProof({ secret: signingSecret, method: req.method ?? 'GET', requestTarget: req.url ?? '/', workspaceId, body: rawBody, timestamp, nonce, bodySha256: bodyDigest, signature })) {
    throw new DomainError(ERROR_CODES.FORBIDDEN, 'asset scanner request proof is missing, invalid, expired, or not bound to this request', 403)
  }
  if (!await consumeAssetScannerNonce(nonce)) throw new DomainError('ASSET_SCANNER_NONCE_REPLAY', 'asset scanner request proof has already been used', 409)
}

function assetScannerWorkspace(req: IncomingMessage): string {
  const workspaceId = header(req, 'x-workspace-id')?.trim()
  if (!workspaceId) throw new DomainError(ERROR_CODES.FORBIDDEN, 'asset scanner workspace binding required', 403)
  return workspaceId
}

function trustedAssetScanPublicKey(keyId: string): string {
  const raw = process.env.ASSET_SCAN_TRUSTED_PUBLIC_KEYS?.trim()
  if (!raw) throw new DomainError('ASSET_SCANNER_TRUST_MISCONFIGURED', 'asset scanner trusted public keys are not configured', 503)
  try {
    const keys = JSON.parse(raw) as unknown
    if (!keys || typeof keys !== 'object' || Array.isArray(keys)) throw new Error('invalid key set')
    const value = (keys as Record<string, unknown>)[keyId]
    if (typeof value !== 'string' || !value.includes('BEGIN PUBLIC KEY')) throw new Error('unknown key id')
    return value
  } catch {
    throw new DomainError('ASSET_SCAN_RECEIPT_KEY_UNTRUSTED', 'asset scan receipt key is not trusted', 403)
  }
}

type WorkerCredential = { token: string; signingSecret: string }

function workerRouteRoles(method: string | undefined, path: string): WorkerRequestRole[] {
  if (method === 'GET') {
    if (/^\/v1\/sync-jobs\/[^/]+\/execution-context$/u.test(path)) return ['sync']
    if (/^\/v1\/generation-jobs\/[^/]+$/u.test(path)) return ['generation']
    if (/^\/v1\/publish-jobs\/[^/]+\/(?:execution-check|media)$/u.test(path)) return ['publish', 'reconcile']
    if (/^\/v1\/worker-events\/[^/]+\/execution-check$/u.test(path)) return ['sync', 'generation', 'publish', 'reconcile', 'scan']
  }
  if (method === 'POST') {
    if (/^\/v1\/sync-jobs\/[^/]+\/(?:progress|result)$/u.test(path)) return ['sync']
    if (path === '/v1/internal/image-generation-jobs/reconciliation') return ['reconcile']
    if (/^\/v1\/(?:generation-jobs|internal\/image-generation-jobs|internal\/image-generation-continuations)\//u.test(path)) return ['generation']
    if (/^\/v1\/publish-jobs\/[^/]+\/observation$/u.test(path)) return ['publish', 'reconcile']
    if (path === '/v1/internal/automation/tick') return ['automation']
    if (path === '/v1/internal/model-usage') return ['generation', 'publish']
    if (path === '/v1/internal/model-usage/reconciliation' || path === '/v1/internal/storage/reconciliation' || path === '/v1/internal/support/sla-scan' || path === '/v1/internal/support/sla-report' || path === '/v1/internal/image-generation-jobs/reconciliation') return ['reconcile']
    if (path === '/v1/ops/data-deletion/complete' || path === '/v1/internal/storage/orphans/cleanup') return ['automation']
    if (/^\/v1\/assets\/[^/]+\/scan$/u.test(path)) return ['scan']
  }
  return []
}

function configuredWorkerCredentials(): Partial<Record<WorkerRequestRole, WorkerCredential[]>> {
  const raw = process.env.WORKER_API_CREDENTIALS?.trim()
  if (!raw) return {}
  try {
    const parsed = JSON.parse(raw) as Record<string, unknown>
    const result: Partial<Record<WorkerRequestRole, WorkerCredential[]>> = {}
    const seenTokens = new Set<string>()
    const seenSecrets = new Set<string>()
    for (const role of WORKER_ROLES) {
      const rawValue = parsed[role]
      if (rawValue === undefined) continue
      const values = Array.isArray(rawValue) ? rawValue : [rawValue]
      if (values.length < 1 || values.length > 2) throw new Error(`${role} must have one current credential and at most one rotation credential`)
      const credentials = values.map(value => {
        if (!value || typeof value !== 'object' || Array.isArray(value)) throw new Error(`${role} credential must be an object`)
        const token = (value as Record<string, unknown>).token
        const signingSecret = (value as Record<string, unknown>).signing_secret
        if (typeof token !== 'string' || !token.trim() || typeof signingSecret !== 'string' || !signingSecret.trim()) throw new Error(`${role} credential is incomplete`)
        const credential = { token: token.trim(), signingSecret: signingSecret.trim() }
        if (seenTokens.has(credential.token) || seenSecrets.has(credential.signingSecret)) throw new Error('worker credentials must be unique across roles and rotation slots')
        seenTokens.add(credential.token)
        seenSecrets.add(credential.signingSecret)
        return credential
      })
      result[role] = credentials
    }
    return result
  } catch {
    throw new DomainError('WORKER_AUTH_MISCONFIGURED', 'WORKER_API_CREDENTIALS must be a valid role credential map', 503)
  }
}

async function requireWorkerAuthorization(req: IncomingMessage) {
  if (workerAuthorizedRequests.has(req)) return
  const credentials = configuredWorkerCredentials()
  if (!requiresStrictAuth() && Object.keys(credentials).length === 0) { workerAuthorizedRequests.add(req); return }
  const requestTarget = req.url ?? '/'
  const path = new URL(requestTarget, 'http://worker.internal').pathname
  const allowedRoles = workerRouteRoles(req.method, path)
  const role = header(req, 'x-worker-role')?.trim() as WorkerRequestRole | undefined
  if (Object.keys(credentials).length === 0) throw new DomainError('WORKER_AUTH_MISCONFIGURED', 'strict worker role credentials are not configured', 503)
  const candidates = role ? credentials[role] : undefined
  if (!role || !allowedRoles.includes(role) || !candidates?.length) throw new DomainError(ERROR_CODES.FORBIDDEN, 'worker role is not authorized for this route', 403)
  const authorization = header(req, 'authorization')
  const credentialIndex = authorization ? candidates.findIndex(candidate => safeEqual(authorization, `Bearer ${candidate.token}`)) : -1
  const credential = credentialIndex >= 0 ? candidates[credentialIndex] : undefined
  if (!credential) throw new DomainError(ERROR_CODES.FORBIDDEN, 'worker internal authorization required', 403)
  const workspaceId = header(req, 'x-workspace-id')?.trim() ?? ''
  const timestamp = header(req, 'x-worker-timestamp')?.trim() ?? ''
  const nonce = header(req, 'x-worker-nonce')?.trim() ?? ''
  const bodySha256 = header(req, 'x-worker-body-sha256')?.trim().toLowerCase() ?? ''
  const signature = header(req, 'x-worker-workspace-signature')?.trim() ?? ''
  const rawBody = await requestBodyBytes(req, DEFAULT_BODY_LIMIT)
  if (!verifyWorkerRequestProof({ secret: credential.signingSecret, role, method: req.method ?? 'GET', requestTarget, workspaceId, body: rawBody, timestamp, nonce, bodySha256, signature })) {
    throw new DomainError(ERROR_CODES.FORBIDDEN, 'worker proof is missing, expired, or not bound to this role and request', 403)
  }
  if (!await consumeWorkerNonce(nonce)) throw new DomainError('WORKER_NONCE_REPLAY', 'worker request proof has already been used', 409)
  enrichRequestObservation(req, {
    workspaceId,
    actorId: `worker:${role}`,
    workerRole: role,
    workerCredentialSlot: credentialIndex === 0 ? 'current' : 'rotation',
    workerProofTimestamp: Number(timestamp),
    workerBodySha256: bodySha256,
    workerNonceSha256: createHash('sha256').update(nonce).digest('hex'),
    workerVerifiedAt: new Date().toISOString(),
  })
  workerAuthorizedRequests.add(req)
}

async function requireWorkerCredentialAuthorization(req: IncomingMessage) {
  if (requiresStrictAuth() || Object.keys(configuredWorkerCredentials()).length > 0) { await requireWorkerAuthorization(req); return }
  const expected = process.env.WORKER_API_TOKEN?.trim()
  if (!expected || header(req, 'authorization') !== `Bearer ${expected}`) throw new DomainError(ERROR_CODES.FORBIDDEN, 'worker credential access required', 403)
}

function assertPublishIdempotency(workspaceId: string, input: { taskId: string; contentVersionId: string; confirmationHash: string; remoteSnapshotHash: string; idempotencyKey: string }) {
  const existing = [...service.publishJobs.values()].find(job => job.idempotencyKey === input.idempotencyKey)
  if (!existing) return
  if (existing.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '幂等键已属于其他工作区', 403)
  if (existing.taskId !== input.taskId || existing.contentVersionId !== input.contentVersionId || existing.confirmationHash !== input.confirmationHash || existing.remoteSnapshotHash !== input.remoteSnapshotHash) {
    throw new DomainError(ERROR_CODES.IDEMPOTENCY_CONFLICT, '幂等键已绑定其他发布意图', 409)
  }
}

function interactiveSessionIntentHash(workspaceId: string, actorId: string, sessionId: string) {
  return createHash('sha256').update(`interactive-session:${workspaceId}:${actorId}:${sessionId}`).digest('hex')
}

function publishConfirmationIntentHash(input: { workspaceId: string; taskId: string; contentVersionId: string; confirmationHash: string; remoteSnapshotHash: string }) {
  return createHash('sha256').update(JSON.stringify({ method: 'publish.confirm', ...input })).digest('hex')
}

function interactiveTicketScope(req: IncomingMessage) {
  const principal = requestPrincipals.get(req)
  const actorId = principal?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
  const sessionId = principal?.sessionId ?? principal?.sessionSubject ?? `api-token:${actorId}`
  return { actorId, sessionId }
}

export function assertImageSelectionTicketPersistence(input: { mode: ApiPersistence['mode']; configured: boolean; production: boolean; testRuntime: boolean }) {
  if (input.production && !input.testRuntime && (input.mode !== 'postgres' || !input.configured)) {
    throw new DomainError('IMAGE_SELECTION_TICKET_PERSISTENCE_REQUIRED', '生产环境图片选择必须使用 Postgres 一次性票据仓储', 503)
  }
}

function imageSelectionTicketRepository() {
  assertImageSelectionTicketPersistence({ mode: persistence.mode, configured: Boolean(persistence.interactiveConfirmationTickets), production: isProduction(), testRuntime: process.env.VITEST === 'true' })
  return imageSelectionTicketRepositoryOverride ?? persistence.interactiveConfirmationTickets ?? memoryInteractiveConfirmationTickets
}

function imageSelectionTicketIntentHash(input: { jobId: string; visualRef: string; expectedRevision: number }) {
  return createHash('sha256').update(JSON.stringify({ method: 'catalog.image.select', jobId: input.jobId, visualRef: input.visualRef, expectedRevision: input.expectedRevision })).digest('hex')
}

function imageSelectionPreferenceIntentHash(input: { jobId: string; visualRef: string; selectedBy: string; reason: string }) {
  return createHash('sha256').update(JSON.stringify(input)).digest('hex')
}

async function issueImageSelectionTickets(req: IncomingMessage, workspaceId: string, job: import('../../../packages/application/src/service.js').ImageGenerationJob, outputs: readonly import('../../../packages/application/src/service.js').VisualGenerationOutput[]) {
  const repository = imageSelectionTicketRepository()
  const { actorId, sessionId } = interactiveTicketScope(req)
  const expiresAt = new Date(Date.now() + 5 * 60_000).toISOString()
  const tickets: Array<{ visual_ref: string; nonce_hash: string; intent_hash: string; expires_at: string }> = []
  for (const output of outputs) {
    const intentHash = imageSelectionTicketIntentHash({ jobId: job.id, visualRef: output.visualRef, expectedRevision: job.revision })
    // Compatibility field name: nonce_hash carries the raw one-time bearer.
    // Only sha256(raw bearer) is stored by the repository.
    const rawNonce = randomBytes(32).toString('hex')
    const storedNonceHash = createHash('sha256').update(rawNonce).digest('hex')
    const issued = await repository.issue({ workspaceId, actorId, sessionId, intentHash, nonceHash: storedNonceHash, expiresAt })
    tickets.push({ visual_ref: output.visualRef, nonce_hash: rawNonce, intent_hash: issued.intentHash, expires_at: issued.expiresAt })
  }
  return tickets
}

async function consumeImageSelectionTicket(input: { req: IncomingMessage; workspaceId: string; params: Record<string, unknown>; job: import('../../../packages/application/src/service.js').ImageGenerationJob; visualRef: string; expectedRevision: number; idempotencyKey: string; selectedBy: string; reason: string }) {
  const rawNonce = required(input.params, 'confirmation_ticket_nonce_hash')
  const suppliedIntentHash = required(input.params, 'confirmation_ticket_intent_hash')
  if (!/^[a-f0-9]{64}$/u.test(rawNonce) || !/^[a-f0-9]{64}$/u.test(suppliedIntentHash)) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_INVALID', '图片选择票据格式无效', 400)
  const expectedTicketIntentHash = imageSelectionTicketIntentHash({ jobId: input.job.id, visualRef: input.visualRef, expectedRevision: input.expectedRevision })
  if (suppliedIntentHash !== expectedTicketIntentHash) throw new DomainError('INTERACTIVE_CONFIRMATION_INTENT_MISMATCH', '图片选择票据与候选或任务版本不一致', 409)
  const repository = imageSelectionTicketRepository()
  const preferenceIntentHash = imageSelectionPreferenceIntentHash({ jobId: input.job.id, visualRef: input.visualRef, selectedBy: input.selectedBy, reason: input.reason })
  const prior = [...(input.job.preferredSelectionHistory ?? []), ...(input.job.preferredSelection ? [input.job.preferredSelection] : [])].find(selection => selection.idempotencyKey === input.idempotencyKey)
  if (prior) {
    if (prior.intentHash !== preferenceIntentHash) throw new DomainError('IDEMPOTENCY_CONFLICT', '幂等键已绑定其他图片候选偏好意图', 409)
    return { replay: true as const }
  }
  const { actorId, sessionId } = interactiveTicketScope(input.req)
  const storedNonceHash = createHash('sha256').update(rawNonce).digest('hex')
  const consumed = await repository.consume({ workspaceId: input.workspaceId, actorId, sessionId, intentHash: suppliedIntentHash, nonceHash: storedNonceHash, legacyNonceHash: rawNonce })
  if (!consumed) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_INVALID', '图片选择票据不存在、已过期、已消费或不属于当前会话', 409)
  return { replay: false as const }
}

async function consumePublishConfirmationTicket(req: IncomingMessage, workspaceId: string, input: { workspaceId: string; taskId: string; contentVersionId: string; confirmationHash: string; remoteSnapshotHash: string; params: Record<string, unknown> }) {
  const rawNonce = typeof input.params.confirmation_ticket_nonce_hash === 'string' ? input.params.confirmation_ticket_nonce_hash.trim() : ''
  const intentHash = typeof input.params.confirmation_ticket_intent_hash === 'string' ? input.params.confirmation_ticket_intent_hash.trim() : ''
  const durable = persistence.mode === 'postgres' && Boolean(persistence.interactiveConfirmationTickets)
  if (!rawNonce && !intentHash) {
    if (isProduction() && durable) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_REQUIRED', '生产发布必须携带未过期的一次性交互确认票据', 400)
    return false
  }
  if (!/^[a-f0-9]{64}$/u.test(rawNonce) || !/^[a-f0-9]{64}$/u.test(intentHash)) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_INVALID', '交互确认票据格式无效', 400)
  const principal = requestPrincipals.get(req)
  const actorId = principal?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
  const sessionId = principal?.sessionId ?? principal?.sessionSubject ?? `api-token:${actorId}`
  const expectedIntent = publishConfirmationIntentHash(input)
  const sessionIntent = interactiveSessionIntentHash(workspaceId, actorId, sessionId)
  if (intentHash !== expectedIntent && intentHash !== sessionIntent) throw new DomainError('INTERACTIVE_CONFIRMATION_INTENT_MISMATCH', '交互确认票据与当前发布意图不一致', 409)
  const storedNonceHash = createHash('sha256').update(rawNonce).digest('hex')
  const consumed = await (persistence.interactiveConfirmationTickets ?? memoryInteractiveConfirmationTickets).consume({ workspaceId, actorId, sessionId, intentHash, nonceHash: storedNonceHash, legacyNonceHash: rawNonce })
  if (!consumed) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_INVALID', '交互确认票据不存在、已过期或已被消费', 409)
  return true
}

async function reservePublishConfirmationTicket(req: IncomingMessage, workspaceId: string, input: { workspaceId: string; taskId: string; contentVersionId: string; confirmationHash: string; remoteSnapshotHash: string; params: Record<string, unknown> }) {
  const rawNonce = typeof input.params.confirmation_ticket_nonce_hash === 'string' ? input.params.confirmation_ticket_nonce_hash.trim() : ''
  const intentHash = typeof input.params.confirmation_ticket_intent_hash === 'string' ? input.params.confirmation_ticket_intent_hash.trim() : ''
  const repository = persistence.interactiveConfirmationTickets
  const durable = persistence.mode === 'postgres' && repository && 'reserve' in repository && 'finalizeInTransaction' in repository
  if (!rawNonce && !intentHash) {
    if (isProduction() && durable) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_REQUIRED', '生产发布必须携带未过期的一次性交互确认票据', 400)
    return undefined
  }
  if (!durable) return undefined
  if (!/^[a-f0-9]{64}$/u.test(rawNonce) || !/^[a-f0-9]{64}$/u.test(intentHash)) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_INVALID', '交互确认票据格式无效', 400)
  const { actorId, sessionId } = interactiveTicketScope(req)
  const expectedIntent = publishConfirmationIntentHash(input)
  const sessionIntent = interactiveSessionIntentHash(workspaceId, actorId, sessionId)
  if (intentHash !== expectedIntent && intentHash !== sessionIntent) throw new DomainError('INTERACTIVE_CONFIRMATION_INTENT_MISMATCH', '交互确认票据与当前发布意图不一致', 409)
  const reservationId = `publish:${randomUUID()}`
  const reservationToken = randomBytes(32).toString('hex')
  const storedNonceHash = createHash('sha256').update(rawNonce).digest('hex')
  const ticket = { workspaceId, actorId, sessionId, intentHash, nonceHash: storedNonceHash, legacyNonceHash: rawNonce, reservationId, reservationToken, reservationExpiresAt: new Date(Date.now() + 10 * 60 * 1000).toISOString() }
  const reservation = await (repository as TransactionalInteractiveConfirmationTicketRepository).reserve(ticket)
  if (!reservation) throw new DomainError('INTERACTIVE_CONFIRMATION_TICKET_INVALID', '交互确认票据不存在、已过期或已被消费', 409)
  return { ticket, reservation }
}

function paramsOf(input: JsonObject): JsonObject {
  const params = input.params
  if (params === undefined) return {}
  if (!params || typeof params !== 'object' || Array.isArray(params)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'MCP params 必须是 JSON 对象', 400)
  return params as JsonObject
}

function assertMcpEnvelope(input: JsonObject) {
  const errors: string[] = []
  if (input.jsonrpc !== '2.0') errors.push('jsonrpc must be 2.0')
  if (!Object.prototype.hasOwnProperty.call(input, 'id') || (input.id !== null && typeof input.id !== 'string' && typeof input.id !== 'number')) {
    errors.push('id must be a string, number, or null')
  }
  if (errors.length) throw new DomainError(ERROR_CODES.INVALID_REQUEST, errors.join('; '), 400)
}

function firstValueExecutionLabel(kind: 'content' | 'visual', available: boolean) {
  const subject = kind === 'content' ? '内容' : '视觉候选'
  return {
    mode: 'read_only_preview' as const,
    simulated: false,
    providerExecuted: false,
    modelCalled: false,
    label: available ? `仅读取已有${subject}预览，未调用${subject}模型` : `暂无${subject}预览，未调用${subject}模型`,
    message: available ? `仅读取已有${subject}预览，未调用${subject}模型` : `暂无${subject}预览，未调用${subject}模型`,
  }
}

function firstValueNextActions(product: import('../../../packages/application/src/service.js').Product, contentAvailable: boolean, visualCount: number) {
  const actions = [`先确认商品事实：${product.factsConfirmed ? '已确认，可继续' : '仍需确认价格、库存、SKU 和图片'}`]
  if (!contentAvailable) actions.push('确认商品后调用 task.understand，开始内容任务')
  else actions.push('审阅已有内容预览，确认后再进入 content.review')
  if (!visualCount) actions.push('如需主图，先补齐已授权素材，再在交互确认后调用 catalog.image.generate')
  else actions.push('审阅视觉候选；选择前不要将候选视为已发布图片')
  actions.push('发布前必须人工审核并明确确认，当前预览不会发布任何内容')
  return actions
}

function merchantFirstValuePreview(workspaceId: string, params: JsonObject) {
  const example = params.example === 'true'
  if (example) {
    const nextActions = ['连接一个平台店铺并选择真实商品，查看基于商家事实的预览', '或上传商品资料后确认事实，再开始内容和视觉任务', '示例不会调用模型、写入商品或发布到平台']
    return {
      readOnly: true,
      previewOnly: true,
      example: true,
      product: { id: null, productId: null, facts: { title: '示例商品：轻云防晒外套', platform: null, storeName: null, accountId: null, remoteProductId: null, skuCount: 1, stock: null, price: null, category: '服饰示例', images: [], attributes: { note: '示例内容，不代表商家真实商品事实' }, sellingPoints: [], factsConfirmed: false, source: 'example', updatedAt: new Date().toISOString(), version: 1 }, sourceIds: ['example:first-value'] },
      contentPreview: { id: 'example-content-preview', taskId: null, version: 1, state: 'example', body: { title: '轻云防晒外套｜通勤轻户外详情页示例', detail: '这里展示详情页结构、卖点证据和平台适配方式。真实商品必须先绑定店铺并确认事实。', sellingPoints: ['结构化详情页模块', '卖点需要商家事实支持', '平台规则和 SEO/GEO 建议需单独确认'] }, sourceIds: ['example:first-value'], factSourceIds: [] },
      visualPreviewRefs: [],
      execution: { mode: 'read_only_preview', simulated: true, providerExecuted: false, modelCalled: false, label: '静态示例预览，未调用模型，未发布任何内容', message: '静态示例预览，未调用模型，未发布任何内容', content: firstValueExecutionLabel('content', true), visual: firstValueExecutionLabel('visual', false) },
      nextActions,
      next_actions: nextActions,
    }
  }
  const productId = typeof params.product_id === 'string' && params.product_id.trim() ? params.product_id.trim() : undefined
  const platform = typeof params.platform === 'string' && params.platform.trim() ? params.platform.trim() as Platform : undefined
  const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
  if (accountId && !platform) throw new DomainError('STORE_PLATFORM_REQUIRED', '使用 account_id 选择首个价值预览时必须同时指定 platform', 400)

  if (isProduction() && (!productId || !platform || !accountId)) {
    throw new DomainError('FIRST_VALUE_SELECTION_REQUIRED', '生产首个价值预览必须明确传入已授权店铺的 product_id、platform 和 account_id', 409, {
      next_actions: ['调用 workspace.health 查看已授权店铺', '调用 catalog.search 选择具体店铺商品', '重新调用 merchant.first_value 并传入 product_id、platform 和 account_id'],
    })
  }

  let product: import('../../../packages/application/src/service.js').Product | undefined
  if (productId) {
    product = service.products.get(productId)
    if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404, { next_actions: ['调用 catalog.search 查看当前工作区商品'] })
    if (platform && product.platform !== platform) throw new DomainError('PLATFORM_SCOPE_MISMATCH', '首个价值预览的平台必须与商品所属平台一致', 409, { next_actions: ['重新选择该商品所属 platform'] })
    if (accountId && product.accountId !== accountId) throw new DomainError('STORE_CONTEXT_MISMATCH', '首个价值预览的商品不属于所选店铺', 409, { next_actions: ['重新选择与商品一致的 platform + account_id'] })
    if (isProduction()) {
      if (!product.accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产首个价值预览的商品必须绑定已授权店铺', 409, { next_actions: ['调用 catalog.search 选择已绑定店铺商品'] })
      service.getActivePlatformAccount(workspaceId, product.accountId, product.platform)
    }
  } else if (accountId && platform) {
    service.getPlatformAccount(workspaceId, accountId, platform)
    product = service.listProducts(workspaceId, { platform, accountId })[0]
  } else if (platform) {
    throw new DomainError('STORE_SELECTION_REQUIRED', '请先明确选择 platform + account_id，首个价值预览不能跨店铺猜测商品', 409, { next_actions: ['调用 workspace.health 查看店铺列表', '明确选择 platform + account_id', '调用 catalog.search 查看该店铺商品'] })
  } else {
    // The fallback is deliberately limited to fixture data and the current
    // workspace. It must never turn an unscoped production request into a
    // real-store or cross-workspace preview.
    product = service.listProducts(workspaceId).find(item => item.source === 'fixture')
  }

  if (!product) {
    throw new DomainError('FIRST_VALUE_PRODUCT_REQUIRED', '当前范围没有可安全预览的商品；请先明确选择商品或同步/导入一件商品', 409, {
      next_actions: accountId && platform
        ? ['调用 catalog.sync.start 同步当前店铺商品', '或调用 catalog.import 导入商品资料', '完成后重新调用 merchant.first_value 并传入 product_id']
        : ['调用 catalog.search 选择一个具体店铺商品', '重新调用 merchant.first_value 并传入 product_id、platform 和 account_id'],
    })
  }

  const productVersion = product.version ?? 1
  const productSourceIds = [`product:${product.id}:v${productVersion}`]
  for (const sellingPoint of product.sellingPoints ?? []) productSourceIds.push(...sellingPoint.sourceIds)
  const productFacts = {
    title: product.title,
    platform: product.platform,
    storeName: product.storeName,
    accountId: product.accountId ?? null,
    remoteProductId: product.remoteId ?? null,
    skuCount: product.skuCount,
    stock: product.stock,
    price: product.price ?? null,
    category: product.category ?? null,
    images: product.images ?? [],
    attributes: product.attributes ?? {},
    sellingPoints: product.sellingPoints ?? [],
    factsConfirmed: product.factsConfirmed,
    source: product.source,
    updatedAt: product.updatedAt,
    version: productVersion,
  }

  const scopedTasks = service.listTasks(workspaceId, { productId: product.id, platform: product.platform })
    .filter(task => (task.accountId ?? null) === (product.accountId ?? null))
  const contentCandidates = scopedTasks.flatMap(task => service.listContentVersions(workspaceId, task.id).map(version => ({ task, version })))
    .sort((left, right) => right.version.version - left.version.version || right.task.createdAt.localeCompare(left.task.createdAt))
  const content = contentCandidates[0]
  const contentPreview = content ? {
    id: content.version.id,
    taskId: content.task.id,
    version: content.version.version,
    state: content.version.state,
    body: content.version.body,
    sourceIds: content.version.factVersionIds,
    factSourceIds: content.version.factVersionIds,
  } : null

  const visualPreviewRefs = [...service.imageGenerationJobs.values()]
    .filter(job => job.workspaceId === workspaceId && job.productId === product.id && job.archiveState === 'archived' && job.state === 'succeeded')
    .filter(job => !job.taskId || scopedTasks.some(task => task.id === job.taskId))
    .flatMap(job => (job.outputs ?? []).map(output => output.visualRef))
    .slice(0, 6)
  const execution = {
    mode: 'read_only_preview' as const,
    simulated: false,
    providerExecuted: false,
    modelCalled: false,
    label: '只读预览，未调用真实模型，未发布任何内容',
    message: '只读预览，未调用真实模型，未发布任何内容',
    content: firstValueExecutionLabel('content', Boolean(contentPreview)),
    visual: firstValueExecutionLabel('visual', visualPreviewRefs.length > 0),
  }
  const nextActions = firstValueNextActions(product, Boolean(contentPreview), visualPreviewRefs.length)
  return {
    readOnly: true,
    previewOnly: true,
    product: { id: product.id, productId: product.id, facts: productFacts, sourceIds: [...new Set(productSourceIds)] },
    contentPreview,
    visualPreviewRefs,
    execution,
    nextActions,
    next_actions: nextActions,
  }
}

function scopeTask(req: IncomingMessage, taskId: string) {
  const task = service.getTask(taskId)
  resolveWorkspace(req, task.workspaceId)
  return task
}

async function enforceTaskBrandAccess(req: IncomingMessage, task: { workspaceId: string; brandId?: string }, minimumRole: BrandAccessRole = 'viewer') {
  if (task.brandId) return enforceBrandAccess(req, task.workspaceId, task.brandId, minimumRole)
  if (requiresStrictAuth() && !hasWorkspaceWideBrandAccess(req)) throw new DomainError('TASK_BRAND_ACCESS_DENIED', '任务未绑定可访问的品', 404)
}

async function filterByTaskBrandAccess<T extends { brandId?: string }>(req: IncomingMessage, workspaceId: string, tasks: T[]) {
  if (!requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)) return tasks
  const actorId = requestPrincipals.get(req)?.actorId
  if (!actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '品权限筛选缺少成员身份', 401)
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const access = new Map<string, boolean>()
  const visible = await Promise.all(tasks.map(async task => {
    if (!task.brandId) return undefined
    if (!access.has(task.brandId)) access.set(task.brandId, await repository.hasBrandAccess({ workspaceId, brandId: task.brandId, externalSubject: actorId }))
    return access.get(task.brandId) ? task : undefined
  }))
  return visible.filter(Boolean) as T[]
}

async function accessibleTaskBrandIds(req: IncomingMessage, workspaceId: string): Promise<readonly string[] | undefined> {
  if (!requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)) return undefined
  const actorId = requestPrincipals.get(req)?.actorId
  if (!actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '品牌权限筛选缺少成员身份', 401)
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const brands = await repository.listBrands({ workspaceId })
  const visible = await Promise.all(brands.map(async brand => await repository.hasBrandAccess({ workspaceId, brandId: brand.id, externalSubject: actorId }) ? brand.id : undefined))
  return visible.filter((brandId): brandId is string => Boolean(brandId))
}

async function accessibleProductIds(req: IncomingMessage, workspaceId: string): Promise<ReadonlySet<string> | undefined> {
  const brandIds = await accessibleTaskBrandIds(req, workspaceId)
  if (brandIds === undefined) return undefined
  const canonical = await (persistence.brandUnits ?? memoryBrandUnits).listCanonicalProducts({ workspaceId, brandIds })
  return new Set(canonical.flatMap(product => product.sourceProductId ? [product.sourceProductId] : []))
}

/**
 * Resolve the normalized product/listing chain before creating a task. Legacy
 * fixture products may legitimately have no canonical row; once a canonical
 * row exists, silently creating a legacy task would split the execution graph.
 */
async function resolveCanonicalTaskScope(input: { workspaceId: string; productId: string; platform: Platform; accountId?: string; brandId?: string; canonicalProductId?: string; listingId?: string; requireListing?: boolean; requireCanonical?: boolean }) {
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const readControl = await canonicalProductReadControl(input.workspaceId)
  const canonicalRows = await repository.listCanonicalProducts({ workspaceId: input.workspaceId, ...(input.brandId ? { brandIds: [input.brandId] } : {}) })
  const candidates = input.canonicalProductId
    ? canonicalRows.filter(row => row.id === input.canonicalProductId)
    : canonicalRows.filter(row => row.sourceProductId === input.productId)
  if (!candidates.length) {
    if (readControl.mode === 'canonical_read' || input.requireCanonical) throw new DomainError('CANONICAL_PRODUCT_MAPPING_REQUIRED', '标准商品链未完成验证，已阻断任务创建，停止创建任务', 409, { reason: 'CANONICAL_MAPPING_MISSING', next_action: 'canonical.product.consistency', read_mode: readControl.mode, product_id: input.productId, canonical_product_id: input.canonicalProductId ?? null })
    return undefined
  }
  if (candidates.length > 1) throw new DomainError('CANONICAL_PRODUCT_AMBIGUOUS', '一个 legacy 商品对应多个规范化商品，已阻止任务创建，请先完成映射治理', 409, { product_id: input.productId, canonical_product_ids: candidates.map(row => row.id) })
  const canonical = candidates[0]!
  if ((readControl.mode === 'canonical_read' || input.requireCanonical) && (!canonical.facts || Object.keys(canonical.facts).length === 0)) throw new DomainError('CANONICAL_PRODUCT_FACTS_REQUIRED', '标准商品事实尚未完整确认，已阻断任务创建', 409, { reason: 'CANONICAL_FACTS_MISSING', next_action: 'catalog.facts.confirm', read_mode: readControl.mode, product_id: input.productId, canonical_product_id: canonical.id })
  const listings = await repository.listListings({ workspaceId: input.workspaceId, brandId: canonical.brandId, canonicalProductId: canonical.id, platform: input.platform, ...(input.accountId ? { accountId: input.accountId } : {}) })
  if (listings.length !== 1) {
    // Task creation must fail closed in every environment. Campaign imports
    // may retain an explicitly legacy target for fixture/backfill workflows;
    // they cannot be promoted to a canonical task without a listing.
    if (isProduction() || input.requireListing) throw new DomainError('CANONICAL_LISTING_REQUIRED', '该商品已有规范化商品记录，但当前平台/店铺没有唯一 listing，已阻止创建孤立任务', 409, { product_id: input.productId, canonical_product_id: canonical.id, platform: input.platform, account_id: input.accountId ?? null, listing_count: listings.length, next_step: '先创建或修复当前平台店铺 listing，再创建任务' })
    return undefined
  }
  if (input.listingId && listings[0]!.id !== input.listingId) throw new DomainError('LISTING_TARGET_MISMATCH', '批量计划 listing 与商品、平台或店铺不一致', 409, { listing_id: input.listingId, expected: listings[0]!.id, canonical_product_id: canonical.id, platform: input.platform, account_id: input.accountId ?? null })
  return { brandId: canonical.brandId, canonicalProductId: canonical.id, listingId: listings[0]!.id }
}

async function resolveCanonicalTaskEntries(workspaceId: string, entries: Array<{ productId: string; platform: Platform; accountId?: string; brandId?: string }>) {
  return Promise.all(entries.map(async entry => {
    const scope = await resolveCanonicalTaskScope({ workspaceId, ...entry, requireListing: true })
    return { ...entry, ...(scope ?? {}) }
  }))
}

async function invalidateCanonicalFactsAfterSync(workspaceId: string, products: readonly Product[]) {
  if ((await canonicalProductReadControl(workspaceId)).mode !== 'canonical_read' || !products.length) return
  await persistenceReady
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const canonicalRows = await repository.listCanonicalProducts({ workspaceId })
  for (const product of products) {
    const candidates = canonicalRows.filter(row => row.sourceProductId === product.id)
    if (candidates.length > 1) throw new DomainError('CANONICAL_PRODUCT_AMBIGUOUS', '同步后发现一个商品对应多个规范商品，已阻断事实继续使用', 409, { product_id: product.id, canonical_product_ids: candidates.map(row => row.id), next_action: 'canonical.product.consistency' })
    const canonical = candidates[0]
    if (!canonical?.facts || Object.keys(canonical.facts).length === 0) continue
    try {
      const updated = await repository.updateCanonicalProductFacts({ workspaceId, id: canonical.id, facts: {}, expectedFactsVersion: canonical.factsVersion })
      await persistEvent(workspaceId, updated.id, 'canonical_product.facts_invalidated_by_sync', updated.factsVersion, { product_id: product.id, canonical_product_id: updated.id, facts_version: updated.factsVersion, source: 'platform_sync', next_action: 'catalog.facts.confirm' })
    } catch (error) {
      if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_REVISION_CONFLICT') throw new DomainError('CANONICAL_PRODUCT_REVISION_CONFLICT', '同步期间标准商品事实已变化，请刷新后重新确认', 409)
      if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_NOT_FOUND') throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', '标准商品不存在或不属于当前工作区', 404)
      throw error
    }
  }
}

async function assertCanonicalTaskScopeForAction(task: Pick<Task, 'workspaceId' | 'productId' | 'platform' | 'accountId' | 'brandId' | 'canonicalProductId' | 'listingId'>) {
  const scope = await resolveCanonicalTaskScope({ workspaceId: task.workspaceId, productId: task.productId, platform: task.platform, ...(task.accountId ? { accountId: task.accountId } : {}), ...(task.brandId ? { brandId: task.brandId } : {}), requireListing: true })
  if (!scope) return undefined
  if (task.canonicalProductId && task.canonicalProductId !== scope.canonicalProductId) throw new DomainError('CANONICAL_PRODUCT_SCOPE_MISMATCH', '任务绑定的规范商品与当前商品链不一致，已阻断后续动作', 409, { task_id: (task as { id?: string }).id ?? null, expected: scope.canonicalProductId, provided: task.canonicalProductId })
  if (task.listingId && task.listingId !== scope.listingId) throw new DomainError('LISTING_TARGET_MISMATCH', '任务绑定的 listing 与当前平台店铺不一致，已阻断后续动作', 409, { task_id: (task as { id?: string }).id ?? null, expected: scope.listingId, provided: task.listingId })
  const readControl = await canonicalProductReadControl(task.workspaceId)
  if (readControl.mode !== 'canonical_read') return { ...scope, readMode: readControl.mode as 'legacy_shadow' | 'dual_verify' | 'canonical_read', canonicalReadRevision: undefined }
  const repository = persistence.brandUnits ?? memoryBrandUnits
  let report: ReturnType<typeof canonicalConsistencyApiReport>
  try {
    const rows = await repository.listCanonicalChainConsistencyRows({ workspaceId: task.workspaceId })
    report = canonicalConsistencyApiReport({ workspaceId: task.workspaceId, ...rows }, persistence.mode === 'postgres' ? 'postgres' : 'memory')
  } catch (error) {
    throw new DomainError('CANONICAL_CONSISTENCY_UNAVAILABLE', '发布前无法读取标准商品一致性证据，已拒绝执行', 503, { cause: error instanceof Error ? error.message.slice(0, 160) : 'consistency read failed' })
  }
  if (report.freshness !== 'fresh' || report.availability !== 'available') throw new DomainError('CANONICAL_CONSISTENCY_UNAVAILABLE', '标准商品一致性证据不可用或已过期，已拒绝执行', 503, { freshness: report.freshness, availability: report.availability })
  const finding = report.findings.find(candidate => candidate.legacyProductId === task.productId)
  if (!finding || finding.status !== 'verified' || finding.canonicalProductId !== scope.canonicalProductId || finding.listingIds.length !== 1 || finding.listingIds[0] !== scope.listingId) throw new DomainError('CANONICAL_EXECUTION_NOT_READY', '目标商品的标准商品一致性尚未验证，已拒绝释放发布凭证', 409, { product_id: task.productId, canonical_product_id: scope.canonicalProductId, listing_id: scope.listingId, finding_status: finding?.status ?? 'missing', next_action: 'canonical.product.consistency' })
  return { ...scope, readMode: readControl.mode as 'legacy_shadow' | 'dual_verify' | 'canonical_read', canonicalReadRevision: String(report.revision) }
}

async function accessibleAssetIds(req: IncomingMessage, workspaceId: string): Promise<ReadonlySet<string> | undefined> {
  const productIds = await accessibleProductIds(req, workspaceId)
  if (productIds === undefined) return undefined
  if (persistence.business) {
    const bindings = await persistence.business.listProductAssetBindings(workspaceId, { status: 'active' })
    const accessible = new Set(bindings.filter(binding => productIds.has(binding.productId)).map(binding => binding.assetId))
    const actorId = requestPrincipals.get(req)?.actorId
    if (actorId) {
      for (const asset of service.listAssets(workspaceId)) {
        if (asset.uploadedByActorIds?.includes(actorId)) accessible.add(asset.id)
      }
    }
    return accessible
  }
  const accessible = new Set(service.listProducts(workspaceId).filter(product => productIds.has(product.id)).flatMap(product => product.sourceAssetIds ?? []))
  const actorId = requestPrincipals.get(req)?.actorId
  if (actorId) {
    for (const asset of service.listAssets(workspaceId)) {
      if (asset.uploadedByActorIds?.includes(actorId)) accessible.add(asset.id)
    }
  }
  return accessible
}

async function enforceAssetAccess(req: IncomingMessage, workspaceId: string, assetId: string, minimumRole: BrandAccessRole = 'viewer') {
  const accessible = await accessibleAssetIds(req, workspaceId)
  if (accessible !== undefined && !accessible.has(assetId)) throw new DomainError('ASSET_NOT_FOUND', '素材不存在或不属于当前可访问品', 404)
  if (minimumRole === 'viewer' || !requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)) return
  const boundProducts = persistence.business
    ? (await persistence.business.listProductAssetBindings(workspaceId, { status: 'active' })).filter(binding => binding.assetId === assetId).map(binding => binding.productId)
    : service.listProducts(workspaceId).filter(product => (product.sourceAssetIds ?? []).includes(assetId)).map(product => product.id)
  const asset = service.assets.get(assetId)
  const actorId = requestPrincipals.get(req)?.actorId
  // An unbound upload has no brand scope yet. Its authenticated uploader may
  // finish parse, rights and facts steps; after binding, brand grants govern.
  if (!boundProducts.length && actorId && asset?.workspaceId === workspaceId && asset.uploadedByActorIds?.includes(actorId)) return
  const canonical = await (persistence.brandUnits ?? memoryBrandUnits).listCanonicalProducts({ workspaceId })
  const brandIds = [...new Set(canonical.filter(product => product.sourceProductId && boundProducts.includes(product.sourceProductId)).map(product => product.brandId))]
  if (!brandIds.length) throw new DomainError('ASSET_NOT_FOUND', '素材不存在或未绑定可编辑品牌', 404)
  if (!actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '素材权限校验缺少成员身份', 401)
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const editable = await Promise.all(brandIds.map(brandId => repository.hasBrandAccess({ workspaceId, brandId, externalSubject: actorId, minimumRole })))
  if (!editable.some(Boolean)) throw new DomainError('ASSET_NOT_FOUND', '素材不存在或当前成员没有编辑权限', 404)
}

async function enforceProductBrandAccess(req: IncomingMessage, workspaceId: string, productId: string, minimumRole: BrandAccessRole = 'viewer') {
  const accessible = await accessibleProductIds(req, workspaceId)
  if (accessible !== undefined && !accessible.has(productId)) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前可访问品', 404)
  if (minimumRole === 'viewer' || !requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)) return
  const actorId = requestPrincipals.get(req)?.actorId
  if (!actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '品权限校验缺少成员身份', 401)
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const canonical = (await repository.listCanonicalProducts({ workspaceId })).filter(product => product.sourceProductId === productId)
  if (!canonical.length) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前可访问品', 404)
  const grants = await Promise.all(canonical.map(product => repository.hasBrandAccess({ workspaceId, brandId: product.brandId, externalSubject: actorId, minimumRole })))
  if (!grants.some(Boolean)) throw new DomainError('BRAND_ACCESS_REQUIRED', '当前成员没有该品所需权限', 403, { brand_ids: canonical.map(product => product.brandId), required_role: minimumRole })
}

async function enforceProductBrandBinding(req: IncomingMessage, workspaceId: string, productId: string, brandId: string) {
  await enforceProductBrandAccess(req, workspaceId, productId)
  if (!requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)) return
  const canonical = await (persistence.brandUnits ?? memoryBrandUnits).listCanonicalProducts({ workspaceId, brandIds: [brandId] })
  if (!canonical.some(product => product.sourceProductId === productId)) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前可访问品', 404)
}

async function taskWriteBrandForProduct(req: IncomingMessage, workspaceId: string, productId: string): Promise<string | undefined> {
  if (!requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)) return undefined
  const actorId = requestPrincipals.get(req)?.actorId
  if (!actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '品牌权限校验缺少成员身份', 401)
  const repository = persistence.brandUnits ?? memoryBrandUnits
  const brands = await repository.listBrands({ workspaceId })
  const editableBrandIds = (await Promise.all(brands.map(async brand => await repository.hasBrandAccess({ workspaceId, brandId: brand.id, externalSubject: actorId, minimumRole: 'editor' }) ? brand.id : undefined)))
    .filter((brandId): brandId is string => Boolean(brandId))
  const matches = (await repository.listCanonicalProducts({ workspaceId, brandIds: editableBrandIds }))
    .filter(product => product.sourceProductId === productId)
    .sort((left, right) => left.brandId.localeCompare(right.brandId) || left.id.localeCompare(right.id))
  if (!matches.length) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前可编辑品', 404)
  const brandIds = [...new Set(matches.map(product => product.brandId))]
  if (brandIds.length > 1) throw new DomainError('PRODUCT_BRAND_AMBIGUOUS', '商品同时属于多个可编辑品，请明确选择品后再创建任务', 409, { product_id: productId, brand_ids: brandIds })
  return brandIds[0]
}

function taskUnderstandingProductIds(understanding: ReturnType<MerchantService['understandTaskRequest']>) {
  return [...new Set([
    ...understanding.productCandidates.map(product => product.id),
    ...understanding.executionPlan.childTasks.flatMap(child => child.candidateProductIds),
  ])]
}

async function enforceTaskRequestCandidates(req: IncomingMessage, workspaceId: string, understanding: ReturnType<MerchantService['understandTaskRequest']>) {
  for (const productId of taskUnderstandingProductIds(understanding)) await enforceProductBrandAccess(req, workspaceId, productId)
  return understanding
}

async function resolveTaskWriteBrands(req: IncomingMessage, workspaceId: string, productIds: readonly string[]) {
  const resolved = new Map<string, string | undefined>()
  for (const productId of [...new Set(productIds)]) resolved.set(productId, await taskWriteBrandForProduct(req, workspaceId, productId))
  return resolved
}

function assignTaskWriteBrands(tasks: Array<{ productId: string; brandId?: string }>, resolved: ReadonlyMap<string, string | undefined>) {
  for (const task of tasks) {
    const brandId = resolved.get(task.productId)
    if (brandId) task.brandId = brandId
  }
}

async function taskCreationBrand(req: IncomingMessage, workspaceId: string, value: unknown): Promise<string | undefined> {
  const brandId = typeof value === 'string' ? value.trim() : ''
  if (brandId) {
    await enforceBrandAccess(req, workspaceId, brandId, 'editor')
    return brandId
  }
  if (requiresStrictAuth() && !hasWorkspaceWideBrandAccess(req)) throw new DomainError('TASK_BRAND_REQUIRED', '受限成员创建任务时必须选择有编辑权限的品', 409)
  return undefined
}

function taskRoleForOperation(operation: string, readOnly = false): BrandAccessRole {
  if (readOnly || ['task.history', 'task.timeline', 'content.versions', 'content.diff', 'content.export', 'publish.get'].includes(operation)) return 'viewer'
  if (operation === 'content.approve' || operation.startsWith('publish.')) return 'publisher'
  return 'editor'
}

function scopeContentVersion(req: IncomingMessage, contentVersionId: string) {
  const version = service.contentVersions.get(contentVersionId)
  if (!version) throw new DomainError(ERROR_CODES.CONTENT_VERSION_NOT_FOUND, '内容版本不存在', 404)
  const task = service.getTask(version.taskId)
  resolveWorkspace(req, task.workspaceId)
  return { task, version }
}

function oauthError(error: OAuthStateError): { status: number; code: string } {
  if (error.code === 'STATE_EXPIRED') return { status: 401, code: ERROR_CODES.OAUTH_STATE_EXPIRED }
  if (error.code === 'STATE_SCOPE_MISMATCH') return { status: 403, code: ERROR_CODES.OAUTH_STATE_SCOPE_MISMATCH }
  if (error.code === 'STATE_REPLAYED') return { status: 409, code: ERROR_CODES.OAUTH_STATE_REPLAYED }
  return { status: 400, code: ERROR_CODES.OAUTH_STATE_INVALID }
}

async function consumeTaskUsage(workspaceId: string, taskId: string, idempotencyKey: string, actorId: string) {
  const task = service.getTask(taskId)
  if (task.workspaceId !== workspaceId) throw new DomainError('TENANT_SCOPE_DENIED', '无权访问该任务', 403)
  const taskScope = { taskId: task.id, ...(task.campaignItemId ? { campaignItemId: task.campaignItemId } : {}) }
  // Recharge is required even when the workspace still has included task
  // quota. This keeps content generation aligned with the plugin's single
  // wallet unlock contract; the quota only determines whether a second debit
  // is needed after the gate passes.
  await assertProviderActionCanStart(workspaceId, `model:${idempotencyKey}`)
  await requirePluginWalletAccess(workspaceId)
  try {
    const charged = await (persistence.usage ?? memoryUsage).consume({ workspaceId, taskId, idempotencyKey, actorId })
    if (charged.charged) {
      try {
        const policy = await (persistence.commercialExtensions ?? memoryCommercialExtensions).getModelMarkupPolicy()
        await recordActionSettlement({ workspaceId, actionKey: `model:${idempotencyKey}`, actionKind: 'model_text', settlement: 'included_quota', amountFen: 0, reservedAmountFen: 0, multiplier: policy.multiplier, settlementStatus: 'authorized', actorId, description: '模型生成调用（套餐行动额度）', ...taskScope })
      } catch (ledgerError) {
        await (persistence.usage ?? memoryUsage).refund({ workspaceId, taskId, idempotencyKey, actorId, reason: '行动账本写入失败，回滚套餐额度' })
        throw ledgerError
      }
      await recordOperationAudit({ workspaceId, actorId, action: 'usage.consume', resourceType: 'task', resourceId: taskId, before: {}, after: charged.snapshot as unknown as Record<string, unknown>, reason: '生成入口额度消费' })
    }
    return { ...charged, settlement: 'included_quota' as const, walletDebited: false }
  } catch (error) {
    if (!(error instanceof Error) || error.message !== 'QUOTA_EXCEEDED') throw error
    await requirePluginWalletAccess(workspaceId)
    await debitPluginWallet({ workspaceId, idempotencyKey: `model:${idempotencyKey}`, actorId, description: '模型生成调用（套餐额度外）', ...taskScope })
    modelBillingReservations.set(`${workspaceId}:model:${idempotencyKey}`, { debitIdempotencyKey: `model:${idempotencyKey}`, actorId, providerRequests: new Set() })
    await recordOperationAudit({ workspaceId, actorId, action: 'usage.overage.consume', resourceType: 'task', resourceId: taskId, before: {}, after: { idempotency_key: idempotencyKey, settlement: 'wallet_overage' }, reason: '套餐额度用尽后使用钱包余额' })
    return { snapshot: await (persistence.usage ?? memoryUsage).get(workspaceId), charged: false, settlement: 'wallet_overage' as const, walletDebited: true }
  }
}

async function markTaskUsageProviderOutcomePending(workspaceId: string, idempotencyKey: string) {
  await persistenceReady
  const actionKey = `model:${idempotencyKey}`
  const action = await persistence.actionLedger?.get(workspaceId, actionKey)
  if (action?.settlementStatus !== 'authorized') return
  await persistence.actionLedger?.transitionSettlementStatus({ workspaceId, actionKey, from: ['authorized'], to: 'pending_receipt' })
}

async function refundTaskUsage(workspaceId: string, taskId: string, idempotencyKey: string, actorId: string, reason: string) {
  const actionKey = `model:${idempotencyKey}`
  modelBillingReservations.delete(`${workspaceId}:${actionKey}`)
  const refunded = await (persistence.usage ?? memoryUsage).refund({ workspaceId, taskId, idempotencyKey, actorId, reason })
  const action = await persistence.actionLedger?.get(workspaceId, actionKey)
  if (action?.settlement === 'included_quota') await refundActionSettlement({ workspaceId, actionKey, reason })
  else await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: actionKey, actorId, reason })
  await releaseDailyModelBudget(workspaceId, actionKey)
  return refunded
}

const auditIdentityPattern = /^[A-Za-z0-9][A-Za-z0-9_.:-]{0,255}$/u

/** Security evidence must always have a concrete, unambiguous identity. */
export function validateOperationAuditContext(input: Pick<import('../../../packages/persistence/src/index.js').OperationAudit, 'workspaceId' | 'actorId' | 'action' | 'resourceType' | 'resourceId'>) {
  const fields = [input.workspaceId, input.actorId, input.action, input.resourceType, input.resourceId]
  if (fields.some(value => typeof value !== 'string' || !auditIdentityPattern.test(value))) {
    throw new DomainError('AUTHZ_AUDIT_CONTEXT_INVALID', '授权审计上下文无效，操作已拒绝', 503)
  }
  return true
}

async function recordOperationAudit(input: Omit<import('../../../packages/persistence/src/index.js').OperationAudit, 'id' | 'createdAt'>) {
  validateOperationAuditContext(input)
  await (persistence.operations ?? memoryOperations).append({ ...input, reason: redactAuditReason(input.reason) })
}

async function changeMemberStatusWithAudit(input: { workspaceId: string; externalSubject: string; targetStatus: MemberStatus; expectedRevision: number; actorId: string; action: string; reason: string }) {
  const repository = persistence.members ?? memoryMembers
  const current = (await repository.list(input.workspaceId)).find(item => item.externalSubject === input.externalSubject)
  if (!current) throw new DomainError('MEMBER_NOT_FOUND', '用户在该工作区不存在', 404)
  if (current.revision !== input.expectedRevision) throw new DomainError('MEMBER_REVISION_CONFLICT', '成员状态已变化，请刷新后重试', 409)
  if (current.status === input.targetStatus) {
    const code = input.targetStatus === 'active' ? 'MEMBER_ALREADY_ACTIVE' : 'MEMBER_ALREADY_SUSPENDED'
    throw new DomainError(code, input.targetStatus === 'active' ? '用户在该工作区已经是激活状态' : '用户在该工作区已经是停用状态', 409)
  }
  const auditInput = { workspaceId: input.workspaceId, actorId: input.actorId, action: input.action, resourceType: 'workspace_member', resourceId: input.externalSubject, before: current as unknown as Record<string, unknown>, reason: input.reason }
  try {
    const changed = await repository.changeStatusWithAudit(input)
    if (repository === memoryMembers) await memoryOperations.append({ ...auditInput, after: changed.member as unknown as Record<string, unknown> })
    return changed.member
  } catch (error) {
    if (error instanceof Error && error.message === 'MEMBER_REVISION_CONFLICT') throw new DomainError('MEMBER_REVISION_CONFLICT', '成员状态已变化，请刷新后重试', 409)
    throw error
  }
}

async function recordGrowthEvent(input: Omit<import('../../../packages/persistence/src/index.js').GrowthEvent, 'id' | 'occurredAt'> & { occurredAt?: string }) {
  await (persistence.growth ?? memoryGrowth).append(input)
}

async function requireActiveCommercialOffer(repository: CommercialExtensionsRepository, code: string, cycle: BillingCycle) {
  const now = Date.now()
  const offer = (await repository.listOffers()).find(item => item.code === code && item.billingCycle === cycle && item.active
    && Date.parse(item.validFrom) <= now
    && (!item.validTo || Date.parse(item.validTo) > now))
  if (!offer) throw new DomainError('COMMERCIAL_OFFER_NOT_AVAILABLE', '套餐不存在、未启用或当前不在销售周期内', 409, { plan_code: code, billing_cycle: cycle })
  return offer
}

const OPS_DOMAIN_METHODS = new Set([
  'ops.canonical.backfill.create', 'ops.canonical.backfill.get', 'ops.canonical.backfill.pause', 'ops.canonical.backfill.resume', 'ops.canonical.backfill.run',
  'ops.canonical.backfill.conflicts.list', 'ops.canonical.backfill.conflict.claim', 'ops.canonical.backfill.conflict.resolve',
  'ops.support.tickets.list', 'ops.support.ticket.get', 'ops.support.ticket.create', 'ops.support.ticket.assign', 'ops.support.ticket.transition', 'ops.support.ticket.comment', 'ops.support.sla.report', 'ops.support.sla.correction.create', 'ops.support.sla.correction.decide', 'ops.support.crm.export',
  'ops.incidents.list', 'ops.incident.get', 'ops.incident.timeline', 'ops.incident.create', 'ops.incident.transition', 'ops.incident.comment', 'ops.incident.commander.assign', 'ops.incident.scope.update',
  'ops.feature-flags.list', 'ops.feature-flag.upsert', 'ops.feature-flag.emergency.set', 'ops.feature-flag.events', 'ops.feature-flag.evaluate',
  'ops.finance.search', 'ops.finance.detail', 'ops.finance.export',
  'ops.audit.list', 'ops.audit.platform.list', 'ops.audit.detail', 'ops.audit.export', 'ops.marketing.image.evidence.export', 'ops.marketing.image.archive.audit', 'ops.marketing.image.billing.audit',
  'ops.member.upsert', 'ops.member.suspend',
])
const PLATFORM_FINANCE_METHODS = new Set(['ops.finance.search', 'ops.finance.detail', 'ops.finance.export'])
const PLATFORM_FEATURE_FLAG_CONTROL_METHODS = new Set(['ops.feature-flags.list', 'ops.feature-flag.upsert', 'ops.feature-flag.emergency.set', 'ops.feature-flag.events'])
/** Explicit control-plane scope registry. These methods still require an
 * authenticated workspace header for correlation, but skip tenant lifecycle
 * gates because they operate on platform-owned data. */
const PLATFORM_COMMERCIAL_METHODS = new Set([
  'ops.commercial.offers.list', 'ops.commercial.offer.upsert',
  'ops.commercial.addons.list', 'ops.commercial.addon.upsert',
  'ops.commercial.coupons.list', 'ops.commercial.coupon.upsert',
  'ops.commercial.export', 'ops.commercial.rollouts.list',
  'ops.commercial.rollout.upsert', 'ops.commercial.model-markup.get',
  'ops.commercial.model-markup.update',
])
export function isPlatformScopeMethod(method: string): boolean {
  return getMcpMethodPolicy(method)?.scope === 'platform'
}
const OPS_MCP_SCHEMA_OVERRIDE_METHODS = new Set(['ops.member.upsert', 'ops.member.suspend', 'catalog.image.review'])
const OPS_DOMAIN_PARAMS_MAX_BYTES = 128 * 1024

type PersistedVisualAuthenticity = {
  publishable: true
  candidateSha256: string
  evaluatedAt: string
  findingCodes: string[]
}

async function platformMediaSpecRepository() {
  await persistenceReady
  return persistence.platformMediaSpecs ?? memoryPlatformMediaSpecs
}

async function platformMappingApprovalRepository() {
  await persistenceReady
  return persistence.mappingPreflightApprovals ?? memoryMappingPreflightApprovals
}

function mappingPreflightApprovalError(error: unknown): never {
  if (!(error instanceof MappingPreflightApprovalRepositoryError)) throw error
  const mapped = error.code === 'MAPPING_PREFLIGHT_PRODUCT_SCOPE_MISMATCH'
    ? { status: 409, message: '字段映射批准与商品工作区范围不一致' }
    : error.code === 'MAPPING_PREFLIGHT_REVISION_CONFLICT'
      ? { status: 409, message: '字段映射批准已被其他请求更新，请重试' }
      : error.code === 'MAPPING_PREFLIGHT_NOT_FOUND'
        ? { status: 404, message: '字段映射批准不存在' }
        : { status: 400, message: '字段映射批准参数无效' }
  throw new DomainError(error.code, mapped.message, mapped.status)
}

function platformMediaSpecError(error: unknown): never {
  if (!(error instanceof PlatformMediaSpecRepositoryError)) throw error
  const mapped: Record<string, { status: number; message: string }> = {
    PLATFORM_MEDIA_SPEC_PLATFORM_OPS_REQUIRED: { status: 403, message: '平台媒体规格管理仅允许 platform_ops 角色' },
    PLATFORM_MEDIA_SPEC_INVALID: { status: 400, message: '平台媒体规格参数或证据无效' },
    PLATFORM_MEDIA_SPEC_NOT_FOUND: { status: 404, message: '平台媒体规格不存在' },
    PLATFORM_MEDIA_SPEC_APPROVAL_EVIDENCE_REQUIRED: { status: 409, message: '缺少可验证且未过期的批准证据' },
    PLATFORM_MEDIA_SPEC_REVISION_CONFLICT: { status: 409, message: '平台媒体规格版本已变化，请刷新后重试' },
    PLATFORM_MEDIA_SPEC_TRANSITION_INVALID: { status: 409, message: '平台媒体规格当前状态不允许该操作' },
    PLATFORM_MEDIA_SPEC_ACTIVE_CONFLICT: { status: 409, message: '同平台、版位和设备已有 active 规格' },
    PLATFORM_MEDIA_SPEC_IDEMPOTENCY_CONFLICT: { status: 409, message: '幂等键已绑定其他媒体规格操作' },
  }
  const value = mapped[error.code] ?? { status: 500, message: '平台媒体规格仓储失败' }
  throw new DomainError(error.code, value.message, value.status)
}

function asPlatform(value: unknown): PlatformMediaSpecPlatform | undefined {
  return typeof value === 'string' && PLATFORM_MEDIA_SPEC_PLATFORMS.includes(value as PlatformMediaSpecPlatform) ? value as PlatformMediaSpecPlatform : undefined
}

function asMediaDevice(value: unknown): PlatformMediaSpecDevice | undefined {
  return value === 'desktop' || value === 'mobile' ? value : undefined
}

function asMediaStatus(value: unknown): PlatformMediaSpecStatus | undefined {
  return value === 'draft' || value === 'approved' || value === 'expired' ? value : undefined
}

function mediaSpecIsActive(spec: StoredPlatformMediaSpec, at = new Date().toISOString()) {
  return spec.status === 'approved' && Boolean(spec.expiresAt) && Date.parse(spec.expiresAt!) > Date.parse(at)
}

async function evaluatePlatformMappingForWorkspace(req: IncomingMessage, workspaceId: string, input: PlatformFieldMappingGateInput) {
  const productId = input?.source?.productId
  if (typeof productId !== 'string' || !productId.trim()) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'mapping source.productId 无效', 400)
  const product = service.products.get(productId)
  if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '字段映射引用的商品不存在或不属于当前工作区', 404)
  await enforceProductBrandAccess(req, workspaceId, product.id)
  if (input.platform !== product.platform) throw new DomainError('PLATFORM_SCOPE_MISMATCH', '字段映射平台必须与商品平台一致', 409)
  if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
  // Evidence state supplied by a merchant request is not an attestation. Until a
  // trusted verifier is wired, production must not turn a self-declared
  // `production_canary` into a durable approval. Tests may opt into explicit
  // fixture evidence so the routing and persistence contract remains exercisable.
  const fixtureEvidenceAllowed = process.env.NODE_ENV === 'test' && process.env.ALLOW_FIXTURE_MAPPING_EVIDENCE === 'true'
  const evaluatedInput = fixtureEvidenceAllowed ? input : {
    ...input,
    schema: { ...input.schema, immutableEvidence: { ...input.schema.immutableEvidence, state: 'unverified' as const } },
    mapping: { ...input.mapping, immutableEvidence: { ...input.mapping.immutableEvidence, state: 'unverified' as const } },
    ...(input.previousSchema ? { previousSchema: { ...input.previousSchema, immutableEvidence: { ...input.previousSchema.immutableEvidence, state: 'unverified' as const } } } : {}),
  }
  let report: PlatformFieldMappingGateResult
  try { report = evaluatePlatformFieldMapping(evaluatedInput) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台字段映射输入结构无效', 400) }
  const productVersion = product.version ?? 1
  const evaluatedAt = new Date().toISOString()
  const maxAgeMs = Number(process.env.PLATFORM_MAPPING_PREFLIGHT_MAX_AGE_MS ?? 15 * 60 * 1_000)
  if (!Number.isSafeInteger(maxAgeMs) || maxAgeMs < 1 || maxAgeMs > 24 * 60 * 60 * 1_000) throw new DomainError('PLATFORM_MAPPING_PREFLIGHT_CONFIG_INVALID', 'PLATFORM_MAPPING_PREFLIGHT_MAX_AGE_MS 必须是 1 到 86400000 的整数', 503)
  const repository = await platformMappingApprovalRepository()
  try {
    const current = await repository.get({ workspaceId, platform: product.platform, productId: product.id })
    if (report.publishable && report.confirmationValid && !report.externallyUnverified) {
      await repository.upsert({
        workspaceId,
        platform: product.platform,
        productId: product.id,
        productVersion,
        mappedPayloadHash: report.mappedPayloadHash,
        remoteSnapshotHash: evaluatedInput.remoteSnapshot.hash,
        schemaVersion: evaluatedInput.schema.version,
        schemaEvidenceHash: evaluatedInput.schema.immutableEvidence.sha256,
        mappingVersion: evaluatedInput.mapping.version,
        mappingEvidenceHash: evaluatedInput.mapping.immutableEvidence.sha256,
        publishable: report.publishable,
        confirmationValid: report.confirmationValid,
        externallyUnverified: report.externallyUnverified,
        findingCodes: report.findings.map(finding => finding.code),
        evaluatedAt,
        expiresAt: new Date(Date.parse(evaluatedAt) + maxAgeMs).toISOString(),
        createdBy: requestActor(req),
        expectedRevision: current?.revision ?? 0,
      })
    } else if (current && !current.revokedAt) {
      await repository.revoke({ workspaceId, platform: product.platform, productId: product.id, expectedRevision: current.revision, revokedAt: evaluatedAt })
    }
  } catch (error) { mappingPreflightApprovalError(error) }
  await persistEvent(workspaceId, product.id, 'platform.mapping.preflight_evaluated', productVersion, {
    product_id: product.id,
    platform: product.platform,
    publishable: report.publishable,
    confirmation_valid: report.confirmationValid,
    externally_unverified: report.externallyUnverified,
    mapped_payload_hash: report.mappedPayloadHash,
    finding_codes: report.findings.map(finding => finding.code),
  })
  return report
}

async function requireCurrentPlatformMapping(task: Task) {
  if (!platformGovernanceGatesRequired()) return undefined
  const product = service.products.get(task.productId)
  if (!product) throw new DomainError('PRODUCT_NOT_FOUND', '发布任务引用的商品不存在', 404)
  const repository = await platformMappingApprovalRepository()
  try {
    const stored = await repository.get({ workspaceId: task.workspaceId, platform: task.platform, productId: task.productId })
    if (!stored) throw new DomainError('PLATFORM_MAPPING_PREFLIGHT_REQUIRED', '发布前必须完成字段映射确认', 409, { next_actions: ['platform.mapping.preflight'] })
    const approval = await repository.resolveActive({ workspaceId: task.workspaceId, platform: task.platform, productId: task.productId, productVersion: product.version ?? 1, mappedPayloadHash: stored.mappedPayloadHash, remoteSnapshotHash: stored.remoteSnapshotHash, schemaVersion: stored.schemaVersion, schemaEvidenceHash: stored.schemaEvidenceHash, mappingVersion: stored.mappingVersion, mappingEvidenceHash: stored.mappingEvidenceHash })
    if (!approval) throw new DomainError('PLATFORM_MAPPING_PREFLIGHT_REQUIRED', '字段映射批准已过期、撤销或与当前商品版本/证据不一致', 409, { next_actions: ['platform.mapping.preflight'] })
    return approval
  } catch (error) {
    if (error instanceof DomainError) throw error
    mappingPreflightApprovalError(error)
  }
}

function parseVisualAuthenticityInputs(params: JsonObject, visualRefs: readonly string[]) {
  const encoded = typeof params.authenticity_evidence_json === 'string' ? params.authenticity_evidence_json.trim() : ''
  if (!encoded) {
    if (platformGovernanceGatesRequired()) throw new DomainError('VISUAL_AUTHENTICITY_EVIDENCE_REQUIRED', '视觉候选审阅必须提供真实性证据', 409)
    return undefined
  }
  let parsed: unknown
  try { parsed = JSON.parse(encoded) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'authenticity_evidence_json 必须是合法 JSON', 400) }
  if (visualRefs.length === 1 && parsed && typeof parsed === 'object' && !Array.isArray(parsed) && !('visual_ref' in parsed)) return new Map([[visualRefs[0]!, parsed as VisualAuthenticityGateInput]])
  if (!Array.isArray(parsed) || parsed.length !== visualRefs.length) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '多候选真实性证据必须逐 visual_ref 提供且数量一致', 400)
  const result = new Map<string, VisualAuthenticityGateInput>()
  for (const item of parsed) {
    if (!item || typeof item !== 'object' || Array.isArray(item)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '真实性证据项无效', 400)
    const record = item as Record<string, unknown>
    if (typeof record.visual_ref !== 'string' || !visualRefs.includes(record.visual_ref) || !record.input || typeof record.input !== 'object' || Array.isArray(record.input) || result.has(record.visual_ref)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '真实性证据 visual_ref 与候选不匹配', 400)
    result.set(record.visual_ref, record.input as VisualAuthenticityGateInput)
  }
  return result
}

function evaluateVisualCandidates(workspaceId: string, visualRefs: readonly string[], params: JsonObject) {
  const inputs = parseVisualAuthenticityInputs(params, visualRefs)
  if (!inputs) return undefined
  const evaluated: Array<{ visualRef: string; output: Record<string, unknown>; report: ReturnType<typeof evaluateVisualAuthenticity>; evidence: PersistedVisualAuthenticity }> = []
  for (const visualRef of visualRefs) {
    const job = service.resolveImageGenerationByVisualRef(workspaceId, visualRef)
    const output = job.outputs?.find(candidate => candidate.visualRef === visualRef)
    const input = inputs.get(visualRef)
    if (!output || !input) throw new DomainError('VISUAL_NOT_FOUND', '真实性证据引用的视觉候选不存在', 404)
    const candidateHash = input.candidateImage?.hash?.replace(/^sha256:/iu, '').toLowerCase()
    if (!candidateHash || candidateHash !== output.sha256.toLowerCase()) throw new DomainError('VISUAL_AUTHENTICITY_HASH_MISMATCH', '真实性证据未绑定当前归档候选', 409)
    const report = evaluateVisualAuthenticity(input)
    if (!report.publishable) throw new DomainError('VISUAL_AUTHENTICITY_BLOCKED', '视觉真实性门禁阻断该候选', 409, { visual_ref: visualRef, finding_codes: report.findings.filter(finding => finding.status === 'block').map(finding => finding.code), requires_human_review: report.requiresHumanReview })
    evaluated.push({ visualRef, output: output as unknown as Record<string, unknown>, report, evidence: { publishable: true, candidateSha256: output.sha256, evaluatedAt: report.evaluatedAt, findingCodes: report.findings.map(finding => finding.code) } })
  }
  for (const item of evaluated) item.output.authenticity = item.evidence
  return evaluated.map(({ visualRef, report }) => ({ visual_ref: visualRef, ...report }))
}

function requireSelectedVisualAuthenticity(workspaceId: string, visualRefs: readonly string[]) {
  if (!platformGovernanceGatesRequired()) return
  for (const visualRef of visualRefs) {
    const job = service.resolveImageGenerationByVisualRef(workspaceId, visualRef)
    const output = job.outputs?.find(candidate => candidate.visualRef === visualRef)
    const authenticity = (output as unknown as { authenticity?: PersistedVisualAuthenticity } | undefined)?.authenticity
    if (!output || !authenticity?.publishable || authenticity.candidateSha256 !== output.sha256) throw new DomainError('VISUAL_AUTHENTICITY_EVIDENCE_REQUIRED', '选图前每个候选都必须通过与归档 SHA-256 绑定的真实性门禁', 409, { visual_ref: visualRef, next_actions: ['catalog.image.review'] })
  }
}

function evaluateStoryboardBeforeRendering(context: GenerationContext) {
  const evidence = (context as unknown as Record<string, unknown>).storyboardQuality
  if (!evidence) {
    if (platformGovernanceGatesRequired()) throw new DomainError('VIDEO_STORYBOARD_QUALITY_REQUIRED', '视频渲染前必须提供已验证的分镜质量证据', 409)
    return undefined
  }
  let report: ReturnType<typeof evaluateVideoStoryboardQuality>
  try { report = evaluateVideoStoryboardQuality(evidence as VideoStoryboardQualityInput) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'storyboardQuality 结构无效', 400) }
  if (!report.storyboardValid || report.externallyUnverified) throw new DomainError('VIDEO_STORYBOARD_QUALITY_BLOCKED', '分镜质量或平台规格证据未通过，禁止调用视频渲染 provider', 409, { finding_codes: report.blocks.map(finding => finding.code), externally_unverified: report.externallyUnverified })
  return report
}

function parseDeliveryBundleFiles(value: unknown[]): DeliveryBundleFile[] {
  return value.map((item, index) => {
    if (!item || typeof item !== 'object' || Array.isArray(item)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `files_json[${index}] 无效`, 400)
    const file = item as Record<string, unknown>
    const path = typeof file.path === 'string' ? file.path : ''
    const mimeType = typeof file.mimeType === 'string' ? file.mimeType : typeof file.mime_type === 'string' ? file.mime_type : ''
    const content = typeof file.content === 'string' ? file.content : typeof file.content_base64 === 'string' ? new Uint8Array(Buffer.from(file.content_base64, 'base64')) : undefined
    if (!path || !mimeType || content === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `files_json[${index}] 缺少 path、mimeType 或 content/content_base64`, 400)
    return { path, mimeType, content }
  })
}

function verifyExportedBundle(workspaceId: string, contentVersionId: string, binaryBody: Uint8Array) {
  const version = service.getContentVersion(workspaceId, contentVersionId)
  const task = service.getTask(version.taskId)
  const product = service.products.get(task.productId)
  if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '导出内容引用的商品不存在', 404)
  const brandId = task.brandId ?? `unassigned-${product.id}`
  const generatedAt = task.createdAt
  const input: DeliveryBundleManifestInput = {
    scope: { workspaceId, taskId: task.id, productId: product.id, brandId },
    entities: {
      workspace: { id: workspaceId, version: 'current' },
      task: { id: task.id, version: String(task.version), workspaceId, productId: product.id, brandId },
      product: { id: product.id, version: String(product.version ?? 1), workspaceId, brandId },
      brand: { id: brandId, version: 'current', workspaceId },
    },
    version: { contentVersionId: version.id, number: version.version, state: version.state, generatedAt, vector: (version.versionVector ? { ...version.versionVector } : {}) as unknown as Record<string, unknown> },
    factSources: [],
    ruleVersions: [],
    contentFiles: [{ path: 'content-bundle.zip', mimeType: 'application/zip', content: binaryBody }],
    deliveryVariants: [],
    assetPreviews: [],
    reviewFindings: version.state === 'approved'
      ? [{ code: 'CONTENT_VERSION_APPROVED', field: 'content', status: 'passed', message: '内容版本已批准；本校验仅证明导出传输完整性。', evidenceSourceIds: [] }]
      : [{ code: 'CONTENT_VERSION_NOT_APPROVED', field: 'content', status: 'blocked', message: '内容版本尚未批准。', evidenceSourceIds: [] }],
    reviewWaivers: [],
    sourceMap: [],
  }
  const built = buildDeliveryBundleManifest(input)
  if (!built.ok) throw new DomainError('DELIVERY_BUNDLE_BUILD_FAILED', '无法生成导出完整性 manifest', 500, { error_codes: built.errors.map(error => error.code) })
  const verification = verifyDeliveryBundle(built.manifest, built.files, built.manifestHash)
  return {
    ...verification,
    scope: 'transport_integrity_only',
    manifest_hash: built.manifestHash,
    artifact_sha256: createHash('sha256').update(binaryBody).digest('hex'),
    artifact_size_bytes: binaryBody.byteLength,
    content_publishable: built.manifest.publishable,
    verify_method: 'delivery.bundle.verify',
  }
}

function assertBoundedOpsParams(params: JsonObject) {
  if (Buffer.byteLength(JSON.stringify(params), 'utf8') > OPS_DOMAIN_PARAMS_MAX_BYTES) throw new DomainError(ERROR_CODES.REQUEST_BODY_TOO_LARGE, '运营控制面参数超过 128 KiB 上限', 413)
}

function aliasValue(input: JsonObject, camel: string, snake = camel): unknown {
  return input[camel] ?? input[snake]
}

function optionalStringValue(input: JsonObject, camel: string, snake = camel): string | undefined {
  const value = aliasValue(input, camel, snake)
  return typeof value === 'string' && value.trim() ? value.trim() : undefined
}

function requiredStringValue(input: JsonObject, camel: string, snake = camel): string {
  const value = optionalStringValue(input, camel, snake)
  if (!value) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `缺少必填字段: ${snake}`, 400)
  return value
}

function optionalNumberValue(input: JsonObject, camel: string, snake = camel): number | undefined {
  const value = aliasValue(input, camel, snake)
  if (value === undefined || value === null || value === '') return undefined
  const parsed = typeof value === 'number' ? value : typeof value === 'string' && /^\d+$/u.test(value) ? Number(value) : Number.NaN
  if (!Number.isSafeInteger(parsed)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${snake} 必须是整数`, 400)
  return parsed
}

function structuredValue(input: JsonObject, camel: string, snake = camel): unknown {
  const value = aliasValue(input, camel, snake)
  if (typeof value !== 'string') return value
  const trimmed = value.trim()
  if (!trimmed || (!trimmed.startsWith('{') && !trimmed.startsWith('['))) return value
  try { return JSON.parse(trimmed) }
  catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${snake} 必须是合法 JSON`, 400) }
}

function stringArrayValue(input: JsonObject, camel: string, snake = camel): string[] | undefined {
  const value = structuredValue(input, camel, snake)
  if (value === undefined || value === null || value === '') return undefined
  if (!Array.isArray(value) || value.some(item => typeof item !== 'string')) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${snake} 必须是字符串数组`, 400)
  return value
}

function opsDomainError(error: unknown): never {
  if (error instanceof DomainError) throw error
  if (error instanceof SupportAuthorizationError || error instanceof FeatureFlagAuthorizationError || error instanceof FinanceSearchAccessError || (error instanceof IncidentServiceError && error.code === 'INCIDENT_FORBIDDEN') || (error instanceof FinanceSearchServiceError && error.code === 'FINANCE_SEARCH_FORBIDDEN') || (error instanceof AuditCenterServiceError && error.code === 'AUDIT_CENTER_FORBIDDEN')) {
    throw new DomainError((error as { code?: string }).code ?? ERROR_CODES.FORBIDDEN, '当前身份无权执行该运营操作', 403)
  }
  if (error instanceof SupportTicketNotFoundError || (error instanceof IncidentServiceError && error.code === 'INCIDENT_NOT_FOUND') || (error instanceof IncidentRepositoryError && error.code === 'INCIDENT_NOT_FOUND') || (error instanceof FeatureFlagRepositoryError && error.code === 'FEATURE_FLAG_NOT_FOUND') || (error instanceof FinanceSearchServiceError && error.code === 'FINANCE_RECORD_NOT_FOUND') || (error instanceof AuditCenterServiceError && error.code === 'AUDIT_EVENT_NOT_FOUND')) {
    throw new DomainError((error as { code?: string }).code ?? ERROR_CODES.NOT_FOUND, error.message, 404)
  }
  if (error instanceof SupportTicketRevisionConflictError || error instanceof SupportTicketIdempotencyConflictError || error instanceof FinanceRecordVersionConflictError || (error instanceof IncidentRepositoryError && ['INCIDENT_REVISION_CONFLICT', 'INCIDENT_IDEMPOTENCY_CONFLICT', 'INCIDENT_INVALID_TRANSITION'].includes(error.code)) || (error instanceof IncidentServiceError && error.code === 'INCIDENT_INVALID_TRANSITION') || (error instanceof FeatureFlagRepositoryError && ['FEATURE_FLAG_REVISION_CONFLICT', 'FEATURE_FLAG_IDEMPOTENCY_CONFLICT'].includes(error.code))) {
    throw new DomainError((error as { code?: string }).code ?? 'OPS_REVISION_CONFLICT', error.message, 409)
  }
  if (error instanceof SupportValidationError || error instanceof IncidentContractError || error instanceof FeatureFlagValidationError || error instanceof FinanceSearchValidationError || error instanceof FinanceSearchCursorError || error instanceof FeatureFlagRepositoryError || error instanceof IncidentRepositoryError || error instanceof FinanceSearchServiceError || error instanceof AuditCenterContractError || error instanceof AuditCenterCursorError || error instanceof AuditCenterServiceError) {
    throw new DomainError((error as { code?: string }).code ?? ERROR_CODES.INVALID_REQUEST, error.message, 400)
  }
  throw error
}

async function invokeOpsDomain<T>(operation: () => Promise<T>): Promise<T> {
  try { return await operation() }
  catch (error) { return opsDomainError(error) }
}

function supportContext(req: IncomingMessage, workspaceId: string): SupportAuthorizationContext {
  const principal = requestPrincipals.get(req)
  const granted = new Set(principal?.activeAuthorizationGrants?.flatMap(grant => grant.capabilities) ?? [])
  if (principal?.actorId && [...granted].some(capability => capability.startsWith('support.'))) {
    const permissions: SupportPermission[] = []
    if (granted.has('support.ticket.read')) permissions.push('support.ticket.read')
    if (granted.has('support.ticket.update')) permissions.push('support.ticket.create', 'support.ticket.assign', 'support.ticket.transition', 'support.ticket.comment')
    if (granted.has('support.customer.export')) permissions.push('support.crm.export')
    return { actorId: principal.actorId, role: 'support', workspaceId, permissions }
  }
  const actorId = requireOperationsRole(req, ['support', 'platform_ops'])
  const role: SupportRole = isPlatformOperations(req) ? 'platform_ops' : 'support'
  return { actorId, role, workspaceId, permissions: supportRolePermissions[role] as readonly SupportPermission[] }
}

function incidentActor(req: IncomingMessage, workspaceId: string) {
  const actorId = requireOperationsRole(req, ['support', 'platform_ops'])
  const roles = authorizedRoles(requestPrincipals.get(req)).filter((role): role is 'support' | 'platform_ops' => role === 'support' || role === 'platform_ops')
  return { actorId, workspaceId, roles }
}

function featureFlagActor(req: IncomingMessage, allowed: readonly string[]) {
  const actorId = requireOperationsRole(req, allowed)
  const roles = effectiveAuthorizationProjection(requestPrincipals.get(req), header(req, 'x-workspace-id')?.trim() ?? '').canonicalRoles
  return {
    id: actorId,
    roles: roles.includes('platform_admin') ? ['platform_admin'] as const
      : roles.includes('ops_admin') ? ['ops_admin'] as const
        : roles.includes('support_agent') || roles.includes('workspace_support') ? ['support'] as const
          : roles.includes('finance_ops') || roles.includes('finance') ? ['finance'] as const
            : roles.includes('reviewer') ? ['reviewer'] as const
              : ['member'] as const,
    workspaceIds: requestPrincipals.get(req)?.workspaces.filter(value => value !== '*') ?? [],
  }
}

async function canonicalProductReadControl(workspaceId: string): Promise<{ mode: CanonicalProductReadMode; source: 'feature_flag' | 'default'; reason?: string; revision?: number }> {
  const repository = persistence.featureFlags
  if (!repository) return { mode: 'legacy_shadow', source: 'default', reason: 'FEATURE_FLAGS_UNAVAILABLE' }
  const evaluation = await repository.evaluate({ flagKey: CANONICAL_PRODUCT_READ_MODE_FLAG, environment: process.env.NODE_ENV ?? 'development', workspaceId })
  const mode = canonicalProductReadModeFromFlag(evaluation)
  if (mode === 'legacy_shadow' && evaluation.enabled && evaluation.value !== undefined && evaluation.value !== 'legacy_shadow') {
    return { mode, source: 'default', reason: 'CANONICAL_READ_MODE_INVALID', ...(evaluation.revision !== undefined ? { revision: evaluation.revision } : {}) }
  }
  return { mode, source: evaluation.matchedBy === 'missing' || evaluation.matchedBy === 'disabled' || evaluation.matchedBy === 'emergency' ? 'default' : 'feature_flag', ...(evaluation.revision !== undefined ? { revision: evaluation.revision } : {}) }
}

export function featureFlagRequestsCanonicalRead(input: {
  key: string
  defaultValue?: { value?: unknown }
  targets?: Array<{ override?: { value?: unknown } }>
}) {
  if (input.key !== CANONICAL_PRODUCT_READ_MODE_FLAG) return false
  return input.defaultValue?.value === 'canonical_read' || (Array.isArray(input.targets) && input.targets.some(target => target?.override?.value === 'canonical_read'))
}

function financePrincipal(req: IncomingMessage) {
  const actorId = requireOperationsRole(req, ['finance', 'platform_ops'])
  const principal = requestPrincipals.get(req)
  return { actorId, roles: authorizedRoles(principal), authorizedWorkspaceIds: principal?.workspaces.filter(value => value !== '*') ?? [] }
}

function auditCenterPrincipal(req: IncomingMessage, workspaceId: string) {
  const principal = requestPrincipals.get(req)
  const actorId = principal?.actorId ?? requestActor(req, 'actor_demo')
  const claimedRoles = authorizedRoles(principal).filter((role): role is AuditAccessRole => role === 'support' || role === 'finance' || role === 'platform_ops')
  const decision = requestAuthorizationDecisions.get(req)
  const workspaceReadAuthorized = Boolean(decision?.authorized && decision.capability === 'audit.read'
    && decision.workbench === 'workspace' && decision.scope.required === 'workspace' && decision.scope.resource_id === workspaceId)
  // Local fixture calls historically omitted x-role. Keep those fixtures
  // operable without upgrading an explicitly claimed support role to finance.
  const roles: AuditAccessRole[] = !requiresStrictAuth() && claimedRoles.length === 0 ? ['platform_ops'] : [...claimedRoles, ...(workspaceReadAuthorized ? ['reader' as const] : [])]
  const authorizedWorkspaceIds = principal?.workspaces.includes('*') ? [workspaceId] : principal?.workspaces.filter(value => value !== '*') ?? [workspaceId]
  return { actorId, roles, authorizedWorkspaceIds }
}

async function externalProviderUsageStatement(input: {
  modelUsage: Array<{ providerRequestId?: string; inputTokens?: number; outputTokens?: number; totalTokens?: number }>
  fromAt?: string
  toAt?: string
}) {
  const client = createNewApiSelfLogClientFromEnv()
  if (!client) return { status: 'externally_unverified', source: 'wormhole_new_api_log_self', endpoint: '/api/log/self', auth: 'user_session_required', note: '未配置中转站只读用户凭据；当前仅完成本地账本、action 和钱包核对。' }
  try {
    const statement = await client.listAll({ ...(input.fromAt ? { startTimestamp: Math.floor(Date.parse(input.fromAt) / 1000) } : {}), ...(input.toAt ? { endTimestamp: Math.floor(Date.parse(input.toAt) / 1000) } : {}) })
    const comparison = compareProviderUsageRecords(input.modelUsage, statement.records)
    const localByRequest = comparison.localByRequest
    const unmatchedLocal = comparison.unmatchedLocal
    const unmatchedProvider = comparison.unmatchedProvider
    const localTokens = input.modelUsage.reduce((sum, item) => sum + (item.totalTokens ?? 0), 0)
    const providerTokens = statement.records.reduce((sum, item) => sum + (item.totalTokens ?? 0), 0)
    const tokenDifference = providerTokens - localTokens
    const matched = unmatchedLocal === 0 && unmatchedProvider === 0 && comparison.duplicateLocalCount === 0 && comparison.duplicateProviderCount === 0 && comparison.tokenMismatchCount === 0 && tokenDifference === 0
    return { status: matched ? 'balanced' : 'needs_review', source: 'wormhole_new_api_log_self', endpoint: '/api/log/self', auth: 'user_session_required', pages: statement.pages, provider_record_count: statement.records.length, matched_record_count: comparison.matchedRecordCount, unmatched_local_count: unmatchedLocal, unmatched_provider_count: unmatchedProvider, duplicate_local_count: comparison.duplicateLocalCount, duplicate_provider_count: comparison.duplicateProviderCount, token_mismatch_count: comparison.tokenMismatchCount, local_total_tokens: localTokens, provider_total_tokens: providerTokens, token_difference: tokenDifference, note: matched ? '中转站用户日志与本地 provider request ID、逐笔 token 核对一致；金额仍以供应商账单或 quota 口径单独核验。' : '中转站用户日志与本地账本存在记录、重复 request 或逐笔 token 差异，已阻断平账。' }
  } catch (error) {
    return { status: 'externally_unverified', source: 'wormhole_new_api_log_self', endpoint: '/api/log/self', auth: 'user_session_required', error: error instanceof Error ? error.message.slice(0, 200) : 'provider usage query failed', note: '中转站日志读取失败；不会把本地一致视为供应商已平账。' }
  }
}

export function compareProviderUsageRecords(
  local: Array<{ providerRequestId?: string; inputTokens?: number; outputTokens?: number; totalTokens?: number }>,
  provider: Array<{ providerRecordId: string; inputTokens?: number; outputTokens?: number; totalTokens?: number }>,
) {
  const localByRequest = new Map<string, { inputTokens?: number; outputTokens?: number; totalTokens?: number }>()
  let duplicateLocalCount = 0
  for (const item of local) {
    const id = item.providerRequestId?.trim()
    if (!id) continue
    if (localByRequest.has(id)) duplicateLocalCount += 1
    else localByRequest.set(id, item)
  }
  const providerByRequest = new Map<string, typeof provider[number]>()
  let duplicateProviderCount = 0
  for (const item of provider) {
    if (providerByRequest.has(item.providerRecordId)) duplicateProviderCount += 1
    else providerByRequest.set(item.providerRecordId, item)
  }
  const unmatchedLocal = [...localByRequest.keys()].filter(id => !providerByRequest.has(id)).length
  const unmatchedProvider = [...providerByRequest.keys()].filter(id => !localByRequest.has(id)).length
  let tokenMismatchCount = 0
  let matchedRecordCount = 0
  for (const [id, localRecord] of localByRequest) {
    const providerRecord = providerByRequest.get(id)
    if (!providerRecord) continue
    matchedRecordCount += 1
    if ((localRecord.inputTokens ?? 0) !== (providerRecord.inputTokens ?? 0) || (localRecord.outputTokens ?? 0) !== (providerRecord.outputTokens ?? 0) || (localRecord.totalTokens ?? 0) !== (providerRecord.totalTokens ?? 0)) tokenMismatchCount += 1
  }
  return { localByRequest, unmatchedLocal, unmatchedProvider, duplicateLocalCount, duplicateProviderCount, tokenMismatchCount, matchedRecordCount }
}

export function shouldHydrateKnowledgeForMethod(method: string, bypassWorkspaceLifecycleGate: boolean, isOpsDomainMethod: boolean) {
  return method !== 'workspace.bootstrap' && !bypassWorkspaceLifecycleGate && (!isOpsDomainMethod || method.startsWith('knowledge.'))
}

/**
 * API-owned evidence wrapper. The application checker remains authoritative
 * for statuses, blocking details, relations, and next actions. Two passes
 * are intentional: the first computes a deterministic report revision, and
 * the second embeds that revision into the same application contract.
 */
export function canonicalConsistencyApiReport(input: CanonicalChainConsistencyInput, source: 'memory' | 'postgres', generatedAt = new Date().toISOString()) {
  const evaluation = { generatedAt, readMode: source === 'postgres' ? 'live' as const : 'snapshot' as const, freshness: 'fresh' as const, availability: 'available' as const }
  const initial = buildCanonicalChainConsistencyReport({ ...input, evaluation })
  const canonicalizedFindings = initial.findings.map(finding => ({ ...finding, canonicalProductId: finding.canonicalProductId ?? null }))
  const revision = createHash('sha256').update(canonicalJson({ workspaceId: initial.workspaceId, status: initial.status, contractStatus: initial.contractStatus, counts: initial.counts, findings: canonicalizedFindings, orphanFindings: initial.orphanFindings })).digest('hex')
  const finalReport = buildCanonicalChainConsistencyReport({ ...input, evaluation: { ...evaluation, revision } })
  return { ...finalReport, findings: finalReport.findings.map(finding => ({ ...finding, canonicalProductId: finding.canonicalProductId ?? null })) }
}

/**
 * Convert consistency findings into the existing conflict-queue shape. The
 * queue's natural key is (audit batch, legacy product, code); keeping the
 * finding code and legacy product ID unchanged makes retries idempotent and
 * gives operators a stable object to claim.
 */
export function canonicalConflictScanItems(report: ReturnType<typeof canonicalConsistencyApiReport>) {
  return report.findings
    .filter(finding => finding.status !== 'verified' && finding.codes.length > 0)
    .flatMap(finding => finding.codes.map(code => ({ legacyProductId: finding.legacyProductId, code, canonicalIds: finding.canonicalProductId ? [finding.canonicalProductId] : [] })))
    .sort((left, right) => left.legacyProductId.localeCompare(right.legacyProductId) || left.code.localeCompare(right.code))
}

/**
 * A queue mutation is not proof that the underlying relation was repaired.
 * Before an operator can mark a known backfill conflict resolved, compare the
 * current consistency observation for that exact legacy product.  The
 * collision code has no equivalent read-side finding and therefore remains
 * non-resolvable through this path until a dedicated remediation proof exists.
 */
export function canonicalConflictResolutionCheck(input: {
  conflict: { legacyProductId: string; code: string }
  report: ReturnType<typeof canonicalConsistencyApiReport>
}): { passed: boolean; findingCodes: string[] } {
  const finding = input.report.findings.find(item => item.legacyProductId === input.conflict.legacyProductId)
  const findingCodes = finding?.codes ?? []
  if (input.conflict.code === 'CANONICAL_ID_COLLISION') return { passed: false, findingCodes }
  const equivalentCodes = input.conflict.code === 'CANONICAL_BRAND_MISMATCH' ? ['BRAND_SCOPE_MISMATCH', 'MISSING_BRAND'] : [input.conflict.code]
  return { passed: !findingCodes.some(code => equivalentCodes.includes(code)), findingCodes }
}

async function persistCanonicalLinkAudit(report: ReturnType<typeof canonicalConsistencyApiReport>) {
  const repository = persistence.unifiedLinkAudit
  // The in-memory repository is a test/dev projection, not durable evidence.
  // A read-only dry-run must not describe that process-local write as persisted.
  if (!repository || persistence.mode !== 'postgres') return { persisted: false, count: 0 }
  const observedAt = report.generatedAt ?? new Date().toISOString()
  const checkRevision = String(report.revision ?? report.generatedAt ?? observedAt)
  const checksum = String(report.revision ?? createHash('sha256').update(canonicalJson(report)).digest('hex'))
  const findings = report.findings.map(finding => ({
    auditKey: `product:${finding.legacyProductId}`, entityType: 'product' as const, entityId: finding.legacyProductId, legacyProductId: finding.legacyProductId,
    ...(finding.canonicalProductId ? { canonicalProductId: finding.canonicalProductId } : {}), ...(finding.listingIds[0] ? { listingId: finding.listingIds[0] } : {}), ...(finding.campaignItemIds[0] ? { campaignItemId: finding.campaignItemIds[0] } : {}), ...(finding.taskIds[0] ? { taskId: finding.taskIds[0] } : {}), ...(finding.publishJobIds[0] ? { publishJobId: finding.publishJobIds[0] } : {}),
    status: finding.status, codes: finding.codes, checkRevision, checksum, observedAt, ...(finding.blocking?.message ? { lastError: finding.blocking.message } : {}),
  }))
  const orphanFindings = report.orphanFindings.map(finding => ({
    auditKey: `${finding.entityType}:${finding.entityId}`, entityType: finding.entityType, entityId: finding.entityId, status: finding.status === 'conflict' ? 'conflict' as const : 'blocked' as const, codes: finding.codes, checkRevision, checksum, observedAt, ...(finding.blocking?.message ? { lastError: finding.blocking.message } : {}),
  }))
  for (const item of [...findings, ...orphanFindings]) await repository.upsert({ workspaceId: report.workspaceId, ...item })
  return { persisted: true, count: findings.length + orphanFindings.length }
}

async function routeMcp(req: IncomingMessage, res: ServerResponse, input: JsonObject) {
  // Some methods use API-local parameter schemas while their shared contract
  // catches up. They must still pass the JSON-RPC envelope boundary before
  // authorization, tenant lookup, or handler dispatch.
  assertMcpEnvelope(input)
  const request = input as unknown as McpRequest
  const method = typeof request.method === 'string' ? request.method : ''
  const params = paramsOf(input)
  const isPlatformWideUserGovernance = isPlatformScopeMethod(method)
  const requestWorkbench = requestPrincipals.get(req)?.workbench ?? 'workspace'
  const bypassWorkspaceLifecycleGate = isPlatformScopeMethod(method) || (method === 'ops.session' && requestWorkbench === 'platform')
  let workspaceId = method === 'workspace.bootstrap'
    ? ''
    : requestWorkbench === 'platform'
      ? header(req, 'x-workspace-id')?.trim() ?? ''
      : resolveWorkspace(req, isPlatformWideUserGovernance ? undefined : params.workspace_id)
  enrichRequestObservation(req, {
    workspaceId: workspaceId || undefined,
    actorId: trustedRequestObservationActor(req),
  })
  if (isPlatformWideUserGovernance && typeof params.workspace_id === 'string' && params.workspace_id.trim()) knownWorkspaces.add(params.workspace_id.trim())
  const id = request.id ?? null
  const result = (value: unknown) => send(res, 200, workspaceId, { jsonrpc: '2.0', id, result: value }, null, req)

  // merchant.first_value is kept server-compatible while the shared contract
  // is being extended. Once the contract contains it, this naturally falls
  // through to the normal method/schema validation path.
  const isFirstValueMethod = method === 'merchant.first_value'
  const isOpsDomainMethod = OPS_DOMAIN_METHODS.has(method)
  const isCampaignLifecycleMethod = CAMPAIGN_LIFECYCLE_METHODS.has(method)
  if (!isMcpMethod(method) && !isFirstValueMethod && !isOpsDomainMethod && !isCampaignLifecycleMethod) throw new DomainError(ERROR_CODES.MCP_METHOD_NOT_FOUND, `不支持的 MCP 方法: ${method}`, 404)
  if (isMcpMethod(method) && !OPS_MCP_SCHEMA_OVERRIDE_METHODS.has(method)) {
    const validation = validateMcpRequest(input)
    if (!validation.valid) throw new DomainError(ERROR_CODES.INVALID_REQUEST, validation.errors.join('; '), 400)
  }
  if (isOpsDomainMethod) assertBoundedOpsParams(params)
  if (isCampaignLifecycleMethod) assertCampaignLifecycleParams(method, params)
  await enforceMcpWorkbenchBoundary(req, workspaceId, method, params)
  if (method !== 'workspace.bootstrap' && !bypassWorkspaceLifecycleGate) {
    const hasMember = await resolveActiveWorkspaceMember(req, workspaceId, false)
    if (!hasMember && !requestPrincipals.get(req)?.activeAuthorizationGrants?.length) await enforceActiveWorkspaceMember(req, workspaceId)
    await requireActiveWorkspace(workspaceId, method)
  }
  await enforceRegisteredMcpCapability(req, workspaceId, method, params)
  if (method !== 'workspace.bootstrap' && !bypassWorkspaceLifecycleGate && !requestMemberChecks.has(req)
    && !exactConsumedGrantForRequest(req, workspaceId)
    && !(method === 'ops.session' && requestPrincipals.get(req)?.activeAuthorizationGrants?.length)) {
    await enforceActiveWorkspaceMember(req, workspaceId)
  }
  await enforceCustomerDataAccess(req, workspaceId, method)
  if (method !== 'workspace.bootstrap' && !bypassWorkspaceLifecycleGate) requireStoreOnboarding(workspaceId, method)
  if (shouldHydrateKnowledgeForMethod(method, bypassWorkspaceLifecycleGate, isOpsDomainMethod)) await hydrateKnowledge(workspaceId)
  const workspaceBillingMethod = method === 'billing.usage.consume' || method === 'billing.usage.refund'
  if (typeof params.task_id === 'string' && params.task_id.trim()) {
    const scopedTask = scopeTask(req, params.task_id.trim())
    if (!workspaceBillingMethod) await enforceTaskBrandAccess(req, scopedTask, taskRoleForOperation(method))
    enrichRequestObservation(req, { taskId: scopedTask.id, platform: scopedTask.platform, accountId: scopedTask.accountId })
  } else if (typeof params.content_version_id === 'string' && params.content_version_id.trim()) {
    const scoped = scopeContentVersion(req, params.content_version_id.trim())
    await enforceTaskBrandAccess(req, scoped.task, taskRoleForOperation(method))
    enrichRequestObservation(req, { taskId: scoped.task.id, platform: scoped.task.platform, accountId: scoped.task.accountId })
  }
  if (typeof params.product_id === 'string' && params.product_id.trim() && method !== 'catalog.import') {
    await enforceProductBrandAccess(req, workspaceId, params.product_id.trim())
    const scopedProduct = service.products.get(params.product_id.trim())
    if (scopedProduct?.workspaceId === workspaceId) enrichRequestObservation(req, { platform: scopedProduct.platform, accountId: scopedProduct.accountId })
  }
  switch (method) {
    case 'merchant.first_value':
      return result(merchantFirstValuePreview(workspaceId, params))
    case 'brand-unit.list': {
      const brandId = typeof params.brand_id === 'string' && params.brand_id.trim() ? params.brand_id.trim() : undefined
      const platform = typeof params.platform === 'string' ? params.platform as Platform : undefined
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      if (accountId && !platform) throw new DomainError('STORE_PLATFORM_REQUIRED', '使用 account_id 筛选品时必须同时指定 platform', 400)
      await persistenceReady
      const listed = await (persistence.brandUnits ?? memoryBrandUnits).listBrands({ workspaceId, ...(brandId ? { brandId } : {}), ...(platform ? { platform } : {}), ...(accountId ? { accountId } : {}) })
      if (brandId) await enforceBrandAccess(req, workspaceId, brandId)
      const items = !requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)
        ? listed
        : (await Promise.all(listed.map(async item => await (persistence.brandUnits ?? memoryBrandUnits).hasBrandAccess({ workspaceId, brandId: item.id, externalSubject: requestPrincipals.get(req)!.actorId }) ? item : undefined))).filter((item): item is typeof listed[number] => Boolean(item))
      return result({ items, count: items.length, storage: persistence.mode, durable: persistence.mode === 'postgres', ...(persistence.mode === 'memory' ? { message: '当前为本地 fixture 运行；生产环境会写入 PostgreSQL。' } : {}) })
    }
    case 'brand-unit.create': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const name = required(params, 'name').normalize('NFKC').trim()
      if (name.length > 120) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '品名称不能超过 120 个字符', 400)
      const requestedId = typeof params.brand_id === 'string' && params.brand_id.trim() ? params.brand_id.trim() : `brand_unit_${randomUUID().replaceAll('-', '').slice(0, 24)}`
      if (!/^[A-Za-z0-9][A-Za-z0-9_-]{1,63}$/u.test(requestedId)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'brand_id 必须是 2 至 64 个字母、数字、下划线或连字符', 400)
      await persistenceReady
      try {
        const unit = await (persistence.brandUnits ?? memoryBrandUnits).createBrand({ workspaceId, id: requestedId, name })
        return result({ ...unit, storage: persistence.mode, durable: persistence.mode === 'postgres', ...(persistence.mode === 'memory' ? { message: '当前为本地 fixture 运行；生产环境会写入 PostgreSQL。' } : {}) })
      } catch (error) {
        if (String(error).includes('BRAND_UNIT_CONFLICT') || (error as { code?: string })?.code === '23505') throw new DomainError('BRAND_UNIT_CONFLICT', 'brand_id 或品名称已存在，请换一个标识', 409, { brand_id: requestedId })
        throw error
      }
    }
    case 'brand-unit.bind-store': {
      const brandId = required(params, 'brand_id')
      await enforceBrandAccess(req, workspaceId, brandId, 'editor')
      const platform = required(params, 'platform') as Platform
      const accountId = required(params, 'account_id')
      const expectedRevision = params.expected_revision === undefined ? undefined : Number(params.expected_revision)
      if (params.expected_revision !== undefined && (typeof params.expected_revision !== 'string' || !/^[1-9][0-9]*$/u.test(params.expected_revision) || !Number.isSafeInteger(expectedRevision))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_revision 必须是正整数字符串', 400)
      await persistenceReady
      const account = isProduction() ? service.getActivePlatformAccount(workspaceId, accountId, platform) : service.getPlatformAccount(workspaceId, accountId, platform)
      if (!account) throw new DomainError('PLATFORM_ACCOUNT_NOT_FOUND', '平台账号不存在或不属于当前工作区', 404)
      try {
        const unit = await (persistence.brandUnits ?? memoryBrandUnits).bindStore({ workspaceId, brandId, platform, accountId, ...(expectedRevision !== undefined ? { expectedRevision } : {}) })
        await recordOperationAudit({ workspaceId, actorId: requestActor(req), action: 'brand.store.bind', resourceType: 'brand_store_binding', resourceId: `${brandId}:${platform}:${accountId}`, before: {}, after: { brand_id: brandId, platform, account_id: accountId, status: 'active' }, reason: typeof params.reason === 'string' && params.reason.trim() ? params.reason.trim() : '绑定品牌与平台店铺' })
        return result({ brandUnit: unit, boundStore: { platform: account.platform, accountId: account.id, tokenState: account.tokenState }, storage: persistence.mode, durable: persistence.mode === 'postgres', ...(persistence.mode === 'memory' ? { message: '当前为本地 fixture 运行；生产环境会写入 PostgreSQL。' } : {}) })
      } catch (error) {
        if (String(error).includes('BRAND_STORE_REVISION_CONFLICT')) throw new DomainError('BRAND_STORE_REVISION_CONFLICT', '品的店铺绑定已被其他操作更新，请刷新后重试', 409)
        if (String(error).includes('BRAND_UNIT_NOT_FOUND')) throw new DomainError('BRAND_UNIT_NOT_FOUND', '品不存在或不属于当前工作区', 404)
        throw error
      }
    }
    case 'brand-unit.product.create': {
      const brandId = required(params, 'brand_id')
      await enforceBrandAccess(req, workspaceId, brandId, 'editor')
      const title = required(params, 'title').normalize('NFKC').trim()
      if (title.length > 256) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '商品标题不能超过 256 个字符', 400)
      await persistenceReady
      const brands = await (persistence.brandUnits ?? memoryBrandUnits).listBrands({ workspaceId, brandId })
      if (!brands[0]) throw new DomainError('BRAND_UNIT_NOT_FOUND', '品不存在或不属于当前工作区', 404, { brand_id: brandId })
      const id = typeof params.product_id === 'string' && params.product_id.trim() ? params.product_id.trim() : `canonical_product_${randomUUID().replaceAll('-', '').slice(0, 24)}`
      const sourceProductId = typeof params.source_product_id === 'string' && params.source_product_id.trim() ? params.source_product_id.trim() : undefined
      let canonicalFacts: Record<string, unknown> = {}
      if (sourceProductId) {
        const sourceProduct = service.products.get(sourceProductId)
        if (!sourceProduct || sourceProduct.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', 'source_product_id 不存在或不属于当前工作区', 404, { source_product_id: sourceProductId })
        await enforceProductBrandAccess(req, workspaceId, sourceProductId)
        const recordedBrandId = typeof (sourceProduct as Product & { brandId?: unknown }).brandId === 'string'
          ? (sourceProduct as Product & { brandId: string }).brandId.trim()
          : typeof sourceProduct.rawPlatformFields?.brand_id === 'string'
            ? sourceProduct.rawPlatformFields.brand_id.trim()
            : ''
        if (recordedBrandId && recordedBrandId !== brandId) throw new DomainError('PRODUCT_BRAND_SCOPE_MISMATCH', 'source_product_id 已归属其他品，不能跨品建立 canonical 商品关系', 409, { source_product_id: sourceProductId, source_brand_id: recordedBrandId, requested_brand_id: brandId })
        if (!sourceProduct.factsConfirmed) throw new DomainError('CANONICAL_PRODUCT_FACTS_REQUIRED', '建立标准商品前必须先确认旧商品事实', 409, { source_product_id: sourceProductId, next_action: 'catalog.facts.confirm' })
        canonicalFacts = {
          ...(sourceProduct.category ? { category: sourceProduct.category } : {}),
          ...(sourceProduct.attributes ? { attributes: structuredClone(sourceProduct.attributes) } : {}),
          ...(typeof sourceProduct.price === 'number' ? { price: sourceProduct.price } : {}),
          sku_ids: (sourceProduct.skus ?? []).map(sku => sku.id),
          selling_points: (sourceProduct.sellingPoints ?? []).filter(point => point.proofStatus === 'confirmed').map(point => point.text),
        }
      }
      try {
        const product = await (persistence.brandUnits ?? memoryBrandUnits).createCanonicalProduct({ workspaceId, id, brandId, title, facts: canonicalFacts, ...(sourceProductId ? { sourceProductId } : {}) })
        return result({ ...product, storage: persistence.mode, durable: persistence.mode === 'postgres' })
      } catch (error) {
        if (String(error).includes('CANONICAL_PRODUCT_CONFLICT') || (error as { code?: string })?.code === '23505') throw new DomainError('CANONICAL_PRODUCT_CONFLICT', 'canonical product_id 已存在，请换一个标识', 409, { product_id: id })
        throw error
      }
    }
    case 'brand-unit.listing.create': {
      const brandId = required(params, 'brand_id')
      await enforceBrandAccess(req, workspaceId, brandId, 'editor')
      const canonicalProductId = required(params, 'canonical_product_id')
      const platform = required(params, 'platform') as Platform
      const accountId = required(params, 'account_id')
      await persistenceReady
      const brands = await (persistence.brandUnits ?? memoryBrandUnits).listBrands({ workspaceId, brandId, platform, accountId })
      if (!brands[0]) throw new DomainError('BRAND_STORE_BINDING_REQUIRED', '创建 listing 前必须先将店铺绑定到该品', 409, { brand_id: brandId, platform, account_id: accountId })
      const account = isProduction() ? service.getActivePlatformAccount(workspaceId, accountId, platform) : service.getPlatformAccount(workspaceId, accountId, platform)
      if (!account) throw new DomainError('PLATFORM_ACCOUNT_NOT_FOUND', '平台账号不存在或不属于当前工作区', 404)
      const id = typeof params.listing_id === 'string' && params.listing_id.trim() ? params.listing_id.trim() : `listing_${randomUUID().replaceAll('-', '').slice(0, 24)}`
      try {
        const listing = await (persistence.brandUnits ?? memoryBrandUnits).createListing({ workspaceId, id, brandId, canonicalProductId, platform, accountId, ...(typeof params.remote_product_id === 'string' && params.remote_product_id.trim() ? { remoteProductId: params.remote_product_id.trim() } : {}) })
        await recordOperationAudit({ workspaceId, actorId: requestActor(req), action: 'brand.listing.create', resourceType: 'product_listing', resourceId: listing.id, before: {}, after: listing as unknown as Record<string, unknown>, reason: typeof params.reason === 'string' && params.reason.trim() ? params.reason.trim() : '创建平台店铺商品关系' })
        return result({ ...listing, storage: persistence.mode, durable: persistence.mode === 'postgres' })
      } catch (error) {
        if (String(error).includes('LISTING_CONFLICT') || (error as { code?: string })?.code === '23505') throw new DomainError('LISTING_CONFLICT', 'listing_id 已存在，请换一个标识', 409, { listing_id: id })
        if ((error as { code?: string })?.code === '23503' || String(error).includes('PRODUCT_LISTING')) throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', 'canonical product 不存在或不属于当前工作区', 404, { canonical_product_id: canonicalProductId })
        throw error
      }
    }
    case 'brand-unit.listing.list': {
      await persistenceReady
      const brandId = typeof params.brand_id === 'string' && params.brand_id.trim() ? params.brand_id.trim() : undefined
      if (brandId) await enforceBrandAccess(req, workspaceId, brandId)
      const listed = await (persistence.brandUnits ?? memoryBrandUnits).listListings({ workspaceId, ...(brandId ? { brandId } : {}), ...(typeof params.canonical_product_id === 'string' && params.canonical_product_id.trim() ? { canonicalProductId: params.canonical_product_id.trim() } : {}), ...(typeof params.platform === 'string' ? { platform: params.platform as Platform } : {}), ...(typeof params.account_id === 'string' && params.account_id.trim() ? { accountId: params.account_id.trim() } : {}) })
      const listings = !requiresStrictAuth() || hasWorkspaceWideBrandAccess(req)
        ? listed
        : (await Promise.all(listed.map(async item => await (persistence.brandUnits ?? memoryBrandUnits).hasBrandAccess({ workspaceId, brandId: item.brandId, externalSubject: requestPrincipals.get(req)!.actorId }) ? item : undefined))).filter((item): item is typeof listed[number] => Boolean(item))
      return result({ items: listings, count: listings.length, storage: persistence.mode, durable: persistence.mode === 'postgres' })
    }
    case 'brand-unit.access.grant': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const brandId = required(params, 'brand_id')
      const role = required(params, 'role') as BrandAccessRole
      if (!['viewer', 'editor', 'publisher', 'admin'].includes(role)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '品权限角色无效', 400)
      await enforceBrandAccess(req, workspaceId, brandId, 'admin')
      const externalSubject = required(params, 'external_subject')
      try {
        await (persistence.brandUnits ?? memoryBrandUnits).grantBrandAccess({ workspaceId, brandId, externalSubject, role })
      } catch (error) {
        if (String(error).includes('ACTIVE_MEMBER_NOT_FOUND')) throw new DomainError('ACTIVE_MEMBER_NOT_FOUND', '只能向当前工作区的 active 成员授予品权限', 404)
        if (String(error).includes('BRAND_UNIT_NOT_FOUND') || (error as { code?: string })?.code === '23503') throw new DomainError('BRAND_UNIT_NOT_FOUND', '品不存在或不属于当前工作区', 404)
        throw error
      }
      await recordOperationAudit({ workspaceId, actorId: requestPrincipals.get(req)?.actorId ?? 'actor_demo', action: 'brand.access.grant', resourceType: 'brand', resourceId: brandId, before: {}, after: { externalSubject, role }, reason: typeof params.reason === 'string' ? params.reason : '更新品权限' })
      return result({ brandId, externalSubject, role })
    }
    case 'canonical.product.consistency': {
      const repository = persistence.brandUnits ?? memoryBrandUnits
      const rows = await repository.listCanonicalChainConsistencyRows({ workspaceId })
      const report = canonicalConsistencyApiReport({ workspaceId, ...rows }, persistence.mode)
      const readControl = await canonicalProductReadControl(workspaceId)
      const audit = await persistCanonicalLinkAudit(report)
      const auditRecords = persistence.unifiedLinkAudit ? await persistence.unifiedLinkAudit.list({ workspaceId, limit: 100 }) : []
      return result({ ...report, readOnly: true, cutover: 'unchanged', read_control: readControl, unified_link_audit: { ...audit, items: auditRecords }, source: persistence.mode, durable: persistence.mode === 'postgres' })
    }
    case 'campaign.batch.create': {
      const brandId = required(params, 'brand_id')
      await enforceBrandAccess(req, workspaceId, brandId, 'editor')
      await persistenceReady
      let targets = params.targets_json !== undefined
        ? parseCampaignTargets(params.targets_json)
        : [{ productId: '', platform: required(params, 'platform') as Platform, accountId: required(params, 'account_id') }]
      const legacyProductIds = params.targets_json === undefined ? parseCampaignProductIds(params.product_ids_json) : targets.filter(target => target.productId).map(target => target.productId)
      if (params.targets_json === undefined) targets.splice(0, 1, ...legacyProductIds.map(productId => ({ productId, platform: targets[0]!.platform, accountId: targets[0]!.accountId })))
      // Both the structured target API and the legacy product_ids_json API
      // must pass through the same canonical resolver. Otherwise an old
      // request can recreate a campaign that splits the execution graph.
      if (targets.length) {
        targets = await Promise.all(targets.map(async target => {
          if (target.productId) {
            const scope = await resolveCanonicalTaskScope({ workspaceId, productId: target.productId, platform: target.platform, accountId: target.accountId, brandId, ...(target.canonicalProductId ? { canonicalProductId: target.canonicalProductId } : {}), ...(target.listingId ? { listingId: target.listingId } : {}) })
            if (!scope) {
              // An explicit canonical target may intentionally point at a
              // platform-specific product different from canonical.sourceProductId
              // (one canonical product can have several platform listings). The
              // later product/store checks still validate the concrete target;
              // validate the supplied canonical/listing pair here instead of
              // treating it as a legacy mapping failure.
              if (target.canonicalProductId) {
                const canonical = await (persistence.brandUnits ?? memoryBrandUnits).getCanonicalProduct({ workspaceId, id: target.canonicalProductId })
                if (!canonical || canonical.brandId !== brandId) throw new DomainError('CANONICAL_PRODUCT_SCOPE_MISMATCH', 'canonical product 不属于当前品或工作区', 409, { product_id: target.productId, canonical_product_id: target.canonicalProductId })
                if (!target.listingId) throw new DomainError('CANONICAL_LISTING_REQUIRED', '标准批量任务必须同时绑定 canonical_product_id 和 listing_id', 409, { canonical_product_id: canonical.id, platform: target.platform, account_id: target.accountId, next_step: '先创建当前平台店铺 listing，再创建批量计划' })
                const listing = (await (persistence.brandUnits ?? memoryBrandUnits).listListings({ workspaceId, brandId, canonicalProductId: canonical.id, listingId: target.listingId, platform: target.platform, accountId: target.accountId }))[0]
                if (!listing) throw new DomainError('LISTING_TARGET_MISMATCH', 'listing_id 不属于当前品、平台或店铺', 409, { listing_id: target.listingId, canonical_product_id: canonical.id, platform: target.platform, account_id: target.accountId })
                return target
              }
              if (target.canonicalProductId || target.listingId) throw new DomainError('CANONICAL_PRODUCT_SCOPE_MISMATCH', 'legacy 商品没有对应的规范化商品链，不能混用 canonical_product_id 或 listing_id', 409, { product_id: target.productId, canonical_product_id: target.canonicalProductId ?? null, listing_id: target.listingId ?? null })
              return target
            }
            if (target.canonicalProductId && target.canonicalProductId !== scope.canonicalProductId) throw new DomainError('CANONICAL_PRODUCT_SCOPE_MISMATCH', '商品与规范化商品绑定不一致', 409, { product_id: target.productId, expected: scope.canonicalProductId, provided: target.canonicalProductId })
            if (target.listingId && target.listingId !== scope.listingId) throw new DomainError('LISTING_TARGET_MISMATCH', 'listing_id 不属于当前商品、平台或店铺', 409, { listing_id: target.listingId, expected: scope.listingId })
            return { ...target, ...scope }
          }
          const canonical = target.canonicalProductId ? await (persistence.brandUnits ?? memoryBrandUnits).getCanonicalProduct({ workspaceId, id: target.canonicalProductId }) : undefined
          if (!canonical) throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', 'canonical product 不存在或不属于当前工作区', 404, { canonical_product_id: target.canonicalProductId })
          if (canonical.brandId !== brandId) throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', 'canonical product 不存在或不属于当前品', 404, { canonical_product_id: target.canonicalProductId, brand_id: brandId })
          const listing = target.listingId
            ? (await (persistence.brandUnits ?? memoryBrandUnits).listListings({ workspaceId, brandId, listingId: target.listingId, platform: target.platform, accountId: target.accountId }))[0]
            : undefined
          if (target.listingId && (!listing || listing.canonicalProductId !== canonical.id)) throw new DomainError('LISTING_TARGET_MISMATCH', 'listing_id 不属于当前品、平台或店铺', 409, { listing_id: target.listingId, brand_id: brandId, platform: target.platform, account_id: target.accountId })
          if (!target.listingId && !fixtureMode) throw new DomainError('CANONICAL_LISTING_REQUIRED', '标准批量任务必须同时绑定 canonical_product_id 和 listing_id', 409, { canonical_product_id: canonical.id, platform: target.platform, account_id: target.accountId, next_step: '先创建当前平台店铺 listing，再创建批量计划' })
          const listingProduct = listing?.remoteProductId
            ? service.listProducts(workspaceId, { platform: target.platform, accountId: target.accountId, remoteProductId: listing.remoteProductId })[0]
            : undefined
          if (listingProduct) return { ...target, productId: listingProduct.id }
          const sourceProduct = canonical.sourceProductId ? service.products.get(canonical.sourceProductId) : undefined
          if (sourceProduct?.workspaceId === workspaceId && sourceProduct.platform === target.platform && sourceProduct.accountId === target.accountId) return { ...target, productId: sourceProduct.id }
          throw new DomainError('LISTING_PRODUCT_FACTS_REQUIRED', '该平台店铺的 listing 尚未同步对应商品事实，不能使用其他平台商品替代', 409, {
            canonical_product_id: canonical.id,
            listing_id: listing?.id ?? null,
            platform: target.platform,
            account_id: target.accountId,
            remote_product_id: listing?.remoteProductId ?? null,
            next_actions: ['先同步该店铺商品，并让 listing.remote_product_id 对应平台商品 ID'],
          })
        }))
      }
      for (const target of targets) {
        const units = await (persistence.brandUnits ?? memoryBrandUnits).listBrands({ workspaceId, brandId, platform: target.platform, accountId: target.accountId })
        if (!units[0]) throw new DomainError('BRAND_STORE_BINDING_REQUIRED', '创建批量运营计划前，必须先将该店铺绑定到指定品', 409, { brand_id: brandId, platform: target.platform, account_id: target.accountId, next_actions: ['调用 brand-unit.bind-store 绑定店铺'] })
        if (target.listingId) {
          const listings = await (persistence.brandUnits ?? memoryBrandUnits).listListings({ workspaceId, brandId, listingId: target.listingId, platform: target.platform, accountId: target.accountId })
          if (!listings[0] || (target.canonicalProductId && listings[0].canonicalProductId !== target.canonicalProductId)) throw new DomainError('LISTING_TARGET_MISMATCH', 'listing_id 不属于当前品、平台或店铺', 409, { listing_id: target.listingId, brand_id: brandId, platform: target.platform, account_id: target.accountId })
        }
        const account = isProduction() ? service.getActivePlatformAccount(workspaceId, target.accountId, target.platform) : service.getPlatformAccount(workspaceId, target.accountId, target.platform)
        if (!account) throw new DomainError('PLATFORM_ACCOUNT_NOT_FOUND', '平台账号不存在或不属于当前工作区', 404)
        const productId = target.productId
        const product = service.products.get(productId)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', `商品 ${productId} 不存在或不属于当前工作区`, 404, { product_id: productId })
        await enforceProductBrandBinding(req, workspaceId, productId, brandId)
        if (product.platform !== target.platform || product.accountId !== target.accountId) throw new DomainError('PRODUCT_STORE_CONTEXT_MISMATCH', `商品 ${productId} 不属于所选平台店铺`, 409, { product_id: productId, expected: { platform: target.platform, account_id: target.accountId }, actual: { platform: product.platform, account_id: product.accountId ?? null } })
      }
      const platform = targets[0]!.platform
      const accountId = targets[0]!.accountId
      const productIds = [...new Set(targets.map(target => target.productId))]
      const idempotencyKey = (typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim() || undefined
      let created
      try {
        created = await (persistence.brandUnits ?? memoryBrandUnits).createCampaign({ id: `campaign_batch_${randomUUID().replaceAll('-', '').slice(0, 24)}`, workspaceId, brandId, platform, accountId, productIds, targets, state: 'draft', ...(idempotencyKey ? { idempotencyKey } : {}) })
      } catch (error) {
        if ((error as { code?: string })?.code === 'CAMPAIGN_IDEMPOTENCY_CONFLICT' || String(error).includes('CAMPAIGN_IDEMPOTENCY_CONFLICT')) throw new DomainError('CAMPAIGN_IDEMPOTENCY_CONFLICT', '幂等键已绑定到另一份批量运营计划，请换用新的幂等键', 409, { idempotency_key: idempotencyKey })
        throw error
      }
      const durableCampaign = await (persistence.brandUnits ?? memoryBrandUnits).getCampaign({ workspaceId, id: created.campaign.id })
      if (!durableCampaign) throw new DomainError('CAMPAIGN_BATCH_NOT_FOUND', '批量运营计划持久化后无法回读', 500, { campaign_id: created.campaign.id })
      const deliveryManifest = await validateCampaignDelivery('create', workspaceId, durableCampaign)
      return result({ ...durableCampaign, count: productIds.length, replayed: created.replayed, delivery_manifest: deliveryManifest, storage: persistence.mode, durable: persistence.mode === 'postgres', execution: 'plan_only', message: '批量运营计划已持久化；当前仍需通过审核后任务流程生成和发布。' })
    }
    case 'campaign.batch.list': {
      const platform = typeof params.platform === 'string' && params.platform.trim() ? params.platform as Platform : undefined
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(Math.max(Number(params.limit), 1), 100) : 50
      if (accountId && !platform) throw new DomainError('STORE_PLATFORM_REQUIRED', '使用 account_id 筛选批量计划时必须同时指定 platform', 400)
      await persistenceReady
      const listed = await (persistence.brandUnits ?? memoryBrandUnits).listCampaigns({ workspaceId, ...(platform ? { platform } : {}), ...(accountId ? { accountId } : {}), limit })
      const visible = []
      for (const campaign of listed) {
        try { await enforceBrandAccess(req, workspaceId, campaign.brandId) } catch { continue }
        visible.push({ id: campaign.id, state: campaign.state, revision: campaign.revision ?? 1, platform: campaign.platform, accountId: campaign.accountId, itemCount: campaign.items?.length ?? campaign.productIds.length, failedCount: campaign.items?.filter(item => item.state === 'failed').length ?? 0, createdAt: campaign.createdAt, updatedAt: campaign.updatedAt })
      }
      return result({ items: visible, count: visible.length, storage: persistence.mode, durable: persistence.mode === 'postgres' })
    }
    case 'campaign.batch.get': {
      const campaignId = required(params, 'campaign_id')
      await persistenceReady
      let campaign = await (persistence.brandUnits ?? memoryBrandUnits).getCampaign({ workspaceId, id: campaignId })
      if (!campaign) throw new DomainError('CAMPAIGN_BATCH_NOT_FOUND', '批量运营计划不存在或不属于当前工作区', 404, { campaign_id: campaignId })
      await enforceBrandAccess(req, workspaceId, campaign.brandId)
      campaign = await refreshCampaignProgress(campaign)
      const deliveryManifest = await validateCampaignDelivery('get', workspaceId, campaign)
      return result({ ...campaign, count: campaign.productIds.length, ...campaignWorkflow(campaign), delivery_manifest: deliveryManifest, storage: persistence.mode, durable: persistence.mode === 'postgres', execution: campaign.taskIds?.length ? 'workflow_active' : 'plan_only', message: campaign.taskIds?.length ? '批量工作流已激活；每个商品会停在需要事实确认、方向确认、审核或发布确认的安全节点。' : '批量运营计划已持久化；调用 campaign.batch.generate 创建逐商品工作流。' })
    }
    case 'campaign.batch.generate': {
      await requirePluginWalletAccess(workspaceId)
      await persistenceReady
      const campaignId = required(params, 'campaign_id')
      const campaign = await (persistence.brandUnits ?? memoryBrandUnits).getCampaign({ workspaceId, id: campaignId })
      if (!campaign) throw new DomainError('CAMPAIGN_BATCH_NOT_FOUND', '批量运营计划不存在或不属于当前工作区', 404, { campaign_id: campaignId })
      await enforceBrandAccess(req, workspaceId, campaign.brandId, 'editor')
      const priorTaskIds = campaign.taskIds
      if (priorTaskIds) {
        const refreshed = await refreshCampaignProgress(campaign)
        const deliveryManifest = await validateCampaignDelivery('generate', workspaceId, refreshed)
        return result({ campaignId, taskIds: priorTaskIds, count: priorTaskIds.length, state: refreshed.state, ...campaignWorkflow(refreshed), delivery_manifest: deliveryManifest, replayed: true, execution: 'workflow_active', next_actions: [...new Set((refreshed.items ?? []).map(item => item.error?.nextAction).filter((action): action is string => Boolean(action)))] })
      }
      const requestText = typeof params.request_text === 'string' && params.request_text.trim() ? params.request_text.trim() : undefined
      const taskIds: string[] = []
      const rawTargets: Array<{ productId: string; platform: Platform; accountId: string; canonicalProductId?: string; listingId?: string }> = campaign.targets
        ?? campaign.items?.map(item => ({ productId: item.productId, platform: item.platform, accountId: item.accountId, ...(item.canonicalProductId ? { canonicalProductId: item.canonicalProductId } : {}), ...(item.listingId ? { listingId: item.listingId } : {}) }))
        ?? campaign.productIds.map(productId => ({ productId, platform: campaign.platform, accountId: campaign.accountId }))
      const targets = await Promise.all(rawTargets.map(async (target, index) => {
        const product = service.products.get(target.productId)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', `商品 ${target.productId} 不存在或不属于当前工作区`, 404, { product_id: target.productId, ordinal: index + 1 })
        if (product.platform !== target.platform || product.accountId !== target.accountId) throw new DomainError('PRODUCT_STORE_CONTEXT_MISMATCH', `商品 ${target.productId} 不属于所选平台店铺`, 409, { product_id: target.productId, ordinal: index + 1, expected: { platform: target.platform, account_id: target.accountId }, actual: { platform: product.platform, account_id: product.accountId ?? null } })
        if (!product.factsConfirmed) throw new DomainError('PRODUCT_FACTS_CONFIRMATION_REQUIRED', '批量生成前必须先确认全部商品事实，已停止创建任务', 409, { product_id: target.productId, ordinal: index + 1, next_action: 'catalog.facts.confirm', read_mode: (await canonicalProductReadControl(workspaceId)).mode })
        if (!campaign.items?.[index]) throw new DomainError('CAMPAIGN_ITEM_MISSING', '批量运营计划缺少持久化明细，已停止创建任务', 409, { campaign_id: campaignId, ordinal: index + 1 })
        const scope = await resolveCanonicalTaskScope({ workspaceId, productId: target.productId, platform: target.platform, accountId: target.accountId, brandId: campaign.brandId, ...(target.canonicalProductId ? { canonicalProductId: target.canonicalProductId } : {}), ...(target.listingId ? { listingId: target.listingId } : {}), requireListing: true, requireCanonical: true })
        if (!scope) throw new DomainError('CANONICAL_PRODUCT_MAPPING_REQUIRED', '批量生成要求所有目标先完成标准商品链绑定，已停止创建任务', 409, { product_id: target.productId, ordinal: index + 1, next_action: 'canonical.product.consistency', read_mode: (await canonicalProductReadControl(workspaceId)).mode })
        if (target.canonicalProductId && target.canonicalProductId !== scope.canonicalProductId) throw new DomainError('CANONICAL_PRODUCT_SCOPE_MISMATCH', '批量计划商品与规范化商品绑定不一致', 409, { product_id: target.productId, expected: scope.canonicalProductId, provided: target.canonicalProductId })
        if (target.listingId && target.listingId !== scope.listingId) throw new DomainError('LISTING_TARGET_MISMATCH', '批量计划 listing 与商品、平台或店铺不一致', 409, { listing_id: target.listingId, expected: scope.listingId })
        return { ...target, ...scope }
      }))
      for (const [index, target] of targets.entries()) {
        const productId = target.productId
        const product = service.products.get(productId)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', `商品 ${productId} 不存在或不属于当前工作区`, 404, { product_id: productId })
        const campaignItem = campaign.items?.[index]
        if (!campaignItem) throw new DomainError('CAMPAIGN_ITEM_MISSING', '批量运营计划缺少持久化明细，已停止创建任务', 409, { campaign_id: campaignId, ordinal: index + 1 })
        const deterministicTaskId = `task_campaign_${createHash('sha256').update(`${workspaceId}:${campaignId}:${campaignItem.id}`).digest('hex').slice(0, 32)}`
        const existingTask = service.tasks.has(deterministicTaskId)
        const task = service.createTask({ workspaceId, productId, platform: target.platform, accountId: target.accountId, brandId: campaign.brandId, canonicalProductId: target.canonicalProductId, listingId: target.listingId, campaignId, campaignItemId: campaignItem.id, taskId: deterministicTaskId, ...(requestText ? { requestText } : {}) })
        taskIds.push(task.id)
        if (!existingTask) {
          await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
          await persistEvent(workspaceId, task.id, 'task.created', task.version, { ...task, campaign_id: campaignId, listing_id: target.listingId ?? null, source: 'campaign.batch.generate' })
        }
      }
      await (persistence.brandUnits ?? memoryBrandUnits).updateCampaignTasks({ workspaceId, id: campaignId, taskIds, state: 'generating' })
      const refreshed = await refreshCampaignProgress((await (persistence.brandUnits ?? memoryBrandUnits).getCampaign({ workspaceId, id: campaignId }))!)
      const workflow = campaignWorkflow(refreshed)
      const deliveryManifest = await validateCampaignDelivery('generate', workspaceId, refreshed)
      return result({ campaignId, taskIds, count: taskIds.length, state: refreshed.state, ...workflow, delivery_manifest: deliveryManifest, replayed: false, execution: 'workflow_active', message: '已为每个商品创建独立工作流；系统会持久化当前节点，不会越过事实确认、方向确认、内容审核或发布确认。', next_actions: [...new Set((refreshed.items ?? []).map(item => item.error?.nextAction).filter((action): action is string => Boolean(action)))] })
    }
    case 'campaign.batch.pause':
    case 'campaign.batch.resume':
    case 'campaign.batch.retry_failed': {
      await persistenceReady
      const campaignId = required(params, 'campaign_id')
      const repository = persistence.brandUnits ?? memoryBrandUnits
      const current = await repository.getCampaign({ workspaceId, id: campaignId })
      if (!current) throw new DomainError('CAMPAIGN_BATCH_NOT_FOUND', '批量运营计划不存在或不属于当前工作区', 404, { campaign_id: campaignId })
      await enforceBrandAccess(req, workspaceId, current.brandId, 'editor')
      const operation = method === 'campaign.batch.pause' ? 'pause' as const : method === 'campaign.batch.resume' ? 'resume' as const : 'retry_failed' as const
      let itemIds: string[] | undefined
      if (operation === 'retry_failed' && params.item_ids_json !== undefined) {
        const parsed = parseJsonArrayParameter(params, 'item_ids_json')
        if (!parsed.length || parsed.length > 50 || parsed.some(value => typeof value !== 'string' || !value.trim()) || new Set(parsed).size !== parsed.length) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'item_ids_json 必须是 1 到 50 个不重复 item ID', 400)
        itemIds = parsed.map(value => String(value).trim())
      }
      let transitioned
      try {
        transitioned = await repository.transitionCampaignLifecycle({ workspaceId, id: campaignId, operation, expectedRevision: Number(required(params, 'expected_revision')), idempotencyKey: required(params, 'idempotency_key'), reason: required(params, 'reason').trim(), ...(itemIds ? { itemIds } : {}) })
      } catch (error) { campaignLifecycleError(error) }
      if (operation === 'retry_failed' && !transitioned.replayed) {
        const selected = new Set(itemIds?.length ? itemIds : (current.items ?? []).filter(item => item.state === 'failed').map(item => item.id))
        for (const item of current.items ?? []) {
          if (!selected.has(item.id) || !item.taskId) continue
          const task = service.tasks.get(item.taskId)
          if (task?.workspaceId === workspaceId && task.state === 'failed_recoverable') {
            service.resumeTask(workspaceId, task.id)
            const resumed = service.tasks.get(task.id)!
            await persistSnapshot(workspaceId, 'task', resumed, resumed as unknown as Record<string, unknown>)
            await persistEvent(workspaceId, resumed.id, 'task.resumed', resumed.version, { source: 'campaign.batch.retry_failed', campaign_id: campaignId, campaign_item_id: item.id })
          }
        }
      }
      if (!transitioned.replayed) await recordOperationAudit({ workspaceId, actorId: requestActor(req), action: `campaign.batch.${operation}`, resourceType: 'campaign', resourceId: campaignId, before: { state: current.state, revision: current.revision ?? 1 }, after: { state: transitioned.campaign.state, revision: transitioned.campaign.revision ?? null, item_ids: itemIds ?? null }, reason: required(params, 'reason').trim() })
      const deliveryManifest = await validateCampaignDelivery(operation, workspaceId, transitioned.campaign)
      return result({ ...transitioned.campaign, ...campaignWorkflow(transitioned.campaign), delivery_manifest: deliveryManifest, replayed: transitioned.replayed, storage: persistence.mode, durable: persistence.mode === 'postgres' })
    }
    case 'workspace.bootstrap': {
      const displayName = required(params, 'display_name')
      if (displayName.length > 120) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'display_name 不能超过 120 个字符', 400)
      const principal = requestPrincipals.get(req)
      const actorId = principal?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant_owner'
      if (!actorId) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '创建工作区需要可识别的商家身份', 401)
      const claimedSubject = typeof params.external_subject === 'string' && params.external_subject.trim() ? params.external_subject.trim() : undefined
      if (claimedSubject && claimedSubject !== actorId) throw new DomainError(ERROR_CODES.FORBIDDEN, '新工作区 owner 只能绑定当前认证身份；external_subject 不能替代认证主体', 403)
      if (requiresStrictAuth() && (!principal?.issuer || !principal.identityId)) throw new DomainError(ERROR_CODES.UNAUTHENTICATED, '创建工作区需要经过可信身份网关解析 issuer 和 subject', 401)
      const issuer = principal?.issuer ?? (fixtureMode ? 'urn:merchant:fixture' : 'urn:merchant:local')
      try {
        const bootstrapped = await (persistence.workspaceBootstrap ?? memoryWorkspaceBootstrap).bootstrap({ issuer, externalSubject: actorId, ...(principal?.identityId ? { identityId: principal.identityId } : {}), candidateWorkspaceId: `ws_${randomUUID().replaceAll('-', '').slice(0, 24)}`, displayName, actorId })
        workspaceId = bootstrapped.workspaceId
        knownWorkspaces.add(workspaceId)
        return result({ workspaceId, displayName: bootstrapped.displayName, status: 'active', reused: !bootstrapped.created, owner: { issuer, externalSubject: actorId, actorId }, binding: { environmentVariable: 'MERCHANT_WORKSPACE_ID', requiredValue: workspaceId, nextStep: '将该值绑定到 Codex 插件后重新调用 workspace.health' } })
      } catch (error) {
        if (error instanceof WorkspaceBootstrapError) {
          if (error.code === 'WORKSPACE_BOOTSTRAP_BINDING_INACTIVE') throw new DomainError(error.code, '该身份已有工作区绑定，但工作区或 owner 成员已停用；请联系管理员恢复，不能另建工作区绕过停用', 409)
          throw new DomainError(error.code, '认证身份与 workspace binding 不一致', 403)
        }
        throw error
      }
    }
    case 'workspace.interactive.confirm':
      {
        const principal = requestPrincipals.get(req)
        const actorId = principal?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
        const sessionId = principal?.sessionId ?? principal?.sessionSubject ?? `api-token:${actorId}`
        const suppliedIntentHash = typeof params.intent_hash === 'string' ? params.intent_hash.trim() : ''
        if (suppliedIntentHash && !/^[a-f0-9]{64}$/u.test(suppliedIntentHash)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'intent_hash 必须是 64 位小写 SHA-256', 400)
        const intentHash = suppliedIntentHash || createHash('sha256').update(`interactive-session:${workspaceId}:${actorId}:${sessionId}`).digest('hex')
        const rawNonce = randomBytes(32).toString('hex')
        const storedNonceHash = createHash('sha256').update(rawNonce).digest('hex')
        const expiresAt = new Date(Date.now() + 15 * 60_000).toISOString()
        const ticket = await (persistence.interactiveConfirmationTickets ?? memoryInteractiveConfirmationTickets).issue({ workspaceId, actorId, sessionId, intentHash, nonceHash: storedNonceHash, expiresAt })
        return result({ enabled: true, scope: 'current_interactive_session', expires_in_seconds: 900, automation: 'read_only', ticket: { nonce_hash: rawNonce, intent_hash: ticket.intentHash, expires_at: ticket.expiresAt }, message: '仅当前交互会话开放写操作；钱包、事实、审核、平台能力和发布确认门禁仍然生效' })
      }
    case 'merchant.start': {
      const requestedPlatform = typeof params.requested_platform === 'string' ? params.requested_platform : undefined
      const requestedGoal = typeof params.requested_goal === 'string' ? params.requested_goal.trim() : undefined
      const attachmentCount = typeof params.attachment_count === 'string' && /^(?:[0-9]|1[0-9]|20)$/u.test(params.attachment_count)
        ? Number(params.attachment_count)
        : undefined
      const hasExplicitIntent = Boolean(requestedPlatform || requestedGoal || attachmentCount !== undefined)
      if (hasExplicitIntent) {
        const intentShape = {
          requestedPlatform: requestedPlatform ?? null,
          requestedGoal: requestedGoal ?? null,
          attachmentCount: attachmentCount ?? 0,
        }
        const suppliedKey = typeof params.idempotency_key === 'string' && params.idempotency_key.trim() ? params.idempotency_key.trim() : undefined
        const key = suppliedKey ?? `merchant-start-${createHash('sha256').update(canonicalJson(intentShape)).digest('hex').slice(0, 32)}`
        const keyHash = createHash('sha256').update(key).digest('hex')
        const intentId = `merchant_intent_${createHash('sha256').update(`${workspaceId}\n${key}`).digest('hex').slice(0, 32)}`
        const payload: Record<string, unknown> = {
          id: intentId,
          ...intentShape,
          idempotencyKeyHash: keyHash,
          state: attachmentCount && attachmentCount > 0 ? 'awaiting_automatic_scan' : 'awaiting_attachment_or_product',
          version: 1,
        }
        if (persistence.persistSnapshotAndEvent) {
          await persistence.persistSnapshotAndEvent({
            workspaceId,
            entityType: 'merchant_intent',
            entityId: intentId,
            entityVersion: 1,
            payload,
            eventType: 'merchant.intent.recorded',
            eventPayload: { intentId, requestedPlatform: requestedPlatform ?? null, attachmentCount: attachmentCount ?? 0 },
          })
        } else {
          const memoryKey = `${workspaceId}:${intentId}`
          const existing = merchantIntents.get(memoryKey)
          if (existing && canonicalJson(existing) !== canonicalJson(payload)) throw new DomainError('MERCHANT_INTENT_IDEMPOTENCY_CONFLICT', '同一开始请求已用于不同意图，请刷新后重试', 409)
          merchantIntents.set(memoryKey, payload)
        }
        const scanning = Boolean(attachmentCount && attachmentCount > 0)
        return result({
          greeting: '好的，我已理解你的目标。',
          workspace: { id: workspaceId, status: (await getWorkspaceStatus(workspaceId)) === 'active' ? 'ready' : 'disabled' },
          intent_id: intentId,
          recognizedIntent: { platform: requestedPlatform ?? null, goal: requestedGoal ?? null, attachment_count: attachmentCount ?? 0 },
          currentStep: { id: scanning ? 'automatic-scan' : 'provide-product', state: scanning ? 'in_progress' : 'ready' },
          nextPrompt: scanning ? '附件正在自动安全检查；通过后会等待你的确认再生成。' : '请附上商品图片，或选择已有商品；我会自动检查并等待你的确认。',
          automation: { asset_scan: 'automatic', continuation: 'durable', administrator_action_required: false },
          action_cards: scanning ? [] : [{ method: 'asset.upload', label: '上传商品图片', required_inputs: ['name', 'mime_type', 'file_path'] }],
        })
      }
      const directory = workspaceStoreDirectory(workspaceId)
      const onboarding = workspaceOnboarding(workspaceId, directory)
      const onboardingV2 = merchantOnboardingProjection(onboarding)
      const setup = setupDiagnostics()
      await persistenceReady
      const brandNavigation = await accessibleBrandNavigation(req, workspaceId)
      const startWalletBalanceFen = persistence.billing ? await persistence.billing.balanceFen(workspaceId) : walletBalanceFen(workspaceId)
      // A subscription quota is reported separately, but the plugin's paid
      // capability card must only say "unlocked" after wallet settlement.
      // Generation/publish gates already use the same wallet boundary.
      const walletUnlocked = startWalletBalanceFen > 0
      const walletCapabilities = billingCapabilityEntitlements({ balanceFen: startWalletBalanceFen, usage: persistence.usage ? await persistence.usage.get(workspaceId) : undefined, setup, stores: directory })
      const current = onboarding.currentStep
      const platformOptions = merchantPlatformOptions(workspaceId, directory)
      const nextPrompt = current.id === 'bind-store'
        ? '选择一个平台连接我的店铺'
        : current.id === 'choose-product'
          ? '查看我当前店铺的商品'
          : current.id === 'add-assets'
            ? '上传我的商品图片和资料'
            : current.id === 'start-content'
              ? '开始为这个商品制作营销内容'
              : current.id === 'publish'
                ? '查看这个商品的发布前检查'
                : '查看当前工作区状态'
      return result({
        greeting: '欢迎使用大麦。',
        productName: '大麦商家营销助手',
        workspace: { id: workspaceId, status: (await getWorkspaceStatus(workspaceId)) === 'active' ? 'ready' : 'disabled' },
        currentStep: { ...current, primaryAction: onboardingV2.current_step.primary_action },
        onboarding_v2: onboardingV2,
        nextPrompt,
        nextInstruction: `你可以直接说：“${nextPrompt}”。`,
        onboarding: onboarding.steps,
        summary: onboarding.summary,
        wallet: {
          balance_cny: (startWalletBalanceFen / 100).toFixed(2),
          unlocked: walletUnlocked,
          recharge_channels: ['alipay', 'wechat'],
          status_method: 'billing.status',
          recharge_method: 'billing.recharge.create',
          message: walletUnlocked ? '已解锁生成、图片、视频、OCR、SEO/GEO 和发布能力' : '充值到账后解锁生成、图片、视频、OCR、SEO/GEO 和发布能力',
          capabilities: walletCapabilities,
        },
        stores: directory.map(store => ({ platform: store.platform, label: store.label, state: store.state, dataMode: store.dataMode, readable: store.readable, writeEnabled: store.writeEnabled })),
        availablePlatforms: SUPPORTED_PLATFORMS,
        platformOptions,
        brandNavigation: { title: '我的品', presentation: 'tree', hierarchy: ['brand', 'platform', 'store'], items: brandNavigation, emptyState: '尚未创建品；先创建一个品，再绑定不同平台和店铺。' },
        modelAccess: { ownership: 'platform', userKeyRequired: false, relay: setup.ai.relay, text: setup.ai.contentGeneration, image: setup.ai.imageGeneration, imageEdit: setup.ai.imageEditing, ocr: setup.ai.imageFacts, video: setup.ai.videoRendering },
        cards: MERCHANT_CAPABILITY_CARDS.map(card => {
          const onboardingState = card.id === 'stores-products' ? onboarding.steps.find(step => step.id === 'choose-product')?.state
            : card.id === 'knowledge-assets' ? onboarding.steps.find(step => step.id === 'add-assets')?.state
              : card.id === 'content' ? onboarding.steps.find(step => step.id === 'start-content')?.state
                : card.id === 'first-value' ? 'available'
                : directory.length ? 'available' : 'needs_store'
          const cta = card.id === 'stores-products' ? '选择店铺并查看商品' : card.id === 'knowledge-assets' ? '上传商品图片和资料' : card.id === 'content' ? '开始内容任务' : card.title
          const action = merchantCapabilityCardAction(card, onboarding, directory)
          const paidCapability = ['content', 'visuals', 'review-publish', 'bulk-publish'].includes(card.id)
          const capabilityGate = paidCapability ? { unlocked: walletUnlocked, method: 'billing.status', reason: walletUnlocked ? '钱包已到账；仍需事实、审核和交互确认门禁' : '充值到账后解锁该能力；授权、只读商品查看和创建充值订单仍可继续' } : undefined
          return { id: card.id, title: card.title, summary: card.summary, entryMethod: card.entryMethod, readOnly: card.readOnly, state: onboardingState, cta, requiredScope: card.id === 'stores-products' || card.id === 'content' ? 'platform + accountId' : 'workspace', ...(capabilityGate ? { capabilityGate } : {}), action: { ...action, confirmation: card.readOnly ? 'none' : 'interactive_confirmation' }, blocked_by: action.blocked_by, next_actions: [{ method: action.method, arguments: action.arguments, reason: action.reason, required_inputs: action.required_inputs }] }
        }),
      })
    }
    case 'workspace.health': {
      const directory = workspaceStoreDirectory(workspaceId)
      const onboardingState = workspaceOnboarding(workspaceId, directory)
      const onboarding = onboardingState.steps
      const onboardingV2 = merchantOnboardingProjection(onboardingState)
      const workspaceRules = await rulePacksForWorkspace(workspaceId)
      const brandNavigation = await accessibleBrandNavigation(req, workspaceId)
      return result({
      ...runtimeHealth(),
      persistence: { mode: persistence.mode, ready: !persistenceError, ...(invalidDurableSnapshots.get(workspaceId)?.length ? { invalidSnapshots: invalidDurableSnapshots.get(workspaceId) } : {}) },
      plugin: { name: 'merchant-marketing', version: '0.1.0' },
      mcp: { status: 'ready', transport: '/mcp' },
      workspace: { id: workspaceId, status: (await getWorkspaceStatus(workspaceId)) === 'active' ? 'ready' : 'disabled' },
      rules: { activeVersions: service.ruleCenter.activeVersionIds() },
      ruleSync: platformRuleSyncStatus(workspaceRules, {
        intervalHours: Number(process.env.PLATFORM_RULE_SYNC_INTERVAL_HOURS ?? 24),
        manifestUrl: process.env.PLATFORM_RULE_SYNC_MANIFEST_URL,
        signingSecretConfigured: Boolean(process.env.PLATFORM_RULE_SYNC_SIGNING_SECRET?.trim()),
      }),
      connectorReadiness: Object.fromEntries(SUPPORTED_PLATFORMS.map(platform => [platform, workspaceConnectorReadiness(platform)])),
      platforms: workspacePlatformStatus(workspaceId),
      commercial: { settings: await (persistence.commercial ?? memoryCommercial).getSettings(workspaceId), platforms: await (persistence.commercial ?? memoryCommercial).listPlatformSettings(workspaceId) },
      storeDirectory: directory,
      onboarding_v2: onboardingV2,
      storeSelection: { requiredForStoreActions: true, key: 'platform + accountId', warning: '别名和店铺名只用于展示与候选匹配，不能替代账号范围确认' },
      capabilityCards: {
        title: '大麦工作台',
        presentation: 'conversation_cards',
        instruction: '优先展示这些卡片；商家选择卡片后再调用 entryMethod，不要求商家记忆工具名或内部 ID。店铺级操作先展示导航列表并确认平台与账号范围。',
        navigation: {
          title: '平台与店铺',
          presentation: 'grouped_list',
          selectionKey: 'platform + accountId',
          items: [
            { id: 'all-stores', title: '全部店铺', scope: 'workspace', action: { method: 'catalog.search', arguments: { scope: 'workspace' } } },
            ...directory.map(store => ({
              id: `store:${store.platform}:${store.accountId}`,
              title: store.label,
              platform: store.platform,
              accountId: store.accountId,
              state: store.state,
              dataMode: store.dataMode,
              readable: store.readable,
              writeEnabled: store.writeEnabled,
              action: { method: 'catalog.search', arguments: { scope: 'store', platform: store.platform, account_id: store.accountId } },
            })),
          ],
          emptyState: '尚未绑定店铺；先选择平台并完成官方授权，再同步商品。',
        },
        brandNavigation: { title: '我的品', presentation: 'tree', hierarchy: ['brand', 'platform', 'store'], items: brandNavigation, emptyState: '尚未创建品；先调用 brand-unit.create，再调用 brand-unit.bind-store。' },
        onboarding,
        cards: MERCHANT_CAPABILITY_CARDS.map(card => ({ ...card, writeGate: card.readOnly ? 'none' : 'interactive_confirmation' })),
      },
    })
    }
    case 'workspace.commercial.get': {
      // The commercial summary is used by both the merchant workspace and the
      // ops console. Keep the summary useful for operators while preventing a
      // regular member from seeing another member's subscription orders.
      const principal = requestPrincipals.get(req)
      const privilegedBillingReader = authorizedRoles(principal).some(role => ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'].includes(role))
      const actorId = requestActor(req, 'actor_demo')
      const orders = await (persistence.subscriptions ?? memorySubscriptions).listOrders(workspaceId, 20, privilegedBillingReader ? undefined : actorId)
      return result({ settings: await (persistence.commercial ?? memoryCommercial).getSettings(workspaceId), platforms: await (persistence.commercial ?? memoryCommercial).listPlatformSettings(workspaceId), usage: await (persistence.usage ?? memoryUsage).get(workspaceId), subscription: await (persistence.subscriptions ?? memorySubscriptions).get(workspaceId), orders, entitlements: await (persistence.entitlements ?? memoryEntitlements).list(workspaceId) })
    }
    case 'subscription.get': {
      const subscription = await (persistence.subscriptions ?? memorySubscriptions).get(workspaceId)
      return result({ ...subscription, commercial_entitlement: commercialEntitlementProjection(subscription), entitlements: await (persistence.entitlements ?? memoryEntitlements).list(workspaceId) })
    }
    case 'subscription.orders.list': {
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Number(params.limit) : 50
      const { scope, actorId } = billingReadScope(req, params)
      const orders = await (persistence.subscriptions ?? memorySubscriptions).listOrders(workspaceId, limit, scope === 'mine' ? actorId : undefined)
      return result(orders)
    }
    case 'subscription.order.create': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance'])
      requireProviderPaymentConfigured()
      const channel = paymentChannel(params)
      const cycle = required(params, 'billing_cycle') as BillingCycle
      if (!['monthly', 'annual'].includes(cycle)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'billing_cycle 必须是 monthly 或 annual', 400)
      const subscriptionRepository = persistence.subscriptions ?? memorySubscriptions
      const idempotencyKey = required(params, 'idempotency_key')
      const extensionRepository = persistence.commercialExtensions ?? memoryCommercialExtensions
      const planCode = required(params, 'plan_code')
      const offer = await requireActiveCommercialOffer(extensionRepository, planCode, cycle)
      let addonCodes: string[] = []
      if (typeof params.addon_codes_json === 'string' && params.addon_codes_json.trim()) {
        try { const parsed = JSON.parse(params.addon_codes_json) as unknown; if (!Array.isArray(parsed) || parsed.some(item => typeof item !== 'string')) throw new Error('invalid'); addonCodes = [...new Set(parsed)] } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'addon_codes_json 必须是字符串数组', 400) }
      }
      const addons = await extensionRepository.listAddons()
      const selectedAddons = addonCodes.map(code => addons.find(item => item.code === code && item.active))
      if (selectedAddons.some(item => !item)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '存在不可用的加购能力', 400)
      const addonTotal = selectedAddons.reduce((sum, item) => sum + (item?.priceCny ?? 0), 0)
      const subtotal = Number((offer.priceCny + addonTotal).toFixed(2))
      const couponCode = typeof params.coupon_code === 'string' && params.coupon_code.trim() ? params.coupon_code.trim() : undefined
      const sourceChannel = typeof params.source_channel === 'string' ? params.source_channel : undefined
      const intent = JSON.stringify({ planCode: offer.code, planName: offer.name, billingCycle: offer.billingCycle, priceCny: subtotal, couponCode: couponCode ?? null, addonCodes: [...addonCodes].sort(), sourceChannel: sourceChannel ?? null, paymentProvider: channel, createdByActorId: actorId })
      const existingOrder = await subscriptionRepository.getOrderByIdempotencyKey(workspaceId, idempotencyKey)
      if (existingOrder) {
        const existingIntent = JSON.stringify({ planCode: existingOrder.planCode, planName: existingOrder.planName, billingCycle: existingOrder.billingCycle, priceCny: existingOrder.priceCny, couponCode: existingOrder.couponCode ?? null, addonCodes: [...existingOrder.addonCodes].sort(), sourceChannel: existingOrder.sourceChannel ?? null, paymentProvider: existingOrder.paymentProvider, createdByActorId: existingOrder.createdByActorId ?? null })
        if (existingIntent !== intent) throw new DomainError('SUBSCRIPTION_ORDER_IDEMPOTENCY_CONFLICT', '订阅订单幂等键已被其他订单意图使用', 409)
        return result(existingOrder)
      }
      const inFlightKey = `${workspaceId}:${idempotencyKey}`
      const inFlight = subscriptionCreationInFlight.get(inFlightKey)
      if (inFlight) {
        if (inFlight.intent !== intent) throw new DomainError('SUBSCRIPTION_ORDER_IDEMPOTENCY_CONFLICT', '订阅订单幂等键已被其他订单意图使用', 409)
        return result(await inFlight.promise)
      }
      const promise = (async () => {
        let paymentAmount = subtotal
        let couponRedeemed = false
        if (couponCode) {
          let coupon
          try { coupon = await extensionRepository.redeemCoupon(couponCode) } catch (error) { if (error instanceof Error && error.message === 'COUPON_NOT_REDEEMABLE') throw new DomainError('COUPON_NOT_REDEEMABLE', '优惠券不存在、已停用、已过期或已达到核销上限', 409); throw error }
          const discount = coupon.discountType === 'percent' ? subtotal * coupon.discountValue / 100 : coupon.discountValue
          paymentAmount = Number(Math.max(0, subtotal - discount).toFixed(2))
          couponRedeemed = true
        }
        const orderNo = `SO${createHash('sha256').update(`${workspaceId}:${idempotencyKey}`).digest('hex').slice(0, 24).toUpperCase()}`
        let checkout: Awaited<ReturnType<typeof createSubscriptionCheckout>>
        try {
          checkout = await createSubscriptionCheckout({ channel, orderId: orderNo, idempotencyKey, workspaceId, amountFen: Math.round(paymentAmount * 100), kind: 'subscriptions' })
        } catch (error) {
          if (couponCode && couponRedeemed) {
            try { await extensionRepository.restoreCoupon(couponCode) } catch { throw new DomainError('COUPON_ROLLBACK_FAILED', '支付下单失败且优惠券回滚失败，请联系运营处理', 503) }
          }
          throw error
        }
        let order
        try {
          order = await subscriptionRepository.createOrder({ workspaceId, orderNo, planCode: offer.code, planName: offer.name, billingCycle: offer.billingCycle, priceCny: subtotal, paymentAmountCny: paymentAmount, includedStores: offer.includedStores, includedTasks: offer.includedTasks, couponCode, addonCodes, sourceChannel, paymentProvider: channel, paymentUrl: checkout.paymentUrl, createdByActorId: actorId, idempotencyKey })
        } catch (error) {
          if (couponCode) {
            try { await extensionRepository.restoreCoupon(couponCode) } catch { throw new DomainError('COUPON_ROLLBACK_FAILED', '订单创建失败且优惠券回滚失败，请联系运营处理', 503) }
          }
          throw error
        }
        await recordOperationAudit({ workspaceId, actorId, action: 'subscription.order.create', resourceType: 'subscription_order', resourceId: order.orderNo, before: {}, after: order as unknown as Record<string, unknown>, reason: '创建订阅订单' })
        await recordGrowthEvent({ workspaceId, eventType: 'subscription.order.created', sourceChannel: order.sourceChannel, actorId, planCode: order.planCode, metadata: { orderNo: order.orderNo, paymentAmountCny: order.paymentAmountCny, couponCode: order.couponCode, addonCodes: order.addonCodes } })
        return { ...order, ...(checkout.providerOrderId ? { provider_order_id: checkout.providerOrderId } : {}), ...(checkout.expiresAt ? { expires_at: checkout.expiresAt } : {}), warning: process.env.PAYMENT_MODE === 'provider' ? '请完成支付，系统只接受支付服务商签名回调后激活订阅' : '当前为本地 fixture，不会产生真实扣款' }
      })()
      subscriptionCreationInFlight.set(inFlightKey, { intent, promise })
      try { return result(await promise) } finally { subscriptionCreationInFlight.delete(inFlightKey) }
    }
    case 'subscription.change': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance'])
      const toPlanCode = required(params, 'to_plan_code'); const cycle = required(params, 'billing_cycle') as BillingCycle
      if (!['monthly', 'annual'].includes(cycle)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'billing_cycle 必须是 monthly 或 annual', 400)
      const reason = required(params, 'reason')
      const rawIdempotencyKey = required(params, 'idempotency_key')
      const requestedEffectiveAt = typeof params.effective_at === 'string' && params.effective_at ? params.effective_at : null
      const requestedChannel = params.channel === 'alipay' || params.channel === 'wechat' ? params.channel : null
      const intent = JSON.stringify({ toPlanCode, billingCycle: cycle, effectiveAt: requestedEffectiveAt, reason, paymentProvider: requestedChannel })
      const inFlightKey = `${workspaceId}:${rawIdempotencyKey}`
      const inFlight = subscriptionChangeInFlight.get(inFlightKey)
      if (inFlight) {
        if (inFlight.intent !== intent) throw new DomainError('SUBSCRIPTION_CHANGE_IDEMPOTENCY_CONFLICT', '套餐变更幂等键已被其他变更意图使用', 409)
        return result(await inFlight.promise)
      }
      const promise = (async () => {
        const current = await (persistence.subscriptions ?? memorySubscriptions).get(workspaceId)
        const offer = await requireActiveCommercialOffer(persistence.commercialExtensions ?? memoryCommercialExtensions, toPlanCode, cycle)
        const difference = Number((offer.priceCny - current.priceCny).toFixed(2)); const upgrade = difference > 0
        const effectiveAt = requestedEffectiveAt ?? (upgrade ? new Date().toISOString() : current.currentPeriodEnd)
        if (Number.isNaN(Date.parse(effectiveAt))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'effective_at 必须是有效时间', 400)
        const channel = upgrade ? paymentChannel(params) : undefined
        const changeIdempotencyKey = `change:${workspaceId}:${rawIdempotencyKey}`
        let order: unknown
        if (upgrade) {
          requireProviderPaymentConfigured()
          const subscriptionRepository = persistence.subscriptions ?? memorySubscriptions
          const existingOrder = (await subscriptionRepository.listOrders(workspaceId, 100)).find(item => item.idempotencyKey === changeIdempotencyKey)
          if (existingOrder) {
            if (existingOrder.planCode !== offer.code || existingOrder.planName !== offer.name || existingOrder.billingCycle !== offer.billingCycle || existingOrder.priceCny !== offer.priceCny || existingOrder.paymentAmountCny !== difference || existingOrder.includedStores !== offer.includedStores || existingOrder.includedTasks !== offer.includedTasks || existingOrder.paymentProvider !== channel) throw new DomainError('SUBSCRIPTION_CHANGE_IDEMPOTENCY_CONFLICT', '套餐变更幂等键已被其他变更意图使用', 409)
            order = existingOrder
            const existingChange = await (persistence.commercialExtensions ?? memoryCommercialExtensions).getPendingChange(workspaceId)
            if (existingChange && existingChange.fromPlanCode === current.planCode && existingChange.fromPriceCny === current.priceCny && existingChange.toPlanCode === offer.code && existingChange.toPriceCny === offer.priceCny && existingChange.billingCycle === cycle && existingChange.priceDifferenceCny === difference && (requestedEffectiveAt === null || existingChange.effectiveAt === effectiveAt) && existingChange.reason === reason) {
              return { change: existingChange, order, mode: 'upgrade_payment_required' }
            }
          } else {
            const orderNo = `SO${Date.now()}${Math.floor(Math.random() * 1000).toString().padStart(3, '0')}`
            const checkout = await createSubscriptionCheckout({ channel: channel!, orderId: orderNo, idempotencyKey: changeIdempotencyKey, workspaceId, amountFen: Math.round(difference * 100), kind: 'subscriptions' })
            order = await subscriptionRepository.createOrder({ workspaceId, orderNo, planCode: offer.code, planName: offer.name, billingCycle: offer.billingCycle, priceCny: offer.priceCny, paymentAmountCny: difference, includedStores: offer.includedStores, includedTasks: offer.includedTasks, paymentProvider: channel!, paymentUrl: checkout.paymentUrl, createdByActorId: actorId, idempotencyKey: changeIdempotencyKey })
          }
        }
        const change = await (persistence.commercialExtensions ?? memoryCommercialExtensions).scheduleChange({ workspaceId, fromPlanCode: current.planCode, toPlanCode: offer.code, fromPriceCny: current.priceCny, toPriceCny: offer.priceCny, billingCycle: cycle, priceDifferenceCny: difference, effectiveAt, reason, createdBy: actorId })
        await recordOperationAudit({ workspaceId, actorId, action: 'subscription.change', resourceType: 'subscription_change', resourceId: change.id, before: current as unknown as Record<string, unknown>, after: { change, order } as unknown as Record<string, unknown>, reason: change.reason })
        return { change, order, mode: upgrade ? 'upgrade_payment_required' : 'downgrade_next_period' }
      })()
      subscriptionChangeInFlight.set(inFlightKey, { intent, promise })
      try { return result(await promise) } finally { subscriptionChangeInFlight.delete(inFlightKey) }
    }
    case 'workspace.usage.get':
      return result(await (persistence.usage ?? memoryUsage).get(workspaceId))
    case 'ops.support.sla.report': {
      const repository = persistence.support
      if (!repository) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置', 503)
      const periodStart = requiredStringValue(params, 'periodStart', 'period_start')
      const periodEnd = requiredStringValue(params, 'periodEnd', 'period_end')
      const cutoffAt = requiredStringValue(params, 'cutoffAt', 'cutoff_at')
      const reportId = optionalStringValue(params, 'reportId', 'report_id') ?? `support-sla:${workspaceId}:${periodStart}:${periodEnd}`
      return result(await generateSupportSlaMonthlyReport({ workspaceId, reportId, periodStart, periodEnd, cutoffAt }))
    }
    case 'ops.support.sla.correction.create': {
      const reporting = persistence.supportSlaReporting ?? memorySupportSlaReporting
      const originalReportId = requiredStringValue(params, 'originalReportId', 'original_report_id')
      const original = await reporting.getReport({ workspaceId, reportId: originalReportId })
      if (!original) throw new DomainError('SUPPORT_SLA_REPORT_NOT_FOUND', '原始 SLA 月报不存在或不属于当前工作区', 404)
      const periodStart = requiredStringValue(params, 'periodStart', 'period_start')
      const periodEnd = requiredStringValue(params, 'periodEnd', 'period_end')
      const cutoffAt = requiredStringValue(params, 'cutoffAt', 'cutoff_at')
      if (periodStart !== original.periodStart || periodEnd !== original.periodEnd || cutoffAt !== original.cutoffAt) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'correction 的报告周期必须与原始月报完全一致', 400)
      const reason = requiredStringValue(params, 'reason', 'reason')
      const idempotencyKey = requiredStringValue(params, 'idempotencyKey', 'idempotency_key')
      const corrected = await buildWorkspaceSupportSlaMonthlyReport({ workspaceId, reportId: original.reportId, periodStart, periodEnd, cutoffAt })
      const correction = createSupportSlaCorrectionRun({ original, corrected, correctionId: idempotencyKey, reason })
      if (!correction) return result({ status: 'no_change', original_report_id: original.reportId, checksum: original.checksum })
      return result(await reporting.createCorrection({ correction }))
    }
    case 'ops.support.sla.correction.decide': {
      const reporting = persistence.supportSlaReporting ?? memorySupportSlaReporting
      const correctionId = requiredStringValue(params, 'correctionId', 'correction_id')
      const correction = await reporting.getCorrection({ workspaceId, correctionId })
      if (!correction) throw new DomainError('SUPPORT_SLA_CORRECTION_NOT_FOUND', 'SLA correction 不存在或不属于当前工作区', 404)
      const existing = await reporting.getCorrectionDecision({ workspaceId, correctionId })
      if (existing) return result(existing)
      const decision = requiredStringValue(params, 'decision', 'decision')
      if (decision !== 'approved' && decision !== 'rejected') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'correction decision 必须是 approved 或 rejected', 400)
      const reason = requiredStringValue(params, 'reason', 'reason')
      const idempotencyKey = requiredStringValue(params, 'idempotencyKey', 'idempotency_key')
      const actorId = requestActor(req)
      const decidedAt = new Date().toISOString()
      const approval: SupportSlaCorrectionApproval = { approvalId: idempotencyKey, correctionId, workspaceId, decision, reason, actorId, idempotencyKey, approvedAt: decidedAt }
      await reporting.addCorrectionApproval({ approval })
      const approvals = await reporting.listCorrectionApprovals({ workspaceId, correctionId })
      if (decision === 'rejected') return result(await reporting.decideCorrection({ decision: { decisionId: `final:${correctionId}:rejected`, correctionId, workspaceId, decision: 'rejected', reason, actorId, idempotencyKey: `final:${correctionId}:rejected`, decidedAt } }))
      if (approvals.length < 2) return result({ status: 'pending_approval', correctionId, workspaceId, approvals, requiredApprovals: 2 })
      if (new Set(approvals.map(item => item.actorId)).size < 2) throw new DomainError('SUPPORT_SLA_CORRECTION_INDEPENDENT_APPROVAL_REQUIRED', 'SLA correction 批准必须来自两个不同的运营身份', 409)
      return result(await reporting.decideCorrection({ decision: { decisionId: `final:${correctionId}:approved`, correctionId, workspaceId, decision: 'approved', reason: '两名独立运营人员批准 correction', actorId, idempotencyKey: `final:${correctionId}:approved`, decidedAt } }))
    }
    case 'ops.support.tickets.list': {
      const repository = persistence.support
      if (!repository) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置', 503)
      const cursorValue = structuredValue(params, 'cursor', 'cursor_json')
      const cursor = cursorValue === undefined ? undefined : isObject(cursorValue)
        ? { id: cursorValue.id, createdAt: cursorValue.createdAt ?? cursorValue.created_at } as SupportTicketPageCursor
        : undefined
      if (cursorValue !== undefined && !cursor) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'cursor 必须是对象', 400)
      const platformScope = optionalStringValue(params, 'platformScope', 'platform_scope')
      if (platformScope && platformScope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform_scope 只能是 platform', 400)
      if (platformScope === 'platform') {
        if (!isPlatformOperations(req)) throw new DomainError(ERROR_CODES.FORBIDDEN, '平台聚合视图需要已绑定的 platform workbench', 403)
        requireOperationsRole(req, ['platform_ops'])
        const limit = Math.min(100, Math.max(1, optionalNumberValue(params, 'limit') ?? 50))
        const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
        const pages = [] as SupportTicketContract[]
        for (let offset = 0; offset < workspaceIds.length; offset += 8) {
          const batch = await Promise.all(workspaceIds.slice(offset, offset + 8).map(targetWorkspaceId => new SupportService(repository).list(supportContext(req, targetWorkspaceId), {
            workspaceId: targetWorkspaceId,
            ...(optionalStringValue(params, 'status') ? { status: optionalStringValue(params, 'status') as SupportTicketStatus } : {}),
            ...(optionalStringValue(params, 'priority') ? { priority: optionalStringValue(params, 'priority') as SupportTicketPriority } : {}),
            ...(optionalStringValue(params, 'assigneeId', 'assignee_id') ? { assigneeId: optionalStringValue(params, 'assigneeId', 'assignee_id') } : {}),
            ...(optionalStringValue(params, 'customerId', 'customer_id') ? { customerId: optionalStringValue(params, 'customerId', 'customer_id') } : {}),
            ...(optionalStringValue(params, 'query') ? { query: optionalStringValue(params, 'query') } : {}),
            limit,
          })))
          for (const page of batch) pages.push(...page.items)
        }
        const groups = new Map<string, { status: SupportTicketStatus; priority: SupportTicketPriority; count: number; latestCreatedAt: string }>()
        for (const ticket of pages) {
          const key = `${ticket.status}:${ticket.priority}`
          const current = groups.get(key)
          if (current) { current.count += 1; if (ticket.createdAt > current.latestCreatedAt) current.latestCreatedAt = ticket.createdAt }
          else groups.set(key, { status: ticket.status, priority: ticket.priority, count: 1, latestCreatedAt: ticket.createdAt })
        }
        const items = [...groups.values()].sort((left, right) => right.latestCreatedAt.localeCompare(left.latestCreatedAt)).map((group, index) => ({ id: `platform-support-group-${index + 1}`, workspaceId: 'platform-aggregate', ticketNumber: `平台聚合-${index + 1}`, subject: `${group.count} 个客服工单`, description: '平台聚合视图已脱敏；切换到明确工作区授权会话查看工单详情。', status: group.status, priority: group.priority, customerId: 'redacted', customerName: '平台聚合', tags: [], revision: 0, createdBy: 'platform_aggregate', createdAt: group.latestCreatedAt, updatedAt: group.latestCreatedAt, aggregate: true, count: group.count }))
        return result({ items: items.slice(0, limit), aggregate: true, truncated: items.length > limit })
      }
      return result(await invokeOpsDomain(() => new SupportService(repository).list(supportContext(req, workspaceId), {
        workspaceId,
        ...(optionalStringValue(params, 'status') ? { status: optionalStringValue(params, 'status') as SupportTicketStatus } : {}),
        ...(optionalStringValue(params, 'priority') ? { priority: optionalStringValue(params, 'priority') as SupportTicketPriority } : {}),
        ...(optionalStringValue(params, 'assigneeId', 'assignee_id') ? { assigneeId: optionalStringValue(params, 'assigneeId', 'assignee_id') } : {}),
        ...(optionalStringValue(params, 'customerId', 'customer_id') ? { customerId: optionalStringValue(params, 'customerId', 'customer_id') } : {}),
        ...(optionalStringValue(params, 'query') ? { query: optionalStringValue(params, 'query') } : {}),
        ...(cursor ? { cursor } : {}),
        ...(optionalNumberValue(params, 'limit') !== undefined ? { limit: optionalNumberValue(params, 'limit') } : {}),
      })))
    }
    case 'ops.support.ticket.get': {
      const repository = persistence.support
      if (!repository) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置', 503)
      return result(await invokeOpsDomain(() => new SupportService(repository).get(supportContext(req, workspaceId), workspaceId, requiredStringValue(params, 'ticketId', 'ticket_id'))))
    }
    case 'ops.support.ticket.create': {
      const repository = persistence.support
      if (!repository) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置', 503)
      return result(await invokeOpsDomain(() => new SupportService(repository).create(supportContext(req, workspaceId), {
        workspaceId,
        subject: requiredStringValue(params, 'subject'), description: requiredStringValue(params, 'description'),
        priority: requiredStringValue(params, 'priority') as SupportTicketPriority,
        customerId: requiredStringValue(params, 'customerId', 'customer_id'), customerName: requiredStringValue(params, 'customerName', 'customer_name'),
        ...(optionalStringValue(params, 'customerEmail', 'customer_email') ? { customerEmail: optionalStringValue(params, 'customerEmail', 'customer_email') } : {}),
        ...(optionalStringValue(params, 'relatedOrderId', 'related_order_id') ? { relatedOrderId: optionalStringValue(params, 'relatedOrderId', 'related_order_id') } : {}),
        ...(optionalStringValue(params, 'relatedTaskId', 'related_task_id') ? { relatedTaskId: optionalStringValue(params, 'relatedTaskId', 'related_task_id') } : {}),
        ...(stringArrayValue(params, 'tags', 'tags_json') ? { tags: stringArrayValue(params, 'tags', 'tags_json') } : {}),
        idempotencyKey: requiredStringValue(params, 'idempotencyKey', 'idempotency_key'),
      })))
    }
    case 'ops.support.ticket.assign':
    case 'ops.support.ticket.transition':
    case 'ops.support.ticket.comment': {
      const repository = persistence.support
      if (!repository) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置', 503)
      const support = new SupportService(repository)
      const context = supportContext(req, workspaceId)
      const common = { workspaceId, ticketId: requiredStringValue(params, 'ticketId', 'ticket_id'), expectedRevision: optionalNumberValue(params, 'expectedRevision', 'expected_revision') ?? 0, idempotencyKey: requiredStringValue(params, 'idempotencyKey', 'idempotency_key') }
      if (method === 'ops.support.ticket.assign') return result(await invokeOpsDomain(() => support.assign(context, { ...common, assigneeId: requiredStringValue(params, 'assigneeId', 'assignee_id') })))
      if (method === 'ops.support.ticket.transition') return result(await invokeOpsDomain(() => support.transition(context, { ...common, status: requiredStringValue(params, 'status') as SupportTicketStatus, reason: requiredStringValue(params, 'reason') })))
      return result(await invokeOpsDomain(() => support.comment(context, { ...common, body: requiredStringValue(params, 'body'), visibility: requiredStringValue(params, 'visibility') as 'internal' | 'customer' })))
    }
    case 'ops.support.crm.export': {
      const repository = persistence.support
      if (!repository) throw new DomainError('SUPPORT_REPOSITORY_UNAVAILABLE', '客服工单仓储未配置', 503)
      return result(await invokeOpsDomain(() => new SupportService(repository).exportCrm(supportContext(req, workspaceId), workspaceId, optionalNumberValue(params, 'limit') ?? 5000)))
    }
    case 'ops.incidents.list':
    case 'ops.incident.get':
    case 'ops.incident.timeline':
    case 'ops.incident.create':
    case 'ops.incident.transition':
    case 'ops.incident.comment':
    case 'ops.incident.commander.assign':
    case 'ops.incident.scope.update': {
      const repository = persistence.incidents
      if (!repository) throw new DomainError('INCIDENT_REPOSITORY_UNAVAILABLE', '事故仓储未配置', 503)
      const incidents = new IncidentsService(repository)
      const actor = incidentActor(req, workspaceId)
      const platformScope = optionalStringValue(params, 'platformScope', 'platform_scope')
      if (platformScope && platformScope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform_scope 只能是 platform', 400)
      const raw = {
        ...(optionalStringValue(params, 'incidentId', 'incident_id') ? { incidentId: optionalStringValue(params, 'incidentId', 'incident_id') } : {}),
        ...(optionalStringValue(params, 'title') ? { title: optionalStringValue(params, 'title') } : {}),
        ...(optionalStringValue(params, 'summary') ? { summary: optionalStringValue(params, 'summary') } : {}),
        ...(optionalStringValue(params, 'severity') ? { severity: optionalStringValue(params, 'severity') } : {}),
        ...(optionalStringValue(params, 'status') ? { status: optionalStringValue(params, 'status') } : {}),
        ...(optionalStringValue(params, 'toStatus', 'to_status') ? { toStatus: optionalStringValue(params, 'toStatus', 'to_status') } : {}),
        ...(optionalStringValue(params, 'commanderId', 'commander_id') ? { commanderId: optionalStringValue(params, 'commanderId', 'commander_id') } : {}),
        ...(optionalStringValue(params, 'note') ? { note: optionalStringValue(params, 'note') } : {}),
        ...(optionalStringValue(params, 'body') ? { body: optionalStringValue(params, 'body') } : {}),
        ...(optionalStringValue(params, 'idempotencyKey', 'idempotency_key') ? { idempotencyKey: optionalStringValue(params, 'idempotencyKey', 'idempotency_key') } : {}),
        ...(optionalNumberValue(params, 'expectedRevision', 'expected_revision') !== undefined ? { expectedRevision: optionalNumberValue(params, 'expectedRevision', 'expected_revision') } : {}),
        ...(optionalNumberValue(params, 'limit') !== undefined ? { limit: optionalNumberValue(params, 'limit') } : {}),
        ...(optionalStringValue(params, 'cursor') ? { cursor: optionalStringValue(params, 'cursor') } : {}),
        affectedComponents: stringArrayValue(params, 'affectedComponents', 'affected_components_json') ?? [],
        affectedWorkspaceIds: stringArrayValue(params, 'affectedWorkspaceIds', 'affected_workspace_ids_json') ?? [],
      }
      if (method === 'ops.incidents.list') {
        if (platformScope === 'platform') {
          if (!isPlatformOperations(req)) throw new DomainError(ERROR_CODES.FORBIDDEN, '平台聚合视图需要已绑定的 platform workbench', 403)
          requireOperationsRole(req, ['platform_ops'])
          const limit = Math.min(100, Math.max(1, optionalNumberValue(params, 'limit') ?? 50))
          const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
          const items = [] as Awaited<ReturnType<IncidentsService['list']>>['items']
          for (let offset = 0; offset < workspaceIds.length; offset += 8) {
            const batch = await Promise.all(workspaceIds.slice(offset, offset + 8).map(targetWorkspaceId => new IncidentsService(repository).list(incidentActor(req, targetWorkspaceId), { ...raw, limit })))
            for (const page of batch) items.push(...page.items)
          }
          const groups = new Map<string, { severity: IncidentSeverity; status: IncidentStatus; count: number; latestUpdatedAt: string }>()
          for (const incident of items) {
            const key = `${incident.severity}:${incident.status}`
            const current = groups.get(key)
            if (current) { current.count += 1; if (incident.updatedAt > current.latestUpdatedAt) current.latestUpdatedAt = incident.updatedAt }
            else groups.set(key, { severity: incident.severity, status: incident.status, count: 1, latestUpdatedAt: incident.updatedAt })
          }
          const aggregateItems = [...groups.values()].sort((left, right) => right.latestUpdatedAt.localeCompare(left.latestUpdatedAt)).map((group, index) => ({ id: `platform-incident-group-${index + 1}`, workspaceId: 'platform-aggregate', title: `${group.count} 起平台事故`, summary: '平台聚合视图已脱敏；切换到明确工作区授权会话查看事故详情。', severity: group.severity, status: group.status, affectedComponents: [], affectedWorkspaceIds: [], revision: 0, createdBy: 'platform_aggregate', createdAt: group.latestUpdatedAt, updatedAt: group.latestUpdatedAt, aggregate: true, count: group.count }))
          return result({ items: aggregateItems.slice(0, limit), aggregate: true, truncated: aggregateItems.length > limit })
        }
        return result(await invokeOpsDomain(() => incidents.list(actor, raw)))
      }
      if (method === 'ops.incident.get') return result(await invokeOpsDomain(() => incidents.get(actor, requiredStringValue(params, 'incidentId', 'incident_id'))))
      if (method === 'ops.incident.timeline') return result(await invokeOpsDomain(() => incidents.timeline(actor, raw)))
      if (method === 'ops.incident.create') return result(await invokeOpsDomain(() => incidents.create(actor, raw)))
      if (method === 'ops.incident.transition') return result(await invokeOpsDomain(() => incidents.transition(actor, raw)))
      if (method === 'ops.incident.comment') return result(await invokeOpsDomain(() => incidents.comment(actor, raw)))
      if (method === 'ops.incident.commander.assign') return result(await invokeOpsDomain(() => incidents.assignCommander(actor, raw)))
      return result(await invokeOpsDomain(() => incidents.updateScope(actor, raw)))
    }
    case 'ops.canonical.backfill.create':
    case 'ops.canonical.backfill.get':
    case 'ops.canonical.backfill.pause':
    case 'ops.canonical.backfill.resume':
    case 'ops.canonical.backfill.run':
    case 'ops.canonical.backfill.conflicts.list':
    case 'ops.canonical.backfill.conflict.claim':
    case 'ops.canonical.backfill.conflict.resolve': {
      requireOperationsRole(req, ['platform_ops'])
      await persistenceReady
      const repository = persistence.canonicalBackfillRuns
      if (!repository || persistence.mode !== 'postgres') throw new DomainError('CANONICAL_BACKFILL_REPOSITORY_UNAVAILABLE', 'canonical backfill 批次控制仅在 PostgreSQL 持久化模式可用', 503)
      const conflictRepository = persistence.canonicalBackfillConflicts
      const actorId = requestActor(req)
      if (method === 'ops.canonical.backfill.create') {
        const run = await repository.create({ workspaceId, dryRun: aliasValue(params, 'dryRun', 'dry_run') === true || aliasValue(params, 'dryRun', 'dry_run') === 'true', ...(optionalNumberValue(params, 'batchLimit', 'batch_limit') === undefined ? {} : { batchLimit: optionalNumberValue(params, 'batchLimit', 'batch_limit') }), createdBy: actorId, reason: requiredStringValue(params, 'reason') })
        await recordOperationAudit({ workspaceId, actorId, action: 'canonical.backfill.create', resourceType: 'canonical_backfill_run', resourceId: run.id, before: {}, after: run as unknown as Record<string, unknown>, reason: run.reason })
        return result(run)
      }
      const runId = optionalStringValue(params, 'runId', 'run_id')
      if (method === 'ops.canonical.backfill.conflicts.list') {
        if (!conflictRepository) throw new DomainError('CANONICAL_BACKFILL_CONFLICT_REPOSITORY_UNAVAILABLE', 'canonical backfill 冲突队列未配置', 503)
        return result(await conflictRepository.list({ workspaceId, ...(runId ? { runId } : {}), ...(optionalStringValue(params, 'status') ? { status: optionalStringValue(params, 'status') as 'open' | 'claimed' | 'resolved' | 'dismissed' } : {}), ...(optionalNumberValue(params, 'limit') === undefined ? {} : { limit: optionalNumberValue(params, 'limit') }) }))
      }
      if (method === 'ops.canonical.backfill.conflict.claim' || method === 'ops.canonical.backfill.conflict.resolve') {
        const conflictId = requiredStringValue(params, 'conflictId', 'conflict_id')
        const expectedRevision = optionalNumberValue(params, 'expectedRevision', 'expected_revision')
        if (expectedRevision === undefined || expectedRevision < 1) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_revision 必须是正整数', 400)
        const action = method.endsWith('.claim') ? 'claim' : 'resolve'
        const reason = requiredStringValue(params, 'reason')
        if (!conflictRepository) throw new DomainError('CANONICAL_BACKFILL_CONFLICT_REPOSITORY_UNAVAILABLE', 'canonical backfill 冲突队列未配置', 503)
        let conflict
        try {
          const status = requiredStringValue(params, 'status') as 'resolved' | 'dismissed'
          if (action === 'resolve' && status === 'resolved') {
            if (!persistence.canonicalBackfillRemediation) throw new DomainError('CANONICAL_BACKFILL_REMEDIATION_UNAVAILABLE', 'canonical backfill 商品事实修复未配置', 503)
            if (optionalStringValue(params, 'remediationType', 'remediation_type') !== 'set_legacy_brand') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'resolved 仅支持 set_legacy_brand 修复类型', 400)
            const expectedProductVersion = optionalNumberValue(params, 'expectedProductVersion', 'expected_product_version')
            if (expectedProductVersion === undefined || expectedProductVersion < 1) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'resolved 必须提供 expected_product_version', 400)
            conflict = await persistence.canonicalBackfillRemediation.setLegacyBrand({
              workspaceId, conflictId, expectedConflictRevision: expectedRevision, expectedProductVersion,
              brandId: requiredStringValue(params, 'brandId', 'brand_id'), actorId, reason,
              resolutionNote: requiredStringValue(params, 'resolutionNote', 'resolution_note'),
              ...(optionalStringValue(params, 'reference') ? { reference: optionalStringValue(params, 'reference') } : {}),
            })
          } else {
            conflict = action === 'claim'
            ? await conflictRepository.claim({ workspaceId, id: conflictId, expectedRevision, assigneeId: actorId })
            : await conflictRepository.resolve({ workspaceId, id: conflictId, expectedRevision, status, assigneeId: actorId, resolutionNote: requiredStringValue(params, 'resolutionNote', 'resolution_note') })
          }
        } catch (error) {
          if (error instanceof CanonicalBackfillConflictRevisionConflictError) throw new DomainError('CANONICAL_BACKFILL_CONFLICT_REVISION_CONFLICT', 'canonical backfill 冲突已被其他操作更新，请刷新后重试', 409, { conflict_id: conflictId, expected_revision: expectedRevision })
          if (error instanceof Error && error.message === 'CANONICAL_BACKFILL_CONFLICT_NOT_FOUND') throw new DomainError('CANONICAL_BACKFILL_CONFLICT_NOT_FOUND', 'canonical backfill 冲突不存在或不属于当前工作区', 404, { conflict_id: conflictId })
          if (error instanceof Error && error.message === 'CANONICAL_BACKFILL_CONFLICT_STATE_INVALID') throw new DomainError('CANONICAL_BACKFILL_CONFLICT_STATE_INVALID', 'canonical backfill 冲突当前状态不允许执行该操作', 409, { conflict_id: conflictId, expected_revision: expectedRevision })
          if (error instanceof CanonicalBackfillRemediationError) {
            const status = new Set(['CANONICAL_BACKFILL_CONFLICT_REVISION_CONFLICT', 'CANONICAL_BACKFILL_SOURCE_VERSION_CONFLICT', 'CANONICAL_BACKFILL_CONFLICT_STATE_INVALID', 'CANONICAL_BACKFILL_SOURCE_ALREADY_BRANDED']).has(error.code) ? 409 : 422
            throw new DomainError(error.code, 'canonical backfill 商品事实修复未通过安全校验', status, { conflict_id: conflictId, expected_revision: expectedRevision })
          }
          throw error
        }
        await recordOperationAudit({ workspaceId, actorId, action: `canonical.backfill.conflict.${action}`, resourceType: 'canonical_backfill_conflict', resourceId: conflict.id, before: { conflict_id: conflictId, revision: expectedRevision }, after: { ...conflict as unknown as Record<string, unknown>, audit_reason: reason, expected_revision: expectedRevision }, reason })
        return result(conflict)
      }
      if (!runId) throw new DomainError('CANONICAL_BACKFILL_RUN_ID_REQUIRED', 'canonical backfill 批次 ID 必填', 400)
      const current = await repository.get({ workspaceId, id: runId })
      if (!current) throw new DomainError('CANONICAL_BACKFILL_RUN_NOT_FOUND', 'canonical backfill 批次不存在', 404)
      if (method === 'ops.canonical.backfill.get') return result(current)
      if (method === 'ops.canonical.backfill.run') {
        if (!['planned', 'running'].includes(current.status)) throw new DomainError('CANONICAL_BACKFILL_RUN_STATE_INVALID', `当前状态 ${current.status} 不允许执行批次`, 409, { status: current.status })
        const executor = persistence.executeCanonicalBackfill
        if (!executor) throw new DomainError('CANONICAL_BACKFILL_EXECUTOR_UNAVAILABLE', 'canonical backfill 执行器未配置', 503)
        const claimed = current.status === 'running' ? current : await repository.update({ id: current.id, workspaceId, expectedRevision: optionalNumberValue(params, 'expectedRevision', 'expected_revision') ?? 0, status: 'running' })
        try {
          const batch = await executor({ workspaceId, dryRun: claimed.dryRun, ...(claimed.cursorProductId ? { afterProductId: claimed.cursorProductId } : {}), ...(claimed.batchLimit ? { limit: claimed.batchLimit } : {}) })
          const conflictQueueFailure = canonicalBackfillConflictQueueFailure({ conflictCount: batch.conflicts.length, configured: Boolean(conflictRepository) })
          if (conflictQueueFailure) throw new DomainError(conflictQueueFailure.code, conflictQueueFailure.code === 'CANONICAL_BACKFILL_CONFLICT_REPOSITORY_UNAVAILABLE' ? 'canonical backfill 发现冲突但人工队列未配置，已阻断批次执行' : 'canonical backfill 冲突数量无效，已阻断批次执行', conflictQueueFailure.status, conflictQueueFailure.details)
          if (batch.conflicts.length) await conflictRepository!.enqueue({ workspaceId, runId: claimed.id, conflicts: batch.conflicts })
          const terminal = batch.conflicts.length > 0 ? 'failed' : batch.nextProductId ? 'running' : 'completed'
          const updated = await repository.update({ id: claimed.id, workspaceId, expectedRevision: claimed.revision, status: terminal, ...(batch.nextProductId ? { cursorProductId: batch.nextProductId } : {}), lastResult: { dryRun: batch.dryRun, creates: batch.creates.length, unchanged: batch.unchanged.length, conflicts: batch.conflicts, insertedIds: batch.insertedIds } })
          await recordOperationAudit({ workspaceId, actorId, action: `canonical.backfill.${terminal}`, resourceType: 'canonical_backfill_run', resourceId: updated.id, before: claimed as unknown as Record<string, unknown>, after: updated as unknown as Record<string, unknown>, reason: terminal === 'failed' ? '批次发现需人工处理的 canonical 冲突' : '执行一个有界 canonical backfill 批次' })
          return result({ run: updated, batch })
        } catch (error) {
          const failed = await repository.update({ id: claimed.id, workspaceId, expectedRevision: claimed.revision, status: 'failed', lastResult: { error: error instanceof Error ? error.message : String(error) } }).catch(() => undefined)
          if (failed) await recordOperationAudit({ workspaceId, actorId, action: 'canonical.backfill.failed', resourceType: 'canonical_backfill_run', resourceId: failed.id, before: claimed as unknown as Record<string, unknown>, after: failed as unknown as Record<string, unknown>, reason: 'canonical backfill 批次执行异常' })
          throw error
        }
      }
      const nextStatus = method === 'ops.canonical.backfill.pause' ? 'paused' : 'running'
      const allowed = method === 'ops.canonical.backfill.pause' ? ['planned', 'running'] : ['paused']
      if (!allowed.includes(current.status)) throw new DomainError('CANONICAL_BACKFILL_RUN_STATE_INVALID', `当前状态 ${current.status} 不允许执行${nextStatus === 'paused' ? '暂停' : '继续'}`, 409, { status: current.status, next_action: current.status === 'failed' ? '人工复核冲突后重新创建批次' : null })
      const updated = await repository.update({ id: current.id, workspaceId, expectedRevision: optionalNumberValue(params, 'expectedRevision', 'expected_revision') ?? 0, status: nextStatus })
      const reason = requiredStringValue(params, 'reason')
      await recordOperationAudit({ workspaceId, actorId, action: `canonical.backfill.${nextStatus}`, resourceType: 'canonical_backfill_run', resourceId: updated.id, before: current as unknown as Record<string, unknown>, after: updated as unknown as Record<string, unknown>, reason })
      return result(updated)
    }
    case 'ops.feature-flags.list':
    case 'ops.feature-flag.upsert':
    case 'ops.feature-flag.emergency.set':
    case 'ops.feature-flag.events':
    case 'ops.feature-flag.evaluate': {
      const repository = persistence.featureFlags
      if (!repository) throw new DomainError('FEATURE_FLAG_REPOSITORY_UNAVAILABLE', '功能开关仓储未配置', 503)
      const flags = new FeatureFlagsService(repository)
      if (method === 'ops.feature-flags.list') {
        const actor = featureFlagActor(req, ['support', 'platform_ops'])
        const request: FeatureFlagListRequest = { ...(optionalStringValue(params, 'environment') ? { environment: optionalStringValue(params, 'environment') } : {}), ...(optionalStringValue(params, 'query') ? { query: optionalStringValue(params, 'query') } : {}), ...(optionalStringValue(params, 'cursor') ? { cursor: optionalStringValue(params, 'cursor') } : {}), ...(optionalNumberValue(params, 'limit') !== undefined ? { limit: optionalNumberValue(params, 'limit') } : {}) }
        return result(await invokeOpsDomain(() => flags.list(actor, request)))
      }
      if (method === 'ops.feature-flag.upsert') {
        const actor = featureFlagActor(req, ['platform_ops'])
        const defaultValue = structuredValue(params, 'defaultValue', 'default_value_json')
        const targets = structuredValue(params, 'targets', 'targets_json')
        if (requiredStringValue(params, 'environment') === 'production' && featureFlagRequestsCanonicalRead({ key: requiredStringValue(params, 'key'), defaultValue: defaultValue as { value?: unknown } | undefined, targets: targets as Array<{ override?: { value?: unknown } }> | undefined })) {
          throw new DomainError('CANONICAL_CUTOVER_EVIDENCE_REQUIRED', '生产环境只有在正式 canonical cutover evidence 签署后才能打开标准链切读', 503, { required_evidence: 'canonical-cutover-evidence', current_mode: 'legacy_shadow' })
        }
        const request: FeatureFlagMutationRequest = { ...(optionalStringValue(params, 'id') ? { id: optionalStringValue(params, 'id') } : {}), key: requiredStringValue(params, 'key'), environment: requiredStringValue(params, 'environment'), description: requiredStringValue(params, 'description'), defaultValue: defaultValue as FeatureFlagMutationRequest['defaultValue'], enabled: aliasValue(params, 'enabled') === true || aliasValue(params, 'enabled') === 'true', targets: targets === undefined ? [] : targets as FeatureFlagMutationRequest['targets'], ...(optionalStringValue(params, 'validFrom', 'valid_from') ? { validFrom: optionalStringValue(params, 'validFrom', 'valid_from') } : {}), ...(optionalStringValue(params, 'validTo', 'valid_to') ? { validTo: optionalStringValue(params, 'validTo', 'valid_to') } : {}), ...(optionalNumberValue(params, 'expectedRevision', 'expected_revision') !== undefined ? { expectedRevision: optionalNumberValue(params, 'expectedRevision', 'expected_revision') } : {}), idempotencyKey: requiredStringValue(params, 'idempotencyKey', 'idempotency_key'), reason: requiredStringValue(params, 'reason') }
        return result(await invokeOpsDomain(() => flags.save(actor, request)))
      }
      if (method === 'ops.feature-flag.emergency.set') {
        const actor = featureFlagActor(req, ['platform_ops'])
        const request: FeatureFlagEmergencyRequest = { id: requiredStringValue(params, 'id'), disabled: aliasValue(params, 'disabled') === true || aliasValue(params, 'disabled') === 'true', expectedRevision: optionalNumberValue(params, 'expectedRevision', 'expected_revision') ?? 0, idempotencyKey: requiredStringValue(params, 'idempotencyKey', 'idempotency_key'), reason: requiredStringValue(params, 'reason') }
        return result(await invokeOpsDomain(() => flags.setEmergency(actor, request)))
      }
      if (method === 'ops.feature-flag.events') return result(await invokeOpsDomain(() => flags.events(featureFlagActor(req, ['support', 'platform_ops']), requiredStringValue(params, 'flagId', 'flag_id'), optionalNumberValue(params, 'limit'))))
      const evaluationWorkspaceId = resolveWorkspace(req, aliasValue(params, 'targetWorkspaceId', 'target_workspace_id'))
      const evaluation: FeatureFlagEvaluationContext = { flagKey: requiredStringValue(params, 'flagKey', 'flag_key'), environment: requiredStringValue(params, 'environment'), ...(optionalStringValue(params, 'identityId', 'identity_id') ? { identityId: optionalStringValue(params, 'identityId', 'identity_id') } : {}), workspaceId: evaluationWorkspaceId, ...(optionalStringValue(params, 'bucketSubject', 'bucket_subject') ? { bucketSubject: optionalStringValue(params, 'bucketSubject', 'bucket_subject') } : {}), ...(optionalStringValue(params, 'at') ? { at: optionalStringValue(params, 'at') } : {}) }
      return result(await invokeOpsDomain(() => flags.evaluate(featureFlagActor(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'finance', 'platform_ops']), evaluation)))
    }
    case 'ops.finance.search':
    case 'ops.finance.detail':
    case 'ops.finance.export': {
      const repository = persistence.financeSearch
      if (!repository) throw new DomainError('FINANCE_SEARCH_REPOSITORY_UNAVAILABLE', '跨租户财务检索仅在 PostgreSQL 持久化模式可用', 503)
      const finance = new FinanceSearchService(repository)
      const principal = financePrincipal(req)
      if (method === 'ops.finance.detail') return result(await invokeOpsDomain(() => finance.detail(principal, { workspaceId: requiredStringValue(params, 'workspaceId', 'target_workspace_id'), kind: requiredStringValue(params, 'kind') as FinanceRecordKind, id: requiredStringValue(params, 'recordId', 'record_id'), ...(optionalStringValue(params, 'expectedVersion', 'expected_version') ? { expectedVersion: optionalStringValue(params, 'expectedVersion', 'expected_version') } : {}), ...(optionalStringValue(params, 'snapshotAt', 'snapshot_at') ? { snapshotAt: optionalStringValue(params, 'snapshotAt', 'snapshot_at') } : {}) })))
      const query: Partial<FinanceSearchQuery> & { limit?: number } = {
        ...(stringArrayValue(params, 'workspaceIds', 'workspace_ids_json') ? { workspaceIds: stringArrayValue(params, 'workspaceIds', 'workspace_ids_json') } : {}),
        ...(stringArrayValue(params, 'kinds', 'kinds_json') ? { kinds: stringArrayValue(params, 'kinds', 'kinds_json') as FinanceRecordKind[] } : {}),
        ...(stringArrayValue(params, 'statuses', 'statuses_json') ? { statuses: stringArrayValue(params, 'statuses', 'statuses_json') } : {}),
        ...(optionalStringValue(params, 'text') ? { text: optionalStringValue(params, 'text') } : {}),
        ...(optionalStringValue(params, 'fromAt', 'from_at') ? { fromAt: optionalStringValue(params, 'fromAt', 'from_at') } : {}),
        ...(optionalStringValue(params, 'toAt', 'to_at') ? { toAt: optionalStringValue(params, 'toAt', 'to_at') } : {}),
        ...(optionalStringValue(params, 'cursor') ? { cursor: optionalStringValue(params, 'cursor') } : {}),
        ...(optionalStringValue(params, 'snapshotAt', 'snapshot_at') ? { snapshotAt: optionalStringValue(params, 'snapshotAt', 'snapshot_at') } : {}),
        limit: optionalNumberValue(params, 'limit') ?? 50,
      }
      if (method === 'ops.finance.search') return result(await invokeOpsDomain(() => finance.search(principal, query)))
      return result(await invokeOpsDomain(() => finance.exportCsv(principal, query)))
    }
    case 'ops.audit.platform.list': {
      const repository = persistence.auditCenter
      if (!repository || !persistence.listWorkspaceIds) throw new DomainError('AUDIT_CENTER_REPOSITORY_UNAVAILABLE', '平台审计聚合需要审计和工作区目录仓储', 503)
      requireOperationsRole(req, ['platform_ops'])
      const platformAudit = new AuditCenterService(repository)
      const workspaceIds = await persistence.listWorkspaceIds()
      return result(await invokeOpsDomain(() => platformAudit.listPlatform(auditCenterPrincipal(req, workspaceId), params, workspaceIds)))
    }
    case 'ops.audit.list':
    case 'ops.audit.export': {
      const repository = persistence.auditCenter
      if (!repository) throw new DomainError('AUDIT_CENTER_REPOSITORY_UNAVAILABLE', '审计中心仓储未配置', 503)
      const service = new AuditCenterService(repository)
      const sources = stringArrayValue(params, 'sources', 'sources_json')
      const query = {
        workspaceId,
        ...(optionalStringValue(params, 'text') ? { text: optionalStringValue(params, 'text') } : {}),
        ...(sources ? { sources: sources as AuditSource[] } : {}),
        ...(optionalStringValue(params, 'actorId', 'actor_id') ? { actorId: optionalStringValue(params, 'actorId', 'actor_id') } : {}),
        ...(optionalStringValue(params, 'action') ? { action: optionalStringValue(params, 'action') } : {}),
        ...(optionalStringValue(params, 'resourceType', 'resource_type') ? { resourceType: optionalStringValue(params, 'resourceType', 'resource_type') } : {}),
        ...(optionalStringValue(params, 'fromAt', 'from_at') ? { fromAt: optionalStringValue(params, 'fromAt', 'from_at') } : {}),
        ...(optionalStringValue(params, 'toAt', 'to_at') ? { toAt: optionalStringValue(params, 'toAt', 'to_at') } : {}),
        ...(optionalStringValue(params, 'cursor') ? { cursor: optionalStringValue(params, 'cursor') } : {}),
        limit: optionalNumberValue(params, 'limit') ?? 50,
      }
      const principal = auditCenterPrincipal(req, workspaceId)
      if (method === 'ops.audit.list') return result(await invokeOpsDomain(() => service.list(principal, query)))
      return result(await invokeOpsDomain(() => service.exportCsv(principal, query)))
    }
    case 'ops.audit.detail': {
      const repository = persistence.auditCenter
      if (!repository) throw new DomainError('AUDIT_CENTER_REPOSITORY_UNAVAILABLE', '审计中心仓储未配置', 503)
      const service = new AuditCenterService(repository)
      return result(await invokeOpsDomain(() => service.detail(auditCenterPrincipal(req, workspaceId), {
        workspaceId,
        source: requiredStringValue(params, 'source') as AuditSource,
        id: requiredStringValue(params, 'id', 'event_id'),
      })))
    }
    case 'ops.members.list': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops'])
      const members = await (persistence.members ?? memoryMembers).list(workspaceId)
      const platformOperator = isPlatformOperations(req)
      const authorization = effectiveAuthorizationProjection(requestPrincipals.get(req), workspaceId)
      const canManage = authorization.capabilities.includes('workspace.member.manage')
      const canAssignOwner = authorization.capabilities.includes('workspace.status.update')
      const actorId = requestActor(req)
      const activeOwnerCount = members.filter(item => item.role === 'workspace_owner' && item.status === 'active').length
      return result(members.map(member => {
        const protectedTarget = !platformOperator && (member.role === 'platform_ops' || (member.role === 'workspace_owner' && !canAssignOwner))
        const canChangeTarget = canManage && !protectedTarget
        const canDeactivateTarget = canChangeTarget && member.externalSubject !== actorId && !(member.role === 'workspace_owner' && member.status === 'active' && activeOwnerCount <= 1)
        return { ...member, governance: { protectedTarget, canChangeTarget, canDeactivateTarget, ...(protectedTarget ? { reasonCode: member.role === 'platform_ops' ? 'PLATFORM_ROLE_CHANGE_REQUIRES_PLATFORM_WORKBENCH' : 'WORKSPACE_OWNER_CHANGE_REQUIRES_OWNER_OR_PLATFORM' } : canDeactivateTarget ? {} : { reasonCode: member.externalSubject === actorId ? 'SELF_SUSPENSION_DENIED' : member.role === 'workspace_owner' && member.status === 'active' && activeOwnerCount <= 1 ? 'LAST_WORKSPACE_OWNER_REQUIRED' : undefined }) } }
      }))
    }
    case 'ops.session': {
      const principal = requestPrincipals.get(req)
      const actorId = principal?.actorId ?? requestActor(req, 'actor_demo')
      const authorization = effectiveAuthorizationProjection(principal, workspaceId)
      const assignableRoles = authorization.capabilities.includes('workspace.member.manage')
        ? [
            ...(['merchant_admin', 'operator', 'support', 'finance'] as const),
            ...(authorization.capabilities.includes('workspace.status.update') ? ['workspace_owner' as const] : []),
            ...(principal?.workbench === 'platform' ? ['platform_ops' as const] : []),
          ]
        : []
      const authorizationRevision = principal?.authorizationRevision ?? 0
      const contextVersion = `${AUTHZ_POLICY_VERSION}:${authorizationRevision}`
      const available = new Set<OpsWorkbench>(principal?.availableWorkbenches ?? [principal?.workbench ?? 'workspace'])
      if (principal?.workbench === 'workspace' && principal.memberStatus === 'active') available.add('workspace')
      if (principal?.workbench === 'platform' && workspaceId) {
        const member = (await (persistence.members ?? memoryMembers).list(workspaceId)).find(item => item.externalSubject === actorId && item.status === 'active')
        if (member) available.add('workspace')
      }
      return result({ schema_version: 2, actor_id: actorId, workspace_id: principal?.workbench === 'platform' ? null : workspaceId, workbench: principal?.workbench ?? 'workspace', available_workbenches: [...available].sort(), context_id: principal?.workbench === 'platform' ? 'platform:global' : `workspace:${workspaceId}`, context_version: contextVersion, authorization_revision: authorizationRevision, context: { id: principal?.workbench === 'platform' ? 'platform:global' : `workspace:${workspaceId}`, workbench: principal?.workbench ?? 'workspace', workspace_id: principal?.workbench === 'platform' ? null : workspaceId, access_mode: principal?.memberStatus === 'active' || principal?.workbench === 'platform' ? 'direct' : principal?.activeAuthorizationGrants?.length ? 'temporary_support' : 'direct', authorization_revision: authorizationRevision }, roles: authorization.roles, canonical_roles: authorization.canonicalRoles, capabilities: authorization.capabilities, assignable_roles: assignableRoles, denied_capabilities: principal?.explicitDeniedCapabilities ?? [], scopes: authorization.scopes, effective_permissions: authorization.atoms.map(atom => ({ capability: atom.capability, effect: atom.effect, scope: atom.scope, source: atom.source, source_id: atom.sourceId, obligations: atom.obligations, ...(atom.effectLimit ? { effect_limit: atom.effectLimit } : {}), ...(atom.expiresAt ? { expires_at: atom.expiresAt } : {}), ...(atom.revision ? { revision: atom.revision } : {}) })), temporary_grants: (principal?.activeAuthorizationGrants ?? []).map(grant => ({ id: grant.id, access_mode: grant.accessMode, workspace_id: grant.workspaceId, capabilities: grant.capabilities, resource_scope: grant.resourceScope, expires_at: grant.expiresAt, max_uses: grant.maxUses, use_count: grant.useCount, revision: grant.revision, authorization_revision: grant.authorizationRevision })), policy_version: AUTHZ_POLICY_VERSION, workspace_granted: principal?.workspaces.includes('*') || (workspaceId ? principal?.workspaces.includes(workspaceId) : false) || !requiresStrictAuth(), identity_id: principal?.identityId ?? null, session_id: principal?.sessionId ?? null, identity_status: principal?.identityStatus ?? null, risk_decision: principal?.riskDecision ?? null, mfa_verified: principal?.mfaVerified ?? false, session_expires_at: principal?.sessionExpiresAt ?? null })
    }
    case 'ops.authorization.matrix.get': {
      const roles = [...CANONICAL_ROLES]
      const items = MCP_METHODS.map(method => {
        const policy = MCP_METHOD_POLICIES[method]
        return {
          method,
          capability: policy.capability,
          workbench: policy.workbench,
          scope: policy.scope,
          data_class: policy.dataClass,
          effect: policy.effect,
          audit: policy.audit,
          obligations: [...policy.obligations],
          role_access: Object.fromEntries(roles.map(role => [role, canonicalRoleMethodAccess(role, policy)])),
        }
      })
      return result({ schema_version: 1, policy_version: AUTHZ_POLICY_VERSION, generated_from: 'MCP_METHOD_POLICIES', method_count: items.length, role_count: roles.length, roles, items })
    }
    case 'ops.authorization.roles.list': {
      const repository = authorizationRepository()
      if (!repository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '持久授权仓储未配置', 503)
      const subjectIdentityId = required(params, 'subject_identity_id')
      return result({ subject_identity_id: subjectIdentityId, authorization_revision: await repository.getAuthorizationRevision(subjectIdentityId), assignments: await repository.listActivePlatformRoles(subjectIdentityId) })
    }
    case 'ops.authorization.role.assign': {
      const repository = authorizationRepository()
      if (!repository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '持久授权仓储未配置', 503)
      const role = required(params, 'role')
      if (!PLATFORM_ASSIGNED_ROLES.includes(role as PlatformAssignedRole) || role === 'platform_owner') throw new DomainError('AUTHZ_PLATFORM_ROLE_UNSUPPORTED', '该平台角色不能通过日常运营入口分配', 400)
      return result(await repository.assignPlatformRole({ subjectIdentityId: required(params, 'subject_identity_id'), role: role as PlatformAssignedRole, assignedBy: requestActor(req), reason: required(params, 'reason'), expectedAuthorizationRevision: Number(required(params, 'expected_authorization_revision')), ...(typeof params.expires_at === 'string' && params.expires_at.trim() ? { expiresAt: params.expires_at.trim() } : {}) }))
    }
    case 'ops.authorization.role.revoke': {
      const repository = authorizationRepository()
      if (!repository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '持久授权仓储未配置', 503)
      return result(await repository.revokePlatformRole({ id: required(params, 'assignment_id'), subjectIdentityId: required(params, 'subject_identity_id'), actorId: requestActor(req), reason: required(params, 'reason'), expectedRevision: Number(required(params, 'expected_revision')), expectedAuthorizationRevision: Number(required(params, 'expected_authorization_revision')) }))
    }
    case 'ops.authorization.grants.list': {
      const repository = authorizationRepository()
      if (!repository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '持久授权仓储未配置', 503)
      const subjectIdentityId = required(params, 'subject_identity_id')
      const targetWorkspaceId = required(params, 'target_workspace_id')
      return result({ subject_identity_id: subjectIdentityId, workspace_id: targetWorkspaceId, authorization_revision: await repository.getAuthorizationRevision(subjectIdentityId), grants: await repository.listActiveGrants(subjectIdentityId, targetWorkspaceId) })
    }
    case 'ops.authorization.grant.issue': {
      const repository = authorizationRepository()
      if (!repository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '持久授权仓储未配置', 503)
      let capabilities: string[]
      let resourceScope: Record<string, unknown>
      try {
        const parsedCapabilities: unknown = JSON.parse(required(params, 'capabilities_json'))
        const parsedScope: unknown = JSON.parse(required(params, 'resource_scope_json'))
        if (!Array.isArray(parsedCapabilities) || parsedCapabilities.some(value => typeof value !== 'string' || !CAPABILITIES.includes(value as CapabilityId)) || !parsedScope || typeof parsedScope !== 'object' || Array.isArray(parsedScope)) throw new Error('invalid grant payload')
        capabilities = parsedCapabilities as string[]
        resourceScope = parsedScope as Record<string, unknown>
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'JIT capability 或 resource scope JSON 无效', 400) }
      const targetWorkspaceId = required(params, 'target_workspace_id')
      if (resourceScope.type !== 'workspace' || !Array.isArray(resourceScope.ids) || resourceScope.ids.length !== 1 || resourceScope.ids[0] !== targetWorkspaceId) throw new DomainError('AUTHORIZATION_GRANT_INVALID', 'JIT 必须精确绑定一个目标工作区', 400)
      const grantKind = required(params, 'grant_kind')
      const accessMode = required(params, 'access_mode')
      if ((grantKind !== 'temporary' && grantKind !== 'support') || (accessMode !== 'read' && accessMode !== 'write')) throw new DomainError('AUTHORIZATION_GRANT_INVALID', 'JIT 类型或读写模式无效', 400)
      return result(await repository.issueGrant({ grantKind, accessMode, subjectIdentityId: required(params, 'subject_identity_id'), workspaceId: targetWorkspaceId, capabilities, resourceScope, reason: required(params, 'reason'), ticketRef: required(params, 'ticket_ref'), issuedBy: requestActor(req), approvedBy: required(params, 'approved_by'), approvedAt: required(params, 'approved_at'), expectedAuthorizationRevision: Number(required(params, 'expected_authorization_revision')), expiresAt: required(params, 'expires_at'), maxUses: Number(required(params, 'max_uses')) }))
    }
    case 'ops.authorization.grant.revoke': {
      const repository = authorizationRepository()
      if (!repository) throw new DomainError('AUTHORIZATION_REPOSITORY_UNAVAILABLE', '持久授权仓储未配置', 503)
      return result(await repository.revokeGrant({ id: required(params, 'grant_id'), subjectIdentityId: required(params, 'subject_identity_id'), actorId: requestActor(req), reason: required(params, 'reason'), expectedRevision: Number(required(params, 'expected_revision')), expectedAuthorizationRevision: Number(required(params, 'expected_authorization_revision')) }))
    }
    case 'ops.workspaces.list': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'finance', 'platform_ops'])
      const hasDirectoryParams = ['query', 'status', 'subscription_status', 'offset', 'limit'].some(key => Object.prototype.hasOwnProperty.call(params, key))
      const query = typeof params.query === 'string' ? params.query.trim() : ''
      if (query.length > 200) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'query 最多 200 个字符', 400)
      const status = typeof params.status === 'string' && params.status.trim() ? params.status.trim() : undefined
      if (status && status !== 'active' && status !== 'disabled') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'status 必须是 active 或 disabled', 400)
      const subscriptionStatus = typeof params.subscription_status === 'string' && params.subscription_status.trim() ? params.subscription_status.trim() : undefined
      if (subscriptionStatus && subscriptionStatus.length > 64) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'subscription_status 最多 64 个字符', 400)
      const requestedLimit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Number(params.limit) : 20
      const offset = typeof params.offset === 'string' && /^\d+$/u.test(params.offset) ? Number(params.offset) : 0
      if (!Number.isSafeInteger(requestedLimit) || requestedLimit < 1 || requestedLimit > 100) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 到 100 的整数', 400)
      if (!Number.isSafeInteger(offset) || offset < 0 || offset > 1_000_000) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'offset 必须是 0 到 1000000 的整数', 400)
      if (isPlatformOperations(req) && hasDirectoryParams && persistence.listWorkspaceDirectory) return result(await persistence.listWorkspaceDirectory({ query: query || undefined, status: status as 'active' | 'disabled' | undefined, subscriptionStatus, offset, limit: requestedLimit }))
      if (isPlatformOperations(req) && persistence.listWorkspaceSummaries && !hasDirectoryParams) return result(await persistence.listWorkspaceSummaries())
      const principal = requestPrincipals.get(req)
      const granted = requiresStrictAuth() ? (principal?.workspaces.filter(id => id !== '*') ?? []) : [...knownWorkspaces]
      const platformWorkspaceIds = isPlatformOperations(req) && persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : []
      const platformScopeIds = isPlatformOperations(req) ? [...new Set([...platformWorkspaceIds, ...knownWorkspaces])] : []
      const workspaceIds = platformScopeIds.length ? platformScopeIds : granted.length ? [...new Set(granted)] : [workspaceId]
      // Each summary performs five repository reads. Keep only two summaries in
      // flight so a platform-wide directory cannot exhaust the shared SQL pool
      // while the rest of the Ops page is loading.
      const summaries = await mapWithConcurrency(workspaceIds, 2, async id => { const [status, settings, usage, subscription, members] = await Promise.all([getWorkspaceStatus(id), (persistence.commercial ?? memoryCommercial).getSettings(id), (persistence.usage ?? memoryUsage).get(id), (persistence.subscriptions ?? memorySubscriptions).get(id), (persistence.members ?? memoryMembers).list(id)]); return { workspaceId: id, status, planName: settings.planName, monthlyPriceCny: settings.monthlyPriceCny, usedTasks: usage.usedTasks, includedTasks: usage.includedTasks, subscriptionStatus: subscription.status, memberCount: members.length } })
      if (!hasDirectoryParams) return result(summaries)
      const filtered = summaries.filter(item => (!query || [item.workspaceId, item.planName].some(value => value.toLocaleLowerCase().includes(query.toLocaleLowerCase()))) && (!status || item.status === status) && (!subscriptionStatus || item.subscriptionStatus === subscriptionStatus))
      return result({ items: filtered.slice(offset, offset + requestedLimit), total: filtered.length, offset, limit: requestedLimit, hasMore: offset + requestedLimit < filtered.length })
    }
    case 'ops.stores.list': {
      requireOperationsRole(req, ['platform_ops'])
      if (params.platform_scope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台店铺目录必须显式声明 platform_scope=platform', 400)
      const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const stores = workspaceIds.flatMap(targetWorkspaceId => workspaceStoreDirectory(targetWorkspaceId).map(store => ({ ...store })))
      const groups = new Map<string, { platform: Platform; state: string; dataMode: string; readable: boolean; writeEnabled: boolean; count: number }>()
      for (const store of stores) {
        const key = [store.platform, store.state, store.dataMode, store.readable, store.writeEnabled].join(':')
        const current = groups.get(key)
        if (current) current.count += 1
        else groups.set(key, { platform: store.platform, state: store.state, dataMode: store.dataMode, readable: store.readable, writeEnabled: store.writeEnabled, count: 1 })
      }
      const items = [...groups.values()].map(group => ({ platform: group.platform, accountId: `platform-aggregate:${group.platform}:${group.state}:${group.dataMode}`, label: `${group.count} 个${PLATFORM_LABELS[group.platform]}店铺`, state: group.state, dataMode: group.dataMode, readable: group.readable, writeEnabled: group.writeEnabled, revision: 0, aggregate: true }))
      return result({ items, total: items.length, aggregate: true })
    }
    case 'ops.brand-units.summary': {
      requireOperationsRole(req, ['platform_ops'])
      if (params.platform_scope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台品牌汇总必须显式声明 platform_scope=platform', 400)
      const repository = persistence.brandUnits ?? memoryBrandUnits
      const summaries = await repository.listPlatformSummary()
      return result({ scope: 'platform', workspaceCount: persistence.listWorkspaceIds ? (await persistence.listWorkspaceIds()).length : summaries.length, brandCount: summaries.reduce((sum, row) => sum + row.brandCount, 0), boundStoreCount: summaries.reduce((sum, row) => sum + row.boundStoreCount, 0), unboundBrandCount: summaries.reduce((sum, row) => sum + row.unboundBrandCount, 0), canonicalProductCount: summaries.reduce((sum, row) => sum + row.canonicalProductCount, 0), listingCount: summaries.reduce((sum, row) => sum + row.listingCount, 0) })
    }
    case 'ops.tasks.summary': {
      requireOperationsRole(req, ['platform_ops'])
      if (params.platform_scope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台任务汇总必须显式声明 platform_scope=platform', 400)
      const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const summaries = await mapWithConcurrency(workspaceIds, 4, async targetWorkspaceId => {
        try {
          const tasks = service.listTasks(targetWorkspaceId)
          const byState = tasks.reduce((counts, task) => { counts[task.state] = (counts[task.state] ?? 0) + 1; return counts }, {} as Record<string, number>)
          const generationQueueCount = [...service.generationJobs.values()].filter(job => job.workspaceId === targetWorkspaceId && ['queued', 'running'].includes(job.state)).length
          const publishQueueCount = service.listPublishJobs(targetWorkspaceId).filter(job => ['queued', 'submitted', 'publishing'].includes(job.state)).length
          return { taskCount: tasks.length, byState, generationQueueCount, publishQueueCount, failed: false }
        } catch {
          return { taskCount: 0, byState: {}, generationQueueCount: 0, publishQueueCount: 0, failed: true }
        }
      })
      const byState = summaries.reduce((counts, summary) => { for (const [state, count] of Object.entries(summary.byState)) counts[state] = (counts[state] ?? 0) + count; return counts }, {} as Record<string, number>)
      return result({ scope: 'platform', workspaceCount: workspaceIds.length, failedWorkspaceCount: summaries.filter(summary => summary.failed).length, taskCount: summaries.reduce((sum, summary) => sum + summary.taskCount, 0), generationQueueCount: summaries.reduce((sum, summary) => sum + summary.generationQueueCount, 0), publishQueueCount: summaries.reduce((sum, summary) => sum + summary.publishQueueCount, 0), byState })
    }
    case 'ops.model-usage.summary': {
      requireOperationsRole(req, ['platform_ops'])
      if (params.platform_scope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台模型用量汇总必须显式声明 platform_scope=platform', 400)
      if (!persistence.modelUsage) throw new DomainError('MODEL_USAGE_LEDGER_NOT_CONFIGURED', '模型用量结算台账未配置', 503)
      const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const summaries = await mapWithConcurrency(workspaceIds, 4, async targetWorkspaceId => {
        try {
          const records = await persistence.modelUsage!.list(targetWorkspaceId, 1000)
          return { failed: false, records }
        } catch {
          return { failed: true, records: [] as Awaited<ReturnType<ModelUsageRepository['list']>> }
        }
      })
      const records = summaries.flatMap(summary => summary.records)
      const byModality = records.reduce((counts, row) => { counts[row.modality] = (counts[row.modality] ?? 0) + 1; return counts }, {} as Record<string, number>)
      const byModel = records.reduce((counts, row) => { counts[row.model] = (counts[row.model] ?? 0) + 1; return counts }, {} as Record<string, number>)
      const bySettlementStatus = records.reduce((counts, row) => { counts[row.settlementStatus] = (counts[row.settlementStatus] ?? 0) + 1; return counts }, {} as Record<string, number>)
      return result({ scope: 'platform', workspaceCount: workspaceIds.length, failedWorkspaceCount: summaries.filter(summary => summary.failed).length, recordCount: records.length, totalTokens: records.reduce((sum, row) => sum + (row.totalTokens ?? 0), 0), providerCostCny: Number(records.reduce((sum, row) => sum + (row.costCny ?? 0), 0).toFixed(6)), customerChargeCny: Number(records.reduce((sum, row) => sum + (row.customerChargeCny ?? 0), 0).toFixed(6)), unsettledRecordCount: records.filter(row => !['settled', 'waived'].includes(row.settlementStatus)).length, byModality, byModel, bySettlementStatus })
    }
    case 'ops.storage.reconciliation.list': {
      requireOperationsRole(req, ['platform_ops'])
      if (requiresStrictAuth() && params.platform_scope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '跨工作区存储对账必须显式声明 platform_scope=platform', 400)
      const statusStore = persistence.storageReconciliation
      if (!statusStore) throw new DomainError('STORAGE_RECONCILIATION_REPOSITORY_UNAVAILABLE', '存储对账仓储未配置', 503)
      const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [workspaceId]
      const reports = (await Promise.all(workspaceIds.map(id => statusStore.list(id)))).flat()
      return result(reports.map(report => ({ workspace_id: report.workspaceId, ...redactStorageReconciliation(report) })))
    }
    case 'ops.users.list': {
      requireOperationsRole(req, ['platform_ops'])
      const query = typeof params.query === 'string' ? params.query.trim().toLocaleLowerCase() : ''
      const status = typeof params.status === 'string' && params.status.trim() ? params.status.trim() : undefined
      if (status && !['invited', 'active', 'suspended'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'status 必须是 invited、active 或 suspended', 400)
      const targetWorkspaceId = typeof params.workspace_id === 'string' && params.workspace_id.trim() ? params.workspace_id.trim() : undefined
      const requestedLimit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Number(params.limit) : 20
      const offset = typeof params.offset === 'string' && /^\d+$/u.test(params.offset) ? Number(params.offset) : 0
      if (!Number.isInteger(requestedLimit) || requestedLimit < 1 || requestedLimit > 100) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 到 100 的整数', 400)
      if (!Number.isSafeInteger(offset) || offset < 0 || offset > 1_000_000) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'offset 必须是 0 到 1000000 的整数', 400)
      const allWorkspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const workspaceIds = targetWorkspaceId ? allWorkspaceIds.filter(id => id === targetWorkspaceId) : allWorkspaceIds
      const memberRepository = persistence.members ?? memoryMembers
      const memberRows = memberRepository.listMany
        ? await memberRepository.listMany(workspaceIds)
        : (await Promise.all(workspaceIds.map(id => memberRepository.list(id)))).flat()
      const filtered = memberRows.filter(member => (!status || member.status === status) && (!query || [member.externalSubject, member.displayName, member.workspaceId, member.role].some(value => value.toLocaleLowerCase().includes(query))))
        .sort((left, right) => new Date(right.updatedAt).getTime() - new Date(left.updatedAt).getTime() || left.externalSubject.localeCompare(right.externalSubject))
      const pageRows = filtered.slice(offset, offset + requestedLimit)
      const pageWorkspaceIds = [...new Set(pageRows.map(member => member.workspaceId))]
      const workspaceStatuses = new Map(await mapWithConcurrency(pageWorkspaceIds, 8, async id => [id, await getWorkspaceStatus(id)] as const))
      const commercialSummaries = await loadPlatformUserCommercialSummaries(pageWorkspaceIds)
      const items = pageRows.map(member => ({ ...member, workspaceStatus: workspaceStatuses.get(member.workspaceId) ?? 'active', commercial: commercialSummaries.get(member.workspaceId) }))
      return result({ items, total: filtered.length, identityCount: new Set(filtered.map(member => member.externalSubject)).size, workspaceCount: new Set(filtered.map(member => member.workspaceId)).size, offset, limit: requestedLimit, truncated: offset + requestedLimit < filtered.length })
    }
    case 'ops.users.export': {
      requireOperationsRole(req, ['platform_ops'])
      const query = typeof params.query === 'string' ? params.query.trim().toLocaleLowerCase() : ''
      const status = typeof params.status === 'string' && params.status.trim() ? params.status.trim() : undefined
      if (status && !['invited', 'active', 'suspended'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'status 必须是 invited、active 或 suspended', 400)
      const targetWorkspaceId = typeof params.workspace_id === 'string' && params.workspace_id.trim() ? params.workspace_id.trim() : undefined
      const requestedLimit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Number(params.limit) : 5000
      if (params.format !== undefined && params.format !== 'csv' && params.format !== 'json') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'format 必须是 csv 或 json', 400)
      const format = params.format === 'json' ? 'json' : 'csv'
      if (!Number.isInteger(requestedLimit) || requestedLimit < 1 || requestedLimit > 5000) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 到 5000 的整数', 400)
      const allWorkspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const workspaceIds = targetWorkspaceId ? allWorkspaceIds.filter(id => id === targetWorkspaceId) : allWorkspaceIds
      const memberRepository = persistence.members ?? memoryMembers
      const memberRows = memberRepository.listMany
        ? await memberRepository.listMany(workspaceIds)
        : (await Promise.all(workspaceIds.map(id => memberRepository.list(id)))).flat()
      // Filter and cap before enrichment. Platform test/prod history can contain
      // many workspaces; enriching every member would turn a narrow CSV export
      // into an unbounded fan-out across usage, subscription and wallet stores.
      const selectedMembers = memberRows
        .filter(member => (!status || member.status === status) && (!query || [member.externalSubject, member.displayName, member.workspaceId, member.role].some(value => value.toLocaleLowerCase().includes(query))))
        .sort((left, right) => new Date(right.updatedAt).getTime() - new Date(left.updatedAt).getTime() || left.externalSubject.localeCompare(right.externalSubject))
        .slice(0, requestedLimit)
      const selectedWorkspaceIds = [...new Set(selectedMembers.map(member => member.workspaceId))]
      const workspaceStatuses = new Map(await mapWithConcurrency(selectedWorkspaceIds, 8, async id => [id, await getWorkspaceStatus(id)] as const))
      const commercialSummaries = await loadPlatformUserCommercialSummaries(selectedWorkspaceIds)
      const filtered = selectedMembers.map(member => ({ ...member, workspaceStatus: workspaceStatuses.get(member.workspaceId) ?? 'active', commercial: commercialSummaries.get(member.workspaceId) }))
      const rows = filtered.map(member => ({ external_subject: member.externalSubject, display_name: member.displayName, workspace_id: member.workspaceId, role: member.role, status: member.status, workspace_status: member.workspaceStatus, plan_code: member.commercial?.planCode ?? null, plan_name: member.commercial?.planName ?? null, subscription_status: member.commercial?.subscriptionStatus ?? null, used_tasks: member.commercial?.usedTasks ?? null, included_tasks: member.commercial?.includedTasks ?? null, remaining_tasks: member.commercial?.remainingTasks ?? null, wallet_balance_cny: member.commercial?.walletBalanceCny ?? null, invited_by: member.invitedBy ?? null, created_at: member.createdAt, updated_at: member.updatedAt }))
      if (format === 'json') return result({ filename: `ops-users-${new Date().toISOString().slice(0, 10)}.json`, content: JSON.stringify(rows, null, 2), count: rows.length, truncated: rows.length === requestedLimit })
      const headers = ['external_subject', 'display_name', 'workspace_id', 'role', 'status', 'workspace_status', 'plan_code', 'plan_name', 'subscription_status', 'used_tasks', 'included_tasks', 'remaining_tasks', 'wallet_balance_cny', 'invited_by', 'created_at', 'updated_at']
      const content = [headers.join(','), ...rows.map(row => headers.map(header => csvCell(String(row[header as keyof typeof row] ?? ''))).join(','))].join('\n')
      return result({ filename: `ops-users-${new Date().toISOString().slice(0, 10)}.csv`, content, count: rows.length, truncated: rows.length === requestedLimit })
    }
    case 'ops.user.detail': {
      requireOperationsRole(req, ['platform_ops'])
      const requestedIdentityId = typeof params.identity_id === 'string' && params.identity_id.trim() ? params.identity_id.trim() : undefined
      const externalSubject = typeof params.external_subject === 'string' && params.external_subject.trim() ? params.external_subject.trim() : undefined
      if (!requestedIdentityId && !externalSubject) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'identity_id 或 external_subject 至少提供一个', 400)
      let identityDetail: IdentityOperationsDetail | undefined
      try {
        const repository = persistence.identities ?? memoryIdentities
        const identity = requestedIdentityId
          ? undefined
          : await repository.resolve({ issuer: typeof params.issuer === 'string' && params.issuer.trim() ? params.issuer.trim() : 'urn:merchant:api-token', externalSubject: externalSubject! })
        if (requestedIdentityId || identity) identityDetail = await repository.detailForOperations(requestedIdentityId ?? identity!.id)
      } catch (error) { mapIdentityLifecycleError(error) }
      const allWorkspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const memberRepository = persistence.members ?? memoryMembers
      const allMembers = memberRepository.listMany
        ? await memberRepository.listMany(allWorkspaceIds)
        : (await Promise.all(allWorkspaceIds.map(id => memberRepository.list(id)))).flat()
      const matchingMembers = allMembers.filter(member => requestedIdentityId ? member.identityId === requestedIdentityId : member.externalSubject === externalSubject)
      if (!matchingMembers.length && !identityDetail) throw new DomainError('USER_IDENTITY_NOT_FOUND', '未找到该平台身份或成员关系', 404)
      const workspaceStatuses = new Map(await Promise.all(matchingMembers.map(async member => [member.workspaceId, await getWorkspaceStatus(member.workspaceId)] as const)))
      const memberships = matchingMembers
        .map(member => ({ ...member, workspaceStatus: workspaceStatuses.get(member.workspaceId) ?? 'active' }))
        .sort((left, right) => new Date(right.updatedAt).getTime() - new Date(left.updatedAt).getTime())
      const auditRepository = persistence.operations ?? memoryOperations
      const audits = (await Promise.all([...new Set(matchingMembers.map(member => member.workspaceId))].map(id => auditRepository.list(id, 200))))
        .flat()
        .filter(audit => audit.resourceType === 'workspace_member' && audit.resourceId === (externalSubject ?? identityDetail?.identity.externalSubject))
        .sort((left, right) => right.createdAt.localeCompare(left.createdAt))
        .slice(0, 100)
      return result({
        identity: {
          ...(identityDetail?.identity ?? {}),
          externalSubject: identityDetail?.identity.externalSubject ?? externalSubject,
          displayName: identityDetail?.identity.displayName || memberships.find(member => member.displayName)?.displayName || '',
          membershipCount: memberships.length,
          activeMembershipCount: memberships.filter(member => member.status === 'active' && member.workspaceStatus === 'active').length,
          firstSeenAt: identityDetail?.identity.firstSeenAt ?? memberships[0]?.createdAt ?? null,
          lastUpdatedAt: identityDetail?.identity.updatedAt ?? memberships[0]?.updatedAt ?? null,
        },
        memberships,
        audits,
        sessions: identityDetail?.sessions.map(({ providerSessionHash: _providerSessionHash, ipHash: _ipHash, userAgentHash: _userAgentHash, ...session }) => session) ?? [],
        lifecycleEvents: identityDetail?.events.map(event => ({
          id: event.id,
          eventType: event.eventType,
          actorId: event.actorId,
          reason: redactAuditReason(event.reason),
          evidence: redactAuditEvidence(event.evidence),
          createdAt: event.createdAt,
        })) ?? [],
      })
    }
    case 'ops.commercial.offers.list':
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
      return result(await (persistence.commercialExtensions ?? memoryCommercialExtensions).listOffers())
    case 'ops.commercial.offer.upsert': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      const price = Number(required(params, 'price_cny'))
      const stores = Number(required(params, 'included_stores'))
      const tasks = Number(required(params, 'included_tasks'))
      const reason = required(params, 'reason').trim()
      const validFromInput = typeof params.valid_from === 'string' && params.valid_from.trim() ? params.valid_from.trim() : new Date().toISOString()
      const validToInput = typeof params.valid_to === 'string' && params.valid_to.trim() ? params.valid_to.trim() : undefined
      const validFromTime = Date.parse(validFromInput)
      const validToTime = validToInput ? Date.parse(validToInput) : undefined
      if (!Number.isFinite(price) || price < 0 || !/^\d+(?:\.\d{1,2})?$/u.test(required(params, 'price_cny')) || !Number.isInteger(stores) || stores < 0 || !Number.isInteger(tasks) || tasks < 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '套餐价格和额度参数无效', 400)
      if (!Number.isFinite(validFromTime) || (validToTime !== undefined && (!Number.isFinite(validToTime) || validToTime <= validFromTime))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '套餐生效时间无效，失效时间必须晚于生效时间', 400)
      let item
      try {
        item = await (persistence.commercialExtensions ?? memoryCommercialExtensions).upsertOffer({ code: required(params, 'code'), name: required(params, 'name'), billingCycle: required(params, 'billing_cycle') as 'monthly' | 'annual', priceCny: Number(price.toFixed(2)), includedStores: stores, includedTasks: tasks, active: params.active !== 'false', validFrom: new Date(validFromTime).toISOString(), ...(validToTime !== undefined ? { validTo: new Date(validToTime).toISOString() } : {}), updatedBy: actorId, expectedRevision: params.expected_revision ? Number(params.expected_revision) : undefined })
      } catch (error) {
        if (error instanceof Error && error.message === 'commercial offer revision conflict') throw new DomainError('COMMERCIAL_OFFER_REVISION_CONFLICT', '套餐已被其他运营人员更新，请刷新后重试', 409)
        throw error
      }
      await recordOperationAudit({ workspaceId, actorId, action: 'commercial.offer.upsert', resourceType: 'commercial_offer', resourceId: item.code, before: {}, after: item as unknown as Record<string, unknown>, reason })
      return result(item)
    }
    case 'ops.commercial.addons.list':
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
      return result(await (persistence.commercialExtensions ?? memoryCommercialExtensions).listAddons())
    case 'ops.commercial.addon.upsert': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      const price = Number(required(params, 'price_cny')); const units = Number(required(params, 'units'))
      if (!Number.isFinite(price) || price < 0 || !/^\d+(?:\.\d{1,2})?$/u.test(required(params, 'price_cny')) || !Number.isInteger(units) || units < 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '加购价格和数量参数无效', 400)
      const item = await (persistence.commercialExtensions ?? memoryCommercialExtensions).upsertAddon({ code: required(params, 'code'), name: required(params, 'name'), kind: required(params, 'kind') as 'platform' | 'image_generation' | 'bulk_sync', priceCny: Number(price.toFixed(2)), units, active: params.active !== 'false', updatedBy: actorId, expectedRevision: params.expected_revision ? Number(params.expected_revision) : undefined })
      await recordOperationAudit({ workspaceId, actorId, action: 'commercial.addon.upsert', resourceType: 'commercial_addon', resourceId: item.code, before: {}, after: item as unknown as Record<string, unknown>, reason: typeof params.reason === 'string' ? params.reason : '加购目录更新' }); return result(item)
    }
    case 'ops.commercial.coupons.list':
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
      return result(await (persistence.commercialExtensions ?? memoryCommercialExtensions).listCoupons())
    case 'ops.commercial.export': {
      requireOperationsRole(req, ['platform_ops'])
      if (params.format !== undefined && params.format !== 'csv' && params.format !== 'json') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'format 必须是 csv 或 json', 400)
      const extensions = persistence.commercialExtensions ?? memoryCommercialExtensions
      const [offers, addons, coupons, rollouts, markup] = await Promise.all([
        extensions.listOffers(),
        extensions.listAddons(),
        extensions.listCoupons(),
        extensions.listRollouts(),
        extensions.getModelMarkupPolicy(),
      ])
      const payload = { exportedAt: new Date().toISOString(), offers, addons, coupons, rollouts, modelMarkup: markup }
      if (params.format === 'json') return result({ filename: `ops-commercial-${new Date().toISOString().slice(0, 10)}.json`, content: JSON.stringify(payload, null, 2), counts: { offers: offers.length, addons: addons.length, coupons: coupons.length, rollouts: rollouts.length } })
      const headers = ['kind', 'id', 'code', 'name', 'billing_cycle', 'price_cny', 'included_stores', 'included_tasks', 'addon_kind', 'units', 'discount_type', 'discount_value', 'max_redemptions', 'redeemed_count', 'active', 'offer_code', 'workspace_id', 'percentage', 'enabled', 'reason', 'valid_from', 'valid_to', 'revision', 'updated_by', 'updated_at']
      const rows = [
        ...offers.map(item => ({ kind: 'offer', id: item.id, code: item.code, name: item.name, billing_cycle: item.billingCycle, price_cny: item.priceCny, included_stores: item.includedStores, included_tasks: item.includedTasks, active: item.active, valid_from: item.validFrom, valid_to: item.validTo ?? '', revision: item.revision, updated_by: item.updatedBy, updated_at: item.updatedAt })),
        ...addons.map(item => ({ kind: 'addon', id: item.id, code: item.code, name: item.name, price_cny: item.priceCny, addon_kind: item.kind, units: item.units, active: item.active, revision: item.revision, updated_by: item.updatedBy, updated_at: item.updatedAt })),
        ...coupons.map(item => ({ kind: 'coupon', id: item.id, code: item.code, discount_type: item.discountType, discount_value: item.discountValue, max_redemptions: item.maxRedemptions, redeemed_count: item.redeemedCount, active: item.active, valid_from: item.validFrom, valid_to: item.validTo ?? '', revision: item.revision, updated_by: item.updatedBy, updated_at: item.updatedAt })),
        ...rollouts.map(item => ({ kind: 'rollout', id: item.id, offer_code: item.offerCode, workspace_id: item.workspaceId ?? '', percentage: item.percentage, enabled: item.enabled, reason: item.reason, revision: item.revision, updated_by: item.updatedBy, updated_at: item.updatedAt })),
        { kind: 'model_markup', id: 'singleton', price_cny: markup.multiplier, reason: markup.reason, revision: markup.revision, updated_by: markup.updatedBy, updated_at: markup.updatedAt },
      ]
      const content = [headers.join(','), ...rows.map(row => headers.map(header => csvCell(String(row[header as keyof typeof row] ?? ''))).join(','))].join('\n')
      return result({ filename: `ops-commercial-${new Date().toISOString().slice(0, 10)}.csv`, content, counts: { offers: offers.length, addons: addons.length, coupons: coupons.length, rollouts: rollouts.length } })
    }
    case 'ops.commercial.coupon.upsert': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      const value = Number(required(params, 'discount_value')); const max = Number(required(params, 'max_redemptions')); const type = required(params, 'discount_type') as 'fixed_cny' | 'percent'
      if (!Number.isFinite(value) || value < 0 || !/^\d+(?:\.\d{1,2})?$/u.test(required(params, 'discount_value')) || !Number.isInteger(max) || max < 0 || (type === 'percent' && value > 100)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '优惠券折扣参数无效', 400)
      const item = await (persistence.commercialExtensions ?? memoryCommercialExtensions).upsertCoupon({ code: required(params, 'code'), discountType: type, discountValue: Number(value.toFixed(2)), maxRedemptions: max, active: params.active !== 'false', validFrom: typeof params.valid_from === 'string' ? params.valid_from : new Date().toISOString(), validTo: typeof params.valid_to === 'string' ? params.valid_to : undefined, updatedBy: actorId, expectedRevision: params.expected_revision ? Number(params.expected_revision) : undefined })
      await recordOperationAudit({ workspaceId, actorId, action: 'commercial.coupon.upsert', resourceType: 'commercial_coupon', resourceId: item.code, before: {}, after: item as unknown as Record<string, unknown>, reason: typeof params.reason === 'string' ? params.reason : '优惠券规则更新' }); return result(item)
    }
    case 'ops.commercial.rollouts.list': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const rollouts = await (persistence.commercialExtensions ?? memoryCommercialExtensions).listRollouts()
      return result(isPlatformOperations(req) ? rollouts : rollouts.filter(item => !item.workspaceId || item.workspaceId === workspaceId))
    }
    case 'ops.commercial.model-markup.get':
      requireOperationsRole(req, ['platform_ops'])
      return result(await (persistence.commercialExtensions ?? memoryCommercialExtensions).getModelMarkupPolicy())
    case 'ops.commercial.model-markup.update': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      const rawMultiplier = required(params, 'multiplier')
      const multiplier = Number(rawMultiplier)
      const expectedRevision = Number(required(params, 'expected_revision'))
      const reason = required(params, 'reason')
      if (!/^\d+(?:\.\d{1,3})?$/u.test(rawMultiplier) || !Number.isFinite(multiplier) || multiplier < 1 || multiplier > 10 || !Number.isInteger(expectedRevision) || expectedRevision < 1) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '模型计费倍率必须在 1.0 至 10.0 之间，最多 3 位小数', 400)
      const repository = persistence.commercialExtensions ?? memoryCommercialExtensions
      const before = await repository.getModelMarkupPolicy()
      let item
      try { item = await repository.updateModelMarkupPolicy({ multiplier, reason, updatedBy: actorId, expectedRevision }) } catch (error) { if (error instanceof Error && error.message.includes('revision conflict')) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '倍率配置已被其他运营人员更新，请刷新后重试', 409); throw error }
      await recordOperationAudit({ workspaceId, actorId, action: 'commercial.model_markup.update', resourceType: 'model_markup_policy', resourceId: 'global', before: before as unknown as Record<string, unknown>, after: item as unknown as Record<string, unknown>, reason })
      return result(item)
    }
    case 'ops.growth.funnel': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
      const platformScope = params.platform_scope === 'platform'
      if (params.platform_scope !== undefined && !platformScope) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform_scope 必须是 platform', 400)
      if (platformScope && !isPlatformOperations(req)) throw new DomainError(ERROR_CODES.FORBIDDEN, '只有平台运营角色可以读取平台增长聚合', 403)
      const sourceChannel = typeof params.source_channel === 'string' ? params.source_channel : undefined
      const from = typeof params.date_from === 'string' ? params.date_from : undefined
      const to = typeof params.date_to === 'string' ? params.date_to : undefined
      if (!platformScope) return result(await (persistence.growth ?? memoryGrowth).funnel({ workspaceId, ...(sourceChannel ? { sourceChannel } : {}), ...(from ? { from } : {}), ...(to ? { to } : {}) }))
      const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const pages = await mapWithConcurrency(workspaceIds, 4, targetWorkspaceId => (persistence.growth ?? memoryGrowth).funnel({ workspaceId: targetWorkspaceId, ...(sourceChannel ? { sourceChannel } : {}), ...(from ? { from } : {}), ...(to ? { to } : {}) }))
      const counts = pages.reduce((aggregate, page) => { for (const [event, count] of Object.entries(page.counts)) aggregate[event] = (aggregate[event] ?? 0) + count; return aggregate }, {} as Record<string, number>)
      return result({ scope: 'platform', workspaceCount: pages.length, ...(sourceChannel ? { sourceChannel } : {}), ...(from ? { from } : {}), ...(to ? { to } : {}), counts, totalEvents: pages.reduce((sum, page) => sum + page.totalEvents, 0) })
    }
    case 'ops.alerts.list': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops'])
      const platformScope = params.platform_scope === 'platform'
      if (params.platform_scope !== undefined && !platformScope) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform_scope 必须是 platform', 400)
      const isPlatformAggregate = platformScope && isPlatformOperations(req)
      if (platformScope && !isPlatformAggregate) throw new DomainError(ERROR_CODES.FORBIDDEN, '只有平台运营角色可以读取平台告警聚合', 403)
      if (params.status !== undefined && params.status !== 'open' && params.status !== 'acknowledged') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'status 必须是 open 或 acknowledged', 400)
      const status = params.status === 'open' || params.status === 'acknowledged' ? params.status : undefined
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Number(params.limit) : 100
      const platform = typeof params.platform === 'string' && params.platform.trim() ? params.platform.trim() : undefined
      if (platform && !SUPPORTED_PLATFORMS.includes(platform as Platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 筛选值无效', 400)
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      const code = typeof params.code === 'string' && params.code.trim() ? params.code.trim() : undefined
      const entityType = typeof params.entity_type === 'string' && params.entity_type.trim() ? params.entity_type.trim() : undefined
      const entityId = typeof params.entity_id === 'string' && params.entity_id.trim() ? params.entity_id.trim() : undefined
      if (isPlatformAggregate && (accountId || entityType || entityId)) throw new DomainError('OPS_CUSTOMER_ACCESS_REQUIRED', '平台告警聚合不接受店铺或客户对象筛选；请切换到明确工作区授权会话', 403)
      const targetWorkspaceIds = isPlatformAggregate && persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [workspaceId]
      const alerts: OperationalAlert[] = []
      for (let offset = 0; offset < targetWorkspaceIds.length; offset += 8) {
        const batch = await Promise.all(targetWorkspaceIds.slice(offset, offset + 8).map(async targetWorkspaceId => {
          let repository: OperationalAlertsRepository
          try {
            repository = await syncOperationalAlerts(targetWorkspaceId)
          } catch (error) {
            // A malformed legacy task must not take down the platform-wide
            // alert read. The persisted alert stream remains queryable and
            // the next reconciliation can repair the source record.
            if (!isPlatformAggregate) throw error
            repository = persistence.alerts ?? memoryAlerts
          }
          return repository.list(targetWorkspaceId, status, 500)
        }))
        alerts.push(...batch.flat())
      }
      const filtered = alerts.filter(alert => (!platform || alert.platform === platform) && (!accountId || alert.accountId === accountId) && (!code || alert.code === code) && (!entityType || alert.entityType === entityType) && (!entityId || alert.entityId === entityId)).sort((left, right) => String(right.observedAt).localeCompare(String(left.observedAt)) || right.id.localeCompare(left.id))
      if (!isPlatformAggregate) return result(filtered.slice(0, Math.min(500, Math.max(1, limit))))
      const grouped = new Map<string, { code: string; severity: OperationalAlert['severity']; platform?: string; status: OperationalAlert['status']; delivery: string; count: number; latestObservedAt: string }>()
      for (const alert of filtered) {
        const delivery = alert.notification?.delivery ?? 'unrecorded'
        const key = [alert.code, alert.severity, alert.platform ?? 'global', alert.status, delivery].join(':')
        // node-postgres materializes timestamptz as Date even though the
        // repository contract exposes ISO strings. Normalize at this API
        // boundary so platform aggregation remains deterministic.
        const rawObservedAt: unknown = alert.observedAt
        const observedAt = rawObservedAt instanceof Date ? rawObservedAt.toISOString() : String(rawObservedAt)
        const current = grouped.get(key)
        if (current) { current.count += 1; if (observedAt > current.latestObservedAt) current.latestObservedAt = observedAt }
        else grouped.set(key, { code: alert.code, severity: alert.severity, ...(alert.platform ? { platform: alert.platform } : {}), status: alert.status, delivery, count: 1, latestObservedAt: observedAt })
      }
      const items = [...grouped.values()].sort((left, right) => right.latestObservedAt.localeCompare(left.latestObservedAt) || left.code.localeCompare(right.code)).map((group, index) => ({
        id: `platform-alert-group-${index + 1}`,
        code: group.code,
        severity: group.severity,
        ...(group.platform ? { platform: group.platform } : {}),
        entityType: 'platform_alert_group',
        entityId: 'redacted',
        title: `${group.count} 条平台告警（${group.code}）`,
        status: group.status,
        observedAt: group.latestObservedAt,
        evidence: { scope: 'platform', count: group.count, notification_delivery: group.delivery },
        nextAction: '切换到明确工作区授权会话后查看客户告警明细。',
      }))
      return result({ items: items.slice(0, Math.min(500, Math.max(1, limit))), aggregate: true, truncated: items.length > limit })
    }
    case 'ops.alert.ack': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops'])
      const alert = await (persistence.alerts ?? memoryAlerts).acknowledge({ workspaceId, id: required(params, 'alert_id'), actorId, reason: required(params, 'reason') })
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.alert.ack', resourceType: 'operational_alert', resourceId: alert.id, before: { status: 'open' }, after: alert as unknown as Record<string, unknown>, reason: required(params, 'reason') })
      return result(alert)
    }
    case 'ops.marketing.summary': {
      requireOperationsRole(req, ['platform_ops'])
      if (params.platform_scope !== 'platform') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台营销汇总必须显式声明 platform_scope=platform', 400)
      const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const summaries = await mapWithConcurrency(workspaceIds, 4, async targetWorkspaceId => {
        try {
          const tasks = service.listTasks(targetWorkspaceId)
          const taskById = new Map(tasks.map(task => [task.id, task]))
          const generationByState = [...service.generationJobs.values()]
            .filter(job => job.workspaceId === targetWorkspaceId && taskById.has(job.taskId))
            .reduce((counts, job) => { counts[job.state] = (counts[job.state] ?? 0) + 1; return counts }, {} as Record<string, number>)
          const publishByState = service.listPublishJobs(targetWorkspaceId)
            .filter(job => taskById.has(job.taskId))
            .reduce((counts, job) => { const state = job.remoteState ?? job.state; counts[state] = (counts[state] ?? 0) + 1; return counts }, {} as Record<string, number>)
          const visualReviewCount = [...service.imageGenerationJobs.values()]
            .filter(job => job.workspaceId === targetWorkspaceId && job.archiveState === 'archived' && job.outputs?.some(output => output.reviewStatus !== 'passed'))
            .reduce((count, job) => count + (job.outputs ?? []).filter(output => output.reviewStatus !== 'passed').length, 0)
          const assetRiskCount = service.listAssets(targetWorkspaceId).filter(asset => asset.readiness.status !== 'ready').length
          const learningSuggestionCount = knowledgeForWorkspace(targetWorkspaceId).listLearningSuggestions(targetWorkspaceId, 'pending').length
          return { failed: false, taskCount: tasks.length, generationByState, publishByState, visualReviewCount, assetRiskCount, learningSuggestionCount }
        } catch {
          return { failed: true, taskCount: 0, generationByState: {}, publishByState: {}, visualReviewCount: 0, assetRiskCount: 0, learningSuggestionCount: 0 }
        }
      })
      const sumCounts = (key: 'generationByState' | 'publishByState') => summaries.reduce((counts, summary) => {
        for (const [state, count] of Object.entries(summary[key])) counts[state] = (counts[state] ?? 0) + count
        return counts
      }, {} as Record<string, number>)
      return result({
        scope: 'platform',
        workspaceCount: workspaceIds.length,
        failedWorkspaceCount: summaries.filter(summary => summary.failed).length,
        taskCount: summaries.reduce((sum, summary) => sum + summary.taskCount, 0),
        generationByState: sumCounts('generationByState'),
        publishByState: sumCounts('publishByState'),
        visualReviewCount: summaries.reduce((sum, summary) => sum + summary.visualReviewCount, 0),
        assetRiskCount: summaries.reduce((sum, summary) => sum + summary.assetRiskCount, 0),
        learningSuggestionCount: summaries.reduce((sum, summary) => sum + summary.learningSuggestionCount, 0),
      })
    }
    case 'ops.marketing.queue': {
      requireWorkspaceDataRole(req)
      const visibleBrandIds = await accessibleTaskBrandIds(req, workspaceId)
      const visibleProductIds = visibleBrandIds === undefined
        ? undefined
        : new Set((await (persistence.brandUnits ?? memoryBrandUnits).listCanonicalProducts({ workspaceId, brandIds: visibleBrandIds })).flatMap(product => product.sourceProductId ? [product.sourceProductId] : []))
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(100, Math.max(1, Number(params.limit))) : 50
      const filterPlatform = typeof params.platform === 'string' && params.platform.trim() ? params.platform.trim() as Platform : undefined
      if (filterPlatform && !SUPPORTED_PLATFORMS.includes(filterPlatform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 筛选值无效', 400)
      const filterAccountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      const filterProductId = typeof params.product_id === 'string' && params.product_id.trim() ? params.product_id.trim() : undefined
      const filterTaskId = typeof params.task_id === 'string' && params.task_id.trim() ? params.task_id.trim() : undefined
      const filterState = typeof params.state === 'string' && params.state.trim() ? params.state.trim() : undefined
      const taskForQueue = (taskId: string) => {
        try {
          const task = service.getTask(taskId)
          return task.workspaceId === workspaceId && (visibleBrandIds === undefined ? true : Boolean(task.brandId && visibleBrandIds.includes(task.brandId))) ? task : undefined
        } catch { return undefined }
      }
      const matchesTask = (taskId: string, platform?: Platform, accountId?: string, productId?: string, state?: string) => {
        if (filterTaskId && taskId !== filterTaskId) return false
        if (filterPlatform && platform !== filterPlatform) return false
        if (filterAccountId && accountId !== filterAccountId) return false
        if (filterProductId && productId !== filterProductId) return false
        if (filterState && state !== filterState) return false
        return true
      }
      const generation = [...service.generationJobs.values()].filter(job => {
        if (job.workspaceId !== workspaceId) return false
        const task = taskForQueue(job.taskId)
        return Boolean(task && matchesTask(job.taskId, task.platform, task.accountId, task.productId, job.state))
      }).sort((left, right) => right.updatedAt.localeCompare(left.updatedAt)).slice(0, limit).map(job => ({ id: job.id, taskId: job.taskId, state: job.state, attempt: job.attempt, contentVersionId: job.contentVersionId ?? null, errorCode: job.errorCode ?? null, errorMessage: job.errorMessage ?? null, waitingReason: job.waitingReason ?? null, assignedOperatorId: job.assignedOperatorId ?? null, assignedAt: job.assignedAt ?? null, revision: job.revision, createdAt: job.createdAt, updatedAt: job.updatedAt }))
      const publish = service.listPublishJobs(workspaceId).filter(job => {
        const task = taskForQueue(job.taskId)
        return Boolean(task && matchesTask(job.taskId, job.platform, job.accountId, task.productId, job.remoteState ?? job.state))
      }).sort((left, right) => right.createdAt.localeCompare(left.createdAt)).slice(0, limit).map(job => ({ id: job.id, taskId: job.taskId, contentVersionId: job.contentVersionId, platform: job.platform, accountId: job.accountId ?? null, state: job.state, remoteState: job.remoteState ?? null, remoteId: job.remoteId ?? null, requestId: job.requestId ?? null, rejection: job.rejection ? { rawCode: job.rejection.rawCode, message: job.rejection.message, fields: job.rejection.fields.map(field => ({ path: field.path, rawCode: field.rawCode ?? null, message: field.message })) } : null, operatorAcknowledgement: job.operatorAcknowledgement ?? null, assignedOperatorId: job.assignedOperatorId ?? null, assignedAt: job.assignedAt ?? null, revision: job.revision, createdAt: job.createdAt, remoteObservedAt: job.remoteObservedAt ?? null }))
      const batches = (await Promise.all([...publishBatches.values()].filter(batch => batch.workspaceId === workspaceId && (!filterPlatform && !filterAccountId && !filterProductId && !filterTaskId && !filterState || batch.items.some(item => {
        const task = taskForQueue(item.taskId)
        return Boolean(task && matchesTask(item.taskId, task.platform, task.accountId, task.productId, item.state))
      }))).map(async batch => {
        await refreshPublishBatch(batch)
        return { id: batch.id, state: batch.state, itemCount: batch.items.length, queuedCount: batch.items.filter(item => ['queued', 'submitted'].includes(item.state)).length, failedCount: batch.items.filter(item => ['failed', 'rejected', 'unknown'].includes(item.state)).length, pauseReason: batch.pauseReason ?? null, updatedAt: batch.updatedAt }
      }))).sort((left, right) => right.updatedAt.localeCompare(left.updatedAt)).slice(0, limit)
      const visuals = [...service.imageGenerationJobs.values()].filter(job => {
        if (job.workspaceId !== workspaceId || (visibleProductIds !== undefined && !visibleProductIds.has(job.productId)) || job.archiveState !== 'archived' || !job.outputs?.some(output => output.reviewStatus !== 'passed')) return false
        const task = job.taskId ? taskForQueue(job.taskId) : undefined
        const product = !task ? service.listProducts(workspaceId).find(item => item.id === job.productId) : undefined
        return matchesTask(job.taskId ?? '', task?.platform ?? product?.platform, task?.accountId ?? product?.accountId, job.productId, 'visual_review')
      }).flatMap(job => (job.outputs ?? []).filter(output => output.reviewStatus !== 'passed').map(output => ({ jobId: job.id, visualRef: output.visualRef, ordinal: output.ordinal, productId: job.productId, taskId: job.taskId ?? null, contentVersionId: job.contentVersionId ?? null, skuIds: job.skuIds ?? [], reviewStatus: output.reviewStatus, archiveState: job.archiveState, assignedOperatorId: job.assignedOperatorId ?? null, assignedAt: job.assignedAt ?? null, revision: job.revision, updatedAt: job.updatedAt }))).slice(0, limit)
      const imageExecutions = persistence.imageGenerationExecutions
        ? (await Promise.all((await persistence.imageGenerationExecutions.list({ workspaceId, states: ['provider_reserved', 'provider_dispatching', 'provider_started', 'outcome_unknown'], limit })).map(async execution => {
          const job = service.getImageGenerationJob(workspaceId, execution.jobId)
          const task = job.taskId ? taskForQueue(job.taskId) : undefined
          const product = service.listProducts(workspaceId).find(item => item.id === job.productId)
          if (visibleProductIds !== undefined && !visibleProductIds.has(job.productId)) return []
          if (!matchesTask(job.taskId ?? '', task?.platform ?? product?.platform, task?.accountId ?? product?.accountId, job.productId, execution.state)) return []
          const reconciliation = await persistence.reconciliationStatuses?.getLatest({ workspaceId, resourceType: 'image_generation_execution', resourceId: job.id })
          const lastAction = typeof reconciliation?.details.resolution === 'string'
            ? `人工收口：${reconciliation.details.resolution}`
            : execution.state === 'outcome_unknown'
              ? 'Provider 结果未知，等待对账'
              : execution.state === 'provider_reserved'
                ? '生成请求已登记，等待提交'
                : execution.state === 'provider_dispatching'
                  ? '正在提交模型请求，等待受理确认'
                  : 'Provider 已启动，等待结果'
          const alertState = Date.now() - Date.parse(execution.updatedAt) >= 15 * 60 * 1000 ? 'open' : 'monitoring'
          return [{ jobId: job.id, taskId: job.taskId ?? null, productId: job.productId, state: execution.state, archiveState: job.archiveState, eventId: execution.eventId, attempt: execution.attempt, providerRequestId: execution.providerRequestId ?? null, errorCode: execution.errorCode ?? null, errorMessage: execution.errorMessage ?? null, assignedOperatorId: job.assignedOperatorId ?? null, assignedAt: job.assignedAt ?? null, revision: job.revision, updatedAt: execution.updatedAt, reconciliationStatus: reconciliation?.status ?? null, reconciliationRevision: reconciliation?.revision ?? null, reconciliationEvidenceRef: typeof reconciliation?.details.evidenceRef === 'string' ? reconciliation.details.evidenceRef : null, reconciliationReason: typeof reconciliation?.details.reason === 'string' ? reconciliation.details.reason : null, alertState, lastAction, closureEvidence: reconciliation?.details.evidenceRef ?? null, nextAction: '查询真实 provider 状态或人工确认；禁止自动重试' }]
        }))).flat()
        : []
      const visibleAssets = service.listAssets(workspaceId).filter(asset => asset.readiness.status !== 'ready' && !filterProductId && !filterTaskId && !filterAccountId && (!filterPlatform || !asset.applicablePlatforms?.length || asset.applicablePlatforms.includes(filterPlatform))).slice(0, limit)
      const durableScanFailures = persistence.assetScanRedrive && visibleAssets.length
        ? await persistence.assetScanRedrive.listRetryableFailures(workspaceId, { assetIds: visibleAssets.map(asset => asset.id), limit, scanMaxAttempts: configuredAssetScanMaxAttempts() })
        : []
      const scanFailureByAssetId = new Map(durableScanFailures.map(failure => [failure.assetId, failure]))
      const uploadedAssetRisks = visibleAssets.map(asset => {
        const durableFailure = scanFailureByAssetId.get(asset.id)
        const scanFailure = durableFailure ? {
          assetId: durableFailure.assetId,
          eventId: durableFailure.event.id,
          errorCode: durableFailure.failure.code,
          errorMessage: durableFailure.failure.message,
          // This is the server-side recovery classification. Security,
          // content-integrity and binding failures remain visible but false.
          retryable: durableFailure.failure.retryable,
          assetRevision: durableFailure.assetRevision,
          sourceRevision: durableFailure.sourceRevision,
          failedAt: durableFailure.event.publishedAt ?? durableFailure.event.createdAt,
        } : null
        const base = { id: asset.id, name: asset.name, mimeType: asset.mimeType, scanStatus: asset.scanStatus, parseStatus: asset.parseStatus, rightsStatus: asset.rightsStatus, rightsScope: asset.rightsScope ?? null, readiness: asset.readiness, revision: asset.revision, createdAt: asset.createdAt, scanFailure }
        if (asset.scanStatus === 'quarantined') return { ...base, nextAction: null, scanAutomation: conversationalAssetScanWaitingState(), nextStep: conversationalAssetScanWaitingState().message }
        if (asset.scanStatus === 'blocked') return { ...base, nextAction: null, nextStep: '联系安全审核并重新上传或解除安全阻断' }
        if (asset.parseStatus === 'failed') return { ...base, nextAction: { method: 'asset.facts.confirm', label: '人工确认素材事实', requiredInputs: ['asset_id', 'facts_json', 'reason'] } }
        if (asset.parseStatus !== 'succeeded') return { ...base, nextAction: { method: 'asset.parse', label: '解析素材事实', requiredInputs: ['asset_id'] } }
        if (asset.rightsStatus !== 'approved' || asset.rightsScope === 'unusable') return { ...base, nextAction: { method: 'asset.rights.update', label: '确认素材商用权益', requiredInputs: ['asset_id', 'rights_status'] } }
        return { ...base, nextAction: { method: 'asset.facts.confirm', label: '确认素材事实', requiredInputs: ['asset_id', 'facts_json', 'reason'] } }
      })
      return result({ generatedAt: new Date().toISOString(), filters: { platform: filterPlatform ?? null, accountId: filterAccountId ?? null, productId: filterProductId ?? null, taskId: filterTaskId ?? null, state: filterState ?? null }, generation, publish, batches, visuals, imageExecutions, learningSuggestions: knowledgeForWorkspace(workspaceId).listLearningSuggestions(workspaceId, 'pending'), assetRisks: knowledgeForWorkspace(workspaceId).queryAssets({ workspaceId }).filter(asset => asset.approvalStatus !== 'approved' || asset.rightsStatus !== 'cleared').map(asset => ({ id: asset.id, kind: asset.kind, name: asset.name, approvalStatus: asset.approvalStatus, rightsStatus: asset.rightsStatus, source: asset.source ?? null, revision: asset.revision, updatedAt: asset.updatedAt })), uploadedAssetRisks })
    }
    case 'ops.marketing.queue.assign': {
      const actorId = requireWorkspaceDataRole(req)
      const itemType = required(params, 'item_type')
      if (itemType !== 'generation' && itemType !== 'publish' && itemType !== 'image') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'item_type 必须是 generation、publish 或 image', 400)
      const expectedRevision = typeof params.expected_revision === 'string' && /^\d+$/u.test(params.expected_revision) ? Number(params.expected_revision) : undefined
      if (params.expected_revision !== undefined && expectedRevision === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_revision 必须是非负整数', 400)
      const assigned = service.assignMarketingQueueItem({ workspaceId, itemType, itemId: required(params, 'item_id'), operatorId: required(params, 'operator_id'), ...(expectedRevision !== undefined ? { expectedRevision } : {}) })
      await persistSnapshot(workspaceId, itemType === 'generation' ? 'generation_job' : itemType === 'publish' ? 'publish_job' : 'image_generation_job', assigned, assigned as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, assigned.id, 'marketing.queue_assigned', assigned.revision, { item_type: itemType, item_id: assigned.id, operator_id: assigned.assignedOperatorId, assigned_at: assigned.assignedAt })
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.queue.assign', resourceType: `${itemType}_job`, resourceId: assigned.id, before: {}, after: { assignedOperatorId: assigned.assignedOperatorId, assignedAt: assigned.assignedAt, revision: assigned.revision }, reason: required(params, 'reason') })
      return result(assigned)
    }
    case 'ops.marketing.image.reconcile': {
      const actorId = requireWorkspaceDataRole(req)
      const jobId = required(params, 'job_id')
      const resolution = params.resolution === 'completed' || params.resolution === 'failed' ? params.resolution : undefined
      if (!resolution) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'resolution 必须是 completed 或 failed', 400)
      const evidenceRef = required(params, 'evidence_ref')
      const reason = required(params, 'reason')
      const idempotencyKey = required(params, 'idempotency_key')
      const expectedRevision = params.expected_revision === undefined ? undefined : optionalNumberValue(params, 'expectedRevision', 'expected_revision')
      const repository = persistence.imageGenerationExecutions
      const statusRepository = persistence.reconciliationStatuses
      if (!repository || !statusRepository) throw new DomainError('IMAGE_GENERATION_DURABLE_NOT_CONFIGURED', '图片生成对账持久化未配置', 503)
      const job = service.getImageGenerationJob(workspaceId, jobId)
      const currentStatus = await statusRepository.getLatest({ workspaceId, resourceType: 'image_generation_execution', resourceId: jobId })
      if (currentStatus?.lastIdempotencyKey === idempotencyKey) return result(currentStatus)
      const execution = await repository.get({ workspaceId, jobId })
      if (execution?.state === 'failed' && resolution === 'failed' && job.state === 'failed' && job.errorCode === 'IMAGE_GENERATION_MANUAL_FAILED' && job.errorMessage === reason) {
        const status = await statusRepository.upsert({ workspaceId, resourceType: 'image_generation_execution', resourceId: jobId, status: 'failed', idempotencyKey, details: { resolution, evidenceRef, reason, executionState: execution.state, settledState: execution.state, recoveredProjection: true }, observedAt: new Date().toISOString() })
        await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.image.reconcile.recover', resourceType: 'image_generation_execution', resourceId: jobId, before: { executionState: execution.state, reconciliationProjection: 'missing' }, after: { resolution, reconciliationRevision: status.revision, recoveredProjection: true }, reason })
        return result({ jobId, resolution, execution, reconciliation: status, recoveredProjection: true })
      }
      if (execution?.state === 'completed' && resolution === 'completed' && job.state === 'succeeded' && job.archiveState === 'archived' && imageJobOutputsAreClean(job)) {
        const status = await statusRepository.upsert({ workspaceId, resourceType: 'image_generation_execution', resourceId: jobId, status: 'succeeded', idempotencyKey, details: { resolution, evidenceRef, reason, executionState: execution.state, settledState: execution.state, recoveredProjection: true }, observedAt: new Date().toISOString() })
        await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.image.reconcile.recover', resourceType: 'image_generation_execution', resourceId: jobId, before: { executionState: execution.state, reconciliationProjection: 'missing' }, after: { resolution, reconciliationRevision: status.revision, recoveredProjection: true }, reason })
        return result({ jobId, resolution, execution, reconciliation: status, recoveredProjection: true })
      }
      if (!execution || !['provider_started', 'outcome_unknown'].includes(execution.state)) throw new DomainError('IMAGE_GENERATION_EXECUTION_NOT_RECONCILABLE', '当前图片执行不在可人工收口状态', 409)
      if (expectedRevision !== undefined && job.revision !== expectedRevision) throw new DomainError('IMAGE_GENERATION_REVISION_CONFLICT', '图片任务已变化，请刷新后重试', 409)
      if (resolution === 'completed' && !(job.state === 'succeeded' && job.archiveState === 'archived' && imageJobOutputsAreClean(job))) throw new DomainError('IMAGE_GENERATION_COMPLETION_EVIDENCE_REQUIRED', '只有任务已成功、产物已归档且安全扫描通过时才能人工确认完成', 409, { reconciliation_required: true })
      let settled
      if (resolution === 'completed') settled = await repository.reconcileCompleted({ workspaceId, jobId })
      else {
        const failed = service.markImageGenerationFailed({ workspaceId, jobId, errorCode: 'IMAGE_GENERATION_MANUAL_FAILED', errorMessage: reason, ...(expectedRevision !== undefined ? { expectedRevision } : {}) })
        await persistence.persistSnapshotAndEvent?.({ workspaceId, entityType: 'image_generation_job', entityId: failed.id, entityVersion: failed.revision, payload: failed as unknown as Record<string, unknown>, eventType: 'image.generation.failed', eventPayload: { job_id: failed.id, error_code: failed.errorCode, error_message: failed.errorMessage, manual: true, evidence_ref: evidenceRef } })
        settled = await repository.reconcileFailed({ workspaceId, jobId, errorCode: failed.errorCode ?? 'IMAGE_GENERATION_MANUAL_FAILED', errorMessage: failed.errorMessage ?? reason })
      }
      const status = await statusRepository.upsert({ workspaceId, resourceType: 'image_generation_execution', resourceId: jobId, status: resolution === 'completed' ? 'succeeded' : 'failed', idempotencyKey, details: { resolution, evidenceRef, reason, executionState: execution.state, settledState: settled.state }, observedAt: new Date().toISOString() })
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.image.reconcile', resourceType: 'image_generation_execution', resourceId: jobId, before: { executionState: execution.state, jobRevision: job.revision }, after: { resolution, status: settled.state, reconciliationRevision: status.revision }, reason })
      return result({ jobId, resolution, execution: settled, reconciliation: status })
    }
    case 'ops.marketing.image.archive.audit': {
      const actorId = requireWorkspaceDataRole(req)
      const requestedLimit = optionalNumberValue(params, 'limit') ?? 100
      if (!Number.isSafeInteger(requestedLimit) || requestedLimit < 1 || requestedLimit > 100) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 到 100 的整数', 400)
      const jobs = [...service.imageGenerationJobs.values()].filter(job => job.workspaceId === workspaceId && Boolean(job.outputs?.length))
      const findings: Array<Record<string, unknown>> = []
      let candidateCount = 0
      for (const job of jobs) for (const output of job.outputs ?? []) {
        candidateCount += 1
        const asset = output.assetId ? service.assets.get(output.assetId) : undefined
        const codes: string[] = []
        if (!asset || asset.workspaceId !== workspaceId) codes.push('ASSET_MISSING_OR_SCOPE_MISMATCH')
        else if (output.sha256 !== asset.sha256 || output.sizeBytes !== asset.sizeBytes || output.mimeType !== asset.mimeType || output.storageKey !== asset.storageKey) codes.push('ASSET_METADATA_MISMATCH')
        if (!output.archiveReceiptId || !output.archiveReceiptDigest) codes.push('ARCHIVE_RECEIPT_MISSING')
        else if (!/^[a-f0-9]{64}$/u.test(output.archiveReceiptDigest)) codes.push('ARCHIVE_RECEIPT_DIGEST_INVALID')
        else if (asset && imageArchiveReceiptDigest({ archiveReceiptId: output.archiveReceiptId, workspaceId, jobId: job.id, assetId: asset.id, objectSha256: asset.sha256, sizeBytes: asset.sizeBytes, mimeType: asset.mimeType, createdAt: output.createdAt }) !== output.archiveReceiptDigest.toLowerCase()) codes.push('ARCHIVE_RECEIPT_DIGEST_MISMATCH')
        if (!asset || asset.scanStatus !== 'clean' || output.storageKey.startsWith('quarantine/')) codes.push('ASSET_NOT_CLEAN_ARCHIVED')
        if (codes.length && findings.length < requestedLimit) findings.push({ jobId: job.id, visualRef: output.visualRef, assetId: output.assetId ?? null, jobState: job.state, archiveState: job.archiveState, codes })
      }
      const resultPayload = { workspaceId, auditedAt: new Date().toISOString(), candidateCount, findingCount: findings.length, truncated: findings.length < candidateCount && findings.length >= requestedLimit, counts: { jobs: jobs.length, candidates: candidateCount, findings: findings.length }, findings, nextAction: findings.length ? '补齐归档 receipt、资产绑定或扫描状态后重新审计；禁止直接标记 completed' : '未发现当前工作区候选级归档证据缺口' }
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.image.archive.audit', resourceType: 'image_generation_archive', resourceId: workspaceId, before: {}, after: { candidateCount, findingCount: findings.length }, reason: '审计图片候选归档证据一致性' })
      return result(resultPayload)
    }
    case 'ops.marketing.image.billing.audit': {
      const actorId = requireWorkspaceDataRole(req)
      const jobId = required(params, 'job_id')
      const job = service.getImageGenerationJob(workspaceId, jobId)
      const execution = persistence.imageGenerationExecutions ? await persistence.imageGenerationExecutions.get({ workspaceId, jobId }) : undefined
      const events = persistence.outbox ? await persistence.outbox.listAggregateEvents(workspaceId, jobId, 100) : []
      const requested = events.find(event => event.eventType === 'image.generation.requested')
      const actionId = requested && typeof requested.payload.action_id === 'string' ? requested.payload.action_id.trim() : undefined
      const action = actionId ? await persistence.actionLedger?.get(workspaceId, actionId) : undefined
      const usage = actionId && persistence.modelUsage ? await persistence.modelUsage.listByAction(workspaceId, actionId) : []
      const codes: string[] = []
      if (!execution?.providerRequestId) codes.push('PROVIDER_REQUEST_ID_MISSING')
      if (!requested) codes.push('REQUEST_EVENT_MISSING')
      if (!actionId) codes.push('ACTION_LEDGER_ID_MISSING')
      if (actionId && !action) codes.push('ACTION_LEDGER_RECORD_MISSING')
      if (actionId && !usage.length) codes.push('MODEL_USAGE_RECORD_MISSING')
      if (execution?.providerRequestId && usage.length && usage.some(item => item.providerRequestId && item.providerRequestId !== execution.providerRequestId)) codes.push('PROVIDER_REQUEST_ID_MISMATCH')
      if (usage.some(item => !['settled', 'waived'].includes(item.settlementStatus))) codes.push('MODEL_USAGE_UNSETTLED')
      if (action && !['settled', 'waived'].includes(action.settlementStatus ?? action.state)) codes.push('ACTION_LEDGER_UNSETTLED')
      if (execution?.providerRequestId && action && action.providerRequestId !== execution.providerRequestId) codes.push(action.providerRequestId ? 'ACTION_PROVIDER_REQUEST_ID_MISMATCH' : 'ACTION_PROVIDER_REQUEST_ID_MISSING')
      for (const item of usage) {
        if (execution?.providerRequestId && item.providerRequestId !== execution.providerRequestId) codes.push(item.providerRequestId ? 'PROVIDER_REQUEST_ID_MISMATCH' : 'MODEL_USAGE_PROVIDER_REQUEST_ID_MISSING')
        if (item.settlementStatus !== 'waived' && item.costCny === undefined) codes.push('MODEL_USAGE_COST_MISSING')
        if (item.settlementStatus !== 'waived' && item.customerChargeCny === undefined) codes.push('MODEL_USAGE_CUSTOMER_CHARGE_MISSING')
      }
      if (usage.length > 1 && new Set(usage.map(item => item.providerRequestId ?? '')).size > 1) codes.push('MODEL_USAGE_PROVIDER_REQUEST_ID_AMBIGUOUS')
      const payload = { workspaceId, jobId: job.id, auditedAt: new Date().toISOString(), jobState: job.state, executionState: execution?.state ?? null, providerRequestId: execution?.providerRequestId ?? null, actionId: actionId ?? null, action: action ? { settlement: action.settlement, state: action.state, settlementStatus: action.settlementStatus ?? null } : null, usage: usage.map(item => ({ id: item.id, providerRequestId: item.providerRequestId ?? null, settlementStatus: item.settlementStatus, modality: item.modality, model: item.model, observedAt: item.observedAt })), codes, ready: codes.length === 0, nextAction: codes.length ? '补齐真实 Provider request、action ledger、model usage/cost 关联并完成结算；禁止静默交付' : '账务关联已形成，可继续等待其他交付门禁' }
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.image.billing.audit', resourceType: 'image_generation_execution', resourceId: jobId, before: {}, after: { ready: payload.ready, codes }, reason: '审计图片任务用量与账务关联' })
      return result(payload)
    }
    case 'ops.marketing.image.evidence.export': {
      const actorId = requireWorkspaceDataRole(req)
      const jobId = required(params, 'job_id')
      const job = service.getImageGenerationJob(workspaceId, jobId)
      const execution = persistence.imageGenerationExecutions ? await persistence.imageGenerationExecutions.get({ workspaceId, jobId }) : undefined
      const reconciliation = persistence.reconciliationStatuses ? await persistence.reconciliationStatuses.getLatest({ workspaceId, resourceType: 'image_generation_execution', resourceId: jobId }) : undefined
      const evidence = persistence.reconciliationEvidence ? await persistence.reconciliationEvidence.list({ workspaceId, jobId, limit: 100 }) : []
      const events = persistence.outbox ? await persistence.outbox.listAggregateEvents(workspaceId, jobId, 100) : []
      const requested = events.find(event => event.eventType === 'image.generation.requested')
      const actionId = requested && typeof requested.payload.action_id === 'string' ? requested.payload.action_id : undefined
      const action = actionId ? await persistence.actionLedger?.get(workspaceId, actionId) : undefined
      const usage = actionId && persistence.modelUsage ? await persistence.modelUsage.listByAction(workspaceId, actionId) : []
      const bundle = {
        schema: 'image-generation-evidence-bundle.v1',
        exportedAt: new Date().toISOString(),
        workspace: { workspaceId, scoped: true },
        release: { releaseId: process.env.RELEASE_ID ?? process.env.GIT_SHA ?? null, gitSha: process.env.GIT_SHA ?? null },
        job: { id: job.id, productId: job.productId, taskId: job.taskId ?? null, state: job.state, archiveState: job.archiveState, revision: job.revision, intentHash: job.intentHash, sourceProductVersion: job.sourceProductVersion, outputCount: job.outputs?.length ?? 0, outputs: (job.outputs ?? []).map(output => ({ visualRef: output.visualRef, assetId: output.assetId ?? null, archiveReceiptId: output.archiveReceiptId ?? null, archiveReceiptDigest: output.archiveReceiptDigest ?? null, ordinal: output.ordinal, mimeType: output.mimeType, sizeBytes: output.sizeBytes, sha256: output.sha256, reviewStatus: output.reviewStatus, createdAt: output.createdAt })) },
        execution: execution ? { eventId: execution.eventId, state: execution.state, attempt: execution.attempt, providerStartedAt: execution.providerStartedAt ?? null, providerRequestId: execution.providerRequestId ?? null, errorCode: execution.errorCode ?? null, errorMessage: execution.errorMessage ?? null, updatedAt: execution.updatedAt } : null,
        reconciliation: reconciliation ? { status: reconciliation.status, revision: reconciliation.revision, observedAt: reconciliation.observedAt, updatedAt: reconciliation.updatedAt, details: { resolution: reconciliation.details.resolution ?? null, evidenceRef: reconciliation.details.evidenceRef ?? null, reason: reconciliation.details.reason ?? null, executionState: reconciliation.details.executionState ?? null } } : null,
        providerEvidence: evidence.map(item => ({ id: item.id, executionAttempt: item.executionAttempt, providerRequestId: item.providerRequestId, queryAttempt: item.queryAttempt, providerState: item.providerState, providerStatus: item.providerStatus ?? null, responseDigest: item.responseDigest ?? null, artifactDigest: item.artifactDigest ?? null, usageLedgerId: item.usageLedgerId ?? null, actionLedgerId: item.actionLedgerId ?? null, observedAt: item.observedAt, errorCode: item.errorCode ?? null, errorMessage: item.errorMessage ?? null, createdAt: item.createdAt })),
        eventReferences: events.map(event => ({ id: event.id, eventType: event.eventType, aggregateId: event.aggregateId, sequence: event.sequence, occurredAt: event.createdAt })),
        billing: { action: action ? { id: action.id, actionKey: action.actionKey, actionKind: action.actionKind, settlement: action.settlement, state: action.state, settlementStatus: action.settlementStatus ?? null, providerRequestId: action.providerRequestId ?? null, createdAt: action.createdAt } : null, usage: usage.map(item => ({ id: item.id, actionId: item.actionId ?? null, modality: item.modality, model: item.model, providerRequestId: item.providerRequestId ?? null, settlementStatus: item.settlementStatus, totalTokens: item.totalTokens ?? null, observedAt: item.observedAt, revision: item.revision })) },
        verification: { workspaceScoped: true, eventBound: Boolean(execution && requested && execution.eventId === requested.id), providerRequestBound: Boolean(execution?.providerRequestId && evidence.every(item => item.providerRequestId === execution.providerRequestId)), assetBytesExcluded: true, storageUrlsExcluded: true, providerPayloadExcluded: true, credentialsExcluded: true, fullChatExcluded: true, billingLinked: Boolean(actionId && action && usage.length) },
      }
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.image.evidence.export', resourceType: 'image_generation_execution', resourceId: jobId, before: {}, after: { schema: bundle.schema, evidenceCount: evidence.length, eventCount: events.length, usageCount: usage.length }, reason: '导出图片异常脱敏证据包' })
      return result({ exportId: randomUUID(), fileName: `image-evidence-${jobId}.json`, contentType: 'application/json; charset=utf-8', json: JSON.stringify(bundle, null, 2), rowCount: 1, truncated: false })
    }
    case 'ops.marketing.visual.review': {
      const actorId = requireWorkspaceDataRole(req)
      const status = params.status === 'passed' || params.status === 'blocked' ? params.status : undefined
      if (!status) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'status 必须是 passed 或 blocked', 400)
      let visualRefs: string[]
      try {
        const parsed = JSON.parse(required(params, 'visual_refs_json'))
        if (!Array.isArray(parsed) || parsed.length < 1 || parsed.length > 50 || parsed.some(value => typeof value !== 'string' || !value.trim())) throw new Error('visual_refs_json')
        visualRefs = [...new Set(parsed.map(value => value.trim()))]
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'visual_refs_json 必须是 1 至 50 个视觉候选引用的 JSON 数组', 400) }
      const expectedRevision = typeof params.expected_revision === 'string' && /^\d+$/u.test(params.expected_revision) ? Number(params.expected_revision) : undefined
      if (params.expected_revision !== undefined && expectedRevision === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_revision 必须是非负整数', 400)
      const jobs = visualRefs.map(visualRef => service.resolveImageGenerationByVisualRef(workspaceId, visualRef))
      if (expectedRevision !== undefined && jobs.some(job => job.revision !== expectedRevision)) throw new DomainError('QUEUE_ASSIGNMENT_VERSION_CONFLICT', '视觉候选队列项目已变化，请刷新后重试', 409)
      const reviewed = service.reviewImageGenerationOutputs(workspaceId, visualRefs, status)
      for (const job of reviewed) {
        await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, job.id, 'marketing.visual_reviewed', job.revision, { job_id: job.id, visual_refs: visualRefs.filter(ref => job.outputs?.some(output => output.visualRef === ref)), status, actor_id: actorId })
      }
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.visual.review', resourceType: 'image_generation_job', resourceId: reviewed[0]?.id ?? visualRefs[0]!, before: { visualRefs }, after: { status, jobIds: reviewed.map(job => job.id) }, reason: required(params, 'reason') })
      return result({ status, visualRefs, jobs: reviewed })
    }
    case 'ops.marketing.generation.retry': {
      const actorId = requireWorkspaceDataRole(req)
      const jobId = required(params, 'job_id')
      const previous = service.getGenerationJob(workspaceId, jobId)
      const task = service.getTask(previous.taskId)
      const usageKey = `${workspaceId}:${jobId}:retry:${previous.revision + 1}`
      const actionId = `model:${usageKey}`
      const prepared = await service.prepareGenerationContext(task.id, actionId)
      await debitPluginWallet({ workspaceId, idempotencyKey: actionId, actorId, description: '模型文案生成重试调用', taskId: task.id, ...(task.campaignItemId ? { campaignItemId: task.campaignItemId } : {}), ...(prepared.contextRef ? { contextLinkId: prepared.contextRef.id, contextHash: prepared.contextRef.contextHash } : {}) })
      try {
        const job = service.retryGeneration({ workspaceId, jobId })
        const authorizationSnapshot = workerAuthorizationSnapshot(req, workspaceId, job.id, 'generation.execute', { method: 'ops.marketing.generation.retry', task_id: task.id, job_revision: job.revision })
        if (!authorizationSnapshot && isProduction()) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '生成重试缺少持久身份授权快照，已拒绝入队', 503)
        await persistSnapshot(workspaceId, 'generation_job', job, job as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, job.id, 'generation.requested', job.revision, { job_id: job.id, task_id: task.id, campaign_item_id: task.campaignItemId ?? null, platform: task.platform, direction_id: task.selectedDirectionId ?? 'default', retry: true, action_id: actionId, run_key: task.id, context_link_id: prepared.contextRef?.id ?? null, context_hash: prepared.contextRef?.contextHash ?? contextEnvelopeHash(prepared.input as unknown as Record<string, unknown>), input_tokens_estimate: prepared.inputTokensEstimate, max_input_tokens: prepared.maxInputTokens, input: prepared.input, ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}) })
        await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.generation.retry', resourceType: 'generation_job', resourceId: job.id, before: { state: 'failed' }, after: job as unknown as Record<string, unknown>, reason: required(params, 'reason') })
        return result(jobWithQueueMetadata(job, workspaceId, 'generation'))
      } catch (error) {
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: actionId, actorId, reason: '生成重试未能可靠入队，释放模型预算与钱包预授权' })
        throw error
      }
    }
    case 'ops.marketing.asset_scan.retry': {
      const actorId = requireWorkspaceDataRole(req)
      const repository = persistence.assetScanRedrive
      if (!repository) throw new DomainError('ASSET_SCAN_REDRIVE_REPOSITORY_UNAVAILABLE', '素材扫描恢复仓储未配置，已拒绝重新排队', 503)
      const assetId = required(params, 'asset_id')
      const eventId = required(params, 'event_id')
      const expectedAssetRevision = Number(required(params, 'expected_asset_revision'))
      const recoveryKey = required(params, 'idempotency_key')
      const reason = required(params, 'reason').trim()
      if (!Number.isSafeInteger(expectedAssetRevision) || expectedAssetRevision < 1) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_asset_revision 必须是正整数', 400)
      const asset = assetForWorkspace(workspaceId, assetId)
      const authorizationSnapshot = workerAuthorizationSnapshot(req, workspaceId, assetId, 'asset.scan.execute', { method: 'ops.marketing.asset_scan.retry', asset_id: assetId, event_id: eventId, expected_asset_revision: expectedAssetRevision })
      if (!authorizationSnapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '素材扫描恢复缺少持久身份授权快照，已拒绝入队', 503)

      // A genuinely new redrive needs live dependency admission and a
      // quarantine-object preflight. A transport replay may arrive after the
      // scan already promoted the object; immutable recovery_key binding in
      // the repository remains authoritative for that replay response.
      if (asset.revision === expectedAssetRevision) {
        const trust = productionAssetScannerReadiness(process.env)
        if (!trust.ready) throw new DomainError('SCANNER_NOT_READY', '素材安全扫描信任配置未就绪，已拒绝重新排队', 503, { reasons: trust.reasons })
        if (!redisHealth) throw new DomainError('SCANNER_HEARTBEAT_MISSING', '未发现素材扫描服务心跳，已拒绝重新排队', 503)
        const readiness = await evaluateScannerRecoveryAdmission({ redis: redisHealth, env: process.env })
        if (!readiness.ready) throw new DomainError(readiness.code, '素材安全扫描服务未就绪，已拒绝重新排队', 503, { scanner: readiness.summary, reasons: readiness.reasons })
        const object = await getAssetStorage().head(workspaceId, asset.storageKey, { includeQuarantine: true })
        if (!object || object.key !== asset.storageKey || object.workspaceId !== workspaceId || object.zone !== 'quarantine'
          || object.sha256 !== asset.sha256 || object.sizeBytes !== asset.sizeBytes || object.contentType.toLowerCase() !== asset.mimeType.toLowerCase()) {
          throw new DomainError('ASSET_SCAN_REDRIVE_OBJECT_BINDING_INVALID', '隔离对象与素材快照不一致，禁止重新扫描；请先调查对象完整性', 409)
        }
      }

      try {
        const redrive = await repository.redrive({ workspaceId, assetId, deadLetterOutboxEventId: eventId, expectedAssetRevision, recoveryKey, actorId, reason, scanMaxAttempts: configuredAssetScanMaxAttempts(), authorizationSnapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) })
        const durableAsset = redrive.asset as unknown as import('../../../packages/application/src/service.js').AssetMetadata
        service.assets.set(assetId, durableAsset)
        invalidateWorkspaceHydration(workspaceId)
        return result({ requestStatus: redrive.replayed ? 'idempotent_replay' : 'accepted', replayed: redrive.replayed, redriveId: redrive.redriveId, auditId: redrive.auditId, newEventId: redrive.event.id, event: { id: redrive.event.id, eventType: redrive.event.eventType, createdAt: redrive.event.createdAt, publishedAt: redrive.event.publishedAt ?? null }, asset: { id: assetId, revision: durableAsset.revision, sourceRevision: durableAsset.sourceRevision, scanStatus: durableAsset.scanStatus }, message: redrive.replayed ? '已返回同一幂等恢复事件；请以当前素材状态和签名扫描回调为准' : '扫描请求已接受；旧失败证据保留，素材不会被人工标记为 clean' })
      } catch (error) {
        if (!(error instanceof AssetScanRedriveError)) throw error
        const status = error.code.endsWith('_NOT_FOUND') ? 404 : error.code.endsWith('_INPUT_INVALID') ? 400 : 409
        throw new DomainError(error.code, '素材扫描恢复条件已变化或失败证据不可安全重试，请刷新后重新检查', status)
      }
    }
    case 'ops.marketing.publish.acknowledge': {
      const actorId = requireWorkspaceDataRole(req)
      const job = service.acknowledgePublish({ workspaceId, publishJobId: required(params, 'publish_job_id'), actorId, reason: required(params, 'reason') })
      await persistSnapshot(workspaceId, 'publish_job', job, job as unknown as Record<string, unknown>)
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.publish.acknowledge', resourceType: 'publish_job', resourceId: job.id, before: { operatorAcknowledgement: null }, after: { operatorAcknowledgement: job.operatorAcknowledgement }, reason: job.operatorAcknowledgement?.reason ?? required(params, 'reason') })
      return result(job)
    }
    case 'ops.marketing.revision.create': {
      const actorId = requireWorkspaceDataRole(req)
      const publishJob = service.getPublishJob(required(params, 'publish_job_id'))
      if (publishJob.workspaceId !== workspaceId) throw new DomainError('TENANT_SCOPE_DENIED', '无权访问该发布任务', 403)
      if (publishJob.remoteState !== 'rejected' && publishJob.state !== 'rejected') throw new DomainError('REVISION_REQUIRES_REJECTED_PUBLISH', '只有平台驳回的发布任务才能创建运营修正版', 409)
      let changes: Partial<import('../../../packages/application/src/service.js').ContentVersion['body']>
      try { const parsed = JSON.parse(required(params, 'changes_json')); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('changes_json'); changes = parsed } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'changes_json 必须是 JSON 对象', 400) }
      let lockedFields: string[] | undefined
      if (typeof params.locked_fields_json === 'string') { try { const parsed = JSON.parse(params.locked_fields_json); if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string')) throw new Error('locked_fields_json'); lockedFields = parsed } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'locked_fields_json 必须是字符串数组', 400) } }
      const source = service.getContentVersion(workspaceId, publishJob.contentVersionId)
      const modified = service.modifyContentVersion({ workspaceId, sourceVersionId: source.id, changes, ...(lockedFields ? { lockedFields } : {}), reason: required(params, 'reason'), ...(typeof params.expected_revision === 'string' && /^\d+$/u.test(params.expected_revision) ? { expectedRevision: Number(params.expected_revision) } : {}) })
      await persistSnapshot(workspaceId, 'content_version', modified.version, modified.version as unknown as Record<string, unknown>)
      await persistSnapshot(workspaceId, 'task', modified.task, modified.task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, modified.version.id, 'content.version_modified', modified.version.revision, { task_id: modified.task.id, source_version_id: source.id, content_version_id: modified.version.id, source_publish_job_id: publishJob.id, reason: required(params, 'reason'), operator_revision: true })
      await recordOperationAudit({ workspaceId, actorId, action: 'ops.marketing.revision.create', resourceType: 'content_version', resourceId: modified.version.id, before: { sourceVersionId: source.id, publishJobId: publishJob.id }, after: modified.version as unknown as Record<string, unknown>, reason: required(params, 'reason') })
      return result({ ...modified, sourcePublishJobId: publishJob.id })
    }
    case 'ops.commercial.rollout.upsert': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops']); const percentage = Number(required(params, 'percentage'))
      if (!Number.isInteger(percentage) || percentage < 0 || percentage > 100) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '灰度百分比必须在 0 到 100 之间', 400)
      const requestedTarget = Object.hasOwn(params, 'target_workspace_id')
        ? (typeof params.target_workspace_id === 'string' ? params.target_workspace_id : undefined)
        : (isPlatformOperations(req) ? undefined : workspaceId)
      const targetWorkspaceId = scopeCommercialRolloutTarget(req, workspaceId, requestedTarget)
      const item = await (persistence.commercialExtensions ?? memoryCommercialExtensions).upsertRollout({ offerCode: required(params, 'offer_code'), ...(targetWorkspaceId ? { workspaceId: targetWorkspaceId } : {}), percentage, enabled: params.enabled === 'true', reason: required(params, 'reason'), updatedBy: actorId, expectedRevision: params.expected_revision ? Number(params.expected_revision) : undefined })
      await recordOperationAudit({ workspaceId, actorId, action: 'commercial.rollout.upsert', resourceType: 'commercial_rollout', resourceId: item.id, before: {}, after: item as unknown as Record<string, unknown>, reason: item.reason }); return result(item)
    }
    case 'ops.member.upsert': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const role = required(params, 'role') as MemberRole
      const repository = persistence.members ?? memoryMembers
      const members = await repository.list(workspaceId)
      const current = members.find(item => item.externalSubject === required(params, 'external_subject'))
      const status = (typeof params.status === 'string' ? params.status : current?.status ?? 'invited') as MemberStatus
      if (!['workspace_owner', 'merchant_admin', 'operator', 'support', 'finance', 'platform_ops'].includes(role) || !['invited', 'active', 'suspended'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '成员角色或状态无效', 400)
      if (role === 'platform_ops' && !isPlatformOperations(req)) throw new DomainError('PLATFORM_ROLE_GRANT_DENIED', '只有平台运营可以授予平台运营角色', 403)
      const actorMemberRole = requestPrincipals.get(req)?.memberRole
      if (role === 'workspace_owner' && !isPlatformOperations(req) && actorMemberRole !== 'workspace_owner') throw new DomainError('WORKSPACE_OWNER_GRANT_DENIED', '只有工作区所有者或平台运营可以授予所有者角色', 403)
      if (current?.role === 'platform_ops' && !isPlatformOperations(req)) throw new DomainError('PLATFORM_ROLE_CHANGE_DENIED', '只有平台运营可以变更平台运营成员', 403)
      if (current?.role === 'workspace_owner' && !isPlatformOperations(req) && actorMemberRole !== 'workspace_owner') throw new DomainError('WORKSPACE_OWNER_CHANGE_DENIED', '只有工作区所有者或平台运营可以变更所有者成员', 403)
      const activeOwnerCount = members.filter(item => item.role === 'workspace_owner' && item.status === 'active').length
      if (!isPlatformOperations(req) && current?.role === 'workspace_owner' && current.status === 'active' && activeOwnerCount <= 1 && (role !== 'workspace_owner' || status !== 'active')) throw new DomainError('LAST_WORKSPACE_OWNER_REQUIRED', '不能降级或停用最后一名有效工作区所有者', 409)
      const expectedRevision = optionalNumberValue(params, 'expectedRevision', 'expected_revision')
      if (current && expectedRevision === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '更新现有成员必须提供 expected_revision', 400)
      const reason = requiredStringValue(params, 'reason')
      try {
        const changed = await repository.upsertWithAudit({ workspaceId, externalSubject: required(params, 'external_subject'), displayName: typeof params.display_name === 'string' ? params.display_name : current?.displayName ?? '', role, status, ...(expectedRevision !== undefined ? { expectedRevision } : {}), actorId, action: 'member.upsert', reason })
        if (repository === memoryMembers) await memoryOperations.append({ workspaceId, actorId, action: 'member.upsert', resourceType: 'workspace_member', resourceId: changed.member.externalSubject, before: (current ?? {}) as unknown as Record<string, unknown>, after: changed.member as unknown as Record<string, unknown>, reason })
        return result(changed.member)
      } catch (error) {
        if (error instanceof Error && error.message === 'MEMBER_REVISION_CONFLICT') throw new DomainError('MEMBER_REVISION_CONFLICT', '成员信息已变化，请刷新后重试', 409)
        throw error
      }
    }
    case 'ops.member.suspend': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const externalSubject = required(params, 'external_subject')
      const members = await (persistence.members ?? memoryMembers).list(workspaceId)
      const target = members.find(item => item.externalSubject === externalSubject)
      const actorMemberRole = requestPrincipals.get(req)?.memberRole
      if (target?.role === 'platform_ops' && !isPlatformOperations(req)) throw new DomainError('PLATFORM_ROLE_CHANGE_DENIED', '只有平台运营可以停用平台运营成员', 403)
      if (target?.role === 'workspace_owner' && !isPlatformOperations(req) && actorMemberRole !== 'workspace_owner') throw new DomainError('WORKSPACE_OWNER_CHANGE_DENIED', '只有工作区所有者或平台运营可以停用所有者成员', 403)
      if (!isPlatformOperations(req) && target?.role === 'workspace_owner' && target.status === 'active' && members.filter(item => item.role === 'workspace_owner' && item.status === 'active').length <= 1) throw new DomainError('LAST_WORKSPACE_OWNER_REQUIRED', '不能停用最后一名有效工作区所有者', 409)
      const expectedRevision = optionalNumberValue(params, 'expectedRevision', 'expected_revision')
      if (expectedRevision === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '停用成员必须提供 expected_revision', 400)
      const member = await changeMemberStatusWithAudit({ workspaceId, externalSubject, targetStatus: 'suspended', expectedRevision, actorId, action: 'member.suspend', reason: required(params, 'reason') })
      return result(member)
    }
    case 'ops.user.suspend': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      if (params.scope === 'identity') {
        const identityId = required(params, 'identity_id')
        if (identityId === requestPrincipals.get(req)?.identityId) throw new DomainError('SELF_SUSPENSION_DENIED', '不能停用当前登录账号；请由另一名平台运营人员执行', 409)
        try { return result(await (persistence.identities ?? memoryIdentities).transitionAccess({ identityId, target: 'suspended', expectedRevision: Number(required(params, 'expected_revision')), actorId, reason: required(params, 'reason'), idempotencyKey: required(params, 'idempotency_key') })) }
        catch (error) { mapIdentityLifecycleError(error) }
      }
      const targetWorkspaceId = required(params, 'workspace_id')
      const externalSubject = required(params, 'external_subject')
      const reason = required(params, 'reason')
      if (externalSubject === actorId) throw new DomainError('SELF_SUSPENSION_DENIED', '不能停用当前登录账号；请由另一名平台运营人员执行', 409)
      const expectedRevision = optionalNumberValue(params, 'expectedRevision', 'expected_revision')
      if (expectedRevision === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '停用成员必须提供 expected_revision', 400)
      const member = await changeMemberStatusWithAudit({ workspaceId: targetWorkspaceId, externalSubject, targetStatus: 'suspended', expectedRevision, actorId, action: 'user.suspend', reason })
      return result(member)
    }
    case 'ops.user.activate': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      if (params.scope === 'identity') {
        try { return result(await (persistence.identities ?? memoryIdentities).transitionAccess({ identityId: required(params, 'identity_id'), target: 'active', expectedRevision: Number(required(params, 'expected_revision')), actorId, reason: required(params, 'reason'), idempotencyKey: required(params, 'idempotency_key') })) }
        catch (error) { mapIdentityLifecycleError(error) }
      }
      const targetWorkspaceId = required(params, 'workspace_id')
      const externalSubject = required(params, 'external_subject')
      const reason = required(params, 'reason')
      const expectedRevision = optionalNumberValue(params, 'expectedRevision', 'expected_revision')
      if (expectedRevision === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '激活成员必须提供 expected_revision', 400)
      const member = await changeMemberStatusWithAudit({ workspaceId: targetWorkspaceId, externalSubject, targetStatus: 'active', expectedRevision, actorId, action: 'user.activate', reason })
      return result(member)
    }
    case 'ops.user.risk.transition': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      let evidence: Record<string, unknown> = {}
      if (typeof params.evidence_json === 'string' && params.evidence_json.trim()) {
        try { const parsed: unknown = JSON.parse(params.evidence_json); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('invalid'); evidence = parsed as Record<string, unknown> }
        catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'evidence_json 必须是 JSON 对象', 400) }
      }
      try { return result(await (persistence.identities ?? memoryIdentities).transitionRisk({ identityId: required(params, 'identity_id'), level: required(params, 'risk_level') as import('../../../packages/persistence/src/identity-lifecycle-repository.js').IdentityRiskLevel, decision: required(params, 'risk_decision') as import('../../../packages/persistence/src/identity-lifecycle-repository.js').IdentityRiskDecision, expectedRevision: Number(required(params, 'expected_revision')), actorId, reason: required(params, 'reason'), evidence, idempotencyKey: required(params, 'idempotency_key') })) }
      catch (error) { mapIdentityLifecycleError(error) }
    }
    case 'ops.user.session.revoke': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      try { return result(await (persistence.identities ?? memoryIdentities).revokeSession({ identityId: required(params, 'identity_id'), sessionId: required(params, 'session_id'), expectedRevision: Number(required(params, 'expected_revision')), actorId, reason: required(params, 'reason'), idempotencyKey: required(params, 'idempotency_key') })) }
      catch (error) { mapIdentityLifecycleError(error) }
    }
    case 'ops.data.delete.list': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops'])
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Number(params.limit) : 100
      if (!Number.isInteger(limit) || limit < 1 || limit > 500) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 到 500 的整数', 400)
      const platformScope = params.platform_scope === 'platform'
      if (params.platform_scope !== undefined && !platformScope) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform_scope 必须是 platform', 400)
      if (platformScope && !isPlatformOperations(req)) throw new DomainError(ERROR_CODES.FORBIDDEN, '只有平台运营角色可以读取平台数据删除申请', 403)
      if (!platformScope) return result(await (persistence.dataLifecycle ?? memoryDataLifecycle).list(workspaceId, limit))
      const workspaceIds = persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [...knownWorkspaces]
      const requests = (await mapWithConcurrency(workspaceIds, 4, targetWorkspaceId => (persistence.dataLifecycle ?? memoryDataLifecycle).list(targetWorkspaceId, limit))).flat()
      const groups = new Map<string, { scope: DataDeletionScope; status: DataDeletionRequest['status']; count: number; latestRequestedAt: string }>()
      for (const request of requests) {
        const key = `${request.scope}:${request.status}`
        const current = groups.get(key)
        if (current) { current.count += 1; if (request.requestedAt > current.latestRequestedAt) current.latestRequestedAt = request.requestedAt }
        else groups.set(key, { scope: request.scope, status: request.status, count: 1, latestRequestedAt: request.requestedAt })
      }
      const items = [...groups.values()].sort((left, right) => right.latestRequestedAt.localeCompare(left.latestRequestedAt)).map((group, index) => ({ id: `platform-deletion-group-${index + 1}`, scope: group.scope, status: group.status, count: group.count, requestedAt: group.latestRequestedAt, aggregate: true, reason: '平台聚合视图已脱敏；切换到明确工作区授权会话查看申请详情。' }))
      return result({ items: items.slice(0, limit), aggregate: true, total: requests.length, truncated: items.length > limit })
    }
    case 'ops.data.delete.cancel': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const reason = requiredOperationalReason(params)
      const request = await (persistence.dataLifecycle ?? memoryDataLifecycle).cancel({ workspaceId, id: required(params, 'request_id'), actorId, reason })
      await recordOperationAudit({ workspaceId, actorId, action: 'data.delete.cancel', resourceType: 'data_deletion_request', resourceId: request.id, before: { status: 'pending' }, after: request as unknown as Record<string, unknown>, reason })
      return result(request)
    }
    case 'ops.data.delete.approve': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const reason = requiredOperationalReason(params)
      const request = await (persistence.dataLifecycle ?? memoryDataLifecycle).approve({ workspaceId, id: required(params, 'request_id'), actorId, reason })
      await recordOperationAudit({ workspaceId, actorId, action: 'data.delete.approve', resourceType: 'data_deletion_request', resourceId: request.id, before: { status: 'pending' }, after: request as unknown as Record<string, unknown>, reason })
      return result(request)
    }
    case 'billing.usage.consume': {
      let charged
      try {
        charged = await (persistence.usage ?? memoryUsage).consume({ workspaceId, taskId: required(params, 'task_id'), idempotencyKey: required(params, 'idempotency_key'), actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'actor_demo' })
      } catch (error) {
        if ((error as { code?: string })?.code === 'USAGE_IDEMPOTENCY_CONFLICT' || String(error).includes('USAGE_IDEMPOTENCY_CONFLICT')) throw new DomainError('USAGE_IDEMPOTENCY_CONFLICT', '用量幂等键已绑定到其他任务，请换用新的幂等键', 409)
        throw error
      }
      if (charged.charged) await recordOperationAudit({ workspaceId, actorId: requestPrincipals.get(req)?.actorId ?? 'actor_demo', action: 'usage.consume', resourceType: 'task', resourceId: required(params, 'task_id'), before: {}, after: charged.snapshot as unknown as Record<string, unknown>, reason: '任务额度消费' })
      return result(charged)
    }
    case 'billing.usage.refund': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
      const refunded = await (persistence.usage ?? memoryUsage).refund({ workspaceId, taskId: required(params, 'task_id'), idempotencyKey: required(params, 'idempotency_key'), reason: required(params, 'reason'), actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'actor_demo' })
      if (refunded.refunded) await recordOperationAudit({ workspaceId, actorId: requestPrincipals.get(req)?.actorId ?? 'actor_demo', action: 'usage.refund', resourceType: 'task', resourceId: required(params, 'task_id'), before: {}, after: refunded.snapshot as unknown as Record<string, unknown>, reason: required(params, 'reason') })
      return result(refunded)
    }
    case 'workspace.commercial.update': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const current = await (persistence.commercial ?? memoryCommercial).getSettings(workspaceId)
      const money = (name: string, fallback: number) => { const value = typeof params[name] === 'string' ? Number(params[name]) : fallback; if (!Number.isFinite(value) || value < 0 || !/^\d+(\.\d{1,2})?$/u.test(String(params[name] ?? value))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${name} 必须是非负金额，最多两位小数`, 400); return Number(value.toFixed(2)) }
      const integer = (name: string, fallback: number) => { const value = typeof params[name] === 'string' ? Number(params[name]) : fallback; if (!Number.isInteger(value) || value < 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${name} 必须是非负整数`, 400); return value }
      const updated = await (persistence.commercial ?? memoryCommercial).updateSettings({ workspaceId, planCode: required(params, 'plan_code'), planName: required(params, 'plan_name'), monthlyPriceCny: money('monthly_price_cny', current.monthlyPriceCny), annualPriceCny: money('annual_price_cny', current.annualPriceCny), includedStores: integer('included_stores', current.includedStores), includedTasks: integer('included_tasks', current.includedTasks), updatedBy: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'actor_demo', expectedRevision: params.expected_revision ? integer('expected_revision', current.revision) : undefined })
      await recordOperationAudit({ workspaceId, actorId: updated.updatedBy, action: 'commercial.settings.update', resourceType: 'commercial_settings', resourceId: workspaceId, before: current as unknown as Record<string, unknown>, after: updated as unknown as Record<string, unknown>, reason: typeof params.reason === 'string' ? params.reason : '运营配置更新' })
      return result(updated)
    }
    case 'platform.settings.get':
      return result({ platforms: await (persistence.commercial ?? memoryCommercial).listPlatformSettings(workspaceId) })
    case 'platform.media.spec.list': {
      const repository = await platformMediaSpecRepository()
      const ops = isPlatformOperations(req)
      const platform = params.platform === undefined ? undefined : asPlatform(params.platform)
      const device = params.device === undefined ? undefined : asMediaDevice(params.device)
      const requestedStatus = params.status === undefined ? undefined : asMediaStatus(params.status)
      if (params.platform !== undefined && !platform || params.device !== undefined && !device || params.status !== undefined && !requestedStatus) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '媒体规格筛选参数无效', 400)
      try {
        const specs = await repository.list({ ...(platform ? { platform } : {}), ...(typeof params.placement === 'string' && params.placement.trim() ? { placement: params.placement.trim() } : {}), ...(device ? { device } : {}), ...(ops && requestedStatus ? { status: requestedStatus } : { status: 'approved' }), ...(ops && typeof params.at === 'string' ? { at: params.at } : {}) })
        const visible = ops ? specs : specs.filter(spec => mediaSpecIsActive(spec))
        return result({ specs: visible, count: visible.length, visibility: ops ? 'all_authorized_states' : 'active_only' })
      } catch (error) { platformMediaSpecError(error) }
    }
    case 'platform.media.spec.get': {
      const repository = await platformMediaSpecRepository()
      const ops = isPlatformOperations(req)
      try {
        const spec = await repository.get(required(params, 'id'), ops && typeof params.at === 'string' ? params.at : undefined)
        if (!spec || !ops && !mediaSpecIsActive(spec)) throw new DomainError('PLATFORM_MEDIA_SPEC_NOT_FOUND', '平台媒体规格不存在或当前没有 active 证据', 404)
        return result({ spec, ...(ops ? { audit: await repository.listAudit(spec.id) } : {}), visibility: ops ? 'operator' : 'active_only' })
      } catch (error) { platformMediaSpecError(error) }
    }
    case 'platform.media.spec.create': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      const repository = await platformMediaSpecRepository()
      const platform = asPlatform(params.platform)
      const device = asMediaDevice(params.device)
      if (!platform || !device || requiredPositiveInteger(params, 'expected_revision', true) !== 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '创建媒体规格的平台、设备或 expected_revision 无效', 400)
      try {
        const created = await repository.createDraft({ actorId, actorRole: 'merchant_ops', ...(typeof params.id === 'string' && params.id.trim() ? { id: params.id.trim() } : {}), platform, placement: required(params, 'placement'), device, version: required(params, 'version'), specJson: parseJsonObjectParameter(params, 'spec_json'), sourceUrl: required(params, 'source_url'), sourceSha256: required(params, 'source_sha256'), checkedAt: required(params, 'checked_at'), ...(typeof params.evidence_artifact_ref === 'string' && params.evidence_artifact_ref.trim() ? { evidenceArtifactRef: params.evidence_artifact_ref.trim() } : {}), ...(typeof params.evidence_artifact_sha256 === 'string' && params.evidence_artifact_sha256.trim() ? { evidenceArtifactSha256: params.evidence_artifact_sha256.trim() } : {}), ...(typeof params.expires_at === 'string' && params.expires_at.trim() ? { expiresAt: params.expires_at.trim() } : {}), reason: required(params, 'reason'), idempotencyKey: required(params, 'idempotency_key') })
        return result({ ...created, audit: await repository.listAudit(created.spec.id) })
      } catch (error) { platformMediaSpecError(error) }
    }
    case 'platform.media.spec.update': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      const repository = await platformMediaSpecRepository()
      const raw = parseJsonObjectParameter(params, 'patch_json')
      const allowed = new Set(['placement', 'device', 'version', 'spec_json', 'source_url', 'source_sha256', 'checked_at', 'evidence_artifact_ref', 'evidence_artifact_sha256', 'expires_at'])
      if (!Object.keys(raw).length || Object.keys(raw).some(key => !allowed.has(key))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'patch_json 包含不允许修改的字段', 400)
      const patch: Record<string, unknown> = {}
      if (raw.placement !== undefined) patch.placement = raw.placement
      if (raw.device !== undefined) patch.device = raw.device
      if (raw.version !== undefined) patch.version = raw.version
      if (raw.spec_json !== undefined) patch.specJson = raw.spec_json
      if (raw.source_url !== undefined) patch.sourceUrl = raw.source_url
      if (raw.source_sha256 !== undefined) patch.sourceSha256 = raw.source_sha256
      if (raw.checked_at !== undefined) patch.checkedAt = raw.checked_at
      if (raw.evidence_artifact_ref !== undefined) patch.evidenceArtifactRef = raw.evidence_artifact_ref
      if (raw.evidence_artifact_sha256 !== undefined) patch.evidenceArtifactSha256 = raw.evidence_artifact_sha256
      if (raw.expires_at !== undefined) patch.expiresAt = raw.expires_at
      try {
        const updated = await repository.updateDraft({ actorId, actorRole: 'merchant_ops', id: required(params, 'id'), expectedRevision: requiredPositiveInteger(params, 'expected_revision'), patch: patch as Parameters<PlatformMediaSpecRepository['updateDraft']>[0]['patch'], reason: required(params, 'reason'), idempotencyKey: required(params, 'idempotency_key') })
        return result({ ...updated, audit: await repository.listAudit(updated.spec.id) })
      } catch (error) { platformMediaSpecError(error) }
    }
    case 'platform.media.spec.approve':
    case 'platform.media.spec.expire': {
      const actorId = requireOperationsRole(req, ['platform_ops'])
      const repository = await platformMediaSpecRepository()
      try {
        const input = { actorId, actorRole: 'merchant_ops' as const, id: required(params, 'id'), expectedRevision: requiredPositiveInteger(params, 'expected_revision'), reason: required(params, 'reason'), idempotencyKey: required(params, 'idempotency_key') }
        const changed = method === 'platform.media.spec.approve' ? await repository.approve(input) : await repository.expire(input)
        return result({ ...changed, audit: await repository.listAudit(changed.spec.id) })
      } catch (error) { platformMediaSpecError(error) }
    }
    case 'platform.mapping.preflight': {
      const input = parseJsonObjectParameter(params, 'input_json') as unknown as PlatformFieldMappingGateInput
      return result(await evaluatePlatformMappingForWorkspace(req, workspaceId, input))
    }
    case 'delivery.bundle.verify': {
      const manifest = parseJsonObjectParameter(params, 'manifest_json') as unknown as DeliveryBundleManifest
      if (manifest.scope?.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '交付 bundle manifest 不属于当前工作区', 403)
      const files = parseDeliveryBundleFiles(parseJsonArrayParameter(params, 'files_json'))
      let verification
      try { verification = verifyDeliveryBundle(manifest, files, required(params, 'expected_manifest_hash')) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, '交付 bundle manifest 或文件结构无效', 400) }
      return result({ ...verification, expected_manifest_hash: required(params, 'expected_manifest_hash').replace(/^sha256:/iu, '').toLowerCase(), verified_at: new Date().toISOString() })
    }
    case 'platform.model.status': {
      const textGate = evaluatePlatformModelGate(process.env, 'text')
      const imageGate = evaluatePlatformModelGate(process.env, 'image')
      const imageEditGate = evaluatePlatformModelGate(process.env, 'image_edit')
      const ocrGate = evaluatePlatformModelGate(process.env, 'ocr')
      const videoGate = evaluatePlatformModelGate(process.env, 'video')
      const relayGate = evaluatePlatformModelRelayGate(process.env)
      const costGate = evaluatePlatformModelCostGate(process.env)
      const releaseMetadataNames = ['PLUGIN_VERSION', 'SKILL_BUNDLE_VERSION', 'MCP_VERSION', 'CONNECTOR_BUILD', 'PROMPT_BUNDLE_VERSION']
      const releaseMetadataMissing = isProduction() ? releaseMetadataNames.filter(name => !process.env[name]?.trim() || process.env[name]!.trim().toLowerCase().includes('fixture') || process.env[name]!.trim().toLowerCase() === 'local') : []
      const releaseMetadataReady = releaseMetadataMissing.length === 0
      const providerUrl = process.env.MODEL_RELAY_BASE_URL?.trim()
      let providerHost: string | undefined
      try { providerHost = providerUrl ? new URL(providerUrl).host : undefined } catch { providerHost = undefined }
      const textReady = textGate.ready
      const imageReady = imageGate.ready
      const rpm = costGate.rpm
      const tpm = costGate.tpm
      const dailyCnyLimit = costGate.dailyCnyLimit
      const costEvidenceByModality = modelCostEvidenceByModality()
      const costEvidenceReady = Object.values(costEvidenceByModality).every(Boolean)
      const costControlReady = costGate.ready && costEvidenceReady
      const ocrReady = ocrGate.ready && Boolean(imageFactsExtractor)
      const imageEditReady = imageEditGate.ready && Boolean(imageEditGenerator)
      const videoReady = videoGate.ready && Boolean(videoGenerator)
      const allModelReady = textReady && imageReady && imageEditReady && ocrReady && videoReady
      return result({
        ownership: 'platform', user_key_binding: false, relay: { configured: relayGate.ready, host: relayGate.endpointHost ?? null, reasons: relayGate.reasons }, state: allModelReady && costControlReady && releaseMetadataReady && (!isProduction() || relayGate.ready) ? 'ready' : !releaseMetadataReady ? 'release_metadata_blocked' : isProduction() && !relayGate.ready ? 'model_relay_blocked' : allModelReady ? 'cost_gate_blocked' : textReady ? 'partial_model_readiness' : 'not_configured',
        provider_host: relayGate.endpointHost ?? textGate.endpointHost ?? providerHost ?? null, image_provider_host: relayGate.endpointHost ?? imageGate.endpointHost ?? null, text_model: process.env.AI_MODEL?.trim() || process.env.MODEL_ID?.trim() || null, image_model: process.env.IMAGE_MODEL?.trim() || process.env.AI_IMAGE_MODEL?.trim() || null, vision_model: process.env.OCR_MODEL?.trim() || process.env.AI_VISION_MODEL?.trim() || null, video_model: process.env.VIDEO_MODEL?.trim() || process.env.AI_VIDEO_MODEL?.trim() || null,
        capabilities: { text_generation: textReady, image_generation: imageReady, image_editing: imageEditReady, image_fact_ocr: ocrReady, video_rendering: videoReady }, endpoints: { text_https: textGate.https, image_https: imageGate.https, image_edit_https: imageEditGate.https, ocr_https: ocrGate.https, video_https: videoGate.https },
        model_readiness: { text: { ...textGate, provider_configured: textReady }, image: { ...imageGate, provider_configured: imageReady }, image_edit: { ...imageEditGate, provider_configured: imageEditReady }, ocr: { ...ocrGate, provider_configured: ocrReady }, video: { ...videoGate, provider_configured: videoReady } },
        quotas: { rpm: rpm || null, tpm: tpm || null, daily_cny_limit: dailyCnyLimit ? dailyCnyLimit.toFixed(2) : null },
        cost_control_ready: costControlReady, cost_evidence_ready: costEvidenceReady, cost_evidence_by_modality: costEvidenceByModality,
        release_metadata_ready: releaseMetadataReady, release_metadata_missing: releaseMetadataMissing,
        next_actions: [...(isProduction() && !relayGate.ready ? ['配置平台模型中转站：' + relayGate.reasons.join('、')] : []), ...(!textReady ? ['配置平台文案模型：' + textGate.reasons.join('、')] : []), ...(!imageReady ? ['配置平台图片模型：' + imageGate.reasons.join('、')] : []), ...(!imageEditReady ? ['配置图片编辑模型和中转 provider：' + imageEditGate.reasons.join('、')] : []), ...(!ocrReady ? ['配置 OCR_MODEL 和 OCR 中转 provider：' + ocrGate.reasons.join('、')] : []), ...(!videoReady ? ['配置 VIDEO_MODEL 和视频中转 provider：' + videoGate.reasons.join('、')] : []), ...(!costGate.ready ? ['配置并审批平台模型 RPM、TPM 和每日人民币成本上限'] : []), ...(!costEvidenceReady ? ['验证中转站 cost_cny，或验证价格快照、实际计费分组和人民币汇率后，再开启成本证据开关'] : []), ...(!releaseMetadataReady ? ['注入不可使用 fixture/local 默认值的发布版本、Skill、MCP、连接器和 prompt 元数据'] : []), ...(allModelReady && costControlReady && releaseMetadataReady && (!isProduction() || relayGate.ready) ? [] : ['完成平台模型供应商额度、成本和数据处理条款审批'])],
      })
    }
    case 'platform.settings.update': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const platform = required(params, 'platform') as CommercialPlatform
      if (!COMMERCIAL_PLATFORMS.includes(platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '不支持的平台', 400)
      const existing = (await (persistence.commercial ?? memoryCommercial).listPlatformSettings(workspaceId)).find(item => item.platform === platform)!
      const updated = await (persistence.commercial ?? memoryCommercial).updatePlatform({ workspaceId, platform, enabled: params.enabled === undefined ? existing.enabled : params.enabled === 'true', displayName: typeof params.display_name === 'string' ? params.display_name : existing.displayName, storeAlias: typeof params.store_alias === 'string' ? params.store_alias : existing.storeAlias, updatedBy: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'actor_demo', expectedRevision: params.expected_revision ? Number(params.expected_revision) : undefined })
      await recordOperationAudit({ workspaceId, actorId: updated.updatedBy, action: 'platform.settings.update', resourceType: 'platform_settings', resourceId: platform, before: existing as unknown as Record<string, unknown>, after: updated as unknown as Record<string, unknown>, reason: typeof params.reason === 'string' ? params.reason : '平台配置更新' })
      return result(updated)
    }
    case 'workspace.metrics': {
      const metricsData = await prepareWorkspaceMetricsData(workspaceId)
      const selectedPlatform = typeof params.platform === 'string' ? params.platform as Platform : undefined
      const selectedAccountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      if (selectedAccountId && !selectedPlatform) throw new DomainError('STORE_PLATFORM_REQUIRED', '使用 account_id 选择店铺时必须同时指定 platform', 400)
      const selectedAccount = selectedAccountId ? service.getPlatformAccount(workspaceId, selectedAccountId, selectedPlatform) : undefined
      const allProducts = service.listProducts(workspaceId)
      const allTasks = service.listTasks(workspaceId)
      const allSyncJobs = service.listSyncJobs(workspaceId)
      const allPublishJobs = service.listPublishJobs(workspaceId)
      const productByIdAll = new Map(allProducts.map(product => [product.id, product]))
      const taskAccount = (task: typeof allTasks[number]) => task.accountId ?? productByIdAll.get(task.productId)?.accountId
      const selected = (platform: Platform, accountId: string | undefined) => (!selectedPlatform || platform === selectedPlatform) && (!selectedAccountId || accountId === selectedAccountId)
      const products = allProducts.filter(product => selected(product.platform, product.accountId))
      const tasks = allTasks.filter(task => selected(task.platform, taskAccount(task)))
      const selectedTaskIds = new Set(tasks.map(task => task.id))
      const syncJobs = allSyncJobs.filter(job => selected(job.platform, job.accountId))
      const publishJobs = allPublishJobs.filter(job => selected(job.platform, job.accountId ?? (allTasks.find(task => task.id === job.taskId) ? taskAccount(allTasks.find(task => task.id === job.taskId)!) : undefined)))
      const generationJobs = [...service.generationJobs.values()].filter(job => job.workspaceId === workspaceId && selectedTaskIds.has(job.taskId))
      const contentVersions = [...service.contentVersions.values()].filter(version => selectedTaskIds.has(version.taskId))
      const parsePeriodBoundary = (key: 'date_from' | 'date_to') => {
        if (typeof params[key] !== 'string' || !params[key]!.trim()) return undefined
        const value = params[key]!.trim()
        if (!Number.isFinite(Date.parse(value))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是合法 ISO 时间`, 400)
        return new Date(value).toISOString()
      }
      const dateFrom = parsePeriodBoundary('date_from')
      const dateTo = parsePeriodBoundary('date_to')
      if (dateFrom && dateTo && dateFrom > dateTo) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'date_from 不能晚于 date_to', 400)
      if (typeof params.risk_limit === 'string' && params.risk_limit.trim() && !/^[1-9]\d*$/u.test(params.risk_limit.trim())) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'risk_limit 必须是正整数', 400)
      const requestedRiskLimit = typeof params.risk_limit === 'string' && params.risk_limit.trim() ? Number(params.risk_limit) : 50
      const riskLimit = Math.min(100, Math.max(1, requestedRiskLimit))
      const inPeriod = (at: string | undefined) => Boolean(at) && (!dateFrom || at! >= dateFrom) && (!dateTo || at! <= dateTo)
      const contentReviews = contentVersions.map(version => ({ version, findings: service.reviewContent(workspaceId, version.id) }))
      const p0FindingCount = contentReviews.reduce((count, item) => count + item.findings.filter(finding => finding.severity === 'error').length, 0)
      const recoveredTasks = tasks.filter(task => task.state === 'failed_recoverable').length
      const lowStockProducts = products.filter(product => typeof product.stock === 'number' && product.stock <= 10)
      const missingImageProducts = products.filter(product => !product.images?.length)
      const latestSyncByStore = new Map<string, typeof syncJobs[number]>()
      for (const job of [...syncJobs].sort((left, right) => left.updatedAt.localeCompare(right.updatedAt))) latestSyncByStore.set(`${job.platform}:${job.accountId}`, job)
      const failedSyncJobs = [...latestSyncByStore.values()].filter(job => job.state === 'failed' || job.state === 'partial')
      const recommendations = [
        ...(lowStockProducts.length ? [{ code: 'LOW_STOCK', priority: 'high', title: '检查低库存商品', action: `有 ${lowStockProducts.length} 个商品库存低于安全线，建议先确认补货或调整投放。`, evidence: lowStockProducts.slice(0, 5).map(product => product.title) }] : []),
        ...(missingImageProducts.length ? [{ code: 'MISSING_IMAGES', priority: 'high', title: '补齐商品主图', action: `有 ${missingImageProducts.length} 个商品没有可审阅主图，建议先从素材库上传并完成权益/安全扫描。`, evidence: missingImageProducts.slice(0, 5).map(product => product.title) }] : []),
        ...(failedSyncJobs.length ? [{ code: 'SYNC_RISK', priority: 'medium', title: '处理同步异常', action: `有 ${failedSyncJobs.length} 个同步任务失败或部分失败，建议查看失败项并重试，不要直接发布旧数据。`, evidence: failedSyncJobs.slice(0, 5).map(job => job.id) }] : []),
        ...(p0FindingCount ? [{ code: 'CONTENT_BLOCKING', priority: 'high', title: '处理内容审核阻断', action: `有 ${p0FindingCount} 个内容审核阻断项，建议先修正文案或素材，再进入发布预览。`, evidence: [`${p0FindingCount} 个阻断项`] }] : []),
      ]
      if (!recommendations.length) recommendations.push({ code: 'NEXT_BEST_ACTION', priority: 'low', title: '继续优化重点商品', action: '当前没有发现阻断风险，可以选择一个已确认商品生成详情和主图，并在发布前完成人工复核。', evidence: [] })
      const platformMetrics = Object.fromEntries(SUPPORTED_PLATFORMS.map(platform => {
        const accounts = service.listPlatformAccounts(workspaceId).filter(account => account.platform === platform && (!selectedAccountId || account.id === selectedAccountId))
        const sync = syncJobs.filter(job => job.platform === platform)
        const publish = publishJobs.filter(job => job.platform === platform)
        return [platform, {
          accounts: { total: accounts.length, connected: accounts.filter(account => account.tokenState === 'connected').length, revoked: accounts.filter(account => account.tokenState === 'revoked').length, refreshRequired: accounts.filter(account => account.tokenState === 'refresh_required').length },
          sync: { total: sync.length, succeeded: sync.filter(job => job.state === 'succeeded').length, partial: sync.filter(job => job.state === 'partial').length, failed: sync.filter(job => job.state === 'failed').length },
          publish: { total: publish.length, submitted: publish.filter(job => job.remoteState === 'submitted').length, published: publish.filter(job => job.remoteState === 'published' || job.state === 'published').length, rejected: publish.filter(job => job.remoteState === 'rejected' || job.state === 'rejected').length, unknown: publish.filter(job => job.remoteState === 'unknown' || job.state === 'unknown' || job.state === 'manual_attention').length },
        }]
      }))

      const accounts = service.listPlatformAccounts(workspaceId).filter(account => selected(account.platform, account.id))
      const productById = new Map(products.map(product => [product.id, product]))
      const taskById = new Map(tasks.map(task => [task.id, task]))
      const resolvedTaskAccount = (task: typeof tasks[number]) => task.accountId ?? productById.get(task.productId)?.accountId
      const resolvedPublishAccount = (job: typeof publishJobs[number]) => job.accountId ?? (taskById.get(job.taskId) ? resolvedTaskAccount(taskById.get(job.taskId)!) : undefined)
      const storeKeys = new Map<string, { platform: Platform; accountId: string }>()
      const addStoreKey = (platform: Platform, accountId: string | undefined) => { if (accountId) storeKeys.set(`${platform}:${accountId}`, { platform, accountId }) }
      for (const account of accounts) addStoreKey(account.platform, account.id)
      for (const product of products) addStoreKey(product.platform, product.accountId)
      for (const task of tasks) addStoreKey(task.platform, resolvedTaskAccount(task))
      for (const job of syncJobs) addStoreKey(job.platform, job.accountId)
      for (const job of publishJobs) addStoreKey(job.platform, resolvedPublishAccount(job))

      type RiskItem = { key: string; type: string; severity: 'high' | 'medium'; platform: Platform; accountId?: string; storeName?: string; entityType: string; entityId: string; title: string; status: string; observedAt?: string; evidence: Record<string, unknown>; nextAction: string }
      const riskItems: RiskItem[] = []
      const pushRisk = (item: RiskItem) => riskItems.push(item)
      const latestPublishByTask = new Map<string, typeof publishJobs[number]>()
      for (const job of [...publishJobs].sort((left, right) => left.createdAt.localeCompare(right.createdAt))) latestPublishByTask.set(job.taskId, job)

      const stores = [...storeKeys.values()].sort((left, right) => `${left.platform}:${left.accountId}`.localeCompare(`${right.platform}:${right.accountId}`)).map(({ platform, accountId }) => {
        const account = accounts.find(item => item.platform === platform && item.id === accountId)
        const storeProducts = products.filter(product => product.platform === platform && product.accountId === accountId)
        const storeTasks = tasks.filter(task => task.platform === platform && resolvedTaskAccount(task) === accountId)
        const storeSync = syncJobs.filter(job => job.platform === platform && job.accountId === accountId)
        const storePublish = publishJobs.filter(job => job.platform === platform && resolvedPublishAccount(job) === accountId)
        const periodTasks = storeTasks.filter(task => inPeriod(task.createdAt))
        const periodSync = storeSync.filter(job => inPeriod(job.createdAt))
        const periodPublish = storePublish.filter(job => inPeriod(job.createdAt))
        const latestSync = [...storeSync].sort((left, right) => right.updatedAt.localeCompare(left.updatedAt))[0]
        const lastSuccessfulSync = [...storeSync].filter(job => job.state === 'succeeded').sort((left, right) => right.updatedAt.localeCompare(left.updatedAt))[0]
        const lastUsableSync = [...storeSync].filter(job => job.state === 'succeeded' || job.state === 'partial').sort((left, right) => right.updatedAt.localeCompare(left.updatedAt))[0]
        const latestPublish = [...storePublish].sort((left, right) => right.createdAt.localeCompare(left.createdAt))[0]
        const storeNames = [...new Set(storeProducts.map(product => product.storeName).filter(Boolean))].sort()
        const platformStoreName = storeNames.length === 1 ? storeNames[0]! : storeNames.length > 1 ? `${storeNames[0]} 等 ${storeNames.length} 个店铺名` : `${platform} 店铺`
        const storeAlias = account?.storeAlias
        const storeName = storeAlias ?? platformStoreName
        const connectionState = account?.tokenState ?? 'account_not_registered'
        const simulated = fixtureMode || storeProducts.some(product => product.source === 'fixture')
        const officialReadable = !simulated && account?.tokenState === 'connected' && connectorRuntime.canRead(platform)
        const dataMode = simulated ? 'fixture' : officialReadable ? 'official_api' : account ? 'account_record_only' : 'unavailable'
        const readable = Boolean(account && account.tokenState === 'connected' && (simulated || officialReadable))
        const currentVersions = contentReviews.filter(item => {
          const task = taskById.get(item.version.taskId)
          return task?.contentVersionId === item.version.id && task.platform === platform && resolvedTaskAccount(task) === accountId
        })
        const blockingFindings = currentVersions.reduce((count, item) => count + item.findings.filter(finding => finding.severity === 'error').length, 0)

        if (account && account.tokenState !== 'connected') pushRisk({ key: `AUTH_RECONNECT:${platform}:${accountId}`, type: 'AUTH_RECONNECT', severity: 'high', platform, accountId, storeName, entityType: 'platform_account', entityId: accountId, title: '店铺授权需重新连接', status: account.tokenState, observedAt: account.revokedAt ?? account.tokenStateUpdatedAt, evidence: { tokenState: account.tokenState, accountRevision: account.authRevision ?? account.revision, dataMode }, nextAction: '在交互会话中重新发起官方授权' })
        if (latestSync && (latestSync.state === 'failed' || latestSync.state === 'partial')) pushRisk({ key: `SYNC_RISK:${platform}:${accountId}`, type: latestSync.state === 'failed' ? 'SYNC_FAILED' : 'SYNC_PARTIAL', severity: latestSync.state === 'failed' ? 'high' : 'medium', platform, accountId, storeName, entityType: 'sync_job', entityId: latestSync.id, title: latestSync.state === 'failed' ? '最新商品同步失败' : '最新商品同步部分失败', status: latestSync.state, observedAt: latestSync.updatedAt, evidence: { jobId: latestSync.id, itemsFailed: latestSync.itemsFailed, pages: latestSync.pages }, nextAction: '在交互会话中查看失败项并确认是否重试' })
        for (const product of storeProducts) {
          if (product.stock <= 10) pushRisk({ key: `LOW_STOCK:${product.id}`, type: 'LOW_STOCK', severity: 'high', platform, accountId, storeName, entityType: 'product', entityId: product.id, title: product.title, status: 'low_stock', observedAt: product.updatedAt, evidence: { stock: product.stock, threshold: 10 }, nextAction: '确认补货、下架或调整推广计划' })
          if (!product.images?.length) pushRisk({ key: `MISSING_IMAGES:${product.id}`, type: 'MISSING_IMAGES', severity: 'high', platform, accountId, storeName, entityType: 'product', entityId: product.id, title: product.title, status: 'missing', observedAt: product.updatedAt, evidence: { imageCount: 0 }, nextAction: '在交互会话中补齐主图并完成权益与安全审查' })
        }
        for (const item of currentVersions) {
          const errors = item.findings.filter(finding => finding.severity === 'error')
          if (!errors.length) continue
          const task = taskById.get(item.version.taskId)!
          pushRisk({ key: `CONTENT_BLOCKING:${item.version.id}`, type: 'CONTENT_BLOCKING', severity: 'high', platform, accountId, storeName, entityType: 'content_version', entityId: item.version.id, title: `任务 ${task.id} 存在内容阻断`, status: item.version.state, evidence: { taskId: task.id, findingCount: errors.length, findingCodes: [...new Set(errors.map(finding => finding.code))].sort() }, nextAction: '在交会话中修正内容并重新审核' })
        }
        for (const job of storePublish.filter(job => latestPublishByTask.get(job.taskId)?.id === job.id)) {
          const state = job.remoteState ?? job.state
          if (state !== 'rejected' && state !== 'unknown' && state !== 'manual_attention') continue
          const rejected = state === 'rejected'
          pushRisk({ key: `PUBLISH_STATUS:${job.taskId}`, type: rejected ? 'PUBLISH_REJECTED' : 'PUBLISH_UNKNOWN', severity: 'high', platform, accountId, storeName, entityType: 'publish_job', entityId: job.id, title: rejected ? '平台发布被驳回' : '平台发布状态未知', status: state, observedAt: job.remoteObservedAt ?? job.createdAt, evidence: rejected ? { taskId: job.taskId, jobId: job.id, rawCode: job.rejection?.rawCode, fields: job.rejection?.fields.map(field => ({ path: field.path, rawCode: field.rawCode, message: field.message })) ?? [] } : { taskId: job.taskId, jobId: job.id }, nextAction: rejected ? '在交互会话中根据驳回原因创建修正版，重新审核和确认' : '在交互会话中核对平台回执，不要自动重发' })
        }

        return {
          platform, accountId, storeName, storeAlias: storeAlias ?? null, platformStoreName, storeNames,
          connection: { state: connectionState, dataMode, readable, simulated },
          authorization: account ? publicAuthorization(account, simulated) : null,
          product: { total: storeProducts.length, onSale: storeProducts.filter(product => product.listingStatus === 'on_sale').length, lowStock: storeProducts.filter(product => product.stock <= 10).length, missingImages: storeProducts.filter(product => !product.images?.length).length, mappingWarnings: storeProducts.filter(product => product.mappingWarnings?.length).length, unconfirmedFacts: storeProducts.filter(product => !product.factsConfirmed).length, lastUpdatedAt: [...storeProducts].sort((left, right) => right.updatedAt.localeCompare(left.updatedAt))[0]?.updatedAt ?? null },
          sync: { total: periodSync.length, succeeded: periodSync.filter(job => job.state === 'succeeded').length, partial: periodSync.filter(job => job.state === 'partial').length, failed: periodSync.filter(job => job.state === 'failed').length, latestJobId: latestSync?.id ?? null, latestState: latestSync?.state ?? null, latestAt: latestSync?.updatedAt ?? null, lastAttemptAt: latestSync?.updatedAt ?? null, lastSuccessfulAt: lastSuccessfulSync?.updatedAt ?? null, lastUsableAt: lastUsableSync?.updatedAt ?? null, failedItems: latestSync?.itemsFailed ?? 0 },
          tasks: { created: periodTasks.length, byState: Object.fromEntries([...new Set(periodTasks.map(task => task.state))].sort().map(state => [state, periodTasks.filter(task => task.state === state).length])) },
          publish: { total: periodPublish.length, submitted: periodPublish.filter(job => job.remoteState === 'submitted' || job.state === 'submitted').length, published: periodPublish.filter(job => job.remoteState === 'published' || job.state === 'published').length, rejected: periodPublish.filter(job => job.remoteState === 'rejected' || job.state === 'rejected').length, unknown: periodPublish.filter(job => job.remoteState === 'unknown' || job.state === 'unknown' || job.state === 'manual_attention').length, latestJobId: latestPublish?.id ?? null, latestState: latestPublish ? latestPublish.remoteState ?? latestPublish.state : null, latestAt: latestPublish ? latestPublish.remoteObservedAt ?? latestPublish.createdAt : null },
          quality: { blockingFindings },
        }
      })

      // Local imports and legacy snapshots can legitimately exist before a
      // platform account is bound. Keep their risks visible, but never place
      // them in stores[] or imply that they came from a real authorized shop.
      for (const product of products.filter(item => !item.accountId)) {
        if (product.stock <= 10) pushRisk({ key: `LOW_STOCK:${product.id}`, type: 'LOW_STOCK', severity: 'high', platform: product.platform, entityType: 'product', entityId: product.id, title: product.title, status: 'low_stock', observedAt: product.updatedAt, evidence: { stock: product.stock, threshold: 10, unboundLocalData: true }, nextAction: '先在交互会话中确认商品所属店铺，再处理补货、下架或推广计划' })
        if (!product.images?.length) pushRisk({ key: `MISSING_IMAGES:${product.id}`, type: 'MISSING_IMAGES', severity: 'high', platform: product.platform, entityType: 'product', entityId: product.id, title: product.title, status: 'missing', observedAt: product.updatedAt, evidence: { imageCount: 0, unboundLocalData: true }, nextAction: '先在交互会话中确认商品所属店铺，再补齐主图并完成权益与安全审查' })
      }
      for (const item of contentReviews) {
        const task = taskById.get(item.version.taskId)
        if (!task || task.contentVersionId !== item.version.id || resolvedTaskAccount(task)) continue
        const errors = item.findings.filter(finding => finding.severity === 'error')
        if (!errors.length) continue
        pushRisk({ key: `CONTENT_BLOCKING:${item.version.id}`, type: 'CONTENT_BLOCKING', severity: 'high', platform: task.platform, entityType: 'content_version', entityId: item.version.id, title: `任务 ${task.id} 存在内容阻断`, status: item.version.state, evidence: { taskId: task.id, findingCount: errors.length, findingCodes: [...new Set(errors.map(finding => finding.code))].sort(), unboundLocalData: true }, nextAction: '先在交互会话中确认任务所属店铺，再修正内容并重新审核' })
      }
      for (const job of publishJobs.filter(item => !resolvedPublishAccount(item) && latestPublishByTask.get(item.taskId)?.id === item.id)) {
        const state = job.remoteState ?? job.state
        if (state !== 'rejected' && state !== 'unknown' && state !== 'manual_attention') continue
        const rejected = state === 'rejected'
        pushRisk({ key: `PUBLISH_STATUS:${job.taskId}`, type: rejected ? 'PUBLISH_REJECTED' : 'PUBLISH_UNKNOWN', severity: 'high', platform: job.platform, entityType: 'publish_job', entityId: job.id, title: rejected ? '未绑定店铺的发布被驳回' : '未绑定店铺的发布状态未知', status: state, observedAt: job.remoteObservedAt ?? job.createdAt, evidence: rejected ? { taskId: job.taskId, jobId: job.id, rawCode: job.rejection?.rawCode, fields: job.rejection?.fields.map(field => ({ path: field.path, rawCode: field.rawCode, message: field.message })) ?? [], unboundLocalData: true } : { taskId: job.taskId, jobId: job.id, unboundLocalData: true }, nextAction: '先在交互会话中确认发布所属店铺并核对平台回执，不要自动重发' })
      }

      const severityOrder = { high: 0, medium: 1 } as const
      riskItems.sort((left, right) => severityOrder[left.severity] - severityOrder[right.severity] || left.key.localeCompare(right.key))
      const metricDates = [...products.map(product => product.updatedAt), ...syncJobs.map(job => job.updatedAt), ...tasks.map(task => task.createdAt), ...publishJobs.map(job => job.remoteObservedAt ?? job.createdAt)].filter(Boolean).sort()
      const unboundProducts = products.filter(product => !product.accountId)
      const unboundTasks = tasks.filter(task => !resolvedTaskAccount(task))
      const unboundPublish = publishJobs.filter(job => !resolvedPublishAccount(job))
      const operationalStores = stores.map(({ platform, accountId, connection, product, sync, tasks: storeTasks, publish, quality }) => ({ platform, accountId, connection, product, sync, tasks: storeTasks, publish, quality }))
      const snapshotHash = createHash('sha256').update(canonicalJson({ stores: operationalStores, risks: riskItems.map(item => ({ key: item.key, status: item.status, observedAt: item.observedAt, evidence: item.evidence })), unbound: { products: unboundProducts.map(item => item.id).sort(), tasks: unboundTasks.map(item => item.id).sort(), publishJobs: unboundPublish.map(item => item.id).sort() } })).digest('hex')
      const warnings = [
        '指标来自当前工作区可持久化业务状态；平台外部吞吐和最终审核不在本地统计中，模型中转 usage/cost 以 model_usage_ledger 为准',
        'quality.recoveryRate 是历史兼容字段，实际表示可恢复失败任务占比；新客户端请使用 quality.recoverableTaskRate',
        '新增、升级和已恢复风险需由 Codex App 原生运行历史根据稳定 risk key 比较，当前响应不伪造历史基线',
      ]
      await persistenceReady
      const modelUsage = persistence.modelUsage ? await persistence.modelUsage.list(workspaceId, 1000) : []
      const modelChargeCny = modelUsage.reduce((sum, item) => sum + (item.customerChargeCny ?? 0), 0)
      const modelTokens = modelUsage.reduce((sum, item) => sum + (item.totalTokens ?? 0), 0)
      const storageReconciliation = redactStorageReconciliation(await persistence.storageReconciliation?.get(workspaceId))
      return result({
        generatedAt: new Date().toISOString(),
        source: metricsData.source,
        dataCompleteness: metricsData.dataCompleteness,
        hydration: metricsData.hydration,
        selection: { mode: selectedAccountId ? 'single_store' : selectedPlatform ? 'platform' : 'workspace', platform: selectedPlatform ?? null, accountId: selectedAccountId ?? null, alias: selectedAccount?.storeAlias ?? null, matchedStores: stores.length },
        period: { from: dateFrom ?? null, to: dateTo ?? null, activityFiltered: Boolean(dateFrom || dateTo), productRisksAreCurrentSnapshot: true },
        comparisonAvailable: false,
        comparisonReason: 'baseline_unavailable',
        dataCoverage: { firstObservedAt: metricDates[0] ?? null, lastObservedAt: metricDates.at(-1) ?? null, products: products.length, tasks: tasks.length, syncJobs: syncJobs.length, publishJobs: publishJobs.length, fixtureDataPresent: fixtureMode || products.some(product => product.source === 'fixture') },
        stores,
        unboundLocalData: { products: unboundProducts.length, tasks: unboundTasks.length, publishJobs: unboundPublish.length, risks: riskItems.filter(item => !item.accountId).length, note: '这些本地数据没有 accountId，不归入任何真实店铺聚合' },
        riskItems: riskItems.slice(0, riskLimit),
        riskSummary: { total: riskItems.length, returned: Math.min(riskItems.length, riskLimit), truncated: riskItems.length > riskLimit, limit: riskLimit },
        snapshotHash,
        productSummary: { total: products.length, lowStock: lowStockProducts.length, missingImages: missingImageProducts.length }, recommendations, platformMetrics,
        taskFunnel: Object.fromEntries([...new Set(tasks.map(task => task.state))].map(state => [state, tasks.filter(task => task.state === state).length])),
        quality: { p0FindingCount, recoverableTaskRate: tasks.length ? recoveredTasks / tasks.length : 0, recoveryRate: tasks.length ? recoveredTasks / tasks.length : 0, modelFailureRate: generationJobs.length ? generationJobs.filter(job => job.state === 'failed').length / generationJobs.length : 0 },
        jobs: { sync: syncJobs.length, generation: generationJobs.length, generationFailed: generationJobs.filter(job => job.state === 'failed').length, publish: publishJobs.length },
        cost: { available: true, source: 'model_usage_ledger', model_usage_records: modelUsage.length, total_tokens: modelTokens, model_charge_cny: modelChargeCny.toFixed(6), note: '仅展示当前工作区已结算的模型使用费；内部成本和定价策略不对商家端暴露' }, storageReconciliation, warnings,
      })
    }
    case 'billing.status': {
      await persistenceReady
      const balanceFen = persistence.billing ? await persistence.billing.balanceFen(workspaceId) : walletBalanceFen(workspaceId)
      const usage = persistence.usage ? await persistence.usage.get(workspaceId) : undefined
      const stores = await storeCapacity(workspaceId)
      const setup = setupDiagnostics()
      const directory = workspaceStoreDirectory(workspaceId)
      const actions = persistence.actionLedger ? await persistence.actionLedger.list(workspaceId, 1000) : []
      const pendingActions = actions.filter(item => ['authorized', 'pending_receipt', 'manual_attention'].includes(item.settlementStatus ?? ''))
      const pendingAuthorizationFen = pendingActions.reduce((sum, item) => sum + (item.reservedAmountFen ?? item.amountFen), 0)
      const billingRoles = authorizedRoles(requestPrincipals.get(req))
      const canViewWorkspaceBilling = !requiresStrictAuth() || billingRoles.some(role => ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'].includes(role))
      return result({
        currency: 'CNY',
        balance_cny: (balanceFen / 100).toFixed(2),
        available_balance_cny: (balanceFen / 100).toFixed(2),
        pending_authorization_cny: (pendingAuthorizationFen / 100).toFixed(2),
        wallet_total_cny: ((balanceFen + pendingAuthorizationFen) / 100).toFixed(2),
        settlement_pending_count: pendingActions.length,
        settlement_message: pendingAuthorizationFen > 0 ? `¥${(pendingAuthorizationFen / 100).toFixed(2)} 正在等待模型成本确认，暂未作为最终消费` : null,
        billing_mode: process.env.PAYMENT_MODE === 'provider' ? 'provider' : 'fixture',
        model_access: { ownership: 'platform', user_key_required: false, access_state: balanceFen > 0 && usage && usage.remainingTasks > 0 ? 'included_quota_available' : balanceFen > 0 ? 'wallet_overage_available' : 'recharge_required', message: balanceFen <= 0 ? '请先充值插件钱包，到账后开放生成、图片、OCR、SEO/GEO、视频和发布能力' : usage && usage.remainingTasks > 0 ? `当前套餐剩余 ${usage.remainingTasks} 次模型行动额度，优先消耗套餐额度` : '套餐额度已用尽，将从插件钱包余额扣除套餐外模型行动' },
        action_entitlement: usage ? { included_tasks: usage.includedTasks, used_tasks: usage.usedTasks, remaining_tasks: usage.remainingTasks, overage_policy: 'wallet' } : { overage_policy: 'wallet' },
        plugin_access: { unlocked: balanceFen > 0, balance_cny: (balanceFen / 100).toFixed(2), unlocks: ['内容生成', '图片生成', '图片/OCR解析', '创意Brief与预览', 'SEO/GEO标题', '视频请求', '发布任务'] },
        capability_entitlements: billingCapabilityEntitlements({ balanceFen, usage, setup, stores: directory }),
        recharge_channels: ['alipay', 'wechat'],
        store_capacity: { ...stores, upgrade_actions: ['升级套餐增加店铺数', '购买店铺加购包'], action_cards: commercialActionCards() },
        provider_ready: process.env.PAYMENT_MODE === 'provider' && paymentProviderReadiness().ready,
        next_actions: balanceFen <= 0 ? ['选择支付宝或微信创建充值订单'] : [],
        action_cards: billingActionCards(),
        viewer: { default_scope: 'mine', available_scopes: canViewWorkspaceBilling ? ['mine', 'workspace'] : ['mine'] },
      })
    }
    case 'billing.recharge.create': {
      const actorId = requestActor(req, 'actor_demo')
      const channel = params.channel === 'alipay' || params.channel === 'wechat' ? params.channel : undefined
      if (!channel) throw new DomainError('BILLING_CHANNEL_INVALID', '充值渠道必须是支付宝或微信', 400)
      const amountFen = parseCnyToFen(params.amount_cny)
      const idempotencyKey = typeof params.idempotency_key === 'string' && params.idempotency_key.trim() ? params.idempotency_key.trim() : `recharge-${workspaceId}-${actorId}-${randomUUID()}`
      const rechargeKey = `${workspaceId}:${idempotencyKey}`
      const existingId = rechargeIdempotency.get(rechargeKey)
      if (existingId) {
        const existing = rechargeOrders.get(existingId)
        if (!existing) rechargeIdempotency.delete(rechargeKey)
        else {
          if (existing.channel !== channel || existing.amountFen !== amountFen || existing.createdByActorId !== actorId) throw new DomainError('BILLING_ORDER_IDEMPOTENCY_CONFLICT', '充值订单幂等键已被其他支付意图使用', 409)
          return result(publicMoneyRecord(existing))
        }
      }
      const inFlight = rechargeCreationInFlight.get(rechargeKey)
      if (inFlight) {
        if (inFlight.channel !== channel || inFlight.amountFen !== amountFen || inFlight.actorId !== actorId) throw new DomainError('BILLING_ORDER_IDEMPOTENCY_CONFLICT', '充值订单幂等键已被其他支付意图使用', 409)
        return result(await inFlight.promise)
      }
      const providerMode = process.env.PAYMENT_MODE === 'provider'
      const creation = (async () => {
      if (persistence.billing) {
        const durableExisting = await persistence.billing.getOrderByIdempotencyKey(workspaceId, idempotencyKey)
        if (durableExisting) {
          if (durableExisting.channel !== channel || durableExisting.amountFen !== amountFen || durableExisting.paymentMode !== (providerMode ? 'provider' : 'fixture') || durableExisting.createdByActorId !== actorId) throw new DomainError('BILLING_ORDER_IDEMPOTENCY_CONFLICT', '充值订单幂等键已被其他支付意图使用', 409)
          return { ...publicMoneyRecord(durableExisting), warning: durableExisting.paymentMode === 'provider' ? '请完成支付，系统只接受支付服务商签名回调后入账' : '当前为本地 fixture，不会产生真实扣款' }
        }
      }
      const localFixturePayment = fixturePaymentAllowed()
      if (isProduction() && !providerMode && !localFixturePayment) throw new DomainError('PAYMENT_NOT_CONFIGURED', '生产环境未配置支付宝/微信支付服务商', 503)
      if (providerMode) {
        const readiness = paymentProviderReadiness()
        if (!readiness.ready) throw new DomainError('PAYMENT_NOT_CONFIGURED', `生产环境支付 provider 未就绪：${readiness.reasons.join(', ')}`, 503, { reasons: readiness.reasons })
      }
      // Keep the provider-side order id stable across retries of the same
      // idempotency key, preventing duplicate provider checkouts.
      const orderId = `recharge_${createHash('sha256').update(`${workspaceId}:${idempotencyKey}`).digest('hex').slice(0, 32)}`
      let paymentUrl: string
      if (providerMode) {
        if (!paymentProvider) throw new DomainError('PAYMENT_NOT_CONFIGURED', '支付 provider adapter 未装配', 503)
        const callbackBase = process.env.PAYMENT_CALLBACK_BASE_URL!.replace(/\/$/u, '')
        try {
          const checkout = await paymentProvider.createCheckout({ channel, orderId, idempotencyKey, workspaceId, amountFen, callbackUrl: `${callbackBase}/billing/callback/${channel}`, description: `merchant-marketing 插件钱包充值 ${amountFen} 分` })
          paymentUrl = checkout.paymentUrl
        } catch (error) {
          throw new DomainError('PAYMENT_PROVIDER_CHECKOUT_FAILED', error instanceof Error ? error.message : '支付服务商下单失败', 503)
        }
      } else paymentUrl = (await fixturePaymentProvider.createCheckout({ channel, orderId, idempotencyKey, workspaceId, amountFen, callbackUrl: `fixture://${workspaceId}/callback`, description: `merchant-marketing 插件钱包充值 ${amountFen} 分` })).paymentUrl
      const order: RechargeOrder = { id: orderId, workspaceId, channel, amountFen, state: 'pending', paymentMode: providerMode ? 'provider' : 'fixture', paymentUrl, createdByActorId: actorId, createdAt: new Date().toISOString(), updatedAt: new Date().toISOString() }
      if (persistence.billing) {
        const durable = await persistence.billing.createOrder({ ...order, idempotencyKey })
        return { ...publicMoneyRecord(durable), warning: providerMode ? '请完成支付，系统只接受支付服务商签名回调后入账' : '当前为本地 fixture，不会产生真实扣款' }
      }
      rechargeOrders.set(order.id, order)
      rechargeIdempotency.set(rechargeKey, order.id)
      return { ...publicMoneyRecord(order), warning: providerMode ? '请完成支付，系统只接受支付服务商签名回调后入账' : '当前为本地 fixture，不会产生真实扣款' }
      })()
      rechargeCreationInFlight.set(rechargeKey, { channel, amountFen, actorId, promise: creation })
      try {
        return result(await creation)
      } finally {
        if (rechargeCreationInFlight.get(rechargeKey)?.promise === creation) rechargeCreationInFlight.delete(rechargeKey)
      }
    }
    case 'billing.recharge.get': {
      await persistenceReady
      const { scope, actorId } = billingReadScope(req, params)
      const orderId = required(params, 'order_id')
      const order = persistence.billing ? await persistence.billing.getOrder(workspaceId, orderId) : rechargeOrders.get(orderId)
      if (!order || order.workspaceId !== workspaceId) throw new DomainError('BILLING_ORDER_NOT_FOUND', '充值订单不存在或不属于当前工作区', 404)
      if (scope === 'mine' && order.createdByActorId !== actorId) throw new DomainError('BILLING_ORDER_NOT_FOUND', '充值订单不存在或不属于当前用户', 404)
      if (params.confirm_test_payment === 'true') {
        if (!fixturePaymentAllowed() || order.paymentMode !== 'fixture') throw new DomainError('PAYMENT_TEST_MODE_DISABLED', '当前环境不允许确认测试支付；正式订单必须等待支付服务商回调', 403)
        const replayed = order.state === 'paid'
        const providerTradeId = `local-test-${order.id}`
        const paid = replayed ? order : await markRechargePaid({ workspaceId, orderId: order.id, providerTradeId, amountFen: order.amountFen, eventSource: 'local_test_checkout' })
        if (!paid) throw new DomainError('BILLING_ORDER_NOT_FOUND', '充值订单不存在或不属于当前工作区', 404)
        if (!replayed) {
          await recordOperationAudit({ workspaceId, actorId: 'local_test_checkout', action: 'billing.recharge.paid', resourceType: 'billing_order', resourceId: order.id, before: order as unknown as Record<string, unknown>, after: paid as unknown as Record<string, unknown>, reason: '本地测试收银台确认支付' })
        }
        return result({ ...publicMoneyRecord(paid), test_payment_confirmed: true, replayed })
      }
      let providerStatus: import('../../../packages/billing/src/payment-provider.js').PaymentStatusResult | undefined
      if (order.paymentMode === 'provider' && order.state === 'pending' && paymentProvider?.queryStatus) {
        try {
          providerStatus = await paymentProvider.queryStatus({ channel: order.channel, orderId: order.id, workspaceId })
          if (providerStatus.state === 'paid') {
            if (providerStatus.amountFen !== order.amountFen) throw new DomainError('PAYMENT_QUERY_AMOUNT_MISMATCH', '支付服务商查单金额缺失或与充值订单不一致', 409, { order_id: order.id, expected_amount_fen: order.amountFen, observed_amount_fen: providerStatus.amountFen ?? null })
            if (!providerStatus.providerTradeId) throw new DomainError('PAYMENT_QUERY_TRADE_ID_MISSING', '支付服务商已支付但未返回交易号，暂不入账', 409, { order_id: order.id })
            const paid = await markRechargePaid({ workspaceId, orderId: order.id, providerTradeId: providerStatus.providerTradeId, amountFen: order.amountFen, eventSource: 'provider_query' })
            if (paid) {
              await recordOperationAudit({ workspaceId, actorId: 'payment_provider_query', action: 'billing.recharge.paid', resourceType: 'billing_order', resourceId: order.id, before: order as unknown as Record<string, unknown>, after: paid as unknown as Record<string, unknown>, reason: '支付服务商查单确认到账' })
              return result({ ...publicMoneyRecord(paid), providerStatus })
            }
          }
        } catch (error) {
          if (error instanceof DomainError) throw error
          throw new DomainError('PAYMENT_PROVIDER_QUERY_FAILED', error instanceof Error ? error.message : '支付服务商查单失败', 503)
        }
      }
      return result({ ...publicMoneyRecord(order), ...(providerStatus ? { providerStatus } : {}) })
    }
    case 'billing.recharge.list': {
      const { scope, actorId } = billingReadScope(req, params)
      const allowedStates: RechargeState[] = ['pending', 'paid', 'closed', 'failed']
      const requestedStates = typeof params.states === 'string' && params.states.trim() ? [...new Set(params.states.split(',').map(value => value.trim()).filter(Boolean))] : allowedStates
      if (requestedStates.some(state => !allowedStates.includes(state as RechargeState))) throw new DomainError('BILLING_ORDER_STATE_INVALID', '充值订单状态只能是 pending、paid、closed 或 failed', 400)
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(100, Math.max(1, Number(params.limit))) : 100
      if (persistence.billing) {
        const [orders, summary] = await Promise.all([
          persistence.billing.listOrders(workspaceId, requestedStates as RechargeState[], limit, scope === 'mine' ? actorId : undefined),
          persistence.billing.countOrdersByState(workspaceId, scope === 'mine' ? actorId : undefined),
        ])
        const total = Object.values(summary).reduce((sum, count) => sum + count, 0)
        return result({ scope, orders: orders.map(publicRechargeOrder), summary, returned: orders.length, total, legacy_unattributed_hidden: scope === 'mine' })
      }
      const allOrders = [...rechargeOrders.values()]
        .filter(order => order.workspaceId === workspaceId)
        .sort((left, right) => right.createdAt.localeCompare(left.createdAt) || right.id.localeCompare(left.id))
      const scopedOrders = allOrders.filter(order => scope === 'workspace' || order.createdByActorId === actorId)
      const orders = scopedOrders.filter(order => requestedStates.includes(order.state)).slice(0, limit)
      const summary = Object.fromEntries(allowedStates.map(state => [state, scopedOrders.filter(order => order.state === state).length]))
      return result({ scope, orders: orders.map(publicRechargeOrder), summary, returned: orders.length, total: scopedOrders.length, legacy_unattributed_hidden: scope === 'mine' })
    }
    case 'billing.transactions': {
      const { scope, actorId } = billingReadScope(req, params)
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(100, Math.max(1, Number(params.limit))) : 20
      await persistenceReady
      if (persistence.billing) {
        const [balanceFen, transactions] = await Promise.all([persistence.billing.balanceFen(workspaceId), persistence.billing.listTransactions(workspaceId, limit, scope === 'mine' ? actorId : undefined)])
        return result({ scope, wallet_scope: 'workspace', balance_cny: (balanceFen / 100).toFixed(2), transactions: transactions.map(publicMoneyRecord), legacy_unattributed_hidden: scope === 'mine' })
      }
      return result({ scope, wallet_scope: 'workspace', balance_cny: (walletBalanceFen(workspaceId) / 100).toFixed(2), transactions: walletTransactions.filter(item => item.workspaceId === workspaceId && (scope === 'workspace' || item.actorId === actorId)).slice(-limit).reverse().map(publicMoneyRecord), legacy_unattributed_hidden: scope === 'mine' })
    }
    case 'billing.refund': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance'])
      const orderId = required(params, 'order_id')
      const reason = required(params, 'reason')
      await persistenceReady
      const order = persistence.billing ? await persistence.billing.getOrder(workspaceId, orderId) : rechargeOrders.get(orderId)
      if (!order || order.workspaceId !== workspaceId) throw new DomainError('BILLING_ORDER_NOT_FOUND', '充值订单不存在', 404)
      if (order.paymentMode === 'provider' && order.state === 'paid') {
        requireProviderPaymentConfigured()
        if (!order.providerTradeId) throw new DomainError('PAYMENT_REFUND_PROVIDER_TRADE_MISSING', '订单缺少支付服务商交易号，不能退款', 409)
        if (!paymentProvider) throw new DomainError('PAYMENT_NOT_CONFIGURED', '支付 provider adapter 未装配', 503)
      }
      const reservation = await reserveRechargeRefund({ workspaceId, orderId, actorId, reason })
      if (reservation.completed) return result({ ...publicMoneyRecord(reservation), replayed: true })
      let providerRefundId = `fixture-refund:${order.id}`
      if (order.paymentMode === 'provider') {
        try {
          const providerRefund = await paymentProvider!.refund({ channel: order.channel, orderId: order.id, providerTradeId: order.providerTradeId!, workspaceId, amountFen: order.amountFen, reason })
          const state = providerRefund.state?.trim().toLowerCase()
          if (state && !['accepted', 'success', 'succeeded', 'completed'].includes(state)) throw new Error(`payment provider refund was not accepted: ${providerRefund.state}`)
          providerRefundId = providerRefund.providerRefundId
        } catch (error) {
          await releaseRechargeRefund({ workspaceId, orderId, reservationKey: reservation.orderId!, actorId, reason: error instanceof Error ? error.message : '支付服务商退款失败' })
          throw new DomainError('PAYMENT_PROVIDER_REFUND_FAILED', error instanceof Error ? error.message : '支付服务商退款失败', 503)
        }
      }
      const refund = await completeRechargeRefund({ workspaceId, orderId, reservationKey: reservation.orderId!, actorId, reason, providerRefundId })
      await recordOperationAudit({ workspaceId, actorId, action: 'billing.refund', resourceType: 'billing_order', resourceId: orderId, before: { state: order.state }, after: { ...refund, providerRefundId } as unknown as Record<string, unknown>, reason })
      return result({ ...publicMoneyRecord(refund), replayed: false })
    }
    case 'billing.reconciliation':
    case 'billing.model-usage.statement': {
      if (method === 'billing.reconciliation') requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
      const billingScope = method === 'billing.model-usage.statement' ? billingReadScope(req, params) : { scope: 'workspace' as const, actorId: requestActor(req, 'actor_demo') }
      const principal = requestPrincipals.get(req)
      const canViewProviderCosts = billingScope.scope === 'workspace' && authorizedRoles(principal).some(role => role === 'finance' || role === 'platform_ops')
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(100, Math.max(1, Number(params.limit))) : 100
      const parseStatementTime = (value: unknown) => { if (typeof value !== 'string' || !value.trim()) return undefined; const parsed = new Date(value); return Number.isFinite(parsed.getTime()) ? parsed.toISOString() : null }
      const fromAt = parseStatementTime(params.from_at)
      const toAt = parseStatementTime(params.to_at)
      if (fromAt === null || toAt === null || (fromAt && toAt && fromAt >= toAt)) throw new DomainError('MODEL_USAGE_STATEMENT_PERIOD_INVALID', 'from_at/to_at 必须是有效且递增的 ISO 时间', 400)
      const allTransactions = persistence.billing ? await persistence.billing.listTransactions(workspaceId, 10000, billingScope.scope === 'mine' ? billingScope.actorId : undefined) : walletTransactions.filter(item => item.workspaceId === workspaceId && (billingScope.scope === 'workspace' || item.actorId === billingScope.actorId)).sort((left, right) => right.createdAt.localeCompare(left.createdAt))
      const transactions = allTransactions.filter(item => (!fromAt || item.createdAt >= fromAt) && (!toAt || item.createdAt < toAt)).slice(0, limit)
      const candidateModelUsage = persistence.modelUsage ? await persistence.modelUsage.listForStatement(workspaceId, { ...(fromAt ? { fromAt } : {}), ...(toAt ? { toAt } : {}), ...(billingScope.scope === 'mine' ? { actorId: billingScope.actorId } : {}) }) : []
      // Model usage is attributed to the actor that authorized its action.
      // Keep the report workspace-scoped, then join the bounded action ledger
      // so older receipts remain reportable without duplicating identity data.
      const fullActionLedger = persistence.actionLedger ? await persistence.actionLedger.list(workspaceId, 1000) : []
      const actionLedger = billingScope.scope === 'mine' ? fullActionLedger.filter(item => item.actorId === billingScope.actorId) : fullActionLedger
      const actorByAction = new Map(actionLedger.map(item => [item.actionKey, item.actorId]))
      const actionByKey = new Map(actionLedger.map(item => [item.actionKey, item]))
      const modelUsage = billingScope.scope === 'mine' && persistence.mode === 'memory' ? candidateModelUsage.filter(item => item.actionId && actorByAction.get(item.actionId) === billingScope.actorId) : candidateModelUsage
      const walletTransactionsByOrder = new Map(allTransactions.filter(item => item.orderId).map(item => [item.orderId!, item]))
      const actionSummary = actionLedger.reduce((acc, item) => { const key = `${item.actionKind}:${item.settlement}:${item.settlementStatus ?? item.state}`; acc[key] = (acc[key] ?? 0) + 1; return acc }, {} as Record<string, number>)
      const modelUsageTotals = modelUsage.reduce((acc, item) => {
        acc.totalTokens += item.totalTokens ?? 0
        acc.costCny += item.costCny ?? 0
        acc.customerChargeCny += item.customerChargeCny ?? 0
        acc.byModality[item.modality] = (acc.byModality[item.modality] ?? 0) + 1
        return acc
      }, { totalTokens: 0, costCny: 0, customerChargeCny: 0, byModality: {} as Record<string, number> })
      const modelUsageByActor = modelUsage.reduce((groups, item) => {
        const actorId = billingScope.scope === 'mine' ? billingScope.actorId : item.actionId ? actorByAction.get(item.actionId) ?? 'unknown' : 'unknown'
        const current = groups.get(actorId) ?? { actor_id: actorId, record_count: 0, input_tokens: 0, output_tokens: 0, total_tokens: 0, provider_cost_cny: 0, customer_charge_cny: 0, unsettled_records: 0, by_modality: {} as Record<string, number> }
        current.record_count += 1
        current.input_tokens += item.inputTokens ?? 0
        current.output_tokens += item.outputTokens ?? 0
        current.total_tokens += item.totalTokens ?? 0
        current.provider_cost_cny += item.costCny ?? 0
        current.customer_charge_cny += item.customerChargeCny ?? 0
        if (!['settled', 'waived'].includes(item.settlementStatus)) current.unsettled_records += 1
        current.by_modality[item.modality] = (current.by_modality[item.modality] ?? 0) + 1
        groups.set(actorId, current)
        return groups
      }, new Map<string, { actor_id: string; record_count: number; input_tokens: number; output_tokens: number; total_tokens: number; provider_cost_cny: number; customer_charge_cny: number; unsettled_records: number; by_modality: Record<string, number> }>())
      const byActor = [...modelUsageByActor.values()].map(item => ({ ...item, provider_cost_cny: canViewProviderCosts ? item.provider_cost_cny.toFixed(6) : null, customer_charge_cny: item.customer_charge_cny.toFixed(6) }))
      const totals = transactions.reduce((acc, item) => { acc[item.type] = (acc[item.type] ?? 0) + item.amountFen; return acc }, {} as Record<string, number>)
      const balanceFen = persistence.billing ? await persistence.billing.balanceFen(workspaceId) : walletBalanceFen(workspaceId)
      const provider = paymentProviderReadiness()
      const unsettledModelUsage = modelUsage.filter(item => !['settled', 'waived'].includes(item.settlementStatus))
      const unknownActorCount = byActor.filter(item => item.actor_id === 'unknown').reduce((sum, item) => sum + item.record_count, 0)
      const walletMismatchCount = modelUsage.reduce((count, item) => {
        if (!item.actionId || item.customerChargeCny === undefined) return count
        const action = actionByKey.get(item.actionId)
        if (!action || (action.settlement !== 'wallet' && action.settlement !== 'wallet_overage') || !['settled', 'waived'].includes(item.settlementStatus)) return count
        const expectedFen = Math.max(1, Math.ceil(item.customerChargeCny * 100))
        const direct = walletTransactionsByOrder.get(item.actionId)
        const settlementDebit = walletTransactionsByOrder.get(`settlement:${item.actionId}`)
        const settlementRefund = walletTransactionsByOrder.get(`settlement-refund:${item.actionId}`)
        const actualFen = direct ? direct.amountFen + (settlementDebit?.type === 'debit' ? settlementDebit.amountFen : 0) - (settlementRefund?.type === 'refund' ? settlementRefund.amountFen : 0) : action.amountFen
        return actualFen === expectedFen ? count : count + 1
      }, 0)
      const orphanActionCount = modelUsage.filter(item => item.actionId && !actionByKey.has(item.actionId)).length
      const missingRunKeyCount = modelUsage.filter(item => !item.budgetRunKey?.trim()).length
      const budgetLinkMismatchCount = modelUsage.filter(item => item.lastError?.code === 'MODEL_USAGE_BUDGET_LINK_CONFLICT').length
      const externalProviderStatement = billingScope.scope === 'workspace'
        ? await externalProviderUsageStatement({ modelUsage, ...(fromAt ? { fromAt } : {}), ...(toAt ? { toAt } : {}) })
        : { status: 'not_applicable_personal_scope', source: 'workspace_provider_account', reason: '外部中转账单只支持工作区级对账；个人视图使用本地可归属用量台账' }
      const localReconciliationStatus = missingRunKeyCount > 0 || budgetLinkMismatchCount > 0 || unknownActorCount > 0 || walletMismatchCount > 0 || orphanActionCount > 0 ? 'needs_review' : unsettledModelUsage.length > 0 ? 'pending' : 'locally_consistent'
      const reconciliationStatus = localReconciliationStatus === 'needs_review' || externalProviderStatement.status === 'needs_review' ? 'needs_review' : localReconciliationStatus
      return result({ currency: 'CNY', statement: { from_at: fromAt ?? null, to_at: toAt ?? null, scope: billingScope.scope, wallet_scope: 'workspace', source: 'model_usage_ledger' }, balance_cny: (balanceFen / 100).toFixed(2), recharge_cny: ((totals.recharge ?? 0) / 100).toFixed(2), debit_cny: ((totals.debit ?? 0) / 100).toFixed(2), refund_cny: ((totals.refund ?? 0) / 100).toFixed(2), transaction_count: transactions.length, transactions: transactions.map(publicMoneyRecord), model_usage: { record_count: modelUsage.length, total_tokens: modelUsageTotals.totalTokens, provider_cost_cny: canViewProviderCosts && billingScope.scope === 'workspace' ? modelUsageTotals.costCny.toFixed(6) : null, customer_charge_cny: modelUsageTotals.customerChargeCny.toFixed(6), unsettled_records: unsettledModelUsage.length, reconciliation_status: reconciliationStatus, reconciliation_checks: { unknown_actor_count: unknownActorCount, orphan_action_count: orphanActionCount, wallet_amount_mismatch_count: walletMismatchCount, missing_run_key_count: missingRunKeyCount, budget_link_mismatch_count: budgetLinkMismatchCount }, external_provider_statement: externalProviderStatement, by_actor: byActor, unsettled: billingScope.scope === 'workspace' ? unsettledModelUsage.slice(0, 100).map(item => ({ id: item.id, revision: item.revision, action_id: item.actionId ?? null, run_key: item.budgetRunKey ?? null, modality: item.modality, model: item.model, settlement_status: item.settlementStatus, allowed_decisions: allowedModelUsageSettlementDecisions(item), attempt_count: item.attemptCount, provider_request_id: canViewProviderCosts ? item.providerRequestId ?? null : null, observed_at: item.observedAt, next_attempt_at: item.nextAttemptAt ?? null, last_error: item.lastError ?? null, settlement_reason: typeof item.metadata?.settlement_reason === 'string' ? item.metadata.settlement_reason : item.settlementStatus })) : [], by_modality: modelUsageTotals.byModality }, action_ledger: { record_count: actionLedger.length, by_kind_settlement_state: actionSummary }, provider: { mode: process.env.PAYMENT_MODE === 'provider' ? 'provider' : 'fixture', ready: process.env.PAYMENT_MODE === 'provider' && provider.ready, reasons: provider.reasons } })
    }
    case 'billing.reconciliation.run': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'finance', 'platform_ops'])
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(100, Math.max(1, Number(params.limit))) : 50
      await persistenceReady
      if (!paymentProvider?.queryStatus) {
        if (isProduction()) throw new DomainError('PAYMENT_RECONCILIATION_UNAVAILABLE', '生产支付 provider 未配置查单能力', 503)
        return result({ state: 'not_configured', checked: 0, settled: [], pending: [], failed: [], next_action: '配置支付 provider query endpoint 后再运行对账' })
      }
      const orders = persistence.billing
        ? await persistence.billing.listOrders(workspaceId, ['pending'], limit)
        : [...rechargeOrders.values()].filter(order => order.workspaceId === workspaceId && order.state === 'pending').slice(0, limit)
      const settled: Array<{ order_id: string; provider_trade_id: string }> = []
      const pending: Array<{ order_id: string; state: string }> = []
      const failed: Array<{ order_id: string; code: string; message: string }> = []
      for (const order of orders.filter(item => item.paymentMode === 'provider')) {
        try {
          const providerStatus = await paymentProvider.queryStatus({ channel: order.channel, orderId: order.id, workspaceId })
          if (providerStatus.state !== 'paid') {
            if (providerStatus.state === 'closed' || providerStatus.state === 'failed') {
              const terminal = await markRechargeProviderState({ workspaceId, orderId: order.id, state: providerStatus.state })
              if (!terminal) {
                failed.push({ order_id: order.id, code: 'BILLING_ORDER_NOT_FOUND', message: '充值订单在对账期间不可见' })
                continue
              }
              const code = providerStatus.state === 'closed' ? 'PAYMENT_PROVIDER_ORDER_CLOSED' : 'PAYMENT_PROVIDER_ORDER_FAILED'
              await persistEvent(workspaceId, order.id, 'billing.recharge.reconciled', 1, { order_id: order.id, state: providerStatus.state, source: 'provider_reconciliation' })
              await recordOperationAudit({ workspaceId, actorId, action: 'billing.reconciliation.run', resourceType: 'billing_order', resourceId: order.id, before: order as unknown as Record<string, unknown>, after: terminal as unknown as Record<string, unknown>, reason: '支付服务商查单确认订单终态' })
              failed.push({ order_id: order.id, code, message: providerStatus.state === 'closed' ? '支付服务商已关闭该充值订单' : '支付服务商报告该充值订单失败' })
            } else {
              pending.push({ order_id: order.id, state: providerStatus.state })
            }
            continue
          }
          if (providerStatus.amountFen !== order.amountFen) {
            failed.push({ order_id: order.id, code: 'PAYMENT_QUERY_AMOUNT_MISMATCH', message: '支付服务商查单金额缺失或与充值订单不一致' })
            continue
          }
          if (!providerStatus.providerTradeId) {
            failed.push({ order_id: order.id, code: 'PAYMENT_QUERY_TRADE_ID_MISSING', message: '支付服务商已支付但未返回交易号' })
            continue
          }
          const paid = await markRechargePaid({ workspaceId, orderId: order.id, providerTradeId: providerStatus.providerTradeId, amountFen: order.amountFen, eventSource: 'provider_reconciliation' })
          if (!paid) {
            failed.push({ order_id: order.id, code: 'BILLING_ORDER_NOT_FOUND', message: '充值订单在对账期间不可见' })
            continue
          }
          await recordOperationAudit({ workspaceId, actorId, action: 'billing.reconciliation.run', resourceType: 'billing_order', resourceId: order.id, before: order as unknown as Record<string, unknown>, after: paid as unknown as Record<string, unknown>, reason: '运营人员执行支付服务商查单对账' })
          settled.push({ order_id: order.id, provider_trade_id: providerStatus.providerTradeId })
        } catch (error) {
          failed.push({ order_id: order.id, code: error instanceof Error && 'code' in error && typeof error.code === 'string' ? error.code : 'PAYMENT_RECONCILIATION_FAILED', message: error instanceof Error ? error.message : '支付服务商查单失败' })
        }
      }
      return result({ state: failed.length ? 'attention_required' : 'completed', checked: orders.length, provider_orders: orders.filter(item => item.paymentMode === 'provider').length, settled, pending, failed, actor_id: actorId, idempotent_settlement: true })
    }
    case 'billing.model-usage.reconciliation.run': {
      const actorId = requireOperationsRole(req, ['finance', 'platform_ops'])
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(100, Math.max(1, Number(params.limit))) : 50
      const reconciliation = await runModelUsageReconciliation({ workspaceId, actorId, limit })
      await recordOperationAudit({ workspaceId, actorId, action: 'billing.model-usage.reconciliation.run', resourceType: 'model_usage', resourceId: workspaceId, before: {}, after: reconciliation, reason: '运营人员执行模型用量结算重试' })
      return result(reconciliation)
    }
    case 'billing.model-usage.resolve': {
      const actorId = requireOperationsRole(req, ['finance', 'platform_ops'])
      if (!persistence.modelUsage) throw new DomainError('MODEL_USAGE_LEDGER_NOT_CONFIGURED', '模型用量结算台账未配置', 503)
      const usageId = required(params, 'usage_id')
      const revisionText = required(params, 'revision')
      if (!/^\d+$/u.test(revisionText)) throw new DomainError('MODEL_USAGE_REVISION_INVALID', 'revision 必须是正整数', 400)
      const revision = Number(revisionText)
      const decision = required(params, 'decision') as ModelUsageSettlementDecision
      if (!['retry', 'waive', 'manual_attention'].includes(decision)) throw new DomainError('MODEL_USAGE_DECISION_INVALID', 'decision 必须是 retry、waive 或 manual_attention', 400)
      const reason = required(params, 'reason').trim()
      const evidenceRef = required(params, 'evidence_ref').trim()
      const before = (await persistence.modelUsage.list(workspaceId, 1000)).find(item => item.id === usageId)
      if (!before) throw new DomainError('MODEL_USAGE_NOT_FOUND', '模型用量记录不存在', 404)
      const allowedDecisions = allowedModelUsageSettlementDecisions(before)
      if (!allowedDecisions.includes(decision)) throw new DomainError('MODEL_USAGE_DECISION_NOT_ALLOWED', '当前结算状态不允许该人工处理动作', 409, { settlement_status: before.settlementStatus, allowed_decisions: allowedDecisions })
      let after
      if (decision === 'retry') after = await settlePendingModelUsage({ workspaceId, usageId, actorId, expectedRevision: revision, reason, evidenceRef })
      else if (decision === 'waive') {
        if (before.actionId) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: before.actionId, actorId, reason: `模型成本缺失豁免：${reason}` })
        after = await persistence.modelUsage.resolve({ workspaceId, id: usageId, expectedRevision: revision, status: 'waived', actorId, reason, evidenceRef })
      } else if (decision === 'manual_attention') {
        if (before.actionId) await persistence.actionLedger?.transitionSettlementStatus({ workspaceId, actionKey: before.actionId, from: ['authorized', 'pending_receipt', 'manual_attention'], to: 'manual_attention' })
        after = await persistence.modelUsage.resolve({ workspaceId, id: usageId, expectedRevision: revision, status: 'manual_attention', actorId, reason, evidenceRef })
      }
      else throw new DomainError('MODEL_USAGE_DECISION_INVALID', 'decision 必须是 retry、waive 或 manual_attention', 400)
      await recordOperationAudit({ workspaceId, actorId, action: 'billing.model-usage.resolve', resourceType: 'model_usage', resourceId: usageId, before: before as unknown as Record<string, unknown>, after: after as unknown as Record<string, unknown>, reason })
      return result({ id: after.id, settlement_status: after.settlementStatus, allowed_decisions: allowedModelUsageSettlementDecisions(after), revision: after.revision, resolved_by: after.resolvedBy ?? null, resolution_reason: after.resolutionReason ?? null, resolution_evidence_ref: after.resolutionEvidenceRef ?? null, resolved_at: after.resolvedAt ?? null })
    }
    case 'billing.export': {
      const { scope, actorId } = billingReadScope(req, params, true)
      const limit = typeof params.limit === 'string' && /^\d+$/u.test(params.limit) ? Math.min(1000, Math.max(1, Number(params.limit))) : 1000
      const format = params.format === 'json' ? 'json' : 'csv'
      const parseExportTime = (value: unknown) => { if (typeof value !== 'string' || !value.trim()) return undefined; const parsed = new Date(value); return Number.isFinite(parsed.getTime()) ? parsed.toISOString() : null }
      const fromAt = parseExportTime(params.from_at)
      const toAt = parseExportTime(params.to_at)
      if (fromAt === null || toAt === null || (fromAt && toAt && fromAt >= toAt)) throw new DomainError('BILLING_EXPORT_PERIOD_INVALID', 'from_at/to_at 必须是有效且递增的 ISO 时间', 400)
      const allTransactions = persistence.billing ? await persistence.billing.listTransactions(workspaceId, limit, scope === 'mine' ? actorId : undefined) : walletTransactions.filter(item => item.workspaceId === workspaceId && (scope === 'workspace' || item.actorId === actorId)).slice(-limit).reverse()
      const transactions = allTransactions.filter(item => (!fromAt || item.createdAt >= fromAt) && (!toAt || item.createdAt < toAt))
      const rows = transactions.map(item => ({ id: item.id, type: item.type, amount_cny: (item.amountFen / 100).toFixed(2), order_id: item.orderId ?? '', description: item.description, created_at: item.createdAt }))
      // Keep the workspace identifier in the export name so downloaded files
      // remain attributable when operators export more than one tenant. The
      // personal export intentionally keeps its stable, user-facing name.
      const filenameBase = scope === 'mine' ? 'my-billing' : `billing-${workspaceId}`
      if (format === 'json') return result({ scope, filename: `${filenameBase}.json`, contentType: 'application/json', content: JSON.stringify(rows) })
      const content = ['id,type,amount_cny,order_id,description,created_at', ...rows.map(row => [row.id, row.type, row.amount_cny, row.order_id, row.description, row.created_at].map(csvCell).join(','))].join('\n')
      return result({ scope, filename: `${filenameBase}.csv`, contentType: 'text/csv; charset=utf-8', content })
    }
    case 'workspace.deactivate': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const reason = required(params, 'reason')
      const before = await getWorkspaceStatus(workspaceId)
      if (before !== 'disabled') {
        await setWorkspaceStatus(workspaceId, 'disabled')
        await persistEvent(workspaceId, `workspace:${workspaceId}`, 'workspace.deactivated', nextWorkspaceEventSequence(workspaceId), { workspace_id: workspaceId, reason })
      }
      await recordOperationAudit({ workspaceId, actorId, action: 'workspace.deactivate', resourceType: 'workspace', resourceId: workspaceId, before: { status: before }, after: { status: 'disabled', dataRetained: true }, reason })
      return result({ workspaceId, status: 'disabled', dataRetained: true, reason })
    }
    case 'workspace.activate': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const reason = required(params, 'reason').trim()
      const before = await getWorkspaceStatus(workspaceId)
      if (before !== 'active') {
        await setWorkspaceStatus(workspaceId, 'active')
        await persistEvent(workspaceId, `workspace:${workspaceId}`, 'workspace.activated', nextWorkspaceEventSequence(workspaceId), { workspace_id: workspaceId, dataRetained: true, reason })
      }
      await recordOperationAudit({ workspaceId, actorId, action: 'workspace.activate', resourceType: 'workspace', resourceId: workspaceId, before: { status: before }, after: { status: 'active', dataRetained: true }, reason })
      return result({ workspaceId, status: 'active', dataRetained: true, reason })
    }
    case 'workspace.data.delete.request': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin'])
      const scope = required(params, 'scope') as DataDeletionScope
      if (!['workspace', 'assets', 'business'].includes(scope)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '删除范围无效', 400)
      const reason = requiredOperationalReason(params)
      const gracePeriodDays = Number(process.env.DELETION_REQUEST_GRACE_DAYS ?? 7)
      if (!Number.isInteger(gracePeriodDays) || gracePeriodDays < 7 || gracePeriodDays > 30) throw new DomainError('DATA_LIFECYCLE_NOT_CONFIGURED', '生产删除宽限期未配置为 7 到 30 天', 503)
      let request
      try {
        request = await (persistence.dataLifecycle ?? memoryDataLifecycle).request({ workspaceId, scope, reason, requestedBy: actorId, gracePeriodDays, idempotencyKey: required(params, 'idempotency_key') })
      } catch (error) {
        if ((error as { code?: string })?.code === 'DATA_DELETION_IDEMPOTENCY_CONFLICT' || String(error).includes('DATA_DELETION_IDEMPOTENCY_CONFLICT')) throw new DomainError('DATA_DELETION_IDEMPOTENCY_CONFLICT', '删除幂等键已绑定到另一份申请，请换用新的幂等键', 409, { idempotency_key: required(params, 'idempotency_key') })
        throw error
      }
      await recordOperationAudit({ workspaceId, actorId, action: 'data.delete.request', resourceType: 'data_deletion_request', resourceId: request.id, before: {}, after: request as unknown as Record<string, unknown>, reason: request.reason })
      return result({ ...request, execution: 'pending_external_approval', message: '删除申请已登记；宽限期和双人审批完成前不会删除数据。' })
    }
    case 'platform.store.list': {
      const platform = typeof params.platform === 'string' && SUPPORTED_PLATFORMS.includes(params.platform as Platform) ? params.platform as Platform : undefined
      return result({ items: platformAccountAccessItems(workspaceId, platform) })
    }
    case 'platform.connect': {
      const platform = required(params, 'platform') as Platform
      await requireStoreCapacity(workspaceId)
      const platformEntitlementKey = `platform-connect:${workspaceId}:${platform}:${typeof params.store_key === 'string' ? params.store_key.trim() : 'default'}`
      // Authorization is the activation/readiness step. It remains available
      // before wallet recharge; an optional platform add-on can still settle
      // the action when present, but it must not gate first-run onboarding.
      const platformEntitlement = await consumeEntitlement({ workspaceId, kind: 'platform', actionKey: platformEntitlementKey, actionKind: 'platform_connect', actorId: requestActor(req), description: '平台连接权益（可选加购）' })
      let authorization: Awaited<ReturnType<typeof beginPlatformAuthorization>>
      try {
        authorization = await beginPlatformAuthorization(req, platform, params, workspaceId)
      } catch (error) {
        if (platformEntitlement) await refundEntitlement({ workspaceId, actionKey: platformEntitlementKey, reason: '平台授权入口创建失败' })
        throw error
      }
      if (!fixtureMode || authorization.ok !== true) return result(authorization)
      // Explicit local演练 shortcut: production still requires the official OAuth
      // callback and never creates an account from an authorization URL alone.
      // Exchange a disposable fixture code as well as updating the account
      // snapshot. This resets a previously revoked fake credential, matching a
      // real reauthorization instead of merely changing local account state.
      const fixtureCredential = await connectorRuntime.connector(platform).exchangeCode({ code: `fixture-code-${platform}`, state: `fixture-state-${workspaceId}-${platform}`, workspaceId })
      const storeKey = typeof params.store_key === 'string' && /^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$/u.test(params.store_key.trim()) ? params.store_key.trim() : undefined
      const account = service.registerPlatformAccount({
        workspaceId,
        platform,
        remoteAccountId: `fixture-store-${workspaceId}-${platform}${storeKey ? `-${storeKey}` : ''}`,
        credentialRef: fixtureCredential.credentialRef,
        grantedScopes: grantedScopes(fixtureCredential.scope),
        accessTokenExpiresAt: fixtureCredential.expiresAt,
        credentialRefreshable: fixtureCredential.refreshable,
      })
      await persistSnapshot(workspaceId, 'platform_account', account, account as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, account.id, 'platform.connected', account.revision, { platform, account_id: account.id, simulated: true })
      const store = workspaceStoreDirectory(workspaceId, platform).find(item => item.accountId === account.id)
      return result({
        ...authorization,
        simulated: true,
        account: {
          id: account.id,
          workspaceId: account.workspaceId,
          platform: account.platform,
          tokenState: account.tokenState,
          authRevision: account.authRevision,
          createdAt: account.createdAt,
          revision: account.revision,
        },
        store,
      })
    }
    case 'platform.store.alias.set': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const platform = required(params, 'platform') as Platform
      const accountId = required(params, 'account_id')
      const expectedRevision = Number(required(params, 'expected_revision'))
      if (!Number.isInteger(expectedRevision) || expectedRevision < 1) throw new DomainError('STORE_ALIAS_VERSION_INVALID', 'expected_revision 必须是正整数', 400)
      const account = service.setPlatformAccountAlias({ workspaceId, platform, accountId, alias: required(params, 'alias'), expectedRevision })
      await persistSnapshot(workspaceId, 'platform_account', account, account as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, account.id, 'platform_account.alias_changed', account.revision, { platform, account_id: account.id, alias: account.storeAlias })
      const store = workspaceStoreDirectory(workspaceId, platform).find(item => item.accountId === account.id)
      return result({ store, selectionKey: { platform, accountId: account.id } })
    }
    case 'catalog.search': {
      const scope = params.scope === 'workspace' ? 'workspace' : params.scope === 'store' ? 'store' : undefined
      const platform = typeof params.platform === 'string' ? params.platform as Platform : undefined
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      if (scope === 'store' && (!platform || !accountId)) throw new DomainError('STORE_SELECTION_REQUIRED', '查看指定店铺商品时，请同时选择平台和店铺', 409, { next_actions: ['选择一个平台店铺', '或明确使用全部店铺只读汇总'] })
      if (accountId && !platform) throw new DomainError('STORE_PLATFORM_REQUIRED', '使用 account_id 查询商品时必须同时指定 platform', 400)
      if (scope !== 'workspace' && (!platform || !accountId)) throw new DomainError('STORE_SELECTION_REQUIRED', '请先选择要查看的具体平台店铺；如需汇总全部店铺，请明确传入 scope=workspace', 409, { next_actions: ['调用 workspace.health 查看店铺列表', '选择 platform + account_id', '或明确使用 scope=workspace 查看全部店铺'] })
      if (scope === 'workspace' && accountId) throw new DomainError('CATALOG_SCOPE_CONFLICT', '全部店铺汇总不能同时指定 account_id，请改用 scope=store', 400)
      if (accountId) service.getPlatformAccount(workspaceId, accountId, platform)
      const directory = new Map(workspaceStoreDirectory(workspaceId).map(store => [`${store.platform}:${store.accountId}`, store]))
      const pageRequest = mcpPagination(params)
      const accessibleBrandIds = await accessibleTaskBrandIds(req, workspaceId)
      const filters = {
        ...(typeof params.query === 'string' ? { query: params.query } : {}),
        ...(platform ? { platform } : {}),
        ...(accountId ? { accountId } : {}),
        ...(typeof params.store_name === 'string' ? { storeName: params.store_name } : {}),
        ...(typeof params.brand_name === 'string' ? { brandName: params.brand_name } : {}),
        ...(typeof params.sku_id === 'string' ? { skuId: params.sku_id } : {}),
        ...(typeof params.remote_product_id === 'string' ? { remoteProductId: params.remote_product_id } : {}),
        ...(typeof params.listing_status === 'string' ? { listingStatus: params.listing_status as import('../../../packages/application/src/service.js').Product['listingStatus'] } : {}),
        ...(typeof params.product_state === 'string' ? { productState: params.product_state as 'active' | 'disabled' } : {}),
        ...(typeof params.sync_status === 'string' ? { syncStatus: params.sync_status as import('../../../packages/application/src/service.js').SyncJobState } : {}),
        ...(typeof params.date_from === 'string' ? { dateFrom: params.date_from } : {}),
        ...(typeof params.date_to === 'string' ? { dateTo: params.date_to } : {}),
      }
      const page = persistence.business
        ? await persistence.business.listProductsPage(workspaceId, { ...pageRequest, ...filters, ...(accessibleBrandIds !== undefined ? { accessibleBrandIds } : {}) })
        : await (async () => { const accessibleIds = await accessibleProductIds(req, workspaceId); const all = service.listProducts(workspaceId, filters).filter(product => accessibleIds === undefined || accessibleIds.has(product.id)).sort((left, right) => right.updatedAt.localeCompare(left.updatedAt) || left.id.localeCompare(right.id)); return { items: all.slice(pageRequest.offset, pageRequest.offset + pageRequest.limit), total: all.length, ...pageRequest } })()
      const canonicalRepository = persistence.brandUnits ?? memoryBrandUnits
      const readControl = await canonicalProductReadControl(workspaceId)
      const products = await Promise.all((page.items as unknown as Product[]).map(async product => {
        const canonicalCandidates = (await canonicalRepository.listCanonicalProducts({ workspaceId })).filter(row => row.sourceProductId === product.id)
        const canonical = canonicalCandidates.length === 1 ? canonicalCandidates[0] : undefined
        const listings = canonical ? await canonicalRepository.listListings({ workspaceId, brandId: canonical.brandId, canonicalProductId: canonical.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}) }) : []
        const verificationStatus = canonicalCandidates.length > 1 ? 'conflict' : !canonical ? 'legacy_only' : listings.length === 1 ? 'verified' : 'blocked'
        return { ...product, product_id: product.id, storeContext: product.accountId ? directory.get(`${product.platform}:${product.accountId}`) ?? { platform: product.platform, accountId: product.accountId } : null, canonical_scope: { verification_status: verificationStatus, read_mode: readControl.mode, canonical_product_id: canonical?.id ?? null, brand_id: canonical?.brandId ?? null, listing_id: listings.length === 1 ? listings[0]!.id : null, listing_count: listings.length, next_action: verificationStatus === 'verified' ? null : 'canonical.product.consistency' } }
      }))
      const product_actions = products.map(product => {
        const base = { product_id: product.id, title: product.title, platform: product.platform, account_id: product.accountId ?? null, facts_confirmed: product.factsConfirmed }
        if (!product.accountId) return { ...base, action: { method: 'platform.connect', label: '绑定商品所属店铺', required_inputs: ['platform'], confirmation: 'interactive_confirmation' } }
        if (!product.factsConfirmed) return { ...base, action: { method: 'catalog.facts.confirm', label: '确认商品、SKU、价格和图片事实', required_inputs: ['product_id'], confirmation: 'interactive_confirmation' } }
        if (product.canonical_scope.verification_status !== 'verified') return { ...base, action: { method: 'canonical.product.consistency', label: '检查标准商品链', required_inputs: [], confirmation: 'none', reason: product.canonical_scope.verification_status } }
        return { ...base, action: null, next_step: '商品事实已确认，可创建内容任务' }
      })
      return result({
        scope: scope === 'workspace' ? 'workspace' : 'store',
        selection: accountId && platform ? { platform, accountId } : null,
        products,
        product_actions,
        total: page.total,
        limit: page.limit,
        offset: page.offset,
        emptyState: products.length ? null : {
          title: '暂时没有找到商品',
          reason: scope === 'workspace' ? '当前工作区还没有可见商品，或筛选条件没有匹配结果' : '该店铺还没有同步或导入商品，或筛选条件没有匹配结果',
          nextActions: scope === 'workspace' ? ['选择具体店铺后重新查询', '同步或导入商品'] : ['同步当前店铺商品', '导入商品资料', '更换筛选条件'],
        },
      })
    }
    case 'catalog.categories': {
      const query = typeof params.query === 'string' ? params.query.trim().toLocaleLowerCase() : ''
      return result((query ? catalogCategories.filter(item => `${item.code}${item.name}${item.fields.join('')}`.toLocaleLowerCase().includes(query)) : catalogCategories).map(item => ({ ...item, category_code: item.code, required_fields: item.fields })))
    }
    case 'catalog.title.optimize': {
      await requirePluginWalletAccess(workspaceId)
      const productId = required(params, 'product_id')
      const product = service.products.get(productId)
      if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
      await enforceProductBrandAccess(req, workspaceId, product.id)
      const requestedPlatform = typeof params.platform === 'string' ? params.platform as Platform : product.platform
      if (!SUPPORTED_PLATFORMS.includes(requestedPlatform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 无效', 400)
      if (requestedPlatform !== product.platform) throw new DomainError('TITLE_PLATFORM_SCOPE_MISMATCH', '标题优化的平台必须与当前商品平台一致', 409, { product_platform: product.platform, requested_platform: requestedPlatform })
      const readControl = await canonicalProductReadControl(workspaceId)
      const canonicalRepository = persistence.brandUnits ?? memoryBrandUnits
      const canonicalCandidates = (await canonicalRepository.listCanonicalProducts({ workspaceId })).filter(row => row.sourceProductId === product.id)
      const canonical = canonicalCandidates.length === 1 ? canonicalCandidates[0] : undefined
      const canonicalListings = canonical
        ? await canonicalRepository.listListings({ workspaceId, brandId: canonical.brandId, canonicalProductId: canonical.id, platform: requestedPlatform, ...(product.accountId ? { accountId: product.accountId } : {}) })
        : []
      const canonicalScope = resolveCanonicalProductReadScope({
        mode: readControl.mode,
        workspaceId,
        platform: requestedPlatform,
        ...(product.accountId ? { accountId: product.accountId } : {}),
        candidates: canonicalCandidates,
        listings: canonicalListings,
      })
      if (canonicalScope?.status === 'blocked') {
        throw new DomainError(canonicalScope.code, '标准商品链未完成验证，已阻断标题优化', 409, { reason: canonicalScope.reason, next_action: 'canonical.product.consistency', read_mode: readControl.mode })
      }
      const generationTitle = canonicalScope?.status === 'verified' ? canonicalScope.title : product.title
      const generationFacts = canonicalScope?.status === 'verified' ? canonicalScope.facts : undefined
      const rulePreflight = await requireGenerationRulePreflight(workspaceId, product.id, '标题优化前平台规则校验未通过')
      requireRuleSafeGenerationText(rulePreflight, [generationTitle, params.keyword, params.objective])
      const suggestions = generateSeoGeoSuggestions({ platform: requestedPlatform, productId: product.id, title: generationTitle, ...(typeof generationFacts?.category === 'string' ? { category: generationFacts.category } : product.category ? { category: product.category } : {}), ...(generationFacts?.attributes && typeof generationFacts.attributes === 'object' ? { attributes: generationFacts.attributes as Record<string, string> } : product.attributes ? { attributes: product.attributes } : {}), ...(Array.isArray(generationFacts?.selling_points) ? { sellingPoints: generationFacts.selling_points.filter((item): item is string => typeof item === 'string') } : product.sellingPoints ? { sellingPoints: product.sellingPoints.filter(item => item.proofStatus === 'confirmed').map(item => item.text) } : {}), ...(typeof params.keyword === 'string' ? { keyword: params.keyword } : {}), ...(typeof params.objective === 'string' ? { objective: params.objective } : {}) })
      requireRuleSafeGenerationText(rulePreflight, [suggestions], '标题优化结果命中当前平台规则禁用表达')
      const seoKey = createHash('sha256').update(JSON.stringify({ productId: product.id, requestedPlatform, keyword: params.keyword ?? '', objective: params.objective ?? '' })).digest('hex')
      const seoDebitKey = `seo-geo:${seoKey}`
      const seoActor = requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
      await debitPluginWallet({ workspaceId, idempotencyKey: seoDebitKey, actorId: seoActor, description: 'SEO/GEO 标题建议调用' })
      try {
        await persistEvent(workspaceId, product.id, 'catalog.title.optimized', product.version ?? 1, { product_id: product.id, platform: requestedPlatform, suggestion_id: suggestions[0]?.id ?? null, ranking_guarantee: false })
      } catch (error) {
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: seoDebitKey, actorId: seoActor, reason: 'SEO/GEO 结果记录失败' })
        throw error
      }
      return result({ product_id: product.id, platform: requestedPlatform, suggestions, rule_preflight: rulePreflight, canonical_scope: canonicalScope?.status === 'verified' ? canonicalScope : null, humanConfirmationRequired: true, rankingGuarantee: false })
    }
    case 'catalog.title.accept': {
      const productId = required(params, 'product_id')
      const platform = required(params, 'platform') as Platform
      const suggestionId = required(params, 'suggestion_id')
      const title = required(params, 'title')
      const expectedVersion = typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined
      if (params.expected_version !== undefined && expectedVersion === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_version 必须是非负整数', 400)
      const readControl = await canonicalProductReadControl(workspaceId)
      if (readControl.mode === 'canonical_read') {
        const product = service.products.get(productId)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
        if (expectedVersion !== undefined && expectedVersion !== product.version) throw new DomainError('PRODUCT_VERSION_CONFLICT', '商品事实已变化，请刷新后再确认 SEO/GEO 标题', 409)
        if (!SUPPORTED_PLATFORMS.includes(platform)) throw new DomainError('PLATFORM_INVALID', 'SEO/GEO 标题的平台无效', 400)
        const expectedSuggestionId = `seo_geo_${product.id}_${platform}`
        if (suggestionId !== expectedSuggestionId) throw new DomainError('SEO_GEO_SUGGESTION_INVALID', 'SEO/GEO 建议已过期或不属于当前商品/平台，请重新生成', 409, { expected_suggestion_id: expectedSuggestionId })
        const trimmedTitle = title.trim()
        if (!trimmedTitle) throw new DomainError('PRODUCT_TITLE_REQUIRED', 'SEO/GEO 标题不能为空', 400)
        const canonicalRepository = persistence.brandUnits ?? memoryBrandUnits
        const candidates = (await canonicalRepository.listCanonicalProducts({ workspaceId })).filter(row => row.sourceProductId === product.id)
        if (candidates.length !== 1) throw new DomainError('CANONICAL_PRODUCT_MAPPING_REQUIRED', '标准商品映射不唯一，已阻断标题写回', 409, { product_id: product.id, next_action: 'canonical.product.consistency' })
        const canonical = candidates[0]!
        const listings = await canonicalRepository.listListings({ workspaceId, brandId: canonical.brandId, canonicalProductId: canonical.id, platform, ...(product.accountId ? { accountId: product.accountId } : {}) })
        if (listings.length !== 1 || !canonical.facts || Object.keys(canonical.facts).length === 0) throw new DomainError('CANONICAL_PRODUCT_SCOPE_INCOMPLETE', '标准商品的事实或唯一平台 listing 尚未验证，已阻断标题写回', 409, { product_id: product.id, canonical_product_id: canonical.id, listing_count: listings.length, next_action: 'canonical.product.consistency' })
        try {
          const updated = await canonicalRepository.updateCanonicalProductTitle({ workspaceId, id: canonical.id, title: trimmedTitle, expectedFactsVersion: canonical.factsVersion })
          await persistEvent(workspaceId, updated.id, 'catalog.title.accepted', updated.factsVersion, { product_id: product.id, canonical_product_id: updated.id, platform, suggestion_id: suggestionId, facts_confirmed: true, source: 'canonical' })
          return result({ product_id: product.id, canonical_product_id: updated.id, listing_id: listings[0]!.id, title: updated.title, factsConfirmationRequired: false, humanConfirmed: true, canonical_scope: { canonical_product_id: updated.id, brand_id: updated.brandId, listing_id: listings[0]!.id, facts_version: updated.factsVersion } })
        } catch (error) {
          if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_REVISION_CONFLICT') throw new DomainError('CANONICAL_PRODUCT_REVISION_CONFLICT', '标准商品标题已被其他操作更新，请刷新后重试', 409)
          if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_NOT_FOUND') throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', '标准商品不存在或不属于当前工作区', 404)
          throw error
        }
      }
      const product = service.acceptSeoGeoTitle({ workspaceId, productId, platform, suggestionId, title, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', ...(expectedVersion !== undefined ? { expectedVersion } : {}) })
      await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, product.id, 'catalog.title.accepted', product.version ?? 1, { product_id: product.id, platform, suggestion_id: suggestionId, facts_confirmed: false })
      return result({ ...product, product_id: product.id, factsConfirmationRequired: true, humanConfirmed: true })
    }
    case 'catalog.import': {
      const platform = required(params, 'platform') as Platform
      const numeric = (key: string) => typeof params[key] === 'string' && params[key]!.trim() ? Number(params[key]) : undefined
      const images = typeof params.images === 'string' && params.images.trim() ? params.images.split(',').map(item => item.trim()).filter(Boolean) : undefined
      let sourceAssetIds: string[] | undefined
      if (typeof params.asset_ids_json === 'string' && params.asset_ids_json.trim()) {
        try {
          const parsed = JSON.parse(params.asset_ids_json)
          if (!Array.isArray(parsed) || parsed.length > 50 || parsed.some(value => typeof value !== 'string' || !value.trim())) throw new Error('asset_ids_json')
          sourceAssetIds = [...new Set(parsed.map(value => value.trim()))]
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'asset_ids_json 必须是最多 50 个素材 ID 的字符串数组 JSON', 400) }
      }
      let skus: import('../../../packages/application/src/service.js').ProductSku[] | undefined
      if (typeof params.skus_json === 'string' && params.skus_json.trim()) {
        try {
          const parsed = JSON.parse(params.skus_json)
          if (!Array.isArray(parsed) || parsed.some(item => !item || typeof item !== 'object' || typeof item.id !== 'string' || typeof item.name !== 'string' || typeof item.price !== 'number' || typeof item.stock !== 'number')) throw new Error('skus_json')
          skus = parsed.map((item: Record<string, any>) => ({ id: item.id.trim(), name: item.name.trim(), price: item.price, stock: item.stock, ...(Array.isArray(item.images) ? { images: item.images.filter((value: unknown): value is string => typeof value === 'string') } : {}), ...(item.attributes && typeof item.attributes === 'object' && !Array.isArray(item.attributes) ? { attributes: Object.fromEntries(Object.entries(item.attributes).filter(([, value]) => typeof value === 'string').map(([key, value]) => [key, value as string])) } : {}) }))
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'skus_json 必须是包含 id、name、price、stock 的 SKU 数组', 400) }
      }
      let attributes: Record<string, string> | undefined
      if (typeof params.attributes_json === 'string' && params.attributes_json.trim()) {
        try {
          const parsed = JSON.parse(params.attributes_json)
          if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('attributes_json must be an object')
          attributes = Object.fromEntries(Object.entries(parsed).filter(([, value]) => typeof value === 'string').map(([key, value]) => [key, value as string]))
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'attributes_json 必须是 JSON 对象', 400) }
      }
      let sellingPoints: import('../../../packages/application/src/service.js').ProductSellingPoint[] | undefined
      if (typeof params.selling_points_json === 'string' && params.selling_points_json.trim()) {
        try {
          const parsed = JSON.parse(params.selling_points_json)
          if (!Array.isArray(parsed)) throw new Error('selling_points_json')
          sellingPoints = parsed.map((item: any, index: number) => ({ id: typeof item.id === 'string' ? item.id : `sp_${index + 1}`, text: typeof item.text === 'string' ? item.text : '', proofStatus: item.proof_status === 'confirmed' || item.proof_status === 'rejected' ? item.proof_status : 'pending', sourceIds: Array.isArray(item.source_ids) ? item.source_ids.filter((value: unknown): value is string => typeof value === 'string') : [] }))
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'selling_points_json 必须是卖点对象数组', 400) }
      }
      const price = numeric('price'); const skuCount = numeric('sku_count'); const stock = numeric('stock')
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      if (!SUPPORTED_PLATFORMS.includes(platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 无效', 400)
      if (isProduction() && !accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产商品导入必须绑定已授权平台账号', 400)
      if (accountId) service.getActivePlatformAccount(workspaceId, accountId, platform)
      const product = service.importProduct({
        workspaceId, platform, ...(accountId ? { accountId } : {}),
        ...(typeof params.remote_id === 'string' && params.remote_id.trim() ? { remoteId: params.remote_id } : {}),
        ...(typeof params.local_product_key === 'string' ? { localProductKey: params.local_product_key } : {}),
        title: required(params, 'title'),
        ...(skus ? { skus } : {}),
        ...(typeof price === 'number' && Number.isFinite(price) ? { price } : {}),
        ...(typeof skuCount === 'number' && Number.isFinite(skuCount) ? { skuCount } : {}),
        ...(typeof stock === 'number' && Number.isFinite(stock) ? { stock } : {}),
        ...(typeof params.category === 'string' && params.category.trim() ? { category: params.category } : {}),
        ...(images ? { images } : {}), ...(sourceAssetIds ? { sourceAssetIds } : {}), ...(attributes ? { attributes } : {}), ...(sellingPoints ? { sellingPoints } : {}),
        ...(typeof params.store_name === 'string' ? { storeName: params.store_name } : {}),
        ...(typeof params.store_differentiation === 'string' ? { storeDifferentiation: params.store_differentiation } : {}),
      })
      await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      return result({ ...product, product_id: product.id })
    }
    case 'catalog.import.batch': {
      let rawItems: unknown
      try {
        rawItems = JSON.parse(required(params, 'products_json'))
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'products_json 必须是商品对象数组 JSON', 400) }
      if (!Array.isArray(rawItems) || rawItems.length < 1 || rawItems.length > 50 || rawItems.some(item => !item || typeof item !== 'object' || Array.isArray(item))) {
        throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'products_json 必须是 1 至 50 个商品对象的 JSON 数组', 400)
      }
      type BatchImportItem = { platform: Platform; accountId?: string; remoteId?: string; localProductKey?: string; title: string; skuCount?: number; skus?: import('../../../packages/application/src/service.js').ProductSku[]; stock?: number; price?: number; category?: string; images?: string[]; sourceAssetIds?: string[]; attributes?: Record<string, string>; sellingPoints?: import('../../../packages/application/src/service.js').ProductSellingPoint[]; storeName?: string; storeDifferentiation?: string }
      const numeric = (value: unknown, field: string, index: number) => {
        if (value === undefined || value === null || value === '') return undefined
        const parsed = typeof value === 'number' ? value : typeof value === 'string' && value.trim() ? Number(value) : Number.NaN
        if (!Number.isFinite(parsed) || parsed < 0) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 ${field} 必须是非负数字`, 400)
        return parsed
      }
      const items: BatchImportItem[] = rawItems.map((raw, index) => {
        const item = raw as Record<string, unknown>
        const platform = typeof item.platform === 'string' ? item.platform as Platform : '' as Platform
        if (!SUPPORTED_PLATFORMS.includes(platform)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 platform 无效`, 400)
        const accountId = typeof item.account_id === 'string' && item.account_id.trim() ? item.account_id.trim() : undefined
        if (isProduction() && !accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', `第 ${index + 1} 项生产导入必须绑定已授权平台账号`, 400)
        if (accountId) service.getActivePlatformAccount(workspaceId, accountId, platform)
        const title = typeof item.title === 'string' ? item.title.trim() : ''
        if (!title) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 title 不能为空`, 400)
        const images = Array.isArray(item.images) ? item.images.filter((value): value is string => typeof value === 'string' && value.trim().length > 0).map(value => value.trim()) : typeof item.images === 'string' ? item.images.split(',').map(value => value.trim()).filter(Boolean) : undefined
        const sourceAssetIds = Array.isArray(item.asset_ids) ? item.asset_ids.filter((value): value is string => typeof value === 'string' && value.trim().length > 0).map(value => value.trim()) : undefined
        if (sourceAssetIds && (sourceAssetIds.length > 50 || new Set(sourceAssetIds).size !== sourceAssetIds.length)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 asset_ids 必须是最多 50 个不重复素材 ID`, 400)
        let skus: import('../../../packages/application/src/service.js').ProductSku[] | undefined
        if (item.skus !== undefined) {
          if (!Array.isArray(item.skus)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 skus 必须是数组`, 400)
          skus = item.skus.map((value, skuIndex) => {
            if (!value || typeof value !== 'object' || Array.isArray(value)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 SKU ${skuIndex + 1} 格式无效`, 400)
            const sku = value as Record<string, unknown>
            if (typeof sku.id !== 'string' || typeof sku.name !== 'string') throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 SKU ${skuIndex + 1} 缺少 id/name`, 400)
            const price = numeric(sku.price, 'SKU price', index); const stock = numeric(sku.stock, 'SKU stock', index)
            if (price === undefined || stock === undefined || !Number.isInteger(stock)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 SKU ${skuIndex + 1} 的 price/stock 无效`, 400)
            const attributes = sku.attributes && typeof sku.attributes === 'object' && !Array.isArray(sku.attributes) ? Object.fromEntries(Object.entries(sku.attributes).filter(([, candidate]) => typeof candidate === 'string').map(([key, candidate]) => [key, candidate as string])) : undefined
            return { id: sku.id.trim(), name: sku.name.trim(), price, stock, ...(Array.isArray(sku.images) ? { images: sku.images.filter((value): value is string => typeof value === 'string') } : {}), ...(attributes ? { attributes } : {}) }
          })
        }
        const attributes = item.attributes && typeof item.attributes === 'object' && !Array.isArray(item.attributes) ? Object.fromEntries(Object.entries(item.attributes).filter(([, value]) => typeof value === 'string').map(([key, value]) => [key, value as string])) : undefined
        const sellingPoints = Array.isArray(item.selling_points) ? item.selling_points.map((value, pointIndex) => {
          if (!value || typeof value !== 'object' || Array.isArray(value)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项卖点 ${pointIndex + 1} 格式无效`, 400)
          const point = value as Record<string, unknown>
          return { id: typeof point.id === 'string' ? point.id : `sp_${pointIndex + 1}`, text: typeof point.text === 'string' ? point.text : '', proofStatus: (point.proof_status === 'confirmed' || point.proof_status === 'rejected' ? point.proof_status : 'pending') as 'pending' | 'confirmed' | 'rejected', sourceIds: Array.isArray(point.source_ids) ? point.source_ids.filter((source): source is string => typeof source === 'string') : [] }
        }) : undefined
        return { platform, ...(accountId ? { accountId } : {}), ...(typeof item.remote_id === 'string' && item.remote_id.trim() ? { remoteId: item.remote_id.trim() } : {}), ...(typeof item.local_product_key === 'string' ? { localProductKey: item.local_product_key } : {}), title, ...(typeof item.category === 'string' ? { category: item.category } : {}), ...(typeof item.store_name === 'string' ? { storeName: item.store_name } : {}), ...(typeof item.store_differentiation === 'string' ? { storeDifferentiation: item.store_differentiation } : {}), ...(images ? { images } : {}), ...(sourceAssetIds ? { sourceAssetIds } : {}), ...(attributes ? { attributes } : {}), ...(sellingPoints ? { sellingPoints } : {}), ...(skus ? { skus, skuCount: skus.length } : {}), ...(numeric(item.price, 'price', index) !== undefined ? { price: numeric(item.price, 'price', index) } : {}), ...(numeric(item.stock, 'stock', index) !== undefined ? { stock: numeric(item.stock, 'stock', index) } : {}), ...(numeric(item.sku_count, 'sku_count', index) !== undefined ? { skuCount: numeric(item.sku_count, 'sku_count', index) } : {}) }
      })
      assertUniqueBatchProductImportIdentities(items)
      const created: ReturnType<typeof service.importProduct>[] = []
      const writes: BatchProductWrite[] = []
      const beforeProducts = new Map([...service.products.entries()]
        .filter(([, product]) => product.workspaceId === workspaceId)
        .map(([id, product]) => [id, structuredClone(product)] as const))
      try {
        for (const item of items) {
          const product = service.importProduct({ workspaceId, ...item })
          created.push(product)
          writes.push({ product, version: product.version ?? 0 })
        }
        const batchId = `catalog_import_batch_${randomUUID()}`
        await persistSnapshotsAndEvent({ workspaceId, snapshots: created.map(product => ({ entityType: 'product' as const, entityId: product.id, entityVersion: product.version ?? 1, payload: product as unknown as Record<string, unknown> })), aggregateId: batchId, eventType: 'catalog.import.batch.completed', sequence: 1, eventPayload: { batch_id: batchId, count: created.length, product_ids: created.map(product => product.id) } })
        await recordOperationAudit({ workspaceId, actorId: requestActor(req), action: 'catalog.import.batch', resourceType: 'product_import_batch', resourceId: batchId, before: {}, after: { count: created.length, product_ids: created.map(product => product.id), atomic: true }, reason: '批量导入商品并建立持久化快照' })
        return result({ batchId, count: created.length, products: created.map(product => ({ ...product, product_id: product.id })), atomic: true, factsConfirmationRequired: true })
      } catch (error) {
        rollbackBatchProducts(service.products, workspaceId, writes, beforeProducts)
        throw error
      }
    }
    case 'catalog.facts.confirm': {
      const productId = required(params, 'product_id')
      const product = service.confirmProductFacts(workspaceId, productId)
      const resumedTasks = service.refreshTasksAfterProductFacts(workspaceId, productId)
      await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, product.id, 'product.facts_confirmed', product.version ?? 1, { product_id: product.id, version: product.version ?? 1 })
      for (const task of resumedTasks) {
        await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, task.id, 'task.facts_unblocked', task.version, { task_id: task.id, product_id: product.id, state: task.state })
      }
      const readControl = await canonicalProductReadControl(workspaceId)
      if (readControl.mode !== 'canonical_read') return result({ ...product, product_id: product.id, resumed_task_ids: resumedTasks.map(task => task.id) })
      await persistenceReady
      const canonicalRepository = persistence.brandUnits ?? memoryBrandUnits
      const candidates = (await canonicalRepository.listCanonicalProducts({ workspaceId })).filter(row => row.sourceProductId === product.id)
      if (candidates.length > 1) throw new DomainError('CANONICAL_PRODUCT_AMBIGUOUS', '一个商品对应多个规范商品，已阻止同步事实，请先完成映射治理', 409, { product_id: product.id, canonical_product_ids: candidates.map(row => row.id), next_action: 'canonical.product.consistency' })
      const canonical = candidates[0]
      if (!canonical) throw new DomainError('CANONICAL_PRODUCT_MAPPING_REQUIRED', '商品事实已确认，但尚未绑定规范商品，无法完成 canonical_read 同步', 409, { product_id: product.id, next_action: 'canonical.product.consistency' })
      if (canonical.facts && Object.keys(canonical.facts).length > 0) return result({ ...product, product_id: product.id, resumed_task_ids: resumedTasks.map(task => task.id), canonical_scope: { canonical_product_id: canonical.id, brand_id: canonical.brandId, facts_version: canonical.factsVersion, facts_synced: false } })
      const facts: Record<string, unknown> = {
        ...(product.category ? { category: product.category } : {}),
        ...(product.attributes ? { attributes: structuredClone(product.attributes) } : {}),
        ...(typeof product.price === 'number' ? { price: product.price } : {}),
        sku_ids: (product.skus ?? []).map(sku => sku.id),
        selling_points: (product.sellingPoints ?? []).filter(point => point.proofStatus === 'confirmed').map(point => point.text),
      }
      try {
        const updated = await canonicalRepository.updateCanonicalProductFacts({ workspaceId, id: canonical.id, facts, expectedFactsVersion: canonical.factsVersion })
        await persistEvent(workspaceId, updated.id, 'canonical_product.facts_confirmed', updated.factsVersion, { product_id: product.id, canonical_product_id: updated.id, facts_version: updated.factsVersion, source: 'legacy_confirmation' })
        return result({ ...product, product_id: product.id, resumed_task_ids: resumedTasks.map(task => task.id), canonical_scope: { canonical_product_id: updated.id, brand_id: updated.brandId, facts_version: updated.factsVersion, facts_synced: true } })
      } catch (error) {
        if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_REVISION_CONFLICT') throw new DomainError('CANONICAL_PRODUCT_REVISION_CONFLICT', '标准商品事实已被其他操作更新，请刷新后重试', 409)
        if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_NOT_FOUND') throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', '标准商品不存在或不属于当前工作区', 404)
        throw error
      }
    }
    case 'catalog.sku.update': {
      const productId = required(params, 'product_id')
      const skuId = required(params, 'sku_id')
      const readControl = await canonicalProductReadControl(workspaceId)
      const canonicalRepository = persistence.brandUnits ?? memoryBrandUnits
      let canonicalBefore: Awaited<ReturnType<typeof canonicalRepository.getCanonicalProduct>>
      if (readControl.mode === 'canonical_read') {
        await persistenceReady
        const candidates = (await canonicalRepository.listCanonicalProducts({ workspaceId })).filter(row => row.sourceProductId === productId)
        if (candidates.length > 1) throw new DomainError('CANONICAL_PRODUCT_AMBIGUOUS', '一个商品对应多个规范商品，已阻止 SKU 事实修改，请先完成映射治理', 409, { product_id: productId, canonical_product_ids: candidates.map(row => row.id), next_action: 'canonical.product.consistency' })
        canonicalBefore = candidates[0]
        if (!canonicalBefore) throw new DomainError('CANONICAL_PRODUCT_MAPPING_REQUIRED', '标准商品映射未完成，已阻止只写 legacy 的 SKU 修改', 409, { product_id: productId, next_action: 'canonical.product.consistency' })
      }
      await enforceProductBrandAccess(req, workspaceId, productId)
      const parseJsonObject = (key: string) => {
        if (typeof params[key] !== 'string') return undefined
        try { const parsed = JSON.parse(params[key] as string); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed) || Object.values(parsed).some(value => typeof value !== 'string')) throw new Error(key); return parsed as Record<string, string> } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是字符串到字符串的 JSON 对象`, 400) }
      }
      const parseImages = () => {
        if (typeof params.images_json !== 'string') return undefined
        try { const parsed = JSON.parse(params.images_json); if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string')) throw new Error('images_json'); return parsed as string[] } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'images_json 必须是图片引用字符串数组 JSON', 400) }
      }
      const priceProvided = typeof params.price === 'string' && params.price.trim().length > 0
      const stockProvided = typeof params.stock === 'string' && params.stock.trim().length > 0
      const price = priceProvided ? Number(params.price) : undefined
      const stock = stockProvided ? Number(params.stock) : undefined
      if ((priceProvided && !Number.isFinite(price)) || (stockProvided && !Number.isFinite(stock))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'price 和 stock 必须是有效数字', 400)
      const images = parseImages()
      const attributes = parseJsonObject('attributes_json')
      if (!priceProvided && !stockProvided && typeof params.name !== 'string' && images === undefined && attributes === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '至少提供一个 SKU 修改字段', 400)
      const expectedVersion = typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined
      if (params.expected_version !== undefined && expectedVersion === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_version 必须是非负整数', 400)
      const product = service.updateProductSku({ workspaceId, productId, skuId, ...(typeof params.name === 'string' ? { name: params.name } : {}), ...(priceProvided ? { price } : {}), ...(stockProvided ? { stock } : {}), ...(images !== undefined ? { images } : {}), ...(attributes !== undefined ? { attributes } : {}), ...(expectedVersion !== undefined ? { expectedVersion } : {}) })
      let canonicalScope: Record<string, unknown> | undefined
      if (readControl.mode === 'canonical_read' && canonicalBefore) {
        const facts: Record<string, unknown> = {
          ...(product.category ? { category: product.category } : {}),
          ...(product.attributes ? { attributes: structuredClone(product.attributes) } : {}),
          ...(typeof product.price === 'number' ? { price: product.price } : {}),
          sku_ids: (product.skus ?? []).map(sku => sku.id),
          selling_points: (product.sellingPoints ?? []).filter(point => point.proofStatus === 'confirmed').map(point => point.text),
        }
        try {
          const updated = await canonicalRepository.updateCanonicalProductFacts({ workspaceId, id: canonicalBefore.id, facts, expectedFactsVersion: canonicalBefore.factsVersion })
          await persistEvent(workspaceId, updated.id, 'canonical_product.facts_updated', updated.factsVersion, { product_id: product.id, canonical_product_id: updated.id, facts_version: updated.factsVersion, source: 'legacy_sku_update', sku_id: skuId })
          canonicalScope = { canonical_product_id: updated.id, brand_id: updated.brandId, facts_version: updated.factsVersion, facts_synced: true }
        } catch (error) {
          if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_REVISION_CONFLICT') throw new DomainError('CANONICAL_PRODUCT_REVISION_CONFLICT', '标准商品事实已被其他操作更新，请刷新后重试', 409)
          if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_NOT_FOUND') throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', '标准商品不存在或不属于当前工作区', 404)
          throw error
        }
      }
      await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, product.id, 'product.sku_updated', product.version ?? 1, { product_id: product.id, sku_id: skuId, version: product.version ?? 1, facts_confirmed: false })
      return result({ ...product, product_id: product.id, sku_id: skuId, factsConfirmationRequired: true, ...(canonicalScope ? { canonical_scope: canonicalScope } : {}) })
    }
    case 'catalog.product.update': {
      const productId = required(params, 'product_id')
      const readControl = await canonicalProductReadControl(workspaceId)
      const canonicalRepository = persistence.brandUnits ?? memoryBrandUnits
      let canonicalBefore: Awaited<ReturnType<typeof canonicalRepository.getCanonicalProduct>>
      if (readControl.mode === 'canonical_read') {
        await persistenceReady
        const candidates = (await canonicalRepository.listCanonicalProducts({ workspaceId })).filter(row => row.sourceProductId === productId)
        if (candidates.length > 1) throw new DomainError('CANONICAL_PRODUCT_AMBIGUOUS', '一个商品对应多个规范商品，已阻止事实修改，请先完成映射治理', 409, { product_id: productId, canonical_product_ids: candidates.map(row => row.id), next_action: 'canonical.product.consistency' })
        canonicalBefore = candidates[0]
        if (!canonicalBefore) throw new DomainError('CANONICAL_PRODUCT_MAPPING_REQUIRED', '标准商品映射未完成，已阻止只写 legacy 的事实修改', 409, { product_id: productId, next_action: 'canonical.product.consistency' })
      }
      const productBefore = service.products.get(productId)
      if (!productBefore || productBefore.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
      await enforceProductBrandAccess(req, workspaceId, productId)
      const parseJsonObject = (key: string) => {
        if (typeof params[key] !== 'string') return undefined
        try { const parsed = JSON.parse(params[key] as string); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed) || Object.values(parsed).some(value => typeof value !== 'string')) throw new Error(key); return parsed as Record<string, string> } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是字符串到字符串的 JSON 对象`, 400) }
      }
      const parseImages = () => {
        if (typeof params.images_json !== 'string') return undefined
        try { const parsed = JSON.parse(params.images_json); if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string')) throw new Error('images_json'); return parsed as string[] } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'images_json 必须是图片引用字符串数组 JSON', 400) }
      }
      const parseSellingPoints = () => {
        if (typeof params.selling_points_json !== 'string') return undefined
        try {
          const parsed = JSON.parse(params.selling_points_json)
          if (!Array.isArray(parsed) || parsed.some(item => !item || typeof item !== 'object' || typeof item.text !== 'string')) throw new Error('selling_points_json')
          return parsed.map((item: Record<string, unknown>, index: number) => ({ id: typeof item.id === 'string' ? item.id : `sp_${index + 1}`, text: (item.text as string).trim(), proofStatus: (item.proof_status === 'confirmed' || item.proof_status === 'rejected' ? item.proof_status : 'pending') as import('../../../packages/application/src/service.js').SellingPointProofStatus, sourceIds: Array.isArray(item.source_ids) ? item.source_ids.filter((value): value is string => typeof value === 'string') : [] }))
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'selling_points_json 必须是卖点对象数组', 400) }
      }
      const images = parseImages(); const attributes = parseJsonObject('attributes_json'); const sellingPoints = parseSellingPoints()
      const priceProvided = typeof params.price === 'string' && params.price.trim().length > 0
      const price = priceProvided ? Number(params.price) : undefined
      if (priceProvided && (!Number.isFinite(price) || price! < 0)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'price 必须是有效的非负数字', 400)
      const expectedVersion = typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined
      if (params.expected_version !== undefined && expectedVersion === undefined) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_version 必须是非负整数', 400)
      if (typeof params.title !== 'string' && typeof params.category !== 'string' && images === undefined && attributes === undefined && sellingPoints === undefined && typeof params.store_differentiation !== 'string' && !priceProvided) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '至少提供一个商品事实修改字段', 400)
      const product = service.updateProductFacts({ workspaceId, productId, ...(typeof params.title === 'string' ? { title: params.title } : {}), ...(typeof params.category === 'string' ? { category: params.category } : {}), ...(images !== undefined ? { images } : {}), ...(attributes !== undefined ? { attributes } : {}), ...(sellingPoints !== undefined ? { sellingPoints } : {}), ...(typeof params.store_differentiation === 'string' ? { storeDifferentiation: params.store_differentiation } : {}), ...(priceProvided ? { price } : {}), ...(expectedVersion !== undefined ? { expectedVersion } : {}) })
      let canonicalScope: Record<string, unknown> | undefined
      if (readControl.mode === 'canonical_read' && canonicalBefore) {
        const facts: Record<string, unknown> = {
          ...(product.category ? { category: product.category } : {}),
          ...(product.attributes ? { attributes: structuredClone(product.attributes) } : {}),
          ...(typeof product.price === 'number' ? { price: product.price } : {}),
          sku_ids: (product.skus ?? []).map(sku => sku.id),
          selling_points: (product.sellingPoints ?? []).filter(point => point.proofStatus === 'confirmed').map(point => point.text),
        }
        try {
          const updatedFacts = await canonicalRepository.updateCanonicalProductFacts({ workspaceId, id: canonicalBefore.id, facts, expectedFactsVersion: canonicalBefore.factsVersion })
          let updated = updatedFacts
          if (typeof params.title === 'string' && params.title.trim() && params.title.trim() !== canonicalBefore.title) updated = await canonicalRepository.updateCanonicalProductTitle({ workspaceId, id: canonicalBefore.id, title: params.title, expectedFactsVersion: updatedFacts.factsVersion })
          await persistEvent(workspaceId, updated.id, 'canonical_product.facts_updated', updated.factsVersion, { product_id: product.id, canonical_product_id: updated.id, facts_version: updated.factsVersion, source: 'legacy_product_update' })
          canonicalScope = { canonical_product_id: updated.id, brand_id: updated.brandId, facts_version: updated.factsVersion, facts_synced: true }
        } catch (error) {
          if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_REVISION_CONFLICT') throw new DomainError('CANONICAL_PRODUCT_REVISION_CONFLICT', '标准商品事实已被其他操作更新，请刷新后重试', 409)
          if (error instanceof Error && error.message === 'CANONICAL_PRODUCT_NOT_FOUND') throw new DomainError('CANONICAL_PRODUCT_NOT_FOUND', '标准商品不存在或不属于当前工作区', 404)
          throw error
        }
      }
      await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, product.id, 'product.facts_updated', product.version ?? 1, { product_id: product.id, version: product.version ?? 1, facts_confirmed: false })
      return result({ ...product, product_id: product.id, factsConfirmationRequired: true, ...(canonicalScope ? { canonical_scope: canonicalScope } : {}) })
    }
    case 'catalog.product.disable': {
      const productId = required(params, 'product_id')
      const product = service.disableProduct({ workspaceId, productId, reason: required(params, 'reason') })
      await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, product.id, 'product.disabled', product.version ?? 1, { product_id: product.id, reason: product.disabledReason })
      return result(product)
    }
    case 'catalog.product.enable': {
      const product = service.enableProduct(workspaceId, required(params, 'product_id'))
      await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, product.id, 'product.enabled', product.version ?? 1, { product_id: product.id })
      return result(product)
    }
    case 'catalog.image.generate': {
      const productId = required(params, 'product_id')
      const product = service.products.get(productId)
      if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
      await enforceProductBrandAccess(req, workspaceId, productId)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      if (!product.factsConfirmed) throw new DomainError('PRODUCT_FACTS_CONFIRMATION_REQUIRED', '请先确认商品、SKU、价格和图片事实，再生成主图', 409)
      const protectedProductValidation = requireProtectedProductIntent(typeof params.direction === 'string' ? params.direction : '')
      const productProtection = protectedProductConclusion(protectedProductValidation)
      requirePlatformModelCostGate('image')
      const requestedPlatform = typeof params.platform === 'string' && params.platform.trim() ? params.platform.trim() as Platform : undefined
      const requestedAccountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      if (requestedPlatform && requestedPlatform !== product.platform) throw new DomainError('IMAGE_PLATFORM_SCOPE_MISMATCH', '图片生成的平台必须与当前商品平台一致', 409, { product_platform: product.platform, requested_platform: requestedPlatform })
      if (requestedAccountId && product.accountId && requestedAccountId !== product.accountId) throw new DomainError('IMAGE_ACCOUNT_SCOPE_MISMATCH', '图片生成的店铺必须与当前商品绑定店铺一致', 409, { product_account_id: product.accountId, requested_account_id: requestedAccountId })
      const rulePreflight = await requireGenerationRulePreflight(workspaceId, product.id, '主图生成前平台规则校验未通过')
      requireRuleSafeGenerationText(rulePreflight, [product.title, params.direction], '主图生成方向命中当前平台规则禁用表达')
      const imageTask = typeof params.task_id === 'string' && params.task_id.trim() ? service.getTask(params.task_id.trim()) : undefined
      if (imageTask && imageTask.workspaceId !== workspaceId) throw new DomainError('TENANT_SCOPE_DENIED', '图片任务不属于当前工作区', 403)
      service.assertBrandVisualGenerationReady(workspaceId, product.platform, imageTask?.region)
      let sourceAssetIds: string[] | undefined
      if (typeof params.asset_ids_json === 'string' && params.asset_ids_json.trim()) {
        try {
          const parsed = JSON.parse(params.asset_ids_json)
          if (!Array.isArray(parsed) || !parsed.length || parsed.some(value => typeof value !== 'string' || !value.trim())) throw new Error('invalid asset_ids_json')
          sourceAssetIds = [...new Set(parsed.map(value => value.trim()))]
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'asset_ids_json 必须是非空的素材 ID 字符串数组 JSON', 400) }
      }
      const imageMode = params.mode === undefined
        ? (sourceAssetIds?.length || product.sourceAssetIds?.length ? 'optimize' : 'create')
        : params.mode === 'create' || params.mode === 'optimize' ? params.mode : undefined
      if (!imageMode) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'mode 必须是 create 或 optimize', 400)
      const effectiveSourceAssetIds = sourceAssetIds ?? (imageMode === 'optimize' ? product.sourceAssetIds : undefined)
      if (imageMode === 'optimize' && !effectiveSourceAssetIds?.length) throw new DomainError('IMAGE_OPTIMIZATION_SOURCE_REQUIRED', '素材优化模式必须提供至少一个已授权商品素材', 400)
      requireApprovedAssetForImageGeneration(workspaceId, product, effectiveSourceAssetIds)
      let skuIds: string[] | undefined
      if (typeof params.sku_ids_json === 'string' && params.sku_ids_json.trim()) {
        try {
          const parsed = JSON.parse(params.sku_ids_json)
          if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string' || !value.trim())) throw new Error('invalid sku_ids_json')
          skuIds = [...new Set(parsed.map(value => value.trim()))]
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'sku_ids_json 必须是 SKU ID 字符串数组 JSON', 400) }
      }
      const idempotencyKey = (typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim() || `image-${workspaceId}-${productId}-${typeof params.direction === 'string' ? params.direction : 'default'}`
      const existingImageJob = [...service.imageGenerationJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === idempotencyKey)
      const walletDebitKey = `image:${idempotencyKey}`
      let entitlementConsumed = false
      const billingRequired = !existingImageJob || existingImageJob.continuation?.billingState === 'pending'
      const billingActorId = existingImageJob?.continuation?.requestedBy ?? requestActor(req)
      if (billingRequired) {
        entitlementConsumed = Boolean(await consumeEntitlement({ workspaceId, kind: 'image_generation', actionKey: walletDebitKey, actionKind: 'model_image', modelRunKey: walletDebitKey, actorId: billingActorId, description: '商品主图生成权益' }))
        if (!entitlementConsumed) await requirePluginWalletAccess(workspaceId)
        if (!entitlementConsumed) await debitPluginWallet({ workspaceId, idempotencyKey: walletDebitKey, modelRunKey: walletDebitKey, actorId: billingActorId, description: '商品主图生成调用' })
        if (existingImageJob?.continuation) {
          existingImageJob.continuation.billingState = 'settled'
          existingImageJob.continuation.state = 'executing'
          existingImageJob.continuation.updatedAt = new Date().toISOString()
          existingImageJob.updatedAt = existingImageJob.continuation.updatedAt
          existingImageJob.revision += 1
          await persistSnapshot(workspaceId, 'image_generation_job', existingImageJob, existingImageJob as unknown as Record<string, unknown>)
        }
      }
      let job: ReturnType<typeof service.enqueueImageGeneration>
      try {
        job = service.enqueueImageGeneration({ workspaceId, productId, idempotencyKey, imageMode, ...(skuIds ? { skuIds } : {}), ...(effectiveSourceAssetIds ? { sourceAssetIds: effectiveSourceAssetIds } : {}), ...(typeof params.task_id === 'string' && params.task_id.trim() ? { taskId: params.task_id.trim() } : {}), ...(typeof params.content_version_id === 'string' && params.content_version_id.trim() ? { contentVersionId: params.content_version_id.trim() } : {}), ...(typeof params.direction === 'string' ? { direction: params.direction } : {}), ...(typeof params.count === 'string' && /^\d+$/u.test(params.count) ? { count: Number(params.count) } : {}) })
      } catch (error) {
        if (entitlementConsumed) await refundModelEntitlement({ workspaceId, actionKey: walletDebitKey, reason: '图片任务创建失败' })
        else if (billingRequired) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: billingActorId, reason: '图片任务创建失败' })
        throw error
      }
      // Durable mode is explicit and fail-closed. It only admits the frozen
      // job here; the normal local fixture path below remains synchronous until
      // the image worker executor and callback are configured.
      if (process.env.IMAGE_GENERATION_EXECUTION_MODE?.trim().toLowerCase() === 'durable') {
        if (!persistence.persistSnapshotAndEvent || !persistence.outbox || !persistence.imageGenerationExecutions) throw new DomainError('IMAGE_GENERATION_DURABLE_NOT_CONFIGURED', '普通图片 Durable Worker 尚未完成生产配置', 503)
        const authorizationSnapshot = workerAuthorizationSnapshot(req, workspaceId, job.id, 'image_generation.execute', { method: 'catalog.image.generate', product_id: product.id, source_product_version: job.sourceProductVersion, intent_hash: job.intentHash })
        if (!authorizationSnapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '图片生成缺少持久身份授权快照，已拒绝入队', 503)
        const eventPayload = {
          job_id: job.id,
          workspace_id: workspaceId,
          product_id: job.productId,
          product_title: product.title,
          category: product.category ?? null,
          task_id: job.taskId ?? null,
          content_version_id: job.contentVersionId ?? null,
          intent_hash: job.intentHash,
          idempotency_key: job.idempotencyKey,
          image_mode: job.imageMode,
          direction: job.direction,
          requested_count: job.count,
          source_asset_ids: job.sourceAssetIds ?? [],
          source_product_version: job.sourceProductVersion,
          visual_brief: job.visualBrief ?? null,
          action_id: walletDebitKey,
          run_key: walletDebitKey,
          authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot),
        }
        if (!existingImageJob) await persistence.persistSnapshotAndEvent({ workspaceId, entityType: 'image_generation_job', entityId: job.id, entityVersion: job.revision, payload: job as unknown as Record<string, unknown>, eventType: 'image.generation.requested', eventPayload })
        return result({ job_id: job.id, product_id: product.id, execution: { mode: 'durable', state: 'queued', provider: 'configured relay', source: 'server' }, rule_preflight: rulePreflight, product_protection: productProtection, job: publicImageJob(job), next_action: { type: 'get_status', label: '查询任务状态', allowed: true } })
      }
      // A nested model-usage callback may hydrate the pre-provider snapshot
      // while the provider request is in flight. Durable archived outputs are
      // authoritative proof that generation completed; recover that state
      // without calling or charging the provider again.
      if (job.state !== 'succeeded' && job.archiveState === 'archived' && Boolean(job.outputs?.length)) {
        job.state = 'succeeded'
        delete job.errorCode
        delete job.errorMessage
        job.updatedAt = new Date().toISOString()
        job.revision += 1
        await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)
      }
      // The local Codex fixture completes immediately so merchants can exercise
      // the entire workflow. Production uses the same handle with an image worker
      // and object-storage provider before exposing the result.
      const imageExecution = executionContract('image', Boolean(imageGenerator))
      if (job.state === 'succeeded') {
        const images = imageJobOutputsAreClean(job) ? await readArchivedGeneratedImages(workspaceId, job) : []
        return result({ job_id: job.id, product_id: product.id, execution: imageExecution, rule_preflight: rulePreflight, product_protection: productProtection, job: publicImageJob(job), ...(images.length ? { images, review: reviewProductImagesForMcp(images) } : {}), ...(job.archiveState === 'external_unarchived' ? { availabilityWarning: '图片提供方仅返回外部地址，本批次未形成可持久读取的归档文件' } : {}) })
      }
      if (job.errorCode === 'IMAGE_ARTIFACT_RECONCILIATION_REQUIRED') {
        throw new DomainError('IMAGE_ARTIFACT_RECONCILIATION_REQUIRED', '图片模型已返回结果，但候选归档尚未确认；任务已停止自动重试并等待对账', 409, { retryable: false, reconciliation_required: true, job_id: job.id })
      }
      await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)
      let completed: Awaited<ReturnType<typeof service.completeImageGeneration>>
      try {
        completed = await service.completeImageGeneration({ workspaceId, jobId: job.id })
      } catch (error) {
        if (!providerSucceededButSettlementPending(error)) {
          if (entitlementConsumed) await refundModelEntitlement({ workspaceId, actionKey: walletDebitKey, reason: '图片生成失败' })
          else if (billingRequired) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: billingActorId, reason: '图片生成失败' })
        }
        await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)
        throw error
      }
      let archived: Awaited<ReturnType<typeof archiveGeneratedImages>>
      try {
        archived = await archiveGeneratedImages(workspaceId, job.id, completed.images)
      } catch (error) {
        const archiveErrorCode = (error as { code?: string })?.code ?? 'IMAGE_ARTIFACT_ARCHIVE_FAILED'
        job.state = 'running'
        job.archiveState = 'pending'
        job.errorCode = 'IMAGE_ARTIFACT_RECONCILIATION_REQUIRED'
        job.errorMessage = '图片模型已返回结果，但候选归档尚未确认；系统不会自动重复生成'
        job.updatedAt = new Date().toISOString()
        job.revision += 1
        delete job.images
        await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)
        throw new DomainError('IMAGE_ARTIFACT_RECONCILIATION_REQUIRED', '图片模型已返回结果，但候选归档尚未确认；任务已停止自动重试并等待对账', 409, { retryable: false, reconciliation_required: true, archive_error_code: archiveErrorCode, job_id: job.id })
      }
      if (archived.continuation) {
        archived.continuation.state = 'completed'
        archived.continuation.updatedAt = new Date().toISOString()
        archived.updatedAt = archived.continuation.updatedAt
        archived.revision += 1
      }
      await persistSnapshot(workspaceId, 'image_generation_job', archived, archived as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, job.id, 'product.image_candidates_generated', archived.revision, { job_id: job.id, product_id: productId, task_id: archived.taskId ?? null, content_version_id: archived.contentVersionId ?? null, candidate_count: archived.outputs?.length ?? 0, archive_receipts: (archived.outputs ?? []).map(output => ({ visual_ref: output.visualRef, asset_id: output.assetId ?? null, receipt_id: output.archiveReceiptId ?? null, receipt_digest: output.archiveReceiptDigest ?? null })), archive_state: archived.archiveState, direction: archived.direction, artifact_role: 'candidate', product_protection: productProtection })
      const deliverableImages = imageJobOutputsAreClean(archived) ? completed.images : []
      return result({ job_id: archived.id, product_id: completed.product.id, execution: imageExecution, rule_preflight: rulePreflight, product_protection: productProtection, job: publicImageJob(archived), product: completed.product, ...(deliverableImages.length ? { images: deliverableImages, review: reviewProductImagesForMcp(deliverableImages) } : { availabilityWarning: '候选图已生成，平台正在自动执行交付前安全扫描；完成前不会返回图片内容，商家无需操作。' }) })
    }
    case 'catalog.image.retry': {
      const jobId = required(params, 'job_id')
      const previous = service.getImageGenerationJob(workspaceId, jobId)
      await enforceProductBrandAccess(req, workspaceId, previous.productId)
      requirePlatformModelCostGate('image')
      const durableRetry = process.env.IMAGE_GENERATION_EXECUTION_MODE?.trim().toLowerCase() === 'durable'
      if (durableRetry && (!persistence.persistSnapshotAndEvent || !persistence.outbox || !persistence.imageGenerationExecutions)) throw new DomainError('IMAGE_GENERATION_DURABLE_NOT_CONFIGURED', '图片安全重试的 Durable Worker 尚未完成生产配置', 503)
      const durableAuthorizationSnapshot = durableRetry ? workerAuthorizationSnapshot(req, workspaceId, previous.id, 'image_generation.execute', { method: 'catalog.image.retry', product_id: previous.productId, source_product_version: previous.sourceProductVersion, intent_hash: previous.intentHash }) : undefined
      if (durableRetry && !durableAuthorizationSnapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '图片安全重试缺少持久身份授权快照，已拒绝扣费和入队', 503)
      const retryKey = required(params, 'idempotency_key')
      const previousRunKey = durableRetry
        ? (await persistence.outbox!.listAggregateEvents(workspaceId, previous.id, 100)).find(event => event.eventType === 'image.generation.requested' && typeof event.payload.run_key === 'string' && event.payload.run_key.trim())?.payload.run_key
        : undefined
      const imageRunKey = typeof previousRunKey === 'string' ? previousRunKey.trim() : `image:${previous.idempotencyKey}`
      const billingActorId = requestActor(req)
      const walletDebitKey = `image:${retryKey}`
      let entitlementConsumed = false
      const existingRetry = [...service.imageGenerationJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === retryKey)
      if (!existingRetry) {
        entitlementConsumed = Boolean(await consumeEntitlement({ workspaceId, kind: 'image_generation', actionKey: walletDebitKey, actionKind: 'model_image', modelRunKey: imageRunKey, actorId: billingActorId, description: '商品主图安全重试权益' }))
        if (!entitlementConsumed) await requirePluginWalletAccess(workspaceId)
        if (!entitlementConsumed) await debitPluginWallet({ workspaceId, idempotencyKey: walletDebitKey, modelRunKey: imageRunKey, actorId: billingActorId, description: '商品主图安全重试调用' })
      }
      let retried: ReturnType<typeof service.retryImageGeneration>
      try {
        retried = service.retryImageGeneration({ workspaceId, jobId, idempotencyKey: retryKey, ...(typeof params.expected_revision === 'string' && /^\d+$/u.test(params.expected_revision) ? { expectedRevision: Number(params.expected_revision) } : {}) })
      } catch (error) {
        if (entitlementConsumed) await refundModelEntitlement({ workspaceId, actionKey: walletDebitKey, reason: '图片安全重试未创建任务' })
        else await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: billingActorId, reason: '图片安全重试未创建任务' })
        throw error
      }
      const retryAuthorizationSnapshot = durableRetry ? workerAuthorizationSnapshot(req, workspaceId, retried.job.id, 'image_generation.execute', { method: 'catalog.image.retry', product_id: retried.job.productId, source_product_version: retried.job.sourceProductVersion, intent_hash: retried.job.intentHash }) : undefined
      if (durableRetry && !retryAuthorizationSnapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '图片安全重试缺少新任务的持久身份授权快照，已停止入队', 503)
      if (!retried.alreadyExists) {
        await persistSnapshot(workspaceId, 'image_generation_job', retried.job, retried.job as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, retried.job.id, 'image.generation.retry_requested', retried.job.revision, { job_id: retried.job.id, previous_job_id: previous.id, idempotency_key: retryKey, source_intent_hash: previous.intentHash })
      }
      if (durableRetry) {
        if (!persistence.persistSnapshotAndEvent || !persistence.outbox || !persistence.imageGenerationExecutions) throw new DomainError('IMAGE_GENERATION_DURABLE_NOT_CONFIGURED', '图片安全重试的 Durable Worker 尚未完成生产配置', 503)
        if (!existingRetry) {
          await persistence.persistSnapshotAndEvent({ workspaceId, entityType: 'image_generation_job', entityId: retried.job.id, entityVersion: retried.job.revision, payload: retried.job as unknown as Record<string, unknown>, eventType: 'image.generation.requested', eventPayload: { job_id: retried.job.id, workspace_id: workspaceId, product_id: retried.job.productId, intent_hash: retried.job.intentHash, idempotency_key: retried.job.idempotencyKey, image_mode: retried.job.imageMode, direction: retried.job.direction, requested_count: retried.job.count, source_asset_ids: retried.job.sourceAssetIds ?? [], source_product_version: retried.job.sourceProductVersion, visual_brief: retried.job.visualBrief ?? null, action_id: walletDebitKey, run_key: imageRunKey, authorization_snapshot: serializedWorkerAuthorizationSnapshot(retryAuthorizationSnapshot!), retry_of_job_id: previous.id }})
        }
        return result({ job_id: retried.job.id, previous_job_id: previous.id, state: 'queued', execution: { mode: 'durable', state: 'queued', provider: 'configured relay', source: 'server' }, job: publicImageJob(retried.job), next_action: { type: 'get_status', label: '查询任务状态', allowed: true } })
      }
      try {
        const completed = await service.completeImageGeneration({ workspaceId, jobId: retried.job.id, runKey: imageRunKey })
        const archived = await archiveGeneratedImages(workspaceId, retried.job.id, completed.images)
        await persistSnapshot(workspaceId, 'image_generation_job', archived, archived as unknown as Record<string, unknown>)
        return result({ job_id: archived.id, previous_job_id: previous.id, state: archived.state, archive_state: archived.archiveState, retry_count: archived.retryCount ?? 1, job: publicImageJob(archived), ...(imageJobOutputsAreClean(archived) ? { images: completed.images } : {}) })
      } catch (error) {
        if (entitlementConsumed) await refundModelEntitlement({ workspaceId, actionKey: walletDebitKey, reason: '图片安全重试失败' })
        else if (!existingRetry) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: billingActorId, reason: '图片安全重试失败' })
        await persistSnapshot(workspaceId, 'image_generation_job', retried.job, retried.job as unknown as Record<string, unknown>)
        throw error
      }
    }
    case 'catalog.image.get': {
      const jobId = typeof params.job_id === 'string' && params.job_id.trim() ? params.job_id.trim() : undefined
      const visualRef = typeof params.visual_ref === 'string' && params.visual_ref.trim() ? params.visual_ref.trim() : undefined
      if (Boolean(jobId) === Boolean(visualRef)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'job_id 与 visual_ref 必须且只能提供一个', 400)
      const job = jobId ? service.getImageGenerationJob(workspaceId, jobId) : service.resolveImageGenerationByVisualRef(workspaceId, visualRef!)
      await enforceProductBrandAccess(req, workspaceId, job.productId)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') {
        const product = service.products.get(job.productId)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
        await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      }
      const images = imageJobOutputsAreClean(job, visualRef) ? await readArchivedGeneratedImages(workspaceId, job, visualRef) : []
      const selectedImages = images
      const selectedOutputs = selectedImages.length
        ? (job.outputs ?? []).filter(output => !visualRef || output.visualRef === visualRef)
        : []
      const signedUrls = selectedOutputs.length === selectedImages.length
        ? selectedOutputs.map(output => output.assetId ? signedAssetDisplayUrl(workspaceId, output.assetId) : undefined)
        : []
      const imageUrls = signedUrls.length && signedUrls.every((value): value is string => typeof value === 'string') ? signedUrls : []
      const selectionTickets = selectedOutputs.length === selectedImages.length && selectedImages.length
        ? await issueImageSelectionTickets(req, workspaceId, job, selectedOutputs)
        : []
      return result({ job_id: job.id, execution: executionContract('image', Boolean(imageGenerator)), ...(selectedImages.length ? { images: selectedImages, ...(imageUrls.length ? { image_urls: imageUrls } : {}), selection_tickets: selectionTickets, review: reviewProductImagesForMcp(selectedImages) } : { availabilityWarning: '平台正在自动执行交付前安全扫描，完成前不会返回图片内容，商家无需操作。' }), job: publicImageJob(job), historicalCandidate: true, platformPublished: false })
    }
    case 'catalog.image.select': {
      const jobId = required(params, 'job_id')
      const visualRef = required(params, 'visual_ref')
      const current = service.getImageGenerationJob(workspaceId, jobId)
      await enforceProductBrandAccess(req, workspaceId, current.productId, 'editor')
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') {
        const product = service.products.get(current.productId)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
        await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      }
      const expectedRevisionRaw = required(params, 'expected_revision')
      if (!/^[1-9][0-9]*$/u.test(expectedRevisionRaw)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_revision 必须是正整数字符串', 400)
      const expectedRevision = Number(expectedRevisionRaw)
      if (!Number.isSafeInteger(expectedRevision)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'expected_revision 超出安全整数范围', 400)
      const idempotencyKey = required(params, 'idempotency_key').trim()
      const selectedBy = requestActor(req).normalize('NFKC').trim()
      const reason = required(params, 'reason').normalize('NFKC').trim()
      await consumeImageSelectionTicket({ req, workspaceId, params, job: current, visualRef, expectedRevision, idempotencyKey, selectedBy, reason })
      const before = {
        revision: current.revision,
        updatedAt: current.updatedAt,
        preferredSelection: current.preferredSelection ? structuredClone(current.preferredSelection) : undefined,
        preferredSelectionHistory: current.preferredSelectionHistory ? structuredClone(current.preferredSelectionHistory) : undefined,
      }
      const selected = service.selectImageGenerationCandidate({ workspaceId, jobId, visualRef, expectedRevision, idempotencyKey, selectedBy, reason })
      const changed = selected.job.revision !== before.revision
      if (changed) {
        try {
          await persistSnapshotsAndEvent({ workspaceId, snapshots: [{ entityType: 'image_generation_job', entityId: selected.job.id, entityVersion: selected.job.revision, payload: selected.job as unknown as Record<string, unknown> }], aggregateId: selected.job.id, eventType: 'image.candidate.preferred', sequence: selected.job.revision, eventPayload: { job_id: selected.job.id, product_id: selected.job.productId, visual_ref: selected.preferredSelection.visualRef, selected_by: selected.preferredSelection.selectedBy, reason: selected.preferredSelection.reason, idempotency_key: selected.preferredSelection.idempotencyKey, intent_hash: selected.preferredSelection.intentHash, previous_visual_ref: before.preferredSelection?.visualRef ?? null, review_status: selected.reviewStatus, currently_usable: selected.currentlyUsable, candidate_publishable: selected.publishable, publish_job_created: false, remote_write_performed: false } })
        } catch (error) {
          selected.job.revision = before.revision
          selected.job.updatedAt = before.updatedAt
          if (before.preferredSelection) selected.job.preferredSelection = before.preferredSelection
          else delete selected.job.preferredSelection
          if (before.preferredSelectionHistory) selected.job.preferredSelectionHistory = before.preferredSelectionHistory
          else delete selected.job.preferredSelectionHistory
          throw error
        }
      }
      return result({ job_id: selected.job.id, product_id: selected.job.productId, visual_ref: selected.preferredSelection.visualRef, preference_status: 'selected', review_status: selected.reviewStatus, currently_usable: selected.currentlyUsable, candidate_publishable: selected.publishable, publishable: false, review_required: true, approval_required: true, platformPublished: false, remote_write_performed: false, revision: selected.job.revision, idempotent_replay: !changed })
    }
    case 'catalog.image.review': {
      const productId = required(params, 'product_id')
      const product = service.products.get(productId)
      if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
      await enforceProductBrandAccess(req, workspaceId, product.id)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      let visualRefs: string[] | undefined
      if (typeof params.visual_refs_json === 'string' && params.visual_refs_json.trim()) {
        try {
          const parsed = JSON.parse(params.visual_refs_json)
          if (!Array.isArray(parsed) || !parsed.length || parsed.length > 6 || parsed.some(value => typeof value !== 'string') || new Set(parsed).size !== parsed.length) throw new Error('invalid')
          visualRefs = parsed
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'visual_refs_json 必须是 1 至 6 个不重复候选引用的 JSON 数组', 400) }
      }
      let images = parseImageListForMcp(params.images) ?? product.images
      if (visualRefs) {
        images = []
        for (const visualRef of visualRefs) {
          const job = service.resolveImageGenerationByVisualRef(workspaceId, visualRef)
          if (job.productId !== product.id) throw new DomainError('VISUAL_SELECTION_SCOPE_MISMATCH', '图片候选不属于当前商品', 409)
          const archived = await readArchivedGeneratedImages(workspaceId, job)
          const index = job.outputs?.findIndex(output => output.visualRef === visualRef) ?? -1
          if (index < 0 || !archived[index]) throw new DomainError('VISUAL_NOT_READY', '图片候选不可读取', 409)
          images.push(archived[index]!)
        }
      }
      if (!visualRefs && platformGovernanceGatesRequired()) throw new DomainError('VISUAL_AUTHENTICITY_EVIDENCE_REQUIRED', '生产真实性审阅必须引用归档 visual_ref，不能仅信任调用者提供的图片地址', 409)
      const authenticityGate = visualRefs ? evaluateVisualCandidates(workspaceId, visualRefs, params) : undefined
      const findings = reviewProductImagesForMcp(images)
      if (visualRefs) {
        const jobs = service.reviewImageGenerationOutputs(workspaceId, visualRefs, findings.some(finding => finding.severity === 'error') ? 'blocked' : 'passed')
        for (const job of jobs) await persistSnapshot(workspaceId, 'image_generation_job', job, job as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, jobs[0]!.id, 'product.image_candidates_reviewed', Math.max(...jobs.map(job => job.revision)), { product_id: product.id, visual_refs: visualRefs, review_status: findings.some(finding => finding.severity === 'error') ? 'blocked' : 'passed', finding_count: findings.length, authenticity_gate: authenticityGate?.map(item => ({ visual_ref: item.visual_ref, status: item.status, publishable: item.publishable, finding_codes: item.findings.map(finding => finding.code) })) ?? null })
      }
      return result({ productId, images: images ?? [], findings, ...(authenticityGate ? { authenticityGate } : {}), ...(visualRefs ? { visualRefs, persistedReviewStatus: findings.some(finding => finding.severity === 'error') ? 'blocked' : 'passed' } : {}), externallyUnverified: authenticityGate ? ['平台最终审核'] : ['视觉真实性证据', '尺寸/清晰度', '主体占比', 'OCR 文字合规', '平台最终审核'] })
    }
    case 'content.visual.select': {
      const scoped = scopeContentVersion(req, required(params, 'content_version_id'))
      await assertCanonicalTaskScopeForAction(scoped.task)
      let visualRefs: string[]
      try {
        const parsed = JSON.parse(required(params, 'visual_refs_json'))
        if (!Array.isArray(parsed) || !parsed.length || parsed.length > 6 || parsed.some(value => typeof value !== 'string') || new Set(parsed).size !== parsed.length) throw new Error('invalid')
        visualRefs = parsed
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'visual_refs_json 必须是 1 至 6 个不重复候选引用的 JSON 数组', 400) }
      requireSelectedVisualAuthenticity(workspaceId, visualRefs)
      const expectedRevision = Number(required(params, 'expected_revision'))
      const idempotencyKey = (typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim()
      if (!idempotencyKey) throw new DomainError(ERROR_CODES.IDEMPOTENCY_KEY_REQUIRED, '选图必须提供幂等键', 400)
      const selected = service.selectVisuals({ workspaceId, contentVersionId: scoped.version.id, visualRefs, expectedRevision, idempotencyKey, selectedBy: requestActor(req), reason: required(params, 'reason') })
      await persistSnapshotsAndEvent({ workspaceId, snapshots: [
        { entityType: 'content_version', entityId: selected.version.id, entityVersion: selected.version.revision, payload: selected.version as unknown as Record<string, unknown> },
        { entityType: 'task', entityId: selected.task.id, entityVersion: selected.task.version, payload: selected.task as unknown as Record<string, unknown> },
      ], aggregateId: selected.version.id, eventType: 'content.visual_selected', sequence: selected.version.revision, eventPayload: { source_content_version_id: selected.source.id, content_version_id: selected.version.id, visual_refs: visualRefs, selected_count: visualRefs.length, reason: required(params, 'reason') } })
      return result({ content_version_id: selected.version.id, parent_content_version_id: selected.source.id, version: selected.version.version, revision: selected.version.revision, state: selected.version.state, visualSelection: { state: 'selected', count: selected.version.visualSelection!.items.length, items: selected.version.visualSelection!.items.map(item => ({ visualRef: item.visualRef, ordinal: item.ordinal, mimeType: item.mimeType, reviewStatus: item.reviewStatus, publishable: false })) }, reviewRequired: true, approvalRequired: true })
    }
    case 'sync.retry_failed': {
      let failureIds: string[] | undefined
      if (typeof params.failure_ids_json === 'string' && params.failure_ids_json.trim()) {
        try {
          const parsed = JSON.parse(params.failure_ids_json)
          if (!Array.isArray(parsed) || parsed.some(item => typeof item !== 'string')) throw new Error('failure_ids_json must be an array')
          failureIds = parsed
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'failure_ids_json 必须是字符串数组 JSON', 400) }
      }
      const sourceJobId = required(params, 'job_id')
      const sourceJob = service.getSyncJob(workspaceId, sourceJobId)
      const policy = automationPolicies.get(automationPolicyKey(workspaceId, sourceJob.platform, sourceJob.accountId))
      if (policy && (sourceJob.retryCount ?? 0) >= policy.retryLimit) throw new DomainError('AUTOMATION_RETRY_LIMIT_REACHED', `该店铺同步失败重试次数已达到自动化策略上限（${policy.retryLimit} 次）`, 409, { platform: sourceJob.platform, account_id: sourceJob.accountId, retry_count: sourceJob.retryCount ?? 0, retry_limit: policy.retryLimit, next_step: '调整自动化策略 retry_limit 或由运营人员确认后重新发起同步' })
      const jobs = service.retrySyncFailures(workspaceId, sourceJobId, failureIds)
      const authorizationSnapshots = new Map(jobs.map(job => [job.id, workerAuthorizationSnapshot(req, workspaceId, job.id, 'catalog.sync.execute', { method: 'sync.retry_failed', source_job_id: sourceJobId, platform: job.platform, account_id: job.accountId, mode: job.mode, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}) })]))
      if (requiresStrictAuth() && [...authorizationSnapshots.values()].some(snapshot => !snapshot)) {
        for (const job of jobs) service.removeSyncJob(workspaceId, job.id)
        throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '同步重试缺少持久身份授权快照，已拒绝入队', 503)
      }
      for (const job of jobs) {
        const authorizationSnapshot = authorizationSnapshots.get(job.id)
        await persistSnapshot(workspaceId, 'sync_job', job, job as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, job.id, 'sync.requested', job.revision, { job_id: job.id, platform: job.platform, account_id: job.accountId, mode: job.mode, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}), retry_of: sourceJobId, ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}) })
      }
      return result({ jobs })
    }
    case 'rule.list': {
      const repository = ruleRepository()
      // Merchant generation/review callers must continue to see only rules
      // that can affect execution. The Ops rule center uses the same method,
      // but needs the complete lifecycle to refresh a newly created draft and
      // submit it for approval without falling through an invisible state.
      const includeLifecycleStates = canViewRuleLifecycle(req)
      const requestedPlatform = typeof params.platform === 'string' && params.platform.trim() ? params.platform.trim() : undefined
      const requestedCategory = typeof params.category === 'string' && params.category.trim() ? params.category.trim() : undefined
      const requestedBrand = typeof params.brand === 'string' && params.brand.trim() ? params.brand.trim() : undefined
      const requestedStore = typeof params.store === 'string' && params.store.trim() ? params.store.trim() : undefined
      const requestedCampaign = typeof params.campaign === 'string' && params.campaign.trim() ? params.campaign.trim() : undefined
      if (requestedPlatform && !SUPPORTED_PLATFORMS.includes(requestedPlatform as Platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 无效', 400)
      const requestedContext: Record<string, string | undefined> = { platform: requestedPlatform, category: requestedCategory, brand: requestedBrand, store: requestedStore, campaign: requestedCampaign }
      const hasContext = Object.values(requestedContext).some(Boolean)
      const matchesContext = (rule: { scope: string; targetId?: string; scopeValue?: string }) => {
        if (!hasContext) return true
        if (rule.scope === 'global') return true
        const expected = requestedContext[rule.scope]
        const target = rule.targetId ?? rule.scopeValue
        return Boolean(expected && target === expected)
      }
      const filterRules = <T extends { status: string; scope: string; targetId?: string; scopeValue?: string }>(rules: T[]) => rules.filter(rule => (includeLifecycleStates || rule.status === 'active') && matchesContext(rule))
      if (repository) return result(filterRules(await rulePacksForWorkspace(workspaceId)))
      if (isProduction()) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '生产规则仓储未配置', 503)
      return result(filterRules(service.ruleCenter.list({ includeInactive: includeLifecycleStates })))
    }
    case 'rule.sync.status': {
      const intervalHours = typeof params.interval_hours === 'string' && Number.isFinite(Number(params.interval_hours)) ? Number(params.interval_hours) : Number(process.env.PLATFORM_RULE_SYNC_INTERVAL_HOURS ?? 24)
      return result(platformRuleSyncStatus(await rulePacksForWorkspace(workspaceId), { intervalHours, manifestUrl: process.env.PLATFORM_RULE_SYNC_MANIFEST_URL, signingSecretConfigured: Boolean(process.env.PLATFORM_RULE_SYNC_SIGNING_SECRET?.trim()) }))
    }
    case 'rule.history': {
      const packId = required(params, 'pack_id')
      const repository = ruleRepository()
      if (repository) return result(await repository.list(workspaceId, packId).then(rows => rows.map(publicRule)))
      if (isProduction()) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '生产规则仓储未配置', 503)
      return result(service.listRuleHistory(packId))
    }
    case 'rule.audit':
    case 'ops.rules.workspace.audit': {
      const packId = typeof params.pack_id === 'string' && params.pack_id.trim() ? params.pack_id.trim() : undefined
      requireRuleAdmin(req)
      const repository = ruleRepository()
      if (repository) return result(await repository.listAudit(workspaceId, packId))
      if (isProduction()) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '生产规则仓储未配置', 503)
      return result(service.listRuleAudit(packId))
    }
    case 'rule.publish': {
      const principal = requireRuleAdmin(req)
      const packId = required(params, 'pack_id')
      const name = required(params, 'name')
      const versionValue = required(params, 'version')
      const scope = required(params, 'scope')
      const sourceKind = required(params, 'source_kind')
      const sourceReference = required(params, 'source_reference')
      const sourceCheckedAt = required(params, 'source_checked_at')
      const reason = required(params, 'reason')
      const status = typeof params.status === 'string' ? params.status : 'draft'
      if (!['global', 'platform', 'category', 'brand', 'store', 'campaign'].includes(scope) || !['official', 'internal', 'legal_review'].includes(sourceKind) || !['draft', 'active'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则发布参数无效', 400)
      if (!Number.isFinite(Date.parse(sourceCheckedAt))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'source_checked_at 必须是合法时间', 400)
      let checks: Record<string, unknown>
      try { const parsed = JSON.parse(required(params, 'checks_json')); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('checks_json'); checks = parsed as Record<string, unknown> } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'checks_json 必须是 JSON 对象', 400) }
      const effectiveFromRaw = typeof params.effective_from === 'string' && params.effective_from.trim() ? params.effective_from.trim() : undefined
      const effectiveToRaw = typeof params.effective_to === 'string' && params.effective_to.trim() ? params.effective_to.trim() : undefined
      if ((effectiveFromRaw && Number.isNaN(Date.parse(effectiveFromRaw))) || (effectiveToRaw && Number.isNaN(Date.parse(effectiveToRaw))) || (effectiveFromRaw && effectiveToRaw && Date.parse(effectiveFromRaw) >= Date.parse(effectiveToRaw))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则有效期必须是合法时间，且 effective_from 早于 effective_to', 400)
      const effectiveFrom = effectiveFromRaw ? new Date(effectiveFromRaw).toISOString() : undefined
      const effectiveTo = effectiveToRaw ? new Date(effectiveToRaw).toISOString() : undefined
      const severity = params.severity === 'warning' ? 'warning' : params.severity === 'error' || params.severity === undefined ? 'error' : undefined
      const action = ['block', 'warn', 'review', 'allow'].includes(String(params.action)) ? String(params.action) : params.action === undefined ? 'block' : undefined
      if (!severity || !action) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则 severity/action 无效', 400)
      const input: JsonObject = { approval: typeof params.approval_json === 'string' ? JSON.parse(params.approval_json) : undefined }
      const approval = status === 'active' ? parseApprovalGrant(req, workspaceId, principal.actorId, input) : undefined
      const at = new Date().toISOString()
      const checksum = createHash('sha256').update(canonicalJson(checks)).digest('hex')
      const repository = ruleRepository()
      if (repository) {
        await persistence.ensureWorkspace?.(workspaceId)
        const versionInput = { id: `rule_${randomBytes(12).toString('hex')}`, workspaceId, packId, name, version: versionValue, scope, status, sourceKind, sourceReference, sourceCheckedAt: new Date(sourceCheckedAt).toISOString(), checksum, checks, severity, action, ...(effectiveFrom ? { effectiveFrom } : {}), ...(effectiveTo ? { effectiveTo } : {}), ...(typeof params.target_id === 'string' && params.target_id.trim() ? { targetId: params.target_id.trim() } : {}), ...(typeof params.scope_value === 'string' && params.scope_value.trim() ? { scopeValue: params.scope_value.trim() } : {}), createdBy: principal.actorId, revision: 1, createdAt: at, updatedAt: at, ...(status === 'active' ? { activatedAt: at } : {}) }
        const audit = { id: `rule_audit_${randomBytes(12).toString('hex')}`, workspaceId, rulePackId: packId, ruleVersionId: versionInput.id, version: versionValue, action: status === 'active' ? 'activated' : 'created', actorId: principal.actorId, reason, occurredAt: at, data: { checksum, ...(approval ? { approval } : {}) } }
        if (repository.insertVersionWithAudit) return result((await repository.insertVersionWithAudit({ version: versionInput, audit })).version)
        const created = await repository.insertVersion(versionInput)
        return result({ version: created, audit: await repository.appendAudit({ ...audit, ruleVersionId: created.id, version: created.version }) })
      }
      if (isProduction()) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '生产规则仓储未配置', 503)
      const published = service.publishRuleVersion({ packId, name, version: versionValue, scope: scope as 'global' | 'platform' | 'category' | 'brand' | 'store' | 'campaign', source: { kind: sourceKind as 'official' | 'internal' | 'legal_review', reference: sourceReference, checkedAt: new Date(sourceCheckedAt).toISOString() }, checks: checks as { forbiddenTerms?: string[]; requiredFields?: string[] }, actorId: principal.actorId, reason })
      if (status === 'active') return result(service.setRuleStatus({ packId, version: versionValue, status: 'active', actorId: principal.actorId, reason }))
      return result(published)
    }
    case 'rule.status': {
      const principal = requireRuleAdmin(req)
      const packId = required(params, 'pack_id'); const versionValue = required(params, 'version'); const status = required(params, 'status'); const reason = required(params, 'reason')
      if (!['active', 'inactive', 'expired'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则状态无效', 400)
      const approval = status === 'active' ? parseApprovalGrant(req, workspaceId, principal.actorId, { approval: typeof params.approval_json === 'string' ? JSON.parse(params.approval_json) : undefined }) : undefined
      const repository = ruleRepository()
      if (repository) {
        const rows = await repository.list(workspaceId, packId); const target = rows.find(row => row.version === versionValue)
        if (!target) throw new DomainError('RULE_VERSION_NOT_FOUND', '规则版本不存在', 404)
        const current = rows.find(row => row.status === 'active' && row.id !== target.id); const at = new Date().toISOString()
        if (repository.transitionStatusWithAudit) return result(publicRule((await repository.transitionStatusWithAudit({ workspaceId, packId, targetId: target.id, status, actorId: principal.actorId, reason, occurredAt: at, targetAuditId: `rule_audit_${randomBytes(12).toString('hex')}`, ...(current ? { currentAuditId: `rule_audit_${randomBytes(12).toString('hex')}` } : {}), auditData: approval ? { approval_ref: approval.approvalRef, approved_by: approval.approvedBy, approved_at: approval.approvedAt } : {} })).version))
        return result(publicRule(await repository.updateStatus({ workspaceId, id: target.id, status, revision: target.revision + 1, updatedAt: at, activatedAt: status === 'active' ? at : null, deactivatedAt: status === 'active' ? null : at })))
      }
      if (isProduction()) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '生产规则仓储未配置', 503)
      return result(service.setRuleStatus({ packId, version: versionValue, status: status as 'active' | 'inactive' | 'expired', actorId: principal.actorId, reason }))
    }
    case 'asset.list': {
      const accessibleIds = await accessibleAssetIds(req, workspaceId)
      const allAssets = service.listAssets(workspaceId)
      const internalAssets = accessibleIds === undefined ? allAssets : allAssets.filter(asset => accessibleIds.has(asset.id))
      const hasAutomaticScanInProgress = internalAssets.some(asset => asset.scanStatus === 'quarantined')
      const nextAction = internalAssets
        .filter(asset => asset.scanStatus !== 'quarantined')
        .map(asset => assetDisplayProjection(asset).nextAction)
        .find(action => action !== null)
      const action_cards = hasAutomaticScanInProgress
        ? []
        : internalAssets.length === 0
        ? [
            { method: 'asset.upload', label: '上传商品图片', required_inputs: ['name', 'mime_type', 'file_path'], confirmation: 'interactive_confirmation' },
            { method: 'asset.upload', label: '上传品牌资料', required_inputs: ['name', 'mime_type', 'file_path'], confirmation: 'interactive_confirmation' },
          ]
        : nextAction
          ? [{ method: nextAction.method, label: nextAction.label, required_inputs: ['asset_id'], confirmation: 'interactive_confirmation' }]
          : []
      const readiness = internalAssets.reduce((summary, asset) => { summary[asset.readiness.status] += 1; return summary }, { draft: 0, ready: 0, blocked: 0 })
      const assets = internalAssets.map(asset => ({
        id: asset.id,
        name: asset.name,
        mimeType: asset.mimeType,
        sizeBytes: asset.sizeBytes,
        createdAt: asset.createdAt,
        source: asset.sourceProviderJobId ? 'generated' : 'merchant_upload',
        scanStatus: asset.scanStatus,
        parseStatus: asset.parseStatus,
        rightsStatus: asset.rightsStatus,
        rightsScope: asset.rightsScope ?? null,
        readiness: asset.readiness,
        display: assetDisplayProjection(asset),
        ...(asset.extractedFactsSource ? { extractedFactsSource: asset.extractedFactsSource } : {}),
        ...(asset.extractedFacts ? { extractedFacts: asset.extractedFacts } : {}),
        ...(asset.factsConfirmedBy ? { factsConfirmedBy: asset.factsConfirmedBy } : {}),
        ...(asset.preference ? { preference: asset.preference } : {}),
      }))
      const asset_actions = internalAssets.map(asset => {
        const base = { asset_id: asset.id, asset_name: asset.name, status: asset.readiness.status, reasons: asset.readiness.reasons }
        const display = assetDisplayProjection(asset)
        const awaitingAutomaticScan = asset.scanStatus === 'quarantined'
        const action = awaitingAutomaticScan ? null : display.nextAction ? { method: display.nextAction.method, label: display.nextAction.label, required_inputs: [ 'asset_id' ], confirmation: 'interactive_confirmation' } : null
        return { ...base, display, action, next_step: awaitingAutomaticScan ? '系统正在自动检查，通过后自动继续；无需操作' : display.nextAction?.label ?? '素材已满足当前 readiness 条件', ...(awaitingAutomaticScan ? { scan_automation: conversationalAssetScanWaitingState() } : {}) }
      })
      const quotaSnapshot = await persistence.storageQuota?.getSnapshot(workspaceId)
      const storageQuota = quotaSnapshot
        ? (() => {
            const usedBytes = Math.max(0, quotaSnapshot.usedBytes)
            const reservedBytes = Math.max(0, quotaSnapshot.reservedBytes)
            const limitBytes = Math.max(0, quotaSnapshot.limitBytes)
            const projectedBytes = usedBytes + reservedBytes
            const utilization = limitBytes > 0 ? projectedBytes / limitBytes : 1
            return {
              usedBytes,
              reservedBytes,
              limitBytes,
              availableBytes: Math.max(0, limitBytes - projectedBytes),
              status: projectedBytes > limitBytes ? 'over_limit' as const : utilization >= 0.8 ? 'near_limit' as const : 'available' as const,
            }
          })()
        : undefined
      return result({ assets, readiness: { ...readiness, total: assets.length }, ...(storageQuota ? { storage_quota: storageQuota } : {}), asset_actions, empty_state: assets.length ? null : { title: '还没有素材', message: '先上传商品图片或品牌资料；上传后系统会自动检查，再继续确认事实和使用权限。' }, action_cards })
    }
    case 'asset.parse': {
      const assetId = required(params, 'asset_id')
      await enforceAssetAccess(req, workspaceId, assetId, 'editor')
      return result(await executeDurableAssetParse(workspaceId, assetId, req))
    }
    case 'asset.facts.confirm': {
      const asset = assetForWorkspace(workspaceId, required(params, 'asset_id'))
      await enforceAssetAccess(req, workspaceId, asset.id, 'editor')
      let facts: Record<string, unknown>
      try {
        const parsed = JSON.parse(required(params, 'facts_json'))
        if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed) || Object.keys(parsed).length === 0) throw new Error('empty')
        facts = parsed as Record<string, unknown>
      } catch {
        throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'facts_json 必须是非空 JSON 对象', 400)
      }
      const reason = required(params, 'reason').trim()
      if (!reason) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '人工补录原因不能为空', 400)
      return result(await confirmDurableAssetFacts({ workspaceId, assetId: asset.id, facts, reason, req }))
    }
    case 'asset.preference.update': {
      const verdict = required(params, 'verdict')
      if (!['excellent', 'disliked', 'unrated'].includes(verdict)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'verdict 必须是 excellent、disliked 或 unrated', 400)
      let reasons: string[] | undefined
      if (typeof params.reasons_json === 'string') {
        try { const parsed = JSON.parse(params.reasons_json); if (!Array.isArray(parsed) || parsed.some(reason => typeof reason !== 'string')) throw new Error('reasons_json'); reasons = parsed } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'reasons_json 必须是字符串数组 JSON', 400) }
      }
      const assetId = required(params, 'asset_id')
      await enforceAssetAccess(req, workspaceId, assetId, 'editor')
      const updated = service.updateAssetPreference({ workspaceId, assetId, verdict: verdict as 'excellent' | 'disliked' | 'unrated', ...(reasons ? { reasons } : {}), ...(typeof params.note === 'string' ? { note: params.note } : {}), actorId: requestActor(req), ...(typeof params.expected_revision === 'string' && /^\d+$/u.test(params.expected_revision) ? { expectedRevision: Number(params.expected_revision) } : {}) })
      await persistSnapshot(workspaceId, 'asset', updated, updated as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, updated.id, 'asset.preference_updated', updated.revision, { asset_id: updated.id, verdict, reasons: updated.preference?.reasons ?? [], actor_id: updated.preference?.updatedBy ?? requestActor(req) })
      return result(updated)
    }
    case 'brand.get': return result(service.getBrandProfile(workspaceId) ?? null)
    case 'brand.extract': {
      let assetIds: string[] | undefined
      if (typeof params.asset_ids_json === 'string') {
        try {
          const parsed = JSON.parse(params.asset_ids_json)
          if (!Array.isArray(parsed) || !parsed.length || parsed.length > 50 || parsed.some(value => typeof value !== 'string' || !value.trim())) throw new Error('asset_ids_json')
          assetIds = parsed.map(value => String(value).trim())
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'asset_ids_json 必须是 1～50 个素材 ID 的字符串数组 JSON', 400) }
      }
      return result(service.extractBrandProfile(workspaceId, assetIds))
    }
    case 'brand.upsert': {
      const parseStringArray = (key: string) => {
        const value = params[key]
        if (value === undefined) return undefined
        if (typeof value !== 'string') throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是 JSON 数组`, 400)
        try { const parsed = JSON.parse(value); if (!Array.isArray(parsed) || parsed.some(item => typeof item !== 'string')) throw new Error('invalid array'); return parsed as string[] } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是字符串数组 JSON`, 400) }
      }
      let details: Record<string, unknown> | undefined
      if (typeof params.details_json === 'string') {
        try { const parsed = JSON.parse(params.details_json); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('details_json'); details = parsed as Record<string, unknown> } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'details_json 必须是 JSON 对象', 400) }
      }
      let visualRules: BrandVisualRules | undefined
      if (typeof params.visual_rules_json === 'string') {
        try { const parsed = JSON.parse(params.visual_rules_json); if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('visual_rules_json'); visualRules = parsed as BrandVisualRules } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'visual_rules_json 必须是 JSON 对象', 400) }
      }
      let resolutions: Record<string, 'existing' | 'candidate'> | undefined
      if (typeof params.conflict_resolutions_json === 'string') {
        try {
          const parsed = JSON.parse(params.conflict_resolutions_json)
          if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed) || Object.values(parsed).some(value => value !== 'existing' && value !== 'candidate')) throw new Error('conflict_resolutions_json')
          resolutions = parsed as Record<string, 'existing' | 'candidate'>
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'conflict_resolutions_json 必须是字段到 existing/candidate 的 JSON 对象', 400) }
      }
      const profile = service.upsertBrandProfile({ workspaceId, name: required(params, 'name'), ...(typeof params.positioning === 'string' ? { positioning: params.positioning } : {}), ...(typeof params.audience === 'string' ? { audience: params.audience } : {}), ...(parseStringArray('tone_json') ? { tone: parseStringArray('tone_json') } : {}), ...(parseStringArray('forbidden_terms_json') ? { forbiddenTerms: parseStringArray('forbidden_terms_json') } : {}), ...(details ? { details } : {}), ...(visualRules ? { visualRules } : {}), ...(typeof params.source === 'string' ? { source: params.source } : {}), ...(resolutions ? { resolutions } : {}) })
      await persistSnapshot(workspaceId, 'brand_profile', profile, profile as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, profile.id, 'brand_profile.updated', profile.revision, { brand_profile_id: profile.id, revision: profile.revision })
      return result(profile)
    }
    case 'brand.tone.preview': {
      return result(service.previewBrandTone(workspaceId, { ...(typeof params.topic === 'string' ? { topic: params.topic } : {}), ...(typeof params.product_id === 'string' ? { productId: params.product_id } : {}) }))
    }
    case 'asset.upload': {
      return result(await uploadAssetForMcp(workspaceId, params, req))
    }
    case 'asset.upload.batch': {
      const raw = required(params, 'assets_json')
      let entries: unknown
      try { entries = JSON.parse(raw) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'assets_json 必须是 JSON 数组', 400) }
      if (!Array.isArray(entries) || entries.length === 0 || entries.length > 20 || entries.some(item => !item || typeof item !== 'object' || Array.isArray(item))) throw new DomainError('ASSET_BATCH_LIMIT', '单批素材数量必须在 1 到 20 个之间', 413)
      const items = entries as JsonObject[]
      const totalBytes = items.reduce((sum, item) => {
        const value = typeof item.content_base64 === 'string' ? item.content_base64 : ''
        return sum + Math.floor(value.replace(/=+$/u, '').length * 3 / 4)
      }, 0)
      if (totalBytes > 250 * 1024 * 1024) throw new DomainError('ASSET_BATCH_LIMIT', '单批素材总大小不能超过 250MB', 413)
      const securityInputs = items.map(item => {
        const encoded = typeof item.content_base64 === 'string' ? item.content_base64 : undefined
        const validBase64 = encoded !== undefined && /^(?:[A-Za-z0-9+/]{4})*(?:[A-Za-z0-9+/]{2}==|[A-Za-z0-9+/]{3}=)?$/u.test(encoded)
        return {
          fileName: typeof item.name === 'string' ? item.name : undefined as never,
          declaredMime: typeof item.mime_type === 'string' ? item.mime_type : undefined as never,
          bytes: validBase64 && encoded.length > 0 ? new Uint8Array(Buffer.from(encoded, 'base64')) : undefined as never,
        }
      })
      const securityResults = classifyAssetUploadBatch(securityInputs, { workspaceId, actorId: requestActor(req), requestId: requestId(req) })
      const rejected = securityResults.map((securityResult, index) => ({ securityResult, index })).filter(item => item.securityResult.decision === 'reject')
      const auditEventIds = await Promise.all(rejected.map(item => persistRejectedAssetUpload(workspaceId, item.securityResult, item.index)))
      const rejectedByIndex = new Map(rejected.map((item, rejectedIndex) => [item.index, { result: item.securityResult, auditEventId: auditEventIds[rejectedIndex]! }]))
      const batchItems: Array<Record<string, unknown>> = []
      const assets = []
      for (const [index, item] of items.entries()) {
        const securityRejection = rejectedByIndex.get(index)
        if (securityRejection) {
          batchItems.push({ index, status: 'failed', error: { code: securityRejection.result.reasonCode ?? 'ASSET_UPLOAD_REJECTED', message: '素材未通过上传安全检查，已拒绝进入隔离区' }, reason_codes: securityRejection.result.reasonCodes, security_audit_event_id: securityRejection.auditEventId })
          continue
        }
        try {
          const asset = await uploadAssetForMcp(workspaceId, item, req, index, true)
          assets.push(asset)
          batchItems.push({ index, status: 'succeeded', asset })
        } catch (error) {
          const failure = error instanceof DomainError ? error : new DomainError(ERROR_CODES.INTERNAL_ERROR, '素材上传失败', 500)
          batchItems.push({ index, status: 'failed', error: { code: failure.code, message: failure.message }, reason_codes: [], security_audit_event_id: null })
        }
      }
      const failed = batchItems.length - assets.length
      return result({ assets, items: batchItems, count: batchItems.length, succeeded: assets.length, failed, counts: { total: batchItems.length, succeeded: assets.length, failed }, partial: failed > 0 && assets.length > 0, totalBytes })
    }
    case 'asset.scan': {
      if (isProduction()) throw new DomainError('MCP_ASSET_SCAN_DISABLED', 'production asset scans are accepted only through the signed platform scanner callback', 410)
      await requireWorkerAuthorization(req)
      const asset = assetForWorkspace(workspaceId, required(params, 'asset_id'))
      if (asset.scanStatus !== 'quarantined' || !asset.storageKey.startsWith('quarantine/')) throw new DomainError('ASSET_SCAN_STATE_INVALID', '素材当前不在待扫描隔离状态', 409)
      return result(await promoteAssetAndPersist(workspaceId, asset, required(params, 'scan_evidence_ref')))
    }
    case 'asset.generation.confirm': {
      const jobId = required(params, 'job_id')
      const current = service.getImageGenerationJob(workspaceId, jobId)
      if (!current.continuation) throw new DomainError('IMAGE_CONTINUATION_NOT_FOUND', '图片任务不是素材续跑任务', 404)
      if (current.continuation.state !== 'awaiting_confirmation') throw new DomainError('IMAGE_CONTINUATION_CONFIRMATION_REQUIRED', '图片续跑当前不在等待商家确认状态', 409, { continuation_state: current.continuation.state })
      const asset = assetForWorkspace(workspaceId, current.continuation.sourceAssetId)
      if (asset.scanStatus !== 'clean') throw new DomainError('IMAGE_CONTINUATION_NOT_READY', '素材尚未完成安全扫描', 409)
      if (imageContinuationGate(asset, current) !== 'ready') throw new DomainError('IMAGE_CONTINUATION_RIGHTS_REQUIRED', '素材权益或适用范围尚未满足图片生成条件', 409)
      const authorizationSnapshot = workerAuthorizationSnapshot(req, workspaceId, asset.id, 'asset.continuation.execute', { method: 'asset.generation.confirm', job_id: current.id, asset_id: asset.id, job_revision: current.revision })
      if (requiresStrictAuth() && !authorizationSnapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '图片续跑缺少持久身份授权快照，已拒绝入队', 503)
      const confirmed = structuredClone(current)
      confirmed.continuation!.state = 'ready'
      confirmed.continuation!.updatedAt = new Date().toISOString()
      confirmed.updatedAt = confirmed.continuation!.updatedAt
      confirmed.revision += 1
      await persistSnapshotsAndEvent({
        workspaceId,
        snapshots: [{ entityType: 'image_generation_job', entityId: confirmed.id, entityVersion: confirmed.revision, payload: confirmed as unknown as Record<string, unknown> }],
        aggregateId: asset.id,
        eventType: 'asset.generation_continuations.ready',
        sequence: confirmed.revision,
        eventPayload: { job_id: confirmed.id, asset_id: asset.id, continuation_job_ids: [confirmed.id], confirmed_by: requestActor(req), ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}) },
      })
      service.imageGenerationJobs.set(confirmed.id, confirmed)
      return result({ job_id: confirmed.id, continuation_state: confirmed.continuation!.state, confirmed: true, user_action_required: false })
    }
    case 'asset.rights.update': {
      const asset = assetForWorkspace(workspaceId, required(params, 'asset_id'))
      await enforceAssetAccess(req, workspaceId, asset.id, 'editor')
      const previousAsset = structuredClone(asset)
      const rightsStatus = required(params, 'rights_status')
      if (!['approved', 'rejected', 'pending'].includes(rightsStatus)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'rights_status 无效', 400)
      let applicablePlatforms: Platform[] | undefined
      if (typeof params.applicable_platforms_json === 'string') {
        try {
          const parsed = JSON.parse(params.applicable_platforms_json)
          if (!Array.isArray(parsed) || parsed.some(value => !SUPPORTED_PLATFORMS.includes(String(value) as Platform))) throw new Error('applicable_platforms_json')
          applicablePlatforms = parsed as Platform[]
        } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'applicable_platforms_json 必须是首发平台字符串数组 JSON', 400) }
      }
      const parseAssetList = (key: string) => {
        if (typeof params[key] !== 'string') return undefined
        try { const parsed = JSON.parse(params[key] as string); if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string')) throw new Error(key); return parsed as string[] } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${key} 必须是字符串数组 JSON`, 400) }
      }
      const applicableRegions = parseAssetList('applicable_regions_json')
      const usageScopes = parseAssetList('usage_scopes_json')
      const rightsScope = typeof params.rights_scope === 'string' ? params.rights_scope as import('../../../packages/application/src/service.js').AssetMetadata['rightsScope'] : undefined
      if (rightsScope && !['owned', 'commercial_authorized', 'limited_use', 'internal_only', 'unknown', 'unusable'].includes(rightsScope)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'rights_scope 无效', 400)
      const updated = service.updateAssetRights({ workspaceId, assetId: asset.id, rightsStatus: rightsStatus as 'approved' | 'rejected' | 'pending', ...(rightsScope ? { rightsScope } : {}), ...(applicablePlatforms ? { applicablePlatforms } : {}), ...(applicableRegions ? { applicableRegions } : {}), ...(usageScopes ? { usageScopes } : {}), ...(typeof params.valid_from === 'string' ? { validFrom: params.valid_from } : {}), ...(typeof params.valid_to === 'string' ? { validTo: params.valid_to } : {}), ...(params.ai_modification_allowed === 'true' || params.ai_modification_allowed === 'false' ? { aiModificationAllowed: params.ai_modification_allowed === 'true' } : {}) })
      const previousJobs = [...service.imageGenerationJobs.values()]
        .filter(job => job.workspaceId === workspaceId && job.continuation?.sourceAssetId === updated.id)
        .map(job => ({ id: job.id, value: structuredClone(job) }))
      const continuationAwaitingConfirmationIds: string[] = []
      const touchedJobs: import('../../../packages/application/src/service.js').ImageGenerationJob[] = []
      for (const { id } of previousJobs) {
        const job = service.imageGenerationJobs.get(id)!
        if (!job.continuation || ['completed', 'executing', 'ready', 'failed'].includes(job.continuation.state)) continue
        const nextState = awaitingConfirmationState(imageContinuationGate(updated, job))
        if (job.continuation.state !== nextState) {
          job.continuation.state = nextState
          job.continuation.updatedAt = new Date().toISOString()
          job.updatedAt = job.continuation.updatedAt
          job.revision += 1
          touchedJobs.push(job)
          if (nextState === 'awaiting_confirmation') continuationAwaitingConfirmationIds.push(job.id)
        }
      }
      try {
        await persistSnapshotsAndEvent({
          workspaceId,
          snapshots: [
            { entityType: 'asset', entityId: updated.id, entityVersion: updated.revision, payload: updated as unknown as Record<string, unknown> },
            ...touchedJobs.map(job => ({ entityType: 'image_generation_job' as const, entityId: job.id, entityVersion: job.revision, payload: job as unknown as Record<string, unknown> })),
          ],
          aggregateId: updated.id,
          eventType: continuationAwaitingConfirmationIds.length ? 'asset.generation_continuations.awaiting_confirmation' : 'asset.rights_updated',
          sequence: updated.revision,
          eventPayload: { asset_id: updated.id, rights_status: updated.rightsStatus, continuation_job_ids: continuationAwaitingConfirmationIds },
        })
      } catch (error) {
        service.assets.set(previousAsset.id, previousAsset)
        for (const previous of previousJobs) service.imageGenerationJobs.set(previous.id, previous.value)
        throw error
      }
      return result(updated)
    }
    case 'catalog.sync': {
      const platform = required(params, 'platform') as Platform
      if (!platformConnectorConfigured(platform)) throw new DomainError('NOT_CONFIGURED', `${platform} 官方 API 尚未配置，无法同步商品`, 503)
      const accountId = (typeof params.account_id === 'string' && params.account_id.trim()) || header(req, 'x-account-id')?.trim() || (isProduction() ? '' : defaultFixtureAccountId(workspaceId, platform))
      if (!accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产同步必须指定已授权平台账号', 400)
      const platformAccount = isProduction() || fixtureMode ? requireActivePlatformAccount(workspaceId, accountId, platform) : undefined
      await ensureFixtureAccount(workspaceId, platform, accountId)
      const syncEntitlementKey = `bulk-sync:${workspaceId}:${platform}:${accountId}:${typeof params.cursor === 'string' ? params.cursor : 'full'}:${header(req, 'idempotency-key')?.trim() ?? 'default'}`
      const syncEntitlement = await consumeEntitlement({ workspaceId, kind: 'bulk_sync', actionKey: syncEntitlementKey, actionKind: 'catalog_sync', actorId: requestActor(req), description: '商品批量同步权益（可选加购）' })
      try {
        const synced = await connectorRuntime.sync(platform, { workspaceId, accountId, ...(platformAccount ? { credentialRef: platformAccount.credentialRef } : {}), traceId: requestId(req) }, typeof params.cursor === 'string' ? params.cursor : undefined)
        const products = service.upsertSyncedProducts({ workspaceId, platform, accountId, items: synced.items })
        await invalidateCanonicalFactsAfterSync(workspaceId, products)
        for (const product of products) await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
        const automation = await scanAutomationAfterOperationalCompletion(workspaceId, platform, accountId, 'catalog.sync.completed')
        return result({ ...synced, products, automation })
      } catch (error) {
        if (syncEntitlement) await refundEntitlement({ workspaceId, actionKey: syncEntitlementKey, reason: '商品批量同步失败' })
        throw error
      }
    }
    case 'catalog.sync.start': {
      return result(await requestCatalogSync(workspaceId, req, params))
    }
    case 'catalog.sync.get': {
      const job = service.getSyncJob(workspaceId, required(params, 'job_id'))
      return result({ ...job, workflow: projectSyncWorkflow(workspaceId, job) })
    }
    case 'deliverable.list': {
      const deliverables = service.listDeliverables(workspaceId, {
      ...(typeof params.query === 'string' ? { query: params.query } : {}),
      ...(typeof params.platform === 'string' ? { platform: params.platform as Platform } : {}),
      ...(typeof params.account_id === 'string' ? { accountId: params.account_id } : {}),
      ...(typeof params.product_id === 'string' ? { productId: params.product_id } : {}),
      ...(typeof params.task_id === 'string' ? { taskId: params.task_id } : {}),
      ...(typeof params.state === 'string' ? { state: params.state as import('../../../packages/application/src/service.js').ContentVersion['state'] } : {}),
      ...(typeof params.date_from === 'string' ? { dateFrom: params.date_from } : {}),
      ...(typeof params.date_to === 'string' ? { dateTo: params.date_to } : {}),
      ...(typeof params.limit === 'string' ? { limit: Number(params.limit) } : {}),
      ...(typeof params.cursor === 'string' ? { cursor: params.cursor } : {}),
      })
      const visibleTasks = await filterByTaskBrandAccess(req, workspaceId, service.listTasks(workspaceId))
      const visibleTaskIds = new Set(visibleTasks.map(task => task.id))
      const items = deliverables.items.filter(item => visibleTaskIds.has(item.task.id))
      return result({
        ...deliverables,
        items,
        empty_state: items.length ? null : { title: '还没有内容交付', message: '先创建内容任务，完成方案、审核和批准后，这里会出现可导出的交付物。' },
        action_cards: items.length ? [{ method: 'content.versions', label: '查看内容版本', required_inputs: ['task_id'], confirmation: 'none' }] : [{ method: 'task.understand', label: '创建内容任务', required_inputs: ['instruction', 'platform', 'account_id'], confirmation: 'interactive_confirmation' }],
      })
    }
    case 'task.history': {
      const pageRequest = mcpPagination(params)
      const filters = {
      ...(typeof params.query === 'string' ? { query: params.query } : {}),
      ...(typeof params.platform === 'string' ? { platform: params.platform as Platform } : {}),
      ...(typeof params.state === 'string' ? { state: params.state as import('../../../packages/application/src/service.js').TaskState } : {}),
      ...(typeof params.product_id === 'string' ? { productId: params.product_id } : {}),
      ...(typeof params.account_id === 'string' ? { accountId: params.account_id } : {}),
      ...(typeof params.brand_name === 'string' ? { brandName: params.brand_name } : {}),
      ...(typeof params.store_name === 'string' ? { storeName: params.store_name } : {}),
      ...(typeof params.remote_product_id === 'string' ? { remoteProductId: params.remote_product_id } : {}),
      ...(typeof params.publish_status === 'string' ? { publishStatus: params.publish_status as import('../../../packages/application/src/service.js').PublishState } : {}),
      ...(typeof params.date_from === 'string' ? { dateFrom: params.date_from } : {}),
      ...(typeof params.date_to === 'string' ? { dateTo: params.date_to } : {}),
      }
      const accessibleBrandIds = await accessibleTaskBrandIds(req, workspaceId)
      const page = persistence.business
        ? await persistence.business.listTasksPage(workspaceId, { ...pageRequest, ...filters, ...(accessibleBrandIds !== undefined ? { accessibleBrandIds } : {}) })
        : await (async () => { const all = await filterByTaskBrandAccess(req, workspaceId, service.listTasks(workspaceId, filters)); return { items: all.slice(pageRequest.offset, pageRequest.offset + pageRequest.limit), total: all.length, ...pageRequest } })()
      const items = page.items as unknown as Task[]
      return result({
        items: items.map(task => ({ ...task, workflows: taskWorkflowProjections(workspaceId, task.id) })),
        total: page.total,
        limit: page.limit,
        offset: page.offset,
        empty_state: items.length ? null : { title: '还没有营销任务', message: '用一句话告诉我商品、平台和营销目标，就可以创建第一条任务。' },
        action_cards: items.length ? [{ method: 'task.resume', label: '继续最近任务', required_inputs: ['task_id'], confirmation: 'none' }] : [{ method: 'task.understand', label: '开始第一条任务', required_inputs: ['instruction', 'platform', 'account_id'], confirmation: 'interactive_confirmation' }],
      })
    }
    case 'task.resume': {
      const task = scopeTask(req, required(params, 'task_id'))
      const resumed = service.resumeTask(workspaceId, task.id)
      return result({ ...resumed, workflows: taskWorkflowProjections(workspaceId, task.id) })
    }
    case 'task.clone': {
      const source = scopeTask(req, required(params, 'task_id'))
      const targetProductId = typeof params.target_product_id === 'string' && params.target_product_id.trim() ? params.target_product_id.trim() : undefined
      const targetBrandId = targetProductId ? await taskWriteBrandForProduct(req, workspaceId, targetProductId) : undefined
      const targetPlatform = typeof params.target_platform === 'string' && SUPPORTED_PLATFORMS.includes(params.target_platform as Platform) ? params.target_platform as Platform : undefined
      if (typeof params.target_platform === 'string' && !targetPlatform) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'target_platform 不是支持的平台', 400)
      if (targetPlatform && !targetProductId) throw new DomainError('TARGET_PRODUCT_REQUIRED', '跨平台复制必须指定目标商品 ID，以重新加载目标平台商品事实和规则', 400)
      const targetAccountId = typeof params.target_account_id === 'string' && params.target_account_id.trim() ? params.target_account_id.trim() : undefined
      const targetProduct = targetProductId ? service.listProducts(workspaceId).find(product => product.id === targetProductId) : service.listProducts(workspaceId).find(product => product.id === source.productId)
      const effectiveTargetPlatform = targetPlatform ?? targetProduct?.platform ?? source.platform
      const effectiveTargetAccountId = targetAccountId ?? targetProduct?.accountId
      // An explicit account-only clone is also a scope rebind.  Without this
      // branch, a historical task whose account drifted from the product would
      // re-run the source-task canonical assertion and could never be safely
      // carried forward into a fresh task.
      const requiresScopeRebind = Boolean(
        (targetProductId && (targetProductId !== source.productId || effectiveTargetPlatform !== source.platform || effectiveTargetAccountId !== source.accountId))
        || (targetAccountId && targetAccountId !== source.accountId),
      )
      if (!requiresScopeRebind) await assertCanonicalTaskScopeForAction(source)
      const reboundScope = requiresScopeRebind && targetProduct
        ? await resolveCanonicalTaskScope({ workspaceId, productId: targetProduct.id, platform: effectiveTargetPlatform, ...(effectiveTargetAccountId ? { accountId: effectiveTargetAccountId } : {}), ...(targetBrandId ? { brandId: targetBrandId } : {}), requireListing: true })
        : undefined
      const cloned = service.cloneTask(workspaceId, source.id, typeof params.request_text === 'string' ? params.request_text : undefined, { ...(targetProductId ? { productId: targetProductId } : {}), ...(targetPlatform ? { platform: targetPlatform } : {}), ...(targetAccountId ? { accountId: targetAccountId } : {}), ...(reboundScope ? { brandId: reboundScope.brandId, canonicalProductId: reboundScope.canonicalProductId, listingId: reboundScope.listingId } : {}), ...(typeof params.region === 'string' && params.region.trim() ? { region: params.region.trim() } : {}) })
      if (targetBrandId) cloned.brandId = targetBrandId
      await persistSnapshot(workspaceId, 'task', cloned, cloned as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, cloned.id, 'task.cloned', cloned.version, { task_id: cloned.id, source_task_id: source.id, source_platform: source.platform, target_platform: cloned.platform, target_product_id: cloned.productId, rule_reload_required: cloned.platform !== source.platform })
      return result({ task: cloned, sourceTaskId: source.id, copyMode: cloned.platform === source.platform ? 'same_platform_fresh_task' : 'cross_platform_fresh_task', ruleReloadRequired: cloned.platform !== source.platform, staleContentCopied: false, stalePromotionCopied: false })
    }
    case 'task.timeline': {
      const taskId = required(params, 'task_id')
      const events = await taskTimeline(workspaceId, taskId, typeof params.limit === 'string' ? Number(params.limit) : 100)
      return result({ events, workflows: taskWorkflowProjections(workspaceId, taskId), next_action: taskWorkflowProjections(workspaceId, taskId)[0]?.next_action ?? { label: '查看任务状态', allowed: true } })
    }
    case 'feedback.list': {
      const task = scopeTask(req, required(params, 'task_id'))
      return result(service.listFeedback(workspaceId, task.id))
    }
    case 'feedback.submit': {
      const task = scopeTask(req, required(params, 'task_id'))
      const rating = required(params, 'rating')
      if (!['liked', 'neutral', 'needs_improvement'].includes(rating)) throw new DomainError('FEEDBACK_RATING_INVALID', '反馈评级无效', 400)
      const feedback = service.submitFeedback({
        workspaceId, taskId: task.id, rating: rating as 'liked' | 'neutral' | 'needs_improvement',
        ...(typeof params.content_version_id === 'string' ? { contentVersionId: params.content_version_id } : {}),
        ...(typeof params.reason === 'string' ? { reason: params.reason } : {}),
        ...(typeof params.comment === 'string' ? { comment: params.comment } : {}),
        actorId: requestActor(req, 'actor_demo'),
      })
      await persistSnapshot(workspaceId, 'feedback', feedback, feedback as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, feedback.id, 'task_feedback_submitted', feedback.revision, taskFeedbackEventPayload(feedback))
      return result(feedback)
    }
    case 'platform.revoke': {
      requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'platform_ops'])
      const platform = required(params, 'platform') as Platform
      const accountId = required(params, 'account_id')
      const account = service.revokePlatformAccount(workspaceId, accountId, platform)
      await persistSnapshot(workspaceId, 'platform_account', account, account as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, account.id, 'platform_account.revoked', account.revision, { account_id: account.id, platform, remote_revoked: false })
      try {
        await connectorRuntime.connector(platform).revoke({ accountId: account.remoteAccountId, credentialRef: account.credentialRef })
      } catch (error) {
        throw new DomainError('PLATFORM_REVOKE_REMOTE_FAILED', error instanceof Error ? error.message : '平台远端凭证撤销失败，本地账号已停止使用', 503)
      }
      return result({ platform, accountId: account.id, state: account.tokenState, remoteRevoked: true })
    }
    case 'task.create': {
      const taskPlatform = required(params, 'platform') as Platform
      await requireEnabledPlatform(workspaceId, taskPlatform)
      const productId = required(params, 'product_id')
      const product = service.products.get(productId)
      const taskAccountId = resolveTaskAccountId(workspaceId, taskPlatform, typeof params.account_id === 'string' ? params.account_id : undefined) ?? (product?.workspaceId === workspaceId && product.platform === taskPlatform ? product.accountId : undefined)
      const brandId = await taskCreationBrand(req, workspaceId, params.brand_id)
      await enforceProductBrandAccess(req, workspaceId, productId)
      requireProductionTaskStore(taskPlatform, taskAccountId)
      if ((isProduction() || fixtureMode) && taskAccountId) service.getActivePlatformAccount(workspaceId, taskAccountId, taskPlatform)
      const canonicalScope = await resolveCanonicalTaskScope({ workspaceId, productId, platform: taskPlatform, ...(taskAccountId ? { accountId: taskAccountId } : {}), ...(brandId ? { brandId } : {}), requireListing: true })
      const task = service.createTask({ workspaceId, productId, platform: taskPlatform, ...(taskAccountId ? { accountId: taskAccountId } : {}), ...(canonicalScope ?? (brandId ? { brandId } : {})), ...(typeof params.region === 'string' ? { region: params.region } : {}) })
      await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, task.id, 'task.created', task.version, task as unknown as Record<string, unknown>)
      const storeContext = task.accountId ? workspaceStoreDirectory(workspaceId, taskPlatform).find(store => store.accountId === task.accountId) : undefined
      return result({ ...task, task_id: task.id, product_id: task.productId, storeContext: storeContext ?? null, selectionSource: product?.accountId ? 'product_binding' : task.accountId ? 'explicit_request' : 'unbound' })
    }
    case 'task.answer': {
      const task = scopeTask(req, required(params, 'task_id'))
      const raw = required(params, 'answers_json')
      let answers: Record<string, string | number | boolean | string[]>
      try {
        const parsed = JSON.parse(raw)
        if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('answers_json must be an object')
        answers = parsed as Record<string, string | number | boolean | string[]>
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'answers_json 必须是 JSON 对象', 400) }
      const answered = service.answerTask(workspaceId, task.id, answers, typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined)
      await persistSnapshot(workspaceId, 'task', answered, answered as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, task.id, 'task.answers_submitted', answered.version, { task_id: task.id, input_snapshot_id: answered.inputSnapshotId, answers: answered.answers, missing_questions: answered.missingQuestions })
      return result(answered)
    }
    case 'task.understand': return result(await enforceTaskRequestCandidates(req, workspaceId, service.understandTaskRequest(workspaceId, required(params, 'request_text'))))
    case 'task.request.create': {
      const requestText = required(params, 'request_text')
      const understanding = await enforceTaskRequestCandidates(req, workspaceId, service.understandTaskRequest(workspaceId, requestText))
      const taskBrandIds = await resolveTaskWriteBrands(req, workspaceId, taskUnderstandingProductIds(understanding))
      requireProductionRequestStores(workspaceId, understanding)
      for (const platform of understanding.platformCandidates) await requireEnabledPlatform(workspaceId, platform)
      const idempotencyKey = (typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim()
      const canonicalScopes = await resolveCanonicalTaskEntries(workspaceId, understanding.executionPlan.childTasks.flatMap(child => {
        const productId = child.candidateProductIds[0]
        const product = productId ? service.products.get(productId) : undefined
        return product ? [{ productId: product.id, platform: child.platform, ...(product.accountId ? { accountId: product.accountId } : {}) }] : []
      }))
      const created = service.createTaskFromRequest({ workspaceId, requestText, canonicalScopes, ...(idempotencyKey ? { idempotencyKey } : {}) })
      assignTaskWriteBrands(created.tasks, taskBrandIds)
      if (!created.replayed) for (const task of created.tasks) {
        await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, task.id, 'task.created', task.version, { ...task, ...(created.taskGroupId ? { task_group_id: created.taskGroupId } : {}), source: 'natural_language_request' })
      }
      return result(created)
    }
    case 'task.sku.split': {
      const source = scopeTask(req, required(params, 'task_id'))
      await assertCanonicalTaskScopeForAction(source)
      const idempotencyKey = (typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim()
      const split = service.splitTaskBySku({ workspaceId, taskId: source.id, ...(idempotencyKey ? { idempotencyKey } : {}) })
      if (!split.replayed) for (const task of split.tasks) {
        await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, task.id, 'task.created', task.version, { ...task, task_group_id: split.taskGroupId, source: 'sku_split' })
      }
      await persistEvent(workspaceId, split.sourceTaskId, 'task.sku_split', source.version, { source_task_id: split.sourceTaskId, task_group_id: split.taskGroupId, sku_ids: split.skuIds, replayed: split.replayed })
      return result(split)
    }
    case 'task.group.create': {
      let entries: Array<{ productId: string; platform: Platform; accountId?: string; region?: string; skuId?: string }>
      try {
        const parsed = JSON.parse(required(params, 'entries_json'))
        if (!Array.isArray(parsed)) throw new Error('entries_json must be an array')
        entries = parsed.map(entry => {
          if (!entry || typeof entry !== 'object' || typeof entry.product_id !== 'string' || !SUPPORTED_PLATFORMS.includes(String(entry.platform) as Platform)) throw new Error('invalid task group entry')
          const platform = entry.platform as Platform
          const product = service.products.get(entry.product_id)
          const accountId = resolveTaskAccountId(workspaceId, platform, typeof entry.account_id === 'string' ? entry.account_id : undefined) ?? (product?.workspaceId === workspaceId && product.platform === platform ? product.accountId : undefined)
          requireProductionTaskStore(platform, accountId)
          if ((isProduction() || fixtureMode) && accountId) service.getActivePlatformAccount(workspaceId, accountId, platform)
          return { productId: entry.product_id, platform, ...(accountId ? { accountId } : {}), ...(typeof entry.region === 'string' && entry.region.trim() ? { region: entry.region.trim() } : {}), ...(typeof entry.sku_id === 'string' && entry.sku_id.trim() ? { skuId: entry.sku_id.trim() } : {}) }
        })
      } catch (error) {
        // Preserve actionable business gates (for example an unbound or
        // revoked store). Only malformed JSON/entry shape is an invalid
        // request; collapsing all DomainErrors here hides the recovery step.
        if (error instanceof DomainError) throw error
        throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'entries_json 必须是有效的任务组数组', 400)
      }
      const entryBrandIds = await resolveTaskWriteBrands(req, workspaceId, entries.map(entry => entry.productId))
      for (const entry of entries) await requireEnabledPlatform(workspaceId, entry.platform)
      const idempotencyKey = (typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim()
      const canonicalEntries = await resolveCanonicalTaskEntries(workspaceId, entries)
      const group = service.createTaskGroup({ workspaceId, entries: canonicalEntries, ...(typeof params.request_text === 'string' ? { requestText: params.request_text } : {}), ...(idempotencyKey ? { idempotencyKey } : {}) })
      assignTaskWriteBrands(group.tasks, entryBrandIds)
      if (!group.replayed) for (const task of group.tasks) {
        await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, task.id, 'task.created', task.version, { ...task, task_group_id: group.id })
      }
      return result(group)
    }
    case 'creative.directions': {
      const task = scopeTask(req, required(params, 'task_id'))
      return result(service.listCreativeDirections(workspaceId, task.id))
    }
    case 'creative.brief': {
      const productId = required(params, 'product_id')
      const product = service.products.get(productId)
      if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
      await enforceProductBrandAccess(req, workspaceId, productId)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      await requirePluginWalletAccess(workspaceId)
      if (!product.factsConfirmed) throw new DomainError('PRODUCT_FACTS_CONFIRMATION_REQUIRED', '请先确认商品、SKU、价格和图片事实，再生成创意 Brief', 409)
      service.assertBrandVisualGenerationReady(workspaceId, product.platform)
      const brief = creativeBrief(workspaceId, product, params)
      const briefDebitKey = `creative-brief:${brief.id}`
      const briefActor = requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
      await debitPluginWallet({ workspaceId, idempotencyKey: briefDebitKey, actorId: briefActor, description: '创意 Brief 生成调用' })
      try {
        await persistEvent(workspaceId, brief.id, 'creative.brief_created', brief.version, { brief_id: brief.id, product_id: product.id, asset_type: brief.assetType, platform: brief.platform, sku_ids: brief.skuIds })
      } catch (error) {
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: briefDebitKey, actorId: briefActor, reason: '创意 Brief 结果记录失败' })
        throw error
      }
      return result(brief)
    }
    case 'creative.preview': {
      const productId = required(params, 'product_id')
      const product = service.products.get(productId)
      if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
      await enforceProductBrandAccess(req, workspaceId, productId)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      await requirePluginWalletAccess(workspaceId)
      if (!product.factsConfirmed) throw new DomainError('PRODUCT_FACTS_CONFIRMATION_REQUIRED', '请先确认商品事实，再生成创意预览', 409)
      service.assertBrandVisualGenerationReady(workspaceId, product.platform)
      const preview = creativePreview(workspaceId, product, params)
      const previewDebitKey = `creative-preview:${preview.id}`
      const previewActor = requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
      await debitPluginWallet({ workspaceId, idempotencyKey: previewDebitKey, actorId: previewActor, description: '创意预览生成调用' })
      try {
        await persistEvent(workspaceId, preview.id, 'creative.preview_created', 1, { preview_id: preview.id, product_id: product.id, asset_type: preview.assetType, platform: preview.platform })
      } catch (error) {
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: previewDebitKey, actorId: previewActor, reason: '创意预览结果记录失败' })
        throw error
      }
      return result(preview)
    }
    case 'creative.directions.update': {
      const task = scopeTask(req, required(params, 'task_id'))
      let directionIds: string[] | undefined
      let changes: Record<string, string> | undefined
      try {
        if (typeof params.direction_ids_json === 'string') {
          const parsed = JSON.parse(params.direction_ids_json)
          if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string')) throw new Error('invalid direction ids')
          directionIds = parsed
        }
        if (typeof params.changes_json === 'string') {
          const parsed = JSON.parse(params.changes_json)
          if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('invalid changes')
          changes = parsed as Record<string, string>
        }
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, '方向参数 JSON 无效', 400) }
      const updated = service.updateCreativeDirections({ workspaceId, taskId: task.id, action: required(params, 'action') as 'regenerate' | 'merge' | 'modify', ...(directionIds ? { directionIds } : {}), ...(typeof params.direction_id === 'string' ? { directionId: params.direction_id } : {}), ...(changes ? { changes } : {}), ...(typeof params.feedback === 'string' ? { feedback: params.feedback } : {}), ...(typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? { expectedVersion: Number(params.expected_version) } : {}) })
      await persistSnapshot(workspaceId, 'task', updated.task, updated.task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, updated.task.id, 'task.directions_updated', updated.task.version, { task_id: updated.task.id, action: params.action, direction_id: updated.newDirection?.id ?? null })
      return result(updated)
    }
    case 'task.select_direction': {
      const task = scopeTask(req, required(params, 'task_id'))
      const selected = service.selectDirection(task.id, required(params, 'direction_id'), typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined)
      await persistSnapshot(workspaceId, 'task', selected, selected as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, selected.id, 'task.direction_selected', selected.version, { task_id: selected.id, direction_id: selected.selectedDirectionId ?? null })
      return result({ ...selected, task_id: selected.id, expected_version: selected.version })
    }
    case 'task.plan.confirm': {
      const task = scopeTask(req, required(params, 'task_id'))
      await assertCanonicalTaskScopeForAction(task)
      const priceImpactConfirmed = params.price_impact_confirmed === true || params.price_impact_confirmed === 'true'
      const planProduct = service.products.get(task.productId)
      if (planProduct) await hydrateDurableRuleSnapshot(workspaceId, planProduct)
      const confirmed = service.confirmProductionPlan(workspaceId, task.id, requestActor(req, typeof params.actor_id === 'string' && params.actor_id.trim() ? params.actor_id.trim() : 'merchant'), typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined, priceImpactConfirmed)
      await persistSnapshot(workspaceId, 'task', confirmed, confirmed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, confirmed.id, 'task.plan_confirmed', confirmed.version, { task_id: confirmed.id, plan_id: confirmed.productionPlan?.id ?? null, actor_id: confirmed.productionPlan?.confirmedBy ?? null })
      await recordOperationAudit({ workspaceId, actorId: confirmed.productionPlan?.confirmedBy ?? requestActor(req), action: 'task.plan_confirmed', resourceType: 'task', resourceId: confirmed.id, before: { state: task.state, version: task.version }, after: { state: confirmed.state, version: confirmed.version, plan_id: confirmed.productionPlan?.id ?? null }, reason: '确认生产方案' })
      return result({ ...confirmed, task_id: confirmed.id, expected_version: confirmed.version })
    }
    case 'content.generate': {
      const task = scopeTask(req, required(params, 'task_id'))
      // Content generation is a canonical business action. Re-check the
      // task's product/listing binding at the action boundary so a legacy
      // task cannot bypass canonical_read after the task was created.
      await assertCanonicalTaskScopeForAction(task)
      await requirePluginWalletAccess(workspaceId)
      await requireGenerationRulePreflight(workspaceId, task.productId)
      if (durableContentGenerationEnvironment()) {
        requirePlatformModelCostGate('text')
        const idempotencyKey = header(req, 'idempotency-key')?.trim() || (typeof params.idempotency_key === 'string' ? params.idempotency_key.trim() : '')
        if (!idempotencyKey) throw new DomainError(ERROR_CODES.IDEMPOTENCY_KEY_REQUIRED, '持久化内容生成必须携带 Idempotency-Key', 400)
        const product = service.products.get(task.productId)
        if (!product || product.workspaceId !== task.workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品快照不存在或不属于当前工作区', 404)
        let existing = [...service.generationJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === idempotencyKey)
        if (!existing) {
          await hydrateDurableIdempotentJob(workspaceId, 'generation_job', idempotencyKey)
          existing = [...service.generationJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === idempotencyKey)
        }
        const reservationId = `generation:${idempotencyKey}`
        const reserved = existing ? false : await reserveDistributedJobSlot(workspaceId, reservationId)
        const usageKey = `generation:${idempotencyKey}`
        const usage = await consumeTaskUsage(workspaceId, task.id, usageKey, requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant')
        try {
          await reserveDailyModelBudget(workspaceId, `model:${usageKey}`, task.id, 'text')
          const prepared = await service.prepareGenerationContext(task.id, `model:${usageKey}`)
          const job = service.enqueueGeneration({ workspaceId, taskId: task.id, idempotencyKey })
          const authorizationSnapshot = workerAuthorizationSnapshot(req, workspaceId, job.id, 'generation.execute', { method: 'content.generate', task_id: task.id, task_version: task.version })
          if (!authorizationSnapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '内容生成缺少持久身份授权快照，已拒绝入队', 503)
          await persistSnapshot(workspaceId, 'generation_job', job, job as unknown as Record<string, unknown>)
          if (job.state === 'queued' && job.revision === 1) await persistEvent(workspaceId, job.id, 'generation.requested', 1, { job_id: job.id, task_id: task.id, campaign_item_id: task.campaignItemId ?? null, platform: task.platform, direction_id: task.selectedDirectionId ?? 'default', action_id: `model:${usageKey}`, run_key: task.id, context_link_id: prepared.contextRef?.id ?? null, context_hash: prepared.contextRef?.contextHash ?? contextEnvelopeHash(prepared.input as unknown as Record<string, unknown>), input_tokens_estimate: prepared.inputTokensEstimate, max_input_tokens: prepared.maxInputTokens, input: prepared.input, authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) })
          return result(jobWithQueueMetadata(job, workspaceId, 'generation'))
        } catch (error) {
          if ((usage.charged || usage.walletDebited) && !existing) await refundTaskUsage(workspaceId, task.id, usageKey, requestPrincipals.get(req)?.actorId ?? 'merchant', '异步生成任务创建失败')
          if (reserved) await releaseDistributedJobSlot(workspaceId, reservationId)
          throw error
        }
      }
      const usageKey = `content.generate:${task.id}`
      const usage = await consumeTaskUsage(workspaceId, task.id, usageKey, requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant')
      let draft
      try { draft = await service.generateDraft(task.id, undefined, `model:${usageKey}`) } catch (error) { if (providerSucceededButSettlementPending(error)) await markTaskUsageProviderOutcomePending(workspaceId, usageKey); else if (usage.charged || usage.walletDebited) await refundTaskUsage(workspaceId, task.id, usageKey, requestPrincipals.get(req)?.actorId ?? 'merchant', '内容生成失败'); throw error }
      const execution = await requireSettledContentExecutionEvidence(workspaceId, 'model:' + usageKey)
      await persistSnapshot(workspaceId, 'content_version', draft, draft as unknown as Record<string, unknown>)
      await persistSnapshot(workspaceId, 'task', service.getTask(task.id), service.getTask(task.id) as unknown as Record<string, unknown>)
      const rulePreflight = service.reviewContentReport(workspaceId, draft.id, await evaluationRules(workspaceId, ruleContextForTask(task)))
      await persistEvent(workspaceId, draft.id, 'content.generated', draft.revision, { task_id: task.id, content_version_id: draft.id, version: draft.version, execution, rule_preflight: { blocking: rulePreflight.blocking, finding_count: rulePreflight.findings.length, rule_hits: rulePreflight.ruleHits ?? [] } })
      return result({ ...draft, execution, rule_preflight: rulePreflight })
    }
    case 'content.codex.prepare': {
      const task = scopeTask(req, required(params, 'task_id'))
      return result(service.prepareCodexDraft(task.id))
    }
    case 'content.codex.commit': {
      if (controlledRelayEnvironment()) throw new DomainError('MODEL_RELAY_REQUIRED', '受控环境禁止绕过中转模型直接提交内容', 409)
      const task = scopeTask(req, required(params, 'task_id'))
      await assertCanonicalTaskScopeForAction(task)
      await requirePluginWalletAccess(workspaceId)
      let body: import('../../../packages/application/src/service.js').ContentVersion['body']
      try {
        const parsed = JSON.parse(required(params, 'body_json')) as Record<string, unknown>
        if (typeof parsed.title !== 'string' || typeof parsed.detail !== 'string' || !Array.isArray(parsed.sellingPoints) || !parsed.sellingPoints.every(value => typeof value === 'string')) throw new Error('content body schema invalid')
        body = { title: parsed.title, detail: parsed.detail, sellingPoints: parsed.sellingPoints as string[], ...(Array.isArray(parsed.modules) ? { modules: parsed.modules as import('../../../packages/ai/src/generator.js').ContentModule[] } : {}), ...(parsed.brief && typeof parsed.brief === 'object' && !Array.isArray(parsed.brief) ? { brief: parsed.brief as import('../../../packages/ai/src/generator.js').StaticBrief } : {}) }
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'body_json 必须包含合法的 title、detail、sellingPoints', 400) }
      const usageKey = `content.codex.commit:${task.id}:${typeof params.expected_version === 'string' ? params.expected_version : 'latest'}`
      const usage = await consumeTaskUsage(workspaceId, task.id, usageKey, requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant')
      let draft
      try { draft = service.commitCodexDraft({ taskId: task.id, body, ...(typeof params.reason === 'string' && params.reason.trim() ? { reason: params.reason.trim() } : {}) }) } catch (error) { if (usage.charged || usage.walletDebited) await refundTaskUsage(workspaceId, task.id, usageKey, requestPrincipals.get(req)?.actorId ?? 'merchant', 'Codex 内容提交失败'); throw error }
      await persistSnapshot(workspaceId, 'content_version', draft, draft as unknown as Record<string, unknown>)
      await persistSnapshot(workspaceId, 'task', service.getTask(task.id), service.getTask(task.id) as unknown as Record<string, unknown>)
      const rulePreflight = service.reviewContentReport(workspaceId, draft.id, await evaluationRules(workspaceId, ruleContextForTask(task)))
      await persistEvent(workspaceId, draft.id, 'content.generated', draft.revision, { task_id: task.id, content_version_id: draft.id, version: draft.version, generation_mode: 'codex_native', rule_preflight: { blocking: rulePreflight.blocking, finding_count: rulePreflight.findings.length, rule_hits: rulePreflight.ruleHits ?? [] } })
      return result({ ...draft, rule_preflight: rulePreflight })
    }
    case 'generation.get': {
      const job = service.getGenerationJob(workspaceId, required(params, 'job_id'))
      if (job.workspaceId !== workspaceId) throw new DomainError('WORKSPACE_SCOPE_MISMATCH', '无权访问该生成任务', 403)
      return result({ ...jobWithQueueMetadata(job, workspaceId, 'generation'), workflow: projectGenerationWorkflow(workspaceId, job) })
    }
    case 'content.review': {
      const scoped = scopeContentVersion(req, required(params, 'content_version_id'))
      await assertCanonicalTaskScopeForAction(scoped.task)
      return result(service.reviewContentReport(workspaceId, scoped.version.id, await evaluationRules(workspaceId, ruleContextForTask(scoped.task))))
    }
    case 'content.review.decide': {
      const scoped = scopeContentVersion(req, required(params, 'content_version_id'))
      await assertCanonicalTaskScopeForAction(scoped.task)
      const status = required(params, 'status')
      if (!['acknowledged', 'waived'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'status 必须是 acknowledged 或 waived', 400)
      const decided = service.setReviewFindingDecision({ workspaceId, contentVersionId: scoped.version.id, code: required(params, 'code'), field: required(params, 'field'), status: status as 'acknowledged' | 'waived', ...(typeof params.reason === 'string' ? { reason: params.reason } : {}), actorId: requestActor(req), ...(typeof params.expected_revision === 'string' && /^\d+$/u.test(params.expected_revision) ? { expectedRevision: Number(params.expected_revision) } : {}) }, await evaluationRules(workspaceId, ruleContextForTask(scoped.task)))
      await persistSnapshot(workspaceId, 'content_version', decided.version, decided.version as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, decided.version.id, 'content.review_decided', decided.version.revision, { content_version_id: decided.version.id, finding_key: decided.decision.key, status: decided.decision.status, reason: decided.decision.reason, actor_id: decided.decision.actorId })
      return result(decided)
    }
    case 'content.versions': {
      const task = scopeTask(req, required(params, 'task_id'))
      const page = mcpPagination(params)
      return result(params.limit !== undefined || params.offset !== undefined ? service.listContentVersionsPage(workspaceId, task.id, page) : service.listContentVersions(workspaceId, task.id))
    }
    case 'content.diff': {
      const scoped = scopeContentVersion(req, required(params, 'content_version_id'))
      const against = typeof params.against_version_id === 'string' ? params.against_version_id : undefined
      return result(service.diffContentVersions(workspaceId, scoped.version.id, against))
    }
    case 'content.export': {
      const contentVersionId = typeof params.content_version_id === 'string' && params.content_version_id.trim() ? params.content_version_id.trim() : undefined
      const deliverableRef = typeof params.deliverable_ref === 'string' && params.deliverable_ref.trim() ? params.deliverable_ref.trim() : undefined
      if (Boolean(contentVersionId) === Boolean(deliverableRef)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '必须且只能指定 content_version_id 或 deliverable_ref 其中一个', 400)
      const scoped = contentVersionId ? scopeContentVersion(req, contentVersionId) : (() => {
        const version = service.resolveDeliverableReference(workspaceId, deliverableRef!)
        return { version, task: service.getTask(version.taskId) }
      })()
      await assertCanonicalTaskScopeForAction(scoped.task)
      const format = typeof params.format === 'string' ? params.format : 'bundle'
      if (!['manifest', 'json', 'markdown', 'bundle'].includes(format)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'format 必须是 manifest、json、markdown 或 bundle', 400)
      await persistExpiredDeliveryIfNeeded(workspaceId, scoped.version.id)
      const exported = service.exportContent(workspaceId, scoped.version.id, format as 'manifest' | 'json' | 'markdown' | 'bundle')
      const exportBytes = exported.binaryBody?.byteLength ?? Buffer.byteLength(exported.body, 'utf8')
      if (!exportBytes || exportBytes > MAX_MCP_EXPORT_BYTES) throw new DomainError('CONTENT_EXPORT_SIZE_LIMIT', '内容导出文件为空或超过 25MB 限制', 413)
      if (!exported.binaryBody) return result(exported)
      const { binaryBody, ...textExport } = exported
      const bundleVerification = verifyExportedBundle(workspaceId, scoped.version.id, binaryBody)
      return result({ ...textExport, binary_base64: Buffer.from(binaryBody).toString('base64'), bundle_verification: bundleVerification })
    }
    case 'content.approve': {
      const taskId = required(params, 'task_id')
      const task = scopeTask(req, taskId)
      await assertCanonicalTaskScopeForAction(task)
      const approved = service.approveContent(taskId, required(params, 'content_version_id'), await evaluationRules(workspaceId, ruleContextForTask(task)), typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined)
      await persistSnapshot(workspaceId, 'content_version', approved.version, approved.version as unknown as Record<string, unknown>)
      await persistSnapshot(workspaceId, 'task', approved.task, approved.task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, approved.version.id, 'content.approved', approved.version.revision, { task_id: approved.task.id, content_version_id: approved.version.id, version: approved.version.version })
      return result(approved)
    }
    case 'content.modify': {
      const scoped = scopeContentVersion(req, required(params, 'content_version_id'))
      await assertCanonicalTaskScopeForAction(scoped.task)
      let lockedFields: string[] | undefined
      if (typeof params.locked_fields_json === 'string') {
        try { const parsed = JSON.parse(params.locked_fields_json); if (!Array.isArray(parsed) || parsed.some(value => typeof value !== 'string')) throw new Error('locked_fields_json must be array'); lockedFields = parsed } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'locked_fields_json 必须是字符串数组', 400) }
      }
      const expectedRevision = typeof params.expected_revision === 'string' && /^\d+$/u.test(params.expected_revision) ? Number(params.expected_revision) : undefined
      const modified = typeof params.module_key === 'string' && params.module_key.trim()
        ? service.regenerateContentModule({ workspaceId, sourceVersionId: scoped.version.id, moduleKey: params.module_key, ...(lockedFields ? { lockedFields } : {}), reason: required(params, 'reason'), ...(expectedRevision !== undefined ? { expectedRevision } : {}) })
        : (() => {
          let changes: Partial<import('../../../packages/application/src/service.js').ContentVersion['body']>
          try {
            const parsed = JSON.parse(required(params, 'changes_json'))
            if (!parsed || typeof parsed !== 'object' || Array.isArray(parsed)) throw new Error('changes_json must be object')
            changes = parsed
          } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'changes_json 必须是 JSON 对象', 400) }
          return service.modifyContentVersion({ workspaceId, sourceVersionId: scoped.version.id, changes, ...(lockedFields ? { lockedFields } : {}), reason: required(params, 'reason'), ...(expectedRevision !== undefined ? { expectedRevision } : {}) })
        })()
      await persistSnapshot(workspaceId, 'content_version', modified.version, modified.version as unknown as Record<string, unknown>)
      await persistSnapshot(workspaceId, 'task', modified.task, modified.task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, modified.version.id, 'content.version_modified', modified.version.revision, { task_id: modified.task.id, source_version_id: modified.source.id, content_version_id: modified.version.id, reason: modified.version.versionVector?.reason, locked_fields: modified.version.lockedFields ?? [], ...(typeof params.module_key === 'string' && params.module_key.trim() ? { regenerated_module: params.module_key.trim() } : {}) })
      return result(modified)
    }
    case 'content.restore': {
      const scoped = scopeContentVersion(req, required(params, 'content_version_id'))
      await assertCanonicalTaskScopeForAction(scoped.task)
      const restored = service.restoreContentVersion(workspaceId, scoped.version.id, typeof params.expected_version === 'string' && /^\d+$/u.test(params.expected_version) ? Number(params.expected_version) : undefined)
      await persistSnapshot(workspaceId, 'content_version', restored.version, restored.version as unknown as Record<string, unknown>)
      await persistSnapshot(workspaceId, 'task', restored.task, restored.task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, restored.version.id, 'content.version_restored', restored.version.revision, { task_id: restored.task.id, source_version_id: restored.source.id, content_version_id: restored.version.id })
      return result(restored)
    }
    case 'publish.prepare': {
      const taskId = required(params, 'task_id')
      const task = scopeTask(req, taskId)
      const canonicalProof = await assertCanonicalTaskScopeForAction(task)
      await requireEnabledPlatform(workspaceId, task.platform)
      const mappingPreflight = await requireCurrentPlatformMapping(task)
      if ((isProduction() || fixtureMode) && !task.accountId) throw new DomainError('STORE_SELECTION_REQUIRED', '发布前必须明确选择商品所属店铺', 409)
      const currentRuleReview = await requireCurrentPublishReview(workspaceId, task)
      const taskBeforePrepare = structuredClone(task)
      const preview = service.preparePublish(taskId)
      if (canonicalProof?.canonicalReadRevision && preview.task.pendingPublish) preview.task.pendingPublish.canonicalReadRevision = canonicalProof.canonicalReadRevision
      try {
        await persistSnapshotsAndEvent({ workspaceId, snapshots: [{ entityType: 'task', entityId: preview.task.id, entityVersion: preview.task.version, payload: preview.task as unknown as Record<string, unknown> }], aggregateId: preview.task.id, eventType: 'publish.prepared', sequence: preview.task.version, eventPayload: { task_id: preview.task.id, content_version_id: preview.version.id, confirmation_hash: preview.confirmationHash, remote_snapshot_hash: preview.remoteSnapshotHash, payload_hash: preview.payloadHash, selection_hash: preview.selectionHash, selected_count: preview.visualPreview.count, image_mode: preview.visualPreview.imageMode } })
      } catch (error) {
        service.tasks.set(task.id, taskBeforePrepare)
        throw error
      }
      return result({ ...preview, currentRuleReview, ...(mappingPreflight ? { mappingPreflight: { publishable: true, confirmationValid: true, mappedPayloadHash: mappingPreflight.mappedPayloadHash } } : {}) })
    }
    case 'publish.batch.prepare': {
      const rawTaskIds = required(params, 'task_ids_json')
      let taskIds: string[]
      try {
        const parsed = JSON.parse(rawTaskIds)
        if (!Array.isArray(parsed) || parsed.length < 1 || parsed.length > 50 || parsed.some(item => typeof item !== 'string' || !item.trim()) || new Set(parsed).size !== parsed.length) throw new Error('invalid')
        taskIds = parsed.map(item => String(item).trim())
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'task_ids_json 必须是 1 至 50 个不重复任务 ID 的 JSON 数组', 400) }
      const taskRefs: Array<{ task: ReturnType<typeof service.getTask>; before: ReturnType<typeof structuredClone> }> = []
      const batchId = 'batch_' + randomUUID()
      for (const taskId of taskIds) {
        const task = scopeTask(req, taskId)
        await enforceTaskBrandAccess(req, task, 'publisher')
        await assertCanonicalTaskScopeForAction(task)
        await requireEnabledPlatform(workspaceId, task.platform)
        await requireCurrentPlatformMapping(task)
        if ((isProduction() || fixtureMode) && !task.accountId) throw new DomainError('STORE_SELECTION_REQUIRED', `任务 ${taskId} 未绑定店铺`, 409)
        await requireCurrentPublishReview(workspaceId, task)
        taskRefs.push({ task, before: structuredClone(task) })
      }
      let previews: Array<ReturnType<typeof service.preparePublish>>
      try {
        previews = taskRefs.map(({ task }) => service.preparePublish(task.id))
      } catch (error) {
        // preparePublish updates only the task pointer/state. Restore every
        // task before returning so a failed batch leaves no partial prepared
        // children in memory or in the next persistence transaction.
        for (const { task, before } of taskRefs) {
          for (const key of Object.keys(task)) delete (task as unknown as Record<string, unknown>)[key]
          Object.assign(task, before)
        }
        throw error
      }
      for (const preview of previews) {
        const canonicalProof = await assertCanonicalTaskScopeForAction(preview.task)
        if (canonicalProof?.canonicalReadRevision && preview.task.pendingPublish) preview.task.pendingPublish.canonicalReadRevision = canonicalProof.canonicalReadRevision
        await persistSnapshot(workspaceId, 'task', preview.task, preview.task as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, preview.task.id, 'publish.prepared', preview.task.version, { task_id: preview.task.id, content_version_id: preview.version.id, confirmation_hash: preview.confirmationHash, remote_snapshot_hash: preview.remoteSnapshotHash, payload_hash: preview.payloadHash, selection_hash: preview.selectionHash, selected_count: preview.visualPreview.count, image_mode: preview.visualPreview.imageMode, batch: true })
      }
      const timestamp = new Date().toISOString()
      const batch: PublishBatch = {
        id: batchId,
        workspaceId,
        state: 'prepared',
        items: previews.map(preview => ({ taskId: preview.task.id, platform: preview.task.platform, ...(preview.task.accountId ? { accountId: preview.task.accountId } : {}), contentVersionId: preview.version.id, confirmationHash: preview.confirmationHash, remoteSnapshotHash: preview.remoteSnapshotHash, state: 'prepared' as const })),
        createdAt: timestamp,
        updatedAt: timestamp,
        revision: 1,
      }
      publishBatches.set(batch.id, batch)
      await persistSnapshot(workspaceId, 'publish_batch', batch, batch as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, batch.id, 'publish.batch.prepared', batch.revision, { batch_id: batch.id, item_count: batch.items.length })
      persistedPublishBatches.set(batch.id, structuredClone(batch))
      return result({ isBatch: true, batchId, batch, count: previews.length, items: previews, confirmationRequiredPerItem: true, executionMode: 'confirm_each_item' })
    }
    case 'publish.batch.confirm': {
      await requirePluginWalletAccess(workspaceId)
      const requestedBatchId = required(params, 'batch_id')
      let batch = publishBatches.get(requestedBatchId)
      if (!batch || batch.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该批量发布批次', 403)
      if (batch?.state === 'paused') throw new DomainError('PUBLISH_BATCH_PAUSED', '该批次已暂停，请先恢复后再确认或重试', 409)
      let confirmations: Array<Record<string, unknown>>
      try {
        const parsed = JSON.parse(required(params, 'confirmations_json'))
        if (!Array.isArray(parsed) || parsed.length < 1 || parsed.length > 50 || parsed.some(item => !item || typeof item !== 'object')) throw new Error('invalid')
        confirmations = parsed as Array<Record<string, unknown>>
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'confirmations_json 必须是 1 至 50 个确认对象的 JSON 数组', 400) }
      assertUniqueBatchTaskIds(confirmations)
      const items: Array<Record<string, unknown>> = []
      for (const confirmation of confirmations) {
        const taskId = typeof confirmation.task_id === 'string' ? confirmation.task_id.trim() : ''
        const itemKey = typeof confirmation.idempotency_key === 'string' ? confirmation.idempotency_key.trim() : ''
        try {
          if (!taskId || !itemKey) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '每个批量发布项都必须有 task_id 和 idempotency_key', 400)
          const task = scopeTask(req, taskId)
          await enforceTaskBrandAccess(req, task, 'publisher')
          await requireEnabledPlatform(workspaceId, task.platform)
          if (batch) {
            const batchItem = batch.items.find(item => item.taskId === taskId)
            if (!batchItem) throw new DomainError('PUBLISH_BATCH_ITEM_NOT_FOUND', `任务 ${taskId} 不属于该批次`, 400)
            if (['published', 'submitted', 'queued'].includes(batchItem.state)) throw new DomainError('PUBLISH_BATCH_ITEM_ALREADY_QUEUED', `任务 ${taskId} 已经排队`, 409)
          }
          const contentVersionId = typeof confirmation.content_version_id === 'string' ? confirmation.content_version_id : ''
          const confirmationHash = typeof confirmation.confirmation_hash === 'string' ? confirmation.confirmation_hash : ''
          const remoteSnapshotHash = typeof confirmation.remote_snapshot_hash === 'string' ? confirmation.remote_snapshot_hash : ''
          if (!contentVersionId || !confirmationHash || !remoteSnapshotHash) throw new DomainError(ERROR_CODES.CONFIRMATION_REQUIRED, `任务 ${taskId} 缺少新鲜确认哈希`, 400)
          const accountId = resolveTaskPublishAccount(task, typeof confirmation.account_id === 'string' ? confirmation.account_id : undefined)
          if ((isProduction() || fixtureMode) && !accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', `任务 ${taskId} 未绑定店铺`, 400)
          if (isProduction() || fixtureMode) service.getActivePlatformAccount(workspaceId, accountId!, task.platform)
          if (!task.contentVersionId || task.contentVersionId !== contentVersionId) throw new DomainError(ERROR_CODES.CONFIRMATION_REQUIRED, `任务 ${taskId} 内容版本已变化`, 400)
          assertPublishIdempotency(workspaceId, { taskId, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: itemKey })
          let existing = [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === itemKey)
          if (!existing) await hydrateDurableIdempotentJob(workspaceId, 'publish_job', itemKey)
          existing = existing ?? [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === itemKey)
          const authorizationSnapshot = existing ? undefined : requirePublishAuthorizationSnapshot(publishAuthorizationSnapshot(req, workspaceId, task))
          if (!existing) await consumePublishConfirmationTicket(req, workspaceId, { workspaceId, taskId, contentVersionId, confirmationHash, remoteSnapshotHash, params: confirmation })
          const reservationId = `publish:${itemKey}`
          let reserved = false
          let walletDebited = false
          try {
            if (!existing) {
              await debitPluginWallet({ workspaceId, idempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', description: '批量商品发布调用' })
              walletDebited = true
            }
            reserved = existing ? false : await reserveDistributedJobSlot(workspaceId, reservationId)
            const job = service.confirmPublish({ workspaceId, taskId, batchId: batch.id, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: itemKey, ...(accountId ? { accountId } : {}), mediaAdapterReady: connectorRuntime.mediaUploadReady(task.platform), ...(authorizationSnapshot ? { authorizationSnapshot } : {}), deferCommit: true })
            await persistPublishJobWithBatch({ batch, task, job, itemState: 'queued' })
            scheduleFixturePublishObservation(job)
            items.push({ task_id: taskId, state: 'queued', job: jobWithQueueMetadata(job, workspaceId, 'publish') })
          } catch (error) {
            if (reserved) await releaseDistributedJobSlot(workspaceId, reservationId)
            if (walletDebited) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', reason: '批量商品发布任务创建失败' })
            throw error
          }
        } catch (error) {
          if (!batch && taskId) batch = { id: `batch_${randomUUID()}`, workspaceId, state: 'prepared', items: [], createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(), revision: 0 }
          if (batch && taskId) {
            const batchItem = batch.items.find(item => item.taskId === taskId)
            if (batchItem) Object.assign(batchItem, { state: 'failed' as const, error: error instanceof DomainError ? { code: error.code, message: error.message } : { code: 'BATCH_ITEM_FAILED', message: error instanceof Error ? error.message : '批量项目失败' } })
          }
          items.push({ task_id: taskId || null, state: 'failed', error: error instanceof DomainError ? { code: error.code, message: error.message } : { code: 'BATCH_ITEM_FAILED', message: error instanceof Error ? error.message : '批量项目失败' } })
        }
      }
      if (!batch) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '批量确认未产生有效项目', 400)
      batch.state = batchStateFromItems(batch.items)
      await savePublishBatch(batch, 'publish.batch.confirmed')
      return result({ batch: true, batchId: batch.id, batchState: batch.state, count: items.length, succeeded: items.filter(item => item.state === 'queued').length, failed: items.filter(item => item.state === 'failed').length, items, atomic: false, retryFailedItems: true })
    }
    case 'publish.batch.get': {
      const batch = publishBatches.get(required(params, 'batch_id'))
      if (!batch || batch.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该批量发布批次', 403)
      await refreshPublishBatch(batch)
      return result(batch)
    }
    case 'publish.batch.pause': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops'])
      const batch = publishBatches.get(required(params, 'batch_id'))
      if (!batch || batch.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该批量发布批次', 403)
      if (['completed', 'failed'].includes(batch.state)) throw new DomainError('PUBLISH_BATCH_TERMINAL', '终态批次不能暂停', 409)
      const reason = required(params, 'reason')
      batch.state = 'paused'
      batch.pauseReason = reason
      for (const item of batch.items) if (item.state === 'prepared' || item.state === 'failed') item.state = 'paused'
      await savePublishBatch(batch, 'publish.batch.paused')
      await recordOperationAudit({ workspaceId, actorId, action: 'publish.batch.pause', resourceType: 'publish_batch', resourceId: batch.id, before: { state: 'active' }, after: { state: batch.state, pauseReason: reason, revision: batch.revision }, reason })
      return result({ ...batch, alreadyQueuedContinue: batch.items.some(item => item.state === 'queued' || item.state === 'submitted') })
    }
    case 'publish.batch.resume': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops'])
      const batch = publishBatches.get(required(params, 'batch_id'))
      if (!batch || batch.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该批量发布批次', 403)
      if (batch.state !== 'paused') return result(batch)
      const previousState = batch.state
      for (const item of batch.items) if (item.state === 'paused') item.state = 'failed'
      batch.pauseReason = undefined
      batch.state = batchStateFromItems(batch.items)
      await savePublishBatch(batch, 'publish.batch.resumed')
      await recordOperationAudit({ workspaceId, actorId, action: 'publish.batch.resume', resourceType: 'publish_batch', resourceId: batch.id, before: { state: previousState }, after: { state: batch.state, revision: batch.revision }, reason: '运营台恢复批量发布' })
      return result(batch)
    }
    case 'publish.batch.retry_failed': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops'])
      await requirePluginWalletAccess(workspaceId)
      const batch = publishBatches.get(required(params, 'batch_id'))
      if (!batch || batch.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该批量发布批次', 403)
      if (batch.state === 'paused') throw new DomainError('PUBLISH_BATCH_PAUSED', '该批次已暂停，请先恢复后再重试', 409)
      let confirmations: Array<Record<string, unknown>>
      try {
        const parsed = JSON.parse(required(params, 'confirmations_json'))
        if (!Array.isArray(parsed) || parsed.length < 1 || parsed.length > 50 || parsed.some(item => !item || typeof item !== 'object')) throw new Error('invalid')
        confirmations = parsed as Array<Record<string, unknown>>
      } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'confirmations_json 必须是 1 至 50 个确认对象的 JSON 数组', 400) }
      assertUniqueBatchTaskIds(confirmations)
      const failedTaskIds = new Set(batch.items.filter(item => item.state === 'failed' || item.state === 'rejected' || item.state === 'unknown').map(item => item.taskId))
      const submittedTaskIds = confirmations.filter(item => typeof item.task_id === 'string').map(item => String(item.task_id))
      const invalid = submittedTaskIds.filter(taskId => !failedTaskIds.has(taskId))
      if (invalid.length) throw new DomainError('PUBLISH_BATCH_RETRY_SCOPE_INVALID', `只能重试失败项: ${invalid.join(', ')}`, 400)
      const items: Array<Record<string, unknown>> = []
      for (const confirmation of confirmations) {
        const taskId = typeof confirmation.task_id === 'string' ? confirmation.task_id.trim() : ''
        const itemKey = typeof confirmation.idempotency_key === 'string' ? confirmation.idempotency_key.trim() : ''
        try {
          if (!taskId || !itemKey) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '每个重试项都必须有 task_id 和新的 idempotency_key', 400)
          const task = scopeTask(req, taskId)
          await enforceTaskBrandAccess(req, task, 'publisher')
          const batchItem = batch.items.find(item => item.taskId === taskId)
          if (!batchItem || !failedTaskIds.has(taskId)) throw new DomainError('PUBLISH_BATCH_RETRY_SCOPE_INVALID', `任务 ${taskId} 不是失败项`, 400)
          const contentVersionId = typeof confirmation.content_version_id === 'string' ? confirmation.content_version_id : ''
          const confirmationHash = typeof confirmation.confirmation_hash === 'string' ? confirmation.confirmation_hash : ''
          const remoteSnapshotHash = typeof confirmation.remote_snapshot_hash === 'string' ? confirmation.remote_snapshot_hash : ''
          if (!contentVersionId || !confirmationHash || !remoteSnapshotHash) throw new DomainError(ERROR_CODES.CONFIRMATION_REQUIRED, `任务 ${taskId} 缺少新的确认哈希`, 400)
          const accountId = resolveTaskPublishAccount(task, typeof confirmation.account_id === 'string' ? confirmation.account_id : undefined)
          await requireEnabledPlatform(workspaceId, task.platform)
          if ((isProduction() || fixtureMode) && !accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', `任务 ${taskId} 未绑定店铺`, 400)
          if (isProduction() || fixtureMode) service.getActivePlatformAccount(workspaceId, accountId!, task.platform)
          if (!task.contentVersionId || task.contentVersionId !== contentVersionId) throw new DomainError(ERROR_CODES.CONFIRMATION_REQUIRED, `任务 ${taskId} 内容版本已变化`, 400)
          assertPublishIdempotency(workspaceId, { taskId, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: itemKey })
          let existing = [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === itemKey)
          if (!existing) await hydrateDurableIdempotentJob(workspaceId, 'publish_job', itemKey)
          existing = existing ?? [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === itemKey)
          const authorizationSnapshot = existing ? undefined : requirePublishAuthorizationSnapshot(publishAuthorizationSnapshot(req, workspaceId, task))
          if (!existing) await consumePublishConfirmationTicket(req, workspaceId, { workspaceId, taskId, contentVersionId, confirmationHash, remoteSnapshotHash, params: confirmation })
          const reservationId = `publish:${itemKey}`
          let reserved = false
          let walletDebited = false
          try {
            if (!existing) {
              await debitPluginWallet({ workspaceId, idempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', description: '批量失败项重试发布调用' })
              walletDebited = true
            }
            reserved = existing ? false : await reserveDistributedJobSlot(workspaceId, reservationId)
            const job = service.confirmPublish({ workspaceId, taskId, batchId: batch.id, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: itemKey, ...(accountId ? { accountId } : {}), mediaAdapterReady: connectorRuntime.mediaUploadReady(task.platform), ...(authorizationSnapshot ? { authorizationSnapshot } : {}), deferCommit: true })
            await persistPublishJobWithBatch({ batch, task, job, itemState: 'queued' })
            scheduleFixturePublishObservation(job)
            items.push({ task_id: taskId, state: 'queued', job: jobWithQueueMetadata(job, workspaceId, 'publish') })
          } catch (error) {
            if (reserved) await releaseDistributedJobSlot(workspaceId, reservationId)
            if (walletDebited) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', reason: '批量失败项重试任务创建失败' })
            throw error
          }
        } catch (error) {
          const failure = error instanceof DomainError ? { code: error.code, message: error.message } : { code: 'BATCH_RETRY_FAILED', message: error instanceof Error ? error.message : '批量重试失败' }
          const batchItem = batch.items.find(item => item.taskId === taskId)
          if (batchItem) Object.assign(batchItem, { state: 'failed' as const, error: failure })
          items.push({ task_id: taskId || null, state: 'failed', error: failure })
        }
      }
      batch.state = batchStateFromItems(batch.items)
      await savePublishBatch(batch, 'publish.batch.retried')
      await recordOperationAudit({ workspaceId, actorId, action: 'publish.batch.retry_failed', resourceType: 'publish_batch', resourceId: batch.id, before: { state: 'failed_or_partial' }, after: { state: batch.state, succeeded: items.filter(item => item.state === 'queued').length, failed: items.filter(item => item.state === 'failed').length, revision: batch.revision }, reason: '运营台重试批量发布失败项' })
      return result({ batch: true, batchId: batch.id, batchState: batch.state, count: items.length, succeeded: items.filter(item => item.state === 'queued').length, failed: items.filter(item => item.state === 'failed').length, items, retry: true, atomic: false })
    }
    case 'publish.confirm': {
      const key = header(req, 'idempotency-key')?.trim()
      if (!key) throw new DomainError(ERROR_CODES.IDEMPOTENCY_KEY_REQUIRED, '发布确认必须携带 Idempotency-Key', 400)
      const taskId = required(params, 'task_id')
      const task = scopeTask(req, taskId)
      await requireEnabledPlatform(workspaceId, task.platform)
      await requireCurrentPlatformMapping(task)
      const contentVersionId = required(params, 'content_version_id')
      const confirmationHash = required(params, 'confirmation_hash')
      const remoteSnapshotHash = required(params, 'remote_snapshot_hash')
      if (!task.contentVersionId || task.contentVersionId !== contentVersionId) throw new DomainError(ERROR_CODES.CONFIRMATION_REQUIRED, '缺少有效的一次性发布确认 token', 400)
      if (isProduction() && !((typeof params.account_id === 'string' && params.account_id.trim()) || task.accountId)) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产发布必须绑定已授权平台账号', 400)
      const publishAccountId = resolveTaskPublishAccount(task, typeof params.account_id === 'string' ? params.account_id : undefined)
      if (isProduction() || fixtureMode) service.getActivePlatformAccount(workspaceId, publishAccountId!, task.platform)
      if (isProduction() && !platformWriteReady(task.platform)) throw new DomainError('PLATFORM_WRITE_NOT_READY', '平台尚未完成生产写入能力验证，当前不会创建发布任务', 503)
      await requirePluginWalletAccess(workspaceId)
      assertPublishIdempotency(workspaceId, { taskId, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: key })
      let existing = [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === key)
      if (!existing) {
        await hydrateDurableIdempotentJob(workspaceId, 'publish_job', key)
        existing = [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === key)
      }
      const authorizationSnapshot = existing ? undefined : requirePublishAuthorizationSnapshot(publishAuthorizationSnapshot(req, workspaceId, task))
      const publishInput = { workspaceId, taskId, contentVersionId, confirmationHash, remoteSnapshotHash, params }
      const fencedTicket = existing ? undefined : await reservePublishConfirmationTicket(req, workspaceId, publishInput)
      if (!existing && !fencedTicket) await consumePublishConfirmationTicket(req, workspaceId, publishInput)
      const reservationId = `publish:${key}`
      let reserved = false
      let walletDebited = false
      try {
        const job = service.confirmPublish({ workspaceId, taskId, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: key, ...(publishAccountId ? { accountId: publishAccountId } : {}), mediaAdapterReady: connectorRuntime.mediaUploadReady(task.platform), authorizationSnapshot, deferCommit: true })
        const currentTask = existing ? service.getTask(taskId) : { ...task, state: 'publishing' as const, version: task.version + 1 }
        const snapshots: Array<{ entityType: 'task' | 'publish_job'; entityId: string; entityVersion: number; payload: Record<string, unknown> }> = [
          { entityType: 'task' as const, entityId: currentTask.id, entityVersion: currentTask.version, payload: currentTask as unknown as Record<string, unknown> },
          { entityType: 'publish_job' as const, entityId: job.id, entityVersion: job.revision, payload: job as unknown as Record<string, unknown> },
        ]
        if (fencedTicket && persistence.persistPublishTransaction) {
          await runFencedSinglePublish({
            ticketRepository: persistence.interactiveConfirmationTickets as TransactionalInteractiveConfirmationTicketRepository,
            ticket: fencedTicket.ticket,
            consumedOperationId: job.id,
            wallet: {
              debit: async () => { await debitPluginWallet({ workspaceId, idempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', description: '商品发布调用' }); walletDebited = true },
              refund: async () => { await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', reason: '商品发布任务创建失败' }); walletDebited = false },
            },
            slot: {
              reserve: async () => { reserved = await reserveDistributedJobSlot(workspaceId, reservationId) },
              release: async () => { if (reserved) await releaseDistributedJobSlot(workspaceId, reservationId); reserved = false },
            },
            persist: async ({ finalizeInTransaction }) => {
              await persistence.persistPublishTransaction!({ workspaceId, snapshots, aggregateId: job.id, eventType: 'publish.requested', sequence: 1, eventPayload: publishEventPayload(job), finalizeTicketInTransaction: finalizeInTransaction })
              return { status: 'committed' as const, value: job }
            },
          })
        } else {
          if (!existing) {
            await debitPluginWallet({ workspaceId, idempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', description: '商品发布调用' })
            walletDebited = true
          }
          reserved = existing ? false : await reserveDistributedJobSlot(workspaceId, reservationId)
          await persistSnapshotsAndEvent({ workspaceId, snapshots, aggregateId: job.id, eventType: 'publish.requested', sequence: 1, eventPayload: publishEventPayload(job) })
        }
        service.commitPublishConfirmation(job)
        scheduleFixturePublishObservation(job)
        return result(jobWithQueueMetadata(job, workspaceId, 'publish'))
      } catch (error) {
        if (!(error instanceof PublishCommitStatusUnknownError)) {
          if (reserved) await releaseDistributedJobSlot(workspaceId, reservationId)
          if (walletDebited) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', reason: '商品发布任务创建失败' })
        }
        throw error
      }
    }
    case 'publish.get': {
      const job = service.getPublishJob(required(params, 'publish_job_id'))
      if (job.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该发布任务', 403)
      await enforceTaskBrandAccess(req, service.getTask(job.taskId), 'viewer')
      return result({ ...jobWithQueueMetadata(job, workspaceId, 'publish'), workflow: projectPublishWorkflow(workspaceId, job) })
    }
    case 'automation.policy.get': {
      const platform = typeof params.platform === 'string' ? params.platform as Platform : undefined
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      validateAutomationScope(workspaceId, platform, accountId)
      const key = automationPolicyKey(workspaceId, platform, accountId)
      const policy = automationPolicies.get(key) ?? { workspaceId, id: `automation_${createHash('sha1').update(key).digest('hex').slice(0, 16)}`, ...(platform ? { platform } : {}), ...(accountId ? { accountId } : {}), enabled: false, mode: 'scan_alert_manual_retry' as const, syncEnabled: false, frequencyMinutes: 60, retryLimit: 2, revision: 1, updatedAt: new Date().toISOString(), pauseReason: '默认关闭，需商家明确开启' }
      return result({ policy, unattendedAutoResubmit: false, humanConfirmationRequired: true })
    }
    case 'automation.policy.list': {
      const platformScope = params.platform_scope === 'platform'
      if (params.platform_scope !== undefined && !platformScope) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform_scope 必须是 platform', 400)
      if (platformScope && !isPlatformOperations(req)) throw new DomainError(ERROR_CODES.FORBIDDEN, '只有平台运营角色可以读取平台自动化策略', 403)
      const targetWorkspaceIds = platformScope && persistence.listWorkspaceIds ? await persistence.listWorkspaceIds() : [workspaceId]
      const policies = [...automationPolicies.values()]
        .filter(policy => targetWorkspaceIds.includes(policy.workspaceId))
        .sort((left, right) => `${left.platform ?? ''}:${left.accountId ?? ''}`.localeCompare(`${right.platform ?? ''}:${right.accountId ?? ''}`))
      const stores = new Map(targetWorkspaceIds.flatMap(id => workspaceStoreDirectory(id).map(store => [`${id}:${store.platform}:${store.accountId}`, store] as const)))
      if (platformScope) {
        const groups = new Map<string, { platform?: Platform; enabled: boolean; mode: AutomationPolicy['mode']; syncEnabled: boolean; frequencyMinutes: number; retryLimit: number; count: number; latestUpdatedAt: string }>()
        for (const policy of policies) {
          const key = [policy.platform ?? 'workspace', policy.enabled, policy.mode, policy.syncEnabled, policy.frequencyMinutes, policy.retryLimit].join(':')
          const current = groups.get(key)
          if (current) { current.count += 1; if (policy.updatedAt > current.latestUpdatedAt) current.latestUpdatedAt = policy.updatedAt }
          else groups.set(key, { ...(policy.platform ? { platform: policy.platform } : {}), enabled: policy.enabled, mode: policy.mode, syncEnabled: policy.syncEnabled, frequencyMinutes: policy.frequencyMinutes, retryLimit: policy.retryLimit, count: 1, latestUpdatedAt: policy.updatedAt })
        }
        const aggregatePolicies = [...groups.values()].sort((left, right) => right.latestUpdatedAt.localeCompare(left.latestUpdatedAt)).map((group, index) => ({ id: `platform-automation-group-${index + 1}`, ...(group.platform ? { platform: group.platform } : {}), accountId: `platform-aggregate:${index + 1}`, enabled: group.enabled, mode: group.mode, syncEnabled: group.syncEnabled, frequencyMinutes: group.frequencyMinutes, retryLimit: group.retryLimit, revision: 0, updatedAt: group.latestUpdatedAt, aggregate: true, count: group.count, store: null }))
        return result({ policies: aggregatePolicies, count: aggregatePolicies.length, scope: 'platform', workspaceCount: targetWorkspaceIds.length, aggregate: true, unattendedAutoResubmit: false, humanConfirmationRequired: true })
      }
      return result({
        policies: policies.map(policy => ({
          ...policy,
          store: policy.platform && policy.accountId ? stores.get(`${policy.workspaceId}:${policy.platform}:${policy.accountId}`) ?? null : null,
          unattendedAutoResubmit: false,
          humanConfirmationRequired: true,
        })),
        count: policies.length,
        ...(platformScope ? { scope: 'platform', workspaceCount: targetWorkspaceIds.length } : {}),
        unattendedAutoResubmit: false,
        humanConfirmationRequired: true,
      })
    }
    case 'automation.policy.update': {
      const platform = typeof params.platform === 'string' ? params.platform as Platform : undefined
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      validateAutomationScope(workspaceId, platform, accountId)
      const key = automationPolicyKey(workspaceId, platform, accountId)
      const previous = automationPolicies.get(key)
      const enabled = required(params, 'enabled') === 'true'
      const frequencyMinutes = typeof params.frequency_minutes === 'string' && /^\d+$/u.test(params.frequency_minutes) ? Math.min(1440, Math.max(5, Number(params.frequency_minutes))) : previous?.frequencyMinutes ?? 60
      const retryLimit = typeof params.retry_limit === 'string' && /^\d+$/u.test(params.retry_limit) ? Math.min(5, Math.max(0, Number(params.retry_limit))) : previous?.retryLimit ?? 2
      const syncEnabled = params.sync_enabled === 'true' ? true : params.sync_enabled === 'false' ? false : previous?.syncEnabled ?? false
      if (syncEnabled && (!platform || !accountId)) throw new DomainError('AUTOMATION_SYNC_SCOPE_REQUIRED', '自动同步必须绑定一个明确的平台店铺，不能对工作区内所有店铺隐式执行', 400)
      const clearWindow = params.clear_window === 'true'
      const hasWindowStart = Object.prototype.hasOwnProperty.call(params, 'window_start')
      const hasWindowEnd = Object.prototype.hasOwnProperty.call(params, 'window_end')
      const windowStart = clearWindow ? undefined : hasWindowStart
        ? (typeof params.window_start === 'string' && params.window_start.trim() ? normalizeAutomationTime(params.window_start, 'window_start') : undefined)
        : previous?.windowStart
      const windowEnd = clearWindow ? undefined : hasWindowEnd
        ? (typeof params.window_end === 'string' && params.window_end.trim() ? normalizeAutomationTime(params.window_end, 'window_end') : undefined)
        : previous?.windowEnd
      if (Boolean(windowStart) !== Boolean(windowEnd)) throw new DomainError('AUTOMATION_WINDOW_INVALID', 'window_start 和 window_end 必须同时提供', 400)
      const actorId = requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
      if (requiresStrictAuth()) requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops'])
      const policy: AutomationPolicy = { workspaceId, id: previous?.id ?? `automation_${randomUUID()}`, ...(platform ? { platform } : {}), ...(accountId ? { accountId } : {}), enabled, mode: syncEnabled ? 'scan_sync_alert_manual_retry' : 'scan_alert_manual_retry', syncEnabled, frequencyMinutes, retryLimit, ...(windowStart ? { windowStart } : {}), ...(windowEnd ? { windowEnd } : {}), ...(previous?.lastRunAt ? { lastRunAt: previous.lastRunAt } : {}), ...(enabled ? { nextRunAt: previous?.nextRunAt ?? new Date().toISOString() } : { pauseReason: required(params, 'reason') }), revision: (previous?.revision ?? 0) + 1, updatedAt: new Date().toISOString() }
      await saveAutomationPolicy(policy)
      await recordOperationAudit({ workspaceId, actorId, action: 'automation.policy.update', resourceType: 'automation_policy', resourceId: policy.id, before: previous ?? {}, after: policy as unknown as Record<string, unknown>, reason: required(params, 'reason') })
      return result({ policy, unattendedAutoResubmit: false, humanConfirmationRequired: true })
    }
    case 'automation.scan': {
      const platform = typeof params.platform === 'string' ? params.platform as Platform : undefined
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      validateAutomationScope(workspaceId, platform, accountId)
      return result(await executeAutomationScan(workspaceId, platform, accountId))
    }
    case 'automation.tick': {
      const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops'])
      return result(await runAutomationTick(workspaceId, req, actorId))
    }
    case 'automation.pause': {
      const reason = required(params, 'reason')
      const platform = typeof params.platform === 'string' ? params.platform as Platform : undefined
      const accountId = typeof params.account_id === 'string' && params.account_id.trim() ? params.account_id.trim() : undefined
      validateAutomationScope(workspaceId, platform, accountId)
      const actorId = requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant'
      if (requiresStrictAuth()) requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops'])
      const key = automationPolicyKey(workspaceId, platform, accountId)
      const previous = automationPolicies.get(key)
      const policy: AutomationPolicy = { workspaceId, id: previous?.id ?? `automation_${randomUUID()}`, ...(platform ? { platform } : {}), ...(accountId ? { accountId } : {}), enabled: false, mode: previous?.mode ?? 'scan_alert_manual_retry', syncEnabled: previous?.syncEnabled ?? false, frequencyMinutes: previous?.frequencyMinutes ?? 60, retryLimit: previous?.retryLimit ?? 2, ...(previous?.windowStart ? { windowStart: previous.windowStart } : {}), ...(previous?.windowEnd ? { windowEnd: previous.windowEnd } : {}), pauseReason: reason, revision: (previous?.revision ?? 0) + 1, updatedAt: new Date().toISOString() }
      await saveAutomationPolicy(policy, 'automation.policy.paused')
      await recordOperationAudit({ workspaceId, actorId, action: 'automation.policy.pause', resourceType: 'automation_policy', resourceId: policy.id, before: previous ?? {}, after: policy as unknown as Record<string, unknown>, reason })
      return result({ policy, paused: true, reason, unattendedAutoResubmit: false })
    }
    case 'knowledge.rule.create': {
      try {
        const actorId = requireRuleAdmin(req).actorId
        const target = Object.fromEntries(['platform', 'category', 'brand', 'store', 'campaign'].filter(key => typeof params[key] === 'string' && String(params[key]).trim()).map(key => [key, String(params[key]).trim()]))
        const rule = knowledgeForWorkspace(workspaceId).createRule({
          workspaceId, name: required(params, 'name'), content: required(params, 'content'), scope: required(params, 'scope') as import('../../../packages/knowledge/src/index.js').RuleScope,
          ...(typeof params.scope_value === 'string' ? { scopeValue: params.scope_value } : {}), target,
          source: { kind: required(params, 'source_kind') as import('../../../packages/knowledge/src/index.js').RuleSourceKind, reference: required(params, 'source_reference'), checkedAt: required(params, 'source_checked_at') },
          version: required(params, 'version'), status: required(params, 'status') as import('../../../packages/knowledge/src/index.js').RuleStatus,
          ...(typeof params.severity === 'string' ? { severity: params.severity as import('../../../packages/knowledge/src/index.js').RuleSeverity } : {}), ...(typeof params.action === 'string' ? { action: params.action as import('../../../packages/knowledge/src/index.js').RuleAction } : {}), ...(typeof params.owner_id === 'string' ? { ownerId: params.owner_id } : {}),
          ...(typeof params.effective_from === 'string' ? { effectiveFrom: params.effective_from } : {}), ...(typeof params.effective_to === 'string' ? { effectiveTo: params.effective_to } : {}),
          ...(typeof params.tags_json === 'string' ? { tags: JSON.parse(params.tags_json) as string[] } : {}),
        })
        await persistEvent(workspaceId, rule.id, 'knowledge.rule.created', rule.revision, rule as unknown as Record<string, unknown>)
        await recordOperationAudit({ workspaceId, actorId, action: 'knowledge.rule.create', resourceType: 'knowledge_rule', resourceId: rule.id, before: {}, after: rule as unknown as Record<string, unknown>, reason: '运营知识规则创建' })
        return result(rule)
      } catch (error) {
        if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400)
        throw error
      }
    }
    case 'knowledge.rule.list': {
      try {
        requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops', 'rules_admin'])
        return result(knowledgeForWorkspace(workspaceId).queryRules({
          workspaceId,
          ...(typeof params.scope === 'string' ? { scope: params.scope as import('../../../packages/knowledge/src/index.js').RuleScope } : {}),
          ...(typeof params.scope_value === 'string' ? { scopeValue: params.scope_value } : {}), ...(typeof params.status === 'string' ? { status: params.status as import('../../../packages/knowledge/src/index.js').RuleStatus } : {}),
          ...(typeof params.as_of === 'string' ? { asOf: params.as_of } : {}), ...(typeof params.platform === 'string' ? { platform: params.platform } : {}), ...(typeof params.category === 'string' ? { category: params.category } : {}), ...(typeof params.brand === 'string' ? { brand: params.brand } : {}), ...(typeof params.store === 'string' ? { store: params.store } : {}), ...(typeof params.campaign === 'string' ? { campaign: params.campaign } : {}), ...(typeof params.text === 'string' ? { text: params.text } : {}),
        }))
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.asset.create': {
      try {
        const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops', 'knowledge_editor'])
        const content = JSON.parse(required(params, 'content_json')) as string | Record<string, unknown>
        const asset = knowledgeForWorkspace(workspaceId).createAsset({ workspaceId, kind: required(params, 'kind') as 'brand' | 'customer', name: required(params, 'name'), content, ...(typeof params.source === 'string' ? { source: params.source } : {}), ...(typeof params.tags_json === 'string' ? { tags: JSON.parse(params.tags_json) as string[] } : {}), ...(typeof params.approval_status === 'string' ? { approvalStatus: params.approval_status as import('../../../packages/knowledge/src/index.js').AssetApprovalStatus } : {}), ...(typeof params.rights_status === 'string' ? { rightsStatus: params.rights_status as import('../../../packages/knowledge/src/index.js').AssetRightsStatus } : {}) })
        await persistEvent(workspaceId, asset.id, 'knowledge.asset.created', asset.revision, asset as unknown as Record<string, unknown>)
        await recordOperationAudit({ workspaceId, actorId, action: 'knowledge.asset.create', resourceType: 'knowledge_asset', resourceId: asset.id, before: {}, after: asset as unknown as Record<string, unknown>, reason: '运营知识资产录入' })
        return result(asset)
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.asset.update': {
      try {
        const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops', 'knowledge_editor'])
        const assetId = required(params, 'asset_id')
        let content: string | Record<string, unknown> | undefined
        if (typeof params.content_json === 'string') content = JSON.parse(params.content_json) as string | Record<string, unknown>
        const updated = knowledgeForWorkspace(workspaceId).updateAsset(workspaceId, assetId, {
          ...(typeof params.name === 'string' ? { name: params.name } : {}), ...(content !== undefined ? { content } : {}), ...(typeof params.source === 'string' ? { source: params.source } : {}),
          ...(typeof params.approval_status === 'string' ? { approvalStatus: params.approval_status as import('../../../packages/knowledge/src/index.js').AssetApprovalStatus } : {}), ...(typeof params.rights_status === 'string' ? { rightsStatus: params.rights_status as import('../../../packages/knowledge/src/index.js').AssetRightsStatus } : {}),
          ...(typeof params.tags_json === 'string' ? { tags: JSON.parse(params.tags_json) as string[] } : {}),
        })
        await persistEvent(workspaceId, updated.id, 'knowledge.asset.updated', updated.revision, updated as unknown as Record<string, unknown>)
        await recordOperationAudit({ workspaceId, actorId, action: 'knowledge.asset.update', resourceType: 'knowledge_asset', resourceId: updated.id, before: {}, after: updated as unknown as Record<string, unknown>, reason: '运营知识资产审批/权益调整' })
        return result(updated)
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.asset.list': {
      try {
        requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops', 'knowledge_reader'])
        let tags: string[] | undefined
        if (typeof params.tags_json === 'string') tags = JSON.parse(params.tags_json) as string[]
        return result(knowledgeForWorkspace(workspaceId).queryAssets({ workspaceId, ...(typeof params.kind === 'string' ? { kind: params.kind as 'brand' | 'customer' } : {}), ...(typeof params.text === 'string' ? { text: params.text } : {}), ...(tags ? { tags } : {}) }))
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.feedback.record': {
      try {
        const feedback = knowledgeForWorkspace(workspaceId).recordFeedback({ workspaceId, kind: required(params, 'kind') as 'feedback' | 'platform_rejection', ...(typeof params.platform === 'string' ? { platform: params.platform } : {}), ...(typeof params.content_id === 'string' ? { contentId: params.content_id } : {}), reason: required(params, 'reason'), ...(typeof params.details === 'string' ? { details: params.details } : {}), ...(typeof params.metadata_json === 'string' ? { metadata: JSON.parse(params.metadata_json) as Record<string, string> } : {}) })
        await persistEvent(workspaceId, feedback.id, 'knowledge.feedback.recorded', 1, feedback as unknown as Record<string, unknown>)
        return result({ feedback, suggestions: knowledgeForWorkspace(workspaceId).listLearningSuggestions(workspaceId).filter(item => item.feedbackId === feedback.id) })
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.learning.list': {
      try { requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops', 'knowledge_reader']); return result(knowledgeForWorkspace(workspaceId).listLearningSuggestions(workspaceId, typeof params.status === 'string' ? params.status as 'pending' | 'confirmed' | 'dismissed' : undefined)) }
      catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.learning.confirm': {
      try {
        const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops', 'rules_admin', 'knowledge_editor'])
        const suggestion = knowledgeForWorkspace(workspaceId).confirmLearningSuggestion({ workspaceId, suggestionId: required(params, 'suggestion_id'), confirmedBy: actorId, ...(typeof params.note === 'string' ? { note: params.note } : {}) })
        await persistEvent(workspaceId, suggestion.id, 'knowledge.learning.confirmed', 1, suggestion as unknown as Record<string, unknown>)
        await recordOperationAudit({ workspaceId, actorId, action: 'knowledge.learning.confirm', resourceType: 'learning_suggestion', resourceId: suggestion.id, before: { status: 'pending' }, after: suggestion as unknown as Record<string, unknown>, reason: typeof params.note === 'string' ? params.note : '运营确认学习建议' })
        return result(suggestion)
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.learning.dismiss': {
      try {
        const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops', 'rules_admin', 'knowledge_editor'])
        const suggestionId = required(params, 'suggestion_id')
        const suggestion = knowledgeForWorkspace(workspaceId).dismissLearningSuggestion(workspaceId, suggestionId, typeof params.note === 'string' ? params.note : undefined)
        await persistEvent(workspaceId, suggestion.id, 'knowledge.learning.dismissed', 1, suggestion as unknown as Record<string, unknown>)
        await recordOperationAudit({ workspaceId, actorId, action: 'knowledge.learning.dismiss', resourceType: 'learning_suggestion', resourceId: suggestion.id, before: { status: 'pending' }, after: suggestion as unknown as Record<string, unknown>, reason: typeof params.note === 'string' ? params.note : '运营驳回学习建议' })
        return result(suggestion)
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.competitor.create': {
      try {
        const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops', 'competitor_reviewer'])
        const analysis = knowledgeForWorkspace(workspaceId).createCompetitorAnalysis({ workspaceId, competitorName: required(params, 'competitor_name'), source: JSON.parse(required(params, 'source_json')), summary: required(params, 'summary'), structure: JSON.parse(required(params, 'structure_json')), sellingPoints: JSON.parse(required(params, 'selling_points_json')), expression: JSON.parse(required(params, 'expression_json')) })
        await persistEvent(workspaceId, analysis.id, 'knowledge.competitor.created', 1, analysis as unknown as Record<string, unknown>)
        await recordOperationAudit({ workspaceId, actorId, action: 'knowledge.competitor.create', resourceType: 'competitor_analysis', resourceId: analysis.id, before: {}, after: analysis as unknown as Record<string, unknown>, reason: '运营竞品公开信息录入' })
        return result(analysis)
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.competitor.list': {
      try { requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'support', 'platform_ops', 'competitor_reviewer']); return result(knowledgeForWorkspace(workspaceId).queryCompetitorAnalyses({ workspaceId, ...(typeof params.competitor_name === 'string' ? { competitorName: params.competitor_name } : {}), ...(typeof params.text === 'string' ? { text: params.text } : {}) })) }
      catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'knowledge.competitor.reference': {
      try {
        const actorId = requireOperationsRole(req, ['workspace_owner', 'merchant_admin', 'operator', 'platform_ops', 'competitor_reviewer'])
        const reference = knowledgeForWorkspace(workspaceId).buildDifferentiationReference({ workspaceId, competitorId: required(params, 'competitor_id'), ownBrandName: required(params, 'own_brand_name'), ownSellingPoints: JSON.parse(required(params, 'own_selling_points_json')) })
        await recordOperationAudit({ workspaceId, actorId, action: 'knowledge.competitor.reference', resourceType: 'competitor_analysis', resourceId: required(params, 'competitor_id'), before: {}, after: reference as unknown as Record<string, unknown>, reason: '运营生成差异化竞品参考' })
        return result(reference)
      } catch (error) { if (error instanceof KnowledgeError) throw new DomainError(error.code, error.message, 400); throw error }
    }
    case 'multimodal.image.edit': {
      requirePlatformModelCostGate('image_edit')
      await requirePluginWalletAccess(workspaceId)
      let request: unknown
      try { request = JSON.parse(required(params, 'request_json')) } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'request_json 必须是合法 JSON', 400) }
      const candidate = createImageEditCandidate(request as never)
      if (!candidate.ok) throw new DomainError(ERROR_CODES.INVALID_REQUEST, candidate.issues.map(issue => `${issue.path}: ${issue.message}`).join('; '), 400)
      const protectedProductValidation = requireProtectedProductIntent(candidate.value.prompt)
      const productProtection = protectedProductConclusion(protectedProductValidation)
      const sourceAsset = assetForWorkspace(workspaceId, candidate.value.sourceImageId)
      const sourceReady = sourceAsset.mimeType.toLowerCase().startsWith('image/')
        && sourceAsset.scanStatus === 'clean'
        && sourceAsset.rightsStatus === 'approved'
        && sourceAsset.rightsScope !== 'unusable'
        && sourceAsset.aiModificationAllowed === true
        && (!sourceAsset.usageScopes?.length || sourceAsset.usageScopes.includes('commercial') || sourceAsset.usageScopes.includes('ai_generation'))
        && (!sourceAsset.validFrom || Date.parse(sourceAsset.validFrom) <= Date.now())
        && (!sourceAsset.validTo || Date.parse(sourceAsset.validTo) >= Date.now())
      if (!sourceReady) throw new DomainError('IMAGE_SOURCE_ASSET_INVALID', '图片编辑必须使用当前工作区内已通过扫描、权益和 AI 修改许可的素材', 409, { asset_id: sourceAsset.id, scan_status: sourceAsset.scanStatus, rights_status: sourceAsset.rightsStatus, scan_user_action_required: false, next_step: '平台会自动完成安全扫描；扫描通过后仅需确认权益和 AI 修改许可' })
      const sourceStored = await getStoredObjectWithRetry(workspaceId, sourceAsset.storageKey)
      const contextProduct = service.products.get(candidate.value.context.product.id)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') {
        if (!contextProduct || contextProduct.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '图片编辑引用的商品不存在或不属于当前工作区', 404)
        await resolveCanonicalTaskScope({ workspaceId, productId: contextProduct.id, platform: contextProduct.platform, ...(contextProduct.accountId ? { accountId: contextProduct.accountId } : {}), requireCanonical: true, requireListing: true })
      }
      if (contextProduct && contextProduct.workspaceId === workspaceId && !contextProduct.factsConfirmed) throw new DomainError('PRODUCT_FACTS_CONFIRMATION_REQUIRED', '图片编辑需要先确认商品事实', 409)
      const rulePreflight = await requireGenerationRulePreflight(workspaceId, candidate.value.context.product.id, '图片编辑前平台规则校验未通过')
      requireRuleSafeGenerationText(rulePreflight, [candidate.value.prompt], '图片编辑指令命中当前平台规则禁用表达')
      const walletDebitKey = `image-edit:${candidate.value.id}`
      await debitPluginWallet({ workspaceId, idempotencyKey: walletDebitKey, modelRunKey: `image-edit:${walletDebitKey}`, actorId: requestActor(req), description: '图片编辑调用' })
      let walletRefunded = false
      const refundEditWallet = async (reason: string) => {
        if (walletRefunded) return
        walletRefunded = true
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: requestActor(req), reason })
      }
      try {
        await persistEvent(workspaceId, candidate.value.id, 'multimodal.image.edit_requested', 1, { ...candidate.value, product_protection: productProtection } as unknown as Record<string, unknown>)
        if (!imageEditGenerator) {
          if (isProduction()) {
            await refundEditWallet('图片编辑 provider 未配置')
            throw new DomainError('IMAGE_EDIT_NOT_CONFIGURED', '生产环境未配置图片编辑中转服务', 503)
          }
          return result({ ...candidate.value, product_protection: productProtection, execution: executionContract('image_edit', false) })
        }
        let images: string[]
        images = await imageEditGenerator.generate({ prompt: appendProtectedProductConstraints(candidate.value.prompt), sourceImages: [{ bytes: sourceStored.body, mimeType: sourceStored.metadata.contentType }], region: candidate.value.region.rect, usageContext: { workspaceId, actionId: walletDebitKey, runKey: `image-edit:${walletDebitKey}` } })
        if (!contextProduct || contextProduct.workspaceId !== workspaceId) return result({ ...candidate.value, product_protection: productProtection, images, rendering: 'candidate', platformPublished: false, execution: executionContract('image_edit', true) })
        const editJob = service.enqueueImageGeneration({ workspaceId, productId: contextProduct.id, sourceAssetIds: [sourceAsset.id], direction: `局部编辑：${candidate.value.prompt}`, count: 1, idempotencyKey: `image-edit:${candidate.value.id}` })
        editJob.state = 'succeeded'
        const archived = await archiveGeneratedImages(workspaceId, editJob.id, images)
        await persistSnapshot(workspaceId, 'image_generation_job', archived, archived as unknown as Record<string, unknown>)
        await persistEvent(workspaceId, archived.id, 'product.image_edit_candidate_generated', archived.revision, { job_id: archived.id, product_id: contextProduct.id, source_asset_id: sourceAsset.id, visual_refs: archived.outputs?.map(output => output.visualRef) ?? [], artifact_role: 'candidate', product_protection: productProtection })
        return result({ ...candidate.value, product_protection: productProtection, images, rendering: 'candidate', platformPublished: false, execution: executionContract('image_edit', true), job: publicImageJob(archived) })
      } catch (error) {
        if (!providerSucceededButSettlementPending(error)) await refundEditWallet('图片编辑任务或 provider 失败')
        throw error
      }
    }
    case 'multimodal.generate': {
      let context: GenerationContext
      try { context = JSON.parse(required(params, 'context_json')) as GenerationContext } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'context_json 必须是合法 JSON', 400) }
      await enforceProductBrandAccess(req, workspaceId, context.product.id)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') {
        const product = service.products.get(context.product.id)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '多模态请求引用的商品不存在或不属于当前工作区', 404)
        await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      }
      await requirePluginWalletAccess(workspaceId)
      const rulePreflight = await generationRulePreflight(workspaceId, context.product.id)
      if (rulePreflight.blocking) throw new DomainError('PLATFORM_RULE_PREFLIGHT_BLOCKED', '当前店铺平台规则存在阻断项，不能继续生成', 409, { rule_preflight: rulePreflight })
      const modality = required(params, 'modality') as 'text' | 'image' | 'video'
      requireRuleSafeGenerationText(rulePreflight, [params.prompt], '多模态生成指令命中当前平台规则禁用表达')
      const request = createOneSentenceGenerationRequest(modality === 'video'
        ? { modality, prompt: required(params, 'prompt'), output: typeof params.output === 'string' ? params.output as 'script' | 'storyboard' | 'rendering' : 'script', context }
        : { modality, prompt: required(params, 'prompt'), context })
      if (!request.ok) throw new DomainError(ERROR_CODES.INVALID_REQUEST, request.issues.map(issue => `${issue.path}: ${issue.message}`).join('; '), 400)
      const storyboardQuality = request.value.modality === 'video' && request.value.output === 'rendering' ? evaluateStoryboardBeforeRendering(context) : undefined
      if (request.value.modality === 'image') requirePlatformModelCostGate('image')
      if (request.value.modality === 'text' || (request.value.modality === 'video' && request.value.output !== 'rendering')) requirePlatformModelCostGate('text')
      if (request.value.modality === 'video' && request.value.output === 'rendering') await requireVideoModelCostPreflight()
      const multimodalKey = createHash('sha256').update(JSON.stringify(request.value)).digest('hex')
      // Image execution derives its provider usage identity from the persisted
      // job idempotency key. Keep the commercial action and budget reservation
      // on that exact identity so the nested image relay cannot create a second,
      // permanently-active reservation.
      const walletDebitKey = request.value.modality === 'image'
        ? `image:multimodal-image:${multimodalKey}`
        : `multimodal:${multimodalKey}`
      const modelRunKey = request.value.modality === 'video' && request.value.output === 'rendering'
        ? `video:${walletDebitKey}`
        : walletDebitKey
      await debitPluginWallet({ workspaceId, idempotencyKey: walletDebitKey, modelRunKey, actorId: requestActor(req), description: `${modality}生成调用` })
      let rendering: Awaited<ReturnType<NonNullable<typeof videoGenerator>['generate']>> | undefined
      let generatedImages: string[] | undefined
      let imageJob: ReturnType<typeof service.enqueueImageGeneration> | undefined
      let generatedText: Awaited<ReturnType<typeof service.generateOneSentenceText>> | undefined
      try {
        if (request.value.modality === 'text') {
          generatedText = await service.generateOneSentenceText({ workspaceId, productId: request.value.context.product.id, prompt: request.value.prompt, actionId: walletDebitKey })
        }
        if (request.value.modality === 'video' && request.value.output !== 'rendering') {
          generatedText = await service.generateOneSentenceText({ workspaceId, productId: request.value.context.product.id, prompt: `${request.value.output}：${request.value.prompt}`, actionId: walletDebitKey })
        }
        if (request.value.modality === 'image') {
          const product = service.products.get(request.value.context.product.id)
          if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '多模态图片请求引用的商品不存在或不属于当前工作区', 404)
          if (!product.factsConfirmed) throw new DomainError('PRODUCT_FACTS_CONFIRMATION_REQUIRED', '多模态图片生成需要先确认商品事实', 409)
          service.assertBrandVisualGenerationReady(workspaceId, product.platform)
          imageJob = service.enqueueImageGeneration({ workspaceId, productId: product.id, direction: request.value.prompt, count: 1, idempotencyKey: `multimodal-image:${multimodalKey}` })
          const completed = await service.completeImageGeneration({ workspaceId, jobId: imageJob.id, runKey: modelRunKey })
          generatedImages = completed.images
          await persistSnapshot(workspaceId, 'image_generation_job', completed.job, completed.job as unknown as Record<string, unknown>)
        }
        if (request.value.modality === 'video' && (request.value.output as string) === 'rendering') {
          if (!videoGenerator) throw new DomainError('VIDEO_GENERATION_NOT_CONFIGURED', '生产环境未配置视频生成中转服务', 503)
          rendering = await archiveCompletedVideo(workspaceId, await videoGenerator.generate({ prompt: request.value.prompt, output: 'rendering', context: request.value.context, usageContext: { workspaceId, actionId: walletDebitKey, runKey: modelRunKey } }))
        }
        if (generatedText) requireRuleSafeGenerationText(rulePreflight, [generatedText], '多模态生成结果命中当前平台规则禁用表达')
      } catch (error) {
        if (!providerSucceededButSettlementPending(error)) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: requestActor(req), reason: '多模态生成 provider 调用失败' })
        throw error
      }
      const providerExecuted = request.value.modality === 'text' || (request.value.modality === 'video' && request.value.output !== 'rendering')
        ? Boolean(contentGenerator)
        : request.value.modality === 'image' ? Boolean(imageGenerator) : Boolean(rendering)
      const execution = { status: rendering ? rendering.status : generatedImages || generatedText ? 'completed' as const : 'requested' as const, ...executionContract(request.value.modality === 'text' ? 'content' : request.value.modality, providerExecuted) }
      try {
        await persistEvent(workspaceId, `multimodal_${randomUUID()}`, rendering || generatedImages || generatedText ? 'multimodal.generation.completed' : 'multimodal.generation.requested', 1, { ...request.value as unknown as Record<string, unknown>, execution, rule_preflight: rulePreflight, ...(storyboardQuality ? { storyboard_quality: storyboardQuality } : {}), ...(imageJob ? { image_job_id: imageJob.id } : {}), ...(generatedText ? { content: generatedText } : {}), ...(generatedImages ? { images: generatedImages } : {}), ...(rendering ? { rendering } : {}) })
      } catch (error) {
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: requestActor(req), reason: '多模态结果记录失败' })
        throw error
      }
      return result({ ...request.value, execution, rule_preflight: rulePreflight, ...(storyboardQuality ? { storyboard_quality: storyboardQuality } : {}), ...(generatedText ? { content: generatedText } : {}), ...(imageJob ? { image_job_id: imageJob.id } : {}), ...(generatedImages ? { images: generatedImages } : {}), ...(rendering ? { rendering } : {}) })
    }
    case 'multimodal.video.request': {
      let context: GenerationContext
      try { context = JSON.parse(required(params, 'context_json')) as GenerationContext } catch { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'context_json 必须是合法 JSON', 400) }
      await enforceProductBrandAccess(req, workspaceId, context.product.id)
      if ((await canonicalProductReadControl(workspaceId)).mode === 'canonical_read') {
        const product = service.products.get(context.product.id)
        if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '视频请求引用的商品不存在或不属于当前工作区', 404)
        await resolveCanonicalTaskScope({ workspaceId, productId: product.id, platform: product.platform, ...(product.accountId ? { accountId: product.accountId } : {}), requireCanonical: true, requireListing: true })
      }
      await requirePluginWalletAccess(workspaceId)
      const suppliedVideoRequestKey = (typeof params.idempotency_key === 'string' && params.idempotency_key.trim()) || header(req, 'idempotency-key')?.trim()
      if (isProduction() && !suppliedVideoRequestKey) throw new DomainError('IDEMPOTENCY_KEY_REQUIRED', '生产视频请求必须提供 idempotency_key 或 Idempotency-Key 请求头', 400)
      const rulePreflight = await generationRulePreflight(workspaceId, context.product.id)
      if (rulePreflight.blocking) throw new DomainError('PLATFORM_RULE_PREFLIGHT_BLOCKED', '当前店铺平台规则存在阻断项，不能继续生成视频', 409, { rule_preflight: rulePreflight })
      const output = required(params, 'output') as 'script' | 'storyboard' | 'rendering'
      requireRuleSafeGenerationText(rulePreflight, [params.prompt], '视频生成指令命中当前平台规则禁用表达')
      if (output !== 'rendering') requirePlatformModelCostGate('text')
      const request = output === 'rendering'
        ? createVideoRenderingRequest({ prompt: required(params, 'prompt'), context })
        : createVideoGenerationRequest({ prompt: required(params, 'prompt'), output, context })
      if (!request.ok) throw new DomainError(ERROR_CODES.INVALID_REQUEST, request.issues.map(issue => `${issue.path}: ${issue.message}`).join('; '), 400)
      const storyboardQuality = request.value.output === 'rendering' ? evaluateStoryboardBeforeRendering(context) : undefined
      if (request.value.output === 'rendering') await requireVideoModelCostPreflight()
      const videoRequestKey = suppliedVideoRequestKey || randomUUID()
      const walletDebitKey = `video:${videoRequestKey}`
      const modelRunKey = request.value.output === 'rendering' ? `video:${walletDebitKey}` : walletDebitKey
      await debitPluginWallet({ workspaceId, idempotencyKey: walletDebitKey, modelRunKey, actorId: requestActor(req), description: '视频生成调用' })
      let rendering: Awaited<ReturnType<NonNullable<typeof videoGenerator>['generate']>> | undefined
      let generatedPlan: Awaited<ReturnType<typeof service.generateOneSentenceText>> | undefined
      try {
        if (request.value.output !== 'rendering') generatedPlan = await service.generateOneSentenceText({ workspaceId, productId: request.value.context.product.id, prompt: `${request.value.output}：${request.value.prompt}`, actionId: walletDebitKey })
        if ((request.value.output as string) === 'rendering') {
          if (!videoGenerator) throw new DomainError('VIDEO_GENERATION_NOT_CONFIGURED', '生产环境未配置视频生成中转服务', 503)
          rendering = await archiveCompletedVideo(workspaceId, await videoGenerator.generate({ prompt: request.value.prompt, output: 'rendering', context: request.value.context, usageContext: { workspaceId, actionId: walletDebitKey, runKey: modelRunKey } }))
        }
        if (generatedPlan) requireRuleSafeGenerationText(rulePreflight, [generatedPlan], '视频脚本或分镜命中当前平台规则禁用表达')
      } catch (error) {
        if (!providerSucceededButSettlementPending(error)) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: requestActor(req), reason: '视频生成 provider 调用失败' })
        throw error
      }
      const execution = { status: rendering ? rendering.status : generatedPlan ? 'completed' as const : 'requested' as const, ...executionContract('video', Boolean(rendering ? videoGenerator : contentGenerator), generatedPlan && contentGenerator ? 'text-relay' : undefined) }
      try {
        await persistEvent(workspaceId, `video_${randomUUID()}`, rendering || generatedPlan ? 'multimodal.video_completed' : 'multimodal.video.requested', 1, { ...request.value as unknown as Record<string, unknown>, execution, rule_preflight: rulePreflight, ...(storyboardQuality ? { storyboard_quality: storyboardQuality } : {}), ...(generatedPlan ? { plan: generatedPlan } : {}), ...(rendering ? { rendering } : {}) })
      } catch (error) {
        await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: walletDebitKey, actorId: requestActor(req), reason: '视频结果记录失败' })
        throw error
      }
      return result({ ...request.value, execution, rule_preflight: rulePreflight, ...(storyboardQuality ? { storyboard_quality: storyboardQuality } : {}), ...(generatedPlan ? { plan: generatedPlan } : {}), ...(rendering ? { rendering } : {}) })
    }
    case 'multimodal.video.get': {
      if (!videoGenerator) throw new DomainError('VIDEO_GENERATION_NOT_CONFIGURED', '生产环境未配置视频生成中转服务', 503)
      const providerJobId = required(params, 'provider_job_id')
      try {
        await assertVideoProviderJobScope(workspaceId, providerJobId)
        const rendering = await archiveCompletedVideo(workspaceId, await videoGenerator.getStatus(providerJobId))
        await persistEvent(workspaceId, `video_${providerJobId}`, 'multimodal.video_status_observed', 1, { provider_job_id: providerJobId, ...rendering })
        return result({ provider_job_id: providerJobId, execution: executionContract('video', true), ...(rendering.assetId ? { asset_id: rendering.assetId, archive_state: rendering.archiveState, ...(rendering.archiveState === 'archived' ? { download_path: `/v1/assets/${encodeURIComponent(rendering.assetId)}/download` } : { availabilityWarning: '视频已安全归档到隔离区，平台自动安全扫描通过后才可下载或发布；无需商家或运营人员操作' }) } : {}), ...rendering })
      } catch (error) {
        if (error instanceof DomainError) throw error
        const archived = service.findAssetBySourceProviderJobId(workspaceId, providerJobId)
        if (archived) return result({ provider_job_id: providerJobId, status: 'completed', asset_id: archived.id, archive_state: archived.scanStatus === 'clean' ? 'archived' : 'quarantined', ...(archived.scanStatus === 'clean' ? { download_path: `/v1/assets/${encodeURIComponent(archived.id)}/download` } : { availabilityWarning: '视频已安全归档到隔离区，平台自动安全扫描通过后才可下载或发布；无需商家或运营人员操作' }), execution: executionContract('video', false) })
        throw new DomainError('VIDEO_PROVIDER_STATUS_FAILED', error instanceof Error ? error.message : '视频 provider 状态查询失败', 503)
      }
    }
  }
}

/**
 * The reconciliation key is a protocol value, not caller-provided metadata.
 * Keep every identity field in the preimage so a replay from another tenant,
 * request event, execution attempt, or provider query attempt cannot alias the
 * original evidence row.
 */
export function imageGenerationReconciliationIdempotencyKey(input: {
  workspaceId: string
  jobId: string
  eventId: string
  intentHash: string
  executionAttempt: number
  providerRequestId: string
  queryAttempt: number
}) {
  const canonical = JSON.stringify({
    version: 1,
    workspace_id: input.workspaceId.trim(),
    job_id: input.jobId.trim(),
    event_id: input.eventId.trim(),
    intent_hash: input.intentHash.trim(),
    execution_attempt: input.executionAttempt,
    provider_request_id: input.providerRequestId.trim(),
    query_attempt: input.queryAttempt,
  })
  return `image-reconcile:${createHash('sha256').update(canonical).digest('hex')}`
}

export async function route(req: IncomingMessage, res: ServerResponse) {
  const url = new URL(req.url ?? '/', `http://${req.headers.host ?? 'localhost'}`)
  const path = url.pathname
  if (req.method === 'GET' && path === '/metrics') {
    const metricsToken = process.env.METRICS_AUTH_TOKEN?.trim()
    if (isProduction() && (!metricsToken || header(req, 'authorization') !== `Bearer ${metricsToken}`)) {
      res.statusCode = metricsToken ? 401 : 503
      res.setHeader('content-type', 'application/json; charset=utf-8')
      res.setHeader('cache-control', 'no-store')
      res.end(JSON.stringify({ error: metricsToken ? 'METRICS_UNAUTHORIZED' : 'METRICS_AUTH_NOT_CONFIGURED' }))
      return
    }
    res.statusCode = 200
    res.setHeader('content-type', 'text/plain; version=0.0.4; charset=utf-8')
    res.setHeader('cache-control', 'no-store')
    res.end(await prometheusMetrics())
    return
  }
  if (req.method === 'OPTIONS') return send(res, 204, isProduction() ? 'unknown' : 'ws_demo', null, null, req)
  const signedAssetDisplayMatch = path.match(/^\/v1\/public\/assets\/([^/]+)\/display$/)
  if (req.method === 'GET' && signedAssetDisplayMatch) return serveSignedAssetDisplay(req, res, url, decodeURIComponent(signedAssetDisplayMatch[1]!))
  const isOAuthCallback = /^\/v1\/oauth\/callback\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)$/.test(path)
  const paymentCallbackMatch = path.match(/^\/v1\/(billing|subscriptions)\/callback\/(alipay|wechat)$/)
  const isWorkerAutomationTick = req.method === 'POST' && path === '/v1/internal/automation/tick'
  const workerRoute = isWorkerRoute(req.method, path)
  // These worker callbacks still read the durable business projection through
  // `service`. A callback may land on a cold API replica, so hydrate the
  // workspace before resolving the job; other worker-only routes stay
  // persistence-first and do not pay the full workspace hydration cost.
  const workerNeedsWorkspaceHydration = workerRoute && (
    /^\/v1\/internal\/image-generation-jobs\/[^/]+\/(?:result|execution|reconciliation-evidence)$/u.test(path)
    || path === '/v1/internal/image-generation-jobs/reconciliation'
    || /^\/v1\/internal\/image-generation-continuations\/[^/]+\/execute$/u.test(path)
  )
  const assetScannerRoute = isAssetScannerRoute(req.method, path)
  const infrastructureProbe = path === '/healthz' || path === '/readyz' || path === '/livez' || path === '/releasez'
  // Health probes are infrastructure-scoped and intentionally unauthenticated;
  // all merchant and MCP routes still pass the production identity boundary.
  // OAuth callbacks are the exception: the platform redirects a browser and
  // cannot attach the merchant API Bearer token. The one-time state and PKCE
  // verifier are the callback's authentication boundary.
  if (assetScannerRoute) await requireAssetScannerAuthorization(req)
  else if (workerRoute) await requireWorkerAuthorization(req)
  else if (!infrastructureProbe && !isOAuthCallback && !paymentCallbackMatch) await authenticate(req)
  const mcpInputForHydration = req.method === 'POST' && path === '/mcp' ? await body(req, MCP_BODY_LIMIT) : undefined
  const mcpMethodForHydration = typeof mcpInputForHydration?.method === 'string' ? mcpInputForHydration.method : undefined
  const hydrateRequestWorkspace = header(req, 'x-workspace-id')?.trim() || (!requiresStrictAuth() ? 'ws_demo' : undefined)
  const requestWorkspace = (() => {
    try { return resolveWorkspace(req) } catch { return isProduction() ? 'unknown' : 'ws_demo' }
  })()
  const platformRateScope = requestPrincipals.get(req)?.workbench === 'platform' && requestWorkspace === 'unknown'
    ? `platform:${requestPrincipals.get(req)?.identityId || requestPrincipals.get(req)?.actorId || 'unidentified'}`
    : undefined
  const httpOperationPolicy = getHttpOperationPolicy(req.method, path)
  if (httpOperationPolicy?.authentication === 'identity' && requestWorkspace !== 'unknown' && requestPrincipals.get(req)?.workbench === 'workspace') {
    await resolveActiveWorkspaceMember(req, requestWorkspace, false)
  }
  // Resolve the shared capability decision immediately after authentication,
  // before tenant hydration, membership/brand lookups or any route handler can
  // create business state. body() is replay-safe because it caches the bytes.
  if (!workerRoute && !assetScannerRoute && !infrastructureProbe && !isOAuthCallback && !paymentCallbackMatch) {
    await enforceRegisteredHttpCapability(req, url, requestWorkspace)
  }
  if (httpOperationPolicy?.authentication === 'identity' && requestWorkspace !== 'unknown' && requestPrincipals.get(req)?.workbench === 'workspace'
    && !requestMemberChecks.has(req) && !exactConsumedGrantForRequest(req, requestWorkspace)) {
    await enforceActiveWorkspaceMember(req, requestWorkspace)
  }
  const normalizedPagedCollection = req.method === 'GET'
    && (path === '/v1/products' || path === '/v1/tasks')
    && Boolean(persistence.business)
  const normalizedPagedMcpCollection = req.method === 'POST'
    && path === '/mcp'
    && (mcpMethodForHydration === 'catalog.search' || mcpMethodForHydration === 'task.history')
    && Boolean(persistence.business)
  if (hydrateRequestWorkspace && !infrastructureProbe && !isOAuthCallback && !paymentCallbackMatch && !isWorkerAutomationTick && !assetScannerRoute && (!workerRoute || workerNeedsWorkspaceHydration)) {
    const requiresFreshAssetRead = Boolean(
      (mcpMethodForHydration?.startsWith('asset.') && mcpMethodForHydration !== 'asset.upload')
      || /^\/v1\/assets\/[^/]+\/(?:download|parse|rights|facts|preference)$/u.test(path),
    )
    // Asset mutations commonly follow upload immediately. A different API
    // replica may still have a warm one-second workspace cache from before the
    // upload, so correctness requires a durable read-through here.
    if (requiresFreshAssetRead && persistence.business) await hydrateWorkspaceFromPersistence(hydrateRequestWorkspace)
    else await hydrateWorkspace(hydrateRequestWorkspace, normalizedPagedCollection || normalizedPagedMcpCollection ? { excludeEntityTypes: ['product', 'task'] } : {})
  }
  if (req.method === 'POST' && path === '/v1/canonical-backfill/conflicts/scan') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    requireOperationsRole(req, ['platform_ops'])
    const auditBatchId = required(input, 'audit_batch_id')
    const reason = required(input, 'reason')
    const run = await persistence.canonicalBackfillRuns?.get({ workspaceId, id: auditBatchId })
    if (!run) throw new DomainError('CANONICAL_BACKFILL_RUN_NOT_FOUND', 'canonical conflict scan 审计批次不存在或不属于当前工作区', 404, { audit_batch_id: auditBatchId })
    const conflictRepository = persistence.canonicalBackfillConflicts
    if (!conflictRepository) throw new DomainError('CANONICAL_BACKFILL_CONFLICT_REPOSITORY_UNAVAILABLE', 'canonical backfill 冲突队列未配置', 503)
    const repository = persistence.brandUnits ?? memoryBrandUnits
    const rows = await repository.listCanonicalChainConsistencyRows({ workspaceId })
    const report = canonicalConsistencyApiReport({ workspaceId, ...rows }, persistence.mode === 'postgres' ? 'postgres' : 'memory')
    // The legacy conflict queue intentionally has a narrower vocabulary than
    // the full consistency report.  Only enqueue migration/backfill conflicts
    // whose evidence can be represented losslessly; relationship findings
    // remain in the read-only consistency report.
    const scanItems = canonicalConflictScanItems(report)
      .map(item => item.code === 'BRAND_SCOPE_MISMATCH' ? { ...item, code: 'CANONICAL_BRAND_MISMATCH' as const } : item)
      .filter((item): item is typeof item & { code: 'MISSING_BRAND' | 'CANONICAL_MAPPING_AMBIGUOUS' | 'CANONICAL_BRAND_MISMATCH' | 'CANONICAL_LEGACY_PRODUCT_MISSING' | 'CANONICAL_ID_COLLISION' | 'TASK_ACCOUNT_MISMATCH' } => ['MISSING_BRAND', 'CANONICAL_MAPPING_AMBIGUOUS', 'CANONICAL_BRAND_MISMATCH', 'CANONICAL_LEGACY_PRODUCT_MISSING', 'CANONICAL_ID_COLLISION', 'TASK_ACCOUNT_MISMATCH'].includes(item.code))
    const queued = await conflictRepository.enqueue({ workspaceId, runId: auditBatchId, conflicts: scanItems })
    await recordOperationAudit({ workspaceId, actorId: requestActor(req), action: 'canonical.backfill.conflicts.scan', resourceType: 'canonical_backfill_run', resourceId: auditBatchId, before: { audit_batch_id: auditBatchId, revision: run.revision }, after: { audit_batch_id: auditBatchId, revision: run.revision, consistency_revision: report.revision, finding_count: scanItems.length, queued_count: queued.length }, reason })
    return send(res, 200, workspaceId, { audit_batch_id: auditBatchId, consistency_revision: report.revision, findings_scanned: report.findings.length, conflicts: queued, idempotent_key: `${auditBatchId}:legacy_product_id:code` }, null, req)
  }
  // Health probes are infrastructure-scoped, not merchant-scoped. Requiring
  // X-Workspace-Id here would make the production container fail its own
  // liveness/readiness check with 401.
  if (req.method === 'GET' && path === '/livez') return send(res, 200, 'system', { process: { ready: true } }, null, req)
  if (req.method === 'GET' && path === '/releasez') {
    const release = {
      release_id: process.env.RELEASE_ID?.trim() || null,
      release_git_sha: process.env.RELEASE_GIT_SHA?.trim() || null,
      manifest_sha256: process.env.RELEASE_MANIFEST_SHA256?.trim() || null,
      image_set_digest: process.env.RELEASE_IMAGE_SET_DIGEST?.trim() || null,
    }
    const valid = Boolean(
      release.release_id
      && /^[0-9a-f]{40}$/u.test(release.release_git_sha ?? '')
      && /^[0-9a-f]{64}$/u.test(release.manifest_sha256 ?? '')
      && /^sha256:[0-9a-f]{64}$/u.test(release.image_set_digest ?? ''),
    )
    if (isProduction() && !valid) return send(res, 503, 'system', { release, ready: false }, { code: 'RELEASE_METADATA_UNAVAILABLE', message: '生产发布元数据未完整注入' }, req)
    return send(res, 200, 'system', { release, ready: valid }, null, req)
  }
  if (req.method === 'GET' && (path === '/healthz' || path === '/readyz')) {
    if (persistenceError) return send(res, 503, 'system', { ...runtimeHealth(), persistence: { mode: 'postgres', ready: false } }, { code: ERROR_CODES.DATABASE_UNAVAILABLE, message: '数据库未就绪' }, req)
    try {
      await persistenceReady
      await persistence.checkHealth?.()
    } catch {
      return send(res, 503, 'system', { ...runtimeHealth(), persistence: { mode: persistence.mode, ready: false } }, { code: ERROR_CODES.DATABASE_UNAVAILABLE, message: '数据库未就绪' }, req)
    }
    if (path === '/readyz') {
      const productionReadiness = productionReadinessDiagnostics()
      if (!productionReadiness.ready) {
        return fail(res, 503, 'system', 'PRODUCTION_READINESS_BLOCKED', '生产关键依赖或发布元数据未就绪', req, {
          gates: productionReadiness.gates,
          next_actions: ['完成模型中转、授权强制模式、持久角色权威、身份、对象存储、支付、规则同步、成本控制和 release metadata 配置后重新检查 /readyz'],
        })
      }
    }
    if (redisHealth) {
      try { await redisHealth.ping() } catch { return send(res, 503, 'system', { ...runtimeHealth(), persistence: { mode: persistence.mode, ready: true }, redis: { ready: false } }, { code: 'REDIS_UNAVAILABLE', message: 'Redis 未就绪' }, req) }
    } else if (requiresStrictAuth()) {
      return send(res, 503, 'system', { ...runtimeHealth(), persistence: { mode: persistence.mode, ready: true }, redis: { ready: false } }, { code: 'REDIS_UNAVAILABLE', message: '生产环境未配置 Redis', }, req)
    }
    let scanner: ScannerReadinessSummary | undefined
    if (scannerHeartbeatRequiredForProbe(path, process.env)) {
      const trust = productionAssetScannerReadiness(process.env)
      if (!trust.ready) {
        return fail(res, 503, 'system', 'SCANNER_NOT_READY', '素材安全扫描信任配置未就绪', req, {
          scanner: { ready: false, configured: false },
          reasons: trust.reasons,
        })
      }
      if (!redisHealth) {
        return fail(res, 503, 'system', 'SCANNER_HEARTBEAT_MISSING', '未发现新鲜的素材安全扫描心跳', req, {
          scanner: { ready: false, configured: true },
          reasons: ['SCANNER_HEARTBEAT_MISSING'],
        })
      }
      let readiness: Awaited<ReturnType<typeof evaluateScannerHeartbeatReadiness>>
      try {
        readiness = await evaluateScannerHeartbeatReadiness({ redis: redisHealth, env: process.env })
      } catch {
        return send(res, 503, 'system', { ...runtimeHealth(), persistence: { mode: persistence.mode, ready: true }, redis: { ready: false } }, { code: 'REDIS_UNAVAILABLE', message: 'Redis 未就绪' }, req)
      }
      scanner = readiness.summary
      if (!readiness.ready) {
        return fail(res, 503, 'system', readiness.code, '素材安全扫描服务未就绪', req, {
          scanner: readiness.summary,
          reasons: readiness.reasons,
        })
      }
    }
    return send(res, 200, 'system', { ...runtimeHealth(), persistence: { mode: persistence.mode, ready: true }, redis: { ready: Boolean(redisHealth) }, ...(scanner ? { scanner } : {}) }, null, req)
  }
  try {
    await persistenceReady
  } catch {
    return fail(res, 503, 'system', ERROR_CODES.DATABASE_UNAVAILABLE, '数据库未就绪', req)
  }
  if (hydrateRequestWorkspace && path !== '/mcp' && !infrastructureProbe && !isOAuthCallback && !paymentCallbackMatch && !workerRoute && !assetScannerRoute) {
    if (!exactConsumedGrantForRequest(req, hydrateRequestWorkspace)) await enforceActiveWorkspaceMember(req, hydrateRequestWorkspace)
  }
  if (!workerRoute) {
    const taskPath = path.match(/^\/v1\/tasks\/([^/]+)(?:\/|$)/u)
    if (taskPath && taskPath[1] !== 'understand') {
      const task = scopeTask(req, decodeURIComponent(taskPath[1]!))
      const privileged = /\/(?:approve|publish)(?:\/|$)/u.test(path)
      await enforceTaskBrandAccess(req, task, taskRoleForOperation(path, req.method === 'GET' && !privileged))
    }
    const contentPath = path.match(/^\/v1\/content-versions\/([^/]+)(?:\/|$)/u)
    if (contentPath) {
      const scoped = scopeContentVersion(req, decodeURIComponent(contentPath[1]!))
      const privileged = /\/(?:approve|publish)(?:\/|$)/u.test(path)
      await enforceTaskBrandAccess(req, scoped.task, taskRoleForOperation(path, req.method === 'GET' && !privileged))
    }
  }
  if (req.method === 'POST' && path === '/v1/internal/automation/tick') {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    await hydrateWorkspace(workspaceId)
    const automation = await runAutomationTick(workspaceId, req, 'worker-automation')
    const ruleSync = automation.skipReason === 'codex_native_automations_only'
      ? { skipped: true, reason: 'codex_native_automations_only' as const }
      : await syncSignedPlatformRules(workspaceId)
    return send(res, 200, workspaceId, { ...automation, rule_sync: ruleSync }, null, req)
  }
  const imageGenerationResultMatch = path.match(/^\/v1\/internal\/image-generation-jobs\/([^/]+)\/result$/u)
  if (req.method === 'POST' && imageGenerationResultMatch) {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const jobId = decodeURIComponent(imageGenerationResultMatch[1]!)
    await hydrateWorkspace(workspaceId)
    const input = await body(req)
    let callback: ReturnType<typeof validateImageGenerationCallbackResult>
    try {
      callback = validateImageGenerationCallbackResult(input, { allowEventId: true })
    } catch (error) {
      throw new DomainError(ERROR_CODES.INVALID_REQUEST, error instanceof Error ? error.message : '图片生成回执 schema 无效', 400)
    }
    const eventId = callback.event_id!
    const ownerToken = callback.owner_token ?? ''
    const intentHash = callback.intent_hash
    const job = service.getImageGenerationJob(workspaceId, jobId)
    if (job.intentHash !== intentHash) throw new DomainError('IMAGE_GENERATION_INTENT_MISMATCH', '图片生成回执与任务意图不匹配', 409)
    const requestedEvents = persistence.outbox ? await persistence.outbox.listAggregateEvents(workspaceId, jobId, 100) : []
    const requested = requestedEvents.find(event => event.id === eventId && event.eventType === 'image.generation.requested')
    if (!requested) throw new DomainError('IMAGE_GENERATION_EVENT_INVALID', '图片生成回执未绑定有效的请求事件', 409)
    if (requested.payload.intent_hash !== intentHash) throw new DomainError('IMAGE_GENERATION_EVENT_INVALID', '图片生成回执的请求事件意图不匹配', 409)
    const execution = persistence.imageGenerationExecutions ? await persistence.imageGenerationExecutions.get({ workspaceId, jobId }) : undefined
    if (job.state === 'succeeded' && job.archiveState === 'archived' && job.outputs?.length) {
      await persistImageGenerationCompletion(workspaceId, job)
      if (execution?.state === 'provider_started' && execution.eventId === eventId && execution.ownerToken === ownerToken) await persistence.imageGenerationExecutions!.markCompleted({ workspaceId, jobId, ownerToken })
      return send(res, 200, workspaceId, { job_id: job.id, state: job.state, archive_state: job.archiveState, already_completed: true }, null, req)
    }
    if (!ownerToken || !execution || execution.eventId !== eventId || execution.ownerToken !== ownerToken || execution.state !== 'provider_started') throw new DomainError('IMAGE_GENERATION_EXECUTION_LEASE_LOST', '图片生成回执没有有效的 provider 执行租约', 409, { retryable: false, reconciliation_required: true })
    const providerRequestId = callback.provider_request_id ?? ''
    if (!providerRequestId || !execution.providerRequestId || providerRequestId !== execution.providerRequestId) throw new DomainError('IMAGE_GENERATION_PROVIDER_REQUEST_ID_MISMATCH', '图片生成回执必须携带与执行租约一致的真实 provider request id', 409, { retryable: false, reconciliation_required: true })
    const callbackError = callback.error
    if (callbackError) {
      job.state = 'failed'
      job.archiveState = 'external_unarchived'
      job.errorCode = callbackError.code
      job.errorMessage = callbackError.message
      job.revision += 1
      job.updatedAt = new Date().toISOString()
      await persistence.persistSnapshotAndEvent?.({ workspaceId, entityType: 'image_generation_job', entityId: job.id, entityVersion: job.revision, payload: job as unknown as Record<string, unknown>, eventType: 'image.generation.failed', eventPayload: { job_id: job.id, intent_hash: intentHash, error_code: job.errorCode, error_message: job.errorMessage } })
      await persistence.imageGenerationExecutions!.markFailed({ workspaceId, jobId, ownerToken, errorCode: job.errorCode, errorMessage: job.errorMessage })
      return send(res, 200, workspaceId, { job_id: job.id, state: job.state, archive_state: job.archiveState, accepted: true }, null, req)
    }
    const images = callback.images!
    const archived = await archiveGeneratedImages(workspaceId, job.id, images)
    await persistImageGenerationCompletion(workspaceId, archived)
    if (archived.archiveState === 'archived' && imageJobOutputsAreClean(archived)) await persistence.imageGenerationExecutions!.markCompleted({ workspaceId, jobId, ownerToken })
    return send(res, 200, workspaceId, { job_id: archived.id, state: archived.state, archive_state: archived.archiveState, candidate_count: archived.outputs?.length ?? 0, accepted: true, reconciliation_required: archived.archiveState !== 'archived' || !imageJobOutputsAreClean(archived) }, null, req)
  }
  const imageGenerationExecutionMatch = path.match(/^\/v1\/internal\/image-generation-jobs\/([^/]+)\/execution$/u)
  if (req.method === 'POST' && imageGenerationExecutionMatch) {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const jobId = decodeURIComponent(imageGenerationExecutionMatch[1]!)
    const input = await body(req)
    const repository = persistence.imageGenerationExecutions
    if (!repository) throw new DomainError('IMAGE_GENERATION_DURABLE_NOT_CONFIGURED', '图片生成执行租约存储未配置', 503)
    const operation = typeof input.operation === 'string' ? input.operation.trim() : ''
    try {
      if (operation === 'claim') {
        const eventId = typeof input.event_id === 'string' ? input.event_id.trim() : ''
        if (!eventId) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '执行租约缺少 event_id', 400)
        const job = service.getImageGenerationJob(workspaceId, jobId)
        const requestedEvents = persistence.outbox ? await persistence.outbox.listAggregateEvents(workspaceId, jobId, 100) : []
        const requested = requestedEvents.find(event => event.id === eventId && event.eventType === 'image.generation.requested')
        if (!requested || requested.payload.intent_hash !== job.intentHash) {
          throw new DomainError('IMAGE_GENERATION_EVENT_INVALID', '执行租约的 event_id 未绑定当前图片任务请求或意图已漂移', 409, { reconciliation_required: true })
        }
        const lease = await repository.claim({ workspaceId, jobId, eventId, leaseMs: 15 * 60 * 1000 })
        return send(res, 200, workspaceId, { execution: lease }, null, req)
      }
      const ownerToken = typeof input.owner_token === 'string' ? input.owner_token.trim() : ''
      if (!ownerToken) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '执行租约操作缺少 owner_token', 400)
      if (operation === 'reserve_provider_operation') {
        const execution = await repository.reserveProviderOperation({ workspaceId, jobId, ownerToken })
        return send(res, 200, workspaceId, { execution }, null, req)
      }
      if (operation === 'begin_provider_dispatch') {
        const execution = await repository.beginProviderDispatch({ workspaceId, jobId, ownerToken })
        return send(res, 200, workspaceId, { execution }, null, req)
      }
      if (operation === 'provider_started') {
        const providerRequestId = typeof input.provider_request_id === 'string' ? input.provider_request_id.trim() : ''
        if (!providerRequestId) throw new DomainError('IMAGE_GENERATION_PROVIDER_REQUEST_ID_REQUIRED', 'Provider 已启动状态必须绑定真实 provider request id', 400)
        return send(res, 200, workspaceId, { execution: await repository.markProviderStarted({ workspaceId, jobId, ownerToken, providerRequestId }) }, null, req)
      }
      if (operation === 'completed') return send(res, 200, workspaceId, { execution: await repository.markCompleted({ workspaceId, jobId, ownerToken }) }, null, req)
      if (operation === 'failed' || operation === 'outcome_unknown') {
        const errorCode = typeof input.error_code === 'string' ? input.error_code : 'IMAGE_GENERATION_FAILED'
        const errorMessage = typeof input.error_message === 'string' ? input.error_message : 'image generation execution failed'
        const execution = operation === 'failed' ? await repository.markFailed({ workspaceId, jobId, ownerToken, errorCode, errorMessage }) : await repository.markOutcomeUnknown({ workspaceId, jobId, ownerToken, errorCode, errorMessage })
        return send(res, 200, workspaceId, { execution }, null, req)
      }
      throw new DomainError(ERROR_CODES.INVALID_REQUEST, '不支持的图片执行租约操作', 400)
    } catch (error) {
      if (error instanceof ImageGenerationExecutionError) {
        const status = error.code === 'IMAGE_GENERATION_EXECUTION_BUSY' ? 409 : error.code === 'IMAGE_GENERATION_EXECUTION_LEASE_LOST' ? 409 : 409
        throw new DomainError(error.code, '图片生成执行租约状态不允许当前操作', status, { execution: error.execution ?? null, retryable: false, reconciliation_required: error.code === 'IMAGE_GENERATION_PROVIDER_OUTCOME_UNKNOWN' })
      }
      throw error
    }
  }
  const imageContinuationMatch = path.match(/^\/v1\/internal\/image-generation-continuations\/([^/]+)\/execute$/u)
  if (req.method === 'POST' && imageContinuationMatch) {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const jobId = decodeURIComponent(imageContinuationMatch[1]!)
    const executed = await executeReadyImageContinuation(workspaceId, jobId)
    return send(res, 200, workspaceId, { job_id: executed.job.id, state: executed.job.state, continuation_state: executed.job.continuation?.state, already_completed: executed.alreadyCompleted }, null, req)
  }
  if (req.method === 'POST' && path === '/v1/internal/storage/reconciliation') {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const report = await runWorkspaceStorageReconciliation(workspaceId)
    return send(res, 200, workspaceId, { report, read_only: true, workspace_id: workspaceId }, null, req)
  }
  if (req.method === 'POST' && path === '/v1/internal/support/sla-scan') {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const input = await body(req)
    const limit = input.limit === undefined ? 100 : Number(input.limit)
    if (!Number.isSafeInteger(limit) || limit < 1 || limit > 1000) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 至 1000 的整数', 400)
    if (input.workspace_id !== undefined && input.workspace_id !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, 'SLA 扫描工作区不匹配', 403)
    const scan = await runSupportSlaScan(workspaceId, limit)
    await recordOperationAudit({ workspaceId, actorId: 'worker:support-sla', action: 'support.sla.scan', resourceType: 'support_sla', resourceId: workspaceId, before: {}, after: scan, reason: 'reconcile worker 扫描客服 SLA 临期与违约事件' })
    return send(res, 200, workspaceId, scan, null, req)
  }
  if (req.method === 'POST' && path === '/v1/internal/support/sla-report') {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const input = await body(req)
    if (input.workspace_id !== undefined && input.workspace_id !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, 'SLA 月报工作区不匹配', 403)
    const periodStart = requiredStringValue(input, 'periodStart', 'period_start')
    const periodEnd = requiredStringValue(input, 'periodEnd', 'period_end')
    const cutoffAt = requiredStringValue(input, 'cutoffAt', 'cutoff_at')
    const reportId = requiredStringValue(input, 'reportId', 'report_id')
    const report = await generateSupportSlaMonthlyReport({ workspaceId, reportId, periodStart, periodEnd, cutoffAt })
    await recordOperationAudit({ workspaceId, actorId: 'worker:support-sla-report', action: 'support.sla.report.generate', resourceType: 'support_sla_report', resourceId: report.reportId, before: {}, after: { checksum: report.checksum, period_start: report.periodStart, period_end: report.periodEnd }, reason: 'reconcile worker 在报告 cutoff 后生成 SLA 月报' })
    return send(res, 200, workspaceId, report, null, req)
  }
  const imageGenerationEvidenceMatch = path.match(/^\/v1\/internal\/image-generation-jobs\/([^/]+)\/reconciliation-evidence$/u)
  if (req.method === 'POST' && imageGenerationEvidenceMatch) {
    // Provider credentials and network calls belong to the Worker adapter. The
    // API accepts only a signed-in-at-the-worker observation and owns the
    // tenant/job/execution checks plus durable state transitions.
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const jobId = decodeURIComponent(imageGenerationEvidenceMatch[1]!)
    await hydrateWorkspace(workspaceId)
    const input = await body(req)
    const repository = persistence.imageGenerationExecutions
    const evidenceRepository = persistence.reconciliationEvidence
    if (!repository || !evidenceRepository) throw new DomainError('IMAGE_GENERATION_DURABLE_NOT_CONFIGURED', '图片生成对账持久化未配置', 503)
    const job = service.getImageGenerationJob(workspaceId, jobId)
    const execution = await repository.get({ workspaceId, jobId })
    const eventId = typeof input.event_id === 'string' ? input.event_id.trim() : ''
    const intentHash = typeof input.intent_hash === 'string' ? input.intent_hash.trim() : ''
    const providerRequestId = typeof input.provider_request_id === 'string' ? input.provider_request_id.trim() : ''
    const idempotencyKey = typeof input.idempotency_key === 'string' ? input.idempotency_key.trim() : ''
    const observedAt = typeof input.observed_at === 'string' ? input.observed_at.trim() : ''
    const providerState = input.provider_state
    const executionAttempt = input.execution_attempt
    const queryAttempt = input.query_attempt
    if (!execution || !eventId || !intentHash || !providerRequestId || !idempotencyKey || !observedAt || !/^[a-f0-9]{64}$/u.test(intentHash)) {
      throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'Provider 对账证据缺少合法 event_id、intent_hash、request_id、幂等键或 observed_at', 400)
    }
    if (input.workspace_id !== undefined && input.workspace_id !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, 'Provider 对账证据工作区不匹配', 403)
    const existingEvidenceForReplay = idempotencyKey ? await evidenceRepository.getByIdempotencyKey({ workspaceId, idempotencyKey }) : undefined
    if (!['provider_started', 'outcome_unknown'].includes(execution.state) && !existingEvidenceForReplay) throw new DomainError('IMAGE_GENERATION_EXECUTION_NOT_RECONCILABLE', '当前图片执行租约不在可对账状态', 409, { reconciliation_required: false, execution_state: execution.state })
    const canonicalInstant = (value: string, field: string) => {
      if (!Number.isFinite(Date.parse(value)) || new Date(value).toISOString() !== value) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${field} 必须是规范 UTC 时间`, 400)
      return value
    }
    canonicalInstant(observedAt, 'observed_at')
    if (!['processing', 'succeeded', 'failed', 'unknown'].includes(String(providerState))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'Provider 对账证据状态无效', 400)
    if (typeof executionAttempt !== 'number' || !Number.isSafeInteger(executionAttempt) || executionAttempt !== execution.attempt || typeof queryAttempt !== 'number' || !Number.isSafeInteger(queryAttempt) || queryAttempt < 1) {
      throw new DomainError('IMAGE_GENERATION_EXECUTION_ATTEMPT_MISMATCH', 'Provider 对账证据的执行次数与服务端租约不匹配', 409, { reconciliation_required: true })
    }
    const expectedIdempotencyKey = imageGenerationReconciliationIdempotencyKey({
      workspaceId, jobId, eventId, intentHash, executionAttempt: executionAttempt as number, providerRequestId, queryAttempt: queryAttempt as number,
    })
    if (idempotencyKey !== expectedIdempotencyKey) throw new DomainError('IMAGE_GENERATION_EVIDENCE_IDEMPOTENCY_KEY_INVALID', 'Provider 对账证据幂等键必须由当前工作区、任务、事件、执行次数、Provider request id 和查询次数稳定生成', 400, { reconciliation_required: true })
    if (job.intentHash !== intentHash) throw new DomainError('IMAGE_GENERATION_INTENT_MISMATCH', 'Provider 对账证据与任务意图不匹配', 409, { reconciliation_required: true })
    const requestedEvents = persistence.outbox ? await persistence.outbox.listAggregateEvents(workspaceId, jobId, 100) : []
    const requested = requestedEvents.find(event => event.id === eventId && event.eventType === 'image.generation.requested')
    if (!requested || requested.payload.intent_hash !== intentHash || execution.eventId !== eventId) {
      throw new DomainError('IMAGE_GENERATION_EVENT_INVALID', 'Provider 对账证据未绑定当前任务的请求事件', 409, { reconciliation_required: true })
    }
    const expectedActionId = typeof requested.payload.action_id === 'string' && requested.payload.action_id.trim() ? requested.payload.action_id.trim() : undefined
    const actionLedgerId = typeof input.action_ledger_id === 'string' && input.action_ledger_id.trim() ? input.action_ledger_id.trim() : undefined
    if (expectedActionId && actionLedgerId !== expectedActionId) throw new DomainError('IMAGE_GENERATION_ACTION_ID_MISMATCH', '图片对账证据的 action ledger 引用与原始扣费授权不匹配', 409, { reconciliation_required: true })
    const actionAuthorization = expectedActionId ? await persistence.actionLedger?.get(workspaceId, expectedActionId) : undefined
    const usageRows = expectedActionId && persistence.modelUsage ? await persistence.modelUsage.listByAction(workspaceId, expectedActionId) : []
    const billingEvidenceReady = !expectedActionId || Boolean(actionAuthorization && ['settled', 'waived'].includes(actionAuthorization.settlementStatus ?? actionAuthorization.state)) && usageRows.length > 0 && usageRows.every(row => ['settled', 'waived'].includes(row.settlementStatus ?? ''))
    if (!execution.providerRequestId || execution.providerRequestId !== providerRequestId) {
      throw new DomainError('IMAGE_GENERATION_PROVIDER_REQUEST_ID_MISMATCH', 'Provider 对账证据的 request id 与执行租约不匹配', 409, { reconciliation_required: true })
    }
    const parseRecord = (value: unknown, field: string) => {
      if (value === undefined) return undefined
      if (!value || typeof value !== 'object' || Array.isArray(value)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${field} 必须是 JSON 对象`, 400)
      if (JSON.stringify(value).length > 32_768) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${field} 超过大小限制`, 400)
      return value as Record<string, unknown>
    }
    const images = input.images === undefined ? undefined : Array.isArray(input.images) && input.images.every(item => typeof item === 'string') ? input.images as string[] : (() => { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'Provider 成功证据的 images 必须是字符串数组', 400) })()
    if (images && images.length > 6) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'Provider 成功证据最多包含 6 张图片', 400)
    const responseDigest = typeof input.response_digest === 'string' ? input.response_digest.trim() : ''
    if (!/^[a-f0-9]{64}$/u.test(responseDigest)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'Provider 对账证据缺少合法 response_digest', 400)
    const artifactDigest = input.artifact_digest === undefined ? undefined : typeof input.artifact_digest === 'string' && /^[a-f0-9]{64}$/u.test(input.artifact_digest.trim()) ? input.artifact_digest.trim() : (() => { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'artifact_digest 必须是 SHA-256 摘要', 400) })()
    const providerStatus = input.provider_status === undefined ? undefined : typeof input.provider_status === 'string' && input.provider_status.trim() ? input.provider_status.trim() : (() => { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'provider_status 无效', 400) })()
    const errorCode = input.error_code === undefined ? undefined : typeof input.error_code === 'string' && input.error_code.trim() ? input.error_code.trim() : (() => { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'error_code 无效', 400) })()
    const errorMessage = input.error_message === undefined ? undefined : typeof input.error_message === 'string' && input.error_message.trim() ? input.error_message.trim() : (() => { throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'error_message 无效', 400) })()
    const providerStateValue = providerState as 'processing' | 'succeeded' | 'failed' | 'unknown'
    if ((providerStateValue === 'failed' || providerStateValue === 'unknown') !== Boolean(errorCode && errorMessage)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'failed/unknown 证据必须同时提供 error_code 和 error_message', 400)
    if ((providerStateValue === 'processing' || providerStateValue === 'succeeded') && (errorCode || errorMessage)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'processing/succeeded 证据不能包含错误详情', 400)
    const usageRecord = parseRecord(input.usage, 'usage')
    const costRecord = parseRecord(input.cost, 'cost')
    const previousEvidence = existingEvidenceForReplay
    let evidence: Awaited<ReturnType<ReconciliationEvidenceRepository['append']>>
    try {
      evidence = await evidenceRepository.append({
      workspaceId, jobId, executionAttempt: Number(executionAttempt), providerRequestId, queryAttempt: Number(queryAttempt), idempotencyKey,
      providerState: providerStateValue, ...(providerStatus ? { providerStatus } : {}), responseDigest, ...(artifactDigest ? { artifactDigest } : {}),
      ...(typeof input.usage_ledger_id === 'string' && input.usage_ledger_id.trim() ? { usageLedgerId: input.usage_ledger_id.trim() } : {}),
      ...(typeof input.action_ledger_id === 'string' && input.action_ledger_id.trim() ? { actionLedgerId: input.action_ledger_id.trim() } : {}),
      ...(usageRecord ? { usage: usageRecord } : {}), ...(costRecord ? { cost: costRecord } : {}),
      observedAt, ...(typeof input.next_attempt_at === 'string' ? { nextAttemptAt: canonicalInstant(input.next_attempt_at.trim(), 'next_attempt_at') } : {}), ...(errorCode ? { errorCode } : {}), ...(errorMessage ? { errorMessage } : {}),
      })
    } catch (error) {
      if (error instanceof ReconciliationEvidenceIdempotencyConflictError) throw new DomainError('IMAGE_GENERATION_EVIDENCE_IDEMPOTENCY_CONFLICT', 'Provider 对账证据幂等键已绑定到不同内容', 409, { reconciliation_required: true })
      if (error instanceof Error && error.message.startsWith('RECONCILIATION_EVIDENCE_')) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'Provider 对账证据未通过持久化校验', 400)
      throw error
    }
    if (previousEvidence) return send(res, 200, workspaceId, { job_id: job.id, provider_request_id: providerRequestId, provider_state: providerStateValue, evidence_id: evidence.id, replayed: true, reconciliation_required: providerStateValue !== 'failed' }, null, req)
    const repaired: Record<string, unknown> = { job_id: job.id, provider_request_id: providerRequestId, provider_state: providerStateValue, evidence_id: evidence.id }
    if (providerStateValue === 'failed') {
      const settled = await repository.reconcileFailed({ workspaceId, jobId: job.id, errorCode: errorCode!, errorMessage: errorMessage! })
      return send(res, 200, workspaceId, { ...repaired, execution: settled, reconciliation_required: false }, null, req)
    }
    if (providerStateValue === 'succeeded' && images?.length) {
      const archived = await archiveGeneratedImages(workspaceId, job.id, images)
      if (archived.archiveState === 'archived' && imageJobOutputsAreClean(archived) && billingEvidenceReady) {
        const settled = await repository.reconcileCompleted({ workspaceId, jobId: job.id })
        return send(res, 200, workspaceId, { ...repaired, execution: settled, archive_state: archived.archiveState, candidate_count: archived.outputs?.length ?? 0, reconciliation_required: false }, null, req)
      }
      return send(res, 200, workspaceId, { ...repaired, archive_state: archived.archiveState, candidate_count: archived.outputs?.length ?? 0, reconciliation_required: true, next_action: billingEvidenceReady ? '等待产物归档与安全扫描完成' : '等待图片用量/成本与原始扣费授权完成结算' }, null, req)
    }
    return send(res, 200, workspaceId, { ...repaired, reconciliation_required: true, next_action: providerStateValue === 'processing' ? 'Provider 仍在处理中，等待下一次查询' : 'Provider 状态或产物尚不足以完成交付，保持对账状态' }, null, req)
  }
  if (req.method === 'POST' && path === '/v1/internal/model-usage') {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const input = await body(req)
    const modality = input.modality
    const model = typeof input.model === 'string' ? input.model.trim() : ''
    const actionId = typeof input.actionId === 'string' ? input.actionId.trim() : undefined
    const runKey = typeof input.runKey === 'string' ? input.runKey.trim() : undefined
    const contextLinkId = typeof input.contextLinkId === 'string' ? input.contextLinkId.trim() : undefined
    const contextHash = typeof input.contextHash === 'string' ? input.contextHash.trim() : undefined
    const providerRequestId = typeof input.providerRequestId === 'string' ? input.providerRequestId.trim() : undefined
    const number = (value: unknown, name: string) => {
      if (value === undefined) return undefined
      if (typeof value !== 'number' || !Number.isFinite(value) || value < 0) throw new DomainError(ERROR_CODES.INVALID_REQUEST, `${name} 必须是非负数`, 400)
      return value
    }
    if (!['text', 'image', 'image_edit', 'ocr', 'video'].includes(String(modality)) || !model || !actionId || !runKey) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '模型用量回执缺少合法 modality、model、actionId 或 runKey', 400)
    if ((contextLinkId === undefined) !== (contextHash === undefined) || (contextHash !== undefined && !/^[a-f0-9]{64}$/u.test(contextHash))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '模型用量回执的 contextLinkId/contextHash 必须成对且合法', 400)
    if (input.workspaceId !== undefined && input.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '模型用量回执工作区不匹配', 403)
    const actionAuthorization = await persistence.actionLedger?.get(workspaceId, actionId)
    if (!actionAuthorization || ['refunded', 'released', 'manual_attention'].includes(actionAuthorization.settlementStatus ?? '') || actionAuthorization.state === 'refunded') {
      throw new DomainError('MODEL_USAGE_ACTION_NOT_AUTHORIZED', '模型中转回执未绑定有效的原始扣费授权，已阻断入账', 409)
    }
    await recordRelayUsage({ workspaceId, actionId, runKey, ...(contextLinkId ? { contextLinkId, contextHash: contextHash! } : {}), modality: modality as RelayUsageRecord['modality'], model, ...(providerRequestId ? { providerRequestId } : {}), ...(input.inputTokens !== undefined ? { inputTokens: number(input.inputTokens, 'inputTokens')! } : {}), ...(input.outputTokens !== undefined ? { outputTokens: number(input.outputTokens, 'outputTokens')! } : {}), ...(input.totalTokens !== undefined ? { totalTokens: number(input.totalTokens, 'totalTokens')! } : {}), ...(input.costCny !== undefined ? { costCny: number(input.costCny, 'costCny')! } : {}), observedAt: typeof input.observedAt === 'string' && Number.isFinite(Date.parse(input.observedAt)) ? input.observedAt : new Date().toISOString(), ...(input.metadata && typeof input.metadata === 'object' && !Array.isArray(input.metadata) ? { metadata: input.metadata as Record<string, unknown> } : {}) })
    return send(res, 200, workspaceId, { recorded: true, action_id: actionId ?? null, provider_request_id: providerRequestId ?? null }, null, req)
  }
  if (req.method === 'POST' && path === '/v1/internal/image-generation-jobs/reconciliation') {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    await hydrateWorkspace(workspaceId)
    const input = await body(req)
    const limit = input.limit === undefined ? 100 : Number(input.limit)
    if (!Number.isSafeInteger(limit) || limit < 1 || limit > 1000) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 至 1000 的整数', 400)
    if (input.workspace_id !== undefined && input.workspace_id !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '图片对账工作区不匹配', 403)
    const cursor = input.cursor === undefined ? undefined : typeof input.cursor === 'string' && input.cursor.trim() ? input.cursor.trim() : (() => { throw new DomainError(ERROR_CODES.INVALID_REQUEST, '图片对账 cursor 必须是非空字符串', 400) })()
    const olderThan = input.older_than === undefined ? undefined : typeof input.older_than === 'string' && Number.isFinite(Date.parse(input.older_than)) ? input.older_than : (() => { throw new DomainError(ERROR_CODES.INVALID_REQUEST, '图片对账 older_than 必须是合法时间', 400) })()
    const repository = persistence.imageGenerationExecutions
    if (!repository) throw new DomainError('IMAGE_GENERATION_DURABLE_NOT_CONFIGURED', '图片生成执行租约存储未配置', 503)
    const page = await repository.listPage({ workspaceId, states: ['provider_reserved', 'provider_dispatching', 'provider_started', 'outcome_unknown'], limit, ...(cursor ? { cursor } : {}), ...(olderThan ? { olderThan } : {}) })
    const executions = page.items
    const repaired: Array<Record<string, unknown>> = []
    const attention: Array<Record<string, unknown>> = []
    for (const execution of executions) {
      const job = service.getImageGenerationJob(workspaceId, execution.jobId)
      if (job.state === 'succeeded' && job.archiveState === 'archived' && Boolean(job.outputs?.length)) {
        const settled = await repository.reconcileCompleted({ workspaceId, jobId: job.id })
        repaired.push({ job_id: job.id, from: execution.state, to: settled.state, reason: 'job_archive_is_authoritative' })
      } else if (job.state === 'failed') {
        const settled = await repository.reconcileFailed({ workspaceId, jobId: job.id, errorCode: job.errorCode ?? 'IMAGE_GENERATION_FAILED', errorMessage: job.errorMessage ?? '图片生成任务已失败' })
        repaired.push({ job_id: job.id, from: execution.state, to: settled.state, reason: 'job_failure_is_authoritative' })
      } else {
        const latestEvidence = persistence.reconciliationEvidence ? await persistence.reconciliationEvidence.getLatest({ workspaceId, jobId: execution.jobId }) : undefined
        if (latestEvidence?.nextAttemptAt && Date.parse(latestEvidence.nextAttemptAt) > Date.now()) continue
        const requestedEvents = persistence.outbox ? await persistence.outbox.listAggregateEvents(workspaceId, job.id, 100) : []
        const requested = requestedEvents.find(event => event.eventType === 'image.generation.requested' && event.payload.intent_hash === job.intentHash)
        attention.push({ job_id: job.id, event_id: execution.eventId, intent_hash: job.intentHash, execution_attempt: execution.attempt, query_attempt: (latestEvidence?.queryAttempt ?? 0) + 1, execution_state: execution.state, provider_request_id: execution.providerRequestId ?? null, reconciliation_required: true, next_action: 'Worker 必须查询真实 Provider 后提交 reconciliation-evidence；API 禁止直接查询 Provider' })
        if (requested?.payload.action_id) attention.at(-1)!.action_id = requested.payload.action_id
      }
    }
    return send(res, 200, workspaceId, { checked: executions.length, repaired, attention, next_cursor: page.nextCursor ?? null, has_more: Boolean(page.nextCursor), scan_watermark: page.scanWatermark, read_only_provider_policy: true, provider_evidence_endpoint: '/v1/internal/image-generation-jobs/{job_id}/reconciliation-evidence' }, null, req)
  }
  if (req.method === 'POST' && path === '/v1/internal/model-usage/reconciliation') {
    await requireWorkerAuthorization(req)
    const workspaceId = headerRequired(req, 'x-workspace-id')
    const input = await body(req)
    const limit = input.limit === undefined ? 50 : Number(input.limit)
    if (!Number.isSafeInteger(limit) || limit < 1 || limit > 100) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'limit 必须是 1 至 100 的整数', 400)
    if (input.workspace_id !== undefined && input.workspace_id !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '模型用量对账工作区不匹配', 403)
    const actorId = 'worker:model-usage-reconciliation'
    const reconciliation = await runModelUsageReconciliation({ workspaceId, actorId, limit })
    await recordOperationAudit({ workspaceId, actorId, action: 'billing.model-usage.reconciliation.worker', resourceType: 'model_usage', resourceId: workspaceId, before: {}, after: reconciliation, reason: 'reconcile worker 自动执行模型用量结算重试' })
    return send(res, 200, workspaceId, reconciliation, null, req)
  }
  if (req.method === 'POST' && paymentCallbackMatch) {
    const input = await body(req)
    const orderId = required(input, 'order_id')
    const providerTradeId = required(input, 'provider_trade_id')
    const amountFen = Number(input.amount_fen)
    const state = typeof input.state === 'string' ? input.state : ''
    const workspaceId = typeof input.workspace_id === 'string' && input.workspace_id.trim() ? input.workspace_id.trim() : ''
    if (!workspaceId || !Number.isSafeInteger(amountFen) || amountFen < 0) throw new DomainError('PAYMENT_CALLBACK_INVALID', '支付回调缺少有效订单、工作区或金额', 400)
    const callbackProof = verifyPaymentCallback(req, { channel: paymentCallbackMatch[2] as RechargeChannel, workspaceId, payload: { order_id: orderId, provider_trade_id: providerTradeId, amount_fen: amountFen, state } })
    const freshCallbackProof = callbackProof ? await consumePaymentCallbackProof({ workspaceId, channel: paymentCallbackMatch[2] as RechargeChannel, ...callbackProof }) : true
    if (paymentCallbackMatch[1] === 'subscriptions') {
      const order = await (persistence.subscriptions ?? memorySubscriptions).getOrderByOrderNo(workspaceId, orderId)
      if (!order) throw new DomainError('SUBSCRIPTION_ORDER_NOT_FOUND', '支付回调对应的订阅订单不存在', 404)
      if (order.paymentProvider !== paymentCallbackMatch[2]) throw new DomainError('PAYMENT_CALLBACK_CHANNEL_MISMATCH', '支付回调渠道与订阅订单渠道不一致', 400)
      if (Math.round(order.paymentAmountCny * 100) !== amountFen) throw new DomainError('SUBSCRIPTION_CALLBACK_AMOUNT_MISMATCH', '支付回调金额与订阅订单支付金额快照不一致', 400)
      if (state !== 'paid' && state !== 'SUCCESS') return send(res, 200, workspaceId, { accepted: true, order_id: orderId, state }, null, req)
      if (!freshCallbackProof && order.status !== 'paid') throw new DomainError('PAYMENT_CALLBACK_NONCE_REPLAY', '支付回调 nonce 已被使用，且订单尚未进入已支付状态', 409)
      if (order.status === 'paid') {
        if (order.providerTradeId !== providerTradeId) throw new DomainError('SUBSCRIPTION_CALLBACK_REPLAY_CONFLICT', '已支付订阅订单不能使用不同的支付交易号重复入账', 409)
        await synchronizeCommercialQuotaFromSubscription(order)
        const entitlements = await grantSubscriptionEntitlements({ workspaceId, orderNo: order.orderNo, addonCodes: order.addonCodes, extensions: persistence.commercialExtensions ?? memoryCommercialExtensions, entitlements: persistence.entitlements ?? memoryEntitlements })
        return send(res, 200, workspaceId, { accepted: true, order_id: orderId, state: 'paid', replayed: true, entitlements_granted: entitlements.map(item => ({ addon_code: item.addonCode, kind: item.kind, units: item.grantedUnits })) }, null, req)
      }
      const paid = await (persistence.subscriptions ?? memorySubscriptions).markPaid({ workspaceId, orderNo: orderId, providerTradeId, eventSource: 'provider_callback' })
      if (!persistence.outbox) await persistEvent(workspaceId, paid.orderNo, 'subscription.order.paid', 1, { order_no: paid.orderNo, provider_trade_id: providerTradeId, plan_code: paid.planCode, addon_codes: paid.addonCodes, included_stores: paid.includedStores, included_tasks: paid.includedTasks, source: 'provider_callback' })
      await synchronizeCommercialQuotaFromSubscription(paid)
      const entitlements = await grantSubscriptionEntitlements({ workspaceId, orderNo: paid.orderNo, addonCodes: paid.addonCodes, extensions: persistence.commercialExtensions ?? memoryCommercialExtensions, entitlements: persistence.entitlements ?? memoryEntitlements })
      await recordOperationAudit({ workspaceId, actorId: 'payment_provider', action: 'subscription.order.paid', resourceType: 'subscription_order', resourceId: orderId, before: order as unknown as Record<string, unknown>, after: paid as unknown as Record<string, unknown>, reason: `支付服务商回调（${paymentCallbackMatch[2]}）` })
      await recordGrowthEvent({ workspaceId, eventType: 'subscription.order.paid', actorId: 'payment_provider', planCode: paid.planCode, sourceChannel: paid.sourceChannel, metadata: { orderNo: paid.orderNo, paymentAmountCny: paid.paymentAmountCny } })
      return send(res, 200, workspaceId, { accepted: true, order_id: orderId, state: 'paid', entitlements_granted: entitlements.map(item => ({ addon_code: item.addonCode, kind: item.kind, units: item.grantedUnits })) }, null, req)
    }
    const rechargeOrder = persistence.billing ? await persistence.billing.getOrder(workspaceId, orderId) : rechargeOrders.get(orderId)
    if (!rechargeOrder || rechargeOrder.workspaceId !== workspaceId) throw new DomainError('BILLING_ORDER_NOT_FOUND', '支付回调对应的充值订单不存在', 404)
    if (rechargeOrder.channel !== paymentCallbackMatch[2]) throw new DomainError('PAYMENT_CALLBACK_CHANNEL_MISMATCH', '支付回调渠道与充值订单渠道不一致', 400)
    if (rechargeOrder.amountFen !== amountFen) throw new DomainError('BILLING_CALLBACK_AMOUNT_MISMATCH', '支付回调金额与订单金额不一致', 400)
    if (isProduction() && rechargeOrder.paymentMode !== 'provider') throw new DomainError('PAYMENT_ORDER_MODE_MISMATCH', '生产环境不能为 fixture 充值订单入账', 409)
    if (state !== 'paid' && state !== 'SUCCESS') return send(res, 200, workspaceId, { accepted: true, order_id: orderId, state }, null, req)
    if (!freshCallbackProof && rechargeOrder.state !== 'paid') throw new DomainError('PAYMENT_CALLBACK_NONCE_REPLAY', '支付回调 nonce 已被使用，且订单尚未进入已支付状态', 409)
    if (rechargeOrder.state === 'paid' && rechargeOrder.providerTradeId && rechargeOrder.providerTradeId !== providerTradeId) throw new DomainError('PAYMENT_CALLBACK_REPLAY_CONFLICT', '已到账订单不能使用不同的支付交易号重复入账', 409)
    const paid = await markRechargePaid({ workspaceId, orderId, providerTradeId, amountFen, eventSource: 'provider_callback' })
    if (!paid) throw new DomainError('BILLING_ORDER_NOT_FOUND', '支付回调对应的充值订单不存在', 404)
    return send(res, 200, workspaceId, { accepted: true, order_id: orderId, state: 'paid' }, null, req)
  }
  if (requestWorkspace !== 'unknown' && !workerRoute && !isOAuthCallback && !paymentCallbackMatch) {
    const customerMethod = customerDataMethodForHttp(req.method, path)
    if (customerMethod) await enforceCustomerDataAccess(req, requestWorkspace, customerMethod)
  }
  if (requestWorkspace !== 'unknown' && !assetScannerRoute && !isHttpOnboardingExempt(path) && !/^\/v1\/oauth\/callback\//u.test(path)) requireStoreOnboarding(requestWorkspace, `http:${path}`)
  await enforceRateLimit(req, platformRateScope ?? requestWorkspace ?? 'unknown')
  if (req.method === 'GET' && path === '/v1/products') {
    const workspaceId = resolveWorkspace(req, url.searchParams.get('workspace_id') ?? undefined)
    const page = paginationRequest(url)
    const accessibleBrandIds = await accessibleTaskBrandIds(req, workspaceId)
    if (persistence.business) {
      const facts = url.searchParams.get('facts_confirmed')
      return send(res, 200, workspaceId, await persistence.business.listProductsPage(workspaceId, {
        ...page,
        ...(accessibleBrandIds !== undefined ? { accessibleBrandIds } : {}),
        ...(url.searchParams.get('query') ? { query: url.searchParams.get('query')! } : {}),
        ...(url.searchParams.get('platform') ? { platform: url.searchParams.get('platform')! } : {}),
        ...(url.searchParams.get('account_id') ? { accountId: url.searchParams.get('account_id')! } : {}),
        ...(url.searchParams.get('store_name') ? { storeName: url.searchParams.get('store_name')! } : {}),
        ...(facts !== null ? { factsConfirmed: facts === 'true' } : {}),
      }), null, req)
    }
    const accessibleIds = await accessibleProductIds(req, workspaceId)
    const products = service.listProducts(workspaceId, {
      ...(url.searchParams.get('query') ? { query: url.searchParams.get('query')! } : {}),
      ...(url.searchParams.get('platform') ? { platform: url.searchParams.get('platform') as Platform } : {}),
      ...(url.searchParams.get('account_id') ? { accountId: url.searchParams.get('account_id')! } : {}),
      ...(url.searchParams.get('store_name') ? { storeName: url.searchParams.get('store_name')! } : {}),
    }).filter(product => (accessibleIds === undefined || accessibleIds.has(product.id)) && (url.searchParams.get('facts_confirmed') === null || product.factsConfirmed === (url.searchParams.get('facts_confirmed') === 'true')))
      .sort((left, right) => right.updatedAt.localeCompare(left.updatedAt) || left.id.localeCompare(right.id))
    return send(res, 200, workspaceId, paginatedResult(url, products), null, req)
  }
  const productGetMatch = path.match(/^\/v1\/products\/([^/]+)$/)
  const productAssetsMatch = path.match(/^\/v1\/products\/([^/]+)\/assets$/)
  if (req.method === 'GET' && productAssetsMatch) {
    const workspaceId = resolveWorkspace(req)
    const productId = decodeURIComponent(productAssetsMatch[1]!)
    const product = service.products.get(productId)
    if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
    await enforceProductBrandAccess(req, workspaceId, productId)
    if (persistence.business) {
      const items = await persistence.business.listProductAssetBindings(workspaceId, { productId })
      return send(res, 200, workspaceId, { items, source: 'normalized_relation' }, null, req)
    }
    const items = service.listAssets(workspaceId)
      .filter(asset => product.sourceAssetIds?.includes(asset.id))
      .map((asset, index) => ({ workspaceId, productId, assetId: asset.id, assetRole: 'source' as const, ordinal: index + 1, status: 'active' as const, createdAt: asset.createdAt, updatedAt: asset.createdAt }))
    return send(res, 200, workspaceId, { items, source: 'compatibility_projection' }, null, req)
  }
  if ((req.method === 'POST' || req.method === 'DELETE') && productAssetsMatch) {
    await persistenceReady
    if (!persistence.business) throw new DomainError('BUSINESS_PERSISTENCE_REQUIRED', '商品素材关系变更需要持久化业务仓储', 503)
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const productId = decodeURIComponent(productAssetsMatch[1]!)
    const assetId = typeof input.asset_id === 'string' ? input.asset_id.trim() : ''
    const brandId = typeof input.brand_id === 'string' ? input.brand_id.trim() : ''
    const assetRole = typeof input.asset_role === 'string' ? input.asset_role.trim() : 'source'
    const expectedVersion = typeof input.expected_version === 'number' ? input.expected_version : Number(input.expected_version)
    const ordinal = input.ordinal === undefined ? undefined : Number(input.ordinal)
    const reason = typeof input.reason === 'string' ? input.reason.trim() : ''
    if (!assetId || !brandId || !reason || !Number.isSafeInteger(expectedVersion) || expectedVersion < 1 || (ordinal !== undefined && (!Number.isSafeInteger(ordinal) || ordinal < 1)) || !['source', 'main', 'secondary', 'detail'].includes(assetRole)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'asset_id、brand_id、reason、expected_version 和合法 asset_role 为必填项', 400)
    await enforceProductBrandBinding(req, workspaceId, productId, brandId)
    const actorId = requestActor(req)
    try {
      const change = { workspaceId, productId, assetId, brandId, assetRole: assetRole as import('../../../packages/persistence/src/index.js').ProductAssetBindingRow['assetRole'], ...(ordinal !== undefined ? { ordinal } : {}), expectedVersion, actorId, reason }
      const binding = req.method === 'POST' ? await persistence.business.bindProductAsset(change) : await persistence.business.unbindProductAsset(change)
      return send(res, req.method === 'POST' ? 201 : 200, workspaceId, { binding, audited: true, source: 'controlled_relation_service' }, null, req)
    } catch (error) {
      if (error instanceof BusinessSnapshotVersionConflictError) throw new DomainError('PRODUCT_ASSET_BINDING_VERSION_CONFLICT', '商品版本已变化，请刷新后重试', 409)
      if (error instanceof Error && error.message === 'PRODUCT_ASSET_BINDING_BRAND_MISMATCH') throw new DomainError('PRODUCT_ASSET_BINDING_BRAND_MISMATCH', '商品或素材不属于指定品牌', 403)
      if (error instanceof Error && error.message === 'PRODUCT_ASSET_BINDING_ASSET_NOT_FOUND') throw new DomainError('PRODUCT_ASSET_BINDING_ASSET_NOT_FOUND', '素材不存在或不属于当前工作区', 404)
      throw error
    }
  }
  if (req.method === 'GET' && productGetMatch) {
    const workspaceId = resolveWorkspace(req)
    const product = service.products.get(decodeURIComponent(productGetMatch[1]!))
    if (!product || product.workspaceId !== workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品不存在或不属于当前工作区', 404)
    await enforceProductBrandAccess(req, workspaceId, product.id)
    return send(res, 200, workspaceId, product, null, req)
  }
  const assetProductsMatch = path.match(/^\/v1\/assets\/([^/]+)\/products$/)
  if (req.method === 'GET' && assetProductsMatch) {
    const workspaceId = resolveWorkspace(req)
    const assetId = decodeURIComponent(assetProductsMatch[1]!)
    const asset = service.listAssets(workspaceId).find(item => item.id === assetId)
    if (!asset) throw new DomainError('ASSET_NOT_FOUND', '素材不存在或不属于当前工作区', 404)
    const accessibleIds = await accessibleProductIds(req, workspaceId)
    if (persistence.business) {
      const items = (await persistence.business.listProductAssetBindings(workspaceId, { assetId })).filter(item => accessibleIds === undefined || accessibleIds.has(item.productId))
      return send(res, 200, workspaceId, { items, source: 'normalized_relation' }, null, req)
    }
    const items = service.listProducts(workspaceId).filter(product => (accessibleIds === undefined || accessibleIds.has(product.id)) && product.sourceAssetIds?.includes(assetId)).map(product => ({ workspaceId, productId: product.id, assetId, assetRole: 'source' as const, ordinal: product.sourceAssetIds!.indexOf(assetId) + 1, status: 'active' as const, createdAt: product.updatedAt, updatedAt: product.updatedAt }))
    return send(res, 200, workspaceId, { items, source: 'compatibility_projection' }, null, req)
  }
  const productImageReviewMatch = path.match(/^\/v1\/products\/([^/]+)\/image-review$/)
  if (req.method === 'GET' && productImageReviewMatch) {
    const workspaceId = resolveWorkspace(req)
    const productId = decodeURIComponent(productImageReviewMatch[1]!)
    await enforceProductBrandAccess(req, workspaceId, productId)
    return send(res, 200, workspaceId, service.reviewProductImages(workspaceId, productId), null, req)
  }
  if (req.method === 'GET' && path === '/v1/tasks') {
    const workspaceId = resolveWorkspace(req, url.searchParams.get('workspace_id') ?? undefined)
    const page = paginationRequest(url)
    if (persistence.business) {
      const accessibleBrandIds = await accessibleTaskBrandIds(req, workspaceId)
      return send(res, 200, workspaceId, await persistence.business.listTasksPage(workspaceId, {
        ...page,
        ...(accessibleBrandIds !== undefined ? { accessibleBrandIds } : {}),
        ...(url.searchParams.get('query') ? { query: url.searchParams.get('query')! } : {}),
        ...(url.searchParams.get('platform') ? { platform: url.searchParams.get('platform')! } : {}),
        ...(url.searchParams.get('state') ? { state: url.searchParams.get('state')! } : {}),
        ...(url.searchParams.get('product_id') ? { productId: url.searchParams.get('product_id')! } : {}),
      }), null, req)
    }
    const tasks = await filterByTaskBrandAccess(req, workspaceId, service.listTasks(workspaceId, {
      ...(url.searchParams.get('query') ? { query: url.searchParams.get('query')! } : {}),
      ...(url.searchParams.get('platform') ? { platform: url.searchParams.get('platform') as Platform } : {}),
      ...(url.searchParams.get('state') ? { state: url.searchParams.get('state') as import('../../../packages/application/src/service.js').TaskState } : {}),
      ...(url.searchParams.get('product_id') ? { productId: url.searchParams.get('product_id')! } : {}),
    }))
    return send(res, 200, workspaceId, paginatedResult(url, tasks), null, req)
  }
  if (req.method === 'GET' && path === '/v1/platform-capabilities') {
    const workspaceId = resolveWorkspace(req, url.searchParams.get('workspace_id') ?? undefined)
    const generatedAt = new Date().toISOString()
    const mediaSpecRepository = await platformMediaSpecRepository()
    const mediaSpecs = await mediaSpecRepository.list({ status: 'approved', device: 'desktop', at: generatedAt })
    return send(res, 200, workspaceId, {
      items: SUPPORTED_PLATFORMS.map(platform => ({
        platform,
        readiness: connectorRuntime.readiness[platform],
        capabilities: projectPlatformCapabilityEvidence(connectorRuntime.capabilityMatrix(platform), mediaSpecs, platform),
      })),
    }, null, req)
  }
  if (req.method === 'GET' && path === '/v1/delivery-readiness') {
    const workspaceId = resolveWorkspace(req, url.searchParams.get('workspace_id') ?? undefined)
    const accessibleIds = await accessibleProductIds(req, workspaceId)
    const products = (persistence.business
      ? (await persistence.business.listProductsPage(workspaceId, { limit: 100, offset: 0 })).items
      : service.listProducts(workspaceId))
      .filter(product => typeof product.id === 'string' && (accessibleIds === undefined || accessibleIds.has(product.id)))
    const repository = await platformMappingApprovalRepository()
    const generatedAt = new Date().toISOString()
    const mappingPreflights = await Promise.all(products.flatMap(product => {
      const productId = typeof product.id === 'string' ? product.id : undefined
      const platform = asPlatform(product.platform)
      if (!productId || !platform) return []
      return [repository.get({ workspaceId, platform, productId }).then(async approval => {
        const active = approval ? await repository.resolveActive({ workspaceId, platform, productId, productVersion: typeof product.version === 'number' ? product.version : 1, mappedPayloadHash: approval.mappedPayloadHash, remoteSnapshotHash: approval.remoteSnapshotHash, schemaVersion: approval.schemaVersion, schemaEvidenceHash: approval.schemaEvidenceHash, mappingVersion: approval.mappingVersion, mappingEvidenceHash: approval.mappingEvidenceHash, at: generatedAt }) : undefined
        const approved = Boolean(active)
        const findingCodes = approval?.findingCodes ?? []
        const findings = findingCodes.map(code => ({ code, message: `字段映射预检 finding：${code}`, nextAction: '修正字段映射后重新执行 platform.mapping.preflight' }))
        if (!approval) findings.push({ code: 'MAPPING_PREFLIGHT_MISSING', message: '尚无持久化字段映射预检审批', nextAction: '执行 platform.mapping.preflight 并完成商家确认' })
        else if (!approved && findings.length === 0) findings.push({ code: 'MAPPING_PREFLIGHT_NOT_ACTIVE', message: '字段映射预检审批已过期、撤销或未满足发布条件', nextAction: '重新执行 platform.mapping.preflight' })
        return { id: `${platform}:${productId}`, platform, productId, status: approved ? 'passed' : approval ? 'blocked' : 'unverified', findings }
      })]
    }))
    const productIds = new Set(products.map(product => product.id))
    const visibleTasks = (await filterByTaskBrandAccess(req, workspaceId, service.listTasks(workspaceId))).filter(task => productIds.has(task.productId))
    const bundles = visibleTasks.flatMap(task => service.listContentVersions(workspaceId, task.id)
      .filter(version => version.state === 'approved')
      .map(version => {
        try {
          const exported = service.exportContent(workspaceId, version.id, 'bundle')
          if (!exported.binaryBody) throw new Error('bundle bytes missing')
          const verification = verifyExportedBundle(workspaceId, version.id, exported.binaryBody)
          const passed = verification.valid === true && verification.content_publishable === true
          return { id: version.id, taskId: task.id, productId: task.productId, status: passed ? 'passed' : 'blocked', findings: passed ? [] : [{ code: 'DELIVERY_BUNDLE_NOT_VERIFIED', message: '交付包完整性或内容发布状态未通过', nextAction: '重新导出并执行 delivery.bundle.verify' }], verification: { valid: verification.valid, manifestHash: verification.manifest_hash, artifactSha256: verification.artifact_sha256 } }
        } catch {
          return { id: version.id, taskId: task.id, productId: task.productId, status: 'blocked', findings: [{ code: 'DELIVERY_BUNDLE_BUILD_FAILED', message: '无法从当前已批准版本生成并验证交付包', nextAction: '修复内容版本或导出配置后重试' }] }
        }
      }))
    const authenticity = [...service.imageGenerationJobs.values()]
      .filter(job => job.workspaceId === workspaceId && productIds.has(job.productId) && job.state === 'succeeded' && job.archiveState === 'archived')
      .flatMap(job => (job.outputs ?? []).map(output => {
        const evidence = output.authenticity as unknown as PersistedVisualAuthenticity | undefined
        const passed = evidence?.publishable === true && evidence.candidateSha256 === output.sha256
        return { id: output.visualRef, jobId: job.id, productId: job.productId, status: passed ? 'passed' : evidence ? 'blocked' : 'unverified', findings: passed ? [] : [{ code: evidence ? 'VISUAL_AUTHENTICITY_BLOCKED' : 'VISUAL_AUTHENTICITY_MISSING', message: evidence ? '视觉真实性证据未满足发布要求' : '视觉候选尚无真实性证据', nextAction: '执行 catalog.image.review 并绑定当前候选 SHA-256' }] }
      }))
    const dimensionStatus = (items: Array<{ status: string }>) => !items.length ? 'unverified' : items.every(item => item.status === 'passed') ? 'passed' : 'blocked'
    const dimensions = { mapping: dimensionStatus(mappingPreflights), bundles: dimensionStatus(bundles), authenticity: dimensionStatus(authenticity) }
    const status = Object.values(dimensions).every(value => value === 'passed') ? 'passed' : Object.values(dimensions).includes('blocked') ? 'blocked' : 'unverified'
    return send(res, 200, workspaceId, { generatedAt, status, dimensions, mappingPreflights, bundles, authenticity }, null, req)
  }
  if (req.method === 'GET' && path === '/v1/rules') {
    const workspaceId = resolveWorkspace(req)
    const requestedPlatform = url.searchParams.get('platform')?.trim()
    if (requestedPlatform && !SUPPORTED_PLATFORMS.includes(requestedPlatform as Platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 无效', 400)
    const appliesToPlatform = (rule: { status: string; scope?: string; targetId?: string; scopeValue?: string }) => rule.status === 'active' && (!requestedPlatform || rule.scope === 'global' || (rule.scope === 'platform' && (rule.targetId ?? rule.scopeValue) === requestedPlatform))
    const respond = <T>(rules: T[]) => url.searchParams.has('limit') || url.searchParams.has('offset') ? paginatedResult(url, rules) : rules
    const repository = ruleRepository()
    if (repository) {
      const packId = url.searchParams.get('pack_id')?.trim() || undefined
      const rows = await repository.list(workspaceId, packId)
      if (rows.length || packId) return send(res, 200, workspaceId, respond(rows.map(publicRule).filter(appliesToPlatform)), null, req)
      return send(res, 200, workspaceId, respond((await persistedRules(workspaceId) ?? []).filter(appliesToPlatform)), null, req)
    }
    if (isProduction()) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '生产规则仓储未配置', 503)
    return send(res, 200, workspaceId, respond(service.listRulePacks().filter(appliesToPlatform)), null, req)
  }
  if (req.method === 'GET' && path === '/v1/catalog/categories') {
    const workspaceId = resolveWorkspace(req)
    const query = url.searchParams.get('query')?.trim().toLocaleLowerCase()
    const items = query ? catalogCategories.filter(item => `${item.code}${item.name}${item.fields.join('')}`.toLocaleLowerCase().includes(query)) : catalogCategories
    return send(res, 200, workspaceId, items, null, req)
  }
  if (req.method === 'GET' && path === '/v1/rules/audit') {
    const workspaceId = resolveWorkspace(req)
    requireRuleAdmin(req)
    const repository = ruleRepository()
    if (!repository) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '规则审计仅在持久化仓储可用时开放', 503)
    return send(res, 200, workspaceId, await repository.listAudit(workspaceId, url.searchParams.get('pack_id')?.trim() || undefined), null, req)
  }
  const createRuleVersionMatch = path.match(/^\/v1\/rules\/([^/]+)\/versions$/)
  if (req.method === 'POST' && createRuleVersionMatch) {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const principal = requireRuleAdmin(req)
    const repository = ruleRepository()
    if (!repository) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '规则写入仅允许使用持久化仓储', 503)
    const packId = decodeURIComponent(createRuleVersionMatch[1]!)
    if (!/^[A-Za-z0-9._:-]{1,128}$/.test(packId)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则包 ID 格式无效', 400)
    if (input.pack_id !== undefined && String(input.pack_id) !== packId) throw new DomainError(ERROR_CODES.WORKSPACE_SCOPE_MISMATCH, '路径规则包与请求体不一致', 403)
    const name = required(input, 'name')
    const versionValue = required(input, 'version')
    const scope = required(input, 'scope')
    const sourceKind = required(input, 'source_kind')
    const sourceReference = required(input, 'source_reference')
    const sourceCheckedAt = required(input, 'source_checked_at')
    const reason = required(input, 'reason')
    const status = typeof input.status === 'string' ? input.status : 'draft'
    if (!['global', 'platform', 'category', 'brand', 'store', 'campaign'].includes(scope)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则 scope 无效', 400)
    if (!['official', 'internal', 'legal_review'].includes(sourceKind)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则 source_kind 无效', 400)
    if (!['draft', 'active'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '新规则状态仅支持 draft 或 active', 400)
    if (!Number.isFinite(Date.parse(sourceCheckedAt))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'source_checked_at 必须是合法时间', 400)
    const checks = objectField(input, 'checks')
    const effectiveFromRaw = typeof input.effective_from === 'string' && input.effective_from.trim() ? input.effective_from.trim() : undefined
    const effectiveToRaw = typeof input.effective_to === 'string' && input.effective_to.trim() ? input.effective_to.trim() : undefined
    if ((effectiveFromRaw && Number.isNaN(Date.parse(effectiveFromRaw))) || (effectiveToRaw && Number.isNaN(Date.parse(effectiveToRaw))) || (effectiveFromRaw && effectiveToRaw && Date.parse(effectiveFromRaw) >= Date.parse(effectiveToRaw))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则有效期必须是合法时间，且 effective_from 早于 effective_to', 400)
    const effectiveFrom = effectiveFromRaw ? new Date(effectiveFromRaw).toISOString() : undefined
    const effectiveTo = effectiveToRaw ? new Date(effectiveToRaw).toISOString() : undefined
    const severity = input.severity === 'warning' ? 'warning' : input.severity === 'error' || input.severity === undefined ? 'error' : undefined
    const action = ['block', 'warn', 'review', 'allow'].includes(String(input.action)) ? String(input.action) : input.action === undefined ? 'block' : undefined
    if (!severity || !action) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则 severity/action 无效', 400)
    const checksum = createHash('sha256').update(canonicalJson(checks)).digest('hex')
    if (typeof input.checksum === 'string' && input.checksum.toLowerCase() !== checksum) throw new DomainError('RULE_CHECKSUM_MISMATCH', '规则校验和与 checks 内容不一致', 409)
    const approval = status === 'active' ? parseApprovalGrant(req, workspaceId, principal.actorId, input) : undefined
    const now = new Date().toISOString()
    await persistence.ensureWorkspace?.(workspaceId)
    const versionInput = {
      id: `rule_${randomBytes(12).toString('hex')}`, workspaceId, packId, name, version: versionValue, scope, status,
      sourceKind, sourceReference, sourceCheckedAt: new Date(sourceCheckedAt).toISOString(), checksum, checks,
      createdBy: principal.actorId, revision: 1, createdAt: now, updatedAt: now, severity, action,
      ...(effectiveFrom ? { effectiveFrom } : {}), ...(effectiveTo ? { effectiveTo } : {}),
      ...(typeof input.target_id === 'string' && input.target_id.trim() ? { targetId: input.target_id.trim() } : {}), ...(typeof input.scope_value === 'string' && input.scope_value.trim() ? { scopeValue: input.scope_value.trim() } : {}),
      ...(status === 'active' ? { activatedAt: now } : {}),
    }
    if (repository.insertVersionWithAudit) {
      try {
        const result = await repository.insertVersionWithAudit({ version: versionInput, audit: { id: `rule_audit_${randomBytes(12).toString('hex')}`, workspaceId, rulePackId: packId, ruleVersionId: versionInput.id, version: versionValue, action: status === 'active' ? 'activated' : 'created', actorId: principal.actorId, reason, occurredAt: now, data: { source_kind: sourceKind, source_reference: sourceReference, checksum, ...(approval ? { approval } : {}) } } })
        return send(res, 201, workspaceId, result, null, req)
      } catch (error) {
        if ((error as { code?: string }).code === '23505') throw new DomainError('RULE_VERSION_CONFLICT', '规则版本已存在，或该规则包已有激活版本', 409)
        throw error
      }
    }
    let created: PersistedRuleVersion
    try {
      created = await repository.insertVersion(versionInput)
    } catch (error) {
      if ((error as { code?: string }).code === '23505') throw new DomainError('RULE_VERSION_CONFLICT', '规则版本已存在，或该规则包已有激活版本', 409)
      throw error
    }
    let audit: PersistedRuleAudit
    try {
      audit = await repository.appendAudit({
        id: `rule_audit_${randomBytes(12).toString('hex')}`, workspaceId, rulePackId: packId, ruleVersionId: created.id,
        version: created.version, action: status === 'active' ? 'activated' : 'created', actorId: principal.actorId,
        reason, occurredAt: now, data: { source_kind: sourceKind, source_reference: sourceReference, checksum, ...(approval ? { approval } : {}) },
      })
    } catch {
      // The current repository contract exposes separate transactions. Surface
      // the partial write explicitly so operators can repair it; never report
      // success for an unaudited administrative mutation.
      throw new DomainError('RULE_AUDIT_WRITE_FAILED', '规则版本已写入，但审计追加失败；需暂停该工作区规则变更并人工核对', 503)
    }
    return send(res, 201, workspaceId, { version: publicRule(created), audit }, null, req)
  }
  const ruleStatusMatch = path.match(/^\/v1\/rules\/([^/]+)\/versions\/([^/]+)\/status$/)
  if (req.method === 'POST' && ruleStatusMatch) {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const principal = requireRuleAdmin(req)
    const repository = ruleRepository()
    if (!repository) throw new DomainError('RULE_REPOSITORY_NOT_CONFIGURED', '规则写入仅允许使用持久化仓储', 503)
    const packId = decodeURIComponent(ruleStatusMatch[1]!)
    const versionName = decodeURIComponent(ruleStatusMatch[2]!)
    const status = required(input, 'status')
    const reason = required(input, 'reason')
    if (!['active', 'inactive', 'expired'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '规则状态无效', 400)
    const rows = await repository.list(workspaceId, packId)
    const target = rows.find(row => row.version === versionName)
    if (!target) throw new DomainError('RULE_VERSION_NOT_FOUND', '规则版本不存在', 404)
    const approval = status === 'active' ? parseApprovalGrant(req, workspaceId, principal.actorId, input) : undefined
    const at = new Date().toISOString()
    const current = rows.find(row => row.status === 'active' && row.id !== target.id)
    if (status === 'active' && current && !repository.transitionStatusWithAudit) throw new DomainError('RULE_ACTIVE_VERSION_EXISTS', '该规则包已有激活版本，请先显式停用后再激活新版本', 409)
    if (repository.transitionStatusWithAudit) {
      try {
        const result = await repository.transitionStatusWithAudit({ workspaceId, packId, targetId: target.id, status, actorId: principal.actorId, reason, occurredAt: at, targetAuditId: `rule_audit_${randomBytes(12).toString('hex')}`, ...(current ? { currentAuditId: `rule_audit_${randomBytes(12).toString('hex')}` } : {}), auditData: approval ? { approval_ref: approval.approvalRef, approved_by: approval.approvedBy, approved_at: approval.approvedAt } : {} })
        return send(res, 200, workspaceId, publicRule(result.version), null, req)
      } catch (error) {
        if ((error as { code?: string }).code === '23505') throw new DomainError('RULE_ACTIVE_VERSION_EXISTS', '该规则包已有激活版本', 409)
        throw error
      }
    }
    let updated: PersistedRuleVersion
    try {
      updated = await repository.updateStatus({ workspaceId, id: target.id, status, revision: target.revision + 1, updatedAt: at, activatedAt: status === 'active' ? at : null, deactivatedAt: status === 'active' ? null : at })
    } catch (error) {
      if ((error as { code?: string }).code === '23505') throw new DomainError('RULE_ACTIVE_VERSION_EXISTS', '该规则包已有激活版本', 409)
      throw error
    }
    try {
      await repository.appendAudit({ id: `rule_audit_${randomBytes(12).toString('hex')}`, workspaceId, rulePackId: packId, ruleVersionId: updated.id, version: updated.version, action: status === 'active' ? 'activated' : status === 'expired' ? 'expired' : 'deactivated', actorId: principal.actorId, reason, occurredAt: at, data: approval ? { approval_ref: approval.approvalRef, approved_by: approval.approvedBy, approved_at: approval.approvedAt } : {} })
    } catch {
      throw new DomainError('RULE_AUDIT_WRITE_FAILED', '规则状态已变更，但审计追加失败；需暂停该工作区规则变更并人工核对', 503)
    }
    return send(res, 200, workspaceId, publicRule(updated), null, req)
  }
  if (req.method === 'GET' && path === '/v1/brand-profile') {
    const workspaceId = resolveWorkspace(req)
    await enforceBrandProfileHttpAccess(req, workspaceId)
    return send(res, 200, workspaceId, { profile: service.getBrandProfile(workspaceId) ?? null }, null, req)
  }
  if (req.method === 'POST' && path === '/v1/brand-profile/extract') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    await enforceBrandProfileHttpAccess(req, workspaceId)
    const assetIds = input.asset_ids === undefined ? undefined : Array.isArray(input.asset_ids) && input.asset_ids.length > 0 && input.asset_ids.length <= 50 && input.asset_ids.every(value => typeof value === 'string' && value.trim()) ? input.asset_ids.map(value => String(value).trim()) : null
    if (assetIds === null) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'asset_ids 必须是 1～50 个素材 ID 的字符串数组', 400)
    return send(res, 200, workspaceId, service.extractBrandProfile(workspaceId, assetIds), null, req)
  }
  if (req.method === 'PUT' && path === '/v1/brand-profile') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    await enforceBrandProfileHttpAccess(req, workspaceId, true)
    const tone = Array.isArray(input.tone) ? input.tone.filter((value): value is string => typeof value === 'string') : undefined
    const forbiddenTerms = Array.isArray(input.forbidden_terms) ? input.forbidden_terms.filter((value): value is string => typeof value === 'string') : undefined
    const details = isObject(input.details) ? input.details : undefined
    const visualRules = isObject(input.visual_rules) ? input.visual_rules as unknown as BrandVisualRules : undefined
    if (input.visual_rules !== undefined && !visualRules) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'visual_rules 必须是对象', 400)
    const resolutions = isObject(input.conflict_resolutions) && Object.values(input.conflict_resolutions).every(value => value === 'existing' || value === 'candidate') ? input.conflict_resolutions as Record<string, 'existing' | 'candidate'> : undefined
    if (input.conflict_resolutions !== undefined && !resolutions) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'conflict_resolutions 必须是字段到 existing/candidate 的对象', 400)
    const profile = service.upsertBrandProfile({ workspaceId, name: required(input, 'name'), ...(typeof input.positioning === 'string' ? { positioning: input.positioning } : {}), ...(typeof input.audience === 'string' ? { audience: input.audience } : {}), ...(tone ? { tone } : {}), ...(forbiddenTerms ? { forbiddenTerms } : {}), ...(details ? { details } : {}), ...(visualRules ? { visualRules } : {}), ...(typeof input.source === 'string' ? { source: input.source } : {}), ...(resolutions ? { resolutions } : {}) })
    await persistSnapshot(workspaceId, 'brand_profile', profile, profile as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, profile.id, 'brand_profile.updated', profile.revision, { brand_profile_id: profile.id, revision: profile.revision, source: typeof input.source === 'string' ? input.source : 'merchant_studio' })
    return send(res, 200, workspaceId, profile, null, req)
  }
  if (req.method === 'GET' && path === '/v1/assets') {
    const workspaceId = resolveWorkspace(req)
    const accessibleIds = await accessibleAssetIds(req, workspaceId)
    const quotaSnapshot = await persistence.storageQuota?.getSnapshot(workspaceId)
    const storageQuota = quotaSnapshot
      ? (() => {
          const usedBytes = Math.max(0, quotaSnapshot.usedBytes)
          const reservedBytes = Math.max(0, quotaSnapshot.reservedBytes)
          const limitBytes = Math.max(0, quotaSnapshot.limitBytes)
          const projectedBytes = usedBytes + reservedBytes
          const utilization = limitBytes > 0 ? projectedBytes / limitBytes : 1
          return { usedBytes, reservedBytes, limitBytes, availableBytes: Math.max(0, limitBytes - projectedBytes), status: projectedBytes > limitBytes ? 'over_limit' as const : utilization >= 0.8 ? 'near_limit' as const : 'available' as const }
        })()
      : undefined
    const rawLimit = url.searchParams.get('limit')
    const rawOffset = url.searchParams.get('offset')
    if (rawLimit !== null || rawOffset !== null) {
      const limit = rawLimit === null ? undefined : Number(rawLimit)
      const offset = rawOffset === null ? undefined : Number(rawOffset)
      if ((limit !== undefined && (!Number.isSafeInteger(limit) || limit < 1 || limit > 100)) || (offset !== undefined && (!Number.isSafeInteger(offset) || offset < 0))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '素材分页参数无效：limit 必须为 1-100，offset 必须为非负整数', 400)
      const all = service.listAssets(workspaceId).filter(asset => accessibleIds === undefined || accessibleIds.has(asset.id))
      return send(res, 200, workspaceId, { items: all.slice(offset ?? 0, (offset ?? 0) + (limit ?? 20)).map(asset => ({ ...asset, display: assetDisplayProjection(asset) })), total: all.length, limit: limit ?? 20, offset: offset ?? 0, ...(storageQuota ? { storage_quota: storageQuota } : {}) }, null, req)
    }
    const assets = service.listAssets(workspaceId)
    const visible = accessibleIds === undefined ? assets : assets.filter(asset => accessibleIds.has(asset.id))
    return send(res, 200, workspaceId, visible.map(asset => ({ ...asset, display: assetDisplayProjection(asset) })), null, req)
  }
  const assetPreferenceMatch = path.match(/^\/v1\/assets\/([^/]+)\/preference$/)
  if (req.method === 'PUT' && assetPreferenceMatch) {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const verdict = required(input, 'verdict')
    if (!['excellent', 'disliked', 'unrated'].includes(verdict)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'verdict 必须是 excellent、disliked 或 unrated', 400)
    const reasons = input.reasons === undefined ? undefined : Array.isArray(input.reasons) && input.reasons.every(reason => typeof reason === 'string') ? input.reasons as string[] : null
    if (reasons === null) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'reasons 必须是字符串数组', 400)
    const assetId = decodeURIComponent(assetPreferenceMatch[1]!)
    await enforceAssetAccess(req, workspaceId, assetId, 'editor')
    const updated = service.updateAssetPreference({ workspaceId, assetId, verdict: verdict as 'excellent' | 'disliked' | 'unrated', ...(reasons ? { reasons } : {}), ...(typeof input.note === 'string' ? { note: input.note } : {}), actorId: requestActor(req), ...(typeof input.expected_revision === 'number' ? { expectedRevision: input.expected_revision } : {}) })
    await persistSnapshot(workspaceId, 'asset', updated, updated as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, updated.id, 'asset.preference_updated', updated.revision, { asset_id: updated.id, verdict, reasons: updated.preference?.reasons ?? [], actor_id: updated.preference?.updatedBy ?? requestActor(req) })
    return send(res, 200, workspaceId, updated, null, req)
  }
  const assetDownloadMatch = path.match(/^\/v1\/assets\/([^/]+)\/download$/)
  if (req.method === 'GET' && assetDownloadMatch) {
    const workspaceId = resolveWorkspace(req)
    const asset = assetForWorkspace(workspaceId, assetDownloadMatch[1]!)
    await enforceAssetAccess(req, workspaceId, asset.id)
    if (!isTrustedCleanAsset(asset)) throw new DomainError('QUARANTINE_ACCESS_DENIED', '素材尚未通过可信安全扫描，暂不可下载', 403)
    let stored: Awaited<ReturnType<typeof getStoredObjectWithRetry>>
    try {
      stored = await getStoredObjectWithRetry(workspaceId, asset.storageKey)
    } catch (error) {
      if (error instanceof ObjectStorageError && error.code === 'OBJECT_NOT_FOUND') {
        throw new DomainError('ASSET_BINARY_UNAVAILABLE', '素材文件不可用，请重新上传', 410)
      }
      throw error
    }
    const storedDigest = createHash('sha256').update(stored.body).digest('hex')
    if (stored.metadata.sha256 !== asset.sha256 || stored.metadata.sizeBytes !== asset.sizeBytes || stored.metadata.contentType.toLowerCase() !== asset.mimeType.toLowerCase() || storedDigest !== asset.sha256) {
      throw new DomainError('ASSET_BINARY_INTEGRITY_FAILED', '素材对象与已扫描快照不一致，已阻止下载', 409, { asset_id: asset.id })
    }
    return sendAssetDownload(res, asset, stored, req)
  }
  const assetScanContentMatch = path.match(/^\/v1\/internal\/assets\/([^/]+)\/scan-content$/)
  if (req.method === 'GET' && assetScanContentMatch) {
    // Scanner routes have a separate machine identity and signed workspace
    // binding. They must not fall back through the merchant membership gate.
    const workspaceId = assetScannerWorkspace(req)
    const asset = assetForWorkspace(workspaceId, decodeURIComponent(assetScanContentMatch[1]!))
    if (asset.scanStatus !== 'quarantined' || !asset.storageKey.startsWith(`quarantine/${workspaceId}/`)) throw new DomainError('ASSET_SCAN_STATE_INVALID', 'asset is not awaiting an automatic platform scan', 409)
    const stored = await getStoredObjectWithRetry(workspaceId, asset.storageKey, { includeQuarantine: true })
    if (stored.metadata.sha256 !== asset.sha256 || stored.metadata.sizeBytes !== asset.sizeBytes || stored.metadata.contentType.toLowerCase() !== asset.mimeType.toLowerCase()) throw new DomainError('ASSET_SCAN_SOURCE_INTEGRITY_FAILED', 'quarantined scan source no longer matches asset metadata', 409)
    res.statusCode = 200
    res.setHeader('content-type', asset.mimeType)
    res.setHeader('content-length', String(stored.body.byteLength))
    res.setHeader('cache-control', 'no-store')
    res.setHeader('x-asset-sha256', asset.sha256)
    res.setHeader('x-asset-source-revision', String(asset.sourceRevision ?? 1))
    res.setHeader('x-asset-object-key', encodeURIComponent(asset.storageKey))
    res.end(Buffer.from(stored.body))
    return
  }
  const assetScanResultMatch = path.match(/^\/v1\/internal\/assets\/([^/]+)\/scan-result$/)
  if (req.method === 'POST' && assetScanResultMatch) {
    const workspaceId = assetScannerWorkspace(req)
    const input = await body(req)
    if (!input.receipt || typeof input.signature !== 'string') throw new DomainError('ASSET_SCAN_RECEIPT_INVALID', 'signed asset scan receipt is required', 400)
    const asset = assetForWorkspace(workspaceId, decodeURIComponent(assetScanResultMatch[1]!))
    const result = await applySignedAssetScanResult(workspaceId, asset, { receipt: input.receipt as SignedAssetScanReceipt['receipt'], signature: input.signature })
    return send(res, 200, workspaceId, { asset_id: result.id, scan_status: result.scanStatus, receipt_id: result.scanReceiptId, receipt_digest: result.scanReceiptDigest }, null, req)
  }
  const assetScanMatch = path.match(/^\/v1\/assets\/([^/]+)\/scan$/)
  if (req.method === 'POST' && assetScanMatch) {
    if (isProduction()) throw new DomainError('LEGACY_ASSET_SCAN_DISABLED', 'production asset scans are completed only by the platform scanner', 410)
    const workspaceId = resolveWorkspace(req)
    const input = await body(req)
    const asset = assetForWorkspace(workspaceId, assetScanMatch[1]!)
    await enforceAssetAccess(req, workspaceId, asset.id, 'editor')
    if (asset.scanStatus !== 'quarantined' || !asset.storageKey.startsWith('quarantine/')) {
      throw new DomainError('ASSET_SCAN_STATE_INVALID', '素材当前不在待扫描隔离状态', 409)
    }
    const evidence = required(input, 'scan_evidence_ref')
    return send(res, 200, workspaceId, await promoteAssetAndPersist(workspaceId, asset, evidence), null, req)
  }
  const assetParseMatch = path.match(/^\/v1\/assets\/([^/]+)\/parse$/)
  if (req.method === 'POST' && assetParseMatch) {
    const workspaceId = resolveWorkspace(req)
    await enforceAssetAccess(req, workspaceId, decodeURIComponent(assetParseMatch[1]!), 'editor')
    return send(res, 200, workspaceId, await executeDurableAssetParse(workspaceId, assetParseMatch[1]!, req), null, req)
  }
  if (req.method === 'POST' && path === '/v1/assets/upload') {
    const workspaceId = resolveWorkspace(req)
    const limit = configuredAssetLimit()
    const contentType = headerRequired(req, 'content-type').trim().toLowerCase()
    const encodedName = headerRequired(req, 'x-asset-name')
    let name = encodedName
    try { name = decodeURIComponent(encodedName) } catch { /* Preserve legacy raw header names. */ }
    const expectedSha256 = header(req, 'x-asset-sha256')?.trim()
    const bytes = await binaryBody(req, limit)
    await requireAssetUploadSecurity(workspaceId, name, contentType, bytes, req)
    const actualSha256 = createHash('sha256').update(bytes).digest('hex')
    if (expectedSha256 && !/^[a-f0-9]{64}$/iu.test(expectedSha256)) throw new DomainError('ASSET_DIGEST_INVALID', 'x-asset-sha256 必须是 SHA-256 摘要', 400)
    if (expectedSha256 && expectedSha256.toLowerCase() !== actualSha256) throw new DomainError('ASSET_DIGEST_MISMATCH', 'x-asset-sha256 与上传内容不一致', 400)
    const pendingKey = `quarantine/${workspaceId}/pending_${randomBytes(12).toString('hex')}/upload.bin`
    const provisional = service.registerAsset({ workspaceId, name, mimeType: contentType, sizeBytes: bytes.byteLength, sha256: actualSha256, storageKey: pendingKey, uploadedByActorId: requestActor(req) })
    if (provisional.deduplication.mode === 'deduplicated') {
      await persistAssetReference(workspaceId, provisional)
      if (isTrustedCleanAsset(provisional)) return send(res, 200, workspaceId, provisional, null, req)
      // Re-upload is the merchant-facing recovery action for both blocked
      // assets and quarantined assets whose earlier scan event terminated.
      // Always mint new scan work for a non-trusted duplicate.
      {
        const previousAsset = structuredClone(provisional)
        const stored = await putQuarantineObject({ workspaceId, assetId: provisional.id, fileName: name, contentType, body: bytes, ...(expectedSha256 ? { expectedSha256 } : {}), expectedSizeBytes: bytes.byteLength })
        try {
          const rescanning = service.prepareAssetRescan({ workspaceId, assetId: provisional.id, storageKey: stored.key, sizeBytes: stored.sizeBytes, sha256: stored.sha256, mimeType: contentType })
          await persistAssetSnapshotAndEvent(workspaceId, rescanning, 'asset.uploaded', { asset_id: rescanning.id, storage_key: stored.key, size_bytes: stored.sizeBytes, sha256: stored.sha256, rescan: true, source_revision: rescanning.sourceRevision }, rescanning as unknown as Record<string, unknown>)
          const automated = await automaticallyScanLocalFixture(workspaceId, rescanning)
          return send(res, 200, workspaceId, { ...automated.asset, scanAutomation: automated.scanAutomation }, null, req)
        } catch (error) {
          service.assets.set(previousAsset.id, previousAsset)
          await compensateStoredAsset(workspaceId, provisional.id, stored.key, 'duplicate asset rescan persistence failed')
          throw error
        }
      }
    }
    let storedKey: string | undefined
    try {
      const stored = await putQuarantineObject({ workspaceId, assetId: provisional.id, fileName: name, contentType, body: bytes, ...(expectedSha256 ? { expectedSha256 } : {}), expectedSizeBytes: bytes.byteLength })
      storedKey = stored.key
      provisional.storageKey = stored.key
      provisional.sha256 = stored.sha256
      provisional.sizeBytes = stored.sizeBytes
      await persistAssetSnapshotAndEvent(workspaceId, provisional, 'asset.uploaded', { asset_id: provisional.id, storage_key: stored.key, size_bytes: stored.sizeBytes, sha256: stored.sha256 }, provisional as unknown as Record<string, unknown>)
      const automated = await automaticallyScanLocalFixture(workspaceId, provisional)
      return send(res, 201, workspaceId, { ...automated.asset, scanAutomation: automated.scanAutomation }, null, req)
    } catch (error) {
      service.assets.delete(provisional.id)
      if (storedKey) await compensateStoredAsset(workspaceId, provisional.id, storedKey, 'asset snapshot or event persistence failed')
      throw error
    }
  }
  const assetRightsMatch = path.match(/^\/v1\/assets\/([^/]+)\/rights$/)
  if (req.method === 'PUT' && assetRightsMatch) {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const asset = assetForWorkspace(workspaceId, decodeURIComponent(assetRightsMatch[1]!))
    await enforceAssetAccess(req, workspaceId, asset.id, 'editor')
    const rightsStatus = required(input, 'rights_status')
    if (!['approved', 'rejected', 'pending'].includes(rightsStatus)) throw new DomainError('ASSET_RIGHTS_STATUS_INVALID', 'rights_status 无效', 400)
    const rightsScope = typeof input.rights_scope === 'string' ? input.rights_scope : undefined
    const applicablePlatforms = Array.isArray(input.applicable_platforms) && input.applicable_platforms.every(value => SUPPORTED_PLATFORMS.includes(String(value) as Platform)) ? input.applicable_platforms as Platform[] : input.applicable_platforms === undefined ? undefined : (() => { throw new DomainError('ASSET_PLATFORM_SCOPE_INVALID', 'applicable_platforms 必须是支持平台数组', 400) })()
    const stringArray = (key: string) => input[key] === undefined ? undefined : Array.isArray(input[key]) && input[key].every(value => typeof value === 'string') ? input[key] as string[] : (() => { throw new DomainError('ASSET_RIGHTS_INPUT_INVALID', `${key} 必须是字符串数组`, 400) })()
    const updated = service.updateAssetRights({ workspaceId, assetId: asset.id, rightsStatus: rightsStatus as 'approved' | 'rejected' | 'pending', ...(rightsScope ? { rightsScope: rightsScope as import('../../../packages/application/src/service.js').AssetMetadata['rightsScope'] } : {}), ...(applicablePlatforms ? { applicablePlatforms } : {}), ...((stringArray('applicable_regions')) ? { applicableRegions: stringArray('applicable_regions') } : {}), ...((stringArray('usage_scopes')) ? { usageScopes: stringArray('usage_scopes') } : {}), ...(typeof input.ai_modification_allowed === 'boolean' ? { aiModificationAllowed: input.ai_modification_allowed } : {}) })
    await persistSnapshot(workspaceId, 'asset', updated, updated as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, updated.id, 'asset.rights_updated', updated.revision, { asset_id: updated.id, rights_status: updated.rightsStatus })
    return send(res, 200, workspaceId, updated, null, req)
  }
  const assetFactsMatch = path.match(/^\/v1\/assets\/([^/]+)\/facts$/)
  if (req.method === 'POST' && assetFactsMatch) {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const assetId = decodeURIComponent(assetFactsMatch[1]!)
    await enforceAssetAccess(req, workspaceId, assetId, 'editor')
    if (!isObject(input.facts) || Object.keys(input.facts).length === 0) throw new DomainError('ASSET_FACTS_EMPTY', 'facts 必须是非空对象', 400)
    const reason = required(input, 'reason').trim()
    if (!reason) throw new DomainError('ASSET_FACTS_REASON_REQUIRED', '人工确认原因不能为空', 400)
    const confirmed = await confirmDurableAssetFacts({ workspaceId, assetId, facts: input.facts, reason, req })
    return send(res, 200, workspaceId, confirmed, null, req)
  }
  if (req.method === 'POST' && path === '/v1/assets') {
    if (isProduction()) throw new DomainError('ASSET_BINARY_UPLOAD_REQUIRED', '生产环境必须通过二进制上传与扫描流程登记素材', 409)
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const asset = service.registerAsset({ workspaceId, name: required(input, 'name'), mimeType: required(input, 'mime_type'), sizeBytes: typeof input.size_bytes === 'number' ? input.size_bytes : -1, sha256: required(input, 'sha256'), storageKey: required(input, 'storage_key'), rightsStatus: input.rights_status === 'approved' || input.rights_status === 'rejected' ? input.rights_status : 'pending', uploadedByActorId: requestActor(req) })
    await persistAssetSnapshotAndEvent(workspaceId, asset, 'asset.registered', { asset_id: asset.id, storage_key: asset.storageKey, size_bytes: asset.sizeBytes, sha256: asset.sha256, fixture: true }, asset as unknown as Record<string, unknown>)
    return send(res, 201, workspaceId, asset, null, req)
  }
  if (req.method === 'POST' && path === '/v1/products/import/batch') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    if (!Array.isArray(input.products) || input.products.length < 1 || input.products.length > 50 || input.products.some(item => !item || typeof item !== 'object' || Array.isArray(item))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'products 必须是 1 至 50 个商品对象的数组', 400)
    type RestBatchItem = Parameters<MerchantService['importProduct']>[0]
    const items: RestBatchItem[] = input.products.map((raw: Record<string, unknown>, index: number) => {
      const platform = typeof raw.platform === 'string' ? raw.platform as Platform : '' as Platform
      if (!SUPPORTED_PLATFORMS.includes(platform)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 platform 无效`, 400)
      const accountId = typeof raw.account_id === 'string' && raw.account_id.trim() ? raw.account_id.trim() : undefined
      if (isProduction() && !accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', `第 ${index + 1} 项生产导入必须绑定已授权平台账号`, 400)
      if (accountId) service.getActivePlatformAccount(workspaceId, accountId, platform)
      const title = typeof raw.title === 'string' ? raw.title.trim() : ''
      if (!title) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 title 不能为空`, 400)
      const sourceAssetIds = Array.isArray(raw.asset_ids) ? raw.asset_ids.filter((value): value is string => typeof value === 'string' && value.trim().length > 0).map(value => value.trim()) : undefined
      if (sourceAssetIds && (sourceAssetIds.length > 50 || new Set(sourceAssetIds).size !== sourceAssetIds.length)) throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 asset_ids 必须是最多 50 个不重复素材 ID`, 400)
      const skus = Array.isArray(raw.skus) ? raw.skus.map((sku: any, skuIndex: number) => {
        if (!sku || typeof sku !== 'object' || typeof sku.id !== 'string' || typeof sku.name !== 'string' || typeof sku.price !== 'number' || typeof sku.stock !== 'number') throw new DomainError('PRODUCT_IMPORT_BATCH_INVALID', `第 ${index + 1} 项 SKU ${skuIndex + 1} 格式无效`, 400)
        return { id: sku.id.trim(), name: sku.name.trim(), price: sku.price, stock: sku.stock, ...(Array.isArray(sku.images) ? { images: sku.images.filter((value: unknown): value is string => typeof value === 'string') } : {}), ...(sku.attributes && typeof sku.attributes === 'object' && !Array.isArray(sku.attributes) ? { attributes: Object.fromEntries(Object.entries(sku.attributes).filter(([, value]) => typeof value === 'string').map(([key, value]) => [key, value as string])) } : {}) }
      }) : undefined
      const sellingPoints = Array.isArray(raw.selling_points) ? raw.selling_points.map((point: any, pointIndex: number) => ({ id: typeof point?.id === 'string' ? point.id : `sp_${pointIndex + 1}`, text: typeof point?.text === 'string' ? point.text : '', proofStatus: point?.proof_status === 'confirmed' || point?.proof_status === 'rejected' ? point.proof_status : 'pending', sourceIds: Array.isArray(point?.source_ids) ? point.source_ids.filter((value: unknown): value is string => typeof value === 'string') : [] })) : undefined
      return { workspaceId, platform, ...(accountId ? { accountId } : {}), ...(typeof raw.remote_id === 'string' && raw.remote_id.trim() ? { remoteId: raw.remote_id.trim() } : {}), ...(typeof raw.local_product_key === 'string' ? { localProductKey: raw.local_product_key } : {}), title, ...(typeof raw.sku_count === 'number' ? { skuCount: raw.sku_count } : {}), ...(skus ? { skus } : {}), ...(typeof raw.stock === 'number' ? { stock: raw.stock } : {}), ...(typeof raw.price === 'number' ? { price: raw.price } : {}), ...(typeof raw.category === 'string' ? { category: raw.category } : {}), ...(Array.isArray(raw.images) ? { images: raw.images.filter((value): value is string => typeof value === 'string') } : {}), ...(sourceAssetIds ? { sourceAssetIds } : {}), ...(raw.attributes && typeof raw.attributes === 'object' && !Array.isArray(raw.attributes) ? { attributes: Object.fromEntries(Object.entries(raw.attributes).filter(([, value]) => typeof value === 'string').map(([key, value]) => [key, value as string])) } : {}), ...(sellingPoints ? { sellingPoints } : {}), ...(typeof raw.store_name === 'string' ? { storeName: raw.store_name } : {}), ...(typeof raw.store_differentiation === 'string' ? { storeDifferentiation: raw.store_differentiation } : {}) }
    })
    assertUniqueBatchProductImportIdentities(items)
    const beforeProducts = new Map([...service.products.entries()].filter(([, product]) => product.workspaceId === workspaceId).map(([id, product]) => [id, structuredClone(product)] as const))
    const created: ReturnType<typeof service.importProduct>[] = []
    const writes: BatchProductWrite[] = []
    try {
      for (const item of items) {
        const product = service.importProduct(item)
        created.push(product)
        writes.push({ product, version: product.version ?? 0 })
      }
      const batchId = `catalog_import_batch_${randomUUID()}`
      await persistSnapshotsAndEvent({ workspaceId, snapshots: created.map(product => ({ entityType: 'product' as const, entityId: product.id, entityVersion: product.version ?? 1, payload: product as unknown as Record<string, unknown> })), aggregateId: batchId, eventType: 'catalog.import.batch.completed', sequence: 1, eventPayload: { batch_id: batchId, count: created.length, product_ids: created.map(product => product.id), transport: 'rest' } })
      await recordOperationAudit({ workspaceId, actorId: requestActor(req), action: 'catalog.import.batch', resourceType: 'product_import_batch', resourceId: batchId, before: {}, after: { count: created.length, product_ids: created.map(product => product.id), atomic: true, transport: 'rest' }, reason: '批量导入商品并建立持久化快照' })
      return send(res, 201, workspaceId, { batchId, count: created.length, products: created, atomic: true, factsConfirmationRequired: true }, null, req)
    } catch (error) {
      rollbackBatchProducts(service.products, workspaceId, writes, beforeProducts)
      throw error
    }
  }
  if (req.method === 'POST' && path === '/v1/products/import') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const rawSkus = Array.isArray(input.skus) ? input.skus : undefined
    const skus = rawSkus?.map((item: any) => {
      if (!item || typeof item !== 'object' || typeof item.id !== 'string' || typeof item.name !== 'string' || typeof item.price !== 'number' || typeof item.stock !== 'number') throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'skus 必须包含 id、name、price、stock', 400)
      return { id: item.id.trim(), name: item.name.trim(), price: item.price, stock: item.stock, ...(Array.isArray(item.images) ? { images: item.images.filter((value: unknown): value is string => typeof value === 'string') } : {}) }
    })
    const rawSellingPoints = Array.isArray(input.selling_points) ? input.selling_points : undefined
    const sellingPoints = rawSellingPoints?.map((item: any, index: number) => ({ id: typeof item.id === 'string' ? item.id : `sp_${index + 1}`, text: typeof item.text === 'string' ? item.text : '', proofStatus: item.proof_status === 'confirmed' || item.proof_status === 'rejected' ? item.proof_status : 'pending', sourceIds: Array.isArray(item.source_ids) ? item.source_ids.filter((value: unknown): value is string => typeof value === 'string') : [] }))
    const rawAssetIds = input.asset_ids
    const sourceAssetIds = rawAssetIds === undefined ? undefined : Array.isArray(rawAssetIds) && rawAssetIds.length > 0 && rawAssetIds.length <= 50 && rawAssetIds.every(value => typeof value === 'string' && value.trim()) ? [...new Set(rawAssetIds.map(value => String(value).trim()))] : null
    if (sourceAssetIds === null || (sourceAssetIds && Array.isArray(rawAssetIds) && sourceAssetIds.length !== rawAssetIds.length)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'asset_ids 必须是最多 50 个不重复素材 ID 的数组', 400)
    const platform = required(input, 'platform') as Platform
    const accountId = typeof input.account_id === 'string' && input.account_id.trim() ? input.account_id.trim() : undefined
    if (!SUPPORTED_PLATFORMS.includes(platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 无效', 400)
    if (isProduction() && !accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产商品导入必须绑定已授权平台账号', 400)
    if (accountId) service.getActivePlatformAccount(workspaceId, accountId, platform)
    const product = service.importProduct({ workspaceId, platform, ...(accountId ? { accountId } : {}), ...(typeof input.remote_id === 'string' && input.remote_id.trim() ? { remoteId: input.remote_id } : {}), ...(typeof input.local_product_key === 'string' ? { localProductKey: input.local_product_key } : {}), title: required(input, 'title'), skuCount: typeof input.sku_count === 'number' ? input.sku_count : undefined, ...(skus ? { skus } : {}), stock: typeof input.stock === 'number' ? input.stock : undefined, price: typeof input.price === 'number' ? input.price : undefined, category: typeof input.category === 'string' ? input.category : undefined, images: Array.isArray(input.images) ? input.images.filter((item): item is string => typeof item === 'string') : undefined, ...(sourceAssetIds ? { sourceAssetIds } : {}), attributes: input.attributes && typeof input.attributes === 'object' && !Array.isArray(input.attributes) ? Object.fromEntries(Object.entries(input.attributes).filter(([, value]) => typeof value === 'string').map(([key, value]) => [key, value as string])) : undefined, ...(sellingPoints ? { sellingPoints } : {}), storeName: typeof input.store_name === 'string' ? input.store_name : undefined, storeDifferentiation: typeof input.store_differentiation === 'string' ? input.store_differentiation : undefined })
    await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
    return send(res, 201, workspaceId, product, null, req)
  }
  const productConfirmMatch = path.match(/^\/v1\/products\/([^/]+)\/confirm$/)
  if (req.method === 'POST' && productConfirmMatch) {
    const workspaceId = resolveWorkspace(req)
    const productId = decodeURIComponent(productConfirmMatch[1]!)
    await enforceProductBrandAccess(req, workspaceId, productId)
    const product = service.confirmProductFacts(workspaceId, productId)
    await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
    return send(res, 200, workspaceId, product, null, req)
  }
  if (req.method === 'GET' && path === '/v1/platform-accounts') {
    const workspaceId = resolveWorkspace(req)
    return send(res, 200, workspaceId, { items: platformAccountAccessItems(workspaceId) }, null, req)
  }

  const authorizeMatch = path.match(/^\/v1\/platform-accounts\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)\/authorize$/)
  if (req.method === 'POST' && authorizeMatch) {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const actorId = requestActor(req, typeof input.actor_id === 'string' && input.actor_id.trim() ? input.actor_id.trim() : 'actor_demo')
    const codeVerifier = randomBytes(32).toString('base64url')
    const state = await oauthStateStore.issue({ workspaceId, actorId, platform: authorizeMatch[1]!, codeVerifier, codeChallenge: hashPkceVerifier(codeVerifier) })
    const platform = authorizeMatch[1] as Platform
    const configuredRedirectUri = configuredOAuthRedirectUri(platform)
    const requestedRedirectUri = typeof input.redirect_uri === 'string' ? input.redirect_uri.trim() : undefined
    const redirectUri = isProduction() ? configuredRedirectUri ?? '' : requestedRedirectUri ?? configuredRedirectUri ?? 'http://127.0.0.1:8787/oauth/callback'
    if (!redirectUri || (isProduction() && (!validOAuthRedirectUri(platform, redirectUri) || (requestedRedirectUri && requestedRedirectUri !== configuredRedirectUri)))) throw new DomainError('OAUTH_REDIRECT_URI_REQUIRED', `生产 OAuth 必须配置匹配 ${platform} 回调路由的 HTTPS ${oauthRedirectEnv[platform]}（或 PUBLIC_OAUTH_REDIRECT_URI 模板）`, 503)
    if (!platformAuthorizationConfigured(platform)) throw new DomainError('NOT_CONFIGURED', `${platform} 官方 OAuth 尚未配置`, 503)
    if (isProduction() && !redisOAuthPort) throw new DomainError('OAUTH_STATE_STORE_UNAVAILABLE', '生产 OAuth 必须配置 Redis 状态存储，禁止使用单副本内存状态', 503)
    const result = await connectorRuntime.connector(platform).authorize({ workspaceId, actorId, redirectUri, state, codeVerifier })
    if (!result.ok) throw new DomainError(result.code ?? 'NOT_CONFIGURED', result.message ?? '平台官方 API 尚未配置', 503)
    return send(res, 200, workspaceId, result, null, req)
  }

  const syncMatch = path.match(/^\/v1\/platform-accounts\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)\/sync$/)
  if (req.method === 'POST' && syncMatch) {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const platform = syncMatch[1] as Platform
    if (!platformConnectorConfigured(platform)) throw new DomainError('NOT_CONFIGURED', `${platform} 官方 API 尚未配置，无法同步商品`, 503)
    const accountId = header(req, 'x-account-id')?.trim() || (typeof input.account_id === 'string' && input.account_id.trim()) || (isProduction() ? '' : defaultFixtureAccountId(workspaceId, platform))
    if (!accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产同步必须指定已授权平台账号', 400)
    const platformAccount = isProduction() || fixtureMode ? requireActivePlatformAccount(workspaceId, accountId, platform) : undefined
    const result = await connectorRuntime.sync(platform, { workspaceId, accountId, ...(platformAccount ? { credentialRef: platformAccount.credentialRef } : {}), traceId: requestId(req) }, typeof input.cursor === 'string' ? input.cursor : undefined)
    const products = service.upsertSyncedProducts({ workspaceId, platform, accountId, items: result.items })
    await invalidateCanonicalFactsAfterSync(workspaceId, products)
    for (const product of products) await persistSnapshot(workspaceId, 'product', product, product as unknown as Record<string, unknown>)
    const automation = await scanAutomationAfterOperationalCompletion(workspaceId, platform, accountId, 'platform-account.sync.completed')
    return send(res, 200, workspaceId, { ...result, products, automation }, null, req)
  }

  const revokeMatch = path.match(/^\/v1\/platform-accounts\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)$/)
  if (req.method === 'DELETE' && revokeMatch) {
    const workspaceId = resolveWorkspace(req)
    const platform = revokeMatch[1] as Platform
    const accountId = header(req, 'x-account-id')?.trim() || new URL(req.url ?? '/', `http://${req.headers.host ?? 'localhost'}`).searchParams.get('account_id')?.trim()
    if (!accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '撤销授权必须指定平台账号', 400)
    const account = service.revokePlatformAccount(workspaceId, accountId, platform)
    await persistSnapshot(workspaceId, 'platform_account', account, account as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, account.id, 'platform_account.revoked', account.revision, { account_id: account.id, platform, remote_revoked: false })
    try {
      await connectorRuntime.connector(platform).revoke({ accountId: account.remoteAccountId, credentialRef: account.credentialRef })
    } catch (error) {
      throw new DomainError('PLATFORM_REVOKE_REMOTE_FAILED', error instanceof Error ? error.message : '平台远端凭证撤销失败，本地账号已停止使用', 503)
    }
    return send(res, 200, workspaceId, { platform, accountId: account.id, state: account.tokenState, remoteRevoked: true }, null, req)
  }

  const callbackMatch = path.match(/^\/v1\/oauth\/callback\/(jd|taobao|tmall|pinduoduo|xiaohongshu|douyin)$/)
  if (req.method === 'GET' && callbackMatch) {
    if (isProduction() && !redisOAuthPort) throw new DomainError('OAUTH_STATE_STORE_UNAVAILABLE', '生产 OAuth 必须配置 Redis 状态存储，禁止使用单副本内存状态', 503)
    const state = url.searchParams.get('state') ?? ''
    const code = url.searchParams.get('code') ?? ''
    if (!code) throw new DomainError(ERROR_CODES.OAUTH_CODE_REQUIRED, 'OAuth callback code is required', 400)
    const suppliedWorkspace = header(req, 'x-workspace-id')?.trim()
    const callback = suppliedWorkspace
      ? await oauthStateStore.consume(state, { workspaceId: suppliedWorkspace, platform: callbackMatch[1]! })
      : await oauthStateStore.consumeCallback(state, callbackMatch[1]!)
    const credential = await connectorRuntime.connector(callbackMatch[1] as Platform).exchangeCode({ code, state, workspaceId: callback.workspaceId, ...(callback.codeVerifier ? { codeVerifier: callback.codeVerifier } : {}) })
    // credentialRef/secret remains inside the connector/vault boundary.
    const account = service.registerPlatformAccount({
      workspaceId: callback.workspaceId,
      platform: callback.platform as Platform,
      remoteAccountId: credential.accountId,
      credentialRef: credential.credentialRef,
      grantedScopes: grantedScopes(credential.scope),
      accessTokenExpiresAt: credential.expiresAt,
      credentialRefreshable: credential.refreshable,
    })
    await persistSnapshot(callback.workspaceId, 'platform_account', account, account as unknown as Record<string, unknown>)
    let initialSync: Record<string, unknown> = { state: 'not_started', reason: 'connector_not_ready' }
    if (platformConnectorConfigured(callback.platform as Platform) && requiresStrictAuth()) {
      // The OAuth callback is authenticated by one-time state + PKCE, but it
      // is not a merchant authorization decision and must not mint a user
      // worker credential. The connected user can immediately start sync from
      // ChatGPT, where the normal MCP capability decision is captured.
      initialSync = { state: 'blocked', reason: 'service_principal_authorization_not_configured', fail_closed: true }
    } else if (platformConnectorConfigured(callback.platform as Platform)) {
      const job = service.createSyncJob({ workspaceId: callback.workspaceId, platform: callback.platform as Platform, accountId: account.id, mode: 'full' })
      await persistSnapshot(callback.workspaceId, 'sync_job', job, job as unknown as Record<string, unknown>)
      await persistEvent(callback.workspaceId, job.id, 'sync.requested', job.revision, { job_id: job.id, platform: callback.platform, account_id: account.id, mode: job.mode })
      initialSync = { state: 'queued', jobId: job.id }
    }
    const store = workspaceStoreDirectory(callback.workspaceId, callback.platform as Platform).find(item => item.accountId === account.id)
    if (wantsOAuthHtml(req)) return sendOAuthCallbackPage(res, 200, { state: 'success', platform: callback.platform, ...(store?.label ? { storeLabel: store.label } : {}), syncState: String(initialSync.state) }, req)
    return send(res, 200, callback.workspaceId, {
      platform: callback.platform,
      accountId: account.id,
      remoteAccountId: account.remoteAccountId,
      connected: true,
      tokenState: 'stored_in_vault',
      ...(store ? { store } : {}),
      initialSync,
      nextActions: [
        'refresh_workspace_health',
        'select_store_by_platform_and_account_id',
        ...(initialSync.state === 'queued' ? [] : ['start_catalog_sync_with_account_id']),
      ],
    }, null, req)
  }
  if (path === '/v1/sync-jobs' && req.method === 'POST') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const platform = String(input.platform ?? '') as Platform
    if (!SUPPORTED_PLATFORMS.includes(platform as Platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'platform 无效', 400)
    if (!platformConnectorConfigured(platform)) throw new DomainError('NOT_CONFIGURED', `${platform} 官方 API 尚未配置，无法同步商品`, 503)
    const accountId = (typeof input.account_id === 'string' && input.account_id.trim()) || header(req, 'x-account-id')?.trim() || (isProduction() ? '' : defaultFixtureAccountId(workspaceId, platform))
    if (!accountId) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产同步必须指定已授权平台账号', 400)
    if (isProduction()) service.getActivePlatformAccount(workspaceId, accountId, platform)
    // REST worker acceptance flows may enqueue before a worker starts. In the
    // local fixture, materialize the same logical account used by the MCP
    // path so normalized product rows satisfy their account foreign key.
    await ensureFixtureAccount(workspaceId, platform, accountId)
    const job = service.createSyncJob({ workspaceId, platform, accountId, mode: input.mode === 'full' ? 'full' : 'incremental', ...(typeof input.cursor === 'string' && input.cursor.trim() ? { cursor: input.cursor } : {}) })
    const authorizationSnapshot = workerAuthorizationSnapshot(req, workspaceId, job.id, 'catalog.sync.execute', { route: 'POST /v1/sync-jobs', platform, account_id: accountId, mode: job.mode, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}) })
  if (requiresStrictAuth() && !authorizationSnapshot) {
      service.removeSyncJob(workspaceId, job.id)
      throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', 'HTTP 商品同步入口尚未建立等价的持久授权决策，已拒绝入队', 503)
    }
    await persistSnapshot(workspaceId, 'sync_job', job, job as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, job.id, 'sync.requested', job.revision, { job_id: job.id, platform, account_id: accountId, mode: job.mode, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}), ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}) })
    return send(res, 202, workspaceId, job, null, req)
  }
  if (path === '/v1/ops/data-deletion/complete' && req.method === 'POST') {
    await requireWorkerAuthorization(req)
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const workerId = headerRequired(req, 'x-worker-id')
    const proofRef = typeof input.execution_proof_ref === 'string' ? input.execution_proof_ref.trim() : ''
    if (!proofRef || proofRef.length > 500 || /[\r\n]/u.test(proofRef)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'execution_proof_ref 必须是单行、非空的外部证据引用', 400)
    await persistenceReady
    const completed = await (persistence.dataLifecycle ?? memoryDataLifecycle).complete({ workspaceId, id: required(input, 'request_id'), workerId, proofRef })
    await recordOperationAudit({ workspaceId, actorId: workerId, action: 'data.delete.complete', resourceType: 'data_deletion_request', resourceId: completed.id, before: { status: 'approved' }, after: completed as unknown as Record<string, unknown>, reason: `外部删除证明：${proofRef}` })
    return send(res, 200, workspaceId, { ...completed, execution: 'external_proof_recorded' }, null, req)
  }
  if (path === '/v1/internal/storage/orphans/cleanup' && req.method === 'POST') {
    await requireWorkerAuthorization(req)
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    await persistenceReady
    const cleanup = await cleanObjectStorageOrphans({
      workspaceId,
      repository: persistence.objectOrphans ?? memoryObjectOrphans,
      limit: typeof input.limit === 'number' ? input.limit : 100,
      deleteObject: objectKey => getAssetStorage().delete(workspaceId, objectKey, { includeQuarantine: true }),
      onDeleted: async row => {
        const quota = persistence.storageQuota
        const reservationKey = assetReservationKeyForDeletedObject(workspaceId, row.objectKey)
        if (!quota || !reservationKey) return
        await quota.releaseAfterPhysicalDeletion({ workspaceId, reservationKey, receipt: { objectKey: row.objectKey, deletedAt: new Date().toISOString(), verification: 'delete_ack' } })
      },
    })
    if (cleanup.manualAttention > 0) {
      const alert = await (persistence.alerts ?? memoryAlerts).upsert({ workspaceId, alertKey: `storage-orphans:${workspaceId}`, code: 'OBJECT_STORAGE_ORPHAN_MANUAL_ATTENTION', severity: 'high', entityType: 'object_storage', entityId: workspaceId, title: `${cleanup.manualAttention} 个对象清理失败，需人工处理`, observedAt: new Date().toISOString(), evidence: cleanup as unknown as Record<string, unknown>, nextAction: '在运营后台核对对象键和存储服务状态，人工删除后关闭告警。' })
      void persistOperationalAlertNotification(alert)
    }
    return send(res, 200, workspaceId, cleanup, null, req)
  }
  const syncProgressMatch = path.match(/^\/v1\/sync-jobs\/([^/]+)\/progress$/)
  if (req.method === 'POST' && syncProgressMatch) {
    await requireWorkerAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const input = await body(req)
    const job = service.getSyncJob(workspaceId, syncProgressMatch[1]!)
    if (['succeeded', 'partial', 'failed'].includes(job.state)) return send(res, 200, workspaceId, job, null, req)
    const pageNumber = Number(input.page_number)
    if (!Number.isInteger(pageNumber) || pageNumber < 1) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'page_number 无效', 400)
    if (pageNumber <= job.pages) return send(res, 200, workspaceId, job, null, req)
    if (!Array.isArray(input.items)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'items 必须是数组', 400)
    const mappedItems = input.items.filter(isObject).map(item => ({
      remoteId: typeof item.remoteId === 'string' ? item.remoteId : typeof item.remote_id === 'string' ? item.remote_id : '',
      title: typeof item.title === 'string' ? item.title : '',
      sku: Array.isArray(item.sku) ? item.sku : [],
      stock: typeof item.stock === 'number' ? item.stock : 0,
      source: item.source === 'fixture' ? 'fixture' as const : 'official_api' as const,
      ...(typeof item.price === 'number' ? { price: item.price } : {}),
      ...(typeof item.category === 'string' ? { category: item.category } : {}),
      ...(Array.isArray(item.images) ? { images: item.images.filter((value): value is string => typeof value === 'string') } : {}),
      ...(isObject(item.attributes) ? { facts: Object.fromEntries(Object.entries(item.attributes).filter(([, value]) => typeof value === 'string' || typeof value === 'number').map(([key, value]) => [key, value as string | number])) } : {}),
      raw: item,
    }))
    const invalidItems = mappedItems.filter(item => !item.remoteId || !item.title)
    const items = mappedItems.filter(item => item.remoteId && item.title)
    const failures = invalidItems.map(item => ({ id: `sync-failure-${job.id}-${pageNumber}-${item.remoteId || 'unknown'}`, ...(item.remoteId ? { remoteId: item.remoteId } : {}), ...(typeof input.cursor === 'string' && input.cursor ? { cursor: input.cursor } : {}), pageNumber, code: 'PRODUCT_REQUIRED_FIELD_MISSING', message: !item.remoteId ? '平台商品缺少 remote_id' : '平台商品缺少 title', raw: item.raw, retryable: true, createdAt: new Date().toISOString() }))
    const products = service.upsertSyncedProducts({ workspaceId, platform: job.platform, accountId: job.accountId, items })
    await invalidateCanonicalFactsAfterSync(workspaceId, products)
    const updated = service.updateSyncJob(workspaceId, job.id, { state: 'running', pages: pageNumber, itemsUpserted: job.itemsUpserted + products.length, itemsFailed: job.itemsFailed + failures.length, failedItems: [...job.failedItems, ...failures], ...(typeof input.next_cursor === 'string' && input.next_cursor ? { nextCursor: input.next_cursor, resumeCursor: input.next_cursor } : {}) })
    await persistSnapshotsAndEvent({
      workspaceId,
      snapshots: [
        ...products.map(product => ({ entityType: 'product' as const, entityId: product.id, entityVersion: product.version ?? 1, payload: product as unknown as Record<string, unknown> })),
        { entityType: 'sync_job', entityId: updated.id, entityVersion: updated.revision, payload: updated as unknown as Record<string, unknown> },
      ],
      aggregateId: updated.id,
      eventType: 'sync.progress',
      sequence: pageNumber,
      eventPayload: { job_id: updated.id, page_number: pageNumber, items_upserted: products.length, items_failed: failures.length, failed_items: failures, ...(updated.nextCursor ? { next_cursor: updated.nextCursor } : {}) },
    })
    return send(res, 200, workspaceId, updated, null, req)
  }
  const syncResultMatch = path.match(/^\/v1\/sync-jobs\/([^/]+)\/result$/)
  if (req.method === 'POST' && syncResultMatch) {
    await requireWorkerAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const input = await body(req)
    const job = service.getSyncJob(workspaceId, syncResultMatch[1]!)
    const state = input.state === 'failed' || input.state === 'partial' ? input.state : 'succeeded'
    if (['succeeded', 'partial', 'failed'].includes(job.state)) {
      if (job.state === state) return send(res, 200, workspaceId, { ...job, replayed: true }, null, req)
      throw new DomainError('SYNC_JOB_TERMINAL_CONFLICT', '同步任务已进入不可逆终态，拒绝迟到结果覆盖', 409, { current_state: job.state, attempted_state: state })
    }
    const updated = service.updateSyncJob(workspaceId, job.id, { state, ...(state === 'succeeded' ? { nextCursor: undefined, resumeCursor: undefined } : {}), ...(typeof input.error_message === 'string' ? { errorMessage: input.error_message } : {}) })
    if (state === 'failed') {
      const syncEntitlementKey = `bulk-sync-job:${job.id}`
      const entitlement = await refundEntitlement({ workspaceId, actionKey: syncEntitlementKey, reason: '异步商品批量同步失败' })
      if (!entitlement.refunded && isProduction()) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: `sync:${syncEntitlementKey}`, actorId: 'worker', reason: '异步商品批量同步失败' })
    }
    await persistSnapshot(workspaceId, 'sync_job', updated, updated as unknown as Record<string, unknown>)
    const automation = state === 'succeeded' || state === 'partial'
      ? await scanAutomationAfterOperationalCompletion(workspaceId, job.platform, job.accountId, `sync-job.result.${state}`)
      : { triggered: false as const, reason: 'sync_failed' as const }
    return send(res, 200, workspaceId, { ...updated, automation }, null, req)
  }
  const syncExecutionContextMatch = path.match(/^\/v1\/sync-jobs\/([^/]+)\/execution-context$/)
  if (req.method === 'GET' && syncExecutionContextMatch) {
    await requireWorkerCredentialAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const job = service.getSyncJob(workspaceId, syncExecutionContextMatch[1]!)
    if (['succeeded', 'partial', 'failed'].includes(job.state)) throw new DomainError('SYNC_JOB_TERMINAL', '同步任务已结束，不再签发执行上下文', 409, { state: job.state })
    const account = service.getActivePlatformAccount(workspaceId, job.accountId, job.platform)
    return send(res, 200, workspaceId, { job_id: job.id, account_id: account.id, credential_ref: account.credentialRef }, null, req)
  }
  const syncJobMatch = path.match(/^\/v1\/sync-jobs\/([^/]+)$/)
  const syncRetryMatch = path.match(/^\/v1\/sync-jobs\/([^/]+)\/retry-failed$/)
  if (req.method === 'POST' && syncRetryMatch) {
    const workspaceId = resolveWorkspace(req)
    const input = await body(req)
    const failureIds = Array.isArray(input.failure_ids) ? input.failure_ids.filter((value): value is string => typeof value === 'string') : undefined
    const sourceJobId = syncRetryMatch[1]!
    const jobs = service.retrySyncFailures(workspaceId, sourceJobId, failureIds)
    const authorizationSnapshots = new Map(jobs.map(job => [job.id, workerAuthorizationSnapshot(req, workspaceId, job.id, 'catalog.sync.execute', { route: 'POST /v1/sync-jobs/:id/retry-failed', source_job_id: sourceJobId, platform: job.platform, account_id: job.accountId, mode: job.mode, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}) })]))
    if (requiresStrictAuth() && [...authorizationSnapshots.values()].some(snapshot => !snapshot)) {
      for (const job of jobs) service.removeSyncJob(workspaceId, job.id)
      throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '同步重试缺少已强制且 capability 匹配的持久身份授权快照，已拒绝入队', 503)
    }
    for (const job of jobs) {
      const authorizationSnapshot = authorizationSnapshots.get(job.id)
      await persistSnapshot(workspaceId, 'sync_job', job, job as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, job.id, 'sync.requested', job.revision, { job_id: job.id, platform: job.platform, account_id: job.accountId, mode: job.mode, ...(job.resumeCursor ? { cursor: job.resumeCursor } : {}), retry_of: sourceJobId, ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}) })
    }
    return send(res, 202, workspaceId, { jobs }, null, req)
  }
  if (req.method === 'GET' && path === '/v1/sync-jobs') {
    const workspaceId = resolveWorkspace(req, url.searchParams.get('workspace_id') ?? undefined)
    const page = paginationRequest(url)
    return send(res, 200, workspaceId, url.searchParams.has('limit') || url.searchParams.has('offset') ? service.listSyncJobsPage(workspaceId, page) : service.listSyncJobs(workspaceId), null, req)
  }
  if (req.method === 'GET' && syncJobMatch) {
    const workspaceId = resolveWorkspace(req)
    const job = service.getSyncJob(workspaceId, syncJobMatch[1]!)
    return send(res, 200, workspaceId, { ...job, workflow: projectSyncWorkflow(workspaceId, job) }, null, req)
  }
  if (req.method === 'GET' && path === '/v1/publish-jobs') {
    const workspaceId = resolveWorkspace(req, url.searchParams.get('workspace_id') ?? undefined)
    const page = paginationRequest(url)
    const visibleTasks = await filterByTaskBrandAccess(req, workspaceId, service.listTasks(workspaceId))
    const visibleTaskIds = new Set(visibleTasks.map(task => task.id))
    const jobs = service.listPublishJobs(workspaceId).filter(job => visibleTaskIds.has(job.taskId))
    return send(res, 200, workspaceId, url.searchParams.has('limit') || url.searchParams.has('offset')
      ? { items: jobs.slice(page.offset, page.offset + page.limit), total: jobs.length, ...page }
      : jobs, null, req)
  }
  if (req.method === 'POST' && path === '/v1/tasks/understand') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const understanding = await enforceTaskRequestCandidates(req, workspaceId, service.understandTaskRequest(workspaceId, required(input, 'request_text')))
    return send(res, 200, workspaceId, understanding, null, req)
  }
  if (req.method === 'POST' && path === '/v1/task-groups') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    if (!Array.isArray(input.entries)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'entries 必须是数组', 400)
    const entries = input.entries.map(entry => {
      if (!isObject(entry) || typeof entry.product_id !== 'string' || typeof entry.platform !== 'string') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '每个子任务必须包含 product_id 和 platform', 400)
      if (!SUPPORTED_PLATFORMS.includes(entry.platform as Platform)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '子任务平台无效', 400)
      const platform = entry.platform as Platform
      const accountId = resolveProductTaskAccount(workspaceId, platform, entry.product_id, typeof entry.account_id === 'string' ? entry.account_id : undefined)
      requireProductionTaskStore(platform, accountId)
      if ((isProduction() || fixtureMode) && accountId) service.getActivePlatformAccount(workspaceId, accountId, platform)
      return { productId: entry.product_id, platform, ...(accountId ? { accountId } : {}), ...(typeof entry.region === 'string' && entry.region.trim() ? { region: entry.region.trim() } : {}), ...(typeof entry.sku_id === 'string' && entry.sku_id.trim() ? { skuId: entry.sku_id.trim() } : {}) }
    })
    const entryBrandIds = await resolveTaskWriteBrands(req, workspaceId, entries.map(entry => entry.productId))
    for (const entry of entries) await requireEnabledPlatform(workspaceId, entry.platform)
    const idempotencyKey = header(req, 'idempotency-key')?.trim() || (typeof input.idempotency_key === 'string' ? input.idempotency_key.trim() : '')
    const canonicalEntries = await resolveCanonicalTaskEntries(workspaceId, entries)
    const group = service.createTaskGroup({ workspaceId, entries: canonicalEntries, ...(typeof input.request_text === 'string' ? { requestText: input.request_text } : {}), ...(idempotencyKey ? { idempotencyKey } : {}) })
    assignTaskWriteBrands(group.tasks, entryBrandIds)
    if (!group.replayed) for (const task of group.tasks) {
      await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, task.id, 'task.created', task.version, { ...task, task_group_id: group.id })
    }
    return send(res, 201, workspaceId, group, null, req)
  }
  if (req.method === 'POST' && path === '/v1/task-requests') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const requestText = required(input, 'request_text')
    const understanding = await enforceTaskRequestCandidates(req, workspaceId, service.understandTaskRequest(workspaceId, requestText))
    const taskBrandIds = await resolveTaskWriteBrands(req, workspaceId, taskUnderstandingProductIds(understanding))
    requireProductionRequestStores(workspaceId, understanding)
    for (const platform of understanding.platformCandidates) await requireEnabledPlatform(workspaceId, platform)
    const idempotencyKey = header(req, 'idempotency-key')?.trim() || (typeof input.idempotency_key === 'string' ? input.idempotency_key.trim() : '')
    const canonicalScopes = await resolveCanonicalTaskEntries(workspaceId, understanding.executionPlan.childTasks.flatMap(child => {
      const productId = child.candidateProductIds[0]
      const product = productId ? service.products.get(productId) : undefined
      return product ? [{ productId: product.id, platform: child.platform, ...(product.accountId ? { accountId: product.accountId } : {}) }] : []
    }))
    const created = service.createTaskFromRequest({ workspaceId, requestText, canonicalScopes, ...(idempotencyKey ? { idempotencyKey } : {}) })
    assignTaskWriteBrands(created.tasks, taskBrandIds)
    if (!created.replayed) for (const task of created.tasks) {
      await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, task.id, 'task.created', task.version, { ...task, ...(created.taskGroupId ? { task_group_id: created.taskGroupId } : {}), source: 'natural_language_request' })
    }
    return send(res, 201, workspaceId, created, null, req)
  }
  const skuSplitMatch = path.match(/^\/v1\/tasks\/([^/]+)\/sku-split$/)
  if (req.method === 'POST' && skuSplitMatch) {
    const source = scopeTask(req, skuSplitMatch[1]!)
    const input = await body(req)
    const idempotencyKey = header(req, 'idempotency-key')?.trim() || (typeof input.idempotency_key === 'string' ? input.idempotency_key.trim() : '')
    const split = service.splitTaskBySku({ workspaceId: source.workspaceId, taskId: source.id, ...(idempotencyKey ? { idempotencyKey } : {}) })
    if (!split.replayed) for (const task of split.tasks) {
      await persistSnapshot(source.workspaceId, 'task', task, task as unknown as Record<string, unknown>)
      await persistEvent(source.workspaceId, task.id, 'task.created', task.version, { ...task, task_group_id: split.taskGroupId, source: 'sku_split' })
    }
    await persistEvent(source.workspaceId, split.sourceTaskId, 'task.sku_split', source.version, { source_task_id: split.sourceTaskId, task_group_id: split.taskGroupId, sku_ids: split.skuIds, replayed: split.replayed })
    return send(res, 201, source.workspaceId, split, null, req)
  }
  if (req.method === 'POST' && path === '/v1/tasks') {
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const idempotencyKey = header(req, 'idempotency-key')?.trim() || (typeof input.idempotency_key === 'string' ? input.idempotency_key.trim() : '')
    if (idempotencyKey.length > 200) throw new DomainError('IDEMPOTENCY_KEY_INVALID', '幂等键不能超过 200 个字符', 400)
    const taskPlatform = required(input, 'platform') as Platform
    await requireEnabledPlatform(workspaceId, taskPlatform)
    const productId = required(input, 'product_id')
    const brandId = await taskCreationBrand(req, workspaceId, input.brand_id)
    await enforceProductBrandAccess(req, workspaceId, productId)
    const taskAccountId = resolveProductTaskAccount(workspaceId, taskPlatform, productId, typeof input.account_id === 'string' ? input.account_id : undefined)
    requireProductionTaskStore(taskPlatform, taskAccountId)
    if ((isProduction() || fixtureMode) && taskAccountId) service.getActivePlatformAccount(workspaceId, taskAccountId, taskPlatform)
    const taskId = idempotencyKey ? `task_request_${createHash('sha256').update(`${workspaceId}:${idempotencyKey}`).digest('hex').slice(0, 32)}` : undefined
    const keyHash = idempotencyKey ? createHash('sha256').update(idempotencyKey).digest('hex') : undefined
    const intentHash = idempotencyKey ? contextEnvelopeHash({ productId, brandId: brandId ?? null, platform: taskPlatform, accountId: taskAccountId ?? null, region: typeof input.region === 'string' ? input.region.trim() : '', requestText: typeof input.request_text === 'string' ? input.request_text.trim() : '', answers: input.answers && typeof input.answers === 'object' && !Array.isArray(input.answers) ? input.answers : null }) : undefined
    const replayedTask = taskId ? service.tasks.get(taskId) : undefined
    if (replayedTask) {
      if (replayedTask.taskRequestKeyHash !== keyHash || replayedTask.taskRequestIntentHash !== intentHash) throw new DomainError('IDEMPOTENCY_KEY_REUSED', '相同幂等键已用于不同的任务创建意图', 409)
      return send(res, 200, workspaceId, replayedTask, null, req)
    }
    const canonicalScope = await resolveCanonicalTaskScope({ workspaceId, productId, platform: taskPlatform, ...(taskAccountId ? { accountId: taskAccountId } : {}), ...(brandId ? { brandId } : {}), requireListing: true })
    const createdTask = service.createTask({ workspaceId, productId, platform: taskPlatform, ...(taskAccountId ? { accountId: taskAccountId } : {}), ...(canonicalScope ?? (brandId ? { brandId } : {})), ...(taskId ? { taskId } : {}), ...(typeof input.region === 'string' ? { region: input.region } : {}), ...(typeof input.request_text === 'string' && input.request_text.trim() ? { requestText: input.request_text.trim() } : {}) })
    if (keyHash && intentHash) { createdTask.taskRequestKeyHash = keyHash; createdTask.taskRequestIntentHash = intentHash }
    const task = input.answers && typeof input.answers === 'object' && !Array.isArray(input.answers)
      ? service.answerTask(workspaceId, createdTask.id, input.answers as Record<string, string | number | boolean | string[]>, createdTask.version)
      : createdTask
    await persistSnapshot(workspaceId, 'task', task, task as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, task.id, 'task.created', task.version, task as unknown as Record<string, unknown>)
    return send(res, 201, workspaceId, task, null, req)
  }
  const taskAnswersMatch = path.match(/^\/v1\/tasks\/([^/]+)\/answers$/)
  if (req.method === 'POST' && taskAnswersMatch) {
    const task = scopeTask(req, taskAnswersMatch[1]!)
    const input = await body(req)
    if (!input.answers || typeof input.answers !== 'object' || Array.isArray(input.answers)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'answers 必须是对象', 400)
    const answered = service.answerTask(task.workspaceId, task.id, input.answers as Record<string, string | number | boolean | string[]>, typeof input.expected_version === 'number' ? input.expected_version : undefined)
    await persistSnapshot(task.workspaceId, 'task', answered, answered as unknown as Record<string, unknown>)
    await persistEvent(task.workspaceId, task.id, 'task.answers_submitted', answered.version, { task_id: task.id, input_snapshot_id: answered.inputSnapshotId, answers: answered.answers, missing_questions: answered.missingQuestions })
    return send(res, 200, task.workspaceId, answered, null, req)
  }
  const taskVersionsMatch = path.match(/^\/v1\/tasks\/([^/]+)\/content-versions$/)
  if (req.method === 'GET' && taskVersionsMatch) {
    const task = scopeTask(req, taskVersionsMatch[1]!)
    const page = paginationRequest(url)
    return send(res, 200, task.workspaceId, url.searchParams.has('limit') || url.searchParams.has('offset') ? service.listContentVersionsPage(task.workspaceId, task.id, page) : service.listContentVersions(task.workspaceId, task.id), null, req)
  }
  const taskTimelineMatch = path.match(/^\/v1\/tasks\/([^/]+)\/timeline$/)
  if (req.method === 'GET' && taskTimelineMatch) {
    const task = scopeTask(req, taskTimelineMatch[1]!)
    const requestedLimit = url.searchParams.get('limit')
    return send(res, 200, task.workspaceId, await taskTimeline(task.workspaceId, task.id, requestedLimit ? Number(requestedLimit) : 100), null, req)
  }
  const feedbackMatch = path.match(/^\/v1\/tasks\/([^/]+)\/feedback$/)
  if (req.method === 'GET' && feedbackMatch) {
    const task = scopeTask(req, feedbackMatch[1]!)
    const page = paginationRequest(url)
    return send(res, 200, task.workspaceId, url.searchParams.has('limit') || url.searchParams.has('offset') ? service.listFeedbackPage(task.workspaceId, task.id, page) : service.listFeedback(task.workspaceId, task.id), null, req)
  }
  if (req.method === 'POST' && feedbackMatch) {
    const task = scopeTask(req, feedbackMatch[1]!)
    const input = await body(req)
    const rating = required(input, 'rating')
    if (!['liked', 'neutral', 'needs_improvement'].includes(rating)) throw new DomainError('FEEDBACK_RATING_INVALID', '反馈评级无效', 400)
    const feedback = service.submitFeedback({
      workspaceId: task.workspaceId, taskId: task.id, rating: rating as 'liked' | 'neutral' | 'needs_improvement',
      ...(typeof input.content_version_id === 'string' ? { contentVersionId: input.content_version_id } : {}),
      ...(typeof input.reason === 'string' ? { reason: input.reason } : {}),
      ...(typeof input.comment === 'string' ? { comment: input.comment } : {}),
      actorId: requestActor(req, 'actor_demo'),
    })
    await persistSnapshot(task.workspaceId, 'feedback', feedback, feedback as unknown as Record<string, unknown>)
    await persistEvent(task.workspaceId, feedback.id, 'task_feedback_submitted', feedback.revision, taskFeedbackEventPayload(feedback))
    return send(res, 201, task.workspaceId, feedback, null, req)
  }
  const taskGetMatch = path.match(/^\/v1\/tasks\/([^/]+)$/)
  if (req.method === 'GET' && taskGetMatch) {
    const task = scopeTask(req, taskGetMatch[1]!)
    return send(res, 200, task.workspaceId, task, null, req)
  }
  const directionMatch = path.match(/^\/v1\/tasks\/([^/]+)\/directions$/)
  if (req.method === 'GET' && directionMatch) {
    const task = scopeTask(req, directionMatch[1]!)
    return send(res, 200, task.workspaceId, service.listCreativeDirections(task.workspaceId, task.id), null, req)
  }
  if (req.method === 'POST' && directionMatch) {
    const task = scopeTask(req, directionMatch[1]!)
    const input = await body(req)
    const selected = service.selectDirection(directionMatch[1]!, required(input, 'direction_id'), typeof input.expected_version === 'number' ? input.expected_version : undefined)
    await persistSnapshot(task.workspaceId, 'task', selected, selected as unknown as Record<string, unknown>)
    await persistEvent(task.workspaceId, selected.id, 'task.direction_selected', selected.version, { task_id: selected.id, direction_id: selected.selectedDirectionId ?? null })
    return send(res, 200, task.workspaceId, selected, null, req)
  }
  const planConfirmMatch = path.match(/^\/v1\/tasks\/([^/]+)\/plan\/confirm$/)
  if (req.method === 'POST' && planConfirmMatch) {
    const task = scopeTask(req, planConfirmMatch[1]!)
    await assertCanonicalTaskScopeForAction(task)
    const input = await body(req)
    const priceImpactConfirmed = input.price_impact_confirmed === true || input.price_impact_confirmed === 'true'
    const confirmed = service.confirmProductionPlan(task.workspaceId, task.id, requestActor(req, typeof input.actor_id === 'string' && input.actor_id.trim() ? input.actor_id.trim() : 'merchant'), typeof input.expected_version === 'number' ? input.expected_version : undefined, priceImpactConfirmed)
    await persistSnapshot(task.workspaceId, 'task', confirmed, confirmed as unknown as Record<string, unknown>)
    await persistEvent(task.workspaceId, confirmed.id, 'task.plan_confirmed', confirmed.version, { task_id: confirmed.id, plan_id: confirmed.productionPlan?.id ?? null, actor_id: confirmed.productionPlan?.confirmedBy ?? null })
    await recordOperationAudit({ workspaceId: task.workspaceId, actorId: confirmed.productionPlan?.confirmedBy ?? requestActor(req), action: 'task.plan_confirmed', resourceType: 'task', resourceId: confirmed.id, before: { state: task.state, version: task.version }, after: { state: confirmed.state, version: confirmed.version, plan_id: confirmed.productionPlan?.id ?? null }, reason: '确认生产方案' })
    return send(res, 200, task.workspaceId, confirmed, null, req)
  }
  async function runFixtureGenerationJob(workspaceId: string, jobId: string) {
    try {
      const job = service.getGenerationJob(workspaceId, jobId)
      if (job.state !== 'queued') return
      const task = service.getTask(job.taskId)
      const rulePreflightBeforeWrite = await requireGenerationRulePreflight(workspaceId, task.productId, '生成前平台规则校验未通过')
      const previous = { job: structuredClone(job), task: structuredClone(task) }
      const completed = service.completeGeneration({ workspaceId, jobId, body: service.fixtureDraftBody(task.id) })
      const completedTask = service.getTask(completed.job.taskId)
      try {
        const rulePreflight = service.reviewContentReport(workspaceId, completed.version.id, { ...(await evaluationRules(workspaceId, ruleContextForTask(completedTask)) ?? { availableRuleVersionIds: [], forbiddenTerms: [], ruleHits: [] }), ruleHits: rulePreflightBeforeWrite.rule_hits })
        await persistSnapshotsAndEvent({ workspaceId, snapshots: [
          { entityType: 'content_version', entityId: completed.version.id, entityVersion: completed.version.revision, payload: completed.version as unknown as Record<string, unknown> },
          { entityType: 'task', entityId: completedTask.id, entityVersion: completedTask.version, payload: completedTask as unknown as Record<string, unknown> },
          { entityType: 'generation_job', entityId: completed.job.id, entityVersion: completed.job.revision, payload: completed.job as unknown as Record<string, unknown> },
        ], aggregateId: completed.job.id, eventType: 'generation.completed', sequence: completed.job.revision, eventPayload: { job_id: completed.job.id, task_id: completed.job.taskId, content_version_id: completed.version.id, version: completed.version.version, execution: executionContract('content', false), rule_preflight: { blocking: rulePreflight.blocking, finding_count: rulePreflight.findings.length, rule_hits: rulePreflight.ruleHits ?? [] } } })
      } catch (error) {
        rollbackGenerationCompletion(previous, completed.version.id)
        throw error
      }
      await releaseDailyModelBudget(workspaceId, `model:generation:${completed.job.idempotencyKey}`)
      await releaseDistributedJobSlot(workspaceId, `generation:${completed.job.idempotencyKey}`)
    } catch (error) {
      const job = service.getGenerationJob(workspaceId, jobId)
      if (job.state === 'succeeded' || job.state === 'failed') return
      const failed = service.failGeneration({ workspaceId, jobId, code: error instanceof DomainError ? error.code : 'FIXTURE_GENERATION_FAILED', message: error instanceof Error ? error.message : '本地演示生成失败' })
      await persistSnapshot(workspaceId, 'generation_job', failed, failed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, failed.id, 'generation.failed', failed.revision, { job_id: failed.id, task_id: failed.taskId, error_code: failed.errorCode ?? 'FIXTURE_GENERATION_FAILED', error_message: failed.errorMessage ?? '本地演示生成失败' })
      await refundTaskUsage(workspaceId, failed.taskId, `generation:${failed.idempotencyKey}`, 'fixture-worker', '本地演示内容生成失败')
      await releaseDistributedJobSlot(workspaceId, `generation:${failed.idempotencyKey}`)
    }
  }

  const generationJobCreateMatch = path.match(/^\/v1\/tasks\/([^/]+)\/content-jobs$/)
  if (req.method === 'POST' && generationJobCreateMatch) {
    const task = scopeTask(req, generationJobCreateMatch[1]!)
    await assertCanonicalTaskScopeForAction(task)
    const input = await body(req)
    const idempotencyKey = header(req, 'idempotency-key')?.trim() || (typeof input.idempotency_key === 'string' ? input.idempotency_key.trim() : '')
    if (!idempotencyKey) throw new DomainError(ERROR_CODES.IDEMPOTENCY_KEY_REQUIRED, '生成任务必须携带 Idempotency-Key', 400)
    const product = service.products.get(task.productId)
    if (!product || product.workspaceId !== task.workspaceId) throw new DomainError('PRODUCT_NOT_FOUND', '商品快照不存在或不属于当前工作区', 404)
    await requirePluginWalletAccess(task.workspaceId)
    const rulePreflight = await requireGenerationRulePreflight(task.workspaceId, product.id)
    requirePlatformModelCostGate('text')
    let existing = [...service.generationJobs.values()].find(candidate => candidate.workspaceId === task.workspaceId && candidate.idempotencyKey === idempotencyKey)
    if (!existing) {
      await hydrateDurableIdempotentJob(task.workspaceId, 'generation_job', idempotencyKey)
      existing = [...service.generationJobs.values()].find(candidate => candidate.workspaceId === task.workspaceId && candidate.idempotencyKey === idempotencyKey)
    }
    if (existing) return send(res, 202, task.workspaceId, { ...jobWithQueueMetadata(existing, task.workspaceId, 'generation'), rule_preflight: rulePreflight }, null, req)
    const reservationId = `generation:${idempotencyKey}`
    const reserved = await reserveDistributedJobSlot(task.workspaceId, reservationId)
      const usageKey = `generation:${idempotencyKey}`
      const usage = await consumeTaskUsage(task.workspaceId, task.id, usageKey, requestActor(req))
      try {
        await reserveDailyModelBudget(task.workspaceId, `model:${usageKey}`, task.id, 'text')
        const prepared = await service.prepareGenerationContext(task.id, `model:${usageKey}`)
      const job = service.enqueueGeneration({ workspaceId: task.workspaceId, taskId: task.id, idempotencyKey })
      const authorizationSnapshot = workerAuthorizationSnapshot(req, task.workspaceId, job.id, 'generation.execute', { route: 'POST /v1/tasks/:id/content-jobs', task_id: task.id, task_version: task.version })
      if (!authorizationSnapshot && isProduction()) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '内容生成缺少持久身份授权快照，已拒绝入队', 503)
      await persistSnapshot(task.workspaceId, 'generation_job', job, job as unknown as Record<string, unknown>)
      if (job.state === 'queued' && job.revision === 1) {
        await persistEvent(task.workspaceId, job.id, 'generation.requested', 1, {
          job_id: job.id, task_id: task.id, campaign_item_id: task.campaignItemId ?? null, platform: task.platform, direction_id: task.selectedDirectionId ?? 'default', action_id: `model:${usageKey}`, run_key: task.id,
          context_link_id: prepared.contextRef?.id ?? null, context_hash: prepared.contextRef?.contextHash ?? contextEnvelopeHash(prepared.input as unknown as Record<string, unknown>), input_tokens_estimate: prepared.inputTokensEstimate, max_input_tokens: prepared.maxInputTokens, input: prepared.input, ...(authorizationSnapshot ? { authorization_snapshot: serializedWorkerAuthorizationSnapshot(authorizationSnapshot) } : {}),
        })
      }
      if ((fixtureMode || process.env.CONNECTOR_FIXTURE_MODE === 'true') && job.state === 'queued' && job.revision === 1) setTimeout(() => void runFixtureGenerationJob(task.workspaceId, job.id), 0)
      return send(res, 202, task.workspaceId, { ...jobWithQueueMetadata(job, task.workspaceId, 'generation'), rule_preflight: rulePreflight }, null, req)
    } catch (error) {
      if ((usage.charged || usage.walletDebited) && !existing) await refundTaskUsage(task.workspaceId, task.id, usageKey, requestActor(req), '异步生成任务创建失败')
      if (reserved) await releaseDistributedJobSlot(task.workspaceId, reservationId)
      throw error
    }
  }
  const generationJobGetMatch = path.match(/^\/v1\/generation-jobs\/([^/]+)$/)
  if (req.method === 'GET' && generationJobGetMatch) {
    const job = service.getGenerationJob(resolveWorkspace(req), generationJobGetMatch[1]!)
    return send(res, 200, job.workspaceId, jobWithQueueMetadata(job, job.workspaceId, 'generation'), null, req)
  }
  if (req.method === 'GET' && path === '/v1/image-generation-jobs') {
    const workspaceId = resolveWorkspace(req, url.searchParams.get('workspace_id') ?? undefined)
    await hydrateWorkspace(workspaceId)
    const accessibleIds = await accessibleProductIds(req, workspaceId)
    const state = url.searchParams.get('state')?.trim()
    const all = service.listImageGenerationJobs(workspaceId).filter(job =>
      (accessibleIds === undefined || accessibleIds.has(job.productId))
      && (!state || job.state === state),
    )
    const page = paginationRequest(url)
    const items = await Promise.all(all.slice(page.offset, page.offset + page.limit).map(async job => ({
      ...publicImageJob(job),
      ...await publicImageJobExecutionProjection(workspaceId, job.id),
      productTitle: service.products.get(job.productId)?.title ?? null,
      platform: service.products.get(job.productId)?.platform ?? null,
      storeName: service.products.get(job.productId)?.storeName ?? null,
    })))
    return send(res, 200, workspaceId, { items, total: all.length, ...page }, null, req)
  }
  const imageGenerationJobGetMatch = path.match(/^\/v1\/image-generation-jobs\/([^/]+)$/u)
  if (req.method === 'GET' && imageGenerationJobGetMatch) {
    const workspaceId = resolveWorkspace(req)
    await hydrateWorkspace(workspaceId)
    const job = service.getImageGenerationJob(workspaceId, decodeURIComponent(imageGenerationJobGetMatch[1]!))
    const execution = await persistence.imageGenerationExecutions?.get({ workspaceId, jobId: job.id })
    const images = imageJobOutputsAreClean(job) ? await readArchivedGeneratedImages(workspaceId, job) : []
    return send(res, 200, workspaceId, {
      job_id: job.id,
      revision: job.revision,
      state: job.state,
      archive_state: job.archiveState,
      product_id: job.productId,
      task_id: job.taskId ?? null,
      content_version_id: job.contentVersionId ?? null,
      image_mode: job.imageMode,
      direction: job.direction,
      requested_count: job.count,
      source_asset_ids: job.sourceAssetIds ?? [],
      source_product_version: job.sourceProductVersion,
      intent_hash: job.intentHash,
      execution_state: execution?.state ?? null,
      provider_request_id: execution?.providerRequestId ?? null,
      execution_attempt: execution?.attempt ?? null,
      reconciliation_required: execution?.state === 'provider_reserved' || execution?.state === 'provider_dispatching' || execution?.state === 'provider_started' || execution?.state === 'outcome_unknown' || job.archiveState !== 'archived',
      error_code: job.errorCode ?? null,
      error_message: job.errorMessage ?? null,
      updated_at: job.updatedAt,
      created_at: job.createdAt,
      outputs: (job.outputs ?? []).map(output => {
        const asset = output.assetId ? service.assets.get(output.assetId) : undefined
        const blockers = [
          ...(job.archiveState !== 'archived' ? ['候选尚未完整归档'] : []),
          ...(!asset || !isTrustedCleanAsset(asset) ? ['安全扫描凭据尚未通过可信校验'] : []),
          ...(output.reviewStatus !== 'passed' ? ['尚未完成人工视觉审核'] : []),
        ]
        return {
          visual_ref: output.visualRef,
          ordinal: output.ordinal,
          asset_id: output.assetId ?? null,
          archive_receipt_id: output.archiveReceiptId ?? null,
          archive_receipt_digest: output.archiveReceiptDigest ?? null,
          storage_key: output.storageKey,
          mime_type: output.mimeType,
          size_bytes: output.sizeBytes,
          sha256: output.sha256,
          created_at: output.createdAt,
          review_status: output.reviewStatus,
          gate: {
            archive: job.archiveState,
            scan: asset?.scanStatus ?? 'unknown',
            rights: asset?.rightsStatus ?? 'unknown',
            authenticity: output.authenticity?.report?.publishable === true ? 'passed' : output.authenticity?.externallyUnverified ? 'unverified' : 'not_checked',
            selectable: blockers.length === 0,
            blockers,
          },
        }
      }),
      ...(images.length ? { images } : {}),
      ...(images.length ? {} : { availability_warning: job.archiveState === 'archived' ? '候选已归档，安全扫描或真实性检查尚未完成。' : '图片任务尚未形成可交付候选。' }),
      next_action: execution?.state === 'outcome_unknown' || execution?.state === 'provider_started' ? { type: 'reconcile', label: '等待对账或查看执行证据', allowed: true } : job.state === 'failed' ? { type: 'review_error', label: '查看失败原因', allowed: true } : job.state === 'succeeded' && images.length ? { type: 'review_candidates', label: '查看候选', allowed: true } : { type: 'refresh_status', label: '刷新任务状态', allowed: true },
    }, null, req)
  }
  const generationJobDeferMatch = path.match(/^\/v1\/generation-jobs\/([^/]+)\/defer$/)
  if (req.method === 'POST' && generationJobDeferMatch) {
    await requireWorkerAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const input = await body(req)
    const retryAfterSeconds = typeof input.retry_after_seconds === 'number' && Number.isFinite(input.retry_after_seconds) ? Math.max(1, Math.ceil(input.retry_after_seconds)) : 60
    const deferred = service.deferGeneration({ workspaceId, jobId: generationJobDeferMatch[1]!, code: typeof input.code === 'string' ? input.code : 'QUOTA_EXHAUSTED', message: typeof input.message === 'string' ? input.message : '模型/平台配额暂满，任务将在配额窗口恢复后重试', retryAfterSeconds })
    await persistSnapshot(workspaceId, 'generation_job', deferred, deferred as unknown as Record<string, unknown>)
    await persistEvent(workspaceId, deferred.id, 'generation.deferred', deferred.revision, { job_id: deferred.id, task_id: deferred.taskId, code: deferred.errorCode ?? 'QUOTA_EXHAUSTED', retry_after_seconds: retryAfterSeconds, next_attempt_at: deferred.nextAttemptAt })
    return send(res, 200, workspaceId, jobWithQueueMetadata(deferred, workspaceId, 'generation'), null, req)
  }
  const generationJobResultMatch = path.match(/^\/v1\/generation-jobs\/([^/]+)\/result$/)
  if (req.method === 'POST' && generationJobResultMatch) {
    await requireWorkerAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const input = await body(req)
    const job = service.getGenerationJob(workspaceId, generationJobResultMatch[1]!)
    if (input.error && typeof input.error === 'object' && !Array.isArray(input.error)) {
      const error = input.error as Record<string, unknown>
      const failed = service.failGeneration({ workspaceId, jobId: job.id, code: typeof error.code === 'string' ? error.code : 'AI_GENERATION_FAILED', message: typeof error.message === 'string' ? error.message : '内容生成失败' })
      await persistSnapshot(workspaceId, 'generation_job', failed, failed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, failed.id, 'generation.failed', failed.revision, { job_id: failed.id, task_id: failed.taskId, error_code: failed.errorCode ?? 'AI_GENERATION_FAILED', error_message: failed.errorMessage ?? '内容生成失败' })
      if (!providerSucceededButSettlementPending(error)) await refundTaskUsage(workspaceId, failed.taskId, `generation:${failed.idempotencyKey}`, header(req, 'x-actor-id')?.trim() || 'worker', '异步内容生成失败')
      await releaseDistributedJobSlot(workspaceId, `generation:${failed.idempotencyKey}`)
      return send(res, 200, workspaceId, failed, null, req)
    }
    if (!input.content || typeof input.content !== 'object' || Array.isArray(input.content)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '缺少生成内容', 400)
    const content = input.content as Record<string, unknown>
    const sellingPoints = Array.isArray(content.sellingPoints) ? content.sellingPoints.filter((value): value is string => typeof value === 'string') : []
    if (typeof content.title !== 'string' || typeof content.detail !== 'string' || !sellingPoints.length) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '生成内容结构无效', 400)
    const brief = readStaticBrief(content.brief)
    const modules = readContentModules(content.modules)
    const completedTaskBeforeWrite = service.getTask(job.taskId)
    let rulePreflightBeforeWrite: Awaited<ReturnType<typeof generationRulePreflight>>
    try {
      rulePreflightBeforeWrite = await requireGenerationRulePreflight(workspaceId, completedTaskBeforeWrite.productId, '排队期间平台规则已发生变化，不能提交该生成结果')
    } catch (error) {
      const failed = service.failGeneration({ workspaceId, jobId: job.id, code: error instanceof DomainError ? error.code : 'PLATFORM_RULE_PREFLIGHT_BLOCKED', message: error instanceof Error ? error.message : '排队期间平台规则已发生变化，不能提交该生成结果' })
      await persistSnapshot(workspaceId, 'generation_job', failed, failed as unknown as Record<string, unknown>)
      await persistEvent(workspaceId, failed.id, 'generation.failed', failed.revision, { job_id: failed.id, task_id: failed.taskId, error_code: failed.errorCode ?? 'PLATFORM_RULE_PREFLIGHT_BLOCKED', error_message: failed.errorMessage ?? '排队期间平台规则已发生变化，不能提交该生成结果' })
      await refundTaskUsage(workspaceId, failed.taskId, `generation:${failed.idempotencyKey}`, header(req, 'x-actor-id')?.trim() || 'worker', '排队期间平台规则变化导致生成阻断')
      await releaseDistributedJobSlot(workspaceId, `generation:${failed.idempotencyKey}`)
      throw error
    }
    const execution = await contentExecutionEvidence(workspaceId, 'model:generation:' + job.idempotencyKey)
    if (['staging', 'preview', 'production'].includes(process.env.NODE_ENV ?? '') && execution.providerExecuted !== true) {
      throw new DomainError('MODEL_RELAY_EVIDENCE_REQUIRED', '中转模型回执尚未完成，内容暂不能交付', 409, {
        operation_status: 'pending_receipt',
        action_id: 'model:generation:' + job.idempotencyKey,
        missing: ['provider_request_id', 'usage', 'cost_cny', 'settlement'],
      })
    }
    const previous = { job: structuredClone(job), task: structuredClone(completedTaskBeforeWrite) }
    const completed = service.completeGeneration({ workspaceId, jobId: job.id, body: { title: content.title, detail: content.detail, sellingPoints, ...(modules ? { modules } : {}), ...(brief ? { brief } : {}) } })
    const completedTask = service.getTask(completed.job.taskId)
    let rulePreflight!: ReturnType<typeof service.reviewContentReport>
    try {
      rulePreflight = service.reviewContentReport(workspaceId, completed.version.id, { ...(await evaluationRules(workspaceId, ruleContextForTask(completedTask)) ?? { availableRuleVersionIds: [], forbiddenTerms: [], ruleHits: [] }), ruleHits: rulePreflightBeforeWrite.rule_hits })
      await persistSnapshotsAndEvent({ workspaceId, snapshots: [
        { entityType: 'content_version', entityId: completed.version.id, entityVersion: completed.version.revision, payload: completed.version as unknown as Record<string, unknown> },
        { entityType: 'task', entityId: completedTask.id, entityVersion: completedTask.version, payload: completedTask as unknown as Record<string, unknown> },
        { entityType: 'generation_job', entityId: completed.job.id, entityVersion: completed.job.revision, payload: completed.job as unknown as Record<string, unknown> },
      ], aggregateId: completed.job.id, eventType: 'generation.completed', sequence: completed.job.revision, eventPayload: { job_id: completed.job.id, task_id: completed.job.taskId, content_version_id: completed.version.id, version: completed.version.version, execution, rule_preflight: { blocking: rulePreflight.blocking, finding_count: rulePreflight.findings.length, rule_hits: rulePreflight.ruleHits ?? [] } } })
    } catch (error) {
      rollbackGenerationCompletion(previous, completed.version.id)
      throw error
    }
    await releaseDistributedJobSlot(workspaceId, `generation:${completed.job.idempotencyKey}`)
    return send(res, 200, workspaceId, { ...jobWithQueueMetadata(completed.job, workspaceId, 'generation'), execution, rule_preflight: rulePreflight }, null, req)
  }
  const contentMatch = path.match(/^\/v1\/tasks\/([^/]+)\/content$/)
  if (req.method === 'POST' && contentMatch) {
    const task = scopeTask(req, contentMatch[1]!)
    await assertCanonicalTaskScopeForAction(task)
    const idempotencyKey = header(req, 'idempotency-key')?.trim() || ''
    await requirePluginWalletAccess(task.workspaceId)
    if (isProduction() && !idempotencyKey) throw new DomainError(ERROR_CODES.IDEMPOTENCY_KEY_REQUIRED, '生产内容生成必须携带 Idempotency-Key', 400)
    const rulePreflight = await requireGenerationRulePreflight(task.workspaceId, task.productId)
    requirePlatformModelCostGate('text')
    const actorId = requestActor(req)
    const usageKey = `content.rest.generate:${idempotencyKey || task.id}`
    const usage = await consumeTaskUsage(task.workspaceId, task.id, usageKey, actorId)
    let draft
    try { draft = await service.generateDraft(contentMatch[1]!, idempotencyKey || undefined, `model:${usageKey}`) } catch (error) { if (providerSucceededButSettlementPending(error)) await markTaskUsageProviderOutcomePending(task.workspaceId, usageKey); else if (usage.charged || usage.walletDebited) await refundTaskUsage(task.workspaceId, task.id, usageKey, actorId, 'REST 内容生成失败'); throw error }
    const execution = await requireSettledContentExecutionEvidence(task.workspaceId, 'model:' + usageKey)
    await persistSnapshot(task.workspaceId, 'content_version', draft, draft as unknown as Record<string, unknown>)
    await persistSnapshot(task.workspaceId, 'task', service.getTask(task.id), service.getTask(task.id) as unknown as Record<string, unknown>)
    const report = service.reviewContentReport(task.workspaceId, draft.id, await evaluationRules(task.workspaceId, ruleContextForTask(task)))
    await persistEvent(task.workspaceId, draft.id, 'content.generated', draft.revision, { task_id: task.id, content_version_id: draft.id, version: draft.version, execution, rule_preflight: { blocking: report.blocking, finding_count: report.findings.length, rule_hits: report.ruleHits ?? [] } })
    return send(res, 201, task.workspaceId, { ...draft, execution, rule_preflight: report }, null, req)
  }
  const approvalMatch = path.match(/^\/v1\/tasks\/([^/]+)\/approve$/)
  if (req.method === 'POST' && approvalMatch) {
    const task = scopeTask(req, approvalMatch[1]!)
    await assertCanonicalTaskScopeForAction(task)
    const input = await body(req)
    const approved = service.approveContent(approvalMatch[1]!, required(input, 'content_version_id'), await evaluationRules(task.workspaceId, ruleContextForTask(task)), typeof input.expected_version === 'number' ? input.expected_version : undefined)
    await persistSnapshot(task.workspaceId, 'content_version', approved.version, approved.version as unknown as Record<string, unknown>)
    await persistSnapshot(task.workspaceId, 'task', approved.task, approved.task as unknown as Record<string, unknown>)
    await persistEvent(task.workspaceId, approved.version.id, 'content.approved', approved.version.revision, { task_id: approved.task.id, content_version_id: approved.version.id, version: approved.version.version })
    return send(res, 200, task.workspaceId, approved, null, req)
  }
  const versionDiffMatch = path.match(/^\/v1\/content-versions\/([^/]+)\/diff$/)
  if (req.method === 'GET' && versionDiffMatch) {
    const scoped = scopeContentVersion(req, versionDiffMatch[1]!)
    const against = url.searchParams.get('against') ?? url.searchParams.get('against_version_id') ?? undefined
    return send(res, 200, scoped.task.workspaceId, service.diffContentVersions(scoped.task.workspaceId, scoped.version.id, against), null, req)
  }
  const versionModifyMatch = path.match(/^\/v1\/content-versions\/([^/]+)\/modify$/)
  if (req.method === 'POST' && versionModifyMatch) {
    const scoped = scopeContentVersion(req, versionModifyMatch[1]!)
    await assertCanonicalTaskScopeForAction(scoped.task)
    const input = await body(req)
    const lockedFields = Array.isArray(input.locked_fields) ? input.locked_fields.filter((value): value is string => typeof value === 'string') : undefined
    const reason = typeof input.reason === 'string' && input.reason.trim() ? input.reason.trim() : 'user_edit'
    const modified = typeof input.module_key === 'string' && input.module_key.trim()
      ? service.regenerateContentModule({ workspaceId: scoped.task.workspaceId, sourceVersionId: scoped.version.id, moduleKey: input.module_key, ...(lockedFields ? { lockedFields } : {}), reason, ...(typeof input.expected_revision === 'number' ? { expectedRevision: input.expected_revision } : {}) })
      : (() => {
        if (!input.changes || typeof input.changes !== 'object' || Array.isArray(input.changes)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'changes 必须是对象', 400)
        return service.modifyContentVersion({ workspaceId: scoped.task.workspaceId, sourceVersionId: scoped.version.id, changes: input.changes as Partial<import('../../../packages/application/src/service.js').ContentVersion['body']>, ...(lockedFields ? { lockedFields } : {}), reason, ...(typeof input.expected_revision === 'number' ? { expectedRevision: input.expected_revision } : {}) })
      })()
    await persistSnapshot(scoped.task.workspaceId, 'content_version', modified.version, modified.version as unknown as Record<string, unknown>)
    await persistSnapshot(scoped.task.workspaceId, 'task', modified.task, modified.task as unknown as Record<string, unknown>)
    await persistEvent(scoped.task.workspaceId, modified.version.id, 'content.version_modified', modified.version.revision, { task_id: modified.task.id, source_version_id: modified.source.id, content_version_id: modified.version.id, reason: modified.version.versionVector?.reason, locked_fields: modified.version.lockedFields ?? [] })
    return send(res, 201, scoped.task.workspaceId, modified, null, req)
  }
  const versionReviewMatch = path.match(/^\/v1\/content-versions\/([^/]+)\/review$/)
  if (req.method === 'GET' && versionReviewMatch) {
    const scoped = scopeContentVersion(req, versionReviewMatch[1]!)
    await assertCanonicalTaskScopeForAction(scoped.task)
    return send(res, 200, scoped.task.workspaceId, service.reviewContentReport(scoped.task.workspaceId, scoped.version.id, await evaluationRules(scoped.task.workspaceId, ruleContextForTask(scoped.task))), null, req)
  }
  const versionReviewDecisionMatch = path.match(/^\/v1\/content-versions\/([^/]+)\/review-decisions$/)
  if (req.method === 'POST' && versionReviewDecisionMatch) {
    const scoped = scopeContentVersion(req, versionReviewDecisionMatch[1]!)
    await assertCanonicalTaskScopeForAction(scoped.task)
    const input = await body(req)
    const status = required(input, 'status')
    if (!['acknowledged', 'waived'].includes(status)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'status 必须是 acknowledged 或 waived', 400)
    const decided = service.setReviewFindingDecision({ workspaceId: scoped.task.workspaceId, contentVersionId: scoped.version.id, code: required(input, 'code'), field: required(input, 'field'), status: status as 'acknowledged' | 'waived', ...(typeof input.reason === 'string' ? { reason: input.reason } : {}), actorId: requestActor(req), ...(typeof input.expected_revision === 'number' ? { expectedRevision: input.expected_revision } : {}) }, await evaluationRules(scoped.task.workspaceId, ruleContextForTask(scoped.task)))
    await persistSnapshot(scoped.task.workspaceId, 'content_version', decided.version, decided.version as unknown as Record<string, unknown>)
    await persistEvent(scoped.task.workspaceId, decided.version.id, 'content.review_decided', decided.version.revision, { content_version_id: decided.version.id, finding_key: decided.decision.key, status: decided.decision.status, reason: decided.decision.reason, actor_id: decided.decision.actorId })
    return send(res, 200, scoped.task.workspaceId, decided, null, req)
  }
  const versionRestoreMatch = path.match(/^\/v1\/content-versions\/([^/]+)\/restore$/)
  if (req.method === 'POST' && versionRestoreMatch) {
    const scoped = scopeContentVersion(req, versionRestoreMatch[1]!)
    await assertCanonicalTaskScopeForAction(scoped.task)
    const input = await body(req)
    const restored = service.restoreContentVersion(scoped.task.workspaceId, scoped.version.id, typeof input.expected_version === 'number' ? input.expected_version : undefined)
    await persistSnapshot(scoped.task.workspaceId, 'content_version', restored.version, restored.version as unknown as Record<string, unknown>)
    await persistSnapshot(scoped.task.workspaceId, 'task', restored.task, restored.task as unknown as Record<string, unknown>)
    await persistEvent(scoped.task.workspaceId, restored.version.id, 'content.version_restored', restored.version.revision, { task_id: restored.task.id, source_version_id: restored.source.id, content_version_id: restored.version.id, version: restored.version.version })
    return send(res, 201, scoped.task.workspaceId, restored, null, req)
  }
  const versionExportMatch = path.match(/^\/v1\/content-versions\/([^/]+)\/export$/)
  if (req.method === 'GET' && versionExportMatch) {
    const scoped = scopeContentVersion(req, versionExportMatch[1]!)
    const requestedFormat = url.searchParams.get('format') ?? 'bundle'
    if (!['manifest', 'json', 'markdown', 'bundle'].includes(requestedFormat)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'format 必须是 manifest、json、markdown 或 bundle', 400)
    await persistExpiredDeliveryIfNeeded(scoped.task.workspaceId, scoped.version.id)
    const exported = service.exportContent(scoped.task.workspaceId, scoped.version.id, requestedFormat as 'manifest' | 'json' | 'markdown' | 'bundle')
    if (exported.binaryBody) {
      const verification = verifyExportedBundle(scoped.task.workspaceId, scoped.version.id, exported.binaryBody)
      res.setHeader('x-delivery-bundle-sha256', verification.artifact_sha256)
      res.setHeader('x-delivery-manifest-sha256', verification.manifest_hash)
      res.setHeader('x-delivery-bundle-verified', String(verification.valid))
      res.setHeader('x-delivery-verification-scope', verification.scope)
    }
    return sendDownload(res, exported, req)
  }
  const prepareMatch = path.match(/^\/v1\/tasks\/([^/]+)\/publish-preview$/)
  if (req.method === 'POST' && prepareMatch) {
    const task = scopeTask(req, prepareMatch[1]!)
    const canonicalProof = await assertCanonicalTaskScopeForAction(task)
    await requireEnabledPlatform(task.workspaceId, task.platform)
    const mappingPreflight = await requireCurrentPlatformMapping(task)
    if ((isProduction() || fixtureMode) && !task.accountId) throw new DomainError('STORE_SELECTION_REQUIRED', '发布前必须明确选择商品所属店铺', 409)
    const currentRuleReview = await requireCurrentPublishReview(task.workspaceId, task)
    const taskBeforePrepare = structuredClone(task)
    const preview = service.preparePublish(prepareMatch[1]!)
    if (canonicalProof?.canonicalReadRevision && preview.task.pendingPublish) preview.task.pendingPublish.canonicalReadRevision = canonicalProof.canonicalReadRevision
    try {
      await persistSnapshotsAndEvent({ workspaceId: task.workspaceId, snapshots: [{ entityType: 'task', entityId: preview.task.id, entityVersion: preview.task.version, payload: preview.task as unknown as Record<string, unknown> }], aggregateId: preview.task.id, eventType: 'publish.prepared', sequence: preview.task.version, eventPayload: { task_id: preview.task.id, content_version_id: preview.version.id, confirmation_hash: preview.confirmationHash, remote_snapshot_hash: preview.remoteSnapshotHash, payload_hash: preview.payloadHash, selection_hash: preview.selectionHash, selected_count: preview.visualPreview.count, image_mode: preview.visualPreview.imageMode } })
    } catch (error) {
      service.tasks.set(task.id, taskBeforePrepare)
      throw error
    }
    return send(res, 200, task.workspaceId, { ...preview, currentRuleReview, ...(mappingPreflight ? { mappingPreflight: { publishable: true, confirmationValid: true, mappedPayloadHash: mappingPreflight.mappedPayloadHash } } : {}) }, null, req)
  }
  if (req.method === 'POST' && path === '/v1/publish-jobs') {
    const input = await body(req)
    const key = header(req, 'idempotency-key')?.trim()
    if (!key) throw new DomainError(ERROR_CODES.IDEMPOTENCY_KEY_REQUIRED, '发布确认必须携带 Idempotency-Key', 400)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const taskId = required(input, 'task_id')
    const contentVersionId = required(input, 'content_version_id')
    const confirmationHash = required(input, 'confirmation_hash')
    const remoteSnapshotHash = required(input, 'remote_snapshot_hash')
    const task = service.getTask(taskId)
    if (task.workspaceId !== workspaceId) throw new DomainError(ERROR_CODES.TENANT_SCOPE_DENIED, '无权访问该任务', 403)
    await enforceTaskBrandAccess(req, task, 'publisher')
    await requireEnabledPlatform(workspaceId, task.platform)
    await requireCurrentPlatformMapping(task)
    if (isProduction() && !((typeof input.account_id === 'string' && input.account_id.trim()) || task.accountId)) throw new DomainError('PLATFORM_ACCOUNT_REQUIRED', '生产发布必须绑定已授权平台账号', 400)
    const publishAccountId = resolveTaskPublishAccount(task, typeof input.account_id === 'string' ? input.account_id : undefined)
    if (isProduction()) {
      service.getActivePlatformAccount(workspaceId, publishAccountId!, task.platform)
      if (!platformWriteReady(task.platform)) throw new DomainError('PLATFORM_WRITE_NOT_READY', '平台尚未完成生产写入能力验证，当前不会创建发布任务', 503)
    }
    await requirePluginWalletAccess(workspaceId)
    assertPublishIdempotency(workspaceId, { taskId, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: key })
    let existing = [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === key)
    if (!existing) {
      await hydrateDurableIdempotentJob(workspaceId, 'publish_job', key)
      existing = [...service.publishJobs.values()].find(candidate => candidate.workspaceId === workspaceId && candidate.idempotencyKey === key)
    }
    const authorizationSnapshot = existing ? undefined : requirePublishAuthorizationSnapshot(publishAuthorizationSnapshot(req, workspaceId, task))
    const publishInput = { workspaceId, taskId, contentVersionId, confirmationHash, remoteSnapshotHash, params: input }
    const fencedTicket = existing ? undefined : await reservePublishConfirmationTicket(req, workspaceId, publishInput)
    if (!existing && !fencedTicket) await consumePublishConfirmationTicket(req, workspaceId, publishInput)
    const reservationId = `publish:${key}`
    let reserved = false
    let walletDebited = false
    try {
      if (existing) {
        const job = service.confirmPublish({ workspaceId, taskId, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: key, ...(publishAccountId ? { accountId: publishAccountId } : {}), mediaAdapterReady: connectorRuntime.mediaUploadReady(task.platform), authorizationSnapshot: publishAuthorizationSnapshot(req, workspaceId, task) })
        return send(res, 202, workspaceId, jobWithQueueMetadata(job, workspaceId, 'publish'), null, req)
      }
      const job = service.confirmPublish({ workspaceId, taskId, contentVersionId, confirmationHash, remoteSnapshotHash, idempotencyKey: key, ...(publishAccountId ? { accountId: publishAccountId } : {}), mediaAdapterReady: connectorRuntime.mediaUploadReady(task.platform), authorizationSnapshot, deferCommit: true })
      const currentTask = { ...service.getTask(taskId), state: 'publishing' as const, version: service.getTask(taskId).version + 1 }
      const snapshots = [
        { entityType: 'task' as const, entityId: currentTask.id, entityVersion: currentTask.version, payload: currentTask as unknown as Record<string, unknown> },
        { entityType: 'publish_job' as const, entityId: job.id, entityVersion: job.revision, payload: job as unknown as Record<string, unknown> },
      ]
      if (fencedTicket && persistence.persistPublishTransaction) {
        await runFencedSinglePublish({
          ticketRepository: persistence.interactiveConfirmationTickets as TransactionalInteractiveConfirmationTicketRepository,
          ticket: fencedTicket.ticket,
          consumedOperationId: job.id,
          wallet: {
            debit: async () => { await debitPluginWallet({ workspaceId, idempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', description: '商品发布调用' }); walletDebited = true },
            refund: async () => { await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', reason: '发布任务创建失败' }); walletDebited = false },
          },
          slot: {
            reserve: async () => { reserved = await reserveDistributedJobSlot(workspaceId, reservationId) },
            release: async () => { if (reserved) await releaseDistributedJobSlot(workspaceId, reservationId); reserved = false },
          },
          persist: async ({ reservation, finalizeInTransaction }) => {
            await persistence.persistPublishTransaction!({ workspaceId, snapshots, aggregateId: job.id, eventType: 'publish.requested', sequence: 1, eventPayload: publishEventPayload(job), finalizeTicketInTransaction: finalizeInTransaction })
            return { status: 'committed' as const, value: job }
          },
        })
      } else {
        if (!existing) {
          await debitPluginWallet({ workspaceId, idempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', description: '商品发布调用' })
          walletDebited = true
        }
        reserved = existing ? false : await reserveDistributedJobSlot(workspaceId, reservationId)
        await persistSnapshotsAndEvent({ workspaceId, snapshots, aggregateId: job.id, eventType: 'publish.requested', sequence: 1, eventPayload: publishEventPayload(job) })
      }
      service.commitPublishConfirmation(job)
      scheduleFixturePublishObservation(job)
      return send(res, 202, workspaceId, jobWithQueueMetadata(job, workspaceId, 'publish'), null, req)
    } catch (error) {
      if (!(error instanceof PublishCommitStatusUnknownError)) {
        if (reserved) await releaseDistributedJobSlot(workspaceId, reservationId)
        if (walletDebited) await refundPluginWalletDebit({ workspaceId, debitIdempotencyKey: reservationId, actorId: requestPrincipals.get(req)?.actorId ?? header(req, 'x-actor-id')?.trim() ?? 'merchant', reason: '发布任务创建失败' })
      }
      throw error
    }
  }
  const publishGetMatch = path.match(/^\/v1\/publish-jobs\/([^/]+)$/)
  const workerExecutionCheckMatch = path.match(/^\/v1\/worker-events\/([^/]+)\/execution-check$/)
  const publishExecutionCheckMatch = path.match(/^\/v1\/publish-jobs\/([^/]+)\/execution-check$/)
  const publishObservationMatch = path.match(/^\/v1\/publish-jobs\/([^/]+)\/observation$/)
  if (req.method === 'GET' && workerExecutionCheckMatch) {
    await requireWorkerCredentialAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const aggregateId = url.searchParams.get('aggregate_id')?.trim() ?? ''
    const requestedOperation = url.searchParams.get('operation')?.trim() as CriticalWorkerOperation
    if (!aggregateId || !Object.values(workerEventOperations).includes(requestedOperation)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, 'aggregate_id 或 operation 无效', 400)
    if (!persistence.outbox) throw new DomainError('AUTHORIZATION_EVENT_REPOSITORY_UNAVAILABLE', '持久事件仓储不可用，已拒绝执行', 503)
    const event = (await persistence.outbox.listAggregateEvents(workspaceId, aggregateId, 1000)).find(candidate => candidate.id === workerExecutionCheckMatch[1])
    if (!event) throw new DomainError('AUTHORIZATION_EVENT_NOT_FOUND', '执行授权事件不存在或不属于当前工作区', 404)
    const expectedOperation = workerEventOperations[event.eventType]
    if (!expectedOperation || expectedOperation !== requestedOperation) throw new DomainError('AUTHZ_EXECUTION_OPERATION_MISMATCH', '事件类型与执行操作不匹配', 403)
    let snapshot: WorkerAuthorizationSnapshot
    try {
      snapshot = parseWorkerAuthorizationSnapshot(event, requestedOperation)
    } catch {
      throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_INVALID', '持久事件缺少有效且精确绑定的授权快照', 403)
    }
    return send(res, 200, workspaceId, { authorization_recheck: await recheckWorkerAuthorizationSnapshot(snapshot, workspaceId, event.aggregateId, { eventId: event.id }) }, null, req)
  }
  if (req.method === 'GET' && publishExecutionCheckMatch) {
    await requireWorkerCredentialAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const job = service.assertPublishExecutionAllowed({ workspaceId, publishJobId: publishExecutionCheckMatch[1]! })
    const eventId = url.searchParams.get('event_id')?.trim() ?? ''
    if (!eventId) throw new DomainError('AUTHZ_EXECUTION_EVENT_REQUIRED', '发布执行缺少持久事件标识，已拒绝释放凭据', 400)
    if (!persistence.outbox) throw new DomainError('AUTHORIZATION_EVENT_REPOSITORY_UNAVAILABLE', '持久事件仓储不可用，已拒绝发布执行', 503)
    const publishEvent = (await persistence.outbox.listAggregateEvents(workspaceId, job.id, 1000)).find(candidate => candidate.id === eventId)
    if (!publishEvent || publishEvent.eventType !== 'publish.requested' || publishEvent.aggregateId !== job.id) throw new DomainError('AUTHORIZATION_EVENT_NOT_FOUND', '发布执行事件不存在或不属于当前发布任务', 404)
    // The durable publish binding protects the task snapshot, but it is not a
    // substitute for checking the current canonical product/listing chain.
    // Re-read it immediately before releasing the credential to the worker so
    // a post-prepare mapping, facts, listing, or read-mode change fails closed.
    const canonicalProof = await assertCanonicalTaskScopeForAction(service.getTask(job.taskId))
    if (canonicalProof?.readMode === 'canonical_read' && (!job.canonicalReadRevision || job.canonicalReadRevision !== canonicalProof.canonicalReadRevision)) throw new DomainError('CANONICAL_EXECUTION_REVISION_STALE', '发布任务引用的标准商品一致性证据已过期，禁止释放发布凭证', 409, { expected_revision: canonicalProof.canonicalReadRevision ?? null, provided_revision: job.canonicalReadRevision ?? null })
    if (isProduction()) {
      const currentTask = service.getTask(job.taskId)
      const preparedTaskRevision: number | null = typeof job.preparedTaskRevision === 'number' ? job.preparedTaskRevision : null
      const expectedTaskRevision = preparedTaskRevision === null ? null : preparedTaskRevision + 1
      if (preparedTaskRevision === null || !Number.isSafeInteger(preparedTaskRevision) || currentTask.state !== 'publishing' || currentTask.version !== expectedTaskRevision) throw new DomainError('AUTHZ_EXECUTION_RESOURCE_STALE', '发布任务引用的任务版本已变化，禁止释放发布凭证', 409, { expected_revision: Number.isSafeInteger(expectedTaskRevision) ? expectedTaskRevision : null, current_revision: currentTask.version })
    }
    const account = service.getActivePlatformAccount(workspaceId, job.accountId!, job.platform)
    const snapshot = job.authorizationSnapshot
    if (!snapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '发布执行缺少入队授权快照，已拒绝释放凭据', 403)
    let eventSnapshot: WorkerAuthorizationSnapshot
    try { eventSnapshot = parseWorkerAuthorizationSnapshot(publishEvent, 'publish.execute') } catch { throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_INVALID', '发布事件缺少有效且精确绑定的授权快照', 403) }
    if (JSON.stringify(serializedWorkerAuthorizationSnapshot(eventSnapshot)) !== JSON.stringify(serializedWorkerAuthorizationSnapshot(snapshot))) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_INVALID', '发布任务与持久事件的授权快照不一致', 403)
    const authorizationRecheck = await recheckWorkerAuthorizationSnapshot(snapshot, workspaceId, job.id, { eventId: publishEvent.id })
    return send(res, 200, workspaceId, { allowed: true, job_id: job.id, account_id: job.accountId, account_revision: job.accountRevision, credential_ref: account.credentialRef, payload_hash: job.payloadHash, media_required: job.selectedVisuals.length > 0, authorization_snapshot: { ...serializedWorkerAuthorizationSnapshot(snapshot), resource_id: job.id }, authorization_recheck: authorizationRecheck }, null, req)
  }
  const publishMediaMatch = path.match(/^\/v1\/publish-jobs\/([^/]+)\/media$/)
  if (req.method === 'GET' && publishMediaMatch) {
    await requireWorkerCredentialAuthorization(req)
    const workspaceId = resolveWorkspace(req)
    const job = service.assertPublishExecutionAllowed({ workspaceId, publishJobId: publishMediaMatch[1]! })
    await assertCanonicalTaskScopeForAction(service.getTask(job.taskId))
    if (!job.selectedVisuals.length) return send(res, 200, workspaceId, { job_id: job.id, media: [] }, null, req)
    return send(res, 200, workspaceId, { job_id: job.id, media: await publishMediaPayload(workspaceId, job) }, null, req)
  }
  if (req.method === 'POST' && publishObservationMatch) {
    await requireWorkerAuthorization(req)
    const input = await body(req)
    const workspaceId = resolveWorkspace(req, input.workspace_id)
    const statusInput = input.status
    if (!statusInput || typeof statusInput !== 'object' || Array.isArray(statusInput)) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '缺少平台状态观测', 400)
    const status = statusInput as Record<string, unknown>
    const source = input.source === 'reconcile' ? 'reconcile' : 'publish'
    const state = status.state
    if (!['submitted', 'published', 'rejected', 'unknown'].includes(String(state))) throw new DomainError(ERROR_CODES.INVALID_REQUEST, '平台状态观测值无效', 400)
    const rejection = readPlatformRejection(status.platform_rejection)
    if (rejection && state !== 'rejected') throw new DomainError(ERROR_CODES.INVALID_REQUEST, '只有平台驳回状态可以携带拒绝详情', 400)
    const job = service.recordPublishObservation({
      workspaceId,
      publishJobId: publishObservationMatch[1]!,
      status: {
        found: status.found === true,
        state: state as 'submitted' | 'published' | 'rejected' | 'unknown',
        ...(typeof status.remote_id === 'string' ? { remoteId: status.remote_id } : {}),
        ...(typeof status.request_id === 'string' ? { requestId: status.request_id } : {}),
        ...(typeof status.simulated === 'boolean' ? { simulated: status.simulated } : {}),
        ...(rejection ? { rejection } : {}),
      },
      ...(typeof input.observed_at === 'string' ? { observedAt: input.observed_at } : {}),
    })
    let boundProduct
    if (job.remoteId) {
      boundProduct = service.bindProductRemoteId(workspaceId, job.taskId, job.remoteId)
      await persistSnapshot(workspaceId, 'product', boundProduct, boundProduct as unknown as Record<string, unknown>)
    }
    await persistSnapshot(workspaceId, 'task', service.getTask(job.taskId), service.getTask(job.taskId) as unknown as Record<string, unknown>)
    await persistSnapshot(workspaceId, 'content_version', service.getContentVersion(workspaceId, job.contentVersionId), service.getContentVersion(workspaceId, job.contentVersionId) as unknown as Record<string, unknown>)
    await persistSnapshot(workspaceId, 'publish_job', job, job as unknown as Record<string, unknown>)
    const knowledgeObservation = publishRejectionKnowledgeObservation(job)
    await persistEvent(workspaceId, job.id, 'publish.observation', job.revision, {
      job_id: job.id,
      task_id: job.taskId,
      source,
      status: job.remoteState ?? job.state,
      ...(job.remoteId ? { remote_id: job.remoteId } : {}),
      ...(job.requestId ? { request_id: job.requestId } : {}),
      ...(job.rejection ? { platform_rejection: { raw_code: job.rejection.rawCode, ...(job.rejection.message ? { message: job.rejection.message } : {}), fields: job.rejection.fields.map(field => ({ path: field.path, ...(field.rawCode ? { raw_code: field.rawCode } : {}), message: field.message })) } } : {}),
      ...(knowledgeObservation ? { knowledge_observation: knowledgeObservation } : {}),
    })
    if (job.remoteState === 'published' || job.remoteState === 'rejected') await releaseDistributedJobSlot(workspaceId, `publish:${job.idempotencyKey}`)
    if (source === 'publish' && job.remoteState === 'submitted') {
      if (requiresStrictAuth() && !job.authorizationSnapshot) throw new DomainError('AUTHZ_EXECUTION_SNAPSHOT_REQUIRED', '发布对账缺少原始发布授权快照，已拒绝入队', 503)
      const reconciliationAuthorization = job.authorizationSnapshot
        ? await deriveWorkerContinuationAuthorizationSnapshot(job.authorizationSnapshot, workspaceId, job.id, 'publish.reconcile', { event: 'publish.reconcile_requested', publish_job_id: job.id, payload_hash: job.payloadHash })
        : undefined
      await persistEvent(workspaceId, job.id, 'publish.reconcile_requested', 1, publishReconcileEventPayload(job, reconciliationAuthorization))
    }
    const automation = job.remoteState === 'published' && job.accountId
      ? await scanAutomationAfterOperationalCompletion(workspaceId, job.platform, job.accountId, `publish.observation.${source}.published`)
      : undefined
    return send(res, 200, workspaceId, { ...jobWithQueueMetadata(job, workspaceId, 'publish'), ...(automation ? { automation } : {}) }, null, req)
  }
  if (req.method === 'GET' && publishGetMatch) {
    const job = service.getPublishJob(publishGetMatch[1]!)
    const workspaceId = resolveWorkspace(req, job.workspaceId)
    await enforceTaskBrandAccess(req, service.getTask(job.taskId), 'viewer')
    return send(res, 200, workspaceId, { ...jobWithQueueMetadata(job, workspaceId, 'publish'), workflow: projectPublishWorkflow(workspaceId, job) }, null, req)
  }
  if (req.method === 'POST' && path === '/mcp') return routeMcp(req, res, mcpInputForHydration ?? await body(req, MCP_BODY_LIMIT))
  throw new DomainError(ERROR_CODES.NOT_FOUND, '路由不存在', 404)
}

function requestFailureMetadata(error: unknown) {
  if (isClientDisconnect(error)) return { status: 499, code: 'CLIENT_DISCONNECTED' }
  if (error instanceof OAuthStateError) { const mapped = oauthError(error); return { status: mapped.status, code: mapped.code } }
  if (error instanceof ConnectorFailure) return { status: error.normalized.code === 'UNAUTHORIZED' ? 401 : error.normalized.code === 'NOT_CONFIGURED' ? 503 : error.normalized.code === 'RATE_LIMITED' ? 429 : error.normalized.code === 'NOT_FOUND' ? 404 : 502, code: error.normalized.code }
  if (error instanceof ObjectStorageError) return { status: error.status, code: error.code }
  if (error instanceof ConnectorMappingPreflightError) return { status: 409, code: 'PLATFORM_MAPPING_PREFLIGHT_REQUIRED' }
  if (error instanceof SyncPaginationError && /mapping preflight/iu.test(error.message)) return { status: 409, code: 'PLATFORM_MAPPING_PREFLIGHT_REQUIRED' }
  if (error instanceof DomainError) return { status: error.status, code: error.code }
  return { status: 500, code: ERROR_CODES.INTERNAL_ERROR }
}

function publishCommitDomainError(error: unknown) {
  if (!(error instanceof PublishCommitStatusUnknownError)) return undefined
  return new DomainError(error.code, error.message, 503, { retryable: false, reconciliation_required: true, next_action: 'query_publish_job' })
}

function modelSettlementDomainError(error: unknown) {
  const code = (error as { code?: unknown })?.code
  if (code === 'MODEL_TASK_COST_ACTUAL_EXCEEDED' || code === 'MODEL_DAILY_COST_ACTUAL_EXCEEDED') return new DomainError(String(code), '模型供应商已完成调用，但实际成本超过安全上限；结果已进入费用核对，不会自动退款或重试', 409, { provider_succeeded: true, reconciliation_required: true })
  if (code === 'MODEL_USAGE_SETTLEMENT_PENDING' || code === 'MODEL_USAGE_COST_MISSING') return new DomainError(String(code), '模型供应商已完成调用，但本地用量结算尚未完成；为避免重复计费，当前结果已阻断且不会自动退款', 503, { provider_succeeded: true, reconciliation_required: true, ...((error as { receiptKey?: unknown }).receiptKey ? { receipt_key: String((error as { receiptKey: unknown }).receiptKey) } : {}) })
  return undefined
}

function isClientDisconnect(error: unknown) {
  if (!(error instanceof Error)) return false
  const code = 'code' in error && typeof error.code === 'string' ? error.code : undefined
  return error.message === 'aborted' || code === 'ECONNRESET' || code === 'ERR_STREAM_PREMATURE_CLOSE'
}

/**
 * A batch request is item-addressed.  Accepting the same task twice would
 * produce duplicate receipts and, depending on processing order, turn a
 * caller mistake into a partial write.  Reject the whole request before any
 * item is authorized, debited, or queued.
 */
function assertUniqueBatchTaskIds(confirmations: Array<Record<string, unknown>>) {
  const taskIds = confirmations.map(item => typeof item.task_id === 'string' ? item.task_id.trim() : '')
  const duplicates = [...new Set(taskIds.filter(Boolean).filter((taskId, index, all) => all.indexOf(taskId) !== index))]
  if (duplicates.length) throw new DomainError('PUBLISH_BATCH_DUPLICATE_TASK', `批量发布请求包含重复任务: ${duplicates.join(', ')}`, 400, { task_ids: duplicates })
}

const server = createServer((req, res) => {
  const startedAt = process.hrtime.bigint()
  beginRequestObservation(req)
  metricInFlight += 1
  res.once('finish', () => { observeHttpMetric(req, res, startedAt); completeRequestObservation(req, res) })
  route(req, res).catch(error => {
    const settlementError = modelSettlementDomainError(error)
    const publishError = publishCommitDomainError(error)
    const mappedError = settlementError ?? publishError
    const observedFailure = requestFailureMetadata(mappedError ?? error)
    if (!mappedError && !isClientDisconnect(error) && !(error instanceof DomainError) && !(error instanceof OAuthStateError) && !(error instanceof ConnectorFailure) && !(error instanceof ObjectStorageError) && !(error instanceof ConnectorMappingPreflightError) && !(error instanceof SyncPaginationError)) {
      const correlation = getRequestCorrelation(req)
      console.error(JSON.stringify({
        event: 'request.unhandled_error',
        request_id: correlation.requestId,
        trace_id: correlation.traceId,
        method: req.method,
        route: (req.url ?? '').split('?')[0],
        error_name: error instanceof Error ? error.name : typeof error,
        error_message: error instanceof Error ? error.message : String(error),
        error_stack: error instanceof Error ? error.stack : undefined,
      }))
    }
    const workspaceId = (() => { try { return resolveWorkspace(req) } catch { return isProduction() ? 'unknown' : 'ws_demo' } })()
    enrichRequestObservation(req, { workspaceId, actorId: trustedRequestObservationActor(req) })
    failRequestObservation(req, observedFailure.status, observedFailure.code)
    if (isClientDisconnect(error) && (res.destroyed || res.writableEnded)) return
    if (wantsOAuthHtml(req)) {
      const fallback = { status: 500, code: ERROR_CODES.INTERNAL_ERROR, message: '授权回调失败' }
      const mapped = error instanceof OAuthStateError
        ? oauthError(error)
        : error instanceof ConnectorFailure
          ? { status: error.normalized.code === 'UNAUTHORIZED' ? 401 : error.normalized.code === 'NOT_CONFIGURED' ? 503 : error.normalized.code === 'RATE_LIMITED' ? 429 : error.normalized.code === 'NOT_FOUND' ? 404 : 502, code: error.normalized.code, message: error.normalized.message }
          : error instanceof DomainError ? { status: error.status, code: error.code, message: error.message } : fallback
      return sendOAuthCallbackPage(res, mapped.status, { state: 'error', platform: (req.url ?? '').split('/').at(-1)?.split('?')[0] ?? 'store', code: mapped.code, message: 'message' in mapped ? mapped.message : error instanceof Error ? error.message : fallback.message }, req)
    }
    if (error instanceof OAuthStateError) {
      const mapped = oauthError(error)
      return fail(res, mapped.status, workspaceId, mapped.code, error.message, req)
    }
    if (error instanceof ConnectorFailure) {
      const status = error.normalized.code === 'UNAUTHORIZED' ? 401 : error.normalized.code === 'NOT_CONFIGURED' ? 503 : error.normalized.code === 'RATE_LIMITED' ? 429 : error.normalized.code === 'NOT_FOUND' ? 404 : 502
      return fail(res, status, workspaceId, error.normalized.code, error.normalized.message, req)
    }
    if (error instanceof ObjectStorageError) return fail(res, error.status, workspaceId, error.code, error.message, req)
    if (error instanceof ConnectorMappingPreflightError) return fail(res, 409, workspaceId, 'PLATFORM_MAPPING_PREFLIGHT_REQUIRED', '平台字段映射批准缺失、失效或与当前商品载荷不一致', req, { stage: error.stage, next_actions: ['platform.mapping.preflight'] })
    if (error instanceof SyncPaginationError && /mapping preflight/iu.test(error.message)) return fail(res, 409, workspaceId, 'PLATFORM_MAPPING_PREFLIGHT_REQUIRED', '平台字段映射批准缺失、失效或与当前商品载荷不一致', req, { stage: 'sync', next_actions: ['platform.mapping.preflight'] })
    const typed = error instanceof DomainError ? error : mappedError ?? new DomainError(ERROR_CODES.INTERNAL_ERROR, '内部错误', 500)
    const retryAfter = typeof typed.details?.retry_after_seconds === 'number' ? typed.details.retry_after_seconds : undefined
    if (retryAfter !== undefined) res.setHeader('retry-after', String(Math.max(1, Math.ceil(retryAfter))))
    return fail(res, typed.status, workspaceId, typed.code, typed.message, req, typed.details)
  })
})

if (process.env.NODE_ENV !== 'test') {
  persistenceReady.then(() => {
    const cleanupIntervalMs = Math.max(5_000, Number(process.env.ASSET_PROMOTION_CLEANUP_INTERVAL_MS ?? 15_000))
    const cleanupTimer = setInterval(() => { void drainPromotionCleanupTasks() }, cleanupIntervalMs)
    cleanupTimer.unref()
    server.once('close', () => clearInterval(cleanupTimer))
    void drainPromotionCleanupTasks()
    server.listen(port, () => console.log(`merchant API listening on http://127.0.0.1:${port} (${persistence.mode})`))
  }).catch(error => {
    console.error('merchant API startup failed: database migration/connection unavailable', error)
    // Do not leave a live Node process with no listener.  Orchestrators can
    // only recover this startup failure when the container actually exits;
    // otherwise the pod stays Running but can never answer its health probe.
    process.exit(1)
  })
}

export { assertUniqueBatchTaskIds, server, service, persistenceReady, memoryMembers as workspaceMembers, memoryOperations as operationAudits }
